---
phase: 02-image-transmission
plan: "03"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - include/sd_storage.h
  - src/sd_storage.cpp
  - include/image_rx_manager.h
  - src/image_rx_manager.cpp
  - include/command_sender.h
  - src/command_sender.cpp
  - src/main_basestation.cpp
  - include/base_station_config.h
  - platformio.ini
autonomous: true
requirements: [IMG-03, IMG-04, IMG-05, PRI-03]
user_setup:
  - service: microSD card module (base station hardware)
    why: "IMG-05 stores received images on the base station SD card; no SD wiring exists yet (research Open Question Q2)"
    hardware:
      - "Wire a microSD SPI breakout to the base ESP32-S3: MOSI to GPIO 11, MISO to GPIO 13, SCK to GPIO 12, CS to GPIO 10, VCC to 3V3, GND to GND. These are the constants SD_MOSI_PIN/SD_MISO_PIN/SD_SCK_PIN/SD_CS_PIN in include/base_station_config.h — if your wiring differs, edit the constants before flashing (no code change needed)."
      - "Format the card FAT32 (not exFAT) before use."
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The base pulls full images with windowed request/response ARQ: 16-chunk windows (IMAGE_WINDOW_REQUEST), missing/corrupt chunks re-requested before the window advances, progress exact by bitmap construction (D-21, IMG-04)"
    - "After IMG_RETRANSMIT_MAX_PASSES (3) passes over missing chunks the image is finalized incomplete: kept on SD, flagged in its sidecar metadata, transfer slot freed for the next queued image (D-24, PRI-03)"
    - "A completed image verifies against the manifest's end-to-end CRC32 (esp_rom_crc32_le over the stored bytes) before it is marked complete (D-23)"
    - "Images land on the SD card as IMG_{id}.JPG with sidecar IMG_{id}.JSON and thumbnails as IMG_{id}_T.JPG in a single flat /images directory, ids zero-padded to 5 (D-29, D-31, D-32)"
    - "The sidecar records capture time, receipt time, altitude and GPS position (latest beacon), trigger source, camera settings at capture, chunk statistics, completeness flag, and storage status (D-30; rssi is recorded as null with a documented reason — the E32 transparent mode exposes no per-packet RSSI, verified: no RSSI API anywhere in the radio driver)"
    - "A thumbnail that arrives with holes falls back to the same window-pull re-request path — one reliability mechanism system-wide, no parallel best-effort path (D-22)"
    - "The transfer panel shows percent and chunks received/total per image derived from real chunk accounting only — never a fabricated complete state (D-20)"
    - "SD-full or SD-init failure stops storing and surfaces a warning in /status and the UI; it never deletes or overwrites existing images (Q5 resolution)"
    - "FIFO pull order: one active full-image pull at a time, in manifest arrival order (D-19 base side)"
  artifacts:
    - path: "src/sd_storage.cpp"
      provides: "SD init on custom SPI pins, /images directory, openChunkWrite/seek+offset writeChunk per chunk, finalization with hand-built String JSON sidecar, stop-storing-and-warn disk-full policy, degrade-without-SD mode"
      contains: "SD.begin"
    - path: "src/image_rx_manager.cpp"
      provides: "Window state machine (advance/re-request/stall), per-image chunk bitmap, bounded pass counting, finalize (CRC verify + storage), thumbnail hole fallback, transfer progress snapshot API"
      contains: "IMG_WINDOW_MAX_CHUNKS"
    - path: "src/main_basestation.cpp"
      provides: "SD init beside initLoRa, GET /img/{id} full-image route, transfer progress panel (D-20 rows), storage status chip"
      contains: "chunksReceived"
  key_links:
    - from: "src/image_rx_manager.cpp process()"
      to: "src/command_sender.cpp sendCommand()"
      via: "window requests ride the Phase 1 tracked-command machinery: CmdSender().sendCommand(IMAGE_WINDOW_REQUEST, 5-byte payload) with the new CMD_ACK_TIMEOUT_WINDOW_MS class; ACK resolves the slot, duplicate/terminal guards apply unchanged; the base speaks ONLY when a window completed, stalled, or a manifest arrived while idle (Pitfall 5 half-duplex discipline — no free-running request timers)"
      pattern: "IMAGE_WINDOW_REQUEST"
    - from: "src/image_rx_manager.cpp chunk receive"
      to: "src/sd_storage.cpp writeChunk()"
      via: "each validated chunk seeks to chunkIndex * chunkSize and writes immediately — no whole-image RAM buffer (Pattern 5: manifest totalSize is untrusted; partial files are correct D-24 output)"
      pattern: "seek|writeChunk"
    - from: "src/main_basestation.cpp /status JSON"
      to: "src/image_rx_manager.cpp getTransferSnapshot()"
      via: "progress rows serialize the snapshot (imageId, kind, receivedChunks, totalChunks, percent, state) — caller fills an array of plain structs, module returns count, mirroring the getCommandQueue pattern"
      pattern: "getTransferSnapshot"
  prohibitions:
    - statement: "MUST NOT report or display an image as transferred, stored, or complete when it is not — progress and completeness derive only from real chunk-bitmap accounting and the verified CRC32 result (no fabricated state)"
      status: unverified
      flagged: true
      verification: judgment
    - statement: "MUST NOT silently delete or overwrite stored images to make SD space — disk-full stops storing and surfaces a warning; flight history is never silently recycled"
      status: unverified
      flagged: true
      verification: judgment
  assumptions:
    - "Probe edge IMG-05 unclassified/unresolved — flagged assumption: a microSD module is (or will be) wired to the proposed FSPI pins GPIO 11/13/12/10 (research A4); constants are editable in include/base_station_config.h and hardware confirmation is a UAT item"
    - "Probe edge IMG-03 unclassified/unresolved — flagged assumption: default QVGA/quality-10 captures (~8-15 KB) complete their background pull in 15-30 s (research A6); UXGA transfers take minutes, accepted under PRI-03"
    - "Probe edge PRI-03 unclassified/unresolved — flagged assumption: finalize-incomplete after 3 passes is acceptable graceful degradation; real-world loss rates on the link are a UAT measurement"
    - "D-30 deviation surfaced, not silently dropped: 'RSSI at receipt' is not hardware-obtainable (E32 transparent mode has no per-packet RSSI; verified no RSSI API in the radio driver) — the sidecar carries rssi: null with a documented reason"
---

<objective>
Base-side expansion of the receive path into the complete pull pipeline: windowed ARQ with exact chunk accounting, stream-to-SD reassembly with D-29..D-32 naming and sidecars, graceful D-24 degradation, and the D-20 transfer progress panel. Completes IMG-03/IMG-04/IMG-05 and the base half of PRI-03.

Purpose: The push path from 02-01 proved the bytes; this plan makes full-image transfer reliable, persistent, and honest — every progress state the operator sees is backed by real accounting, and a degraded link degrades the system instead of hanging it.
Output: New SdStorage module, ARQ-completing ImageRxManager, window-request issuing through the Phase 1 command machinery, full-image HTTP route, transfer progress UI.
</objective>

<execution_context>
@C:/Users/Amp/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Amp/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-image-transmission/02-CONTEXT.md
@.planning/phases/02-image-transmission/02-RESEARCH.md
@.planning/phases/02-image-transmission/02-PATTERNS.md
@.planning/phases/02-image-transmission/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SdStorage module — custom-SPI init, D-29/31/32 naming, seek+offset chunk writes, sidecar, stop-storing-and-warn</name>
  <reversibility rating="costly">The on-disk naming convention and sidecar format are the contract Phase 3's gallery reads; changing them after images accumulate means a migration over stored files.</reversibility>
  <read_first>
  - .planning/phases/02-image-transmission/02-RESEARCH.md (Code Examples: SD init with custom SPI pins, Pattern 5 stream-to-SD, Pitfall 8)
  - .planning/phases/02-image-transmission/02-PATTERNS.md (section "src/sd_storage.cpp" — No Analog Found guidance; singleton/loop/header conventions from auto_capture)
  - include/base_station_config.h (constants home; MAX_IMAGE_SIZE at ~line 50)
  - src/auto_capture.cpp ~8-15 (singleton accessor pattern to copy)
  - include/image_protocol.h (ImageKind, CaptureSource enums; IMG_* constants)
  </read_first>
  <files>include/base_station_config.h, include/sd_storage.h, src/sd_storage.cpp, platformio.ini</files>
  <action>
  Add SD pin constants to include/base_station_config.h: SD_SCK_PIN = 12, SD_MISO_PIN = 13, SD_MOSI_PIN = 11, SD_CS_PIN = 10 (ESP32-S3 DevKitC-1 FSPI-adjacent free GPIOs; the LoRa UART uses 14/48/19/20/21 and the LED 39 — no conflict; editable constants per the user_setup note).

  Create the module (singleton SDStorage(), banner + gated debug like every module). API in include/sd_storage.h:
  - begin(): construct a dedicated SPIClass(HSPI) instance, sdSPI.begin(SD_SCK_PIN, SD_MISO_PIN, SD_MOSI_PIN, SD_CS_PIN), then SD.begin(SD_CS_PIN, sdSPI) — the instance-overload form is mandatory for custom pins on arduino-esp32 (research Code Example; the default-bus overload ignores them). On failure set a degraded flag, log, and return true anyway UNCHANGED IN BEHAVIOR CONTRACT: the station runs without storage (degrade, never halt). SD.mkdir("/images") on success (flat layout, D-32).
  - isAvailable(): the honest availability flag (false until a successful begin, false after a mid-flight write failure).
  - openTransfer(imageId, totalSize): truncate-create /images/IMG_{id}.JPG (id %05u zero-padded — D-29) keeping the handle open for seek writes; same for thumbnails as IMG_{id}_T.JPG (D-31 — written the moment the thumbnail completes).
  - writeChunk(imageId, kind, chunkIndex, chunkSize, const uint8_t* data, len): file.seek(chunkIndex * chunkSize) then write — out-of-order arrival lands at the right offset (Pattern 5; no whole-image RAM buffer; untrusted totalSize never pre-allocated).
  - finalizeImage(metadata struct): close the file, then write the sidecar IMG_{id}.JSON ONCE at finalization (Pitfall 8 — not per chunk) as hand-built String JSON matching the main_basestation style (no ArduinoJson on the base env — research Supporting table). Sidecar fields per D-30: id, captureTimeMs (from manifest), receiptTimeMs (millis at finalize), triggerSource (string form of CaptureSource: manual/interval/event-altitude/event-distance/event-phase), altitudeM, lat, lon (latest beacon values), cameraSettings {resolution, quality, brightness, contrast, saturation, exposure, wbMode} (from the manifest settings trailer), chunksReceived, chunksTotal, bytesReceived, complete (CRC result), storedToSd. Include "rssi": null with a trailing comment in the source explaining the E32 transparent-mode limitation (D-30 deviation, surfaced).
  - Disk-full policy (Q5, stop-storing-and-warn): when SD.exists-based free-space heuristic fails, open() fails, or a write fails mid-transfer, set degraded, stop storing (subsequent transfers complete in-accounting but are not persisted), surface via a getStatus() the UI and /status consume. NO deletion of any existing file, ever.
  - serveFile(imageId, kind) helper or equivalent: opens the stored file for the HTTP route to stream (server.streamFile pattern) and reports absence honestly.

  platformio.ini: add -<sd_storage.cpp> to the balloon env's build_src_filter (base-only module, exclusion discipline).
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation && grep -q "SD.begin(SD_CS_PIN" src/sd_storage.cpp && grep -q "SPIClass" src/sd_storage.cpp && grep -q "IMG_%05u" src/sd_storage.cpp && grep -q "_T.JPG" src/sd_storage.cpp && ! grep -q "SD.remove" src/sd_storage.cpp && ! grep -q "rollover" src/sd_storage.cpp</automated>
  </verify>
  <acceptance_criteria>
  - include/base_station_config.h defines SD_SCK_PIN 12, SD_MISO_PIN 13, SD_MOSI_PIN 11, SD_CS_PIN 10
  - src/sd_storage.cpp begins the SPI instance with all four custom pins and passes it into SD.begin (the instance-overload form; the pin-less overload appears nowhere)
  - File names are produced with %05u zero-padding; thumbnails use the _T.JPG suffix; all files live under /images
  - writeChunk seeks to chunkIndex * chunkSize before every write (out-of-order safe, no pre-allocation from untrusted sizes)
  - The sidecar is written exactly once per image at finalization, as hand-built String JSON, containing every D-30 field plus "rssi": null
  - The module contains no file-removal or oldest-recycling call of any kind (gates: no SD.remove, no rollover logic) — disk-full degrades to stop-storing-and-warn
  - begin() failure leaves the station running with isAvailable() false (degraded, not halted)
  - platformio.ini balloon env excludes sd_storage.cpp; both targets build SUCCESS
  </acceptance_criteria>
  <done>SD persistence exists behind a module the rest of the system can query honestly: correct names, offset-safe chunk writes, one sidecar per image, and a disk-full policy that warns instead of destroying history (IMG-05 code path; D-29..D-32 encoded).</done>
</task>

<task type="auto">
  <name>Task 2: Window ARQ in ImageRxManager + window-request issuing through the Phase 1 command machinery (D-21/D-22/D-23/D-24)</name>
  <reversibility rating="costly">D-21's windowed ARQ is a wire-level contract shared by both firmwares (CONTEXT rates it costly); this task implements the base half.</reversibility>
  <read_first>
  - src/image_rx_manager.cpp + include/image_rx_manager.h (state after 02-01: thumbnail reassembly, presence dedupe, CRC completion)
  - src/command_sender.cpp + include/command_sender.h (sendCommand, ackTimeoutFor ~24-46, handleResponse terminal-state guard ~336-338, getCommandQueue snapshot pattern ~492-512)
  - include/image_protocol.h (PayloadImageWindowRequest, IMG_WINDOW_MAX_CHUNKS, IMG_WINDOW_STALL_MS, IMG_RETRANSMIT_MAX_PASSES)
  - include/command_protocol.h (CameraCommand::IMAGE_WINDOW_REQUEST, CMD_ACK_TIMEOUT_WINDOW_MS)
  - src/sd_storage.h (Task 1 API)
  - .planning/phases/02-image-transmission/02-RESEARCH.md (Patterns 3/5, Pitfalls 4/5/10/11)
  </read_first>
  <files>include/image_rx_manager.h, src/image_rx_manager.cpp, include/command_sender.h, src/command_sender.cpp</files>
  <action>
  Extend ImageRxManager into the full ARQ receiver:
  - Per-transfer state: manifest fields, presence bitmap (uint8_t array sized totalChunks — max 250 at the 50000/200 cap), receivedCount, active window {base, count}, per-image pass counter, lastProgressMs, file-write routing into SDStorage (thumbnail writes to the _T file as chunks arrive; full writes to the main file), and a FIFO of pending full-image pulls (manifest arrival order — D-19; one ACTIVE pull at a time).
  - On FULL_IMAGE manifest: validate (02-01 rules), open the SD transfer, enqueue the pull; if no pull is active, activate it and issue the first window request. The latest-thumbnail RAM retention from 02-01 stays (serving robustness when SD is degraded).
  - Window driver in process(): when the active window's bitmap slice is complete, advance — next window request, or finalize when all chunks present. Stall detection: lastProgressMs older than IMG_WINDOW_STALL_MS (8000) with an incomplete window or incomplete push (thumbnail case — D-22: a pushed thumbnail with holes is re-requested through the SAME window path; a thumbnail is just another pullable image, no parallel best-effort mechanism). On stall: re-request ONLY the missing indices of the current scope (the balloon's idempotent re-arm covers it), increment the image's pass counter.
  - D-24 bound: pass counter exceeding IMG_RETRANSMIT_MAX_PASSES (3) finalizes the image incomplete — close the file, keep it on SD, sidecar complete:false with chunk statistics, free the slot, activate the next queued pull. Never loops forever (PRI-03).
  - Finalize (complete path): compute the end-to-end CRC32 over the stored bytes by reading the file back (chunks arrive out of order, so the CRC cannot stream; 50 KB read-back is bounded) with esp_rom_crc32_le and compare against the manifest crc32 (D-23). Match: sidecar complete:true; mismatch: sidecar complete:false with a crcMismatch note; either way the slot frees and the FIFO advances.
  - Window-request issuing: build PayloadImageWindowRequest (imageId, startChunk, count big-endian; count <= IMG_WINDOW_MAX_CHUNKS) and send via CmdSender().sendCommand(CameraCommand::IMAGE_WINDOW_REQUEST, payload, 5). Only issue a request when: a manifest arrived while idle, a window completed, or a stall fired (Pitfall 5: the base never free-runs request timers during an active stream — the protocol self-serializes the half-duplex link).
  - Progress snapshot API getTransferSnapshot(TransferRow* rows, uint8_t maxRows) -> uint8_t: plain structs {imageId, kind, receivedChunks, totalChunks, state} with a LOCKED state vocabulary (QUEUED, RECEIVING, RETRYING, COMPLETE, INCOMPLETE) mirroring the Phase 1 locked-vocabulary discipline — one state-to-string mapping shared by every consumer (D-20). States derive from the bitmap and pass counter only.
  - src/command_sender.cpp: add the IMAGE_WINDOW_REQUEST case to ackTimeoutFor returning CMD_ACK_TIMEOUT_WINDOW_MS (Pitfall 4 — the 16-chunk answer stream outlives the 5 s SETTINGS window; the ACK itself arrives fast, the class bounds the whole exchange). No other sender changes: window ACKs resolve slots through the existing handleResponse machinery with its terminal-state and duplicate guards intact.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-basestation && pio run -e esp32-s3-balloon && grep -q "IMG_WINDOW_MAX_CHUNKS" src/image_rx_manager.cpp && grep -q "IMG_RETRANSMIT_MAX_PASSES" src/image_rx_manager.cpp && grep -q "IMAGE_WINDOW_REQUEST" src/image_rx_manager.cpp && grep -q "CMD_ACK_TIMEOUT_WINDOW_MS" src/command_sender.cpp && grep -q "esp_rom_crc32_le" src/image_rx_manager.cpp && node scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - Window advance happens only when every index of the current window is present in the bitmap; re-requests name only missing indices
  - A stalled stream (no progress for 8000 ms) triggers a re-request and increments the pass counter; 3 exceeded passes finalize incomplete and free the slot for the next queued pull
  - Finalize reads the file back and compares esp_rom_crc32_le against the manifest CRC before setting complete:true (mismatch is recorded, never shown as complete)
  - Window requests go through CmdSender().sendCommand with the 5-byte big-endian payload; ackTimeoutFor returns the 15000 ms class for IMAGE_WINDOW_REQUEST
  - The base issues requests only on manifest-while-idle, window-complete, or stall — no free-running timers
  - Thumbnail hole fallback uses the identical window path (no separate thumbnail retry mechanism exists in the codebase)
  - getTransferSnapshot exposes the five-state locked vocabulary computed from bitmap/pass data only
  - Both targets build SUCCESS; harness still exits 0
  </acceptance_criteria>
  <done>Full images are pulled reliably with exact accounting, verified end-to-end, persisted with metadata, and degraded gracefully after bounded retries (IMG-03, IMG-04, IMG-05 base halves; D-21..D-24 encoded).</done>
</task>

<task type="auto">
  <name>Task 3: Base UI — transfer progress panel (D-20), full-image route, storage status</name>
  <read_first>
  - src/main_basestation.cpp (after 02-01: /img/{id}_t route, latestThumbId, telemetry JSON; the D-16 Command Queue panel markup/JS as the direct analog; locked status vocabulary mapping pattern ~972-987; hand-built JSON ~1008-1084)
  - include/image_rx_manager.h (getTransferSnapshot, TransferRow, state vocabulary)
  - include/sd_storage.h (isAvailable/status)
  - .planning/phases/02-image-transmission/02-CONTEXT.md (D-20 exact wording: percent + chunks received/total, e.g. 143/380)
  </read_first>
  <files>src/main_basestation.cpp</files>
  <action>
  Extend the base web UI:
  - handleStatus additions: a transfers array serialized from getTransferSnapshot (id, kind, percent, chunks as received/total, state string from the single locked mapping) and a storage object {available, degradedReason} from SdStorage.
  - New route GET /img/{id} (no suffix): stream /images/IMG_{id}.JPG via SDStorage's file handle and server.streamFile with Content-Type image/jpeg; 404 when absent or SD degraded. Numeric-only id parsing (strtol), same discipline as the thumbnail route.
  - Transfer progress panel below the Command Queue panel, built on its pattern: one row per snapshot entry — image id, kind badge (THUMB/FULL), a percent bar (width = percent), the literal chunks counter "N/M" (received/total from the snapshot), and a state chip rendered from the same locked state vocabulary string the JSON uses. The panel shows an empty-state line when no transfers are live. The thumbnail image element from 02-01 upgrades: when a transfer row's thumbnail completes, its src points at /img/{id}_t; when the FULL row completes, an anchor to /img/{id} appears beside it.
  - Storage status chip in the status bar: computed truth from SdStorage (OK / UNAVAILABLE / FULL-or-write-failed), IN-03 discipline — never a hardcoded OK.
  - No-fabricated-state: every displayed percent, chunk count, and state comes from the snapshot JSON; nothing is optimistic, predicted, or defaulted to complete.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-basestation && grep -q "getTransferSnapshot" src/main_basestation.cpp && grep -q "storage" src/main_basestation.cpp && grep -q "chunks" src/main_basestation.cpp && grep -q "img/" src/main_basestation.cpp</automated>
  </verify>
  <acceptance_criteria>
  - /status contains the transfers array (id, kind, percent, chunks, state) and the storage object, both computed from module state at poll time
  - GET /img/{id} streams the stored JPEG or returns 404; id parsing is numeric-only with no string concatenation into file paths
  - The transfer panel renders one row per snapshot entry with percent, N/M chunks, and a state chip from the single locked vocabulary; empty state when idle
  - A completed FULL row exposes a link to /img/{id}; the thumbnail element points at the _t route
  - The storage chip reflects SdStorage's real availability/degradation (three distinct states reachable; no hardcoded value)
  - State strings rendered in the UI come from the transfers JSON; the module-side mapping is the single producer of vocabulary values, and the UI's only vocabulary use is presentation mapping (chip label/color) — it never invents or defaults a state
  - Base target builds SUCCESS
  </acceptance_criteria>
  <done>The operator sees honest, live transfer progress per image, can open completed full images, and sees storage health — D-20 delivered on the D-16 panel pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link → base firmware | Manifests/chunks are untrusted: sizes, counts, indices arrive from the shared channel |
| SD medium → HTTP client | Stored files are served to the open AP network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | DoS | ImageRxManager manifest/chunk validation | high | mitigate | MAX_IMAGE_SIZE 50000 cap, chunkSize <= 200, totalChunks == ceil(totalSize/chunkSize), chunkIndex < totalChunks, dataLen bounds — a pathological manifest cannot pre-allocate or seek past bounds (Pitfall 11); D-24 bounds total passes so a loss-flooding attacker cannot hold the slot forever |
| T-02-08 | Tampering | file naming from imageId | medium | mitigate | Names built only from %05u-formatted numeric ids parsed with strtol; no network-supplied strings enter any file path |
| T-02-09 | Information disclosure | /img routes on the open AP | medium | accept | Unauthenticated image serving on the open network — same exposure class as the existing Phase 1 routes; carried on the WR-08 ledger for /gsd-secure-phase (ASVS L1: no new auth surface is required this phase, none was removed) |
| T-02-10 | DoS | SD writes vs. web loop | low | mitigate | Chunk-sized writes as they arrive, sidecar written once at finalization (Pitfall 8); no whole-file buffering |
</threat_model>

<verification>
- pio run -e esp32-s3-balloon -e esp32-s3-basestation reports SUCCESS for both targets
- node scripts/verify_protocol_roundtrip.mjs still exits 0
- All Task acceptance-criteria greps pass (SD pin-instance form, %05u naming, no file removal anywhere, window/pass constants, timeout class, snapshot wiring)
- Hardware UAT (deferred): a full image completes and opens via /img/{id}; pulling the card shows IMG_{id}.JPG/_T.JPG/.JSON; inducing link loss mid-transfer finalizes incomplete after 3 passes with the slot freed; SD absence leaves the station serving RAM thumbnails with an honest storage chip
</verification>

<success_criteria>
- Full-image pulls complete with verified integrity and land on SD with complete metadata (IMG-03, IMG-05)
- Chunk reliability is windowed, idempotent, and bounded (IMG-04, PRI-03, D-21..D-24)
- Transfer UI shows only bitmap-backed truth (D-20)
- SD-full and SD-absent degrade safely with warnings, never deletion (Q5)
</success_criteria>

## Artifacts this phase produces (plan 02-03)

- New module SdStorage (SDStorage()) with begin/isAvailable/openTransfer/writeChunk/finalizeImage/status/serveFile surface; SD pin constants SD_SCK_PIN/SD_MISO_PIN/SD_MOSI_PIN/SD_CS_PIN in include/base_station_config.h
- ImageRxManager additions: per-transfer bitmap/window/pass state, FIFO pull queue, getTransferSnapshot(TransferRow*, uint8_t) with the QUEUED/RECEIVING/RETRYING/COMPLETE/INCOMPLETE vocabulary, thumbnail hole fallback, finalize-with-read-back-CRC
- CommandSender.ackTimeoutFor case for IMAGE_WINDOW_REQUEST (CMD_ACK_TIMEOUT_WINDOW_MS)
- HTTP route GET /img/{id}; /status additions transfers[] and storage{}
- Sidecar JSON schema (D-30 field set) as the Phase 3 gallery contract

<output>
Create `.planning/phases/02-image-transmission/02-03-SUMMARY.md` when done
</output>
