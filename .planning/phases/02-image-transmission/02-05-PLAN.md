---
phase: 02-image-transmission
plan: "05"
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - include/image_protocol.h
  - include/image_tx_manager.h
  - src/image_tx_manager.cpp
  - src/command_handler.cpp
  - src/image_rx_manager.cpp
  - include/sd_storage.h
  - src/sd_storage.cpp
  - scripts/verify_protocol_roundtrip.mjs
autonomous: true
gap_closure: true
requirements: [IMG-02, IMG-03, IMG-04, IMG-05, PRI-01, PRI-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Gap 1 (SC-3 / IMG-03 / D-19): a full-image pull survives its documented operating point — any accepted chunk resets the transfer's pass counter (D-24 then bounds only CONSECUTIVE unhealed stalls), the balloon services an armed window within IMG_WINDOW_SERVICE_PREEMPT_MS (5000 ms, strictly below the base's IMG_WINDOW_STALL_MS 8000 ms) even while push work is pending, and queue overflow evicts an entry of the active-pull class only when no parked/queued/unarmed entry exists"
    - "Gap 2 (D-22 / IMG-02 / IMG-04): a thumbnail that arrives with holes heals through the same windowed-pull mechanism — PayloadImageWindowRequest is 6 bytes carrying imageKind (imageId BE16, imageKind u8, startChunk BE16, count u8), the balloon arms and serves THUMBNAIL windows from thumbBuffer (including THUMB_PUSHED oversize parks whose full never announces), and the base issues a thumbnail heal only when no full pull is active AND the same-id FULL slot exists (manifest received proves the push finished; oversize fallback after IMG_THUMB_HEAL_IDLE_MS 24000 ms so no thumbnail row can stick at RECEIVING forever)"
    - "Gap 2 eviction safety (D-19): a THUMBNAIL window request never evicts balloon queue entries — implicit FIFO eviction (evictEntriesOlderThan) runs only for FULL_IMAGE window requests, so a thumbnail heal can never free the active pull's target mid-stream"
    - "Gap 2 routing (D-20): while a thumbnail heal window is armed on the base (thumbnail slot windowActive), chunks for that image id route to the THUMBNAIL slot — thumbnail-window bytes never land in a QUEUED full's SD file"
    - "Gap 3 (D-20): a slot-pressure eviction returns a fully reset slot — a manifest arriving under slot pressure starts at 0/N with fresh accounting, never the previous occupant's terminal flag, receivedCount, bytesReceived, or percent"
    - "Gap 4 (D-30 / SC-4 / IMG-05): thumbnail and full sidecars persist as separate files — IMG_{id}_T.JSON and IMG_{id}.JSON — so the full's finalization no longer truncates the thumbnail's D-30 record; the _T sidecar convention mirrors D-31's IMG_{id}_T.JPG and is the Phase 3 gallery contract"
    - "PRI-01 non-regression: the beacon early-return still precedes all chunk work in ImageTxManager::process() and command responses still outrank both by main_balloon loop order — the new window preemption lives inside the chunk branch (pushPending) only"
    - "Transfer-panel presentation contract (UI-SPEC): healing is displayed as RETRYING within the locked five-state vocabulary QUEUED/RECEIVING/RETRYING/COMPLETE/INCOMPLETE — gap closure adds no new display state; INCOMPLETE rows keep truthful partial percent/chunks and are never rounded up to COMPLETE; a row links to bytes only when COMPLETE"
    - "Both firmware targets build SUCCESS and the wire harness exits 0 with the 6-byte window-request contract locked as an executable clause"
  artifacts:
    - path: "include/image_protocol.h"
      provides: "imageKind field in PayloadImageWindowRequest (6-byte payload), IMG_WINDOW_SERVICE_PREEMPT_MS (5000), IMG_THUMB_HEAL_IDLE_MS (24000)"
      contains: "imageKind"
    - path: "src/image_tx_manager.cpp"
      provides: "Kind-aware handleWindowRequest (thumbnail arming from thumbBuffer, eviction only on FULL requests), windowKind-aware serviceWindowChunk, push preemption for starved armed windows, SERVED completion state, windowEverArmed flag, class-ranked overflow eviction"
      contains: "windowKind"
    - path: "src/image_rx_manager.cpp"
      provides: "passCount reset on accepted-chunk progress, 6-byte kind-carrying issueWindowRequest, gated thumbnail heal (no-active-pull + full-manifest-arrived + oversize idle fallback), heal-window chunk routing, allocateSlot step-3 full slot reset"
      contains: "passCount = 0"
    - path: "src/sd_storage.cpp"
      provides: "kind-suffixed sidecarPath — IMG_{id}_T.JSON for thumbnails, IMG_{id}.JSON for fulls"
      contains: "_T.JSON"
    - path: "scripts/verify_protocol_roundtrip.mjs"
      provides: "6-byte encodeWindowRequest/decodeWindowRequest codecs with imageKind at offset 2 and the updated (img-f) clause"
  key_links:
    - from: "src/image_rx_manager.cpp issueWindowRequest()"
      to: "src/image_tx_manager.cpp handleWindowRequest()"
      via: "the 6-byte PayloadImageWindowRequest (imageId BE16 @0, imageKind @2, startChunk BE16 @3, count @5) — base encodes t.imageKind, balloon decodes payload[2] and validates it against the ImageKind enum before any buffer match; the harness codecs mirror the exact layout"
      pattern: "imageKind"
    - from: "src/image_tx_manager.cpp pushPending() preemption"
      to: "findWindowServiceEntry()"
      via: "before any push work, an armed window whose entry lastActivityMs is older than IMG_WINDOW_SERVICE_PREEMPT_MS consumes the pass's single transmit — the base's 8000 ms stall can never trip while the balloon holds an armed window"
      pattern: "IMG_WINDOW_SERVICE_PREEMPT_MS"
    - from: "src/sd_storage.cpp writeSidecar()"
      to: "SdStorage::sidecarPath(out, cap, id, kind)"
      via: "sidecar name derives from meta.kind — thumbnail and full finalizations write distinct files, so neither truncates the other (WR-05)"
      pattern: "_T\\.JSON"
  prohibitions:
    - statement: "MUST NOT let image chunk traffic (push work, window service, or the new preemption) delay or displace the telemetry beacon or pending command responses at a transmit opportunity — the preemption lives inside the chunk branch, after the beacon early-return (PRI-01, carried from 02-02)"
      status: unverified
      flagged: true
      verification: judgment
    - statement: "MUST NOT report or display an image as transferred, stored, or complete when it is not — the passCount reset must not fabricate progress (only accepted-chunk bytes reset passes), and the reset slot must start from zeroed accounting (D-20, carried from 02-03; the CR-02 zombie was a live violation of this prohibition)"
      status: unverified
      flagged: true
      verification: judgment
    - statement: "MUST NOT let a new capture (its push work or its queue slot) abandon an in-progress pull — overflow eviction treats the previously-armed ANNOUNCED class as the last resort, and a THUMBNAIL window request never evicts anything (D-19)"
      status: unverified
      flagged: true
      verification: judgment
    - statement: "MUST NOT add a parallel best-effort thumbnail recovery path — the heal rides the same windowed ARQ machinery with the same D-24 3-pass bound; only accepted-chunk progress may reset the counter, so consecutive unhealed stalls still finalize INCOMPLETE (D-22/D-24/PRI-03)"
      status: unverified
      flagged: true
      verification: judgment
    - statement: "MUST NOT introduce any SD file-removal or recycling call — the sidecar naming change is additive; existing images and sidecars are never deleted or overwritten across kinds (carried from 02-03)"
      status: unverified
      flagged: true
      verification: judgment
  assumptions:
    - "Probe edge IMG-03/IMG-04 unclassified/unresolved — flagged assumption: the 5000 ms preemption bound and the whole completion argument rest on research A1/A3/A6 (9.6 kbps air rate, ~210-280 ms/packet, QQVGA 2.5-4 KB, QVGA 8-15 KB); hardware UAT must confirm fulls complete under the D-28 20 s cadence. The structural facts the fixes address (never-reset passes, unbounded push priority, drop-oldest of the pull target) hold regardless of the exact air rate"
    - "Probe edge IMG-02 unclassified/unresolved — flagged assumption: a healed thumbnail's end-to-end RETRYING-to-COMPLETE timing is a hardware-UAT measurement (real chunk loss required); the code path is proven by build + harness only"
    - "Probe edge PRI-01 unclassified/unresolved — flagged assumption: non-regression is code-proven (branch order); runtime beacon/chunk interleaving over the half-duplex link stays a UAT item"
    - "IMG-05 (SD wiring), CTRL-05 (live GPS), and PRI-03 (real loss rates) coverage rows remain owned and carried by plans 02-03/02-04 — this plan touches no camera, GPS, or SD-mount surface beyond the sidecar name"
    - "Oversize thumbnail heal fallback (IMG_THUMB_HEAL_IDLE_MS 24000 ms) is a planner extension of the verifier's prescribed two-gate rule: without it, an oversize image's thumbnail (which never gets a FULL manifest) could sit at RECEIVING forever with no terminal resolution. 24 s = 3x the stall window, beyond any plausible queued-push wait; it bounds the row to the honest D-24 INCOMPLETE path instead of an infinite stall"
---

<objective>
Gap-closure plan for the 4 gaps in 02-VERIFICATION.md (status gaps_found, 18/27). All four are policy/accounting defects in the transfer state machines — the architecture (Phase 1 transport, framing, module boundaries) is sound and untouched:

- Gap 1 (CR-03, Blocker, SC-3/IMG-03): full pulls structurally cannot complete — passCount never resets on progress (src/image_rx_manager.cpp:125,:175); pushPending gives push work unbounded priority over window service (src/image_tx_manager.cpp:354-385); overflow drop-oldest frees the active pull target and finished entries are never freed (src/image_tx_manager.cpp:281-295).
- Gap 2 (CR-01, Blocker, D-22/IMG-02/IMG-04): thumbnail hole recovery cannot work — PayloadImageWindowRequest has no image-kind field (include/image_protocol.h:146-150); handleWindowRequest serves only ANNOUNCED fullBuffer (src/image_tx_manager.cpp:533,:546,:604-609); evictEntriesOlderThan can free the active pull's target on a misdirected re-request (:561); the base stall loop issues requests without checking an active pull (src/image_rx_manager.cpp:157-189).
- Gap 3 (CR-02, Blocker, D-20): allocateSlot step 3 evicts a non-terminal slot without the full reset step 2 performs (src/image_rx_manager.cpp:328-341) — zombie slot, fabricated accounting.
- Gap 4 (WR-05, Warning, D-30/SC-4): sidecarPath ignores kind (src/sd_storage.cpp:93-95) — thumbnail and full sidecars collide on IMG_{id:05d}.JSON and the full's write destroys the thumbnail's record.

Purpose: make the transfer machinery survive its own documented operating point (D-28 20 s cadence, research A1/A3/A6 airtime) so IMG-03/IMG-04's "reliable/background completion" halves hold, restore D-22 as a real single reliability mechanism, restore D-20 truthful accounting under slot pressure, and stop the systematic destruction of thumbnail metadata — all without regressing PRI-01 (beacon early-return + loop-order arbitration stay exactly as verified).
Output: One amended wire contract (kind-addressable window requests, harness-locked), retuned balloon TX policy (bounded interleaving, completion-aware eviction), corrected base RX accounting/gating/routing, kind-suffixed sidecars.
</objective>

<execution_context>
@C:/Users/Amp/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Amp/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-image-transmission/02-CONTEXT.md
@.planning/phases/02-image-transmission/02-RESEARCH.md
@.planning/phases/02-image-transmission/02-VERIFICATION.md
@.planning/phases/02-image-transmission/02-REVIEW.md
@.planning/phases/02-image-transmission/02-UI-SPEC.md
@.planning/phases/02-image-transmission/02-02-SUMMARY.md
@.planning/phases/02-image-transmission/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire contract — kind-addressable window requests + balloon thumbnail servicing (Gap 2 balloon half, D-22)</name>
  <reversibility rating="costly">PayloadImageWindowRequest is a wire-level contract shared by both firmwares and mirrored in the harness (D-21 is CONTEXT-rated costly for the same reason); both sides plus the harness must change together in this task.</reversibility>
  <read_first>
  - include/image_protocol.h (PayloadImageWindowRequest at lines 144-150; constants block lines 54-87)
  - src/image_tx_manager.cpp (handleWindowRequest lines 517-591, serviceWindowChunk lines 593-631, findWindowServiceEntry lines 335-352, evictEntriesOlderThan lines 637-645)
  - include/image_tx_manager.h (ImageTxEntry struct lines 77-108, ImageTxEntryState lines 41-48, WindowRequestResult lines 55-60)
  - src/command_handler.cpp (handleImageWindowRequest lines 679-730 — the payloadLength check at 683 and the ACK echo memcpy at 702)
  - src/command_sender.cpp (handleResponse terminal-state guard region ~319-367 — confirms the ACK echo length is carried, not parsed per-field; no sender change needed)
  - scripts/verify_protocol_roundtrip.mjs (encodeWindowRequest/decodeWindowRequest lines 664-673, the img-f clause at 930-945)
  - .planning/phases/02-image-transmission/02-REVIEW.md (CR-01 fix proposal — the struct field order is prescribed there)
  </read_first>
  <files>include/image_protocol.h, include/image_tx_manager.h, src/image_tx_manager.cpp, src/command_handler.cpp, scripts/verify_protocol_roundtrip.mjs</files>
  <behavior>
  - Harness test 1: encodeWindowRequest({imageId: 42, imageKind: 1, startChunk: 7, count: 16}) returns a 6-byte Buffer whose bytes are 00 2A 01 00 07 10 (imageId BE16, imageKind u8, startChunk BE16, count u8)
  - Harness test 2: decodeWindowRequest round-trips boundary values — imageId 0 and 65535, imageKind 0 (THUMBNAIL) and 1 (FULL_IMAGE), count 1 and 16 — symmetrically
  - Harness test 3: the img-f clause asserts encodeWindowRequest(...).length equals 6 and its message text names 6 bytes; suite exits 0
  </behavior>
  <action>
  Wire contract (per review CR-01 fix):
  - include/image_protocol.h: extend PayloadImageWindowRequest with a uint8_t imageKind field placed BETWEEN imageId and startChunk (final order: imageId BE16, imageKind u8, startChunk BE16, count u8 — 6 bytes total); update the struct's size comment. Add two constants with load-bearing comments: IMG_WINDOW_SERVICE_PREEMPT_MS = 5000 (used by Task 2; strictly less than IMG_WINDOW_STALL_MS 8000 so an armed window can never trip the base's stall while the balloon holds it) and IMG_THUMB_HEAL_IDLE_MS = 24000 (used by Task 3; oversize thumbnails never receive a FULL manifest, so this idle bound is the only proof their push finished).
  - scripts/verify_protocol_roundtrip.mjs: update encodeWindowRequest to allocate 6 bytes (imageKind at offset 2, startChunk moved to offset 3, count at offset 5) and decodeWindowRequest to read the same layout; update the img-f clause to assert length 6 and to exercise both kinds; keep the boundary values style of the existing clause.

  Balloon servicing (kind-aware, D-22):
  - include/image_tx_manager.h: add uint8_t windowKind to ImageTxEntry (the ImageKind the armed window serves); note in the struct comment that the window context is kind-parameterized.
  - src/image_tx_manager.cpp handleWindowRequest: change the length guard from len smaller than 5 to len smaller than 6; decode imageId (readUint16 at 0), imageKind (payload[2]), startChunk (readUint16 at 3), count (payload[5]). Reject any imageKind value outside the ImageKind enum (not 0 and not 1) with INVALID_RANGE before any entry matching. Split target matching by kind: FULL_IMAGE requests keep matching used entries in state ANNOUNCED with validation against fullTotalChunks (existing behavior); THUMBNAIL requests match used entries with the same imageId whose thumbBuffer is non-null and whose state is NOT PUSH_THUMB_MANIFEST and NOT PUSH_THUMB_CHUNKS (push provably finished — this admits THUMB_PUSHED oversize parks, ANNOUNCED entries, and later the SERVED state from Task 2), validating startChunk and count against thumbTotalChunks instead. On duplicate matches keep the earliest-enqueueSeq preference. CRITICAL (Gap 2 eviction safety): call evictEntriesOlderThan ONLY on the FULL_IMAGE request path — a THUMBNAIL request must never evict anything (the verifier proved :561 can free the active pull's target on a misdirected re-request). Set entry.windowKind at arming alongside the existing windowStart/windowCount/windowNextIndex assignments; arming stays idempotent (re-arm re-points the cursor).
  - src/image_tx_manager.cpp serviceWindowChunk: select the source buffer and length by entry.windowKind — thumbBuffer/thumbLength for THUMBNAIL windows, fullBuffer/fullLength otherwise; the offset math (windowNextIndex times IMG_CHUNK_PAYLOAD_SIZE, tail clamp by remaining bytes) is unchanged and now reads the selected buffer. Keep the one-transmit-per-pass and window-complete clear behavior.
  - src/image_tx_manager.cpp findWindowServiceEntry: admit an armed THUMBNAIL window on a THUMB_PUSHED entry in addition to ANNOUNCED entries — condition becomes state equals ANNOUNCED, or state equals THUMB_PUSHED with windowArmed set.
  - src/command_handler.cpp handleImageWindowRequest: change the payload guard from payloadLength smaller than 5 to smaller than 6, and the ACK echo from copying 5 payload bytes to copying 6 with result.responseLength 6; update the echo comment to name imageId BE16, imageKind u8, startChunk BE16, count u8.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation && node scripts/verify_protocol_roundtrip.mjs && grep -q "imageKind" include/image_protocol.h && grep -q "payload\[2\]" src/image_tx_manager.cpp && grep -q "windowKind" src/image_tx_manager.cpp && grep -q "cmd.payloadLength < 6" src/command_handler.cpp && grep -q "memcpy(result.responseData, cmd.payload, 6)" src/command_handler.cpp && grep -q "6 bytes" scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - PayloadImageWindowRequest in include/image_protocol.h contains the imageKind field between imageId and startChunk and its comment names the 6-byte layout
  - encodeWindowRequest emits exactly 6 bytes with imageKind at offset 2; decodeWindowRequest mirrors it; the img-f clause message names 6 bytes; harness exits 0
  - handleWindowRequest rejects imageKind values outside the ImageKind enum with INVALID_RANGE and rejects payloads shorter than 6 bytes
  - A THUMBNAIL window request arms against thumbBuffer with startChunk/count validated against thumbTotalChunks for any entry whose push finished (THUMB_PUSHED included); a FULL_IMAGE request behaves exactly as before against ANNOUNCED entries
  - evictEntriesOlderThan is reachable only from the FULL_IMAGE request path — no THUMBNAIL request path calls it
  - serviceWindowChunk slices thumbBuffer for THUMBNAIL windows and fullBuffer otherwise; findWindowServiceEntry admits armed THUMB_PUSHED entries
  - The command handler ACK echo copies 6 bytes and sets responseLength to 6
  - Both firmware targets build SUCCESS
  </acceptance_criteria>
  <done>The window-request wire contract is kind-addressable and harness-locked, and the balloon can actually serve thumbnail bytes for heal windows (D-22 becomes implementable end-to-end; Gap 2 balloon half closed).</done>
</task>

<task type="auto">
  <name>Task 2: Balloon TX policy — bounded push/window interleaving + completion-aware eviction (Gap 1 balloon half, D-19/D-24)</name>
  <read_first>
  - src/image_tx_manager.cpp (process lines 73-109 — the beacon early-return; pushPending lines 354-385; enqueueCapture overflow block lines 273-296; serviceWindowChunk window-complete branch lines 624-629; handleWindowRequest as rewritten by Task 1)
  - include/image_tx_manager.h (ImageTxEntryState lines 41-48, ImageTxEntry lines 77-108)
  - .planning/phases/02-image-transmission/02-REVIEW.md (CR-03 fix items (a) and (c))
  - .planning/phases/02-image-transmission/02-VERIFICATION.md (Gap 1 reason — the starvation arithmetic and the drop-oldest failure chain)
  </read_first>
  <files>include/image_tx_manager.h, src/image_tx_manager.cpp</files>
  <action>
  Push preemption (CR-03 fix a — bound push-induced starvation):
  - At the TOP of pushPending(), before the findActiveEntry() push-work block: look up the armed window via findWindowServiceEntry(); when it exists and millis() minus its entry.lastActivityMs exceeds IMG_WINDOW_SERVICE_PREEMPT_MS, call serviceWindowChunk on it and return (one transmit per pass). Rationale comment: the armed entry's lastActivityMs advances only via its own transmits and re-arms, so a neighbor capture's push naturally ages it; preempting at 5000 ms keeps the base's 8000 ms stall clock (which resets on every accepted chunk) from ever tripping while a window is armed. Beacon non-regression: this preemption lives inside the chunk branch — the beacon early-return in process() still runs first; do not touch process() ordering.

  Completion marker (SERVED) and re-arm:
  - Add SERVED to ImageTxEntryState with a comment: the FULL window whose clamped span reached fullTotalChunks has been fully served — the entry keeps both buffers (tail-chunk loss heals must still be servable) but is a preferred eviction candidate.
  - In serviceWindowChunk's window-complete branch: when windowKind is FULL_IMAGE and windowStart plus windowCount reaches fullTotalChunks, set entry.state to SERVED (in addition to clearing windowArmed).
  - In handleWindowRequest's FULL_IMAGE arming path: a SERVED target transitions back to ANNOUNCED when armed (a tail re-request re-opens service); THUMBNAIL windows never change entry state.

  Overflow eviction by class (CR-03 fix c — never free the active pull target first):
  - Add bool windowEverArmed to ImageTxEntry; set it true in the arming code path (both kinds).
  - Replace enqueueCapture's unconditional drop-oldest overflow block with a class-ranked eviction: pick the oldest (lowest enqueueSeq) entry within the FIRST non-empty class, in this order — class 1 THUMB_PUSHED (parked, nothing left to deliver); class 2 SERVED (full delivered through its tail; heal-only); class 3 ANNOUNCED with windowEverArmed false (queued, no airtime invested); class 4 PUSH_THUMB_MANIFEST or PUSH_THUMB_CHUNKS (partially delivered push); class 5 ANNOUNCED with windowEverArmed true (the active pull context, armed or between windows — LAST resort). Log every overflow eviction naming the class and both image ids; the class-5 log line must name it as the active-pull class explicitly. freeEntry stays the freeing mechanism (this also implements entries freed on completion via class 1/2 preference; superseding newer-ID FULL requests and the 15-minute TTL sweep remain the other freeing paths, unchanged).
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation && node scripts/verify_protocol_roundtrip.mjs && grep -q "IMG_WINDOW_SERVICE_PREEMPT_MS" src/image_tx_manager.cpp && grep -q "SERVED" include/image_tx_manager.h && grep -q "windowEverArmed" src/image_tx_manager.cpp && node -e "const s=require('fs').readFileSync('src/image_tx_manager.cpp','utf8');const p=s.indexOf('void ImageTxManager::pushPending');const e=s.indexOf('bool ImageTxManager::pushThumbManifest');const body=s.slice(p,e);if(!(body.indexOf('IMG_WINDOW_SERVICE_PREEMPT_MS')>=0&&body.indexOf('IMG_WINDOW_SERVICE_PREEMPT_MS')<body.indexOf('findActiveEntry()'))){process.exit(1)};const pr=s.indexOf('void ImageTxManager::process');const pe=s.indexOf('return; // one transmit per pass',pr);if(!(pr>=0&&pe>pr&&s.indexOf('pushPending()',pe)>pe)){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
  - pushPending() checks the armed-window preemption BEFORE the findActiveEntry() push-work block (node order-gate passes); the preempt bound constant IMG_WINDOW_SERVICE_PREEMPT_MS is used in the comparison
  - The beacon early-return in process() still precedes the pushPending() call (node order-gate passes) — PRI-01 arbitration untouched
  - ImageTxEntryState contains SERVED; serviceWindowChunk sets it only when a FULL window's served span reaches fullTotalChunks; arming a SERVED entry re-enters ANNOUNCED
  - ImageTxEntry contains windowEverArmed, set true on every arming
  - enqueueCapture's overflow block evicts by the 5-class ranking with the oldest-within-class rule, and logs the class name and image ids on every eviction; the class-5 log names the active-pull class
  - Both firmware targets build SUCCESS; harness still exits 0
  </acceptance_criteria>
  <done>Push work can no longer starve an armed window past the base's stall bound, and queue overflow cannot free the active pull's entry while any finished/parked/queued entry exists (Gap 1 balloon half closed; D-19 preserved under pressure).</done>
</task>

<task type="auto">
  <name>Task 3: Base RX — pass reset, gated thumbnail heal, heal routing, slot-pressure reset, kind-suffixed sidecars (Gaps 1-RX, 2-base, 3, 4; D-20/D-22/D-24/D-30)</name>
  <read_first>
  - src/image_rx_manager.cpp (process pull driver lines 95-152 and thumbnail loop lines 154-190; acceptChunk commit block lines 526-549; onChunkFrame routing lines 216-244; allocateSlot lines 298-347; issueWindowRequest lines 722-760; findTransfer/findActivePull lines 278-296)
  - include/image_rx_manager.h (ImageRxTransfer fields: passCount, lastProgressMs, windowActive, pullActive, terminal — lines 56-98)
  - src/sd_storage.cpp (sidecarPath lines 93-95, writeSidecar lines 246-254 and following)
  - include/sd_storage.h (sidecarPath declaration line 126 and the finalizeImage comment lines 100-103)
  - .planning/phases/02-image-transmission/02-REVIEW.md (CR-02 fix, CR-03 fix (b), WR-05 fix — the kind-suffix option)
  - .planning/phases/02-image-transmission/02-UI-SPEC.md (Color truth requirements; the two gap-closure backstop truths under UI Considerations)
  </read_first>
  <files>src/image_rx_manager.cpp, include/sd_storage.h, src/sd_storage.cpp</files>
  <action>
  Pass accounting (CR-03 fix b — D-24 bounds only consecutive unhealed stalls):
  - In acceptChunk's commit block, beside the receivedCount increment: reset the transfer's pass counter to zero (t.passCount = 0) with a comment stating that accepted-chunk progress retires charged passes — a healed stall is not a failed pass, so three cumulative healed stalls over a pull's lifetime can no longer finalize it INCOMPLETE.

  Kind-carrying requests:
  - issueWindowRequest: change the payload buffer and sendCommand length from 5 to 6 and encode t.imageKind at offset 2 (imageId BE16 at 0, imageKind u8 at 2, startChunk BE16 at 3, count u8 at 5 — exactly the Task 1 contract); update the layout comment.

  Gated thumbnail heal (CR-01 base half):
  - In the process() thumbnail stall loop, before any window request is issued for a stalled non-terminal THUMBNAIL slot, add two gates: gate 1 — skip while findActivePull() returns non-null (the module's speak-only-on-three-triggers discipline; a full pull owns the link); gate 2 — require a same-id FULL slot to exist (findTransfer of that imageId with kind FULL_IMAGE, terminal or not — its manifest's arrival proves the balloon finished the thumbnail push, because the push always precedes the announcement). Fallback for the no-full-slot case (oversize images never announce a full; without this the row could sit at RECEIVING forever): allow the heal only after the slot's lastProgressMs is older than IMG_THUMB_HEAL_IDLE_MS, else skip. The existing 3-pass bound then resolves the row honestly.

  Heal-window chunk routing (Gap 2 — thumbnail bytes must never corrupt a QUEUED full's file):
  - In onChunkFrame, before the existing FULL-first precedence: when a THUMBNAIL slot for this imageId exists, is non-terminal, and has windowActive set (a heal window is in flight), route the chunk to that THUMBNAIL slot directly; otherwise keep the existing precedence (non-terminal FULL slot first, then THUMBNAIL). Comment: the base never runs a full pull and a thumbnail heal simultaneously (gate 1), so the armed heal window unambiguously owns this id's incoming chunks — the chunk body carries no kind byte, so the requester's own state is the discriminator.

  Slot-pressure reset (CR-02 fix, Gap 3):
  - In allocateSlot step 3, immediately after the finalizeIncomplete(*oldest, "slot pressure") call, add a full slot re-initialization assigning ImageRxTransfer{} through the pointer (finalizeIncomplete already released the slot's work memory; the assignment clears the stale terminal flag and counters exactly as step 2 does before returning the slot).

  Kind-suffixed sidecars (WR-05 fix, Gap 4):
  - sd_storage: give sidecarPath a kind parameter — THUMBNAIL produces /images/IMG_%05u_T.JSON, anything else /images/IMG_%05u.JSON; update the declaration in include/sd_storage.h (and the finalizeImage comment that names the sidecar file) and the single caller writeSidecar to pass meta.kind. Both D-30 records now persist independently; add a comment noting the _T sidecar convention mirrors D-31's IMG_{id}_T.JPG and is the Phase 3 gallery contract.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation && node scripts/verify_protocol_roundtrip.mjs && grep -q "passCount = 0" src/image_rx_manager.cpp && grep -A3 'finalizeIncomplete(\*oldest, "slot pressure")' src/image_rx_manager.cpp | grep -q "ImageRxTransfer{}" && grep -q "IMG_THUMB_HEAL_IDLE_MS" src/image_rx_manager.cpp && grep -q "payload, 6" src/image_rx_manager.cpp && grep -q "IMG_%05u_T.JSON" src/sd_storage.cpp && grep -q "sidecarPath" include/sd_storage.h</automated>
  </verify>
  <acceptance_criteria>
  - acceptChunk resets the owning transfer's pass counter to zero on every newly committed chunk (the only writer of zero; all other passCount sites remain increments or comparisons)
  - issueWindowRequest builds and sends the 6-byte kind-carrying payload (imageKind at offset 2); the balloon's Task 1 decoder accepts it
  - The thumbnail stall loop issues a heal request only when no full pull is active AND the same-id FULL slot exists, or (oversize fallback) after IMG_THUMB_HEAL_IDLE_MS of no progress
  - onChunkFrame routes chunks to an armed heal window's THUMBNAIL slot before the FULL-slot precedence; the FULL-first precedence is otherwise unchanged
  - allocateSlot step 3 fully resets the evicted slot (grep window after the slot-pressure finalizeIncomplete contains the ImageRxTransfer{} assignment) — a slot-pressure manifest starts at 0/N with a fresh terminal flag
  - sidecarPath takes kind and emits IMG_{id}_T.JSON for thumbnails; writeSidecar passes meta.kind; the full's finalization no longer opens the thumbnail's sidecar path
  - Both firmware targets build SUCCESS; harness exits 0
  </acceptance_criteria>
  <done>All four base-side truths hold: pulls survive healed stalls, thumbnail heals fire only when the link discipline allows and land in the right slot, slot pressure yields clean accounting, and both D-30 sidecar records survive (Gaps 1-RX, 2-base, 3, 4 closed).</done>
</task>

</tasks>

<assumption_delta_decision>
noun: none promoted — no core identity changed
decision: no-change
rationale: the detector fired on the time unit "second" inside SC-5's "5-second rate" (already dispositioned no-change in 02-02) and on "configurable" inside the D-26 thresholds description (a locked user decision from CONTEXT, not a derived-to-chosen transition introduced now). This gap-closure plan changes transfer policy and one payload layout; it adds no second instance of any core entity — one balloon, one base, one image-ID sequence, one reliability mechanism (D-22 reaffirmed by the kind-addressable heal).
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link → balloon firmware | Window-request payloads (now 6 bytes) are untrusted input: imageId, imageKind, startChunk, count arrive from the shared channel |
| RF link → base firmware | Manifest/chunk frames remain untrusted; chunk routing now consults local heal-window state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Tampering/DoS | ImageTxManager::handleWindowRequest (imageKind byte) | high | mitigate | The new imageKind byte is validated against the ImageKind enum (0/1 only) BEFORE any entry matching or buffer access; the 6-byte length bound is enforced before decode; per-kind startChunk/count are validated against that kind's totalChunks before arming — extends the existing T-02-04 validation, so a crafted request cannot address an unowned buffer |
| T-02-12 | DoS | thumbnail heal arming on parked entries | medium | mitigate | One armed window system-wide (BUSY result otherwise), count capped at IMG_WINDOW_MAX_CHUNKS, one transmit per process pass, and the preemption bound — repeated heal requests cannot monopolize the radio or hold a window open unboundedly |
| T-02-13 | DoS | enqueueCapture overflow eviction | low | accept | The class-5 last-resort eviction of an active-pull entry remains reachable only when all 3 queue slots are pull-context or mid-push entries; bounded queue depth 3 by construction, every eviction is logged naming its class — accepted residual, recoverable by the base's bounded retry/stall machinery |
</threat_model>

<verification>
- pio run -e esp32-s3-balloon -e esp32-s3-basestation reports SUCCESS for both targets
- node scripts/verify_protocol_roundtrip.mjs exits 0 with the 6-byte window-request clause (the harness is the executable wire spec — a 5-byte regression fails it)
- All task acceptance-criteria greps and node order-gates pass: passCount zero-writer present; slot-pressure reset present; preemption precedes push work inside pushPending; beacon early-return still precedes pushPending in process() (PRI-01 non-regression)
- Structural re-checks mirroring the verifier's own method: no evictEntriesOlderThan call reachable from the THUMBNAIL request path; acceptChunk remains the only passCount reset site; serviceWindowChunk selects its source buffer by windowKind
- Hardware UAT (carried, unchanged from 02-VERIFICATION.md plus one addition): full-image pulls complete under the 20 s capture cadence (validates Gap 1 fixes against A1/A3/A6 airtime reality); a link-loss-induced thumbnail hole heals through RETRYING to COMPLETE (validates Gap 2 end-to-end); telemetry age stays within ~10 s during transfers (PRI-01); SD card shows IMG_{id}.JPG, IMG_{id}_T.JPG, IMG_{id}.JSON AND IMG_{id}_T.JSON after a capture whose both kinds finalized (validates Gap 4)
</verification>

<success_criteria>
- Gap 1 closed: no path exists by which push work, a queued capture, or queue overflow kills or false-finalizes an in-progress pull (SC-3/IMG-03/D-19/D-24)
- Gap 2 closed: a thumbnail with holes is recoverable through the same windowed ARQ as fulls, gated to the link discipline, and can never evict or corrupt the active pull (D-22/IMG-02/IMG-04)
- Gap 3 closed: slot-pressure manifests start from zeroed accounting (D-20)
- Gap 4 closed: thumbnail and full D-30 sidecars persist independently (SC-4/IMG-05)
- PRI-01 arbitration untouched and provably ordered; both targets green; harness green
</success_criteria>

## Artifacts this phase produces (plan 02-05)

- PayloadImageWindowRequest.imageKind field (6-byte wire payload; order imageId BE16, imageKind u8, startChunk BE16, count u8)
- New constants in include/image_protocol.h: IMG_WINDOW_SERVICE_PREEMPT_MS (5000), IMG_THUMB_HEAL_IDLE_MS (24000)
- ImageTxEntryState::SERVED (new state value); ImageTxEntry.windowKind and ImageTxEntry.windowEverArmed fields
- Amended behavior (no signature change): ImageTxManager::handleWindowRequest (kind-aware decode/match/serve, eviction only on FULL requests), serviceWindowChunk (kind-selected source buffer, SERVED transition), findWindowServiceEntry (armed THUMB_PUSHED admitted), pushPending (armed-window preemption), enqueueCapture (class-ranked overflow eviction)
- ImageRxManager: acceptChunk pass-reset, issueWindowRequest 6-byte payload, gated thumbnail heal loop, onChunkFrame heal-window routing, allocateSlot step-3 reset (no new public symbols)
- SdStorage::sidecarPath signature change: sidecarPath(char*, size_t, uint16_t imageId, uint8_t kind); new thumbnail sidecar file name IMG_{id}_T.JSON
- Harness: encodeWindowRequest/decodeWindowRequest 6-byte codecs and the amended (img-f) clause

<output>
Create `.planning/phases/02-image-transmission/02-05-SUMMARY.md` when done
</output>
