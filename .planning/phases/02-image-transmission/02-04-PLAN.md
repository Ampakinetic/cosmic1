---
phase: 02-image-transmission
plan: "04"
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - include/auto_capture.h
  - src/auto_capture.cpp
  - src/command_handler.cpp
  - src/main_basestation.cpp
autonomous: true
requirements: [CTRL-05]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A capture fires when GPS altitude moves at least the configured altitude delta from the last-event baseline, or horizontal distance moves at least the distance delta, or the flight-phase machine first enters LAUNCH, APEX, PARACHUTE_DESCENT, or LANDING (D-25; phase subset per research Q3 — the wire enum's 8 values are authoritative, CONTEXT's 6-state description is descriptive)"
    - "Trigger thresholds are configurable from the base station web UI via SET_EVENT_THRESHOLDS with ACK and are reported back through GET_STATUS (D-26, Phase 1 settings pattern)"
    - "An event capture resets the interval baseline, and both modes coexist without double-capture bursts at the interval deadline (D-27); every capture shares AutoCapture's single uint16 image-ID sequence (CR-05 invariant preserved)"
    - "A global minimum spacing (default 20 s, configurable) gates ALL automatic captures — interval and event alike — so GPS jitter or fast phase changes cannot machine-gun the camera or flood the transfer queue (D-28)"
    - "AUTO_CAPTURE_DISABLE remains the master off-switch: no event fires while auto-capture is disabled (Phase 1 SC-5 / T-01-14 semantics preserved — event triggers live inside AutoCapture, never beside it)"
    - "Distance and altitude evaluation require a valid GPS fix; an invalid fix produces no event (Pitfall 12)"
  artifacts:
    - path: "src/auto_capture.cpp"
      provides: "Event-trigger evaluation in process(): altitude/distance deltas from baselines, flight-phase first-entry detection with a seen-mask, min-spacing gate across all automatic sources, interval baseline reset on event fire, threshold bounds re-validation in-module"
      contains: "EVENT"
    - path: "src/command_handler.cpp"
      provides: "SET_EVENT_THRESHOLDS case (7-byte payload, big-endian, bounds-validated) delegating to AutoCapture, and GET_STATUS populating the extended ResponseStatusData event fields"
      contains: "SET_EVENT_THRESHOLDS"
    - path: "src/main_basestation.cpp"
      provides: "Event Capture settings card (altitude delta m, distance delta m, min spacing s, enable switch) with WR-07-style full-range validation, plus current values displayed from GET_STATUS"
      contains: "set-event-thresholds"
  key_links:
    - from: "src/auto_capture.cpp event fire path"
      to: "src/camera_manager.cpp setLastCaptureSource()"
      via: "each event path stamps its CaptureSource (EVENT_ALTITUDE/EVENT_DISTANCE/EVENT_PHASE) before captureImage() so the manifest and sidecar carry the true trigger (D-30 trigger source)"
      pattern: "setLastCaptureSource"
    - from: "src/main_basestation.cpp /set-event-thresholds route"
      to: "src/command_handler.cpp SET_EVENT_THRESHOLDS case"
      via: "route validates the full long range before truncation (WR-07 pattern), builds the 7-byte big-endian payload, sends via CmdSender().sendCommand — ACK/NACK and the queue panel behave exactly like the Phase 1 settings forms"
      pattern: "SET_EVENT_THRESHOLDS"
    - from: "src/auto_capture.cpp process()"
      to: "src/system_state.cpp getFlightPhase()"
      via: "phase-trigger evaluation reads the flight-phase machine directly (transition cost: one enum compare against a seen bitmask); the machine is active by default (flightModeEnabled = true)"
      pattern: "getFlightPhase"
  prohibitions:
    - statement: "MUST NOT fire event-triggered captures while auto-capture is disabled — AUTO_CAPTURE_DISABLE stays the master off-switch for every automatic capture source"
      status: unverified
      flagged: true
      verification: judgment
  assumptions:
    - "Probe edge CTRL-05 unclassified/unresolved — flagged assumption: GPS altitude/distance deltas at the 150 m / 500 m defaults and the flight-phase machine's transitions are meaningful trigger signals on real flights (research sketch constants); hardware UAT with live GPS validates both the deltas and that D-28 spacing holds under jitter"
    - "TinyGPSPlus::distanceBetween(lat1, lon1, lat2, lon2) static helper assumed available (research A2); if the signature is absent at implementation, fall back to a local haversine — either way the distance baseline math stays inside auto_capture.cpp"
---

<objective>
Balloon-side capture intelligence: extend the AutoCapture module (the sole automatic-capture authority and image-ID owner) with event-based triggers — altitude delta, horizontal distance delta, and flight-phase first-entry — plus their UI-configurable thresholds. Completes CTRL-05 and the last Phase 2 requirement.

Purpose: Project.md's hybrid auto-capture means interval AND event modes coexist through one authority; extending AutoCapture (not adding a parallel timer) preserves the Phase 1 CR-05 single-authority invariant and its verification gates.
Output: Event engine in auto_capture, SET_EVENT_THRESHOLDS command handling with GET_STATUS reporting, base UI threshold card.
</objective>

<execution_context>
@C:/Users/Amp/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Amp/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-image-transmission/02-CONTEXT.md
@.planning/phases/02-image-transmission/02-RESEARCH.md
@.planning/phases/02-image-transmission/02-PATTERNS.md
@.planning/phases/02-image-transmission/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Event-trigger engine inside AutoCapture (D-25/D-27/D-28)</name>
  <read_first>
  - include/auto_capture.h + src/auto_capture.cpp (whole file — interval authority, allocateImageId, the in-module bounds re-validation style at ~53-56, the process() millis idiom at ~86-106)
  - src/system_state.h (FlightPhase enum — 8 values, ~29-38; getFlightPhase)
  - src/main_balloon.cpp ~639 (Sensors().getGPSData() feeding pattern; gpsData fields: altitude/lat/lon/valid)
  - src/camera_manager.h (setLastCaptureSource/getLastCaptureSource from 02-01)
  - include/image_protocol.h (CaptureSource enum values EVENT_ALTITUDE/EVENT_DISTANCE/EVENT_PHASE)
  - .planning/phases/02-image-transmission/02-RESEARCH.md (event-trigger evaluation sketch, Pitfall 12, Q3 phase subset)
  </read_first>
  <files>include/auto_capture.h, src/auto_capture.cpp</files>
  <action>
  Extend AutoCapture (no new module, no parallel timer — the CR-05 single-authority invariant is load-bearing):
  - State in include/auto_capture.h: eventConfig {altDeltaM, distDeltaM, minSpacingS, eventsEnabled} with flight defaults EVENT_DEFAULT_ALT_DELTA_M = 150, EVENT_DEFAULT_DIST_DELTA_M = 500, EVENT_DEFAULT_MIN_SPACING_S = 20 (D-28 band midpoint, research-proposed); baselines lastEventAltM/lastEventLat/lastEventLon + initialized flag; a uint8_t phaseSeenMask (one bit per FlightPhase value, 8 max); lastBeacon-independent event baseline tracking. Public API: setEventConfig(altDeltaM, distDeltaM, minSpacingS, eventsEnabled) that RE-VALIDATES bounds in-module (alt 10..5000 m, dist 10..50000 m, spacing 5..3600 s — the handler validation pattern mirrored, so a bypassed handler cannot set absurd values), getEventConfig() for GET_STATUS.
  - process() gains an event-evaluation stage that runs ONLY when the master enable is on (enabled == true — AUTO_CAPTURE_DISABLE stops ALL automatic capture: interval AND events; the master off-switch semantics from Phase 1 SC-5 are non-negotiable) and eventsEnabled is set:
    - Altitude: GPS fix valid AND baseline initialized AND |altitude - lastEventAltM| >= altDeltaM → fire(EVENT_ALTITUDE) and reset lastEventAltM to the current altitude (hysteresis: movement counts only beyond threshold from the last FIRED baseline).
    - Distance: GPS fix valid AND baseline initialized AND distance between the baseline lat/lon and current lat/lon >= distDeltaM → fire(EVENT_DISTANCE) and reset the position baseline. Use the TinyGPSPlus distanceBetween static helper if its signature matches (research A2); otherwise a local haversine in meters inside this file. On fire, also refresh the altitude baseline (one movement event, one coherent baseline reset).
    - Phase: getFlightPhase() compared against phaseSeenMask — a phase value not yet seen sets its bit; if that phase is one of LAUNCH, APEX, PARACHUTE_DESCENT, LANDING (the four operationally meaningful moments, research Q3 — GROUND/RECOVERY and the two ascent sub-phases stay silent; steady ascent is the altitude trigger's job), fire(EVENT_PHASE). Non-triggering phases still set their seen bit (first-entry semantics, once per boot).
    - Baseline initialization: on the first valid GPS fix, seed baselines without firing.
  - fire(source): the single gate — if millis() - lastCaptureTime < minSpacingS * 1000, skip. A skipped DELTA event does not update its baseline (the exceeded delta re-arms and fires as soon as spacing allows); a skipped PHASE event DOES set its seen bit (the moment is passed — do not queue stale intent). When the gate passes: stamp Camera().setLastCaptureSource(source value), call camera->captureImage(), update lastCaptureTime (D-27: the event resets the interval baseline — the next interval capture counts a full interval from the event), and record the image ID via allocateImageId() exactly as the interval path does. The min-spacing check must ALSO gate the interval branch (D-28: "between any two automatic captures regardless of trigger source") — restructure the interval condition to require both interval elapsed AND spacing satisfied, keeping the baseline-advance-before-attempt discipline from Phase 1 (T-01-09) intact.
  - Keep the wraparound-safe millis comparisons and the guard-clause style of the existing process().
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation && grep -q "EVENT_ALTITUDE" src/auto_capture.cpp && grep -q "PARACHUTE_DESCENT" src/auto_capture.cpp && grep -q "setLastCaptureSource" src/auto_capture.cpp && grep -q "minSpacing" src/auto_capture.cpp && grep -q "allocateImageId" src/auto_capture.cpp && ! grep -q "Timer<" src/auto_capture.cpp</automated>
  </verify>
  <acceptance_criteria>
  - Event evaluation executes only when enabled AND eventsEnabled are both true (master disable stops all automatic capture)
  - Altitude/distance evaluation requires a valid GPS fix; the first valid fix seeds baselines without firing
  - Fire semantics: source stamped via setLastCaptureSource before captureImage; lastCaptureTime updated (interval baseline reset per D-27); ID allocated through the existing allocateImageId — the single sequence
  - The min-spacing gate wraps BOTH the interval branch and every event branch; a skipped delta event leaves its baseline unchanged, a skipped phase event still marks the phase seen
  - The four triggering phases are LAUNCH, APEX, PARACHUTE_DESCENT, LANDING; all eight phases set their seen bits exactly once per boot
  - setEventConfig re-validates the 10..5000 / 10..50000 / 5..3600 bounds in-module
  - No second timer abstraction is introduced (gate: no Timer template in the file); AutoCapture remains the only automatic capture path
  - Both targets build SUCCESS
  </acceptance_criteria>
  <done>Captures now fire on altitude change, position change, and the four meaningful flight-phase moments — through the existing single authority, with global spacing and honest baselines (CTRL-05 balloon side complete).</done>
</task>

<task type="auto">
  <name>Task 2: SET_EVENT_THRESHOLDS command + GET_STATUS reporting + base UI threshold card (D-26)</name>
  <read_first>
  - src/command_handler.cpp (settings-command case pattern — handleAutoCaptureEnable or a SET_* handler; the 02-02 IMAGE_WINDOW_REQUEST case; GET_STATUS handler populating ResponseStatusData)
  - include/command_protocol.h (ResponseStatusData extended event fields from 02-01; PayloadSetEventThresholds layout in include/image_protocol.h)
  - src/main_basestation.cpp (Phase 1 settings-form pattern: handleAutoCaptureEnable ~917-951 with WR-07 full-long range validation before truncation; form HTML + card layout; status display of GET_STATUS values)
  - include/auto_capture.h (Task 1 getEventConfig)
  </read_first>
  <files>src/command_handler.cpp, src/main_basestation.cpp</files>
  <action>
  Balloon (src/command_handler.cpp):
  - Add case CameraCommand::SET_EVENT_THRESHOLDS: decode the 7-byte payload big-endian (altDeltaM u16, distDeltaM u16, minSpacingSec u16, flags u8 via readUint16 — Pitfall 9), validate the same bounds the module enforces (reject out-of-range with NACK_PARAM through the existing result mapping), call AutoCap().setEventConfig(...), ACK on success. Idempotent: re-sending the same config just re-sets it.
  - GET_STATUS: populate the extended ResponseStatusData event fields (eventThresholdAltM, eventThresholdDistM, eventMinSpacingSec, eventFlags with bit0 = eventsEnabled) from AutoCap().getEventConfig() alongside the existing fields — the struct travels as before (memcpy'd little-endian island, size unchanged at 28).

  Base (src/main_basestation.cpp):
  - Route POST /set-event-thresholds (mirroring the auto-capture route shape): read altitude-m, distance-m, spacing-s, enabled args; validate each as long against the FULL range (10..5000, 10..50000, 5..3600; enabled 0/1) BEFORE narrowing to the wire width (WR-07 lesson — range-check the long, then truncate); on validation failure return 400 with the offending field named; on success build the 7-byte big-endian payload (writeUint16 x3 + flags byte) and send via CmdSender().sendCommand(SET_EVENT_THRESHOLDS, payload, 7); reply 200 with the tracked sequence like the Phase 1 forms so the command lands in the D-16 queue panel with its normal ACK/retry semantics.
  - Event Capture settings card following the existing card pattern: three numeric inputs (altitude delta m, distance delta m, min spacing s) with min/max/step hints matching the validated ranges, an enable toggle, and a Save button posting the route. Current values displayed from the GET_STATUS poll (extend the existing status rendering — values shown are the balloon-reported truth, not the last-submitted form).
  - The card disables its inputs while a SET_EVENT_THRESHOLDS command is in flight, same as the Phase 1 forms.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation && grep -q "SET_EVENT_THRESHOLDS" src/command_handler.cpp && grep -q "set-event-thresholds" src/main_basestation.cpp && grep -q "eventThresholdAltM" src/command_handler.cpp && node scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - The balloon handler decodes all fields big-endian, validates bounds, and rejects out-of-range with NACK_PARAM; ACK flows through createResponsePacket
  - GET_STATUS response data carries the four live event fields from AutoCap().getEventConfig()
  - /set-event-thresholds validates every field as long against the full range before building the 7-byte payload; 400 names the offending field; 200 returns the tracked sequence
  - The settings card displays balloon-reported current values and disables during an in-flight command
  - Both targets build SUCCESS; harness still exits 0
  </acceptance_criteria>
  <done>Thresholds round-trip base UI → command protocol → balloon module → GET_STATUS → UI display with ACK/retry semantics identical to the Phase 1 settings (D-26 complete; CTRL-05 closed at code level).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link → balloon firmware | SET_EVENT_THRESHOLDS payloads are untrusted input on the shared channel |
| HTTP client → base routes | /set-event-thresholds accepts numeric input on the open AP network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Tampering/DoS | SET_EVENT_THRESHOLDS handler | medium | mitigate | Double validation (handler bounds check + in-module re-validation in setEventConfig) — a crafted command cannot set spacing below 5 s or deltas outside sane flight ranges, so the camera cannot be machine-gunned via the command path even by a spoofed frame |
| T-02-12 | DoS | event engine under GPS jitter | medium | mitigate | D-28 min spacing gates every automatic source; delta hysteresis (baseline resets only on fire); GPS-valid requirement before evaluation (Pitfall 12) |
| T-02-13 | Tampering | /set-event-thresholds route | low | mitigate | Full-range long validation before narrowing (WR-07) — no truncation surprise, no negative/overflow values reach the wire |
</threat_model>

<verification>
- pio run -e esp32-s3-balloon -e esp32-s3-basestation reports SUCCESS for both targets
- node scripts/verify_protocol_roundtrip.mjs still exits 0 (threshold payload codec covered by 02-01 clause f)
- All Task acceptance-criteria greps pass (event sources, phase constants, source stamping, spacing gate, handler + route wiring)
- Hardware UAT (deferred): drive altitude/distance deltas with live GPS and observe event-stamped captures (trigger source visible in the transfer panel/sidecar); fire AUTO_CAPTURE_DISABLE and confirm no event fires; confirm an event near the interval deadline produces exactly one capture
</verification>

<success_criteria>
- Event triggers fire on all three D-25 signal families with configurable thresholds (CTRL-05)
- Single-authority and master-off-switch invariants hold (CR-05/SC-5 preserved)
- Threshold UX matches the Phase 1 settings pattern end to end (D-26)
- Both targets build; harness green
</success_criteria>

## Artifacts this phase produces (plan 02-04)

- AutoCapture event API: setEventConfig/getEventConfig, EVENT_DEFAULT_ALT_DELTA_M/EVENT_DEFAULT_DIST_DELTA_M/EVENT_DEFAULT_MIN_SPACING_S constants, phaseSeenMask first-entry tracking, fire() source-stamped capture path
- CommandHandler case SET_EVENT_THRESHOLDS (bounds-validated, NACK_PARAM on range violation) and GET_STATUS population of the four event fields
- Base route POST /set-event-thresholds and the Event Capture settings card

<output>
Create `.planning/phases/02-image-transmission/02-04-SUMMARY.md` when done
</output>
