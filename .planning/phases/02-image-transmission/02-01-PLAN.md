---
phase: 02-image-transmission
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - include/image_protocol.h
  - include/command_protocol.h
  - src/command_protocol.cpp
  - src/camera_manager.cpp
  - src/camera_manager.h
  - include/image_tx_manager.h
  - src/image_tx_manager.cpp
  - include/image_rx_manager.h
  - src/image_rx_manager.cpp
  - src/main_balloon.cpp
  - src/main_basestation.cpp
  - src/command_sender.cpp
  - src/command_handler.cpp
  - scripts/verify_protocol_roundtrip.mjs
  - platformio.ini
autonomous: true
requirements: [IMG-01, IMG-02, IMG-04]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A real JPEG thumbnail produced by the reworked createThumbnail is serialized into 0x12 manifest + 0x13 chunk packets whose length-driven framing survives embedded 0x0D 0x0A byte pairs in the payload data, proven end-to-end by the host harness (IMG-04 chunk path, SC-1)"
    - "createThumbnail no longer leaves currentThumbnail.buffer dangling on ANY failure path — every early return nulls the member and clears valid (CR-04 closed before its first Phase 2 caller)"
    - "Both receivers branch on the packet-type byte at buffer[2] BEFORE body arithmetic and discard frames of unknown type: a CRC-valid RESPONSE heard by the balloon can no longer execute as a command, and the base no longer parses manifests/chunks with response arithmetic (WR-12 closed)"
    - "After any capture (manual CAPTURE_NOW or interval), the balloon pushes the thumbnail — manifest(kind=thumbnail) followed by chunks paced one per loop pass — with no operator action and no round trip (D-17 push half, IMG-01)"
    - "The base station accumulates pushed thumbnail chunks, verifies the end-to-end CRC32 (esp_rom_crc32_le) against the manifest at completion, and serves the finished thumbnail at GET /img/{id}_t.jpg (IMG-02 code path; the 10-second window is a hardware-UAT item)"
    - "The base web UI displays the latest completed capture's thumbnail (latestThumbId from /status) (IMG-02)"
    - "Both firmware targets build green with the new modules correctly excluded from the opposite env (build-filter discipline)"
  artifacts:
    - path: "include/image_protocol.h"
      provides: "The phase's complete wire contract: 0x12/0x13/0x14 packet-type constants, ImageManifestBody/ImageChunkBody/TelemetryBeaconBody/payload structs, chunk/window/queue/round constants"
      contains: "PACKET_TYPE_IMAGE_MANIFEST"
    - path: "src/command_protocol.cpp"
      provides: "serialize/deserialize for manifest, chunk, and telemetry-beacon packets plus createManifestPacket/createChunkPacket factories that assign packet.type as the first field"
      contains: "createManifestPacket"
    - path: "src/camera_manager.cpp"
      provides: "CR-04/WR-11 fix: createThumbnail captures the QQVGA frame buffer first, then allocates exactly fb->len; every early return nulls thumbnail.buffer; the pre-capture size estimate is gone from this path"
    - path: "src/image_tx_manager.cpp"
      provides: "Balloon transfer module (push-only in this plan): detects new image IDs from AutoCap().getLastImageId(), calls captureThumbnail, takes PSRAM ownership of full+thumbnail buffers, pushes thumbnail chunks one per loop pass"
      contains: "ImageTx()"
    - path: "src/image_rx_manager.cpp"
      provides: "Base reassembly module (thumbnail-only in this plan): manifest validation, RAM accumulation with presence dedupe, CRC32 completion check, latest-thumbnail retention"
      contains: "ImageRx()"
    - path: "scripts/verify_protocol_roundtrip.mjs"
      provides: "Extended wire-format harness: manifest/chunk/beacon round-trips, oversize rejection, embedded end-marker framing, type-byte rejection clauses, factory type-assignment teeth"
  key_links:
    - from: "src/image_tx_manager.cpp process()"
      to: "src/auto_capture.cpp getLastImageId()"
      via: "polls AutoCap().getLastImageId() each loop pass — the single image-ID authority (CR-05 invariant); a changed ID means a capture completed and its buffers must be taken over before the next capture frees them"
      pattern: "AutoCap\\(\\)\\.getLastImageId\\(\\)"
    - from: "src/command_protocol.cpp createManifestPacket/createChunkPacket"
      to: "src/command_sender.cpp + src/command_handler.cpp receive dispatch"
      via: "factories assign packet.type first-field (0x12/0x13) exactly as the Phase 1 CR-01 lesson requires; receivers switch on buffer[2] and compute expectedTotal per type (manifest 7+bodyLen+4 with bodyLen=27; chunk 7+5+bodyLen+4 with bodyLen=dataLen)"
      pattern: "PACKET_TYPE_IMAGE_MANIFEST|PACKET_TYPE_IMAGE_CHUNK"
    - from: "src/main_basestation.cpp /img/{id}_t.jpg route"
      to: "src/image_rx_manager.cpp latest thumbnail buffer"
      via: "HTTP route serves the retained most-recent completed thumbnail bytes with image/jpeg content type; 404 when no completed thumbnail exists"
      pattern: "img/.*_t|latestThumb"
  assumptions:
    - "Probe edge IMG-01 unclassified/unresolved — flagged assumption: the E32 modules run the EBYTE factory-default 9.6 kbps air rate, making the modeled ~210-280 ms per 240-byte packet real (research A1/A5); hardware UAT must confirm before any timing constant is tuned"
    - "Probe edge IMG-02 unclassified/unresolved — flagged assumption: QQVGA thumbnails at JPEG quality ~20 run ~2.5-4 KB (research A3), so a 12-18-chunk push lands in 3-6 s, inside the 10-second display window; must be measured on hardware with real scenes"
    - "Probe edge IMG-04 unclassified/unresolved — flagged assumption: per-chunk CRC16 + length-driven framing + the Phase 1 terminal/duplicate guards are sufficient reliability for reassembly on the half-duplex link; degraded-link behavior is a hardware-UAT item"
---

<objective>
Tracer plan: the first image bytes travel the full path — balloon capture to base station web UI — for the THUMBNAIL only. Task 1 fixes the CR-04/WR-11 thumbnail defect (the must-fix-before-first-caller carried from Phase 1's 01-VERIFICATION.md) and locks the entire Phase 2 wire contract in one header. Task 2 builds the balloon push side. Task 3 builds the base receive side and shows the thumbnail in the web UI. Full-image pull ARQ, SD storage, and event triggers are later plans (02-02, 02-03, 02-04) that extend — never restructure — what this plan proves.

Purpose: IMG-02 rides CR-04 (this plan wires createThumbnail's first caller, so the double-free defect MUST die here), and every later plan consumes the wire contract defined here. Proving the byte path end-to-end now catches protocol dead-ends before three modules grow on top of them.
Output: Fixed camera_manager, new image_protocol.h contract + serializers, push/receive modules (thumbnail path), base UI thumbnail display, extended wire harness, updated build filters.
</objective>

<execution_context>
@C:/Users/Amp/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Amp/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-image-transmission/02-CONTEXT.md
@.planning/phases/02-image-transmission/02-RESEARCH.md
@.planning/phases/02-image-transmission/02-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Close CR-04/WR-11 and lock the image wire contract (0x12/0x13/0x14, window + threshold commands) with harness teeth</name>
  <reversibility rating="costly">The 0x12/0x13/0x14 wire types and body layouts are a contract shared by both firmwares and the host harness; changing them later means reworking both sides of the radio link (CONTEXT rates D-17/D-21 costly for the same reason).</reversibility>
  <read_first>
  - src/camera_manager.cpp (createThumbnail ~lines 281-342 — the defect site; freeCurrentThumbnail/freeCurrentImage ~579-595; captureThumbnail ~204-226)
  - src/camera_manager.h (ImageData/ThumbnailData structs ~14-34; public API surface)
  - include/command_protocol.h (whole file — PacketType usage at 14-15, CameraCommand enum 28-47, constants block 179-205, ResponseStatusData 163-173)
  - src/command_protocol.cpp (serializeCommand/serializeResponse; byte-order helpers writeUint16/writeUint32/readUint16/readUint32 ~325-347; factories createCommandPacket/createResponsePacket)
  - scripts/verify_protocol_roundtrip.mjs (existing 15 clauses; the defective-variant teeth pattern)
  - .planning/phases/02-image-transmission/02-PATTERNS.md (sections "include/image_protocol.h" and "src/command_protocol.cpp")
  - .planning/phases/02-image-transmission/02-RESEARCH.md (Patterns 1-2, Pitfalls 1/9/10/11)
  </read_first>
  <files>src/camera_manager.cpp, src/camera_manager.h, include/image_protocol.h, include/command_protocol.h, src/command_protocol.cpp, scripts/verify_protocol_roundtrip.mjs</files>
  <action>
  CR-04/WR-11 fix (per 01-VERIFICATION.md must-fix-before-first-caller and CONTEXT domain block): rework CameraManager::createThumbnail in src/camera_manager.cpp to capture-then-allocate ordering. First swap frame settings (setFrameSize(FRAMESIZE_QQVGA) and setQuality(20) — quality 20, not the current 15, per the 10-second airtime window math, IMG-02/research A3), then esp_camera_fb_get(); on fb miss restore settings, NULL thumbnail.buffer, set thumbnail.valid=false, return false. On success allocate exactly fb->len with malloc, memcpy the frame, fill length/width/height (quality=20, timestamp=millis(), valid=true), esp_camera_fb_return(fb), restore frame size and quality on EVERY path. This eliminates the pre-capture size estimate from this path entirely; the size-estimate helper elsewhere in the same file then has zero remaining callers — delete that function definition too. Keep the existing freeCurrentThumbnail()/freeCurrentImage() null-after-free discipline untouched. In src/camera_manager.h add a CaptureSource-tracking field: lastCaptureSource as uint8_t with a getter getLastCaptureSource() and setter setLastCaptureSource(uint8_t), defaulting to 1 (interval) — sources: 0 manual, 1 interval, 2 event-altitude, 3 event-distance, 4 event-phase (constants live in image_protocol.h; do NOT redefine them here).

  Create include/image_protocol.h — the single wire contract for the phase, laid out section-for-section like include/command_protocol.h (PATTERNS assignment):
  - Packet types: PACKET_TYPE_IMAGE_MANIFEST = 0x12, PACKET_TYPE_IMAGE_CHUNK = 0x13, PACKET_TYPE_TELEMETRY_BEACON = 0x14 as static constexpr PacketType beside the 0x10/0x11 constants.
  - Transfer constants: IMG_CHUNK_PAYLOAD_SIZE = 200 (fits the 240-byte transport: 7 header + 5 chunk overhead + 200 + 4 trailer = 216), IMG_WINDOW_MAX_CHUNKS = 16 (D-21 suggested window), IMG_TX_QUEUE_DEPTH = 3 (PSRAM budget ~57 KB of 8 MB), IMG_RETRANSMIT_MAX_PASSES = 3 (D-24), IMG_WINDOW_STALL_MS = 8000, IMG_ENTRY_TTL_MS = 900000, TELEMETRY_BEACON_INTERVAL_MS = 5000, IMG_MANIFEST_BODY_SIZE = 27.
  - enum ImageKind : uint8_t { THUMBNAIL = 0, FULL_IMAGE = 1 }; enum CaptureSource : uint8_t { MANUAL = 0, INTERVAL = 1, EVENT_ALTITUDE = 2, EVENT_DISTANCE = 3, EVENT_PHASE = 4 }.
  - ImageManifestBody wire layout (27 bytes, ALL multi-byte fields big-endian via CommandProtocol::writeUint16/writeUint32 — Pitfall 9/IN-08, never memcpy native structs): imageId u16, imageKind u8, captureSource u8, totalSize u32, chunkSize u16, totalChunks u16, crc32 u32 (over the original bytes of THIS kind's payload — thumbnail CRC over thumbnail bytes, full CRC over full bytes, per D-23), captureTimeMs u32 (balloon millis at capture), then a 7-byte camera-settings trailer: resolution u8 (FrameSize wire code), quality u8, brightness i8, contrast i8, saturation i8, exposure i8, wbMode u8.
  - ImageChunkBody wire layout: imageId u16, chunkIndex u16 (0-based), dataLen u8 (<= IMG_CHUNK_PAYLOAD_SIZE), then dataLen bytes. The framed header bodyLen field carries dataLen so chunk framing uses the same arithmetic as commands (7 + 5 + bodyLen + 4).
  - TelemetryBeaconBody wire layout (17 bytes, big-endian): seq u16, altitudeCm i32, tempCentiC i16, latE6 i32, lonE6 i32, flags u8 (bit0 gpsValid). This is the minimal telemetry-over-E32 surface that makes PRI-01/SC-5 observable (research Open Question Q1 option a — planner-confirmed; richer telemetry stays Phase 3).
  - PayloadImageWindowRequest (5 bytes): imageId u16, startChunk u16, count u8.
  - PayloadSetEventThresholds (7 bytes): altDeltaM u16, distDeltaM u16, minSpacingSec u16, flags u8 (bit0 eventsEnabled).

  Extend include/command_protocol.h: add IMAGE_WINDOW_REQUEST = 0x30 and SET_EVENT_THRESHOLDS = 0x31 to the CameraCommand enum; add CMD_ACK_TIMEOUT_WINDOW_MS = 15000 to the ACK-timeout block (a 16-chunk stream costs ~4-6.5 s of airtime — Pitfall 4: neither SETTINGS 5000 nor COMPLEX 10000 fits); extend ResponseStatusData by consuming reserved bytes — eventThresholdAltM u16, eventThresholdDistM u16, eventMinSpacingSec u16, eventFlags u8 — shrinking reserved from 17 to 10 so the struct size is unchanged (28 bytes, still <= CMD_MAX_RESPONSE_DATA). Keep the header comment noting this struct is memcpy'd little-endian on both ends (the documented island — same-platform pair, internally consistent; do not mix big-endian-encoded fields into it).

  Extend src/command_protocol.cpp: serializeManifest/deserializeManifest (fixed 27-byte body, big-endian field-by-field using the existing helpers), serializeChunk/deserializeChunk (5-byte overhead + dataLen), serializeTelemetryBeacon/deserializeTelemetryBeacon (17 bytes), and factories createManifestPacket(const ImageManifestBody&) / createChunkPacket(uint16_t imageId, uint16_t chunkIndex, const uint8_t* data, uint8_t dataLen) that assign packet.type (0x12/0x13) as the FIRST field — the Phase 1 CR-01 lesson: the factory owns the type byte. Reuse calculateCRC16/validateCRC and the header/trailer conventions unchanged.

  Extend scripts/verify_protocol_roundtrip.mjs with clauses asserting (exit 0 only if ALL pass): (a) manifest round-trip preserves every big-endian field and bodyLen == 27; (b) createManifestPacket assigns the 0x12 type byte — include a defective local variant that skips the type assignment and assert the sweep catches it (teeth, mirroring the Phase 1 CR-01 pattern); (c) chunk round-trip with payload bytes containing the consecutive pair 0x0D 0x0A survives length-driven framing; (d) chunk dataLen > IMG_CHUNK_PAYLOAD_SIZE is rejected by serializeChunk; (e) beacon round-trip field preservation; (f) window-request and threshold payloads encode/decode symmetrically; (g) the receiver type-dispatch rule: frames whose buffer[2] is not a type accepted by that side are discarded (transcribe the rule as a table: balloon accepts 0x10 only; base accepts 0x11/0x12/0x13/0x14); (h) ResponseStatusData layout arithmetic totals 28 bytes after the reserved shrink. Keep the script's header comment contract (mirrors command_protocol.cpp; update when the wire format changes).
  </action>
  <verify>
  <automated>node scripts/verify_protocol_roundtrip.mjs && pio run -e esp32-s3-balloon -e esp32-s3-basestation && ! grep -q "estimateImageSize" src/camera_manager.cpp && grep -q "PACKET_TYPE_IMAGE_MANIFEST" include/image_protocol.h && grep -q "CMD_ACK_TIMEOUT_WINDOW_MS" include/command_protocol.h && grep -q "createManifestPacket" src/command_protocol.cpp</automated>
  </verify>
  <acceptance_criteria>
  - src/camera_manager.cpp createThumbnail allocates exactly fb->len after esp_camera_fb_get() returns; every early-return path assigns thumbnail.buffer = nullptr and thumbnail.valid = false before returning false
  - grep for the pre-capture size-estimate function name in src/camera_manager.cpp returns nothing (call site and dead definition both removed)
  - createThumbnail sets thumbnail quality 20 (the constant 20 appears in the quality assignment, 15 does not)
  - include/image_protocol.h defines PACKET_TYPE_IMAGE_MANIFEST/PACKET_TYPE_IMAGE_CHUNK/PACKET_TYPE_TELEMETRY_BEACON, ImageKind, CaptureSource, both payload structs, and IMG_CHUNK_PAYLOAD_SIZE = 200
  - include/command_protocol.h CameraCommand contains IMAGE_WINDOW_REQUEST = 0x30 and SET_EVENT_THRESHOLDS = 0x31; CMD_ACK_TIMEOUT_WINDOW_MS = 15000 sits in the timeout block
  - ResponseStatusData gains the four event fields and shrinks reserved to 10; the struct remains 28 bytes
  - All new wire fields in src/command_protocol.cpp pass through writeUint16/writeUint32/readUint16/readUint32 (no native-struct memcpy onto the wire)
  - scripts/verify_protocol_roundtrip.mjs exits 0 with the new clauses; the defective-variant clause provably fails when the type assignment is skipped
  - pio run reports SUCCESS for both esp32-s3-balloon and esp32-s3-basestation
  </acceptance_criteria>
  <done>The thumbnail defect is closed, the full Phase 2 wire contract exists in one header with serializers and factories that own their type bytes, and the host harness proves framing, bounds, endianness, and type-rejection rules end-to-end. CR-04/WR-11 and the protocol half of WR-12 are closed at code level.</done>
</task>

<task type="auto">
  <name>Task 2: Balloon push path — ImageTxManager takes ownership and pushes the thumbnail; type-safe receive dispatch (WR-12)</name>
  <read_first>
  - .planning/phases/02-image-transmission/02-PATTERNS.md (sections "src/image_tx_manager.cpp", "src/main_balloon.cpp", shared patterns: singleton skeleton, non-blocking loop, terminal-state guard)
  - src/auto_capture.cpp (singleton accessor ~8-15, process() ~86-106, getLastImageId; allocateImageId ~112-116)
  - src/camera_manager.cpp + src/camera_manager.h (currentImage/currentThumbnail access, captureThumbnail(), getLastCaptureSource from Task 1)
  - src/command_handler.cpp (processIncomingByte framing ~630-683; command dispatch switch ~147-186; handleCaptureNow)
  - src/main_balloon.cpp (begin wiring ~381-387, loop wiring ~725-728)
  - platformio.ini (base env build_src_filter with the -<auto_capture.cpp> exclusion pattern ~277)
  - src/e32_lora.h (transmit signature)
  </read_first>
  <files>include/image_tx_manager.h, src/image_tx_manager.cpp, src/main_balloon.cpp, src/command_handler.cpp, platformio.ini</files>
  <action>
  Create the balloon-side transfer module (singleton ImageTx() exactly like AutoCap() — PATTERNS shared pattern; debug banner + gated Serial logging like every module). Public API in include/image_tx_manager.h: begin(E32LoRa* lora), process(), plus getters used by later plans (queue snapshot for logging). Internal transfer-entry struct holding: imageId, captureSource, captureTimeMs, settings snapshot (copied from CameraManager cached getters at enqueue), full buffer pointer/length (PSRAM), thumbnail buffer pointer/length, crc32 for each, chunkSize, totalChunks per kind, per-entry state, next push index, lastActivityMs.

  process() behavior (push-only in this plan — the entry struct and states are final architecture, window servicing arrives in 02-02 as an extension, not a rework):
  - Poll AutoCap().getLastImageId() every pass. On change from the last-enqueued ID: call Camera().captureThumbnail() (the FIRST caller — Task 1 made this safe), then take ownership by ps_malloc + memcpy of BOTH currentImage and currentThumbnail (Pitfall 7: the next capture's freeCurrentImage must not pull bytes out from under a transfer; copy BEFORE returning from this branch), stamp captureSource from Camera().getLastCaptureSource(), compute both CRC32s with esp_rom_crc32_le, and push the entry onto the queue (fixed depth IMG_TX_QUEUE_DEPTH = 3; on overflow drop the OLDEST entry with a Serial warning — never silently).
  - Push discipline (one transmit per loop pass — Pattern 4; E32 transmit is synchronous and costs ~250-400 ms): first transmit the manifest for the pending thumbnail (kind=THUMBNAIL, totalSize/thumbLen, chunkSize 200, totalChunks = ceil, crc32, captureTimeMs, settings trailer), then one 0x13 chunk per subsequent process() pass until the thumbnail is fully pushed. Full-image announcement (kind=FULL_IMAGE manifest) is 02-02 — do NOT emit it here.
  - If a thumbnail buffer is missing (captureThumbnail failed), still enqueue the full image and skip thumbnail pushes — log it.

  Wire src/main_balloon.cpp: begin() — ImageTx().begin(&E32LoRaModule()) beside CmdHandler().begin (~381); loop() — ImageTx().process() AFTER AutoCap().process() (~728). This ordering is the first half of PRI-01 arbitration: command responses (sent inside CmdHandler().process()) always get the transmit opportunity before image traffic; at most one chunk transmit can ever sit between a response and the radio.

  WR-12 receive fix in src/command_handler.cpp processIncomingByte: once receiveIndex >= CMD_HEADER_SIZE, switch on the type byte buffer[2] — only PACKET_TYPE_COMMAND (0x10) proceeds into the existing expectedTotal arithmetic and handling; any other type (0x11/0x12/0x13/0x14 or unknown) resets receive state and discards the frame (Pitfall 6: a CRC-valid RESPONSE or manifest heard by the balloon must never execute as a command).

  Also in src/command_handler.cpp handleCaptureNow: call Camera().setLastCaptureSource(0) (manual) before the capture so enqueued manual images are stamped correctly (interval captures keep the default 1).

  platformio.ini: add -<image_tx_manager.cpp> to the base station env's build_src_filter (the established exclusion pattern beside auto_capture.cpp) so the base build never sees the balloon module.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation && grep -q "ImageTx()" src/image_tx_manager.cpp && grep -q "AutoCap().process();" src/main_balloon.cpp && node -e "const s=require('fs').readFileSync('src/main_balloon.cpp','utf8');const a=s.indexOf('AutoCap().process();');const b=s.indexOf('ImageTx().process();');if(a<0||b<0||b<a){process.exit(1)}" && grep -q "resetReceiveState" src/command_handler.cpp && ! grep -q "LoRaManager" src/image_tx_manager.cpp</automated>
  </verify>
  <acceptance_criteria>
  - src/image_tx_manager.cpp defines the ImageTx() singleton accessor and takes PSRAM ownership (ps_malloc + memcpy) of the full and thumbnail buffers at enqueue time, before any later capture can free them
  - The transfer queue is bounded at 3 entries with drop-oldest and a Serial warning on overflow
  - Thumbnail push pace is exactly one transmit per process() call (manifest first, then one chunk per pass)
  - src/main_balloon.cpp loop calls CmdHandler().process(), then AutoCap().process(), then ImageTx().process() — enforced by the node order-check in the verify command
  - src/command_handler.cpp receive path switches on the type byte and resets on anything that is not a 0x10 command frame
  - handleCaptureNow stamps the manual capture source before capturing
  - platformio.ini base env excludes image_tx_manager.cpp; both targets build SUCCESS
  - src/image_tx_manager.cpp contains no reference to the excluded SPI-era radio manager class (gate: grep LoRaManager finds nothing)
  </acceptance_criteria>
  <done>After any capture, the balloon autonomously pushes a framed, CRC-protected thumbnail stream over the E32 link, owns its buffers safely, and its receiver can no longer be confused by foreign packet types. IMG-01's push half and the balloon half of WR-12 are closed at code level.</done>
</task>

<task type="auto">
  <name>Task 3: Base receive path — ImageRxManager reassembles the thumbnail, serves it over HTTP, shows it in the UI</name>
  <read_first>
  - .planning/phases/02-image-transmission/02-PATTERNS.md (sections "src/image_rx_manager.cpp", "src/main_basestation.cpp", shared patterns: length-driven framing, terminal-state guard, hand-built JSON)
  - src/command_sender.cpp (processIncomingByte ~267-317 — the framing accumulator to extend; handleResponse; the connected/link truth pattern)
  - src/main_basestation.cpp (initLoRa ~440-450 for setRxBufferSize insertion; handleStatus hand-built JSON ~1008-1084; existing UI card/panel HTML+JS patterns; route registration)
  - include/base_station_config.h (constants home; MAX_IMAGE_SIZE)
  - include/image_protocol.h + src/command_protocol.cpp (Task 1 contract)
  - .planning/phases/02-image-transmission/02-RESEARCH.md (Pitfalls 2, 5, 11)
  </read_first>
  <files>src/command_sender.cpp, include/image_rx_manager.h, src/image_rx_manager.cpp, src/main_basestation.cpp, platformio.ini</files>
  <action>
  Create the base-side reassembly module (singleton ImageRx(), same skeleton discipline). Public API in include/image_rx_manager.h: onManifestFrame(const uint8_t* frame, size_t len), onChunkFrame(const uint8_t* frame, size_t len) (both receive the COMPLETE framed packet after CommandSender validation), process(), plus snapshot getters: getLatestThumbId(), getLatestThumbData()/Length(), getTelemetrySnapshot() (ageMs, altitudeM, tempC, lat, lon, gpsValid).

  Reassembly behavior (thumbnail path only in this plan — window ARQ and SD arrive in 02-03):
  - On manifest (kind=THUMBNAIL): validate before trusting (Pitfall 11): totalSize <= MAX_IMAGE_SIZE (50000, include/base_station_config.h), chunkSize <= IMG_CHUNK_PAYLOAD_SIZE, totalChunks == ceil(totalSize/chunkSize) and >= 1, imageKind is a known value; reject/ignore with a log otherwise. Allocate the accumulation buffer (malloc totalSize), zero a presence array[totalChunks], store manifest fields.
  - On chunk: validate imageId matches the in-flight manifest, chunkIndex < totalChunks, dataLen <= chunkSize; duplicate indices are dropped idempotently (presence array); copy payload at offset chunkIndex * chunkSize; increment received count.
  - On completion (all chunks present): verify esp_rom_crc32_le(0, buffer, totalSize) == manifest crc32 (D-23 — the end-to-end check per-chunk CRC16 cannot provide). On match: retain as the latest completed thumbnail (replacing the previous one; free the old), expose getLatestThumbId(). On mismatch: free, log, keep the previous thumbnail (no fabricated state — the UI keeps showing the last VERIFIED thumbnail).
  - Track telemetry beacon state: on a decoded 0x14 body, store the decoded fields with a receipt millis stamp (feeds the freshness surface; the balloon sends none until 02-02 — absent telemetry is reported as absent, never fabricated).

  src/command_sender.cpp processIncomingByte (WR-12 base half): after the header is buffered, switch on buffer[2]: 0x11 RESPONSE keeps the existing 7+4+bodyLen+4 path; 0x12 MANIFEST uses 7+bodyLen+4 with bodyLen forced to IMG_MANIFEST_BODY_SIZE; 0x13 CHUNK uses 7+5+bodyLen+4 (same arithmetic shape as commands); 0x14 BEACON uses 7+17+4; default resets receive state. On a fully received, CRC-valid 0x12/0x13/0x14 frame, deserialize (Task 1 functions) and forward to ImageRx() callbacks. Do NOT let these frames touch the tracked-command table — only RESPONSE packets resolve tracked commands (keeps the CR-03 terminal-state machinery exactly as Phase 1 left it).

  src/main_basestation.cpp:
  - Pitfall 2: insert LoRaSerial.setRxBufferSize(1024) in initLoRa() BEFORE the E32LoRaModule().begin(...) call (default 256-byte ring cannot absorb E32 sub-packed bursts).
  - Register GET /img/{id}_t/ route (path matcher for /img/ with _t suffix): serve the retained latest thumbnail bytes as image/jpeg via server.send_P-style streaming with Content-Type image/jpeg; 404 when the requested id is not the retained one or none exists. ID matching is numeric-only (strtol on the path segment; no string building into file paths).
  - handleStatus JSON additions (hand-built String style, computed truth only): latestThumbId (0 when none verified), telemetry object {ageMs, altitudeM, tempC, lat, lon, gpsValid} or telemetry: null when no beacon has ever been received.
  - UI: a "Latest capture" card beneath the existing panels — an img element whose src is set to /img/{id}_t.jpg whenever the polled /status latestThumbId changes (reuse the existing poll cadence and chip/label styling; IN-03-style computed truth: show a "waiting for first image" label until latestThumbId > 0; never show a stale image under a new id — clear the src on id change until the new thumbnail loads).

  platformio.ini: add -<image_rx_manager.cpp> to the balloon env's build_src_filter (base-only module).
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation && grep -q "setRxBufferSize(1024)" src/main_basestation.cpp && grep -q "onManifestFrame" src/image_rx_manager.cpp && grep -q "esp_rom_crc32_le" src/image_rx_manager.cpp && grep -q "latestThumbId" src/main_basestation.cpp && node scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - src/command_sender.cpp receive path dispatches on the type byte with per-type expectedTotal arithmetic (0x11 existing path unchanged; 0x12/0x13/0x14 forwarded to ImageRx; unknown types reset the accumulator)
  - ImageRx validates manifest fields against MAX_IMAGE_SIZE/chunkSize/totalChunks consistency before allocating anything
  - Duplicate chunk indices are dropped idempotently; completion requires every index present
  - Completed thumbnails pass the end-to-end CRC32 comparison before being exposed (a mismatch frees the buffer and retains the previous verified thumbnail)
  - The /img/{id}_t route serves image/jpeg from the retained verified thumbnail and 404s otherwise; route ID parsing is numeric-only
  - /status includes latestThumbId and an honest telemetry object (null when no beacon received)
  - LoRaSerial.setRxBufferSize(1024) executes before E32 begin in initLoRa()
  - The web UI shows the latest verified thumbnail and a waiting label before the first one arrives
  - platformio.ini balloon env excludes image_rx_manager.cpp; both targets build SUCCESS; harness still exits 0
  </acceptance_criteria>
  <done>A capture on the balloon results in the operator seeing that photo's thumbnail in the base station web UI with no operator action — the phase's first end-to-end user-visible capability (IMG-02 code path). The base half of WR-12 is closed and the receive foundation for 02-03's ARQ is in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link → balloon firmware | Untrusted shared channel: any CRC-valid frame the E32 hears is parsed by command_handler |
| RF link → base firmware | Untrusted shared channel: manifests/chunks/beacons parsed by command_sender dispatch into ImageRx |
| HTTP client → base routes | Open AP network: /img routes and /status serve data; no auth (carried WR-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering/DoS | command_sender.cpp + command_handler.cpp receive paths | high | mitigate | Type-byte dispatch before body arithmetic (WR-12 fix), per-type expectedTotal bounds, existing oversize rejection extended to new types — a malformed image frame resets the accumulator instead of overflowing it |
| T-02-02 | Tampering/DoS | image_rx_manager.cpp manifest handling | high | mitigate | Manifest field validation (totalSize <= MAX_IMAGE_SIZE, chunkSize <= 200, totalChunks == ceil) before any allocation — a pathological manifest is ignored, not obeyed (Pitfall 11) |
| T-02-03 | Spoofing | E32 RF channel | medium | accept | CRC-valid spoofed frames execute on an unauthenticated link — v1 has no link crypto by scope decision; blast radius bounded to camera capture and junk-image storage; carried on the /gsd-secure-phase ledger (WR-08 family) |
</threat_model>

<verification>
- node scripts/verify_protocol_roundtrip.mjs exits 0 (all Phase 1 clauses plus the new manifest/chunk/beacon/type-rejection clauses)
- pio run -e esp32-s3-balloon -e esp32-s3-basestation reports SUCCESS for both targets
- All Task acceptance-criteria greps pass (type constants present, estimate removed, dispatch present, buffer sizing present, build filters present)
- Hardware UAT (deferred to /gsd-verify-work 2): thumbnail visible within 10 s of capture over real radios; timing validated against the 9.6 kbps air-rate assumption A1
</verification>

<success_criteria>
- CR-04/WR-11 and WR-12 are closed at code level (Phase 1 must-fix-before-first-caller satisfied before the first createThumbnail caller ships)
- The complete Phase 2 wire contract exists in include/image_protocol.h and is proven by the host harness
- Thumbnail flows balloon→base→web UI with end-to-end CRC verification (IMG-01 push half, IMG-02 code path, IMG-04 chunk path)
- Both targets build with correct module exclusion
</success_criteria>

## Artifacts this phase produces (plan 02-01)

- New file include/image_protocol.h — constants PACKET_TYPE_IMAGE_MANIFEST/PACKET_TYPE_IMAGE_CHUNK/PACKET_TYPE_TELEMETRY_BEACON, IMG_CHUNK_PAYLOAD_SIZE, IMG_WINDOW_MAX_CHUNKS, IMG_TX_QUEUE_DEPTH, IMG_RETRANSMIT_MAX_PASSES, IMG_WINDOW_STALL_MS, IMG_ENTRY_TTL_MS, TELEMETRY_BEACON_INTERVAL_MS, IMG_MANIFEST_BODY_SIZE; enums ImageKind, CaptureSource; wire structs ImageManifestBody, ImageChunkBody, TelemetryBeaconBody, PayloadImageWindowRequest, PayloadSetEventThresholds
- CameraCommand enum additions IMAGE_WINDOW_REQUEST (0x30), SET_EVENT_THRESHOLDS (0x31); constant CMD_ACK_TIMEOUT_WINDOW_MS; ResponseStatusData fields eventThresholdAltM/eventThresholdDistM/eventMinSpacingSec/eventFlags (reserved shrunk to 10)
- CommandProtocol static methods serializeManifest/deserializeManifest/serializeChunk/deserializeChunk/serializeTelemetryBeacon/deserializeTelemetryBeacon; factories createManifestPacket/createChunkPacket
- CameraManager members getLastCaptureSource()/setLastCaptureSource(uint8_t)
- New modules ImageTxManager (ImageTx()) and ImageRxManager (ImageRx()) with begin/process and the snapshot getters listed in Tasks 2-3
- HTTP route GET /img/{id}_t on the base station; /status fields latestThumbId and telemetry
- Harness clauses (a)-(h) in scripts/verify_protocol_roundtrip.mjs

<output>
Create `.planning/phases/02-image-transmission/02-01-SUMMARY.md` when done
</output>
