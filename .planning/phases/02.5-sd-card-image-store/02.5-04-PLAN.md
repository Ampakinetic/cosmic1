---
phase: 02.5-sd-card-image-store
plan: 04
type: execute
wave: 4
depends_on: ["02.5-01", "02.5-02", "02.5-03"]
files_modified:
  - .planning/phases/02.5-sd-card-image-store/02.5-04-SUMMARY.md
autonomous: false
requirements: [IMG-01, IMG-03, IMG-04, PRI-03, STORE-01, STORE-02, STORE-03, STORE-04]
user_setup:
  - service: bench hardware
    why: "Bench session #12 is operator-run: flash both boards, run the series, provide consoles"
    env_vars: []
    dashboard_config:
      - task: "Insert a FAT32 SD card into the BALLOON board's slot (CLK=39/CMD=38/D0=40 wiring per decision D-01) and confirm the boot console shows the SdStore mount line before starting the series"
        location: "Balloon board serial console at 115200"
      - task: "Name the session consoles and record them (convention: balloon6.log / base6.log or operator's choice recorded as a deviation)"
        location: "Bench workstation"
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Series A on the file-backed path: spaced captures persist to card before manifest, thumbnails push, FULL windows serve from the file, and the base completes CRC-verified transfers (IMG-01/IMG-03/IMG-04/STORE-01 proven on hardware)"
    - "Crash-resume demo: a balloon reboot mid-FULL-transfer ends with the boot rescan re-announcing the undelivered file and the base completing the ORIGINAL capture without any re-capture (STORE-02 proven on hardware)"
    - "Fallback labeling check: with the card absent/unmountable, captures still work via the volatile path AND the console carries the honest volatile-degradation line (STORE-03 labeling proven on hardware)"
    - "Card-full refusal check: captures refused with the named log (and NACK_BUSY base-side for CAPTURE_NOW) while beacons/telemetry continue — or the unstageable scenario recorded by name per the project's honesty convention (STORE-04 judged on evidence)"
    - "Riding clauses judged on this shared session: SC-3 visible-effect pairs (tenth round riding from phase 01), WR-03 cadence discriminator (tenth round), QVGA-restore clause (tenth round)"
    - "Ledger flips happen ONLY on verbatim bench evidence; requirements flip complete only when their truths are log-proven; unstageable scenarios are recorded by name, never fabricated"
  artifacts:
    - path: ".planning/phases/02.5-sd-card-image-store/02.5-04-SUMMARY.md"
      provides: "Session record: pre-flight ELFs, per-step verdicts with verbatim log quotes, ledger flips, residual gaps"
  key_links:
    - from: "Pre-flight ELF SHA256 recording"
      to: "Console boot lines"
      via: "SHA verified on-disk vs .pio/build before decode/attribution (01-34 convention)"
      why: "Every verdict must be attributable to the deployed firmware"
    - from: "Bench evidence lines"
      to: "Ledger entries (01-UAT.md G-entries / WINDOWS / requirements)"
      via: "flip only on quoted log lines with file:line provenance"
      why: "Nothing flips on intention — the phase completes on evidence"
  assumptions:
    - requirement: IMG-01
      category: concurrency
      status: unresolved
      probe: "If interrupted or run in parallel, what is guaranteed?"
    - requirement: IMG-03
      category: concurrency
      status: unresolved
      probe: "If interrupted or run in parallel, what is guaranteed?"
    - requirement: IMG-04
      category: concurrency
      status: unresolved
      probe: "If interrupted or run in parallel, what is guaranteed?"
    - requirement: PRI-03
      category: unclassified
      status: unresolved
      probe: "unclassified — review manually"
    - requirement: STORE-01
      category: concurrency
      status: unresolved
      probe: "If interrupted or run in parallel, what is guaranteed?"
    - requirement: STORE-02
      category: concurrency
      status: unresolved
      probe: "If interrupted or run in parallel, what is guaranteed?"
    - requirement: STORE-03
      category: concurrency
      status: unresolved
      probe: "If interrupted or run in parallel, what is guaranteed?"
    - requirement: STORE-04
      category: unclassified
      status: unresolved
      probe: "unclassified — review manually"
  prohibitions:
    - statement: "No balloon code path may delete, truncate, or overwrite a card image file to reclaim space — the card is a keep-everything flight archive; only the manual serial clear (explicit CONFIRM token) may delete"
    - statement: "No capture may be silently dropped: card-full refusal and SD-write-failure fallback must each surface a named, greppable log line (and the NACK/error class where a command is involved)"
    - statement: "The volatile PSRAM queue must never be entered, logged, or labeled as the normal path — every fallback admission names the degradation and its loss-on-reboot consequence"
---

<objective>
Bench session #12 — the phase's verification moment and trailing wave (project convention: code rounds first, one bench moment last). The operator flashes both boards and runs the structured series; this plan supplies the automatable pre-flight, the operator checkpoint script, and the evidence-driven ledger flips. Coverage demanded by the ROADMAP success criterion 6: series A end-to-end on the file-backed path, the crash-resume demo (reboot mid-transfer -> boot rescan re-announces -> base completes), SD-write-failure fallback labeling, card-full refusal, and the riding clauses (SC-3 pairs, WR-03 cadence, QVGA restore — tenth-round rides carried from phase 01).

Purpose: STORE-01..04 and the IMG/PRI truths are code-proven by plans 01-03; they flip to hardware-proven only on this session's verbatim evidence.

Output: 02.5-04-SUMMARY.md with per-step verdicts, quoted log lines, ledger updates, and any residual gaps honestly routed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-sd-card-image-store/CONTEXT.md
@.planning/phases/02.5-sd-card-image-store/02.5-01-SUMMARY.md
@.planning/phases/02.5-sd-card-image-store/02.5-02-SUMMARY.md
@.planning/phases/02.5-sd-card-image-store/02.5-03-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automatable pre-flight — builds, harness, ELF provenance, instrument-suite carry-over</name>
  <files></files>
  <read_first>
@.planning/phases/01-command-protocol-control/01-34-SUMMARY.md (the 01-34 bench-session pattern this plan mirrors: pre-flight + operator checkpoint + evidence-driven flips)
@src/main_balloon.cpp (the carried instrument suite: [STACK] watermark incl. the WR-01 latch fix, [TWDT] handle one-shot, [IDLE0] registered printf, [I2C] disposition, G01_D1_IDLE_HOOK_DISABLED A/B guard)
  </read_first>
  <action>
Run the pre-flight and record every result in the plan summary draft:
1. Dual-target build: `pio run -e esp32-s3-balloon -e esp32-s3-basestation` — both must end SUCCESS.
2. Wire harness: `node scripts/verify_protocol_roundtrip.mjs` — exit 0 (wire frozen claim re-proven at the deployed commit).
3. ELF provenance: record SHA256 of `.pio/build/esp32-s3-balloon/firmware.elf` and the basestation equivalent; the summary instructs the operator to verify the balloon SHA on the bench console/boot or via the flashed artifact before any crash attribution (01-34 convention: decode verdicts only against the verified ELF).
4. Instrument carry-over greps: [STACK]/[TWDT]/[IDLE0]/[I2C] instrument code and the G01_D1_IDLE_HOOK_DISABLED guard present in src/main_balloon.cpp (CONTEXT.md: the round-#14 instruments carry over to the new architecture; the D1 A/B question stays live for phase 01's round #15 — this session does NOT close D1, it only must not regress).
5. Operator handoff: a flash checklist (both boards reflashed TOGETHER — the 01-13 both-boards rule; balloon SD card inserted; console files named and recorded).

NO bench claims are made from this task. Everything hardware rides the operator checkpoint.
  </action>
  <verify>
    <automated>pio run -e esp32-s3-balloon 2>&1 | tail -2 && pio run -e esp32-s3-basestation 2>&1 | tail -2 && node scripts/verify_protocol_roundtrip.mjs > /dev/null 2>&1 && echo HARNESS_OK && sha256sum .pio/build/esp32-s3-balloon/firmware.elf</automated>
  </verify>
  <acceptance_criteria>
- Both build invocations end SUCCESS; harness exit 0
- Balloon + base ELF SHA256 values recorded in the summary draft
- Instrument-suite greps answered (each tag present in src/main_balloon.cpp) and recorded
- The flash checklist names the both-boards-together rule and the SD-card precondition
  </acceptance_criteria>
  <done>Pre-flight artifacts recorded; the operator has a complete, unambiguous session script and handoff.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: BENCH SESSION #12 — operator-run structured series on the file-backed path</name>
  <files></files>
  <what-built>Phase 2.5 firmware (file-backed balloon image pipeline: persist-before-manifest, chunk-from-file serving, boot rescan resume, card-full refusal gates, manual SDCLEAR) built green on both targets with ELF SHA256 recorded by Task 1, ready to flash and exercise on the bench pair.</what-built>
  <how-to-verify>
    Operator runs the session script below and returns pasted console excerpts covering every step; the executor maps each demanded line to file:line before any ledger flip.
  </how-to-verify>
  <resume-signal>Operator pastes the console evidence (or console file names) for steps 0-5 and states which steps PROVED, FAILED, or could not be staged — the executor resumes with Task 3's ledger mapping using that evidence.</resume-signal>
  <read_first>
@.planning/phases/02.5-sd-card-image-store/02.5-01-SUMMARY.md (log-line vocabulary to grep for: "SdStore:", "persisted to SD", "VOLATILE fallback", "card full", "torn capture", rescan summary lines)
@.planning/phases/02.5-sd-card-image-store/02.5-02-SUMMARY.md (resume admission log lines)
@.planning/phases/02.5-sd-card-image-store/02.5-03-SUMMARY.md (refusal log prefixes: CommandHandler:/AutoCapture:/ImageTx:)
  </read_first>
  <action>
Present the operator with the session script (single structured checkpoint; the executor stops and waits for the operator's pasted console evidence before proceeding to Task 3):

STEP 0 — Mount check (decision D-01 pin ownership verified live, the 01-09-style operator checkpoint): power the balloon with the card inserted; the boot console must show the "SdStore:" mount line. A mount failure is recorded with its exact line (pins 39/38/40 named) and the session continues in honest-degradation terms — do not improvise wiring mid-session.

STEP 1 — Series A, file-backed end-to-end (STORE-01/IMG-01/IMG-03/IMG-04): issue spaced CAPTURE_NOW commands (interim discipline from phase 01: space them). For each capture record: the "persisted to SD" line timestamped BEFORE the thumbnail manifest line (STORE-01 ordering on air); thumbnail push completion; FULL manifest; window service lines; base-side CRC-verified COMPLETE (both kinds). Deliverable: per-capture verdicts with base5-style file:line quotes.

STEP 2 — Crash-resume demo (STORE-02): during a FULL window transfer (mid-service, not at a boundary), reset the balloon. After reboot record: the rescan summary line, the re-announced thumbnail + FULL manifest, and the base completing the ORIGINAL image id without any re-capture command. This is the phase's headline demo — verbatim quotes from both consoles.

STEP 3 — Fallback labeling (STORE-03): power the balloon with the card REMOVED (or a card known to fail mount); issue a CAPTURE_NOW; record the capture completing via the volatile path AND the honest degradation line naming volatile loss-on-reboot. Restore the card afterward.

STEP 4 — Card-full refusal (STORE-04): stage fullness if practical (small card pre-filled near capacity from a PC; or the headroom constant makes near-full refusal reachable). Record the named refusal log and, for CAPTURE_NOW, the NACK_BUSY verdict base-side; confirm beacons/telemetry continued throughout. If staging is impractical this session, record the scenario UNSTAGEABLE-BY-NAME with the reason (project convention: recorded, never fabricated) — STORE-04 stays open narrowed to the live-fire check.

STEP 5 — Riding clauses (tenth round, carried from phase 01): SC-3 visible-effect pairs (issue the settings command pairs and judge the dashboard visible effects per 01-UAT.md), WR-03 cadence discriminator (manual capture vs interval baseline behavior), QVGA-restore clause (settings cycle returns the camera to the pre-cycle resolution with a clean capture).

STEP 6 — Console hygiene: record console file names used (naming deviations recorded, never renamed post-hoc), crash census (rst codes / Guru / canary / mojibake — any D1-class event is recorded and routed, NOT diagnosed here), and the [MEM]/[STACK]/[TWDT]/[IDLE0]/[I2C] instrument lines.
  </action>
  <verify>
    <human-check>Operator returns pasted console excerpts (or console files) covering steps 0-5 with the specific lines each step demands; the executor maps each demanded line to file:line before any ledger flip.</human-check>
  </verify>
  <acceptance_criteria>
- Every step has an explicit verdict: PROVEN (with quotes), FAILED (with quotes + routed gap), or UNSTAGEABLE (named + reason) — no step left unjudged
- Step 1 verdicts discriminate the file-backed path specifically (persist-before-manifest ordering visible in timestamps)
- Step 2 demonstrates rescan re-announce AND base completion of the original id, or records exactly where the chain broke
- Steps 3/4: fallback labeling and refusal lines quoted verbatim, or named-unstageable
- Step 5: each riding clause judged or explicitly re-riding with reason
  </acceptance_criteria>
  <done>The session is fully evidenced: each phase truth either log-proven, failed-with-evidence, or honestly unstageable.</done>
</task>

<task type="auto">
  <name>Task 3: Evidence-driven ledger flips and session summary</name>
  <files>.planning/phases/02.5-sd-card-image-store/02.5-04-SUMMARY.md</files>
  <read_first>
@.planning/phases/02.5-sd-card-image-store/02.5-04-SUMMARY.md (the Task 1-2 draft record)
@.planning/phases/01-command-protocol-control/01-UAT.md (G-entry/WINDOWS ledger conventions; where riding-clause verdicts are recorded)
  </read_first>
  <action>
Flip ledgers strictly on Task 2 evidence, in the project's established vocabulary:
1. Requirements: STORE-01..04 (and the IMG-01/03/04 file-backed re-proofs + PRI-03 non-regression) flip complete ONLY with their quoted log lines recorded in the summary; failed or unstageable truths stay open, narrowed to the named uncovered class with the session's evidence attached.
2. Riding clauses: SC-3 / WR-03 / QVGA-restore verdicts are recorded against the phase 01 ledgers (01-UAT.md entries updated in place with session-#12 quotes) — judged means closed, re-riding means the round count advances with the reason.
3. D1 non-regression: any crash-class event observed is recorded in the census and routed to the phase 01 debug thread (round #15 owns D1; this phase does not diagnose it) — note explicitly whether the file-backed path's lighter RAM profile changes the crash picture or not, WITHOUT claiming a fix.
4. New gaps: any defect surfaced by the session enters the ledger open-first with a named mechanism and discriminator line, mirroring the sessions-7..11 convention.
5. The summary ends with the phase-completion statement or the honest list of what still blocks it, plus the SD-cache-related todos carried from session #11 if any remain unaddressed.
  </action>
  <verify>
    <automated>grep -c "VERDICT" .planning/phases/02.5-sd-card-image-store/02.5-04-SUMMARY.md; grep -c "UNSTAGEABLE\|PROVEN\|FAILED" .planning/phases/02.5-sd-card-image-store/02.5-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
- The summary contains a verdict block for every step 0-5 with verbatim file:line quotes for each PROVEN/FAILED
- No ledger entry flips without a quote (auditable: each flip cites its evidence line)
- Requirements marked complete match exactly the truths log-proven; the rest are open-narrowed with named discriminators
- The D1 boundary statement is present (no fix claim, census recorded)
  </acceptance_criteria>
  <done>Phase 2.5's completion state is fully evidence-backed: STORE-01..04 and the riding clauses each carry a verdict with provenance, and any residual work is named with its next discriminator.</done>
</task>

</tasks>

## Artifacts this phase produces

- **New file:** `.planning/phases/02.5-sd-card-image-store/02.5-04-SUMMARY.md` — the session record and the phase's evidence ledger
- **New record shapes:** ELF SHA256 provenance entries (balloon + base) verified against `.pio/build`; per-step verdict blocks using the vocabulary PROVEN / FAILED / UNSTAGEABLE, each carrying verbatim file:line console quotes
- **New ledger citations:** requirement flips (STORE-01..04, IMG-01/03/04 file-backed re-proofs, PRI-03) each citing their evidence line; riding-clause verdicts (SC-3 pairs, WR-03 cadence, QVGA restore) recorded against the phase 01 ledgers (01-UAT.md); crash-census + D1 boundary statement (no fix claim, events routed to the phase 01 debug thread)
- **No firmware symbols:** this plan produces no source changes — it is the phase's verification moment

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator evidence -> ledger | Bench log lines are the only input that flips requirements; fabricated or misattributed evidence is the failure class this plan's provenance rules guard against |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.5-11 | Repudiation | ELF/console attribution | high | mitigate | SHA256 recorded pre-flight; verdicts decoded only against the verified ELF (01-34 convention); console names + deviations recorded |
| T-02.5-12 | Repudiation | Ledger flips without evidence | high | mitigate | Flip-only-on-quote rule; each flip cites file:line; unstageable scenarios named, never fabricated (project honesty convention) |

ASVS Level 1 applies (security_block_on: high — both high threats mitigated by the evidence-provenance rules).
</threat_model>

<verification>
- Pre-flight: builds 2/2, harness exit 0, ELFs SHA-recorded, instrument suite greps answered
- Session: steps 0-5 each carry PROVEN/FAILED/UNSTAGEABLE with quotes
- Ledgers: flips cite evidence; riding clauses resolved or explicitly re-riding; D1 boundary honest
- Phase close: ROADMAP success criteria 1-6 each mapped to their evidence (criteria 1-5 code+bench, 6 is this session)
</verification>

<success_criteria>
Bench session #12 completes with full verdict coverage: series A file-backed, crash-resume, fallback labeling, card-full refusal, and the three riding clauses — each PROVEN with quotes, FAILED with a routed gap, or named-unstageable; ledgers updated accordingly.
</success_criteria>

<output>
Create `.planning/phases/02.5-sd-card-image-store/02.5-04-SUMMARY.md` when done
</output>
