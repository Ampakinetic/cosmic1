---
phase: 02.5-sd-card-image-store
plan: 04
subsystem: bench-verification
tags: [bench-session-12, sd-card, file-backed-pipeline, crash-resume, card-full, riding-clauses, operator-checkpoint]

# Dependency graph
requires:
  - phase: 02.5-sd-card-image-store (plans 01-03)
    provides: BalloonSdStore (persist-before-manifest, bootRescan, hasHeadroomFor, SDCLEAR), file-backed ImageTxManager (card-served chunks, delivery bits), card-full refusal gates (CommandHandler NACK_BUSY / AutoCapture skip)
provides:
  - "PENDING (Task 3, continuation): session #12 evidence record — per-step verdicts, ledger flips, residual gaps"
affects: [phase 02.5 completion, phase 01 round #15 shared session, /gsd-ship gate]

# Actuals (#2632)
actuals:
  tokens: 12000   # pre-flight session; final value set by the continuation closeout
  tasks: 1        # Task 1 done; Task 2 = operator checkpoint; Task 3 = continuation
  commits: 0      # updated by the continuation closeout

status: draft-preflight   # NOT final — Task 3 (continuation agent) completes this summary from operator evidence

key-decisions: []         # filled by Task 3

requirements-completed: [] # flips ONLY on operator evidence (Task 3)
---

# Phase 2.5 Plan 04: Bench Session #12 Summary — DRAFT (pre-flight recorded; verdicts pending operator evidence)

**Status: Task 1 (automatable pre-flight) COMPLETE. Task 2 is a `checkpoint:human-verify` (gate=blocking-human) — bench session #12 is operator-run. This file holds the pre-flight record, the flash checklist, and the session script. The continuation agent finalizes verdicts and ledger flips strictly from the operator's pasted evidence. NOTHING in the verdict sections may be fabricated.**

## Pre-Flight Record (Task 1 — executed 2026-08-28/29, executor session)

### Builds

| Target | Result | Duration |
|---|---|---|
| `pio run -e esp32-s3-balloon` | **SUCCESS** | 32.0 s |
| `pio run -e esp32-s3-basestation` | **SUCCESS** | 30.8 s |

### Wire harness

- `node scripts/verify_protocol_roundtrip.mjs` → exit 0 (**HARNESS_OK**) — the wire-frozen claim re-proven at the deployed commit (HEAD `d1dbca7`).

### ELF provenance (T-02.5-11 mitigation — decode verdicts only against the verified ELF; 01-34 convention)

| Artifact | SHA256 |
|---|---|
| `.pio/build/esp32-s3-balloon/firmware.elf` | `14f2f5f72e5a8adc90b648018640cf00b7e550ddd229d8b1e77eb90aa9cd8b3d` |
| `.pio/build/esp32-s3-basestation/firmware.elf` | `602d378b067c3db2aaef5ecfa548f489561f2dfa7a8c7ef528a80b103defddf0` |

Built Aug 29 04:32 (balloon) / Aug 29 03:21 (base) local. The operator verifies the flashed balloon firmware against the balloon SHA before any crash attribution (boot console, `esptool image_info`, or `idf.py`-decoded backtrace against this exact ELF).

### Instrument-suite carry-over greps (src/main_balloon.cpp unless noted)

| Instrument | Count | Location evidence |
|---|---|---|
| `[STACK]` | 3 | main_balloon.cpp:52, :143, :493 (round-#14 watermark instrument) |
| `[TWDT]` | 1 | **src/image_tx_manager.cpp:171** — NOT main_balloon.cpp (see pre-flight note below); prints `ImageTx: [TWDT] idle0 wdt status=...` at boot |
| `[IDLE0]` | 10 | main_balloon.cpp:129-176 (hook + WR-01 latch + [STACK] sampler), :329-353 (registration print), :462-466 (1 Hz sampler) |
| `[I2C]` | 5 | main_balloon.cpp:51, :1017, :1047, :1052 (BMP280 probe/recovered prints) |
| `G01_D1_IDLE_HOOK_DISABLED` | 3 | main_balloon.cpp (A/B guard present; registration :350-353) |

**Pre-flight note (plan-expectation correction, no code change):** the plan's verify chain expected `grep -c "[TWDT]" src/main_balloon.cpp` non-zero. The [TWDT] instrument has lived in `src/image_tx_manager.cpp` since round #13 (01-30) — it prints from ImageTx boot (:171). The instrument is present, wired, and prints at boot; only the plan's grep target was wrong. Recorded here per the honesty convention; no source change made (this plan produces no firmware symbols).

### Session log-line vocabulary (greppable strings the operator looks for, verified in source)

| Step | Line | Source |
|---|---|---|
| 0 mount OK | `SdStore: card mounted, /images ready` | sd_store_balloon.cpp:82 |
| 0 mount FAIL | `SdStore: SD_MMC.begin failed (1-bit SDMMC pins CLK=39 CMD=38 D0=40); ...` | sd_store_balloon.cpp:59 |
| 0 rescan summary | `SdStore: rescan found %u undelivered image(s), admitted %u` | main_balloon.cpp:740 |
| 1 persist | `ImageTx: image %u persisted to SD (full %u B / %u chunks, thumb %u B / %u chunks)` | image_tx_manager.cpp:583 |
| 1 persist detail | `SdStore: persisted image %u (full %u B, thumb %u B, commit META %s %u B)` | sd_store_balloon.cpp:244 |
| 2 torn | `SdStore: torn capture at image %u skipped (no validating META/size mismatch) — file kept on card` | sd_store_balloon.cpp:478/:557 |
| 2 delivered-skip | `SdStore: rescan skipped %u already-delivered record(s) - history kept on card` | sd_store_balloon.cpp:568 |
| 3 fallback admission | `ImageTx: SD write failed for image %u - VOLATILE fallback engaged (image lost on reboot)` | image_tx_manager.cpp:502 |
| 3 mount-down | `SD card store unavailable - captures take the volatile fallback` / `...no rescan; nothing to resume (volatile fallback regime)` | main_balloon.cpp:717/:743 |
| 4 card full (persist layer) | `SdStore: card full - image %u persist refused (%llu B free < %llu B required)` | sd_store_balloon.cpp:184 |
| 4 card full (CAPTURE_NOW gate) | `CommandHandler: capture refused - card full (keep-everything archive; manual clear via SDCLEAR CONFIRM)` + base books NACK_BUSY "Capture refused - card full" | command_handler.cpp:263/:261 |
| 4 card full (interval) | `AutoCapture: capture skipped - card full (image id %u not taken)` | auto_capture.cpp:296 |
| SDCLEAR | `SdStore: manual clear requires: SDCLEAR CONFIRM` / `SdStore: cleared /images/...` / `SdStore: manual clear removed %u file(s)` | sd_store_balloon.cpp:594/:6xx/:631 |
| Instruments | `[IDLE0] hook cpu0 registered=1 (G-01-10)`; `ImageTx: [TWDT] idle0 wdt status=ESP_OK`; `[STACK]` watermark; `[I2C]` episode lines | main_balloon.cpp:351, image_tx_manager.cpp:171, main_balloon.cpp:1047 |

## Operator Handoff — Flash Checklist

1. **Both boards reflashed TOGETHER (the 01-13 both-boards rule):** `pio run -e esp32-s3-balloon -t upload && pio run -e esp32-s3-basestation -t upload`. Mixed old/new firmware breaks chunk framing (01-09 air-rate severance lesson; both boards enforce 9.6k at boot).
2. **SD card precondition (decision D-01):** insert a FAT32 card into the BALLOON board's slot (CLK=39/CMD=38/D0=40) BEFORE the series; the boot console must show `SdStore: card mounted, /images ready`.
3. **Consoles named and recorded before starting** (convention: `balloon6.log` / `base6.log`, or operator's choice recorded as a deviation — never renamed post-hoc). Serial at 115200.
4. **ELF SHA provenance:** verify the flashed balloon artifact against SHA256 `14f2f5f7...8b3d` (full value above) before any crash attribution.
5. **Interim discipline:** space CAPTURE_NOW commands (phase-01 interim discipline carries into session #12 unless a step demands otherwise).

## Operator Session Script (Task 2 — steps 0-6, verbatim demands)

- **STEP 0 — Mount check:** power the balloon with the card inserted; boot console must show the `SdStore:` mount line (`SdStore: card mounted, /images ready`). A mount failure is recorded with its exact line (pins 39/38/40 named) and the session continues in honest-degradation terms — do not improvise wiring mid-session.
- **STEP 1 — Series A, file-backed end-to-end (STORE-01/IMG-01/IMG-03/IMG-04):** spaced CAPTURE_NOW commands. Per capture record: the `persisted to SD` line timestamped BEFORE the thumbnail manifest line (STORE-01 ordering on air); thumbnail push completion; FULL manifest; window service lines; base-side CRC-verified COMPLETE (both kinds). Deliverable: per-capture verdicts with base5-style file:line quotes.
- **STEP 2 — Crash-resume demo (STORE-02):** during a FULL window transfer (mid-service, not at a boundary), reset the balloon. After reboot record: the rescan summary line, the re-announced thumbnail + FULL manifest, and the base completing the ORIGINAL image id without any re-capture command. The phase's headline demo — verbatim quotes from both consoles.
- **STEP 3 — Fallback labeling (STORE-03):** power the balloon with the card REMOVED (or a known-fail card); issue CAPTURE_NOW; record the capture completing via the volatile path AND the honest degradation line naming volatile loss-on-reboot. Restore the card afterward.
- **STEP 4 — Card-full refusal (STORE-04):** stage fullness if practical (small card pre-filled near capacity from a PC; or headroom constant makes near-full refusal reachable). Record the named refusal log and, for CAPTURE_NOW, the NACK_BUSY verdict base-side; confirm beacons/telemetry continued throughout. If staging is impractical, record UNSTAGEABLE-BY-NAME with the reason — STORE-04 stays open narrowed to the live-fire check.
- **STEP 5 — Riding clauses (tenth round, carried from phase 01):** SC-3 visible-effect pairs (settings command pairs; judge dashboard visible effects per 01-UAT.md), WR-03 cadence discriminator (manual capture vs interval baseline behavior), QVGA-restore clause (settings cycle returns the camera to the pre-cycle resolution with a clean capture).
- **STEP 6 — Console hygiene:** record console file names (naming deviations recorded, never renamed post-hoc), crash census (rst codes / Guru / canary / mojibake — any D1-class event is recorded and routed to the phase-01 debug thread, NOT diagnosed here), and the [MEM]/[STACK]/[TWDT]/[IDLE0]/[I2C] instrument lines.

## Per-Step Verdicts

**PENDING OPERATOR EVIDENCE — to be completed by the continuation agent (Task 3). Vocabulary: PROVEN (with verbatim file:line quotes) / FAILED (with quotes + routed gap) / UNSTAGEABLE (named + reason). No step left unjudged.**

| Step | Verdict | Evidence quotes |
|---|---|---|
| 0 Mount check | PENDING | — |
| 1 Series A file-backed | PENDING | — |
| 2 Crash-resume demo | PENDING | — |
| 3 Fallback labeling | PENDING | — |
| 4 Card-full refusal | PENDING | — |
| 5 Riding clauses (SC-3 / WR-03 / QVGA) | PENDING | — |
| 6 Console hygiene + instruments | PENDING | — |

## Ledger Flips

**PENDING (Task 3, continuation):** requirement flips (STORE-01..04, IMG-01/03/04 file-backed re-proofs, PRI-03 non-regression) ONLY with quoted log lines; riding-clause verdicts recorded against the phase-01 ledgers (01-UAT.md); D1 non-regression census + boundary statement (no fix claim); new gaps open-first with named mechanism + discriminator.
