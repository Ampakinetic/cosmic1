---
phase: 01-command-protocol-control
plan: 30
type: execute
wave: 22
depends_on: []
files_modified:
  - .planning/debug/d1-crash-regression-push-start.md
  - src/image_tx_manager.cpp
  - src/main_balloon.cpp
  - src/e32_lora.cpp
  - include/e32_lora.h
  - include/image_protocol.h
  - platformio.ini
  - .planning/WINDOWS.md
  - .planning/phases/01-command-protocol-control/01-UAT.md
autonomous: false
gap_closure: true
requirements: [CTRL-01, CTRL-06, PRI-02, IMG-03]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low   # calibration store has 0 samples (factor 1, not applied); mirrors 01-28 sizing — this round swaps the service-phase audit for the lull-phase audit + the §8.6 convergence/determinism questions, and drops the WDT-topology work §7 already landed

must_haves:
  truths:
    - statement: "The LULL-PHASE candidate set — the §7.3 arithmetic it never had — is audited and dispositioned in debug doc §9: every candidate that plausibly executed in the inter-window lull (balloon3.log :495-:519) carries the four-property disposition (worst-case duration vs the 10 s TWDT stage-1 period, masks interrupts?, holds a kernel lock across a blocking wait?, suspends the flash cache?) with an ELIMINATED-or-SUSPECT verdict. The minimum candidate set: each Wire-0 transaction site (the BMP280 read path AND the 1 Hz OLED full-frame refresh, including what ESP_ERR_INVALID_STATE at esp32-hal-i2c-ng.c:275 actually means in the deployed driver — which FSM state produces it and whether that path holds a portMUX or spins), GPS UART1 reads (post-3f2c2c6 pin-41 wiring), NVS/flash operations reachable during a lull (auto_capture.cpp image-ID persistence among them — confirm when it actually writes), the TX-light radio sites that DID run (the [BCN] seq=23 beacon transmit incl. Lever A's drain, the GET_STATUS response transmit), and Serial.printf console output"
      verification: explicit
    - statement: "The §8.6 crash-context determinism question is ANSWERED: the three expressions are tabulated (session 7 first-post-boot push; session 8 mid-service, chunk 1/14 of the seventh re-armed window; session 9 inter-window lull at t=124026 ms) against their shared constants (TG0WDT stage-1, Saved PC in the tick-ISR chain, zero project frames, loopTask healthy, [MEM] healthy), and an explicit hypothesis table contrasts lull-phase starver vs service-phase starver vs phase-independent starver — each with its predicted discriminator signature and a verdict on whether the IDLE0-starvation-on-CPU0 family remains the best fit when the wedge forms or terminates in a TX-light lull"
      verification: explicit
    - statement: "The §8.6 convergence question is ANSWERED with a ranked reading: the crash-adjacent Wire-0 ESP_ERR_INVALID_STATE carrying session-7-class mojibake on its own line is dispositioned across reading (a) driver-state corruption as a co-effect of the same fault, (b) an I2C bus wedge with interrupts masked on CPU0 (INVALID_STATE the driver's honest refusal), and (c) coincidence — with evidence for/against each, the adjacency + own-line mojibake + no-successful-read-after facts carried as discriminating evidence (never explained away), and each reading's predicted round-#3 discriminator outcome stated"
      verification: explicit
    - statement: "The §8.6 proximity question is addressed honestly: why session 8's single mojibake line sat ~1070 lines from its reset while session 9's sat ON the crash-adjacent line — the terminal-phase-degradation reading vs coincidence, bounded by the one-sample-per-session limit"
      verification: explicit
    - statement: "The §8.6 discriminator menu is dispositioned and the round's instrumentation (and lever, if the evidence names one) is specified for Task 2 with bench-discriminator log lines: a CPU0 idle-observability counter sampled from the existing 1 Hz block, I2C-bus health instrumentation at the Wire-0 failure site (bounded: transaction count + driver FSM state at failure), a TWDT stage-0 subscription check against the build's config — every line latch-guarded, greppable, bounded, and carrying a named removal condition (the [MEM]/B1/B2 convention)"
      verification: explicit
    - statement: "The Task 2 checkpoint record exists and its selection maps to §9's evidence — and the honesty guard holds: two consecutive lever fixes are bench-disconfirmed (01-25 warm-up at session 8, 01-28 Lever A at session 9), so if §9 again reaches only elimination + blast radius with NO named mechanism site, the evidence-honest selection is instrument-only (option b), never a third speculative lever"
      verification: explicit
    - statement: "Both firmware targets build green and the wire-format harness exits 0 on the round's final commit; Lever A, the B1/B2 discriminators, the [MEM] instrumentation, and the round-#10 discriminator lines all remain in source with their removal conditions unchanged"
      verification: explicit
    - statement: "Ledger honesty: WINDOWS entry 15 and 01-UAT.md G-01-10 carry the round-#13 determination (root_cause extended, the round-#3 missing item marked DONE) but stay OPEN — the crash truth flips only on 01-31's hardware evidence; the SUMMARY makes no bench claims"
      verification: explicit
  artifacts:
    - ".planning/debug/d1-crash-regression-push-start.md — §9 appended (round #13): lull-phase reconstruction + candidate arithmetic, crash-context determinism table, the convergence verdict, the proximity reading, the ranked root cause (or honest instrument-first record), the discriminator-menu disposition, and the Task 2 checkpoint record"
    - "The fix and/or instrumentation at the §9-named sites — candidates carried in files_modified (src/main_balloon.cpp lull-phase sensor/I2C/UART paths, src/image_tx_manager.cpp instrumentation block, src/e32_lora.cpp, include/image_protocol.h constants, platformio.ini); the actually-touched subset is named in §9 and the SUMMARY; untouched candidates are explicitly dispositioned in §9"
    - ".planning/WINDOWS.md — entry 15 reason extended with the round-#13 determination; entry stays open; counts unchanged (2/17/19)"
    - ".planning/phases/01-command-protocol-control/01-UAT.md — G-01-10 root_cause extended + missing list updated (the debug-round-#3 item DONE); status stays open"
    - ".planning/phases/01-command-protocol-control/01-30-SUMMARY.md"
  key_links:
    - "Retained deployed ELF .pio/build/esp32-s3-balloon/firmware.elf (SHA256 e09dd034a5ab…, verifier-reproduced at re-verification #11) <-> the audit: the SHA is re-confirmed before any new addr2line run; the §8 decodes (0x4037c7fa -> esp_vApplicationTickHook freertos_hooks.c:34) are recorded and verifier-reproduced — new decodes run only against the SHA-verified build"
    - "Lever A / B1 / B2 / [MEM] <-> round #13: all four stay in (their removal condition — G-01-10 closed on bench evidence — is unmet); round #3's instrumentation ADDS observability, it never strips the existing discriminators"
    - "Round-#3 discriminators <-> 01-31's D1 acceptance: the bench session's pass/fail reads the §9-named lines, not just absence of resets"
    - "The lull audit <-> §7's service audit: §9 reuses §7.1's WDT topology and §7.3's four-property template verbatim — the lull set gets the same arithmetic the service set got, no weaker standard"
  prohibitions:
    - statement: "MUST NOT flip WINDOWS 15 or G-01-10 to resolved in this plan — crash-truth closure requires 01-31's hardware session (all three crash expressions survived); code + builds + harness only establishes the fix/instrumentation is wired"
      status: flagged
      verification: backstop
    - statement: "MUST NOT ship a third speculative lever unless §9's lull audit names a concrete mechanism site — the 01-25 and 01-28 disconfirmations are the evidence bound; elimination-only audits select instrument-only"
      status: flagged
      verification: backstop
    - statement: "MUST NOT alter the wire format, the 01-18 quiet gate constants (CMD_TX_CHANNEL_QUIET_MS 750 in include/command_sender.h), or D-05/D-07 pacing — the harness must exit 0 unchanged unless §9's evidence explicitly names one of them as the root cause (then named explicitly in §9)"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT delete, mute, or gate Lever A, the B1/B2 discriminators, the [MEM] instrumentation, or the round-#10 discriminator lines — removal conditions are unchanged and 01-31's judgments depend on them"
      status: flagged
      verification: backstop
    - statement: "MUST NOT apply the §7.6-rejected WDT-config lever (lengthen CONFIG_ESP_TASK_WDT_TIMEOUT_S / drop stage-1) — it masks the symptom and weakens every future bench tripwire; the rejection stands unless §9's evidence re-opens it by name"
      status: flagged
      verification: backstop
---

<objective>
Close the root-cause half of the D1 crash regression's THIRD expression (G-01-10 / WINDOWS 15, 01-VERIFICATION.md gap 1, the phase's top blocker) on the axis debug doc §8.6 names: audit the INTER-WINDOW-LULL candidate set with the §7.3 arithmetic it never had, answer §8.6's three questions (lull-vs-service starver fit + crash-context determinism; what the crash-adjacent I2C failure with own-line mojibake implies; why the corruption signature moved adjacent), disposition the §8.6 discriminator menu, and land the evidence-selected fix and/or instrumentation with named bench discriminators — code + builds + harness only; hardware acceptance deferred to 01-31.

Purpose: session 9 disconfirmed the 01-28 Lever-A fix as sufficient a SECOND time and moved the crash point out of sustained TX-heavy service into the TX-light lull after a cleanly completed first window — "sustained service" is no longer the constant context, and the lull set (Wire-0 transactions, GPS UART1 reads, NVS/flash ops) has never had the candidate-block arithmetic. Every riding truth (G-01-7 series A ninth attempt, SC-3 pairs eighth round, WR-03 discriminator, CIF/QVGA clauses, dashboard LOOK) is gated behind this fix, and even SPACED single captures are proven unsafe (session 9 died on the FIRST image's lull).

Output: debug doc §9 with the lull reconstruction, dispositioned candidates, the determinism table, the convergence verdict, the ranked cause, and the named instrumentation/lever + bench discriminators; the selection landed green on both targets with the harness passing; WINDOWS 15 and G-01-10 carrying the round-#13 determination, honestly still open.

Spec-less probe fallback: recorded visible skip — phase_req_ids null this run, no probe predicates generated (standard goal-backward verification, the established determination).

<assumption_delta_decision>
The deterministic assumption-delta scan fired on the Phase 1 ROADMAP section with the same two signals rounds #11 and #12 dispositioned: "second" in the executed 01-09 bench step "second-card retry" and "fallback" in the executed 01-14 plan description "never-wedge fallback" — both trace to completed-plan prose in the roadmap listing, not to round #13's scope. Decision: no-change — this round audits and instruments a watchdog-starvation defect on the existing single-pipeline architecture, introducing no second platform, auth method, tenant, region, or source of truth. No identity-model promotion is warranted; no invariant test added (the [BOOT]/[LOOP]/[MEM] discriminators plus §9's new instruments already encode the crash-regression tripwire).
</assumption_delta_decision>
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/debug/d1-crash-regression-push-start.md
@.planning/phases/01-command-protocol-control/01-28-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-29-SUMMARY.md
@.planning/WINDOWS.md

Binding evidence and constraints:
- Session-9 evidence (verifier-reproduced at re-verification #11; debug doc §8 carries the full record; every citation locatable in balloon3.log 804 lines / base3.log 221 lines): the smoke's first window served CLEAN — thumb COMPLETE 9/9 (base3.log:79), FULL window 1 armed (balloon3.log:421), ALL 16/16 chunks on air (:427-:491) — then in the TX-light lull (:495-:519: GET_STATUS serviced :497-:501, [BCN] seq=23 :505, healthy [MEM] :511, Performance Max 1277 ms :514, telemetry TX :518-:519) the balloon reset at t=124026 ms: :520 i2cWrite ESP_ERR_INVALID_STATE with the session-7 corruption-class bytes ON the line (replacing the T of STATE; raw bytes e2 88 a9 e2 94 90 e2 95 9c — the UTF-8-replacement-char grep returns 0, the twice-documented §7.4 trap), :523 rst:0x7 TG0WDT_SYS_RST, :524 Saved PC:0x4037c7fa = esp_vApplicationTickHook (freertos_hooks.c:34), one frame above session-8's tick_hook sample in the SAME tick-ISR chain, ZERO project frames. B2 [LOOP] = ZERO fires whole-console (loop healthy to the final second); B1 = TASK_WDT at boot 2 (:615); [MEM] healthy (:372/:511). Crash-signature counts (verifier-reproduced): rst:0x7=1, rst:0xc=0, Guru=0, canary=0, [LOOP]=0, i2cWrite=1 (the adjacent one), mojibake=1.
- Deployed-build identity: .pio/build/esp32-s3-balloon/firmware.elf SHA256 e09dd034a5ab7b904278902106a0348d12c800ad88b03daac5d29029ad0cb5f7 (mtime Aug 28 12:06 matching the deployed boot banner) — CONFIRM before any new decode; addr2line toolchain: xtensa-esp32s3-elf-addr2line under the toolchain-xtensa-esp32s3 package in the PlatformIO packages directory, flags -pfiaC -e. Source is unchanged since the round-#12 fix commit 6ca9a36 (verifier-confirmed zero drift) — if the SHA has drifted, a rebuild from HEAD should reproduce it; a non-reproducing rebuild is itself a finding to record before any decode is trusted.
- WDT topology (already determined, §7.1 — reuse, do not re-derive): rst:0x7 TG0WDT_SYS_RST = the TASK watchdog's stage-1 10 s silent hardware backstop (MWDT0/TG0); ONLY IDLE0 feeds the TWDT in this firmware (loopTask unsubscribed, pinned CPU1); stage-0 INT @5 s prints unconditionally and was never observed — every crash proves IDLE0 starved >=10 s on CPU0 while tick chains stayed alive.
- Established verdicts this round must not quietly reopen: ONE mechanism family across sessions 7/8 (§7.4), now with session 9's third PC sample in the same chain (§8.5); all EIGHT service-phase project-code starver candidates eliminated (§7.3); Lever A correctly retained as hazard removal but disconfirmed as the cure (§8.3); [MEM]/B1/B2 stay in until G-01-10 closes on bench evidence.
- House debug convention: evidence quotes carry file:line; every framework/config claim carries a re-derivable path; the checkpoint auto-advance convention (01-23/01-25/01-26/01-28): under workflow.auto_advance=true the Task 2 disposition auto-selects the evidence-named option and records the selection as pending end-of-phase operator confirmation (this joins the FOUR existing pending confirmations as the fifth).
- Scope guards: the 01-18 quiet gate (CMD_TX_CHANNEL_QUIET_MS 750 / channelQuietForTx in src/command_sender.cpp) and the 01-15/01-19 command-path semantics are protected surfaces; scripts/verify_protocol_roundtrip.mjs is the wire-format tripwire.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit the lull phase — candidate arithmetic, crash-context determinism, the §8.6 convergence verdict — debug doc §9</name>
  <files>.planning/debug/d1-crash-regression-push-start.md</files>
  <read_first>
  - balloon3.log — the full session: boot 1 banner (:23), warm-up + B1 (:102-:103), capture/enqueue (:366-:375), thumb chunk sends (:383-:414), FULL window arm (:421) + all 16 chunk sends (:427-:491), the genuine re-announce hold (:492), the lull (:495-:519 — GET_STATUS :497-:501, [BCN] seq=23 :505, [MEM] :511, Performance :514, beacon TX :518-:519), the failing i2cWrite with on-line mojibake (:520), the ROM banner (:521-:522), rst:0x7 (:523), Saved PC (:524), boot 2 (:530 onward — warm-up/B1 :614-:615, honest post-reboot rejects :679/:692/:713, no-valid-buffer :647)
  - base3.log — the base-side counterpart: command ACKs (:28/:61/:89), thumb COMPLETE (:79), manifest CRC (:81), retransmit requests + NACK_INVALID x3 (:138-:181), INCOMPLETE 15/36 (:194)
  - .planning/debug/d1-crash-regression-push-start.md — §1-§8 IN FULL: §7.1's WDT topology (reuse verbatim), §7.3's four-property candidate template (the arithmetic standard §9 must match), §7.4's one-family verdict, §8.4's mojibake/adjacency record, §8.5's three-PC-samples table, §8.6's brief this task executes
  - The deployed framework sources under the PlatformIO packages dir (IDF 5.5.4 / arduino-esp32 pinned by platformio.ini): esp32-hal-i2c-ng.c (what error path produces ESP_ERR_INVALID_STATE at :275 — which driver FSM state, whether the transaction holds a portMUX or masks interrupts), the Wire library wrapper (timeout behavior, lock scope across a transaction), the idle task / port_systick sources as needed to re-check §7.1's claims against the lull context
  - src/main_balloon.cpp — the loop (:253-330 region), the 1 Hz OLED refresh block (Wire-0, shared with BMP280), processSensors' BMP280 + GPS UART1 read paths, processCommunications/processPacketHandling ordering
  - src/auto_capture.cpp — the NVS image-ID persistence site (678d4f1): WHEN it writes (capture time? boot? every allocate?) — decides whether NVS ops are lull-reachable
  - src/image_tx_manager.cpp — sendTelemetryBeacon's transmit path (incl. Lever A's drain), the GET_STATUS response's E32 transmit route, the [MEM]/B1/B2 instrumentation block
  - src/e32_lora.cpp — transmit (:207-270 post-Lever-A), the AUX waits
  - .pio/build/esp32-s3-balloon/ — firmware.elf (SHA e09dd034 confirm) + config artifacts (sdkconfig / config/sdkconfig.h) for the TWDT stage-0 subscription question and the I2C/UART config
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-10 gap block (the session-9 extension + the missing-list round-#3 item this task discharges)
  </read_first>
  <action>
  Execute the §8.6 brief and write §9 (NINE — SESSION-9 DEBUG ROUND #3) into the debug doc, following the house evidence convention (file:line quotes, re-derivable paths, honest limits). Six subsections:
  1. PROVENANCE + LULL RECONSTRUCTION. Confirm the deployed ELF SHA256 equals e09dd034a5ab7b904278902106a0348d12c800ad88b03daac5d29029ad0cb5f7 before anything else (record the check; a drifted SHA gets investigated and recorded before any decode is trusted). Then reconstruct the lull from balloon3.log :495-:519 plus the loop structure: enumerate every activity that ran between the last window chunk (:491) and the reset (:523) — the GET_STATUS service + its response transmit, the [BCN] seq=23 beacon, BMP280 Wire-0 reads, the 1 Hz OLED refresh if its tick landed in-window, GPS UART1 polling, loop bookkeeping, Serial prints — each with its cadence and, where the log allows, its timestamp bracket.
  2. LULL-PHASE CANDIDATE ARITHMETIC (the §7.3 standard). For each lull candidate: worst-case duration vs the 10 s stage-1 period, and the four properties — masks interrupts? holds a kernel lock across a blocking wait? suspends the flash cache? feeds or starves IDLE0? The minimum set: (i) each Wire-0 site — read the deployed esp32-hal-i2c-ng.c and name exactly which FSM state produces ESP_ERR_INVALID_STATE at :275 and whether the transaction path holds a spinlock or masks interrupts; compute the OLED full-frame transfer time at the configured Wire clock; (ii) GPS UART1 reads (post-3f2c2c6 pin-41; the poll loop's structure); (iii) NVS/flash ops reachable in a lull (name when auto_capture.cpp's NVS write actually fires and whether any flash write window exists here); (iv) the two TX-light radio sites (beacon incl. Lever A's bounded drain; GET_STATUS response); (v) Serial.printf. Disposition each ELIMINATED (with the eliminating property) or SUSPECT (with the keeping property). If the INVALID_STATE error's own semantics (driver-state rejection, not bus timeout) change what a Wire transaction can do to CPU0, say so — that is §8.6 question 2's factual foundation.
  3. CRASH-CONTEXT DETERMINISM (§8.6 question 1). Tabulate the three expressions (session 7 first-post-boot push; session 8 mid-service chunk 1/14 of re-armed window 7; session 9 lull t=124026 ms) against the shared constants (TG0WDT stage-1, PC in the tick-ISR chain, zero project frames, loopTask healthy, [MEM] healthy). Then the explicit hypothesis table: lull-phase starver vs service-phase starver vs phase-independent starver (an intermittent interrupt-masking/delivery fault that fires stochastically or accumulates regardless of TX load) — each row states its predicted discriminator signature for 01-31 and its verdict on whether IDLE0-starvation-on-CPU0 remains the best fit. Reconcile with §7.4's one-family verdict and §8.5's three-PC table.
  4. THE CONVERGENCE VERDICT (§8.6 question 2). Rank readings (a) driver-state corruption as co-effect, (b) I2C bus wedge with interrupts masked on CPU0, (c) coincidence — with the evidence for/against each (direct adjacency, own-line mojibake, no successful read after, the outwardly-healthy lull, the t=114s starvation back-computation from the 10 s stage-1 period). State what each reading predicts for the round-#3 instruments. Also answer §8.6 question 3 (why the corruption moved adjacent: session 8's single mojibake ~1070 lines from reset vs session 9's ON the fatal line) — terminal-phase degradation vs coincidence, bounded by the one-sample limit.
  5. RANKED ROOT CAUSE. One ranked cause with evidence for/against and the honest limit (the 01-25 §3 convention: if the mechanism cannot be uniquely confirmed pre-bench, say so by elimination + blast-radius reasoning ONLY). If the honest result is again elimination-only, §9 says so explicitly — that outcome routes Task 2 to instrument-first.
  6. DISCRIMINATOR MENU DISPOSITION + NAMED INSTRUMENTS/LEVER. Disposition each §8.6 menu item: the CPU0 idle-observability counter (sampled from the existing 1 Hz block — bounded, latch-guarded), I2C-bus health instrumentation at the Wire-0 failure site (transaction count + driver FSM state at failure, bounded), the TWDT stage-0 subscription check (from the build's config artifacts: is stage-0 enabled, which tasks subscribed — turning §7.1's inferred-from-silence stage-0 death into an observable). Name the log line(s) 01-31 greps (the [MEM]/B1/B2 naming convention with the G-01-10 citation), each with its removal condition. If §9 named a fix lever, specify it and its site; if not, say the round ships instrumentation only.
  Record every framework-source and config claim with its file path so the checker can re-derive it.
  </action>
  <verify>
  <automated>grep -q "^## 9" .planning/debug/d1-crash-regression-push-start.md && grep -qi "lull" .planning/debug/d1-crash-regression-push-start.md && grep -qi "INVALID_STATE" .planning/debug/d1-crash-regression-push-start.md && grep -qi "coincidence" .planning/debug/d1-crash-regression-push-start.md && grep -qi "e09dd034" .planning/debug/d1-crash-regression-push-start.md</automated>
  </verify>
  <acceptance_criteria>
  - Debug doc §9 exists with all six subsections; every session-9 citation is locatable in balloon3.log/base3.log by line number; the ELF SHA check is recorded before any decode claim
  - Every candidate in the lull minimum set carries duration arithmetic and the four properties with an ELIMINATED-or-SUSPECT disposition — the same standard §7.3 applied to the service set, including the deployed-driver ESP_ERR_INVALID_STATE semantics read from source with its path cited
  - The three-expression determinism table and the lull/service/phase-independent hypothesis table are present, each hypothesis row carrying a predicted 01-31 discriminator signature
  - The convergence reading is ranked across (a)/(b)/(c) with evidence, never explained away; the adjacency + own-line mojibake + no-successful-read facts are carried; the proximity question is answered or its one-sample limit stated
  - The §8.6 discriminator menu is dispositioned item-by-item; the round's named log lines are specified with the G-01-10 citation and a removal condition; the ranked cause states the honest limit (or explicitly records elimination-only)
  </acceptance_criteria>
  <done>§9 lands in the debug doc: the lull phase is reconstructed and every candidate carries the four-property arithmetic, the three expressions are reconciled into a determinism table with named hypotheses, the I2C+mojibake convergence is ranked, and the round's instrumentation (and lever, if evidence-named) is specified with bench discriminators.</done>
  <reversibility rating="reversible">Documentation-only audit record; no code or config changes in this task.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fix-shape disposition checkpoint — select the round-#13 lever/instrument set from Task 1's ranked evidence</name>
  <files>.planning/debug/d1-crash-regression-push-start.md</files>
  <read_first>
  - .planning/debug/d1-crash-regression-push-start.md — §9 (Task 1's ranked cause, the discriminator-menu disposition, the named lever/instrument candidates); §5 and §7's FIX SHAPE SELECTED records (the checkpoint-record convention)
  - .planning/phases/01-command-protocol-control/01-28-SUMMARY.md — the round-#12 auto-advance provenance (this round's record joins the FOUR existing pending operator confirmations as the fifth)
  </read_first>
  <what-built>
  Task 1's §9 audit: the lull-phase reconstruction + candidate arithmetic, the crash-context determinism table, the §8.6 convergence verdict, the discriminator-menu disposition, and the ranked root cause (or the honest elimination-only record) with named instrument/lever options.
  </what-built>
  <how-to-verify>
  Decide the fix shape from the evidence, then record the selection in a "FIX SHAPE SELECTED (round #13 Task 2 checkpoint record)" subsection closing §9 (the §5/§7 convention):
  - Option (a): implement the evidence-named lever — only when §9 names a concrete mechanism site (a blocking/locking/masking stretch whose removal provably targets the ranked cause).
  - Option (b): instrument-only — ship the §9-dispositioned diagnostics (CPU0 idle observability, I2C health at the failure site, TWDT stage-0 check) and let 01-31's bench discriminate. The HONESTY GUARD: two consecutive lever fixes are bench-disconfirmed (01-25 warm-up, session 8; 01-28 Lever A, session 9) — if §9 again reaches only elimination + blast radius with no named site, option (b) is the evidence-honest selection, never a third speculative lever.
  - Option (a)+(b) combined is legitimate when the lever is named AND the mechanism confirmation needs the instruments (the 01-25/01-28 shape).
  Under workflow.auto_advance=true the house convention (01-23/01-25/01-26/01-28) auto-selects the evidence-supported option and records the selection + provenance as pending end-of-phase operator confirmation, joining the four existing pending confirmations.
  </how-to-verify>
  <acceptance_criteria>
  - The checkpoint record exists in the debug doc, naming the selected option, the evidence that selected it, and (if auto-advanced) the provenance line per the 01-23/01-25/01-26/01-28 convention
  - The selection maps to §9's evidence: a lever selection without a §9-named mechanism site is a failure of this task, not a preference; an instrument-only selection names which §9 menu items ship
  </acceptance_criteria>
  <resume-signal>Type "approved" with the selected option (and the operator's own reasoning if overriding the evidence-ranked one), or route back with what the evidence fails to support.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Implement the selected fix/instrumentation + bench discriminators; builds + harness; ledgers honestly extended (no flips)</name>
  <files>src/image_tx_manager.cpp, src/main_balloon.cpp, src/e32_lora.cpp, include/e32_lora.h, include/image_protocol.h, platformio.ini, .planning/WINDOWS.md, .planning/phases/01-command-protocol-control/01-UAT.md</files>
  <read_first>
  - .planning/debug/d1-crash-regression-push-start.md — §9 (the ranked cause + the Task 2 selection + the named discriminator lines this task ships); §7.6 (Lever A + B1/B2 — the retained surfaces this task must not disturb)
  - The fix/instrumentation-site files §9 names (of the candidates in files_modified — read the exact functions §9 fingers, plus their headers)
  - src/command_sender.cpp — the protected quiet-gate surfaces (channelQuietForTx region, CMD_TX_CHANNEL_QUIET_MS 750 in include/command_sender.h) this task must not touch
  - scripts/verify_protocol_roundtrip.mjs — the wire tripwire that must exit 0 unchanged
  - .planning/WINDOWS.md — entry 15 (extend the reason; stay open; counts stay 2/17/19)
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-10 (root_cause extension + the missing-list round-#3 item to mark DONE)
  </read_first>
  <action>
  Implement the Task 2 selection exactly as §9 specifies it:
  1. The fix and/or instrumentation at the §9-named site(s), each new construct carrying a code comment citing G-01-10 and the §9 subsection that ranked the mechanism (the house convention — every fix and diagnostic cites its gap and its evidence).
  2. The §9-named discriminator log line(s) — latch-guarded (once per episode, the reannounceHoldLogged convention), greppable by 01-31, bounded, honest (reporting what happened, never asserting health), each with its named removal condition in-source.
  3. Retained surfaces untouched: Lever A (e32_lora.cpp drain block), B1 [BOOT] reset-cause, B2 [LOOP] slow-pass, the [MEM] instrumentation, the round-#10 discriminator lines (budget re-arm, class-5 eviction labels, base deadline release), the D2 receipt-ever flag — their removal conditions are unchanged.
  4. Protected surfaces byte-identical: no wire-format change (the harness is the proof), no quiet-gate constant change (CMD_TX_CHANNEL_QUIET_MS stays 750 in include/command_sender.h), no D-05/D-07 pacing change, no command-path semantic change, no WDT-config lever (§7.6 rejection stands) — unless §9's evidence explicitly named one as the root cause (then the change is exactly what §9 specifies and the SUMMARY says so).
  5. Verify: pio run -e esp32-s3-balloon -e esp32-s3-basestation (2/2 SUCCESS) and node scripts/verify_protocol_roundtrip.mjs (exit 0) on the round's final commit.
  6. Ledgers: extend WINDOWS entry 15's reason with the round-#13 determination (entry stays open; counts unchanged 2/17/19) and 01-UAT.md G-01-10's root_cause + missing list (mark the debug-round-#3 item DONE with §9's verdict; status stays open). Both ledger updates quote §9's verdict; neither claims the crash fixed at code level.
  7. Commit fix(01-30): D1 debug round #3 — <the §9-named lever/instrument set>, citing the debug doc.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation 2>&1 | grep -c "SUCCESS" | grep -qv "^0$" && node scripts/verify_protocol_roundtrip.mjs && grep -q "CMD_TX_CHANNEL_QUIET_MS = 750" include/command_sender.h && grep -q "open_count: 2" .planning/WINDOWS.md && grep -qi "round-#13\|round #13" .planning/WINDOWS.md</automated>
  </verify>
  <acceptance_criteria>
  - Both firmware targets build SUCCESS and the harness exits 0 on the round's final commit
  - The §9-named fix/instrumentation landed at the §9-named site with a G-01-10-citing comment; every §9-named discriminator line exists in source (grep finds it verbatim) with its removal condition in-source
  - CMD_TX_CHANNEL_QUIET_MS is still 750; the harness (wire format) unchanged; Lever A, B1/B2, [MEM], the round-#10 discriminator lines, and the D2 receipt-ever flag still present in source
  - WINDOWS entry 15's reason carries the round-#13 determination and the entry is still open (front-matter counts still 2/17/19); 01-UAT.md G-01-10 still open with the extended root_cause and the round-#3 missing item marked DONE
  - The SUMMARY makes NO bench claims — the round's hardware acceptance belongs to 01-31
  </acceptance_criteria>
  <done>The round-#13 selection is wired at the evidence-named site(s) with its discriminators shipped, both targets and the harness are green, and the ledgers carry the audit honestly open pending the 01-31 hardware session.</done>
  <reversibility rating="costly">The touched sites sit in the balloon's live sensor/radio loop; a wrong lever or a perturbing instrument would surface only at the bench — hence the Task 2 disposition gate before this task, and the bounded/latched instrument requirement.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crash evidence -> root cause | Retained consoles (balloon3.log/base3.log) + the deployed ELF are the only ground truth; framework sources/config are read-only reference material — no untrusted input enters the firmware in this round |
| fix/instrumentation -> radio + sensor loop | The changes sit in the live balloon loop every future transfer depends on; the wire harness + protected-surface greps are the regression bound, and new instruments are bounded/latched so they cannot perturb timing into a new failure mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-30-01 | Tampering | evidence integrity | medium | mitigate | §9 claims carry file:line quotes and framework-source paths the checker can re-derive; the ELF SHA e09dd034 is re-confirmed before any decode is trusted (the §6.1/§8.1 convention) |
| T-01-30-02 | DoS | a third speculative fix masking the crash | high | mitigate | Task 2's evidence-gated disposition + the instrument-only honesty guard (two lever disconfirmations are the bound); the §9-named discriminator lines make masking visible at 01-31 |
| T-01-30-03 | DoS | fix/instrumentation regresses the wire/quiet-gate surfaces | high | mitigate | Protected-surface negative greps + harness exit 0 as the task's automated gate; the 01-28 prohibition carried verbatim |
| T-01-30-04 | DoS | new instrumentation perturbing the timing it observes | medium | mitigate | Every instrument bounded (transaction-count/state lines, not continuous prints), latch-guarded, and carried with a removal condition; the bench plan reads them as evidence, never as health assertions |
| T-01-30-05 | Safety | bench hardware | low | accept | No hardware work in this plan (code + docs only); the bench rig risk stays with 01-31's protocol |

## Artifacts this phase produces

- Debug doc §9 (round-#13 audit) + the Task 2 checkpoint record — .planning/debug/d1-crash-regression-push-start.md
- The fix/instrumentation: modified functions at the §9-named site(s) within src/main_balloon.cpp / src/image_tx_manager.cpp / src/e32_lora.cpp / include/e32_lora.h / include/image_protocol.h / platformio.ini (the touched subset named in §9 and the SUMMARY) — new named discriminator log line(s) in the instrumented path(s)
- WINDOWS entry 15 reason extension; 01-UAT.md G-01-10 root_cause extension + missing-list round-#3 DONE marker
- 01-30-SUMMARY.md
- No new modules, no env vars, no wire-format changes

<verification>
- §9 complete with the six subsections; every claim re-derivable (config path or file:line quoted); the ELF SHA check recorded
- Final commit builds 2/2 and the harness exits 0; protected-surface greps clean; retained discriminator surfaces intact
- WINDOWS 15 / G-01-10 open with the round-#13 determination recorded — zero code-level flips
</verification>

<success_criteria>
- The lull-phase axis is converted from an unaudited blind spot into a reconstructed window + dispositioned candidates + a determinism table + a ranked cause (or an honest instrument-first record when the mechanism cannot be uniquely confirmed pre-bench)
- The §8.6 three questions are answered with evidence, and the round's instruments/lever are wired green with their 01-31 discriminators named
- Every riding truth's gate is now the single 01-31 bench session
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-30-SUMMARY.md` when done
</output>
