---
phase: 01-command-protocol-control
plan: "06"
type: execute
wave: 4
depends_on: ["01-05"]
gap_closure: true
files_modified:
  - src/command_protocol.cpp
  - include/command_protocol.h
  - src/command_handler.cpp
  - include/command_handler.h
  - src/command_sender.cpp
  - src/main_basestation.cpp
  - scripts/verify_protocol_roundtrip.mjs
autonomous: true
requirements: [CTRL-01, CTRL-02, CTRL-06, PRI-02]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "LoRa command packets transmitted from base station to balloon and acknowledged (SC-2 protocol-conformance clause, gap 1): every successful balloon response — every ACK and every STATUS — is built by createResponsePacket and serialized with header type byte 0x11 (PACKET_TYPE_RESPONSE), matching the documented contract (include/command_protocol.h ResponsePacket comment, common_types.h RESPONSE=0x11) and identical to the NACK path; command packets carry 0x10 by assignment in createCommandPacket, not only by serializer hardcoding"
    - "The host wire-format harness models the real response construction path (WR-05 closed): its createResponsePacket transcription feeds a serializer mirror that emits the constructed packet's type byte, and a dedicated clause asserts serialized byte 2 equals 0x11 for both the ACK and NACK paths while a defective no-type-assignment variant provably yields 0x00 — the harness can no longer certify a type byte the firmware does not emit"
    - "Balloon status reporting is truthful (plan 01-04 must-have artifact, gap 2): GET_STATUS currentResolution is derived from the real esp32-camera framesize_t via a name-based reverse mapping (frameSizeFromEsp), so the boot default (real QVGA) reports as project QVGA 320x240, not QQVGA 160x120; the resolution UI option for protocol value 8 is labeled CIF 400x296 and executes real FRAMESIZE_CIF, so it succeeds on the OV2640 instead of always NACKing (WR-01 resolved)"
    - "Failed commands are retried with timeout and user is notified (SC-4 accounting integrity, gap 3): a duplicate or late response for a command already in a terminal state (ACKED/FAILED/TIMEOUT) is discarded by CommandSender::handleResponse before any state transition, counter change, or response overwrite — pendingCommandCount decrements exactly once per command, so the normal retry-edge duplicate ACK (retryCommand retransmitting the same sequenceNumber at the D-05 window edge) can no longer underflow the uint8 counter and latch hasPendingCommands() true forever"
  artifacts:
    - path: "src/command_protocol.cpp"
      provides: "createResponsePacket assigns packet.type = PACKET_TYPE_RESPONSE and createCommandPacket assigns packet.type = PACKET_TYPE_COMMAND (struct-level symmetry with the serializers); createACK/createNACK/createStatus use the shared constant instead of inline casts"
      contains: "PACKET_TYPE_RESPONSE"
    - path: "include/command_protocol.h"
      provides: "PACKET_TYPE_RESPONSE constant next to PACKET_TYPE_COMMAND; FrameSize value 8 renamed to the CIF name with a truthful comment stating the codes are protocol-internal and mapped by name, not numeric matches"
      contains: "FRAMESIZE_CIF"
    - path: "src/command_handler.cpp"
      provides: "frameSizeFromEsp name-based reverse mapping (real framesize_t to project FrameSize) used by handleGetStatus; framesizeFromInt maps the renamed code to real FRAMESIZE_CIF"
      contains: "frameSizeFromEsp"
    - path: "include/command_handler.h"
      provides: "frameSizeFromEsp declaration next to framesizeFromInt"
      contains: "frameSizeFromEsp"
    - path: "src/command_sender.cpp"
      provides: "Terminal-state guard at the top of handleResponse: ACKED/FAILED/TIMEOUT slots return before response storage or counter changes"
      contains: "CommandState::TIMEOUT"
    - path: "src/main_basestation.cpp"
      provides: "Resolution select option for value 8 labeled with the true 400x296 mode name (CIF)"
      contains: "CIF 400x296"
    - path: "scripts/verify_protocol_roundtrip.mjs"
      provides: "createResponsePacketMirror transcription + serializer mirror emitting the packet's type field + clause (f) asserting byte 2 == 0x11 on ACK and NACK paths with defective-variant teeth"
      contains: "createResponsePacketMirror"
  key_links:
    - from: "src/command_handler.cpp CommandHandler::process success responses"
      to: "createResponsePacket -> CommandProtocol::serializeResponse -> wire byte 2"
      via: "every ACK/STATUS traverses this chain; the factory's packet.type is the byte the serializer emits verbatim (command_protocol.cpp:159)"
      pattern: "createResponsePacket"
    - from: "src/camera_manager.h getFrameSize (real esp32-camera framesize_t)"
      to: "src/command_handler.cpp handleGetStatus -> ResponseStatusData.currentResolution"
      via: "frameSizeFromEsp name-based reverse mapping replaces the raw cross-enum numeric reinterpretation"
      pattern: "frameSizeFromEsp"
    - from: "src/command_sender.cpp deserializeResponse output"
      to: "handleResponse terminal-state guard"
      via: "duplicate/late responses for terminal slots return before pendingCommandCount or statistics change"
      pattern: "CommandState::ACKED || cmd->state == CommandState::FAILED"
  prohibitions:
    - statement: "MUST NOT report fabricated protocol or camera state: every path that constructs a response packet must carry its documented packet type (0x11 RESPONSE) onto the wire, and GET_STATUS fields must reflect actual sensor/module state through real conversions — never a raw numeric reinterpretation across differently-numbered enums"
      status: unverified
      flagged: true
      verification: judgment
---

<objective>
Close the three verifier-confirmed response-path criticals from re-verification #2 (01-VERIFICATION.md, commit 11ca491; sourced from 01-REVIEW.md ca682b4): (1) createResponsePacket never assigns packet.type, so every ACK/STATUS goes on the wire as 0x00 instead of the documented 0x11, and the regression harness masks this by hardcoding 0x11; (2) handleGetStatus raw-casts between differently-numbered FrameSize enums, so boot QVGA reports as 160x120, and the same mislabeled enum makes the UI value-8 resolution option permanently NACK; (3) handleResponse has no terminal-state guard, so the designed retry flow's duplicate ACK at the 2s window edge double-decrements the uint8 pendingCommandCount (0 to 255) and latches hasPendingCommands() true forever.

Purpose: all three sit inside phase must-have truth domains — SC-2 protocol conformance (the command/ACK protocol specification is a phase deliverable), the truthful-status artifact plan 01-04 claimed, and SC-4's notification integrity (CTRL-06/PRI-02). All are deterministic, code-only, hardware-free fixes sharing one theme (response-path correctness); the verifier recommended closing them in a single focused plan with the faithful harness clause.
Output: both firmware targets green, the wire-format harness green with the new faithful response-construction clause, every successful response conforming to the documented wire format, truthful GET_STATUS resolution, and duplicate-response-safe command accounting.
</objective>

<execution_context>
@C:/Users/Amp/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Amp/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/phases/01-command-protocol-control/01-REVIEW.md

Ground truth for the enum fix (real framesize_t numbering, pinned library):
- .pio/libdeps/esp32-s3-balloon/esp32-camera/driver/include/sensor.h — the real enum the camera reports (QQVGA=1, QVGA=5, CIF=6, HVGA=7, VGA=8, SVGA=9, XGA=10, HD=11, SXGA=12, UXGA=13); project FrameSize codes are 5..13 with different meanings
</context>

<tasks>

<!-- planner-discipline-allow: FRAMESIZE_QXGA -->

<task type="auto">
  <name>Task 1: Response packet-type conformance on the real construction path + faithful harness clause (gap 1, CR-01/WR-05)</name>
  <read_first>
  - src/command_protocol.cpp (createCommandPacket ~353-369; createResponsePacket ~371-387; serializeCommand ~44-91 — note the hardcoded type byte at ~60; serializeResponse ~144-188 — note it emits resp.type verbatim at ~159; createACK ~237-252, createNACK ~254-271, createStatus ~273-286 — the three inline 0x11 casts)
  - include/command_protocol.h (constants block at line 14; ResponsePacket struct ~90-100 with the "0x11 for responses" contract comment)
  - include/common_types.h (PacketType enum ~37-38: COMMAND = 0x10, RESPONSE = 0x11)
  - src/command_handler.cpp (~90-110 — CommandHandler::process builds ALL successful responses through createResponsePacket, then sendResponse serializes them)
  - scripts/verify_protocol_roundtrip.mjs (whole file — the mirror contract in the header comment; serializeResponse mirror ~126-160 with the hardcoded type constant at ~139; existing clause structure (a)-(e); the defective-variant pattern used for clause (d) as the model for the new teeth)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md (gaps[0] — reason and missing list)
  - .planning/phases/01-command-protocol-control/01-REVIEW.md (CR-01 and WR-05 findings with the fix sketch)
  </read_first>
  <files>src/command_protocol.cpp, include/command_protocol.h, scripts/verify_protocol_roundtrip.mjs</files>
  <reversibility rating="reversible">Small additive edits to factory functions plus a harness extension; the wire-format change aligns the emitted byte with the already-documented contract, and both endpoints ignore the type byte today, so the change is behavior-preserving for the current pair and trivially revertible.</reversibility>
  <action>
  Gap 1 closure — the packet-type field must be set where the packet is constructed, not only where some serializers hardcode it:

  1. include/command_protocol.h: next to the existing PACKET_TYPE_COMMAND constant (line 14), add the sibling constant PACKET_TYPE_RESPONSE for 0x11, same static constexpr style.
  2. src/command_protocol.cpp createResponsePacket (~371-387): immediately after the zero-initialized declaration, assign packet.type = PACKET_TYPE_RESPONSE as the first field set, so every ACK and STATUS built through this factory (the entire success path of CommandHandler::process) carries the documented response type. In the same function's twin createCommandPacket (~353-369), assign packet.type = PACKET_TYPE_COMMAND for symmetry — the command type is currently correct on the wire only because serializeCommand hardcodes the byte, which is the same latent asymmetry the verifier flagged.
  3. Replace the three inline casts to 0x11 inside createACK (~239), createNACK (~256), and createStatus (~275) with the new shared constant so all four response factories derive the type from one definition.
  4. Harness faithfulness (WR-05): in scripts/verify_protocol_roundtrip.mjs, restructure the response mirror so it models the real construction path instead of hardcoding the type constant at the type-byte position (~139). Add a createResponsePacketMirror function transcribing the C++ factory: start from a zero-initialized packet object (type 0), set responseType, refSequence, clamped data/dataLength and end markers, and — unless a defective option is requested, modeling the pre-fix omission — set type to PACKET_TYPE_RESPONSE exactly as the fixed firmware does. Change the serializeResponse mirror to accept that packet object and emit the packet's own type field at byte 2, mirroring the firmware serializer emitting resp.type verbatim. Route the existing response-mirror call sites (clauses (c) and (e)) through the factory so nothing bypasses it.
  5. New clause (f), appended after clause (d)'s block with the same assert style: (f1) serialize a fixed-factory ACK (ResponseType ACK 0x01, a refSequence, small data) and assert byte 2 of the serialized packet equals 0x11; (f2) same for a NACK (a NACK responseType with a short message as data) — assert byte 2 equals 0x11; (f3) assert the ACK-path and NACK-path type bytes are identical; (f4) defective variant — build via the factory with the type assignment omitted and assert byte 2 equals 0x00 and differs from the fixed path, proving the clause has teeth against the CR-01 defect. Also assert the fixed ACK packet passes validateCRC (the type byte is inside the CRC-covered region). Update the file-header clause list comment to document (f).

  Interoperability note (do not change receivers): deserializeResponse reads buffer[2] into resp.type but never validates it, so the base station keeps accepting responses; the fix conforms the wire to the documented contract rather than altering any receiver.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation && node scripts/verify_protocol_roundtrip.mjs && grep -q "packet.type = PACKET_TYPE_RESPONSE" src/command_protocol.cpp && grep -q "packet.type = PACKET_TYPE_COMMAND" src/command_protocol.cpp && grep -q "PACKET_TYPE_RESPONSE" include/command_protocol.h && ! grep -q "static_cast<PacketType>(0x11)" src/command_protocol.cpp && grep -q "createResponsePacketMirror" scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - src/command_protocol.cpp contains packet.type = PACKET_TYPE_RESPONSE in createResponsePacket and packet.type = PACKET_TYPE_COMMAND in createCommandPacket (positive greps in the verify command)
  - include/command_protocol.h defines PACKET_TYPE_RESPONSE next to PACKET_TYPE_COMMAND; zero occurrences of the inline 0x11 cast remain in src/command_protocol.cpp (negative grep in the verify command)
  - scripts/verify_protocol_roundtrip.mjs contains createResponsePacketMirror; its serializer mirror no longer hardcodes the type byte at the construction-agnostic position; clause (f) asserts byte 2 == 0x11 for ACK and NACK paths and 0x00 for the defective variant; node scripts/verify_protocol_roundtrip.mjs exits 0 with all clauses passing
  - Both pio environments report SUCCESS (the header change is shared by both builds)
  </acceptance_criteria>
  <done>Every successful balloon response is serialized with header type 0x11 on the exact path CommandHandler uses, commands carry 0x10 by assignment, and the regression harness proves it by transcribing the real factory — the protocol deliverable conforms to its documented wire format on the entire success path.</done>
</task>

<task type="auto">
  <name>Task 2: Truthful GET_STATUS resolution via name-based reverse mapping + enum relabel (gap 2, CR-02/WR-01)</name>
  <read_first>
  - src/command_handler.cpp (handleGetStatus ~572-595 — the raw cross-enum reinterpretation at ~579; framesizeFromInt ~703-735 — the forward by-name mapping to keep symmetric with)
  - include/command_protocol.h (FrameSize enum block ~57-68 — the false comment above it and the value-8 enumerator with the 400x296 comment)
  - include/command_handler.h (the framesizeFromInt declaration — where the new reverse-mapping declaration goes)
  - src/camera_manager.h (~line 109 — getFrameSize returns the real framesize_t from the pinned esp32-camera library, NOT the project FrameSize)
  - .pio/libdeps/esp32-s3-balloon/esp32-camera/driver/include/sensor.h (~lines 83-102 — authoritative real numbering: QQVGA=1, QCIF=2, HQVGA=3, 240X240=4, QVGA=5, CIF=6, HVGA=7, VGA=8, SVGA=9, XGA=10, HD=11, SXGA=12, UXGA=13)
  - src/main_basestation.cpp (~606-622 — the resolution select; the value-8 option whose label names a quad-xga size it does not deliver)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md (gaps[1] — reason and missing list)
  - .planning/phases/01-command-protocol-control/01-REVIEW.md (CR-02 and WR-01 findings with the reverse-mapping sketch)
  </read_first>
  <files>src/command_handler.cpp, include/command_protocol.h, include/command_handler.h, src/main_basestation.cpp</files>
  <reversibility rating="reversible">A same-value enumerator relabel (wire code 8 unchanged), one new pure mapping function, one call-site substitution, and one UI label string — all revertible; the protocol wire format does not change.</reversibility>
  <action>
  Gap 2 closure — the project FrameSize codes must be translated to and from the real esp32-camera enum by NAME, never by numeric reinterpretation:

  1. include/command_protocol.h FrameSize block (~57-68): rewrite the block comment to state these are protocol-internal wire codes whose numeric values deliberately differ from the esp32-camera framesize_t and are translated by name in CommandHandler (framesizeFromInt forward, frameSizeFromEsp reverse). Rename the value-8 enumerator from FRAMESIZE_QXGA to FRAMESIZE_CIF — value stays 8 and the trailing 400x296 comment stays, because 400x296 is the real CIF mode, while the real quad-xga mode is 2048x1536 and unreachable on the OV2640.
  2. src/command_handler.cpp framesizeFromInt (~703-735): update the renamed case so project FRAMESIZE_CIF maps to real FRAMESIZE_CIF (the true 400x296 mode) instead of mapping to the unsupported 2048x1536 mode.
  3. Add the reverse mapping: declare FrameSize frameSizeFromEsp(framesize_t espFrameSize) const in include/command_handler.h next to the framesizeFromInt declaration, and define it in the Camera Helpers section of src/command_handler.cpp — a switch over the REAL enum by name returning the project code of the same name for the nine supported sizes (QQVGA, QVGA, HQVGA, CIF, VGA, SVGA, XGA, SXGA, UXGA); the default case returns FrameSize::FRAMESIZE_QVGA (the boot default) for real sizes that have no protocol code (96x96, QCIF, 240x240, HVGA, HD), with a comment stating that fallback explicitly.
  4. src/command_handler.cpp handleGetStatus (~579): replace the raw numeric reinterpretation of camera->getFrameSize() with a call to frameSizeFromEsp — the value stored in ResponseStatusData.currentResolution must be the project code whose NAME matches the camera's real framesize, so a balloon at the boot default (real QVGA, 320x240) reports project QVGA, not the 160x120 code.
  5. src/main_basestation.cpp resolution select (~616): relabel the value-8 option to name the true mode it now executes — CIF at 400x296 — so the operator is no longer offered an option that always NACKs with a misleading failure message. No other UI change.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation && node scripts/verify_protocol_roundtrip.mjs && grep -q "frameSizeFromEsp(camera->getFrameSize())" src/command_handler.cpp && grep -q "frameSizeFromEsp" include/command_handler.h && ! grep -rq "FRAMESIZE_QXGA" src include && grep -q "CIF 400x296" src/main_basestation.cpp && ! grep -q "QXGA 400x296" src/main_basestation.cpp && ! grep -qi "matching esp_camera" include/command_protocol.h</automated>
  </verify>
  <acceptance_criteria>
  - src/command_handler.cpp handleGetStatus calls frameSizeFromEsp(camera->getFrameSize()); the reverse mapping is declared in include/command_handler.h and defined with by-name cases for the nine supported sizes plus the documented QVGA default (positive greps in the verify command)
  - Zero occurrences of the old quad-xga enumerator name anywhere in src/ and include/ (recursive negative grep in the verify command) — the rename is complete: enum definition, framesizeFromInt case, and no other references existed
  - include/command_protocol.h carries the truthful protocol-internal-codes comment; the false matching-claim comment is gone (case-insensitive negative grep in the verify command)
  - src/main_basestation.cpp value-8 option label reads CIF 400x296; the old mislabeled option text is gone (greps in the verify command)
  - A balloon at the boot default reports QVGA 320x240 (real FRAMESIZE_QVGA=5 maps by name to project code 6, not the project code 5 that the old cast produced); the value-8 UI option now executes real FRAMESIZE_CIF and can succeed on the OV2640
  - Both pio environments report SUCCESS and the wire-format harness still exits 0 (the wire codes themselves are unchanged)
  </acceptance_criteria>
  <done>GET_STATUS reports the camera's actual resolution through a name-based translation in both directions, the protocol enum's documentation tells the truth about its own numbering, and every resolution option offered in the UI names a mode the OV2640 can actually produce — the truthful-status artifact claim of plan 01-04 holds again.</done>
</task>

<task type="auto">
  <name>Task 3: Terminal-state guard in CommandSender::handleResponse + full regression battery (gap 3, CR-03)</name>
  <read_first>
  - src/command_sender.cpp (handleResponse ~319-357 — the missing guard; findTrackedCommand ~363-371 — matches any non-IDLE slot including terminal ones; cancelCommand ~148-168 — the OTHER findTrackedCommand caller, whose own decrement guard already no-ops terminal slots, and the reason findTrackedCommand must keep matching them; retryCommand ~428-442 — retransmits with the SAME sequenceNumber, which is what produces the duplicate ACK at the window edge)
  - include/command_sender.h (CommandState enum; TrackedCommand struct ~30-36; pendingCommandCount uint8_t ~101; hasPendingCommands ~72)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md (gaps[2] — reason and missing list; note the UAT item 2 instruction to run the degraded-link test AFTER this fix)
  - .planning/phases/01-command-protocol-control/01-REVIEW.md (CR-03 finding with the guard sketch)
  </read_first>
  <files>src/command_sender.cpp</files>
  <reversibility rating="reversible">One early-return guard in a single function; no state-machine redesign, no wire-format change — trivially revertible.</reversibility>
  <action>
  Gap 3 closure — a terminal command slot must never be transitioned or counted a second time:

  1. src/command_sender.cpp handleResponse (~319-357): immediately after the existing unknown-sequence early return, add a terminal-state guard as a single-line condition — cmd->state == CommandState::ACKED || cmd->state == CommandState::FAILED || cmd->state == CommandState::TIMEOUT — that returns before storing the response (cmd->response / cmd->hasResponse) and before any counter change. Write the condition on one line with a comment above it explaining: duplicate or late response — retryCommand retransmits with the same sequenceNumber, so at the D-05 window edge a late first ACK and the re-execution's second ACK can both arrive; the first one already performed the counted transition and this one must be discarded (a second decrement underflows the uint8 pendingCommandCount from 0 to 255 and latches hasPendingCommands() true forever).
  2. Placed BEFORE the response storage so a terminal slot keeps the response that actually caused its transition, not the duplicate.
  3. Do NOT restrict findTrackedCommand to PENDING/SENT slots: cancelCommand (~148-168) legitimately resolves terminal slots (its own decrement guard no-ops them), and the UI state/queue queries read terminal slots. The guard belongs in handleResponse only — the one consumer whose job is to perform transitions.

  Then run the full regression battery for the plan: both firmware targets and the extended wire-format harness from Task 1.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation && node scripts/verify_protocol_roundtrip.mjs && sed -n '/void CommandSender::handleResponse/,/^}/p' src/command_sender.cpp | grep -q "CommandState::ACKED || cmd->state == CommandState::FAILED || cmd->state == CommandState::TIMEOUT"</automated>
  </verify>
  <acceptance_criteria>
  - The body of CommandSender::handleResponse contains the single-line terminal-state guard on ACKED/FAILED/TIMEOUT returning before response storage and counter updates (region-scoped grep in the verify command)
  - cancelCommand, findTrackedCommand, retryCommand, and the process() timeout/retry branches are unchanged (guard-only fix; their prior-verified mechanics stay intact)
  - The retry-edge scenario is safe by construction: first ACK transitions SENT to ACKED and decrements once; the late duplicate ACK hits the guard and returns — pendingCommandCount can never decrement twice for one command, so the uint8 counter cannot underflow and hasPendingCommands() cannot latch true
  - Both pio environments report SUCCESS; node scripts/verify_protocol_roundtrip.mjs exits 0 (full battery green for the whole plan)
  </acceptance_criteria>
  <done>A duplicate or late response for a terminal command is silently discarded with a debug path preserved for unknown sequences only — SC-4's accounting integrity holds under the designed retry flow, unblocking the hardware UAT degraded-link test (verifier note: run that UAT item after this fix, since the duplicate-ACK race is most likely exactly there).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Balloon response construction -> LoRa wire | The on-wire packet-type field is a routing datum for any type-filtering consumer (Phase 2 image pipeline sharing the channel, sniffers); it must carry its documented value on every construction path |
| Balloon camera state -> GET_STATUS report -> operator | The status report is the operator's (and Phase 2's) view of camera state; it must reflect the real sensor state, not a numeric reinterpretation |
| LoRa RF -> CommandSender accounting -> operator notification | Duplicate/late RF deliveries must not corrupt the pending-command accounting that drives the UI queue panel and link LED |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-17 | Tampering | createResponsePacket -> serializeResponse type byte (src/command_protocol.cpp) | high | mitigate | Factory assigns PACKET_TYPE_RESPONSE so every ACK/STATUS carries the documented 0x11; harness clause (f) asserts serialized byte 2 for both ACK and NACK paths and proves the defective variant is detectable — the wire conforms to the protocol contract on the entire success path |
| T-01-18 | Information Disclosure | handleGetStatus resolution field (src/command_handler.cpp) | medium | mitigate | Name-based reverse mapping (frameSizeFromEsp) replaces the cross-enum numeric reinterpretation; boot QVGA reports truthfully; the value-8 UI option executes real FRAMESIZE_CIF so the operator is no longer offered a mode that always fails |
| T-01-19 | DoS | CommandSender pending-command accounting (src/command_sender.cpp) | high | mitigate | Terminal-state guard discards duplicate/late responses before any counter change; the uint8 pendingCommandCount cannot underflow, so hasPendingCommands() and the queue panel cannot be permanently corrupted by the normal retry-edge duplicate ACK |
</threat_model>

<spec_gates_record>
Visible, recorded gate choices for this cycle:

- Spec-less edge-probe fallback: VISIBLY SKIPPED — no new requirement IDs to probe this run (all six phase IDs were probed and resolved in rounds 1-2); no probe predicates generated. The three gaps' edge semantics (response type conformance, name-based resolution truthfulness, exactly-once response accounting) are carried directly as must_haves truths sourced from the structured verification gaps.
- Prohibitions fallback: VISIBLY SKIPPED (same reason). The truthfulness prohibition (no fabricated protocol or camera state) is authored descriptor-less into must_haves.prohibitions, flagged-unverified for human review at UAT — gap 2 violated its spirit, this plan restores it.
- UI-SPEC ## UI Considerations: 7 explicit truths + 1 backstop remain carried by executed plan 01-03 (SC-1 verified by re-verification); this plan's only UI-surface change is the resolution option label truth fix (one string, folded into the gap-2 truth) — the UI-SPEC's own constraint that form options mirror firmware-validated modes is what this label correction restores. No other surface touched — no re-lift, no silent drop.
- Assumption-delta detector: detected=false — in-place fixes to delivered modules; the enumerator rename is a same-value relabel of a protocol-internal code with no wire-format change and no second instance of anything introduced.
- API coverage: no external APIs — firmware-internal protocol factories and a first-party host harness only.
- Schema push gate: skipped — no ORM/schema files exist in this ESP32/PlatformIO firmware project.
</spec_gates_record>

<verification>
- pio run -e esp32-s3-balloon -e esp32-s3-basestation — both SUCCESS after all three fixes
- node scripts/verify_protocol_roundtrip.mjs — exit 0, all clauses including the new (f) passing
- Source gates: packet.type assignments present in both factories; shared PACKET_TYPE_RESPONSE constant used (no inline 0x11 casts left in src/command_protocol.cpp); frameSizeFromEsp wired into handleGetStatus and declared in the header; old enumerator name absent project-wide; mislabeled UI option text absent; false enum comment absent; terminal-state guard present inside handleResponse
- System-level semantics code-proven: every successful response conforms to the documented wire format on the real construction path; GET_STATUS resolution is name-mapped in both directions; pendingCommandCount changes exactly once per command
- Runtime behavior over the radio link remains hardware UAT items 1-4 in 01-VERIFICATION.md — this plan is what makes UAT item 2 (degraded-link retry) meaningful, per the verifier's note that the duplicate-ACK race is most probable on a slow link
</verification>

<success_criteria>
- Gap 1 closed: the entire success path (every ACK and STATUS) serializes with header type 0x11, matching the NACK path and the documented contract; the harness certifies the real construction path, not a hardcoded constant (WR-05)
- Gap 2 closed: GET_STATUS reports the camera's actual resolution via name-based mapping (boot QVGA reports as QVGA 320x240); the value-8 resolution option executes real CIF 400x296 and succeeds on the OV2640 (WR-01 resolved); the enum comment tells the truth
- Gap 3 closed: duplicate/late responses for terminal commands are discarded before any accounting change — the retry-edge double-decrement and uint8 underflow are impossible; the UI pending count and statistics stay consistent under the designed retry flow (CTRL-06/PRI-02 defect removed)
- Both firmware targets green; extended wire-format harness green
- Phase 1 left with zero open code gaps at the same code-level standard the verifier applied; remaining open items are the security gate (/gsd-secure-phase 1) and hardware UAT items 1-4
</success_criteria>

<artifacts_produced>
Symbols and files this plan creates or renames (for drift-verification exclusion):

- New constant: PACKET_TYPE_RESPONSE (include/command_protocol.h, next to PACKET_TYPE_COMMAND)
- New function: CommandSender-side none; CommandHandler::frameSizeFromEsp(framesize_t) const — declaration include/command_handler.h, definition src/command_handler.cpp
- Renamed enumerator: FrameSize value 8 FRAMESIZE_QXGA -> FRAMESIZE_CIF (wire code 8 unchanged; sole referencing sites updated: enum definition, framesizeFromInt case)
- New harness symbols: createResponsePacketMirror (factory transcription with defective option), restructured serializeResponse mirror emitting the packet's type field, clause (f) assertions
- Behavior changes at existing sites: createResponsePacket and createCommandPacket assign packet.type; createACK/createNACK/createStatus derive the type from the shared constant; handleGetStatus uses frameSizeFromEsp; handleResponse gains the terminal-state guard; base-station resolution option value 8 label corrected to CIF 400x296
- No new files; no new struct fields, CLI flags, or config keys
</artifacts_produced>

<output>
Create `.planning/phases/01-command-protocol-control/01-06-SUMMARY.md` when done
</output>
