---
phase: 01-command-protocol-control
plan: "03"
type: execute
wave: 2
depends_on: ["01-02"]
gap_closure: true
files_modified:
  - src/main_basestation.cpp
autonomous: true
requirements: [CTRL-01, CTRL-02, CTRL-03, CTRL-04, CTRL-06]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Base station web interface has camera control section with trigger button and settings forms for ALL 7 camera settings (SC-1): quality, brightness, contrast, resolution, saturation, exposure, white balance"
    - "Failed commands are retried with timeout and the user is notified (SC-4 UI half): the Last Command panel shows the per-command outcome using the locked status vocabulary Sent / ACK Received / Failed (retry N) / Timeout"
    - "Auto-capture UI controls exist: interval input (1-3600 seconds), Enable Auto-Capture (accent) and Disable Auto-Capture (danger) buttons, ON/OFF state chip (CTRL-03/CTRL-04 UI)"
    - "UI truth (empty): before any command, the Last Command panel renders the 'No commands yet' heading and documented empty-state body; every form renders a complete documented default value"
    - "UI truth (loading): an in-flight command shows the locked 'Sent' state string immediately after POST; the 1s /status poll advances the panel to a terminal state"
    - "UI truth (populated): Sent/Acked/Failed/Pending counters advance as commands process; status cells never truncate"
    - "UI truth (partial): each setting is an independent single-field form always displaying a complete value"
    - "UI truth (overflow): status bar collapses to multi-row layout under a 480px media query; Last Command text wraps within card width — no clipping"
    - "UI truth (zero-one-many): numeric readouts only, no singular/plural copy; layout identical at 0, 1, and many"
    - "UI truth (long-text): message and label text wrap at 14px/1.5 inside the card; no ellipsis or truncation"
    - statement: "Terminal 'Timeout' / 'Failed (retry N)' with documented error copy renders after retries are exhausted — held-out verification is the hardware UAT degraded-link test (power balloon off, 3 retries, TIMEOUT surfaced); the CommandState-to-string mapping itself is code-verifiable without hardware"
      verification: backstop
    - "LED truth (IN-03): the link LED is driven by real link state (green 'Ready' after recent ACKed activity, red 'No link' after a terminal TIMEOUT/FAILED outcome or stale link, yellow 'Unknown' before any command completes) — never a hardcoded true value"
  artifacts:
    - path: "src/main_basestation.cpp"
      provides: "All 7 settings forms + routes, auto-capture card + routes, Last Command panel, real link state in /status, WR-07-safe input validation"
      contains: "/set-resolution"
  key_links:
    - from: "src/main_basestation.cpp new POST routes"
      to: "CmdSender().sendCommand"
      via: "each route range-checks a long, narrows, and forwards SET_RESOLUTION/SET_SATURATION/SET_EXPOSURE/SET_WB_MODE/AUTO_CAPTURE_ENABLE/AUTO_CAPTURE_DISABLE"
      pattern: "sendCommand\\(CameraCommand::SET_SATURATION"
    - from: "footer script updateStatus"
      to: "/status"
      via: "1s fetch consumes lastCmd/lastSeq/lastState/lastRetry/connected and renders the Last Command panel and link LED"
      pattern: "lastState"
  prohibitions:
    - statement: "MUST NOT display a green or Ready link indicator when the LoRa link has no recent confirmed activity or the last command failed — the LED must reflect real link state"
      status: unverified
      flagged: true
      verification: judgment
---

<objective>
Complete the base station control panel: the 4 missing camera settings forms and routes (CTRL-02 UI, 3 of 7 to 7 of 7), auto-capture controls (CTRL-03/CTRL-04 UI), per-command outcome surfacing with the locked status vocabulary (SC-4 notification half), WR-07-safe input validation on every route, and the LED truth requirement (IN-03) from the approved UI design contract.

Purpose: SC-1 and the user-notification half of SC-4 are currently partial; the UI design contract (01-UI-SPEC.md) fixes the exact copy, tokens, and state vocabulary — this plan implements it verbatim.
Output: Rewritten src/main_basestation.cpp interface layer on top of the repaired protocol from plan 01-02.
</objective>

<execution_context>
@C:/Users/Amp/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Amp/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-command-protocol-control/01-UI-SPEC.md
@.planning/phases/01-command-protocol-control/01-PATTERNS.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md

src/main_basestation.cpp (the file being extended — forms, routes, footer script)
</context>

<tasks>

<task type="auto">
  <name>Task 1: All 7 settings forms and routes with safe validation (CTRL-02 UI, WR-07) + UI-SPEC token harmonization</name>
  <read_first>
  - src/main_basestation.cpp (whole file — HTML_HEADER CSS block, handleRoot forms, handleSetQuality/handleSetBrightness/handleSetContrast handlers, initWebServer route registration, sendResponse helper)
  - .planning/phases/01-command-protocol-control/01-UI-SPEC.md (Copywriting Contract form table, Design System new components, Spacing Scale, Typography, Color)
  - include/command_protocol.h (FrameSize enum values 5-13, WhiteBalanceMode enum 0-4, payload structs)
  - .planning/phases/01-command-protocol-control/01-PATTERNS.md (section "src/main_basestation.cpp" — WR-07 range-check pattern)
  </read_first>
  <files>src/main_basestation.cpp</files>
  <action>
  Add four POST routes in initWebServer next to the existing ones: /set-resolution, /set-saturation, /set-exposure, /set-wb. Add four handlers handleSetResolution, handleSetSaturation, handleSetExposure, handleSetWBMode modeled on handleSetQuality, each with the WR-07-safe pattern: read server.arg into a long via toInt, range-check the long BEFORE narrowing (resolution: 5..13 matching FrameSize; saturation and exposure: -2..2; white balance: 0..4 matching WhiteBalanceMode), reject out-of-range with sendResponse(400, "Error", ...) naming the valid range, then narrow with static_cast and call CmdSender().sendCommand(CameraCommand::SET_X, &value, 1) with a 1-byte payload. Resolution and WB values are sent as their numeric enum value byte.

  Fix WR-07 in the three EXISTING handlers: handleSetQuality currently narrows to uint8_t before its range check (a value like 300 truncates to 44 and passes); change it and handleSetBrightness/handleSetContrast to the same long-first range-check pattern (quality 0..63; brightness and contrast -2..2). Every handler (new and existing, plus handleCapture) records the returned sequence in appState.lastCommandSequence and a short command name in a new appState.lastCommandName char field (e.g. "Capture", "Set Quality", "Set Resolution", "Set Saturation", "Set Exposure", "Set White Balance", "Auto-Capture On", "Auto-Capture Off") — the Last Command panel in Task 3 consumes these.

  Append four forms to the Camera Settings card in handleRoot after the contrast form, separated by the existing hr style: Resolution as a select (name="resolution") with the nine UI-SPEC options QQVGA 160x120 through UXGA 1600x1200 (option values 5..13, default QVGA 320x240 selected), Saturation as number input (min -2 max 2 value 0), Exposure as number input (min -2 max 2 value 0), White Balance as a select (name="wb") with Auto/Sunny/Cloudy/Office/Home (values 0..4, default Auto). Submit buttons copy the locked UI-SPEC labels: Set Resolution, Set Saturation, Set Exposure, Set White Balance.

  CSS harmonization per the approved UI-SPEC (same tokens, no new visual language): add select styling identical to input[type=number] (#334155 background, 1px #475569 border, radius 5px, 14px text, 8px padding); declare line-height 1.5 on body; adjust the off-grid spacings named in the UI-SPEC Spacing Scale (5px label gaps to 4px, 10px grid/message gaps to 8px, 15px form-group/button/status-bar padding to 16px — 20px values unchanged); add a 480px max-width media query collapsing the status-bar grid (see Task 3 for the grid change) and stacking cards.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-basestation && test "$(grep -c "server.on(\"/set-" src/main_basestation.cpp)" = "7" && grep -q "name=\"resolution\"" src/main_basestation.cpp && grep -q "name=\"wb\"" src/main_basestation.cpp && grep -q "@media (max-width: 480px)" src/main_basestation.cpp && ! grep -q "uint8_t quality = server.arg" src/main_basestation.cpp</automated>
  </verify>
  <acceptance_criteria>
  - initWebServer registers 7 settings POST routes (/set-quality, /set-brightness, /set-contrast, /set-resolution, /set-saturation, /set-exposure, /set-wb)
  - Every settings handler range-checks a long from toInt() before any narrowing cast; the direct uint8_t assignment from serverArg().toInt() pattern is gone from handleSetQuality
  - Resolution select offers the 9 documented options with values 5..13 and QVGA selected by default; WB select offers Auto/Sunny/Cloudy/Office/Home with values 0..4 and Auto default
  - All 7 form labels and button texts match the UI-SPEC Copywriting Contract table verbatim
  - select elements are styled with the same rule values as input[type=number]; body declares line-height 1.5; the 480px media query exists
  - Each handler stores appState.lastCommandSequence and appState.lastCommandName on successful queueing
  - pio run -e esp32-s3-basestation reports SUCCESS
  </acceptance_criteria>
  <done>The web UI exposes and correctly validates all 7 camera settings (CTRL-02 UI complete), input can no longer bypass firmware ranges via integer truncation (WR-07 closed), and the UI-SPEC spacing/typography harmonization is applied.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-capture card and routes (CTRL-03/CTRL-04 UI)</name>
  <read_first>
  - src/main_basestation.cpp (capture card markup in handleRoot, handleCapture handler, sendResponse helper — after Task 1 edits)
  - .planning/phases/01-command-protocol-control/01-UI-SPEC.md (Auto-Capture card spec, Copywriting Contract feedback strings, interval constraint 1-3600 seconds sent as value x 1000)
  - src/command_handler.cpp (handleAutoCaptureEnable interval validation 1000..3600000 ms and its big-endian readUint32 payload convention)
  - include/command_protocol.h (CameraCommand AUTO_CAPTURE_ENABLE/AUTO_CAPTURE_DISABLE, PayloadAutoCaptureEnable, CommandProtocol::writeUint32)
  </read_first>
  <files>src/main_basestation.cpp</files>
  <action>
  Add POST routes /auto-capture and /auto-capture-stop in initWebServer. handleAutoCaptureEnable: require arg "interval" (seconds), read into a long, range-check 1..3600, reject out-of-range with the valid range in the message; convert to milliseconds by multiplying by 1000; encode the interval as a 4-byte BIG-ENDIAN payload using CommandProtocol::writeUint32 into a local uint8_t array (do NOT memcpy the PayloadAutoCaptureEnable struct — the balloon reads the payload with readUint32, and struct layout is native little-endian); send CameraCommand::AUTO_CAPTURE_ENABLE with payloadSize 4; on queue success set appState.lastCommandSequence/lastCommandName ("Auto-Capture On") and reply 200 with the UI-SPEC success copy "Auto-capture enabled — capturing every {N}s" using the seconds value. handleAutoCaptureDisable: send CameraCommand::AUTO_CAPTURE_DISABLE with no payload, set the appState fields ("Auto-Capture Off"), reply 200 with "Auto-capture disabled".

  Add an Auto-Capture card to handleRoot after the Camera Settings card, using the existing card/form/button classes: heading "⏱ Auto-Capture"; a form POSTing to /auto-capture containing a number input (name="interval", min 1, max 3600, value 10, label copy "Interval (1-3600 seconds):") and a submit button "Enable Auto-Capture" (default blue accent styling); a second form POSTing to /auto-capture-stop with a button class "danger" labeled "Disable Auto-Capture"; and a state chip element (id="autocapture-chip") defaulting to "OFF", which the Task 3 script updates to "ON · every {N}s" after an enable command reaches ACKED and back to "OFF" after a disable reaches ACKED (the chip is display-only until the ACK arrives — no optimistic ON state).
  </action>
  <verify>
  <automated>pio run -e esp32-s3-basestation && grep -q "server.on(\"/auto-capture\", HTTP_POST" src/main_basestation.cpp && grep -q "server.on(\"/auto-capture-stop\", HTTP_POST" src/main_basestation.cpp && grep -q "writeUint32" src/main_basestation.cpp && grep -q "autocapture-chip" src/main_basestation.cpp && grep -q "button class=\"danger\".*Disable Auto-Capture\|Disable Auto-Capture" src/main_basestation.cpp</automated>
  </verify>
  <acceptance_criteria>
  - /auto-capture validates interval seconds in 1..3600 as a long before narrowing/multiplication; /auto-capture-stop requires no parameters
  - The enable payload is 4 bytes written big-endian via CommandProtocol::writeUint32 (readable by the balloon's readUint32)
  - Card markup uses existing classes only, with the Enable button in default accent styling and Disable in the danger class; chip element id "autocapture-chip" exists with default text "OFF"
  - Handler replies use the exact UI-SPEC feedback copy including the seconds value
  - appState.lastCommandSequence/lastCommandName are set on both routes
  - pio run -e esp32-s3-basestation reports SUCCESS
  </acceptance_criteria>
  <done>The operator can enable and disable interval auto-capture from the web UI with firmware-mirrored validation and byte-order-correct payloads (CTRL-03/CTRL-04 UI delivered; balloon side lands in plan 01-04).</done>
</task>

<task type="auto">
  <name>Task 3: Last Command panel, real link LED (IN-03), and status JSON completion (SC-4 notification half)</name>
  <read_first>
  - src/main_basestation.cpp (HTML_FOOTER script, handleStatus, processLoRa, appState struct, status-bar markup in handleRoot — after Task 1-2 edits)
  - include/command_sender.h (CommandState enum: IDLE/PENDING/SENT/ACKED/FAILED/TIMEOUT; getCommandState; getCommandRetryCount added by plan 01-02)
  - .planning/phases/01-command-protocol-control/01-UI-SPEC.md (Copywriting Contract status vocabulary and empty/error state copy, LED truth requirement, UI Considerations states)
  </read_first>
  <files>src/main_basestation.cpp</files>
  <action>
  Extend BaseStationState with: uint32_t lastAckTime; uint16_t ackedAtLastPoll; and reuse existing lastCommandSequence/lastCommandName. In processLoRa(), compare CmdSender().getCommandsAcked() against ackedAtLastPoll — when it increases, set lastAckTime to millis() and store the new counter.

  Rewrite handleStatus() to emit: sent, acked, failed, pending (pending as the integer CmdSender() pending count via hasPendingCommands or a new counter read — keep the existing sources), plus new fields lastCmd (string, empty before any command), lastSeq (number), lastState (string), lastRetry (number), connected (boolean), linkText (string). Compute per-command state from CmdSender().getCommandState(appState.lastCommandSequence) and CmdSender().getCommandRetryCount(appState.lastCommandSequence); map CommandState to the LOCKED vocabulary: PENDING and SENT map to "Sent", ACKED to "ACK Received", FAILED to "Failed (retry N)" with N the retry count, TIMEOUT to "Timeout", and no-command (sequence 0 or IDLE) to an empty lastState. Link semantics (LED truth): if no command has ever completed, connected=false with linkText "Unknown"; else if the last terminal outcome was TIMEOUT or FAILED and no ACK has arrived since, connected=false with linkText "No link"; else if an ACK was seen within LINK_STALE_MS (define a constant 30000 ms), connected=true with linkText "Ready"; else connected=false with linkText "Unknown". Remove the hardcoded connected literal entirely — the value must come from this computation.

  Add a Last Command card to handleRoot between the status bar and the Manual Capture card: heading "📡 Last Command", a server-rendered empty state with the exact UI-SPEC copy (heading "No commands yet", body "Trigger a capture or change a setting — the result of your last command appears here.") inside elements the script replaces (ids: lastcmd-name, lastcmd-state), plus a state styling hook that maps "ACK Received" to the message success class, "Sent" to info, and "Failed"/"Timeout" to error.

  Update the status bar to five cells (Status, Sent, Acked, Failed, Pending — the Failed cell with id "cmd-failed" that the footer script already references but which does not exist today, which currently makes updateStatus throw every second) and change the status-bar grid to repeat(auto-fit, minmax(110px, 1fr)) so five cells fit one desktop row and collapse under the 480px media query from Task 1; the link status cell's text (id "link-text", initial "Unknown") and LED (existing id "status-led", initial class led yellow) are driven by the script.

  Extend the HTML_FOOTER script's updateStatus(): set cmd-sent/cmd-acked/cmd-failed/cmd-pending; set the LED class from data.connected (green/red) with yellow when linkText is "Unknown"; set the link text; populate the Last Command panel — when lastSeq is 0 keep the empty-state markup, otherwise render lastCmd, the seq, and lastState with the class mapping above. No spinner or skeleton elements (UI-SPEC loading model is status-line only).
  </action>
  <verify>
  <automated>pio run -e esp32-s3-basestation && ! grep -q '"connected":true' src/main_basestation.cpp && grep -q "lastState" src/main_basestation.cpp && grep -q "getCommandRetryCount" src/main_basestation.cpp && grep -q "cmd-failed" src/main_basestation.cpp && grep -q "ACK Received" src/main_basestation.cpp && grep -q "No commands yet" src/main_basestation.cpp</automated>
  </verify>
  <acceptance_criteria>
  - /status emits lastCmd, lastSeq, lastState, lastRetry, connected, linkText; the connected value is computed from ack activity and terminal outcomes, and the hardcoded connected literal no longer appears in the file
  - CommandState-to-string mapping matches the locked vocabulary exactly: PENDING/SENT to "Sent", ACKED to "ACK Received", FAILED to "Failed (retry N)" (N from getCommandRetryCount), TIMEOUT to "Timeout"
  - A status cell with id "cmd-failed" exists (the footer script's dangling reference is fixed) and the grid is auto-fit with a 480px collapse
  - The Last Command card renders the exact empty-state copy before any command and per-command name/seq/state afterward, with success/info/error class mapping
  - LED starts yellow with "Unknown", shows green "Ready" only after recent ACKed activity, red "No link" after terminal failure with no ACK since
  - processLoRa records lastAckTime when the acked counter increases
  - pio run -e esp32-s3-basestation reports SUCCESS
  </acceptance_criteria>
  <done>The user sees the outcome of every command they issue — ACK Received, Failed (retry N), or Timeout — in the Last Command panel, and the link LED reflects real link health (SC-4 notification half and the UI-SPEC LED truth requirement delivered; IN-03 closed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web client → base station HTTP | Unauthenticated POST routes parse attacker-controlled form strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | All POST handlers | high | mitigate | WR-07: every handler range-checks the long from toInt() before narrowing — truncation-bypass values (e.g. quality far above 63) are rejected with 400 |
| T-01-06 | Spoofing | /status link state | low | mitigate | LED/connected derived from CommandSender counters and terminal states (server-side), not client-supplied data |
| T-01-07 | Info Disclosure | AP with default WPA2 credential in source | low | accept | Field-device default documented in firmware; credential rotation is user setup at deployment, out of code scope |
</threat_model>

<flagged_assumptions>
Spec-less edge-probe items left unresolved at plan time (surfaced, not dismissed):

- CTRL-02 (adjacency — when two things are exactly equal or just touch, do they merge, collide, or separate?): each setting is an independent single-field form; equal values simply re-send the command (idempotent setter on the balloon). Assumed acceptable; UAT confirms.
- CTRL-02 (empty — result for empty/single/null input): missing args are rejected 400 by the hasArg check; forms always carry documented defaults so an empty submit cannot occur from the UI. Direct API posts with empty values rely on the 400 path.
- CTRL-02 (ordering — when elements compare equal, is output order specified and stable?): single-field forms have no intra-form ordering; card order is fixed by handleRoot emission order (quality, brightness, contrast, resolution, saturation, exposure, WB).
</flagged_assumptions>

<verification>
- pio run -e esp32-s3-basestation — SUCCESS after each task
- Structural gates: 7 /set- routes registered, writeUint32 big-endian encode present, hardcoded connected literal absent, locked status strings present, empty-state copy present, cmd-failed cell present, 480px media query present
- Behavioral verification of the panel/LED against a live balloon is UAT (01-VERIFICATION.md human items 1-2); the CommandState mapping itself is code-verifiable
</verification>

<success_criteria>
- SC-1 complete: trigger button plus all 7 settings forms
- SC-4 notification half: per-command ACKED/TIMEOUT/FAILED outcome visible with the locked status vocabulary
- CTRL-03/CTRL-04 UI controls delivered; IN-03 LED truth and WR-07 validation closed
- UI conforms to the approved 01-UI-SPEC.md contract (tokens, copy, states)
</success_criteria>

<artifacts_produced>
Symbols and surfaces this plan creates (for drift-verification exclusion):

- HTTP routes: /set-resolution, /set-saturation, /set-exposure, /set-wb, /auto-capture, /auto-capture-stop (POST)
- Handlers: handleSetResolution, handleSetSaturation, handleSetExposure, handleSetWBMode, handleAutoCaptureEnable, handleAutoCaptureDisable
- BaseStationState new fields: lastCommandName, lastAckTime, ackedAtLastPoll
- /status JSON new fields: lastCmd, lastSeq, lastState, lastRetry, connected (computed), linkText
- UI elements: Last Command card (lastcmd-name, lastcmd-state), Auto-Capture card (autocapture-chip), cmd-failed status cell, link-text element, LINK_STALE_MS constant (30000), 480px media query
</artifacts_produced>

<output>
Create `.planning/phases/01-command-protocol-control/01-03-SUMMARY.md` when done
</output>
