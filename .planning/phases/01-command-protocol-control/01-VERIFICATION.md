---
phase: 01-command-protocol-control
verified: 2026-08-28T18:05:00Z
status: gaps_found
score: 4/5 must-haves verified
behavior_unverified: 1 # SC-3, narrowed to its final clause for the NINTH consecutive round: resolution-class clauses bench-proven (sessions 4/5, PLUS new current-firmware evidence this round — the CIF wire-8 half executed cleanly at session 10) and camera_manager.cpp still untouched since 01-13 (90300b8, commit scopes verifier-read, zero source drift at HEAD); session 10 issued zero non-RESOLUTION settings commands (verifier-grepped base4.log: 0 SET_BRIGHTNESS/contrast/saturation/quality/exposure/WB) — the pairwise visible-effect judgments remain operator-unjudged
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/5
  gaps_closed: [] # NONE of the three #11 gaps closed. Gap 1 (D1) evolved to a FOURTH expression — but round #13's instrument-only round answered its designed purpose: the [IDLE0] instrument delivered the campaign's FIRST positive instrument answer (starvation DISCONFIRMED for the session-10 crash). Gap 2 (G-01-7) MOVED MATERIALLY: the series finally RAN on the ninth attempt (6/8 COMPLETE, every round-#10 discriminator engaged for the first time) and the residual mechanism is NAMED by the discriminators themselves (depth-3 queue cycling evicts the base-activated prior full) — the gap went from unjudgeable-by-absence to failed-with-named-mechanism, still open. Gap 3 rode a ninth round, with ONE clause half-discharged (CIF wire-8 ran clean as specified for the first time; QVGA restore did not)
  gaps_remaining:
    - "D1 crash regression (G-01-10 / WINDOWS 15) — FOURTH EXPRESSION, the campaign's best-evidenced: an IDLE0 STACK-CANARY PANIC with a full dump at the TX-heaviest stretch of the five-session campaign. balloon4.log:1428-:1429 'Guru Meditation Error: Core 0 panic'ed (Unhandled debug exception)' / 'Debug exception reason: Stack canary watchpoint triggered (IDLE0)' at chunk 12/14 of image 43's re-armed 14-chunk FULL window (:1380 re-arm -> :1427 12/14 sent); faulting PC 0x400570ea memset in ROM writing 36 zero bytes at SP+0x1c inside IDLE0's stack (A2 dest 0x3fcbc87c, A3 fill 0x0, A4 len 0x24), EXCVADDR 0x0, EXCCAUSE 0x1, backtrace |<-CORRUPTED, zero project frames; Core 1 SIMULTANEOUSLY spinning in SysTickIsrHandler -> xPortSysTickHandler -> xTaskIncrementTickOtherCores -> spinlock_acquire (esp_cpu_compare_and_set cpu.c:205) — the same tick-ISR/lock chain the prior three expressions sampled; rst:0xc :1483, [BOOT] reset-cause: PANIC :1576 (B1's first PANIC reading), clean re-init, no second crash. INSTRUMENT ANSWERS (all verifier-reproduced): [IDLE0] armed both boots (:33/:1505) with ZERO frozen lines whole-console -> STARVATION DISCONFIRMED for this crash (the campaign's first positive instrument answer); [TWDT] = ESP_ERR_NOT_FOUND BOTH boots (:105/:1577) — §7's 'only IDLE0 feeds the TWDT' premise UNDER RE-EXAMINATION (wrong-handle bug xTaskGetIdleTaskHandle vs xTaskGetIdleTaskHandleForCPU(0), or genuinely-unsubscribed; not discriminated by this console); [I2C] did not fire at its trigger (:1093 i2cWrite was on the HAL WRITE path; instrument wired to the invalid-READING path — coverage gap); [MEM] healthy a fourth session (loopTask stackHW 5772 constant — the breached stack is IDLE0's, which [MEM] does not monitor). Round-#14 routed in STATE.md + debug doc §10.5: [STACK] IDLE0-watermark instrument FIRST (one session CONFIRMs/REFUTEs the stack-capacity hypothesis), [TWDT] handle fix, [IDLE0] printf fix, [I2C] write-path coverage — NO lever until [STACK] answers"
    - "G-01-7 series-A burst full-delivery — NINTH attempt, FIRST JUDGED: the series ran (seq 7/8/9) and FAILED its numeric bar with the residual NAMED. 6/8 finalize verdicts COMPLETE (best series-A partial ever; image 40 both kinds COMPLETE base4.log:67-68/:156-157 — the first-post-boot push survived a THIRD consecutive session; image 42 both kinds COMPLETE :269-:270/:425-:426); seq=9 hit the forbidden 'timeout after 3 retries' (:250, third session with this terminal: 5/7/10); image 41 0/2 INCOMPLETE (thumb 6/8 :539-:540, full 0/31 :484-:485 — eviction-starved, NOT silent: every step a quoted line); image 43 full INCOMPLETE 14/119 (:712-:713) CRASH-CAUSED. ALL round-#10 discriminators engaged for the first time in nine attempts (verifier-reproduced census: 're-announce budget re-armed' ×2 balloon4.log:737/:815; FULL-manifest re-announces ×3 :690/:799/:803; 're-announce held' ×4 all genuine :460/:733/:887/:1644; base 'full-pull activation deadline reached' :428; receipt-evidenced class-5 evictions ×3 :816-:817/:1295; 'unknown/evicted image 41 rejected' ×7 :1003-:1162). RESIDUAL NAMED BY THE DISCRIMINATORS: the depth-3 queue cycles under the burst — base deadline-activated image 41's full-pull (:428) -> image 42's window request evicted the pending entry (:817) -> seven follow-up requests honestly rejected -> 0/31, while the newest capture's full (42) completed. The levers work as designed; the design's queue depth vs the burst pattern is the mismatch. Lever class routed: admission control or oldest-queued-full eviction, AFTER D1 round #4"
    - "The riding bench-moment clauses — NINTH round (one clause half-discharged): SC-3 visible-effect pairs NOT reached (ninth); WR-03 cadence discriminator NOT run (ninth; SC-5 advisory NOT discharged — zero AUTO_CAPTURE commands verifier-grepped both consoles); QVGA restore (wire 6) NOT reached; dashboard operator-LOOKED glance + UI-SPEC lifts NOT reached. HALF-DISCHARGED: CIF-cycle wire-8-as-specified ran CLEAN for the first time (SET_RESOLUTION seq=26 ACKed base4.log:558-:560 -> image 43 captured CIF-class 23800 B / 119 chunks vs QVGA 6136 B / 31, balloon4.log:1291, thumb COMPLETE 7/7 base4.log:588, ZERO FB-OVF both consoles) — the QVGA-restore half still rides"
  regressions:
    - "NONE new in code: round #13's code work is the three instruments (1a737dd: exactly 4 files — src/main_balloon.cpp +105, src/image_tx_manager.cpp +18, 2 ledgers; ZERO deletions; d78a6e5 docs-only §9 audit 397 lines; a488661/fd0f62a/377594c docs-only). Builds 2/2 SUCCESS and harness exit 0 both VERIFIER-RE-RUN this round, and the verifier's rebuild from HEAD reproduces the deployed ELF SHA256 acce78241149382fca72ba810d02e4c6c68042b49d7813243e6effb91ee42428 EXACTLY — deterministic-build proof that the crashing image IS the verified HEAD source. Protected surfaces intact (CMD_TX_CHANNEL_QUIET_MS = 750 command_sender.h:25; Lever A present; [MEM]/B1/B2/round-#10 lines present; camera_manager.cpp and image_rx_manager.cpp untouched). The D1 recurrence is the SAME open gap evolved to a fourth expression. D2 (WINDOWS 16) and WR-02 (WINDOWS 18) watches GREEN on the round-#13 firmware (zero health-check-failed; the D2 're-announce held' ×4 all genuine by receipt evidence). WR-01 GREEN (zero Critical battery; beacons batt=valid ×65 / invalid ×0). One NEW bench defect observed and honestly routed, not hidden: the [TWDT] instrument's own handle ambiguity (ESP_ERR_NOT_FOUND), the [IDLE0] registered=0 printf bug (esp_err_t->bool; the hook genuinely armed — zero frozen lines PROVES the counter incremented), and the [I2C] write-path coverage gap — all three routed to round #14 per the plan's no-inline-fix prohibition"
gaps:
  - truth: "The balloon survives post-capture radio push through sustained FULL-window service AND its inter-window lulls (G-01-10 / WINDOWS 15, the phase's top blocker): a capture's enqueue/manifest/chunk/window/lull sequence never hard-resets the firmware, so buffers survive to serve the transfer"
    status: failed
    reason: "Bench session #10 (01-31, round-#13 firmware — the verifier's rebuild from HEAD reproduces the deployed ELF SHA acce782411... exactly): the balloon CRASHED a FOURTH time, mid-service of image 43's re-armed 14-chunk FULL window (chunk 12/14), as an IDLE0 stack-canary panic with a full dump (balloon4.log:1428-:1429; faulting PC ROM memset 0x400570ea, dest 0x3fcbc87c inside IDLE0's stack, fill 0, 36 bytes; Core 1 concurrently spinning in the SysTick -> tick-increment -> spinlock_acquire chain; backtrace corrupted; rst:0xc; [BOOT] PANIC at boot 2). The round-#13 instruments ANSWERED — the campaign's first positive instrument answer: [IDLE0] zero frozen lines whole-console DISCONFIRMS starvation for this crash (the §7 'starved >=10 s' chain cannot describe a canary panic while IDLE0 is scheduled); [TWDT] ESP_ERR_NOT_FOUND both boots puts §7's WDT-topology premise under honest re-examination (instrument handle bug vs genuinely-unsubscribed — round #14 must discriminate with xTaskGetIdleTaskHandleForCPU(0)); [I2C] coverage gap on the write path; [MEM] healthy a fourth session. Class 1 (first-post-boot push) survived a THIRD consecutive session (image 40 both kinds COMPLETE); the crash hit the campaign's TX-heaviest stretch (AUX-low-missed ×81, the phase's largest image at 119 chunks) — H-lull stays REJECTED (service-phase crash); H-phase-independent maturation WEAKENED (crash time correlates with LOAD, not a fixed clock); the labeled stack-capacity hypothesis awaits the [STACK] watermark instrument. The known non-fatal i2cWrite+mojibake class RESTORED (:1093, t=259068 ms, successful BMP280 reads :1095-:1096, 335 lines pre-crash — session 9's adjacency did NOT recur). Downstream: image 43 full INCOMPLETE 14/119 is crash-caused; the post-crash seq=29 window-request timeout (:681) and seq=31/32 NACK_INVALIDs are the honest recovery behavior of a rebooted balloon (beacon seq restarted at 0)"
    artifacts:
      - path: ".planning/debug/d1-crash-regression-push-start.md"
        issue: "none — §10 (1315-1465) carries the fourth expression in the house convention: provenance/ELF-SHA first, event map, instrument answers, i2cWrite/mojibake census, the LABELED stack-capacity hypothesis, and the round-#14 brief; the gap is the crash itself, not the record"
      - path: "src/image_tx_manager.cpp"
        issue: "[TWDT] instrument (:172) uses xTaskGetIdleTaskHandle() from CPU1 setup context — ambiguous on SMP; returns ESP_ERR_NOT_FOUND both boots; the §7 WDT-chain premise rides on a question this console cannot settle (routed round #14)"
      - path: "src/main_balloon.cpp"
        issue: "[IDLE0] registered printf bug (:237 esp_err_t->bool prints 0 on SUCCESS — cosmetic, the hook provably armed); [I2C] probe covers the invalid-READING transition only (:836-866), not the HAL WRITE path the session's failure used (coverage gap, routed round #14)"
    missing:
      - "Debug round #4 (round #14): [STACK] instrument FIRST — uxTaskGetStackHighWaterMark(NULL) from the existing [IDLE0] idle hook at 1 Hz with a new-low latch (the [MEM] pattern applied to IDLE0) — one bench session CONFIRMs/REFUTEs the IDLE-stack-capacity hypothesis; fix the [TWDT] handle (ForCPU(0)) and re-verify the deployed WDT config before further WDT-chain reasoning; fix the [IDLE0] printf (print err==ESP_OK); extend the [I2C] probe to the write-failure path; optional hook-unregistered A/B build; NO lever until [STACK] answers (three elimination-only rounds + two bench-disconfirmed levers make lever-speculation ahead of the watermark evidence dishonest by the standing guard)"
      - "Re-establish crash-context determinism: FOUR expressions now (S7 canary on IDLE1 + INT_WDT panic + silent rst:0x7; S8 silent rst:0x7 mid-service; S9 silent rst:0x7 in-lull adjacent to I2C+mojibake; S10 canary on IDLE0 mid-service TX-heaviest) — one labeled CPU0-side fault family with load-dependent signature is the working hypothesis; the [STACK] instrument is the designed discriminator"
  - truth: "G-01-7 series-A acceptance (burst full-delivery): 3 back-to-back unspaced captures — 3 ACKs, 3 captures, 6/6 verdicts COMPLETE, zero command timeouts, zero silently absent fulls, with the round-#10 discriminators proving lever engagement (or the residual mechanism named)"
    status: failed
    reason: "RAN FOR THE FIRST TIME (ninth attempt) and FAILED its numeric bar with the residual NAMED — no longer unjudgeable: 6/8 COMPLETE (image 40 2/2, image 42 2/2; image 41 0/2 eviction-starved with every step a quoted line; image 43 thumb COMPLETE 7/7 + full crash-caused INCOMPLETE); seq=9 CAPTURE_NOW hit the forbidden 'timeout after 3 retries' (base4.log:250 — retries 1-2 burned BEFORE the quiet-gate hold :225, the retry counter advances while the chunk stream is busy; third session with this terminal: 5/7/10). The escape clause SUCCEEDED: every round-#10 discriminator engaged for the first time (census verifier-reproduced, see gaps_remaining) and named the residual mechanism: depth-3 queue cycling — the newest capture's window request evicts the previous capture's base-activated full-pull. The levers work as designed; the design's queue depth vs the burst pattern is the mismatch. NEXT LEVER CLASS routed: admission control (refuse/queue a new capture while an earlier full-pull is base-activated) or oldest-queued-full eviction — gated behind D1 round #4 (the series cannot run clean while the firmware dies mid-burst)"
    artifacts:
      - path: "src/image_tx_manager.cpp"
        issue: "none new — the round-#10 levers are now BENCH-VERIFIED ENGAGED (budget re-arm, receipt-evidenced eviction labels, re-announce ladder all fired as designed); the defect is the design's depth-3 queue policy under bursts, needing its own lever round"
    missing:
      - "D1 debug round #4 + fix first; then the admission-control/oldest-queued-eviction lever round for the named depth-3-queue-cycling residual; then the series-A re-run: 3 unspaced captures, 3/3 ACKs (no forbidden terminals), 6/6 COMPLETE, zero command timeouts, zero silently-absent fulls"
      - "Ledger completeness note (verifier-found, info-level): base4.log contains a SECOND 'timeout after 3 retries' — seq=29 IMAGE_WINDOW_REQUEST at :681, whose retry ladder spanned the crash (retries hit the crashed/rebooting balloon; beacon seq restarts at 0 at :671; the follow-ups seq=31/32 were honestly NACK_INVALID'd by the rebooted balloon). Crash-caused recovery behavior, not a series-A defect — but the session-10 ledger records (WINDOWS entry 3, 01-UAT G-01-7) name only seq=9; a one-line census amendment at the next ledger touch would make the record complete"
  - truth: "The bench-moment clauses: SC-3 visible-effect pairs judged; WR-03 cadence discriminator quoted; CIF-cycle (wire-8) + QVGA-restore clauses exercised; dashboard operator-LOOKED glance + UI-SPEC lifts"
    status: partial
    reason: "NINTH round riding, ONE clause half-discharged: session 10 aborted at the crash before the settings/cadence/dashboard steps (SC-3 pairs zero non-RESOLUTION commands — verifier-grepped; WR-03 zero AUTO_CAPTURE commands — ninth round; QVGA restore wire-6 not reached; dashboard glance not reached — commands flowed through it, /api/state served throughout, but no LOOK verdicts). DISCHARGED HALF: the CIF-cycle wire-8-as-specified clause ran CLEAN for the first time ever (seq=26 SET_RESOLUTION ACKed base4.log:558-:560 -> image 43 CIF-class 119 chunks balloon4.log:1291, thumb COMPLETE 7/7, ZERO FB-OVF both consoles) — this also adds CURRENT-firmware bench evidence to SC-3's resolution-class clause (previously resting on sessions 4/5 with a no-regression-surface argument); the QVGA-restore half still rides. All honestly recorded in 01-UAT.md (Test 3/Test 4 session-10 updates verifier-read: 'NINTH round riding') and routed in STATE.md Next Steps"
    artifacts:
      - path: ".planning/phases/01-command-protocol-control/01-UAT.md"
        issue: "Test 3 stays 'issue' — the visible-effect-pair clause is its last open settings clause (the CIF wire-8 half is now judged clean; the QVGA-restore clause rides)"
    missing:
      - "Post-D1 bench session: SC-3 pairs (brightness -2/+2 plus one other class at fixed QVGA), WR-03 cadence discriminator (AUTO_CAPTURE_ENABLE 20 s + manual capture, quote both 'Captured image ID' timestamps — also discharges the SC-5 coincidental-reliance advisory), QVGA restore (wire 6) with wire-evidence sizing + zero FB-OVF, dashboard LOOK glance incl. the d8ba14e gallery-detail fields and all UI-SPEC lifts"
deferred:
  - truth: "Full-resolution gallery image viewer (operator bench wish; presentation-only)"
    addressed_in: "Phase 3 backlog"
    evidence: ".planning/todos/pending/2026-08-23-full-resolution-gallery-image-viewer.md (verifier-verified on disk this round)"
  - truth: "Gallery thumb number overlay + Incomplete-badge explanatory copy (operator bench friction)"
    addressed_in: "Phase 3 backlog (gallery UX)"
    evidence: ".planning/todos/pending/2026-08-24-gallery-thumb-number-overlay.md and 2026-08-24-incomplete-badge-on-renderable-images.md (verifier-verified on disk)"
  - truth: "Thumb-first image delivery + antenna-pointing overlay (operator requests, captured at 898fcc6)"
    addressed_in: "Phase 3 backlog"
    evidence: ".planning/todos/pending/2026-08-27-thumb-first-image-delivery-with-on-demand-full-retrieval.md and 2026-08-27-antenna-pointing-overlay-for-base-web-ui.md (verifier-verified on disk)"
behavior_unverified_items:
  - truth: "Balloon receives camera commands and adjusts camera settings accordingly (SC-3 — pairwise visible-effect clause only)"
    test: "At fixed QVGA, adjust ONE setting between two captures and compare the pair (brightness -2 vs +2, plus at least one other class: contrast, saturation, or quality 10 vs 30)"
    expected: "Visible pairwise differences per exercised setting, recorded per pair"
    why_human: "Sensor visible-effect acceptance needs the physical camera and operator judgment — NINE consecutive bench rounds have issued zero non-RESOLUTION settings commands (session-10 command mix verifier-grepped: 0 SET_BRIGHTNESS/contrast/saturation/quality/exposure/WB; the session aborted on the D1 fourth expression before any settings step). The resolution clause is now DOUBLE-supported: sessions 4/5 bench-proofs (camera_manager.cpp untouched since 01-13 — commit scopes verifier-read through HEAD) PLUS session 10's clean CIF wire-8 execution on round-#13 firmware"
coincidental_reliance_items:
  - truth: "Both manual trigger and interval-based auto-capture work end-to-end (SC-5)"
    reason: "fixture-only"
    harden: "The manual half keeps strengthening: session 10 added a FOURTH and FIFTH current-generation proof point (image 40 both kinds COMPLETE base4.log:67-68/:156-157 — the first-post-boot push survived a third consecutive session; image 42 both kinds COMPLETE :269-:270/:425-:426 — the first burst-session full COMPLETEs). The auto-capture half still rests on Test 4's bench PASS, which predates the 01-19 baseline advance and 02-04 event triggers; non-interference is established by code reading only (zero AUTO_CAPTURE commands at sessions 6-10 — NINTH round). The WR-03 cadence discriminator at the post-D1 bench moment is precisely the hardening step — routed in STATE.md Next Steps"
---

# Phase 1: Command Protocol & Control Verification Report (Re-verification #12, after gap closure 01-30 + 01-31)

**Phase Goal:** Establish bidirectional LoRa communication for camera control
**Verified:** 2026-08-28T18:05:00Z
**Status:** gaps_found
**Re-verification:** Yes — #12, after gap-closure round #13 (plans 01-30 D1 debug round #3 + instruments, 01-31 bench session #10; commits d78a6e5/1a737dd/a488661/fd0f62a/377594c — all verifier-confirmed in git log; zero source drift HEAD..working-tree in src/, include/, scripts/; the verifier's rebuild from HEAD reproduces the deployed ELF SHA256 acce78241149382fca72ba810d02e4c6c68042b49d7813243e6effb91ee42428 exactly — deterministic-build provenance for the crashing image)

## Goal Achievement

Round #13 executed its two halves exactly as planned, and — for the first time in the D1 campaign — the half that failed on hardware failed FORWARD: the instrument-only round (01-30) shipped exactly what its honesty guard demanded (three verified, bounded, latch-guarded instruments; NO third speculative lever after two bench-disconfirmed levers), and the bench half (01-31) converted the campaign's biggest blind spot into data. The crash recurred — a FOURTH expression, the richest yet: an IDLE0 stack-canary panic with a full dump at chunk 12/14 of image 43's re-armed window, in the TX-heaviest stretch of the five-session campaign. But the round's instruments ANSWERED: the [IDLE0] tick counter ran with zero frozen episodes to the crash instant, DISCONFIRMING starvation as this crash's mechanism — the campaign's first positive instrument answer, and the answer that kills the §7 'starved >=10 s' chain as a universal explanation. The [TWDT] probe returned ESP_ERR_NOT_FOUND at both boots, putting §7's load-bearing WDT-topology premise under honest re-examination (instrument handle bug vs genuinely-unsubscribed — recorded as a two-reading question, not resolved by fiat). The [I2C] probe missed its trigger (write-path coverage gap — recorded, routed). B1 delivered the campaign's first PANIC reading. Nothing was forced: zero ledger flips, requirements-completed: [], WINDOWS counts 2/17/19 reconcile, the log-naming deviation (balloon4/base4 vs balloon15/base10) recorded, and the three instrument defects discovered mid-session were ROUTED to round #14 per the plan's no-inline-fix prohibition instead of being patched silently.

What also finally happened: the G-01-7 series-A RAN on its ninth attempt — and the round-#10 discriminators engaged for the first time, every one of them, turning nine rounds of "unjudgeable" into a NAMED residual: the depth-3 queue cycles under the burst; the newest capture's window request evicts the previous capture's base-activated full-pull; the levers work as designed while the design's queue depth loses the burst. 6/8 verdicts COMPLETE is the best series-A partial ever, and the first-post-boot push survived its third consecutive session. The numeric acceptance still failed (seq=9's forbidden timeout terminal, third session with it), so the gap stays open — but it is now a narrower, lever-ready truth.

What still did not happen: the SC-3 visible-effect pairs (NINTH round riding — though the CIF wire-8 half of the resolution family finally ran clean as specified), the WR-03 cadence discriminator (ninth), the QVGA restore, and the operator-LOOKED dashboard glance. All routed to the post-D1 bench moment.

Note on mode: ROADMAP.md marks Phase 1 `Mode: mvp`, but the goal is not in user-story format, so standard goal-backward verification applies (same determination as all prior rounds).

### Observable Truths

Must-haves are the 5 ROADMAP success criteria (roadmap contract governs; the round's own 01-30/01-31-PLAN truths are assessed separately below).

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Base station web interface has camera control section with trigger button and settings forms (SC-1) | ✓ VERIFIED | Regression check: round #13's commits touch no UI markup (1a737dd = 2 source files of instruments + 2 ledgers; d78a6e5/a488661/fd0f62a/377594c docs-only — verifier read the stats); the last UI change remains d8ba14e. Session 10 drove the dashboard throughout (the full seq 1-32 command history queued through it; /api/state polls throughout base4.log, e.g. :433). The LOOK glance rides the post-D1 bench |
| 2 | LoRa command packets transmitted from base station to balloon and acknowledged (SC-2) | ✓ VERIFIED (regression caveat: D1 + burst-command reliability) | Session 10's command loop is largely clean: seq=2 smoke ACKed, seq=7 ACKed, seq=8 ACKed after 2 retries (base4.log:199/:207/:208 — the retry ladder WORKING), seq=26 SET_RESOLUTION ACKed (:558-:560), GET_STATUSs serviced; harness exit 0 AND builds 2/2 at HEAD (both verifier-run this round). CAVEATS, both honestly routed: seq=9's forbidden timeout terminal (:250 — the G-01-7 burst axis, third session with it) and seq=29's post-crash window-request timeout (:681 — recovery behavior of a rebooted balloon, verifier-classified; see the ledger-completeness note under gap 2). The balloon still dies mid-session on the D1 axis — tracked by the D1 gap, not hidden here |
| 3 | Balloon receives camera commands and adjusts camera settings accordingly (SC-3) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | NARROWED TO ITS FINAL CLAUSE, ninth round: resolution-class clauses bench-proven (sessions 4/5) AND newly RE-EXERCISED clean on current firmware — session 10's CIF wire-8 cycle (ACK, CIF-class 119-chunk capture, thumb COMPLETE, zero FB-OVF) with camera_manager.cpp untouched since 01-13 (90300b8; commit scopes verifier-read; zero source drift). ONLY the pairwise visible-effect judgments remain — zero non-RESOLUTION settings commands for the NINTH consecutive round (session aborted on the D1 fourth expression before any settings step; verifier-grepped). See behavior_unverified_items |
| 4 | Failed commands are retried with timeout and user is notified (SC-4) | ✓ VERIFIED | The retry machinery now has CURRENT-firmware live exercise from session 10 itself: seq=8 ACKed after 2 honest retries; seq=9 and seq=29 honest terminalization at exactly 3 retries (the forbidden-terminal and crash-caused classes respectively); post-crash NACK_INVALIDs honest rejections from the rebooted balloon. Combined with the standing sessions-5/6/7 evidence. The D1 crash is a firmware-death defect, not a retry-machinery defect |
| 5 | Both manual trigger and interval-based auto-capture work end-to-end (SC-5) | ✓ VERIFIED (coincidental-reliance) | The manual half added a FOURTH and FIFTH current-generation proof point: session 10's image 40 both kinds COMPLETE (first-post-boot push survived a third consecutive session) and image 42 both kinds COMPLETE (the first burst-session full COMPLETEs) on round-#13 firmware. Test 4 PASS stands (interval cadence, disable semantics, shared ID sequence; NVS sequence continued 43->44 across the session-10 crash reboot, balloon4.log System ready + restored next capture ID 44). ADVISORY persists: the auto-capture half's bench evidence predates 01-19/02-04 changes; the WR-03 discriminator has still never run on current HEAD (zero AUTO_CAPTURE at sessions 6-10 — ninth round). See coincidental_reliance_items |

**Score:** 4/5 truths verified (1 present, behavior-unverified — the visible-effect clause, ninth round riding)

### 01-30 Round Truths (the debug round's own acceptance)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | §9 audit: lull-phase reconstruction + candidate arithmetic + determinism + convergence + ranked cause + menu disposition | ✓ VERIFIED | 397 lines appended at d78a6e5 (docs-only commit, verifier-read stats); §9.1-§9.6 + the Task 2 record all present (verifier read §9 in full, lines 920-1314). Every session-9 citation line-locatable; the ELF SHA e09dd034 re-confirmed FIRST; the deployed-driver ESP_ERR_INVALID_STATE semantics read from source with paths (i2c-ng.c:237-293/:80/:130, i2c_master.c:678-738/:726-728 — a bus-level non-DONE COMPLETION, not an FSM refusal); all 8 lull candidates carry the four-property arithmetic and are ELIMINATED; the t≈114 s back-computation; H-lull REJECTED / H-service WEAKENED / H-phase-independent BEST FIT with predicted 01-31 signatures; convergence (b) ranked #1 with counter-evidence carried; elimination-only honestly stated a third time |
| 2 | Task 2 checkpoint: instrument-only per the honesty guard | ✓ VERIFIED | The FIX SHAPE SELECTED record names option (b), the evidence that selected it (§9.5 elimination-only a third time; two bench-disconfirmed levers), and the auto-advance provenance (fifth pending operator confirmation). The guard HELD: no lever shipped, e32_lora.cpp/e32_lora.h/image_protocol.h/platformio.ini untouched by disposition (commit scope verifier-read) |
| 3 | The three instruments landed at the §9.6-named sites with G-01-10 citations + removal conditions | ✓ VERIFIED | [IDLE0]: main_balloon.cpp:138 hook (one volatile increment), :236-237 registration + boot line, :340-361 1 Hz sample + latched frozen line, re-armed on healthy sample. [I2C]: :840-857 i2c_master_probe on the BMP280-invalid transition, latch-guarded with episode/probe counters. [TWDT]: image_tx_manager.cpp:159-172 esp_task_wdt_status one-shot beside B1. 8 REMOVAL CONDITION occurrences across the two files (4 per file, matching the SUMMARY claim); G-01-10 citations in-source on every line |
| 4 | Builds green + harness exit 0 on the round's final commit | ✓ VERIFIED | VERIFIER-RE-RUN at HEAD 377594c: `pio run -e esp32-s3-balloon -e esp32-s3-basestation` = 2 succeeded (56.5 s); `node scripts/verify_protocol_roundtrip.mjs` = exit 0 (all checks PASS). Plus the stronger provenance: the rebuild reproduces the deployed ELF SHA acce782411... EXACTLY |
| 5 | Ledger honesty: WINDOWS 15 + G-01-10 carry the round-#13 determination, stay open, no bench claims | ✓ VERIFIED | Both read: WINDOWS entry 15 carries the ROUND-#13 UPDATE in table + JSON (SESSION-10 clause added by 01-31), status open, counts 2/17/19; 01-UAT G-01-10 root_cause carries the round-#13 determination + the round-#12-discriminator missing item marked DONE; the 01-30-SUMMARY's Bench Claims section says NONE |
| 6 | Protected surfaces byte-identical | ✓ VERIFIED | CMD_TX_CHANNEL_QUIET_MS = 750 (command_sender.h:25); Lever A present (uart_ll_is_tx_idle ×3 in e32_lora.cpp); [MEM] 8 mentions in image_tx_manager.cpp; round-#10 discriminator lines present (budget re-arm in image_tx_manager.cpp, full-pull activation in image_rx_manager.cpp); D2 receipt-ever flag intact; 1a737dd = exactly 4 files, zero deletions |

### 01-31 Round Truths (the bench session's acceptance)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D1 acceptance: all three crash expressions survived, zero crash signatures | ✗ FAILED (fourth expression) | Verifier reproduced every load-bearing line: :1428-:1429 canary panic on IDLE0; the full register dump (PC 0x400570ea memset in ROM; A2 0x3fcbc87c dest, A3 0x0 fill, A4 0x24 len; EXCVADDR 0x0; EXCCAUSE 0x1); the corrupted Core-0 backtrace; the Core-1 SysTickIsrHandler -> xPortSysTickHandler -> xTaskIncrementTickOtherCores -> spinlock_acquire spin; :1478 ELF banner acce78241 (= the verifier's rebuild SHA); :1483 rst:0xc; :1576 [BOOT] PANIC. Class 1 survived a third session (image 40 2/2 COMPLETE); classes 2-3 (sustained multi-window + lull dwell) never reached — the session aborted at the crash |
| 2 | The §9 instruments engaged (or their absence explained as negative evidence) | ✓ VERIFIED (as evidence, the round's designed purpose) | [IDLE0] armed both boots (:33/:1505) with ZERO frozen lines whole-console — the printf-bug diagnosis is sound (esp_freertos_hooks.h:44 esp_err_t return; and structurally: an unarmed hook would freeze the counter and produce frozen lines EVERY second — zero frozen lines proves the counter incremented); starvation DISCONFIRMED. [TWDT] ESP_ERR_NOT_FOUND both boots (:105/:1577) — two-reading question honestly recorded, handle fix routed. [I2C] zero lines with the coverage gap named (:1093 was a WRITE-path failure; instrument wired at main_balloon.cpp:836-866 to the reading path). B1 POWERON/PANIC; B2 zero; [MEM] healthy (stackHW 5772 constant — the breached stack is IDLE0's, unmonitored by [MEM]) |
| 3 | G-01-7 series-A acceptance (ninth attempt) | ✗ FAILED — but FINALLY JUDGED, residual NAMED | 6/8 verdicts COMPLETE (verifier-reproduced base4.log:67-68/:156-157/:269-:270/:425-:426/:588/:712); seq=9 forbidden terminal (:250); image 41 eviction-starved 0/2 with every step a quoted line; the full discriminator census reproduced (budget re-armed ×2 :737/:815; re-announces ×3 :690/:799/:803; holds ×4 all genuine :460/:733/:887/:1644; base deadline :428; class-5 evictions ×3 :816-:817/:1295; evicted-rejections ×7 :1003-:1162 — plus 3 post-reboot recovery rejections :1640-:1674 correctly excluded from the census). Residual: depth-3 queue cycling (see gap 2) |
| 4 | WR-03 cadence discriminator quoted | ✗ NOT RUN | Zero AUTO_CAPTURE commands (verifier-grepped both consoles); ninth round riding; SC-5 advisory NOT discharged |
| 5 | SC-3 visible-effect pairs judged | ✗ NOT RUN | Zero non-RESOLUTION settings commands (verifier-grepped); ninth round riding |
| 6 | CIF-cycle (wire-8) + QVGA-restore clauses | ⚠️ PARTIAL — wire-8 half CLEAN for the first time | SET_RESOLUTION seq=26 ACKed (base4.log:558-:560); image 43 captured CIF-class (23800 B / 119 chunks, balloon4.log:1291 — vs QVGA 6136 B / 31); thumb COMPLETE 7/7 (base4.log:588); ZERO FB-OVF both consoles (verifier-grepped). The full's INCOMPLETE is crash-caused. QVGA restore (wire 6) NOT reached |
| 7 | Dashboard glance operator-LOOKED | ✗ NOT RUN | Commands flowed through the dashboard all session but no LOOK verdicts before the abort |
| 8 | WR-01 battery watch + WR-02 health-check discriminator | ✓ VERIFIED GREEN | Zero 'Critical battery' lines both consoles; beacons batt=valid ×65 / batt=invalid ×0 (verifier census); zero 'health check failed' lines (WR-02 fixed-entry watch holds on the round-#13 firmware) |
| 9 | Ledger honesty: flips only on operator-observed evidence; failed session recorded per convention | ✓ VERIFIED (with one completeness note) | ZERO flips; WINDOWS 15/3 extended in BOTH table and JSON (SESSION-10 ANSWER / SESSION 10 clauses verifier-read), counts 2/17/19 reconcile; 01-UAT G-01-10 FOURTH-expression extension + G-01-7 named-residual update + Test 3/Test 4 'NINTH round riding' notes all verifier-read; STATE.md carries the round outcome + the full round-#14 brief + the /gsd-secure-phase 1 gate; requirements-completed: [] honored; the naming deviation (balloon4/base4 vs balloon15/base10) recorded, files never renamed. NOTE: the ledgers name only seq=9's timeout; base4.log also holds seq=29's post-crash timeout (:681) — verifier-classified crash-caused recovery behavior (beacon seq restart at 0 :671; honest NACK_INVALIDs follow), info-level completeness note, changes no verdict |
| 10-17 | UI-SPEC lifts (7 explicit + LED-truth backstop) | ⚠️ UNVERIFIED-BY-LOG | No dashboard observation session occurred; nothing observed either way — no pass claimed, all ride the post-D1 bench glance |

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Full-resolution gallery image viewer | Phase 3 backlog | Todo on disk (verifier-verified this round) |
| 2 | Gallery thumb number overlay + Incomplete-badge copy | Phase 3 backlog | Todos on disk (verifier-verified) |
| 3 | Thumb-first image delivery + antenna-pointing overlay | Phase 3 backlog | Todos on disk (verifier-verified, captured at 898fcc6) |

Step 9b check: no later milestone phase exists to defer the riding clauses to — Phases 2 and 3 are already executed and Phase 1 closes last. The D1 debug round #4, the G-01-7 admission-control lever, and the post-D1 bench moment are Phase-1 closeout activities routed in STATE.md Next Steps, not later-roadmap deferrals, so they remain gaps.

### Required Artifacts (round #13: plans 01-30, 01-31)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.planning/debug/d1-crash-regression-push-start.md` §9 (01-30) | six subsections + checkpoint record, 397 lines | ✓ VERIFIED | Lines 920-1314 read in full; every claim path-cited; the ELF SHA check recorded before decodes; the Task 2 record names option (b) with provenance |
| `.planning/debug/d1-crash-regression-push-start.md` §10 (01-31) | the fourth-expression dump + instrument answers + round-#14 routing | ✓ VERIFIED | Lines 1315-1465; provenance/SHA first (acce78241 console banner = verifier rebuild); event map (all six events verifier-reproduced); instrument answers with the printf-bug and two-reading [TWDT] honesty; the LABELED stack-capacity hypothesis; the five-item round-#14 routing |
| The instruments: `src/main_balloon.cpp` + `src/image_tx_manager.cpp` (1a737dd) | [IDLE0]/[I2C]/[TWDT] wired, latch-guarded, removal conditions in-source | ✓ VERIFIED | All three verifier-read at HEAD at the cited lines; 8 REMOVAL CONDITION occurrences; G-01-10 citations in-source; zero deletions in the commit |
| `balloon4.log` / `base4.log` (01-31, retained session evidence) | every citation locatable | ✓ VERIFIED | 1768 / 740 lines. Verifier re-read every load-bearing citation: the fatal window (:1296 arm -> :1380 re-arm -> :1427 12/14 -> :1428-:1429 panic), [IDLE0] (:33/:1505), [TWDT] (:105/:1577), [BOOT] (:104/:1576), ELF banner (:1478), rst:0xc (:1483), System ready (:1606), i2cWrite non-fatal (:1093 + reads :1095-:1096), CIF manifest (:1291), discriminator census lines, base ACKs/timeouts/verdicts — all reproduce verbatim. Naming deviation from balloon15/base10 recorded, files never renamed |
| `01-UAT.md` / `WINDOWS.md` / `STATE.md` (01-31) | honest failed-session record, zero flips, round-#14 routed | ✓ VERIFIED | Read: G-01-10 FOURTH-expression extension (stays OPEN); G-01-7 named-residual update (stays OPEN); Test 3/Test 4 session-10 notes; WINDOWS entries 15/3 in table AND JSON, counts 2/17/19 reconciled; STATE.md Next Steps item 1 IS the round-#14 brief; item 2 the /gsd-secure-phase 1 gate |
| `01-REVIEW.md` (377594c, fresh code review this round) | the round's review record | ✓ VERIFIED | 0 critical / 2 warnings / 7 info across 23 files; WR-01 serializeResponse latent-unreachable, WR-02 comment citing nonexistent call sites (misleading during the crash hunt), 7 dead-code infos — all warning/info level, none blocking must-haves |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| The deployed crashing image <-> HEAD source | deterministic-build provenance | ELF SHA256 acce782411... | ✓ WIRED (verifier-reproduced) | The verifier's own rebuild from HEAD reproduces the SHA exactly; the console banner (:1478) matches; §10.1's provenance-first convention held |
| [IDLE0] hook registration | IDLE0 tick liveness | esp_register_freertos_idle_hook_for_cpu(cb, 0) + 1 Hz sample | ✓ WIRED (bench-proven) | main_balloon.cpp:236-237/:340-361; the instrument's designed negative answer delivered (zero frozen lines = counter incrementing = hook armed = IDLE0 alive to the crash instant) |
| [I2C] probe | Wire-0 bus health at the BMP280-invalid transition | i2c_master_probe on i2cBusHandle(0) | ✓ WIRED (coverage gap honestly recorded) | main_balloon.cpp:840-857 — wired to the READING path; the session's failure was on the WRITE path; the gap is named in §10.3 and routed |
| [TWDT] one-shot | TWDT stage-0 subscription fact | esp_task_wdt_status(xTaskGetIdleTaskHandle()) | ⚠️ WIRED BUT AMBIGUOUS (the round's routed defect) | image_tx_manager.cpp:172 — the call is wired and fired (ESP_ERR_NOT_FOUND both boots) but the handle is core-ambiguous from CPU1 setup context; round #14 fixes to ForCPU(0) before further WDT-chain reasoning rides on it |
| Retained surfaces (Lever A, B1/B2, [MEM], round-#10 levers, D2 flag) | round #13 | commit-scope + grep evidence | ✓ WIRED | 1a737dd touched exactly 2 source files (instruments only); Lever A/B1/B2/[MEM]/round-#10 lines/D2 flag all grep-verified present at HEAD |
| The round-#13 work <-> 01-31's D1 acceptance | the session reads the named lines | §9.6's named log lines | ✓ WIRED | The session quoted [IDLE0]/[TWDT]/B1/B2/[MEM] readings per §10.3 — the audit's instruments are exactly what the bench read |

### Data-Flow Trace (Level 4)

Not applicable this round — 01-30 shipped bounded diagnostic instruments whose data sources are real framework calls (the idle-hook counter, i2c_master_probe against the live bus handle, esp_task_wdt_status against the WDT subsystem), and 01-31 shipped no code. Their live outputs are in balloon4.log (:33/:105/:1505/:1577 and the whole-console zero counts).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Dual-target build (independent) | `pio run -e esp32-s3-balloon -e esp32-s3-basestation` | 2 succeeded in 56.5 s (verifier-run at HEAD 377594c) | ✓ PASS |
| Wire-format regression harness | `node scripts/verify_protocol_roundtrip.mjs` | exit 0, all checks PASS (verifier-run) | ✓ PASS |
| Deployed-ELF provenance (deterministic rebuild) | `sha256sum .pio/build/esp32-s3-balloon/firmware.elf` after the verifier's own build | acce78241149382fca72ba810d02e4c6c68042b49d7813243e6effb91ee42428 — matches §10.1's console-verified banner AND the prior .pio artifact byte-for-byte | ✓ PASS |
| Session-10 crash citations | sed balloon4.log :1427-:1429/:1478/:1483/:1576/:1606/:1093-:1096/:1291/:1296/:1380; base4.log :250/:428/:558-:560/:67-:68/:156-:157/:269-:270/:425-:426/:484/:539/:588/:712 | 12/14 sent -> Guru Meditation -> canary IDLE0; ELF banner; rst:0xc; [BOOT] PANIC; clean re-init; non-fatal i2cWrite + reads; CIF manifest 23800 B/119 chunks; window arms; base seq=9 timeout, deadline activation, CIF ACKs, all 8 finalize verdicts — all verbatim | ✓ PASS |
| Crash-signature counts | grep -c per signature, whole balloon4.log | rst:0x7=0, rst:0xc=1, Guru=1, canary=1 (on IDLE0), [IDLE0] frozen=0, [LOOP]=0, [I2C]=0, i2cWrite=1 (:1093, non-fatal class), mojibake raw-byte=1 (:1093) — matches §10's census exactly | ✓ PASS |
| Instrument-engagement census | grep [IDLE0]/[TWDT]/[BOOT]/budget re-armed/re-announce/evict/unknown-evicted/AUX-low, both logs | [IDLE0] registered ×2, frozen 0; [TWDT] NOT_FOUND ×2; holds ×4; budget re-armed ×2; evictions ×3; pre-crash rejections ×7 (+3 post-reboot); AUX-low ×81 — matches the ledgers | ✓ PASS |
| Ledger-count reconciliation | grep open_count/fixed_count/total_count WINDOWS.md | 2/17/19; SESSION-10 clauses present in BOTH table and JSON copies (2+2 matches) | ✓ PASS |
| Forbidden-terminal census | grep -c 'timeout after' base4.log | 2 (:250 seq=9 CAPTURE_NOW — the series-A forbidden terminal; :681 seq=29 window request — post-crash recovery; beacon seq restart at :671 evidences the reboot) — the ledgers name only seq=9; verifier note recorded (info) | ✓ PASS (with note) |
| Riding-clause NOT-RUN confirmation | grep SET_*/AUTO_CAPTURE, base4.log | 0 non-resolution settings commands, 1 SET_RESOLUTION (the CIF wire-8), 0 AUTO_CAPTURE | ✓ PASS |
| Regression watches | grep Critical battery / health check failed / batt= | 0 / 0 / batt=valid ×65, invalid ×0 — WR-01/WR-02 GREEN | ✓ PASS |
| Source drift + commit scopes | `git status --short -- src/ include/ scripts/`; `git show --stat` d78a6e5/1a737dd/a488661/377594c | drift empty; d78a6e5 docs-only (397-line §9); 1a737dd = exactly 4 files zero deletions; 01-31 commits docs-only; camera_manager/image_rx/UI untouched | ✓ PASS |
| WINDOWS integrity | full-file read + counts | 19 entries, open ids exactly {3, 15}, 2/17/19 reconcile across front matter / table / JSON | ✓ PASS |
| Protected-surface greps | CMD_TX_CHANNEL_QUIET_MS / Lever A / [MEM] / round-#10 lines / REMOVAL CONDITION | 750 intact; uart_ll_is_tx_idle ×3; [MEM] ×8; discriminator lines present; 8 removal-condition occurrences | ✓ PASS |
| Debt markers | grep TBD/FIXME/XXX/HACK/PLACEHOLDER in both touched source files | 0 matches | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes exist in this project; the phase's executable verification is the wire-format harness + dual-target build (both run above) — SKIPPED (no probes declared).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| CTRL-01 | 01-30/01-31 (+prior) | User can trigger camera capture from base station web interface | ✓ SATISFIED (regression note) | Session 10: dashboard trigger -> ACK -> capture on round-#13 firmware; image 40 both kinds COMPLETE (first-post-boot push survived a third session) and image 42 both kinds COMPLETE; the D1 crash bounded the session mid-burst — routed as the phase's top blocker |
| CTRL-02 | 01-31 (+prior) | User can adjust all camera settings remotely | ✓ SATISFIED (riding note) | All 7 settings execute for real (01-04 sensor setters; ACKs on the wire); the resolution family gained NEW current-firmware evidence (CIF wire-8 clean); the visible-effect pair judgment rides (SC-3 clause, ninth round) |
| CTRL-03 | 01-31 (+prior) | Manual + automatic capture modes | ✓ SATISFIED (riding note) | Manual bench-proven current-generation (session 8 image 37 end-to-end; sessions 9/10 thumbs+fulls through the crash moments); auto-capture Test 4 PASS (WR-03 discriminator rides, ninth round) |
| CTRL-04 | 01-31 (+prior) | Fixed interval timing | ✓ SATISFIED | AutoCapture wraparound-safe timer; Test 4 cadence PASS; NVS ID sequence held across the session-10 crash reboot (43->44) |
| CTRL-06 | 01-30/01-31 (+prior) | Failed commands retried with timeout | ✓ SATISFIED | Session 10 gave the retry machinery its most demanding live exercise yet: seq=8 ACKed after 2 retries, seq=9/seq=29 honest terminalization at exactly 3 retries; quiet-gate constants intact through round #13 (verifier-read) |
| PRI-02 | 01-30/01-31 (+prior) | Retry mechanism with timeout for failed transmissions | ✓ SATISFIED | Same evidence as CTRL-06 |

Riding IDs per the project's gap-closure convention (marked Complete in REQUIREMENTS.md from their owning phases; no new claims this round — both plans' records honored requirements-completed: [] on the failed-session convention): IMG-03 (01-30), IMG-02/IMG-03 (01-31). No orphaned requirements: REQUIREMENTS.md maps exactly CTRL-01..04, CTRL-06, PRI-02 to Phase 1 (verifier re-grepped: 6 rows, all Complete); REQUIREMENTS.md was NOT touched by round #13 (honored).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (all round-#13 touched source files) | - | TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER scan | — | CLEAN — zero matches across both touched source files (verifier-run) |
| src/image_tx_manager.cpp | 172 | [TWDT] instrument core-ambiguous handle (ESP_ERR_NOT_FOUND both boots) | ⚠️ Warning | A diagnostic returning a misleading reading during the crash hunt; honestly diagnosed as a two-reading question and ROUTED to round #14 (ForCPU(0)) per the plan's no-inline-fix prohibition; not a stub — the call is real and fired |
| src/main_balloon.cpp | 237 | [IDLE0] registered printf bug (esp_err_t->bool prints 0 on SUCCESS) | ℹ️ Info | Cosmetic; the hook provably armed (zero frozen lines requires the counter to increment); routed round #14 |
| src/main_balloon.cpp | 836-866 | [I2C] probe wired to the reading path only; the session's failure was on the WRITE path | ⚠️ Warning (coverage) | The instrument did not fire at its trigger opportunity — recorded as a coverage gap in §10.3, not passed off as a negative answer; routed round #14 |
| src/command_protocol.cpp | 154-179 | 01-REVIEW WR-01: serializeResponse emits a corrupt frame for dataLength above CMD_MAX_RESPONSE_DATA (latent — unreachable with today's factories) | ⚠️ Warning (review) | Not reachable in Phase 1 call paths (review-verified); recorded in 01-REVIEW.md, routed to the ledger |
| (balloon loop comment) | - | 01-REVIEW WR-02: fault-containment comment cites call sites that do not exist | ℹ️ Info (review) | Misleading during the crash hunt; recorded in 01-REVIEW.md with 7 dead-code infos |

### Decision Coverage

CONTEXT.md decisions for this phase were translated in earlier rounds and re-checked at prior verifications; round #13 introduced no new CONTEXT decisions (its Task 2 checkpoint selection is recorded in §9's FIX SHAPE SELECTED subsection and the ledgers; the auto-advance convention was applied correctly — the FIFTH pending operator confirmation). COVERAGE.md re-affirmed for round #13 (no external API surface; detector signal traced to embedded /api/ routes). No drift found.

### Human Verification Required

These items need the physical bench / operator judgment; all are routed (STATE.md Next Steps: debug round #4, then the G-01-7 lever round, then the bench moment), not dropped:

### 1. SC-3 visible-effect pairs (ninth round riding)

**Test:** At fixed QVGA: SET_BRIGHTNESS -2, capture; SET_BRIGHTNESS +2, capture; judge the pair visibly different. Repeat for one other class (contrast -2/+2, saturation -2/+2, or quality 10 vs 30).
**Expected:** Visible pairwise differences, per-pair verdicts recorded in Test 3.
**Why human:** Sensor visible-effect acceptance needs the physical camera and operator judgment; nine bench rounds have issued zero non-RESOLUTION settings commands.

### 2. WR-03 cadence discriminator

**Test:** AUTO_CAPTURE_ENABLE at 20 s; let one interval capture fire; trigger a manual CAPTURE_NOW; compare the next interval capture's timestamp to the manual one.
**Expected:** Next interval capture lands ~20 s AFTER the manual capture (both 'Captured image ID' timestamps quoted). Also discharges the SC-5 coincidental-reliance advisory.
**Why human:** Needs the running firmware pair and wall-clock observation; never run on current HEAD.

### 3. D1 round-#4 bench + the series-A re-run (after the [STACK] instrument answers and the admission-control lever lands)

**Test:** On post-round-#4 firmware: the three crash-expression survivals (first-post-boot push, sustained multi-window full, >=3-minute lull dwell) with the [STACK]/[TWDT]/[I2C] instrument readings quoted; then 3 unspaced captures.
**Expected:** Zero crash signatures (grep counts quoted, mojibake via the corrected raw-byte method); the [STACK] watermark either confirms or refutes the stack-marginality reading; series A: 3/3 ACKs, 6/6 COMPLETE, zero forbidden terminals, with the admission-control lever's discriminator lines proving engagement.
**Why human:** RF-pair behavior under real half-duplex load; hardware acceptance only.

### 4. Dashboard LOOK glance + UI-SPEC lifts + QVGA-restore clause

**Test:** Exercise the dashboard (d8ba14e gallery detail still unlooked-at after six sessions); QVGA restore (SET_RESOLUTION wire 6) with the next capture QVGA-class by wire evidence, zero FB-OVF.
**Expected:** All seven UI-SPEC considerations + LED-truth backstop hold; gallery detail fields render; the restore clause judged as specified.
**Why human:** Browser observations leave no console evidence; operator-looked (not did-not-flag) is the bar.

### 5. FIVE pending auto-advanced disposition confirmations (human decision points)

**Test:** Confirm (or route back) the five auto-selected dispositions now pending end-of-phase operator confirmation: 01-23's fix-all (WINDOWS 10-14), 01-25's fix-shape option-a + instrumentation ride-along, 01-26's option-a fix-all (WINDOWS 17-19), 01-28's option (a)+(b) (Lever A + B1/B2), and 01-30's option (b) instrument-only ([IDLE0]/[I2C]/[TWDT]). Note THREE of the five levers/instrument-sets are now bench-disconfirmed or defect-revealed as sufficient (01-25's warm-up at session 8; 01-28's Lever A at session 9; 01-30's instruments answered but revealed their own defects) — the confirmation questions for those are whether they STAY (all are hazard removals/diagnostics whose retention is harmless and arguably load-bearing for round #4) while the next fix supersedes them.
**Expected:** Each confirmation (or route-back) recorded in the ledger.
**Why human:** Acceptance of robustness-edge-case risk and fix-shape selection is a maintainer judgment; all five were auto-advanced under workflow.auto_advance and honestly recorded as pending.

### Gaps Summary

Three structured gaps, all honestly ledgered — the same three as verification #11, materially evolved:

1. **D1 crash regression, FOURTH EXPRESSION (G-01-10 / WINDOWS 15, BLOCKER)** — the instrument-only round worked exactly as designed and still the balloon died: an IDLE0 stack-canary panic with a full dump at the campaign's TX-heaviest moment. The evidence base is TRANSFORMED, not merely extended: starvation is DISCONFIRMED for this crash (the [IDLE0] instrument's first positive answer), the §7 WDT-topology premise is under honest re-examination ([TWDT] ESP_ERR_NOT_FOUND — two readings, routed), the working hypothesis is a labeled CPU0-side fault family with load-dependent signature (stack-capacity breach under IRQ pressure vs scheduler/tick wedge), and round #14 has a one-session discriminator ([STACK] watermark) before any lever. This remains the phase's top blocker: no complete clean bench session exists on HEAD until it closes.
2. **G-01-7 series-A burst full-delivery** — from eight rounds of "unjudgeable" to FAILED-WITH-NAMED-RESIDUAL: the series ran (6/8 COMPLETE, best ever), every round-#10 discriminator engaged for the first time, and the residual is named by the discriminators themselves (depth-3 queue cycling evicts the base-activated prior full). The lever class is identified (admission control / oldest-eviction) and gated behind D1 round #4.
3. **The riding bench-moment clauses** — SC-3 visible-effect pairs (ninth round), WR-03 cadence discriminator (ninth), QVGA restore, operator-LOOKED dashboard glance + UI-SPEC lifts. One clause half-discharged this round (CIF wire-8 clean as specified, first time). All routed; all need one clean post-D1 session.

SUMMARY vs Reality: no material divergences found this round — every load-bearing claim in 01-30/01-31-SUMMARY that this verifier checked reproduces (the §9/§10 structure and citations, the three instruments at their cited lines with removal conditions, both ledger gates re-run independently, the deterministic ELF-SHA provenance, every crash/dump/instrument/discriminator/verdict citation, all grep counts including the corrected mojibake method at raw-byte level, ledger integrity in both copies, the zero-flip convention, requirements-completed: [], naming-deviation handling, the routed-not-fixed instrument defects). One completeness note (verifier-found, info-level): base4.log holds a second 'timeout after 3 retries' (seq=29, post-crash recovery class) that the session-10 ledger records do not name — it changes no verdict (the acceptance already failed on seq=9) and the crash-caused classification is solidly evidenced; a one-line census amendment at the next ledger touch completes the record.

The phase goal itself — bidirectional LoRa command/control with ACK, retry, manual + auto capture — remains achieved and hardware-proven, and this round STRENGTHENED it: the command loop ran the busiest session of the campaign (33 commands queued, retries firing and resolving live, the first burst-session full COMPLETEs, the CIF wire-8 clause finally exercised clean), and the crash-hunt discipline produced the campaign's first positive instrument answer. But HEAD firmware still hard-crashes during the post-capture transfer lifecycle (four expressions, four signatures, one labeled family), the burst-delivery gap has its first named mechanism but no lever yet, and the riding clauses accumulate a ninth round. The honest verdict is gaps_found with a clear, evidence-ranked next action: debug round #4 ([STACK] instrument FIRST — no lever until it answers), then the G-01-7 admission-control lever, then one clean bench session that judges everything at once.

---

_Verified: 2026-08-28T18:05:00Z_
_Verifier: Claude (gsd-verifier)_
