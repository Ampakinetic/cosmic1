---
phase: 01-command-protocol-control
verified: 2026-08-28T14:40:55Z
status: gaps_found
score: 4/5 must-haves verified
behavior_unverified: 1 # SC-3, narrowed to its final clause for the TENTH consecutive round: resolution-class clauses bench-proven (sessions 4/5, CIF wire-8 at session 10, AND now SVGA wire-10 at session 11 — 32850 B / 165 chunks, re-init 6->11, zero FB-OVF) and camera_manager.cpp still untouched; session 11 issued zero non-RESOLUTION settings commands (verifier-grepped both consoles: 0 SET_BRIGHTNESS/contrast/saturation/quality/exposure/WB) — the pairwise visible-effect judgments remain operator-unjudged
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/5
  gaps_closed: [] # NONE of the three #13 gaps closed — but BOTH blockers materially advanced. Gap 1 (D1): the round's designed deliverable WORKED — the campaign's FIRST zero-crash session (balloon5.log/base5.log) delivered all three instrument answers: [TWDT]=ESP_OK settles the two-reading question per the §11.2 pre-registered prediction, [STACK] refutes the stack-capacity hypothesis per §11.3's pre-written rule (240-word baseline, zero new-lows), [I2C] closed by absence; starvation (sessions 10/11) AND stack capacity (session 11) are both DISCONFIRMED; the wedge family is the remaining candidate; D1 stays open on the honest ladder bars (classes 2/3 not met). Gap 2 (G-01-7): the first lever landed against a bench-NAMED residual ENGAGED (in honest-reject form, discriminator line fired 8x) and FAILED via a NEW named mode — OVER-REJECTION (rejects regardless of capacity need at 2/5 occupancy) — a distinct recorded verdict from never-engaged; the capacity-aware admission fix is routed. Gap 3 unchanged in substance: TENTH round riding
  gaps_remaining:
    - "D1 crash regression (G-01-10 / WINDOWS 15) — STILL OPEN after the campaign's FIRST zero-crash session, with the evidence base transformed a second time: the round-#14 instruments ANSWERED exactly per their pre-registered predictions (balloon5.log, round-#14 firmware, balloon ELF SHA256 32d31f270cb8... verifier-reproduced from HEAD): [TWDT] = ESP_OK (:105) resolves the two-reading question — IDLE0 genuinely IS TWDT-subscribed, §7's WDT topology premise STANDS, the sessions-8/9 TG0WDT attribution survives, session-10's ESP_ERR_NOT_FOUND was the wrong-handle instrument bug (fix proven live by [IDLE0] registered=1 :33); [STACK] = the 240-word boot baseline (:138) is the ONLY watermark line whole-console, ZERO new-lows through every phase including all multi-window service — the IDLE-stack-capacity hypothesis REFUTED per §11.3's pre-written rule, with the honest caveats recorded (no crash instant to read; the session's TX-heaviest stretch was lighter than session 10's — AUX-missed x40 vs x81, largest SERVED image 41 chunks vs 119); [I2C] = 0 lines + 0 i2cWrite (no episode; the branch-b swallowing disposition stands as the coverage answer). Ladder honesty: class 1 passed a FOURTH session (image 44 first-post-boot push both kinds COMPLETE base5.log:99/:275, 41 chunks across 3 windows); class 2's >=7-window SVGA bar NOT met (image 45's SVGA full never completed — it IS the over-rejection evidence); class 3's >=3-min dwell NOT met (~50 s, [BCN] seq=27 :641 -> seq=37 :767). Zero crash signatures whole-console (rst:0x7 / rst:0xc / Guru / canary / i2cWrite / corrected-raw-byte mojibake each 0 — verifier-grepped). With starvation AND stack capacity both disconfirmed, the WEDGE FAMILY (sessions 7/8/9's scheduler/tick-chain class) is the remaining candidate and the §11.4 A/B hook-unregistered build rises as round #15's discriminator. Round-#15 routed in STATE.md + §11.6"
    - "G-01-7 series-A burst full-delivery — the round-#14 lever ENGAGED-and-FAILED via a NEW named mode, OVER-REJECTION: series A as designed (3 back-to-back unspaced) NOT RUN (the session produced 2 captures ~50 s apart — images 44/45), and the lever's honest-rejection form fired EIGHT times against image 45's SVGA FULL (32850 B / 165 chunks): 'window request for image 45 rejected - queue holds only receipt-evidenced entries (G-01-7)' balloon5.log:883/:896/:916/:944/:986/:1011/:1034/:1056, mirroring base NACK_INVALID seq 16/17/18/20/21/23/24/25 (base5.log:403-:516) and two finalize INCOMPLETE 0/165 lifecycles (:456-:457, :522-:523) — while the queue held 2 of 5 slots. Mechanism confirmed at source (evictEntriesOlderThan, image_tx_manager.cpp:1381-1456): the scan forces lowest-class eviction whenever ANY older candidate exists and returns false when every older candidate is class 5 — regardless of whether admission needs a slot; the sole older candidate was image 44's balloon-side entry (base-side fully COMPLETE base5.log:99/:275, balloon-side still ANNOUNCED-armed = class 5 until TTL — the balloon has no transfer-complete feedback), so the protection starved the newer pull for an entry whose data was already fully delivered. Thumbs immune by construction (kind-0 bypasses the scan — image 45's thumb 6/6 while its FULL starved). Zero avoid-form lines, zero queue-overflow, zero evictions, zero command timeouts (4th consecutive session). Fix class routed: capacity-aware supersede admission (force-evict only on actual capacity need; SERVED/complete entries as the safe reclaim class) + the fresh 01-REVIEW WR-02 companion fix (receipt stamp before range validation — see regressions)"
    - "The riding bench-moment clauses — TENTH round (nothing reached, honestly recorded): SC-3 visible-effect pairs NOT reached (0 non-RESOLUTION settings commands — verifier-grepped); WR-03 cadence discriminator NOT reached (0 AUTO_CAPTURE commands); QVGA restore NOT reached (the camera was left at SVGA — no wire-6 restore issued); dashboard operator-LOOKED glance + UI-SPEC lifts NOT reached. All recorded TENTH-round in Test 3/Test 4 notes (verifier-read) and routed to the round-#15 shared bench session"
  regressions:
    - "NONE in code: round #14's code work is exactly two commits, scopes verifier-verified (7d3abbc = 2 source files, instruments only, +68/+15 with the idle-hook counter/frozen-latch byte-identical; 61e19b7 = 4 code files, queue policy only). Builds 2/2 SUCCESS (56.7 s) and the wire-format harness exit 0 both VERIFIER-RE-RUN at HEAD; the verifier's up-to-date rebuild at HEAD reproduces BOTH deployed ELF SHAs byte-for-byte (balloon 32d31f270cb8..., base ce657093cb0a... — the exact images session 11 ran); the A/B-disabled build (-DG01_D1_IDLE_HOOK_DISABLED=1) VERIFIER-RUN SUCCESS, restoring cleanly afterward. Protected surfaces intact: CMD_TX_CHANNEL_QUIET_MS = 750 (command_sender.h:25); Lever A (uart_ll_is_tx_idle x3); [MEM] x8; B1/B2; round-#10 discriminator lines (budget re-arm, class-5 overflow labels, base deadline release); D2 receipt-ever flag; evictionClassOf body unforked; sweepExpiredEntries TTL 900000 unchanged; camera_manager.cpp / image_rx_manager.cpp / UI untouched. D2 (WINDOWS 16) and WR-02 (WINDOWS 18) watches GREEN on the round-#14 firmware (0 health-check-failed). WR-01 GREEN (0 Critical battery; batt=valid x52 / invalid x0 — verifier-grepped base5.log). PRI-01 orderings verified in source (beacon early-return :279-282 precedes pushPending :286; preemption :678 precedes findActiveEntry :689). NEW this round (post-round review commit 446959e, NOT a regression of round-#14 work but two findings IN round-#14 code): 01-REVIEW WR-01 — the [STACK] instrument's zero sentinel means a genuine 0-word watermark (the CONFIRM-arm signature) can be latched but NEVER printed (print guard s_idle0StackMinWords != 0, main_balloon.cpp:427 — reviewer reading verifier-confirmed at source) — must be fixed BEFORE the round-#15 heavy-tail re-run or the discriminator cannot report the exact signature it hunts; 01-REVIEW WR-02 — the per-entry receipt stamp (image_tx_manager.cpp:1137) and budget re-arm fire BEFORE the per-kind range validation (:1149), so a CRC-valid range-invalid request promotes an entry to protected class 5 and re-arms its budget (verifier-confirmed at source) — companion fix for the round-#15 lever. Plus 01-REVIEW WR-03 (supply-chain hash lock fails open on unknown vendor entries — hardening, today's output unaffected). All three are warning-level, honestly recorded in 01-REVIEW.md (0 critical / 3 warnings / 7 info), and NOT yet routed in STATE.md (the review commit postdates the last STATE update) — route at the next ledger touch"
gaps:
  - truth: "The balloon survives post-capture radio push through sustained FULL-window service AND its inter-window lulls (G-01-10 / WINDOWS 15, the phase's top blocker): a capture's enqueue/manifest/chunk/window/lull sequence never hard-resets the firmware, so buffers survive to serve the transfer"
    status: failed
    reason: "Bench session #11 (01-34, round-#14 firmware — the verifier's HEAD rebuild reproduces both deployed ELF SHAs exactly): ZERO crashes end-to-end (the campaign's first), and all three round-#14 instruments ANSWERED per their pre-registered predictions — [TWDT]=ESP_OK (:105, two-reading question RESOLVED: IDLE0 IS subscribed, §7 stands, session-10's NOT_FOUND was the wrong-handle bug), [STACK] refutes stack capacity (240-word baseline :138 sole watermark line, zero new-lows; caveats honestly recorded: no crash instant, TX lighter than session 10), [I2C] closed by absence. BUT the acceptance bars were not met: class 2's >=7-window SVGA bar NOT met (the SVGA test image 45 never completed — starved by the G-01-7 over-rejection), class 3's >=3-min dwell NOT met (~50 s), series A not run. Zero-crash is survival evidence, not a fix verdict — no lever landed between sessions, and the campaign's lightest TX session cannot clear a load-dependent fault family. D1 stays OPEN with the wedge family (sessions 7/8/9) the remaining candidate class"
    artifacts:
      - path: ".planning/debug/d1-crash-regression-push-start.md"
        issue: "none — §11.6 carries the session-11 record in the house convention (provenance/ELF SHAs first, instrument answers against the predicted signatures, census, ladder honesty, round-#15 routing); NO §12 because nothing crashed; the gap is the unmet bars, not the record"
      - path: "src/main_balloon.cpp"
        issue: "the [STACK] instrument works for the healthy-margin reading it delivered, but the fresh 01-REVIEW WR-01 defect (zero sentinel: a genuine 0-word watermark latches at :160 yet the print guard at :427 can never emit it) would blind the CONFIRM arm of the round-#15 heavy-tail re-run — fix the sentinel before that session"
    missing:
      - "Debug round #5 (round #15): build the §11.4 A/B hook-unregistered image (G01_D1_IDLE_HOOK_DISABLED — VERIFIER-PROVEN buildable this round) + fix the [STACK] zero sentinel (01-REVIEW WR-01) + re-run the ladder heavy-tail bars (>=7-window SVGA sustained service, >=3-min lull dwell) at session-10-class TX load, with the A/B build's survival/loss discriminating hook-dispatch vs kernel-side wedge"
      - "Then the G-01-7 capacity-aware admission lever + the series-A full protocol on the shared bench session; if both close, /gsd-secure-phase 1 is the remaining phase-close gate"
  - truth: "G-01-7 series-A acceptance (burst full-delivery): 3 back-to-back unspaced captures — 3 ACKs, 3 captures, 6/6 verdicts COMPLETE, zero command timeouts, zero silently absent fulls, with the lever's discriminator lines proving engagement (or the residual mechanism named)"
    status: failed
    reason: "The round-#14 lever (01-33: IMG_TX_QUEUE_DEPTH 3->5 with bounded arithmetic + supersede-path victim selection ranked through an unforked evictionClassOf) ENGAGED for the first time in its honest-rejection form and FAILED via the NEW over-rejection mode: 8 honest-reject lines (balloon5.log:883-:1056) ↔ 8 NACK_INVALIDs (base5.log:403-:516), image 45's SVGA FULL starved 0/165 through two full D-24 lifecycles (:456-:457, :522-:523) at 2/5 queue occupancy, behind image 44's fully-delivered-but-still-queued entry. The verdict is ENGAGED-AND-FAILING (distinct from never-engaged — the discriminator lines did exactly their designed job), every step a quoted line, and the mechanism is confirmed at source. Series A as a 3-capture protocol never ran (2 captures ~50 s apart). Zero command timeouts a 4th consecutive session — the forbidden terminal did not recur"
    artifacts:
      - path: "src/image_tx_manager.cpp"
        issue: "evictEntriesOlderThan (:1381-1456) refuses admission whenever every older candidate is class 5, with no capacity-need check and no safe-reclaim ranking for fully-delivered (SERVED/complete, balloon-side ANNOUNCED-until-TTL) entries — the over-rejection mode; the fresh 01-REVIEW WR-02 adds a companion defect: the per-entry receipt stamp (:1137) precedes range validation (:1149), letting malformed requests pin entries into the protected class"
    missing:
      - "Round #15 companion lever: capacity-aware supersede admission — force-evict only when admission actually needs a slot, treat balloon-side fully-delivered entries as the safe reclaim class ahead of in-flight receipt-evidenced entries; fold in the 01-REVIEW WR-02 stamp-order fix (stamp only fully-validated requests)"
      - "Then the full series-A protocol on the round-#15 shared bench session: 3 unspaced captures, 3/3 ACKs, 6/6 COMPLETE, zero forbidden terminals, with the lever's discriminator lines proving engagement or absence explained"
  - truth: "The bench-moment clauses: SC-3 visible-effect pairs judged; WR-03 cadence discriminator quoted; QVGA-restore clause exercised; dashboard operator-LOOKED glance + UI-SPEC lifts"
    status: partial
    reason: "TENTH round riding, nothing reached, everything honestly recorded: the session's only settings command was the single SET_RESOLUTION wire 10 (seq=12, for the class-2 SVGA attempt — which ADDS current-firmware evidence to SC-3's resolution-class clause: image 45 captured SVGA-class 32850 B / 165 chunks after re-init 6->11 with ZERO FB-OVF both consoles, verifier-grepped); 0 non-RESOLUTION settings commands, 0 AUTO_CAPTURE, no wire-6 restore, no dashboard LOOK verdicts. Test 3 stays 'issue' on its visible-effect clause; the resolution clause now rests on THREE independent current-generation proofs (sessions 4/5 bench, session 10 CIF wire-8, session 11 SVGA wire-10)"
    artifacts:
      - path: ".planning/phases/01-command-protocol-control/01-UAT.md"
        issue: "Test 3 stays 'issue' — the visible-effect-pair clause is its last open settings clause; Test 4's WR-03 note stays unjudged (tenth round)"
    missing:
      - "Post-D1 bench session (routed to the round-#15 shared session): SC-3 pairs (brightness -2/+2 plus one other class at fixed QVGA), WR-03 cadence discriminator (AUTO_CAPTURE_ENABLE 20 s + manual capture, quote both 'Captured image ID' timestamps — also discharges the SC-5 coincidental-reliance advisory), QVGA restore (wire 6) with wire-evidence sizing + zero FB-OVF, dashboard LOOK glance incl. the d8ba14e gallery-detail fields and all UI-SPEC lifts"
deferred:
  - truth: "Full-resolution gallery image viewer (operator bench wish; presentation-only)"
    addressed_in: "Phase 3 backlog"
    evidence: ".planning/todos/pending/2026-08-23-full-resolution-gallery-image-viewer.md (verifier-verified on disk this round)"
  - truth: "Gallery thumb number overlay + Incomplete-badge explanatory copy (operator bench friction)"
    addressed_in: "Phase 3 backlog (gallery UX)"
    evidence: ".planning/todos/pending/2026-08-24-gallery-thumb-number-overlay.md and 2026-08-24-incomplete-badge-on-renderable-images.md (verifier-verified on disk)"
  - truth: "Thumb-first image delivery + antenna-pointing overlay (operator requests)"
    addressed_in: "Phase 3 backlog"
    evidence: ".planning/todos/pending/2026-08-27-thumb-first-image-delivery-with-on-demand-full-retrieval.md and 2026-08-27-antenna-pointing-overlay-for-base-web-ui.md (verifier-verified on disk)"
  - truth: "Balloon SD-card picture cache (STORE-01..04) — NEW this round, operator-directed"
    addressed_in: "Phase 2.5 (inserted post-round by db9625e)"
    evidence: ".planning/ROADMAP.md Phase 2.5 section + .planning/todos/pending/2026-08-28-balloon-sd-picture-cache.md; the insertion cites session-#11 over-rejection evidence as motivation — it is a NEW capability phase, NOT a deferral target for D1 or G-01-7, which remain Phase-1 closeout gaps"
behavior_unverified_items:
  - truth: "Balloon receives camera commands and adjusts camera settings accordingly (SC-3 — pairwise visible-effect clause only)"
    test: "At fixed QVGA, adjust ONE setting between two captures and compare the pair (brightness -2 vs +2, plus at least one other class: contrast, saturation, or quality 10 vs 30)"
    expected: "Visible pairwise differences per exercised setting, recorded per pair"
    why_human: "Sensor visible-effect acceptance needs the physical camera and operator judgment — TEN consecutive bench rounds have issued zero non-RESOLUTION settings commands (session-11 command mix verifier-grepped: 0 SET_BRIGHTNESS/contrast/saturation/quality/exposure/WB; the session's single settings command was the class-2 SVGA attempt). The resolution clause is now TRIPLE-supported: sessions 4/5 bench-proofs + session 10's clean CIF wire-8 + session 11's clean SVGA wire-10 (32850 B / 165 chunks, re-init, zero FB-OVF) with camera_manager.cpp untouched since 01-13"
coincidental_reliance_items:
  - truth: "Both manual trigger and interval-based auto-capture work end-to-end (SC-5)"
    reason: "fixture-only"
    harden: "The manual half keeps strengthening: session 11 added a SIXTH and SEVENTH current-generation proof point (image 44 both kinds COMPLETE base5.log:99/:275 — the first-post-boot push survived a FOURTH consecutive session; image 45 thumb COMPLETE :382). The auto-capture half still rests on Test 4's bench PASS, which predates the 01-19 baseline advance and 02-04 event triggers; non-interference is established by code reading only (zero AUTO_CAPTURE commands at sessions 6-11 — TENTH round). The WR-03 cadence discriminator at the post-D1 bench moment is precisely the hardening step — routed in STATE.md Next Steps"
---

# Phase 1: Command Protocol & Control Verification Report (Re-verification #13, after gap closure 01-32 + 01-33 + 01-34)

**Phase Goal:** Establish bidirectional LoRa communication for camera control
**Verified:** 2026-08-28T14:40:55Z
**Status:** gaps_found
**Re-verification:** Yes — #13, after gap-closure round #14 (plans 01-32 D1 debug round #4 instrument package, 01-33 G-01-7 lever round, 01-34 bench session #11; commits 7d3abbc/b33d07c/a672f70/4c35ddc, b98934d/61e19b7/70191a9, e2b1ff2/48131d0 — all verifier-confirmed in git log; zero source drift HEAD..working-tree in src/, include/, scripts/; the verifier's up-to-date rebuild at HEAD reproduces BOTH deployed ELF SHAs byte-for-byte — balloon 32d31f270cb8..., base ce657093cb0a... — the exact images session 11 ran)

## Goal Achievement

Round #14 executed its three plans exactly as written, and — for the first time in the D1 campaign — the hardware session answered every question it was asked without opening a new one. The instrument package (01-32) worked precisely to its §11 design: the [TWDT] ForCPU(0) fix returned ESP_OK, exactly what the pinned-sdkconfig derivation predicted in writing BEFORE the session, converting session-10's ambiguity into a settled fact (IDLE0 genuinely IS watchdog-subscribed; the sessions-8/9 watchdog attribution stands; session-10's NOT_FOUND was the instrument's own wrong-handle bug — now proven fixed by registered=1). The [STACK] watermark instrument delivered its one-session discriminator answer: 240 words free at boot, never lower, through every multi-window service — the stack-capacity hypothesis is REFUTED under §11.3's pre-written rule, with its caveats recorded rather than smoothed. The [I2C] disposition closed by absence. The result is the campaign's FIRST zero-crash session — and the ledgers refuse to over-claim it: zero-crash is survival evidence, not a fix verdict; two of three candidate mechanisms are now DISCONFIRMED, the wedge family remains, and D1 stays open on the honest ladder bars (classes 2/3 not met).

The G-01-7 lever round (01-33) landed the campaign's first lever against a bench-NAMED residual, with the lever-selection audit's arithmetic comment shipped beside the constant and evictionClassOf reused verbatim. The bench (01-34) then produced the round's most instructive failure: the lever ENGAGED — its honest-rejection discriminator line fired eight times, verbatim as designed — and FAILED via a mechanism nobody had named before, because no prior round had instrumented this path: OVER-REJECTION. The queue held 2 of 5 slots; the sole older candidate was an entry whose data the base had fully received; the protection refused admission anyway, because the code has no capacity-need check and no safe-reclaim class for fully-delivered entries. Every step is a quoted line at both consoles. The verdict recorded is engaged-and-failing, distinct from never-engaged — exactly the three-way separation the lever's discriminator lines were built to provide.

What still did not happen: the full series-A protocol (2 captures, not 3 back-to-back), the ladder's heavy-tail bars, and — for the TENTH round — the SC-3 visible-effect pairs, the WR-03 cadence discriminator, the QVGA restore, and the operator-LOOKED dashboard glance. All routed to the round-#15 shared bench session.

Post-round, a fresh code review (446959e, 0 critical / 3 warnings / 7 info) examined the round-#14 code and found two real defects IN it — the [STACK] zero-sentinel (which would blind the CONFIRM arm of the next re-run: the review's reading is verifier-confirmed at source) and the receipt-stamp-before-range-validation order (verifier-confirmed at source) — both warning-level, both routed to round #15 in the round routing, though STATE.md predates the review commit and should carry them at its next touch.

Note on mode: ROADMAP.md marks Phase 1 `Mode: mvp`, but the goal is not in user-story format, so standard goal-backward verification applies (same determination as all prior rounds).

### Observable Truths

Must-haves are the 5 ROADMAP success criteria (roadmap contract governs; the round's own 01-32/01-33/01-34-PLAN truths are assessed separately below).

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Base station web interface has camera control section with trigger button and settings forms (SC-1) | ✓ VERIFIED | Regression check: round #14's commits touch no UI (7d3abbc = 2 source files of instruments; 61e19b7 = 4 code files of queue policy; the verifier read both commit stats); the last UI change remains d8ba14e. Session 11 drove the dashboard throughout (both CAPTURE_NOWs and the SET_RESOLUTION queued through it; base5.log /status polls throughout). The LOOK glance rides the round-#15 bench |
| 2 | LoRa command packets transmitted from base station to balloon and acknowledged (SC-2) | ✓ VERIFIED (regression caveat: D1 + burst-delivery) | Session 11's command loop is clean: seq=12 SET_RESOLUTION ACKed (balloon executed "Set resolution to 10" :782, SUCCESS :785); both CAPTURE_NOWs captured (images 44/45); zero "timeout after 3 retries" both logs (verifier-grepped — the forbidden terminal absent a 4th session); the 8 window-request NACK_INVALIDs are the balloon HONESTLY REJECTING under the new lever — bidirectional communication working exactly as designed, the starvation it names tracked by the G-01-7 gap, not hidden here. The D1 axis: zero crashes this session — the campaign's first clean session on record |
| 3 | Balloon receives camera commands and adjusts camera settings accordingly (SC-3) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | NARROWED TO ITS FINAL CLAUSE, TENTH round: the resolution clause gained its THIRD current-firmware proof — session 11's SVGA wire-10 cycle (seq=12 ACKed -> image 45 captured SVGA-class 32850 B / 165 chunks after camera re-init 6->11, thumb COMPLETE 6/6, ZERO FB-OVF both consoles, verifier-grepped) with camera_manager.cpp untouched since 01-13 (90300b8; commit scopes verifier-read through HEAD; zero source drift). ONLY the pairwise visible-effect judgments remain — zero non-RESOLUTION settings commands for the TENTH consecutive round (verifier-grepped both consoles). See behavior_unverified_items |
| 4 | Failed commands are retried with timeout and user is notified (SC-4) | ✓ VERIFIED | The retry machinery's standing evidence (sessions 5/6/7/10: honest terminalization at exactly 3 retries, retry ladders resolving live, NACK_INVALID honest rejections) stands; session 11 added the transfer-layer's disciplined face: image 45's full went through TWO complete bounded D-24 pass lifecycles, each honestly terminalized INCOMPLETE at exactly 3 passes, each request an honest NACK — bounded retry with named outcomes, never silent. Zero command timeouts a 4th consecutive session |
| 5 | Both manual trigger and interval-based auto-capture work end-to-end (SC-5) | ✓ VERIFIED (coincidental-reliance) | The manual half added a SIXTH and SEVENTH current-generation proof point: session 11's image 44 both kinds COMPLETE (the first-post-boot push survived a FOURTH consecutive session) on round-#14 firmware. Test 4 PASS stands (interval cadence, disable semantics, shared ID sequence; NVS ID sequence continuous — image 44/45 follow the session-10 crash reboot's 43->44 restore). ADVISORY persists: the auto-capture half's bench evidence predates 01-19/02-04 changes; the WR-03 discriminator has still never run on current HEAD (zero AUTO_CAPTURE at sessions 6-11 — TENTH round). See coincidental_reliance_items |

**Score:** 4/5 truths verified (1 present, behavior-unverified — the visible-effect clause, tenth round riding)

### 01-32 Round Truths (the debug round's own acceptance)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The [STACK] instrument is wired (throttled in-hook sample, running-min latch, 1 Hz new-low print, citations + removal conditions) | ✓ VERIFIED | Source read at HEAD: hook sample main_balloon.cpp:155-166 (uxTaskGetStackHighWaterMark(NULL) :159, the (count & 0x3FF)==0 throttle :158, min-latch :160-161), print :427-432 with the exact format string; REMOVAL CONDITION count 7+4=11 as the summary claims; G-01-10 + §10.5 citations in-source. BENCH-PROVEN ENGAGED: the :138 baseline line is the exact designed output |
| 2 | The [TWDT] handle fix + the WDT-config re-derivation settle the two-reading question on paper | ✓ VERIFIED | image_tx_manager.cpp:172 esp_task_wdt_status(xTaskGetIdleTaskHandleForCPU(0)) with the ROUND-#14 comment; §11.2 re-derivation with the exact pinned-sdkconfig path (framework-arduinoespressif32-libs/esp32s3/sdkconfig:2175-2180: INIT=y, CHECK_IDLE_TASK_CPU0=y, CPU1 not set, TIMEOUT_S=5, PANIC=y) — prediction (a) recorded in writing, then CONFIRMED live at 01-34 (ESP_OK :105) |
| 3 | The [IDLE0] registered printf fix (esp_err_t == ESP_OK) | ✓ VERIFIED | main_balloon.cpp:277-278 read at HEAD; BENCH-PROVEN: registered=1 at both... at the single boot (balloon5.log:33) |
| 4 | The [I2C] write-path coverage dispositioned per the decision tree | ✓ VERIFIED | Branch (b) executed: NO write-path poller added (grep: only the reading-path probe :904-921); §11.3.1 records the swallowing chain with source paths (Adafruit_SSD1306::display() void, discards all five endTransmission returns; TwoWire keeps no error state); the HAL [E] i2cWrite line named as the write-path observable. Session 11: 0 i2cWrite lines — no episode, disposition stands |
| 5 | The A/B hook guard (G01_D1_IDLE_HOOK_DISABLED) with #else self-identification | ✓ VERIFIED | main_balloon.cpp:267-281: the guard wraps ONLY the registration pair; the #else prints the DISABLED line (:280); §11.4 records the recipe. VERIFIER-RUN: PLATFORMIO_BUILD_FLAGS="-DG01_D1_IDLE_HOOK_DISABLED=1" pio run -e esp32-s3-balloon = SUCCESS; default build restored and SHA-verified afterward |
| 6 | Builds green + harness exit 0 + every retained surface byte-intact | ✓ VERIFIED | VERIFIER-RE-RUN at HEAD: dual build 2/2 SUCCESS (56.7 s); harness exit 0; quiet gate 750 (command_sender.h:25); uart_ll_is_tx_idle x3; [MEM] x8; round-#10 discriminator lines present; D2 receipt-ever flag present; B1/B2 present; REMOVAL CONDITION count 11 |
| 7 | Ledger honesty: WINDOWS 15 + G-01-10 carry the round-#14 package, stay open, no bench claims | ✓ VERIFIED | WINDOWS entry 15 ROUND-#14 UPDATE in BOTH table and JSON (verifier-parsed: 19 entries, open ids exactly {3,15}, counts 2/17/19); 01-UAT G-01-10 extended; §11 tabulates every discriminator line; the 01-32 SUMMARY makes no bench claims (verified by read) |

### 01-33 Round Truths (the lever round's own acceptance)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The lever-selection record exists with evidence, source reading, arithmetic, ranked menu, selection + provenance | ✓ VERIFIED | 01-G01-7-LEVER.md read: sections 1-4 (EVIDENCE BASE :9, SOURCE READING :43, ARITHMETIC :59, RANKED MENU :83) + LEVER SELECTED checkpoint record :105 with the auto-advance provenance and SHIPPED DEPTH: 5 (:113, :123) |
| 2 | The lever implemented: depth constant with arithmetic; supersede path ranked through evictionClassOf; honest-reject with named line; no new protocol surface | ✓ VERIFIED | image_protocol.h:74 IMG_TX_QUEUE_DEPTH = 5 with the full arithmetic comment (290,960 B = 3.5% of 8.35 MB PSRAM; +176 B internal; TTL unchanged — citing the record's section 3 and the census); evictEntriesOlderThan (image_tx_manager.cpp:1381-1456): pass-1 mid-service deferral verbatim, pass-2 ranks victims through evictionClassOf (call sites :1413/:1443 beside the overflow scan's :571 — reused, NOT forked; the function body diff-verified unchanged), returns false when only class-5 candidates remain, the two discriminator lines verbatim (:1182, :1444); bool signature declared in include/image_tx_manager.h:291 with the return contract |
| 3 | Round-#10/01-21/01-22 lever surfaces preserved and composed with | ✓ VERIFIED | Re-announce ladder + budget re-arm (:1017, :1141), base full-arm deadline (image_rx_manager.cpp:941), two-pass overflow scan with both labels (:587, :591), quiet gate 750, D2 flag — all grep-verified at HEAD |
| 4 | Builds green + harness exit 0 + PRI-01 orderings preserved | ✓ VERIFIED | VERIFIER-RE-RUN: 2/2 SUCCESS, harness exit 0. PRI-01 verified STRUCTURALLY at source: beacon due -> sendTelemetryBeacon() + return (:279-282) precedes pushPending (:286); the window-service preemption check (:678) precedes findActiveEntry (:689); the lever touches queue admission/eviction only. NOTE (info): "PRI-01 node order-gates" is structural code reading — no automated order-gate script exists in scripts/; the property holds and the wire harness (incl. beacon round-trip checks) exits 0 |
| 5 | Ledger honesty: WINDOWS 3 + G-01-7 carry the lever record, stay open, no bench claims | ✓ VERIFIED | WINDOWS entry 3 ROUND-#14 LEVER SHIPPED in BOTH copies + the census completeness amendment (seq=29 corrected to GET_STATUS at base4.log:681, restart evidence :675 — the Rule-1 citation-fact deviation honestly executed); 01-UAT G-01-7 lever-shipped marker; entry stays open; the 01-33 SUMMARY makes no bench claims |

### 01-34 Round Truths (the bench session's acceptance)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The [STACK] instrument ANSWERS — the trajectory quoted and dispositioned against §11's predicted signatures | ✓ VERIFIED (the ANSWER delivered; D1 correctly NOT flipped) | 240-word boot baseline (:138) is the ONLY watermark line whole-console (grep count 1); zero new-lows through all multi-window service; REFUTES stack capacity per §11.3's pre-written rule; the caveats recorded verbatim in §11.6 (no crash instant; AUX-missed x40 vs x81, largest SERVED image 41 chunks vs 119 — the refutation is as strong as the exercised load) |
| 2 | The [TWDT] two-reading question RESOLVED by the live line + registered=1 | ✓ VERIFIED | [TWDT] idle0 wdt status=ESP_OK (balloon5.log:105) — exactly §11.2's config prediction; registered=1 (:33) proves the printf fix; recorded in §11.6 + both ledgers. §7's premise STANDS; the sessions-8/9 attribution survives; session-10's NOT_FOUND confirmed as the wrong-handle bug |
| 3 | The [I2C] coverage answer lands | ✓ VERIFIED (by absence) | 0 [I2C] lines + 0 i2cWrite whole-console (verifier-grepped) — no episode occurred; §11.3.1's branch-b swallowing disposition stands as the coverage answer, exactly as the plan's either-way branch specifies |
| 4 | D1 acceptance: the three-expression survival ladder + zero crash signatures | ✗ FAILED (honest bars) — the campaign's first ZERO-CRASH session, and correctly NOT a flip | Crash census ALL ZERO (rst:0x7 / rst:0xc / Guru / canary / i2cWrite / corrected-raw-byte mojibake — each verifier-grepped 0); single boot ([BOOT] POWERON :104, sole reset-cause line; B2 zero; [MEM] x40, heap floor 8310804 :807). Class 1 passed a FOURTH session (image 44 first-post-boot push both kinds COMPLETE base5.log:99/:275). BUT class 2's >=7-window SVGA bar NOT met (image 45's SVGA full starved 0/165 — the over-rejection evidence), class 3's >=3-min dwell NOT met (~50 s, :641->:767), series A not run. The plan's own bar ("flips ONLY on the full ladder + a passing series A") was not met and the ledgers did not flip — the honesty contract held |
| 5 | G-01-7 series-A acceptance on the admission lever | ✗ FAILED — ENGAGED-AND-FAILING via the NEW over-rejection mode, every step a quoted line | Series A as designed NOT RUN (2 captures ~50 s apart); the lever's honest-reject line fired 8x (balloon5.log:883-:1056) ↔ NACK_INVALID seq 16/17/18/20/21/23/24/25 (base5.log:403-:516), image 45 SVGA FULL 0/165 through two D-24 lifecycles (:456-:457, :522-:523) at 2/5 occupancy; mechanism verifier-confirmed at source (evictEntriesOlderThan has no capacity-need check; the sole older candidate was base-side-COMPLETE/balloon-side-class-5 image 44); thumbs immune (image 45 thumb 6/6 :382); zero avoid-form/overflow/evict lines; zero command timeouts. The residual is re-named and the fix class routed — the three-way engagement separation worked exactly as designed |
| 6 | WR-03 cadence discriminator (tenth round riding) | ✗ NOT RUN | 0 AUTO_CAPTURE commands both consoles (verifier-grepped); recorded TENTH-round in the Test 4 note; SC-5 advisory NOT discharged |
| 7 | SC-3 visible-effect pairs (tenth round riding) | ✗ NOT RUN | 0 non-RESOLUTION settings commands (verifier-grepped); recorded TENTH-round in the Test 3 note |
| 8 | QVGA restore clause (wire 6) | ✗ NOT RUN | The camera was left at SVGA; no wire-6 restore issued (grep: exactly 1 SET_RESOLUTION all session); recorded TENTH-round |
| 9 | Ledger honesty: flips ONLY on operator-observed evidence; failed session recorded per convention | ✓ VERIFIED | ZERO flips (WINDOWS stays 2/17/19, verifier-parsed both copies); 01-UAT G-01-10 SESSION-11 EXTENSION + G-01-7 SESSION-11 UPDATE read in full — every load-bearing line reproduces (the 8 rejections, the 8 NACKs, both 0/165 lifecycles, the 240-word reading, the lull span); requirements-completed: [] honored; NO §12 (nothing crashed — §11.6 is the session's record); STATE.md carries the round outcome + the full round-#15 routing + requirements-completed: []; Test 3/Test 4 TENTH-round notes verified; the log-naming collision honestly recorded (the session-#5-of-2026-08-25 files WERE overwritten by same-named files — recorded per the 01-24 convention, not silent) |
| 10-17 | UI-SPEC lifts (7 explicit + LED-truth backstop) | ⚠️ UNVERIFIED-BY-LOG | No dashboard observation session occurred; nothing observed either way — no pass claimed, all ride the round-#15 bench glance |

**Round-#14 score:** 01-32 7/7, 01-33 5/5, 01-34: the instrument/ledger truths verified, the two acceptance truths honestly failed, the riding clauses honestly not-run.

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Full-resolution gallery image viewer | Phase 3 backlog | Todo on disk (verifier-verified this round) |
| 2 | Gallery thumb number overlay + Incomplete-badge copy | Phase 3 backlog | Todos on disk (verifier-verified) |
| 3 | Thumb-first image delivery + antenna-pointing overlay | Phase 3 backlog | Todos on disk (verifier-verified) |
| 4 | Balloon SD-card picture cache (STORE-01..04) | Phase 2.5 (new, operator-directed, db9625e) | ROADMAP Phase 2.5 + todo on disk; cites session-#11 evidence as motivation — a new capability phase, NOT a deferral target for the open gaps |

Step 9b check: Phase 2.5's insertion does NOT cover D1 or G-01-7 — those remain Phase-1 closeout activities routed in STATE.md Next Steps. Phases 2/3 are executed; Phase 1 closes last. The three gaps remain gaps.

### Required Artifacts (round #14: plans 01-32, 01-33, 01-34)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/main_balloon.cpp` (01-32) | [STACK] instrument, printf fix, A/B guard | ✓ VERIFIED | All constructs read at HEAD at the cited lines; hook counter/frozen-latch byte-identical (commit diff: +68/-2); bench-proven readings |
| `src/image_tx_manager.cpp` (01-32/01-33) | [TWDT] ForCPU(0) fix; ranked supersede path + discriminator lines | ✓ VERIFIED | :172 fix; evictEntriesOlderThan rewrite read in full; evictionClassOf body unforked (diff-verified); both lines verbatim |
| `include/image_protocol.h` / `include/image_tx_manager.h` (01-33) | depth 5 + arithmetic comment; bool declaration | ✓ VERIFIED | :74 with the comment citing the record's arithmetic; :291 bool with return contract |
| `.planning/debug/d1-crash-regression-push-start.md` §11 + §11.6 | implementation record, config re-derivation, discriminator table, A/B recipe, provenance; session-11 reading | ✓ VERIFIED | §11.1-11.5 verified at lines 1467-1635; §11.6 (session-11 record) read — provenance-first, instrument answers, census, ladder honesty, round-#15 routing; NO §12 (nothing crashed) |
| `01-G01-7-LEVER.md` (01-33, new) | four-section audit + checkpoint record + SHIPPED DEPTH | ✓ VERIFIED | Read in structure; census quotes carry balloon4.log line numbers |
| `balloon5.log` / `base5.log` (01-34, retained session evidence) | every citation locatable | ✓ VERIFIED | 1068 / 524 lines. Verifier reproduced every load-bearing citation: instruments (:33/:104/:105/:138), [MEM] floor (:807), lull (:641/:767), 8 rejections (:883-:1056), image 44 window arms (:397/:516/:526/:598/:661), captures (:262/:793), SVGA re-init (:781-:785); base: image 44 COMPLETEs (:99/:275), image 45 thumb (:382), 2x 0/165 INCOMPLETE (:456-:457/:522-:523), 8 NACKs (:403-:516), batt=valid x52. Census greps all reproduce |
| `01-UAT.md` / `WINDOWS.md` / `STATE.md` (01-34) | honest records, zero flips, round-#15 routed | ✓ VERIFIED | Read: SESSION-11 extensions in both gap blocks; WINDOWS entries 3/15 in table AND JSON (JSON verifier-parsed: 19 entries, open {3,15}); counts 2/17/19 reconcile; STATE.md Next Steps item 1 IS the round-#15 brief |
| `.pio/build/*/firmware.elf` (provenance) | the images session 11 ran | ✓ VERIFIED | On-disk SHAs match §11.6/01-UAT claims exactly (32d31f27.../ce65709...); the verifier's up-to-date rebuild at HEAD reproduces both byte-for-byte; A/B-disabled build also verifier-run (restored afterward) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| The deployed session-11 images <-> HEAD source | build provenance | on-disk ELF SHAs + up-to-date rebuild | ✓ WIRED (verifier-reproduced) | Both SHAs reproduce at HEAD; build banner Aug 28 2026 23:13:40 in-console; NOTE (info): a forced recompile after flag perturbation yields a different SHA with identical source — the firmware embeds build-time metadata, so "deterministic rebuild" is proven here in the incremental-up-to-date sense (build dir current at HEAD), which still ties the deployed image to HEAD source |
| [STACK] instrument <-> §11.3's predicted signatures | pre-registered prediction | §11.3 table written BEFORE the session | ✓ WIRED (the round's designed pattern) | The healthy-constant-margin REFUTE signature is what the session read; the prediction was written first (forward-predictive discipline) |
| [TWDT] ForCPU(0) fix <-> §11.2's config prediction | prediction -> live line | sdkconfig:2179 CHECK_IDLE_TASK_CPU0=y | ✓ WIRED (prediction CONFIRMED live) | ESP_OK at :105 = reading (a) exactly as derived |
| The 01-33 lever <-> the 8 rejections | discriminator line | "queue holds only receipt-evidenced entries (G-01-7)" | ✓ WIRED (bench-proven) | Source line :1182 fired verbatim 8x; the never-engaged vs engaged-and-failing separation worked |
| evictEntriesOlderThan <-> the over-rejection mechanism | capacity-need check absence | source read :1381-1456 | ✓ WIRED (verifier-read) | `victimClass >= 5 && !evictedLower -> return false` with no capacity-need guard — the exact mechanism the ledger names |
| Retained surfaces (Lever A, B1/B2, [MEM], round-#10 lines, D2 flag, round-#13 instruments) | round #14 | commit-scope + grep evidence | ✓ WIRED | 7d3abbc touched exactly 2 source files (instruments only); 61e19b7 touched 4 code files (queue policy only); all protected greps pass at HEAD |
| The round-#14 work <-> 01-34's acceptance | the session reads the named lines | §11.3's discriminator table | ✓ WIRED | The session quoted exactly the tabulated lines — [STACK]/[TWDT]/registered/B1/B2/[MEM] all read |

### Data-Flow Trace (Level 4)

Not applicable this round in the web-UI sense — 01-32/01-33 shipped firmware instruments and scheduling policy whose data sources are real framework calls (uxTaskGetStackHighWaterMark, esp_task_wdt_status, the queue-entry scan), and 01-34 shipped no code. Their live outputs are in balloon5.log (:33/:105/:138/:807) and reproduced above.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Dual-target build (independent) | `pio run -e esp32-s3-balloon -e esp32-s3-basestation` | 2 succeeded in 56.7 s (verifier-run at HEAD) | ✓ PASS |
| Wire-format regression harness | `node scripts/verify_protocol_roundtrip.mjs` | exit 0, all checks PASS — 52 PASS lines (NOTE: the 01-33 summary says "54/54"; actual count 52 — cosmetic overstatement, exit code and pass status unaffected) | ✓ PASS (with note) |
| Deployed-ELF provenance | `sha256sum .pio/build/*/firmware.elf` after the verifier's own build | balloon 32d31f270cb8... / base ce657093cb0a... reproduce exactly (up-to-date rebuild at HEAD); A/B-disabled build also SUCCESS; on-disk ELFs restored to deployed SHAs after the A/B experiment | ✓ PASS |
| Session-11 instrument citations | sed balloon5.log :33/:104/:105/:138/:807/:641/:767 | registered=1; [BOOT] POWERON; [TWDT] ESP_OK; 240-word baseline (sole watermark line); [MEM] heap floor 8310804; lull [BCN] seq=27->37 — all verbatim | ✓ PASS |
| Session-11 G-01-7 citations | grep balloon5.log :883-:1056; base5.log :403-:516/:456-:457/:522-:523/:99/:275/:382 | 8 rejections verbatim; 8 NACK_INVALIDs at the cited seqs; 2x "0/165 chunks after 3 passes" + "0 B persisted, complete=false"; image 44 both kinds COMPLETE; thumb 6/6 — all verbatim | ✓ PASS |
| Crash-signature census | grep -c per signature, whole balloon5.log | rst:0x7=0, rst:0xc=0, Guru=0, canary=0, i2cWrite=0, corrected raw-byte mojibake=0, "timeout after 3 retries"=0 both logs, FB-OVF=0 both logs — matches §11.6's census exactly | ✓ PASS |
| Riding-clause NOT-RUN confirmation | grep SET_*/AUTO_CAPTURE, both logs | 0 non-resolution settings commands; exactly 1 SET_RESOLUTION (seq=12 wire 10 -> "Set resolution to 10"); 0 AUTO_CAPTURE | ✓ PASS |
| Regression watches | grep Critical battery / health check failed / batt= | 0 / 0 / batt=valid x52, invalid x0 — WR-01/WR-02 GREEN | ✓ PASS |
| Source drift + commit scopes | `git status --short -- src/ include/ scripts/`; `git show --stat` on all round commits | drift empty; 7d3abbc = 2 source files (+68/+15, -3); 61e19b7 = 4 code files + 2 ledgers + lever record; remaining commits docs-only (incl. the post-round db9625e Phase-2.5 insertion and 446959e review report); camera_manager/image_rx/UI untouched | ✓ PASS |
| WINDOWS integrity | JSON parse + counts | 19 entries, open ids exactly {3, 15}, 17 fixed; 2/17/19 reconcile across front matter / table / JSON; SESSION-11 x4 (both entries, both copies) | ✓ PASS |
| PRI-01 orderings | source read image_tx_manager.cpp | beacon early-return :279-282 precedes pushPending :286; preemption :678 precedes findActiveEntry :689 (structural; no automated order-gate script exists — wording note) | ✓ PASS |
| Protected-surface greps | quiet gate / Lever A / [MEM] / round-#10 lines / D2 flag / TTL / REMOVAL CONDITION | 750 intact; uart_ll_is_tx_idle x3; [MEM] x8; discriminator lines present; inboundWindowRequestSeen present; IMG_ENTRY_TTL_MS 900000 unchanged; REMOVAL CONDITION 11 (7+4) | ✓ PASS |
| Review-finding verification | source read main_balloon.cpp:155-166/:427-432; image_tx_manager.cpp:1137-1160 | 01-REVIEW WR-01 (zero sentinel: 0 latches at :160, print guard :427 can never emit it) CONFIRMED; WR-02 (stamp :1137 + budget re-arm precede range validation :1149) CONFIRMED | ✓ PASS (findings confirmed real) |
| Debt markers | grep TBD/FIXME/XXX/HACK/PLACEHOLDER in the four touched code files | 0 matches | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes exist in this project; the phase's executable verification is the wire-format harness + dual-target build + the A/B build variant (all run above) — SKIPPED (no probes declared).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| CTRL-01 | 01-32/01-33/01-34 (+prior) | User can trigger camera capture from base station web interface | ✓ SATISFIED (regression note) | Session 11: dashboard trigger -> ACK -> capture on round-#14 firmware; image 44 both kinds COMPLETE (first-post-boot push survived a FOURTH session); the D1 blocker now has two disconfirmed mechanisms and a routed discriminator — still the phase's top open risk until round #15's session |
| CTRL-02 | 01-34 (+prior) | User can adjust all camera settings remotely | ✓ SATISFIED (riding note) | All 7 settings execute for real (01-04 sensor setters; ACKs on the wire); the resolution family now has THREE current-firmware proofs (sessions 4/5, CIF at session 10, SVGA at session 11 — 32850 B/165 chunks, re-init, zero FB-OVF); the visible-effect pair judgment rides (SC-3 clause, tenth round) |
| CTRL-03 | 01-33/01-34 (+prior) | Manual + automatic capture modes | ✓ SATISFIED (riding note) | Manual bench-proven current-generation (image 44 end-to-end, image 45 thumb); auto-capture Test 4 PASS stands (WR-03 discriminator rides, tenth round) |
| CTRL-04 | 01-34 (+prior) | Fixed interval timing | ✓ SATISFIED | AutoCapture wraparound-safe timer; Test 4 cadence PASS; NVS ID sequence continuous (44->45) |
| CTRL-06 | 01-32/01-33/01-34 (+prior) | Failed commands retried with timeout | ✓ SATISFIED | Session 11: zero command timeouts a 4th consecutive session; the transfer layer ran TWO complete bounded D-24 lifecycles with honest NACK_INVALID outcomes; quiet-gate and retry constants intact through round #14 (verifier-read) |
| PRI-02 | 01-32/01-33/01-34 (+prior) | Retry mechanism with timeout for failed transmissions | ✓ SATISFIED | Same evidence as CTRL-06 |

Riding IDs per the project's gap-closure convention (marked Complete in REQUIREMENTS.md from their owning phases; no new claims this round — all three plans' records honored requirements-completed: [] on the failed-session convention): IMG-03 (01-32), IMG-02/IMG-03 (01-33/01-34). No orphaned requirements: REQUIREMENTS.md maps exactly CTRL-01..04, CTRL-06, PRI-02 to Phase 1 (verifier re-grepped: 6 rows, all Complete); the post-round db9625e added ONLY Phase 2.5 STORE rows — Phase 1's mapping untouched.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (all round-#14 touched code files) | - | TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER scan | — | CLEAN — zero matches (verifier-run) |
| src/main_balloon.cpp | 155-166, 427-432 | 01-REVIEW WR-01: [STACK] zero sentinel — a genuine 0-word watermark is latched but can never print (print guard excludes 0) | ⚠️ Warning (review-confirmed at source) | Blinds the CONFIRM arm of round-#15's heavy-tail re-run — the exact signature the instrument hunts; fix before that session (routed in the round routing) |
| src/image_tx_manager.cpp | 1137 vs 1149 | 01-REVIEW WR-02: receipt stamp + budget re-arm precede per-kind range validation — malformed requests promote entries to protected class 5 and re-arm budgets | ⚠️ Warning (review-confirmed at source) | Companion defect for the round-#15 capacity-aware admission lever; contradicts the site's own T-01-21-01 comment |
| scripts/embed_web_assets.mjs | 105 | 01-REVIEW WR-03: supply-chain hash lock fails open on unknown vendor entries | ⚠️ Warning (review) | Hardening; both current files are recorded, today's output unaffected |
| src/image_tx_manager.cpp | 1381-1456 | The over-rejection mode itself: honest-reject fires with no capacity-need check and no safe-reclaim class for fully-delivered entries | ⚠️ Warning (the round's named residual) | This IS gap 2 — the starved 0/165 at 2/5 occupancy; routed as the round-#15 companion lever, not hidden |
| 01-33-SUMMARY.md | coverage D2 | "harness exit 0 (54/54 PASS)" — actual count 52 | ℹ️ Info | Cosmetic overstatement; exit 0 and all-pass verified |
| 01-33-SUMMARY.md | coverage D2 | "PRI-01 node order-gates green" — no automated order-gate script exists; the claim rests on structural code reading | ℹ️ Info | The property holds (verifier-read the orderings); the vocabulary overstates the automation |
| 01-32-SUMMARY.md / 01-33-SUMMARY.md | key-decisions | Both label their auto-advanced selections "the sixth pending operator confirmation" | ℹ️ Info | Bookkeeping double-count: STATE.md's official list (01-23/25/26/28/30/32) counts six WITHOUT 01-33's option (a), which is recorded in 01-G01-7-LEVER.md — count it at the next ledger touch |
| Round-#13 report's provenance wording | - | "deterministic-build proof" — this round's experiment shows a forced recompile embeds new build timestamps (source-identical, different SHA) | ℹ️ Info | Provenance here is the up-to-date-rebuild + on-disk-SHA match, which still ties deployed images to HEAD; future reports should use that phrasing |

### Decision Coverage

CONTEXT.md decisions for this phase were translated in earlier rounds and re-checked at prior verifications; round #14 introduced no new CONTEXT decisions (01-33's Task 2 checkpoint selection is recorded in 01-G01-7-LEVER.md's LEVER SELECTED section with auto-advance provenance; 01-32's in §11.5 — the campaign's sixth/seventh pending confirmations per the note above). COVERAGE.md re-affirmed for round #14 (no external API surface; 11 unresolved spec-less probe rows authored in full in 01-34's must_haves.flagged_assumptions — verifier-counted 11, none silently dropped; they remain human-judgment items per the spec-less fallback contract). The post-round db9625e Phase-2.5 insertion is operator-directed scope addition, not decision drift. No drift found.

### Human Verification Required

These items need the physical bench / operator judgment; all are routed (STATE.md Next Steps: the round-#15 shared bench session, then the security gate), not dropped:

### 1. SC-3 visible-effect pairs (tenth round riding)

**Test:** At fixed QVGA: SET_BRIGHTNESS -2, capture; SET_BRIGHTNESS +2, capture; judge the pair visibly different. Repeat for one other class (contrast -2/+2, saturation -2/+2, or quality 10 vs 30).
**Expected:** Visible pairwise differences, per-pair verdicts recorded in Test 3.
**Why human:** Sensor visible-effect acceptance needs the physical camera and operator judgment; ten bench rounds have issued zero non-RESOLUTION settings commands.

### 2. WR-03 cadence discriminator

**Test:** AUTO_CAPTURE_ENABLE at 20 s; let one interval capture fire; trigger a manual CAPTURE_NOW; compare the next interval capture's timestamp to the manual one.
**Expected:** Next interval capture lands ~20 s AFTER the manual capture (both 'Captured image ID' timestamps quoted). Also discharges the SC-5 coincidental-reliance advisory.
**Why human:** Needs the running firmware pair and wall-clock observation; never run on current HEAD.

### 3. D1 round-#15 bench: the A/B discriminator + the heavy-tail ladder re-run

**Test:** Build the §11.4 A/B hook-unregistered image (verifier-proven buildable) and the sentinel-fixed default; flash both arms across sessions; re-run the heavy-tail bars — a >=7-window SVGA sustained full service and a >=3-minute TX-light lull dwell — at session-10-class TX load, with [STACK]/[TWDT]/[I2C]/[MEM] readings quoted and the A/B arm identified by its console line.
**Expected:** Zero crash signatures through both bars; the A/B arm's survival/loss pattern discriminates hook-dispatch vs kernel-side wedge; the [STACK] trajectory (with the sentinel fixed) reports any genuine collapse.
**Why human:** RF-pair behavior under real half-duplex load; hardware acceptance only.

### 4. G-01-7 series-A acceptance on the capacity-aware lever (with the WR-02 fix)

**Test:** On post-round-#15 firmware: 3 back-to-back unspaced CAPTURE_NOWs.
**Expected:** 3/3 ACKs (no forbidden terminals), 6/6 verdicts COMPLETE, zero command timeouts, zero silently absent fulls; the capacity-aware lever's discriminator lines proving engagement (or natural-cadence absence explained); the over-rejection mode gone (a full deliverable behind a fully-served older entry no longer starves).
**Why human:** Burst behavior under real RF load; the acceptance bars are operator-observed verdicts.

### 5. Dashboard LOOK glance + UI-SPEC lifts + QVGA-restore clause

**Test:** Exercise the dashboard (d8ba14e gallery detail still unlooked-at after seven sessions); QVGA restore (SET_RESOLUTION wire 6) with the next capture QVGA-class by wire evidence, zero FB-OVF.
**Expected:** All seven UI-SPEC considerations + LED-truth backstop hold; gallery detail fields render; the restore clause judged as specified.
**Why human:** Browser observations leave no console evidence; operator-looked (not did-not-flag) is the bar.

### 6. SIX (by STATE.md's count — see the info note) pending auto-advanced disposition confirmations

**Test:** Confirm (or route back) the auto-selected dispositions now pending end-of-phase operator confirmation: 01-23's fix-all (WINDOWS 10-14), 01-25's fix-shape option-a + instrumentation ride-along, 01-26's option-a fix-all (WINDOWS 17-19), 01-28's option (a)+(b) (Lever A + B1/B2), 01-30's option (b) instrument-only, and 01-32's §10.5 package (plus 01-33's option (a) recorded in 01-G01-7-LEVER.md). FOUR of the levers/instrument-sets are now bench-informed (01-25's warm-up disconfirmed; 01-28's Lever A disconfirmed as the mechanism; 01-30's instruments answered and were fixed forward; 01-33's lever engaged-and-failed with the fix routed) — the confirmation questions include whether each STAYS (all are hazard removals/diagnostics whose retention is harmless or load-bearing) while the next fix supersedes.
**Expected:** Each confirmation (or route-back) recorded in the ledger.
**Why human:** Acceptance of robustness-edge-case risk and fix-shape selection is a maintainer judgment; all were auto-advanced under workflow.auto_advance and honestly recorded as pending.

### 7. The 11 spec-less probe rows (flagged assumptions)

**Test:** The 11 unresolved spec-less probe rows authored in 01-34's must_haves.flagged_assumptions (CTRL-01..06, PRI-02 across unclassified/adjacency/empty/ordering/idempotency/concurrency) remain explicit human-judgment items.
**Expected:** Each either answered at the requirement tier or consciously waived at phase close.
**Why human:** No SPEC answer exists; the no-silent-drop contract keeps them surfaced, never auto-resolved.

### Gaps Summary

Three structured gaps, all honestly ledgered — the same three as verification #12, materially evolved in the evidence base while unchanged in count:

1. **D1 crash regression, STILL OPEN (G-01-10 / WINDOWS 15, BLOCKER) — but the campaign's first zero-crash session is on record and two of three candidate mechanisms are DISCONFIRMED by pre-registered instruments.** The round worked exactly as its honesty guard demanded: [TWDT]=ESP_OK settled the two-reading question per the written prediction; [STACK] refuted stack capacity per the pre-written rule with its caveats recorded; [I2C] closed by absence. The honest bars hold: classes 2/3 not met, series A not run, no fix verdict exists — zero-crash on the campaign's lightest TX session cannot clear a load-dependent family. The wedge family remains; the A/B hook-unregistered build is the round-#15 discriminator (verifier-proven buildable this round); the fresh review's WR-01 sentinel defect must be fixed first so the CONFIRM arm can report.
2. **G-01-7 burst full-delivery — the lever ENGAGED-and-FAILED via the NEW over-rejection mode.** The campaign's first lever landed against a bench-named residual did its designed job: the discriminator lines separated engaged-and-failing from never-engaged, and the failure is fully quoted at both consoles and confirmed at source (no capacity-need check; fully-delivered entries have no safe-reclaim class). The capacity-aware admission fix + the review's WR-02 stamp-order companion are routed. Series A as a 3-capture protocol still has not run clean; zero command timeouts now hold four consecutive sessions.
3. **The riding bench-moment clauses — TENTH round.** Nothing reached, everything honestly recorded; the resolution clause gained a third current-firmware proof (SVGA clean). All routed to the round-#15 shared session.

SUMMARY vs Reality: no material divergences found this round — every load-bearing claim in 01-32/01-33/01-34-SUMMARY that this verifier checked reproduces (all instrument constructs at their cited lines with the claimed removal-condition arithmetic 8->11; the lever record's four sections + SHIPPED DEPTH: 5; evictionClassOf unforked; the ledgers' ROUND-#14/SESSION-11 records in both copies with counts reconciling; the zero-flip convention; requirements-completed: []; every session-11 log citation including all 8 rejections, all 8 NACKs, both 0/165 lifecycles, the 240-word reading, the census zeros; the ELF SHAs; the A/B build claim). Three cosmetic discrepancies noted (harness count 52 vs 54; "node order-gates" vocabulary; the sixth-confirmation double-count), plus the provenance-vocabulary note. One NEW material fact the summaries could not carry (they predate it): the fresh 01-REVIEW's two warnings land inside round-#14's own code and are verifier-confirmed real — they route to round #15, not back.

The phase goal itself — bidirectional LoRa command/control with ACK, retry, manual + auto capture — remains achieved and hardware-proven, and this round STRENGTHENED it again: the command loop ran the campaign's first zero-crash session (the forbidden terminal absent a fourth time, honest NACKs, bounded D-24 lifecycles, the SVGA resolution command executing clean), and the crash hunt eliminated two of its three labeled mechanisms with pre-registered predictions confirmed in writing before the data. But HEAD firmware has still not demonstrated the heavy-tail survival bars, the burst-delivery acceptance has now failed through two distinct named modes (queue-cycling, then over-rejection) with the fix for the second routed but not built, and the riding clauses accumulate a tenth round. The honest verdict is gaps_found with a clear, evidence-ranked next action: round #15 — the A/B discriminator build + the [STACK] sentinel fix + the capacity-aware admission lever (+ the WR-02 companion) + one shared bench session that runs the heavy-tail ladder, series A, and every riding clause at once; then /gsd-secure-phase 1 as the remaining phase-close gate.

---

_Verified: 2026-08-28T14:40:55Z_
_Verifier: Claude (gsd-verifier)_
