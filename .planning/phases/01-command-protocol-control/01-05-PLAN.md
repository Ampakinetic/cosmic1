---
phase: 01-command-protocol-control
plan: "05"
type: execute
wave: 3
depends_on: ["01-04"]
gap_closure: true
files_modified:
  - src/main_balloon.cpp
  - src/camera_manager.h
  - src/camera_manager.cpp
  - src/auto_capture.cpp
autonomous: true
requirements: [CTRL-01, CTRL-03, CTRL-04]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Both manual trigger and interval-based auto-capture work end-to-end (SC-5, full roadmap clause): AutoCapture is the balloon's ONLY automatic capture trigger — enabling with a commanded interval captures at exactly that cadence, and disabling stops it: after an ACKed AUTO_CAPTURE_DISABLE no automatic capture occurs on the balloon until the next commanded trigger, at any interval including values above 30 seconds"
    - "AutoCapture is the single image-ID authority: every image ID (manual CAPTURE_NOW and interval captures) comes from AutoCapture::allocateImageId(); no second image-ID counter exists anywhere in the balloon firmware, so IDs cannot collide from 1 (CTRL-01 manual-capture path protected)"
    - "GET_STATUS lastImageId is truthful at system level: the divergence source — legacy-timer captures invisible to the AutoCapture module — is removed, so every capture that happens is a capture the module knows about"
    - "An AUTO_CAPTURE_DISABLE ACK corresponds to the actual system state: the operator is never told auto-capture is off while the balloon keeps auto-capturing within 30 seconds (SC-5 disable semantics; 01-04 prohibition no longer violated in effect)"
  artifacts:
    - path: "src/main_balloon.cpp"
      provides: "Legacy 30-second capture path fully removed (function, forward declaration, loop call) and the vestigial command placeholder removed; the only periodic capture call in the loop is AutoCap().process()"
      contains: "AutoCap().process"
    - path: "src/camera_manager.h"
      provides: "Legacy timing-helper declaration removed (dead after Task 1 — its only caller was the legacy block)"
      contains: "captureImage"
    - path: "src/camera_manager.cpp"
      provides: "Legacy timing-helper definition removed; capture/setter/getter methods untouched"
    - path: "src/auto_capture.cpp"
      provides: "process() comment reworded to describe the wraparound-safe idiom without citing the removed CameraManager method; behavior unchanged"
      contains: "allocateImageId"
  key_links:
    - from: "src/main_balloon.cpp processPacketHandling"
      to: "AutoCapture::process"
      via: "the only periodic capture call in the balloon main loop; fires camera->captureImage() at the commanded interval while enabled and never otherwise"
      pattern: "AutoCap\\(\\)\\.process"
    - from: "src/command_handler.cpp handleCaptureNow + handleAutoCaptureEnable"
      to: "AutoCapture::allocateImageId"
      via: "single shared uint16 image-ID sequence for manual and interval captures (unchanged by this plan; protected by removal of the colliding legacy counter)"
      pattern: "allocateImageId"
  prohibitions:
    - statement: "MUST NOT acknowledge success for actions that were not executed — after an ACKed AUTO_CAPTURE_DISABLE the balloon must not continue automatic capture, and no capture path may exist outside AutoCapture and the manual CAPTURE_NOW handler (no second timer, no second image-ID counter)"
      status: unverified
      flagged: true
      verification: judgment
---

<objective>
Close the single remaining verified gap CR-05: the balloon's legacy 30-second capture timer in processCamera() still runs beside the commanded AutoCapture module — gated only on appState.cameraActive, never on commanded state — so an ACKed AUTO_CAPTURE_DISABLE leaves the balloon capturing every 30 seconds, commanded intervals above 30 s are polluted by interleaved captures, its static nextImageId counter collides with AutoCapture::allocateImageId() from ID 1, and each legacy capture frees/replaces the camera frame buffer, destroying commanded captures awaiting future use.

Purpose: SC-5's "disabling stops it" clause (and plan 01-04's own must-have) does not hold at system level while a second, ungated capture trigger exists; CTRL-03/CTRL-04 are blocked and REQUIREMENTS.md's Complete marks on them are overstated until this closes. Fix is code-only — no hardware required.
Output: src/main_balloon.cpp with the legacy path fully removed (AutoCapture becomes the sole capture/ID authority), same-path dead code swept from CameraManager, both firmware targets green, wire-format harness green.
</objective>

<execution_context>
@C:/Users/Amp/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Amp/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/phases/01-command-protocol-control/01-REVIEW.md

The replacement authority (read its contract before deleting the legacy path):
- include/auto_capture.h — AutoCapture: the commanded interval timer and the single image-ID sequence this fix leaves as the only automatic capture trigger
</context>

<tasks>

<!-- planner-discipline-allow: processCamera -->
<!-- planner-discipline-allow: processIncomingCommands -->
<!-- planner-discipline-allow: nextImageId -->
<!-- planner-discipline-allow: createCameraPacket -->
<!-- planner-discipline-allow: isTimeToCapture -->

<task type="auto">
  <name>Task 1: Remove the legacy 30-second capture path — AutoCapture becomes the only capture/ID authority (CR-05)</name>
  <read_first>
  - src/main_balloon.cpp (forward-declaration block ~lines 124-142; loop() subsystem-call block ~lines 250-256; loop() tail ~lines 274-277; processCamera definition ~lines 660-692; initializeSubsystems ~lines 379-398 to see the AutoCap().begin wiring that stays; processPacketHandling ~lines 755-772 to see the CmdHandler/AutoCap calls that stay)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md (gaps: section — CR-05 reason and missing list)
  - .planning/phases/01-command-protocol-control/01-REVIEW.md (CR-05 finding — consequences 1-3)
  - include/auto_capture.h (the module that supersedes the legacy timer)
  </read_first>
  <files>src/main_balloon.cpp</files>
  <reversibility rating="reversible">Pure deletion of a superseded code path in git history; no architectural change — AutoCapture already delivers the intended behavior.</reversibility>
  <action>
  CR-05 closure: the balloon firmware must have exactly ONE automatic capture trigger (AutoCapture) and ONE image-ID sequence (AutoCapture::allocateImageId). In src/main_balloon.cpp delete three parts of the legacy pre-Phase-1 timer:

  1. The processCamera() function definition (~lines 660-692, between processSensors and processCommunications) — the whole function. Its entire body is the legacy path: the appState.cameraActive early-return gate, the Camera().isTimeToCapture(30000) check, the Camera().captureImage() call, the CameraData conversion with its own static uint16_t nextImageId counter starting at 1, and the PacketMgr().createCameraPacket() call whose packets go to the stubbed LoRaComm path and are never transmitted. CameraData and createCameraPacket have no other use in this file (verified: lines 674-687 are the only occurrences), so the block is self-contained.
  2. The processCamera() forward declaration in the Main Loop Functions declaration block (~line 127).
  3. The processCamera() call in loop() (~line 253).

  Same-legacy-path placeholder cleanup: delete the empty processIncomingCommands() function (~lines 886-889 — its body is only a "would process" comment), its forward declaration (~line 142), its loop() call (~line 276), and the now-stale "// Process incoming commands" comment line directly above that call. Command flow uses CmdHandler().process() inside processPacketHandling — the placeholder predates Phase 1 and executes nothing.

  Why full removal rather than a debug gate: the legacy packet is never transmitted (processCommunications' send path is fully commented out), so the block's only live effects are the CR-05 harms — captures within 30 s of an ACKed disable, cadence pollution for commanded intervals above 30 s, ID collision from the second counter, and frame-buffer churn destroying commanded captures.

  Do NOT touch: processPacketHandling (CmdHandler().process() ~line 765 and AutoCap().process() ~line 768 stay exactly as they are), initializeSubsystems (AutoCap().begin(&Camera()) ~line 393 stays), processPowerManagement's appState.cameraActive use (legitimate low-power camera shutdown), processSensors/processCommunications bodies, and any other subsystem call in loop(). Comments naming the deleted functions go with the deletions.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon && ! grep -q "processCamera" src/main_balloon.cpp && ! grep -q "processIncomingCommands" src/main_balloon.cpp && ! grep -q "nextImageId" src/main_balloon.cpp && ! grep -q "createCameraPacket" src/main_balloon.cpp && grep -q "AutoCap().process" src/main_balloon.cpp && grep -q "AutoCap().begin" src/main_balloon.cpp && grep -q "CmdHandler().process" src/main_balloon.cpp</automated>
  </verify>
  <acceptance_criteria>
  - Zero occurrences of processCamera, processIncomingCommands, nextImageId, and createCameraPacket in src/main_balloon.cpp — including in comments (source assertion via the negative greps in the verify command)
  - loop()'s subsystem block calls processSensors, processCommunications, processPowerManagement, processPacketHandling — no capture step remains between sensors and communications
  - AutoCap().begin(&Camera()) in initializeSubsystems and CmdHandler().process() + AutoCap().process() in processPacketHandling are present and unchanged (positive greps in the verify command)
  - pio run -e esp32-s3-balloon reports SUCCESS
  </acceptance_criteria>
  <done>The balloon has no capture path outside AutoCapture and the commanded CAPTURE_NOW handler: after an ACKed AUTO_CAPTURE_DISABLE nothing captures, commanded intervals in 1000..3600000 ms keep exact cadence with no 30-second interleave, and exactly one image-ID sequence exists — SC-5 disable semantics restored at code level.</done>
</task>

<task type="auto">
  <name>Task 2: Same-path dead-code sweep + dual-target build and wire-format regression battery</name>
  <read_first>
  - src/camera_manager.h (~line 125 — the timing-helper declaration among the capture methods; surrounding captureImage/getter declarations to keep)
  - src/camera_manager.cpp (~lines 494-499 — the definition between the capture-duration helper and the Power Management section)
  - src/auto_capture.cpp (~lines 86-105 — process() with the comment citing the removed CameraManager method at ~line 92)
  - scripts/verify_protocol_roundtrip.mjs (read-only — understand what the regression battery asserts; no changes to it)
  </read_first>
  <files>src/camera_manager.h, src/camera_manager.cpp, src/auto_capture.cpp</files>
  <action>
  Remove the CameraManager timing helper whose only caller was the legacy timer block deleted in Task 1 — its project-wide occurrences are the declaration, the definition, and one comment citation; no live call site remains.

  1. Delete the isTimeToCapture(uint32_t intervalMs) const declaration from src/camera_manager.h (~line 125) and its definition from src/camera_manager.cpp (~lines 494-499 — the wraparound interval helper that returns true when lastCaptureTime is 0). AutoCapture::process implements its own wraparound-safe baseline; the CameraManager copy is dead code from the legacy path.
  2. Reword the comment in src/auto_capture.cpp process() (~lines 91-92) that describes the idiom as the same as the deleted CameraManager method: keep the wraparound-safe millis-difference description and the T-01-09 baseline-before-attempt note, drop the reference to the now-nonexistent method.
  3. Regression battery: build BOTH firmware targets and run the host wire-format harness to prove the removal changed nothing on the wire and nothing in either build.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation && node scripts/verify_protocol_roundtrip.mjs && ! grep -rq "isTimeToCapture" src include</automated>
  </verify>
  <acceptance_criteria>
  - Recursive grep over src and include returns zero matches for isTimeToCapture — declaration, definition, and comment citation all gone (verify command's final gate)
  - auto_capture.cpp process() retains the wraparound-safe millis idiom with baseline updated before the capture attempt; its comment describes the idiom without citing any CameraManager method
  - pio run for esp32-s3-balloon AND esp32-s3-basestation both report SUCCESS
  - node scripts/verify_protocol_roundtrip.mjs exits 0 with all clauses passing (wire format untouched by this plan)
  </acceptance_criteria>
  <done>The legacy path leaves zero residue in the codebase, both firmware targets build green, and the 10-clause wire-format regression harness passes unchanged — the CR-05 fix is regression-free outside the deleted path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LoRa RF → balloon capture scheduling | Commanded AUTO_CAPTURE_ENABLE/DISABLE must be the sole authority over automatic capture; no local path may override the commanded state |
| Operator → base station UI truth | The OFF state and ACK shown to the operator must match the balloon's actual capture behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14 | Repudiation | AUTO_CAPTURE_DISABLE ACK vs actual balloon behavior | high | mitigate | CR-05 fix: delete the ungated legacy 30 s timer so balloon capture state has a single authority (AutoCapture); an ACKed disable truthfully means no further automatic captures — enforced by negative-grep gates proving no capture call exists outside AutoCap().process and the CAPTURE_NOW handler |
| T-01-15 | Tampering | image-ID sequence (GET_STATUS lastImageId; Phase 2 image sequencing) | medium | mitigate | Remove the second static counter so AutoCapture::allocateImageId() remains the single uint16 sequence shared by manual and interval captures — IDs cannot collide from 1 and lastImageId cannot misname the actual last capture |
| T-01-16 | DoS | camera frame-buffer churn | low | mitigate | Removing the legacy path eliminates unscheduled captureImage() calls that freed/replaced the frame buffer while a commanded capture awaited use |
</threat_model>

<spec_gates_record>
Visible, recorded gate choices for this cycle:

- Spec-less edge-probe fallback: VISIBLY SKIPPED — no new requirement IDs to probe this run (all six phase IDs were probed and resolved in plans 01-02..01-04); no probe predicates generated. CR-05's edge semantics (disable-then-idle, interval fidelity above 30 s, single ID sequence) are carried directly as must_haves truths sourced from the verification gap.
- Prohibitions fallback: VISIBLY SKIPPED (same reason). The 01-04 prohibition (no ACK without a real action) is re-carried in must_haves.prohibitions because the verifier judged it violated-in-effect by CR-05 — this plan restores it.
- Assumption-delta detector: detected=false — this plan removes a path and introduces no second instance of anything; no identity-model question to answer.
- API coverage: detector fired on a false positive (the word "API" in plan 01-03 prose about the first-party web server's HTTP endpoints); reasoned no-external-API declaration written to COVERAGE.md in this phase directory.
- UI-SPEC ## UI Considerations: 7 explicit truths + 1 backstop remain carried by executed plan 01-03 (SC-1 verified); this plan touches no UI surface — no re-lift, no silent drop.
- Schema push gate: skipped — no ORM/schema files exist in this ESP32/PlatformIO firmware project.
</spec_gates_record>

<verification>
- pio run -e esp32-s3-balloon -e esp32-s3-basestation — both SUCCESS after the removals
- node scripts/verify_protocol_roundtrip.mjs — exit 0, all clauses pass (wire format untouched)
- Source gates: legacy identifiers absent from src/main_balloon.cpp; the timing helper absent project-wide; AutoCap().begin/process and CmdHandler().process wiring intact
- System-level semantics code-proven by absence: after an ACKed AUTO_CAPTURE_DISABLE no code path captures — the only automatic trigger (AutoCap().process) is disabled by the command the ACK answered
- Runtime cadence over the radio link remains hardware UAT item 4 in 01-VERIFICATION.md — this fix is what unblocks that test's disable half
</verification>

<success_criteria>
- SC-5 disable semantics hold at code level: AutoCapture is the balloon's only automatic capture trigger; nothing captures after an ACKed AUTO_CAPTURE_DISABLE
- Commanded intervals across the full 1000..3600000 ms range keep exact cadence (no 30-second interleave)
- One image-ID sequence project-wide; GET_STATUS lastImageId truthful at system level (data-flow divergence eliminated)
- CTRL-03/CTRL-04 unblocked — the existing Complete marks in REQUIREMENTS.md become accurate at the same code-level standard as the other four phase requirements (hardware UAT still outstanding, as it is for SC-2/SC-3/SC-4)
- Both firmware targets green; wire-format harness 10/10
</success_criteria>

<artifacts_produced>
Symbols and files this plan creates (for drift-verification exclusion):

- No new files. No new classes, functions, flags, or struct fields — this plan only removes.
- Removals: processCamera() (definition, forward declaration, loop call) and processIncomingCommands() (definition, declaration, call) from src/main_balloon.cpp; the static nextImageId counter and the createCameraPacket call site inside the deleted block; CameraManager::isTimeToCapture(uint32_t) const (declaration + definition); one comment reword in src/auto_capture.cpp process().
</artifacts_produced>

<output>
Create `.planning/phases/01-command-protocol-control/01-05-SUMMARY.md` when done
</output>
