---
status: diagnosed
phase: 01-command-protocol-control
source: [01-VERIFICATION.md]
started: 2026-08-18T11:14:05Z
updated: 2026-08-28T13:45:00Z
---

## Current Test
[testing complete]

## Tests

### 1. End-to-end command round trip over real radios
expected: ACK within 2 s; queue row updates to terminal state; counters advance
result: pass
note: re-test after debug fix 1223f46 (balloon boot abort — three stacked causes); original failure logged in Gaps as G-01-1

### 2. Retry/TIMEOUT on degraded link including the duplicate-ACK edge
expected: With the link degraded (range/antenna removed), a retried command shows
retry progress and reaches TIMEOUT/FAILED per the locked vocabulary; the pending
count returns to 0 and stays there — including after a late duplicate ACK arrives
for a command already ACKED by an earlier retry (the 01-06 terminal-state guard is
what makes this safe; prior verifier noted this test is only meaningful now)
result: pass

### 3. Camera settings on the physical sensor
expected: Adjusting camera settings from the web UI produces visible changes in
captured images (brightness/contrast/saturation/exposure/WB/quality/frame size);
the "CIF 400x296" option succeeds (post-01-06 relabel — previously always NACK'd)
result: issue
reported: "Excellent, the data is working, and the image capture is working. However the thumbnail image looks corrupted, and there is a 'Incomplete' message over it. The full image looks good." (01-09 bench session, iteration 5)
severity: major
note: G-01-3's blocking halves (Storage Unavailable; ALL transfers timing out) RESOLVED on hardware at the 01-09 bench — Storage tile OK, command round-trip ACK, one capture completed end-to-end with the full image CRC-verified and rendered intact from SD. Residual narrowed to the thumbnail push burst (new gap G-01-5); the settings-visibility + CIF spot-check clauses ride the G-01-5 re-test round (images only now flow). UPDATE (01-10 round): the discriminator round landed BOTH kinds COMPLETE on v2/v3 fresh captures (thumbnails intact both times — see G-01-5 discriminator_evidence), but the standing corrupt-thumbnail symptom is attributed to the phantom-failure + overwrite mechanisms, so Test 3 stays 'issue' until 01-11's remediation is bench-verified. WR-01 regression watch across all 01-10 sessions: GREEN (0 'Critical battery' lines, no Emergency entry, no camera auto-disable; beacons batt=valid — quotes in G-01-5 discriminator_evidence). Settings visible-effect + CIF 400x296 spot-checks DEFERRED to 01-11 Task 3's bench re-verification (operator-approved deviation — that task re-runs the series on remediated firmware). UPDATE (01-11 remediation + bench re-verification, 2026-08-24): G-01-5 and G-01-6 RESOLVED on the bench — phantom-failure accounting fixed (0 chunk FAILED across all three post-fix sessions vs ~41/capture baseline; 31/0/90 benign AUX-missed lines), gallery grows across reboots (index 2->3->4->5, IDs 2/3/4/5 sequential; quotes in the gap entries). The settings series then surfaced a NEW defect: SET_RESOLUTION value 9 (VGA) floods 'cam_hal: FB-OVF' (4,379 lines) and fails all subsequent captures until reboot (G-01-8), and the unspaced-capture sessions exposed concurrent-transfer starvation (G-01-7). Test 3 stays 'issue' pending G-01-7/G-01-8 closure; with capture spacing, all four kinds COMPLETE (session 3). UPDATE (01-12 remediation + bench session #4, 2026-08-24 ~13:16, balloon4.log/base4.log): G-01-8 RESOLVED — three SET_RESOLUTION growth commands (CIF wire-8, VGA wire-9, SVGA wire-10) each executed via the new re-init path with ZERO 'cam_hal: FB-OVF' lines at every settings step (the session's only 4 FB-OVF lines are series-A thumbnail-path events at balloon4.log:353-354/:396-397; VGA baseline was 4,379) and every capture succeeded (IDs 6-11 all 'CAPTURE_NOW - SUCCESS', camera never died) — quotes in the G-01-8 entry. G-01-7 stays OPEN — the levers are in and functioning (hold fired 3x, zero mid-service evictions, heals serialized ahead of fulls) but the series-A acceptance failed: image 7 thumb INCOMPLETE 35/36, image 7 full all-chunks-received but stored-bytes CRC mismatch, image 8 full silently never transferred (lost FULL manifest). Residual mechanism named and rescoped (see G-01-7). NEW gap G-01-9 opened: full-sized-thumbnail payload/metadata mismatch (the corruption the operator sees in thumbs), all-chunks CRC-mismatch fulls, and the lost-manifest silent drop. Settings visible-effect pairs (SC-3), QVGA restore, and reboot boot-resolution clauses were NOT exercised this session — they stay open under this Test. WR-01 regression watch: GREEN (no 'Critical battery' lines; beacons batt=valid throughout; balloon4.log:56 'Emergency Shutdown: Enabled' is the known benign boot config echo). UPDATE (01-13/01-14/01-15 remediation + bench session #5, 2026-08-25, balloon5.log/base5.log): the transfer-quality defect classes are ELIMINATED at bench — zero 'finalized INCOMPLETE' verdicts in either log (first session ever; session 4 had thumb 35/36 + two CRC-mismatch fulls), zero stored-bytes CRC mismatches INCLUDING CIF (31 chunks, base5.log:776) and SVGA (80/82 chunks, :1243/:2770) fulls, every logged thumbnail a genuine QQVGA payload (1176-1340 B / 6-7 chunks, drained-stale-frame discriminator at all 7 logged captures), zero BUSY deferrals needed, beacons held cadence (484 accepted, worst gap 2 consecutive seqs). Series-A acceptance STILL fails on two narrower axes: the third unspaced CAPTURE_NOW (seq=6) lost all 4 transmissions under the concurrent chunk storm and timed out honestly (base5.log:126), and image 15's full was silently lost to manifest AIR LOSS (balloon5.log:274 sent / never received / TX-success consumed the one-shot announce — G-01-9 defect C residual). Settings series: CIF (3a) PASS (re-init 6->8 balloon5.log:1000-1004, image 17 both kinds COMPLETE, thumb 1332 B); SVGA (3b) PASS (re-init 8->11 :1502-1506, images 18+19 both kinds COMPLETE, thumbs 1283/1176 B vs session-4's 145-chunk impostor); QVGA restore (3c) PASS by wire evidence — two SET_RESOLUTION-to-6 ACKed (:5048, :6384) and post-restore image 20 is QVGA-class (4090 B / 21 chunks, identical to boot-QVGA image 14's 4094 B / 21 chunks; NOT SVGA-class 16354 B / 82), both kinds COMPLETE (base5.log:3148/:3258); the operator's 'resolution still looks high' impression is contradicted by the wire (they were likely viewing pre-restore image 19). Reboot clause (3d) EXERCISED and verified BASE-SIDE: beacon seq reset 451->0 amid continuous post-reset traffic (base5.log:3341-3343 — chunk continuity across the reset proves a fresh ImageTxManager, i.e. a real reboot, not an air gap), first post-reboot capture = image 21 (NVS ID sequence continued 20->21) at QVGA-class sizing (4094 B / 21 chunks), both kinds COMPLETE (:3378/:3518); the balloon console detached BEFORE the reboot (balloon5.log ends at idle telemetry, last [BCN] seq=452 at :6777), so balloon-side boot-resolution lines are absent — clause judged on base-side wire evidence with that gap recorded. SC-3 visible-effect pairs (3e) NOT RUN — zero settings commands issued all session beyond SET_RESOLUTION (base5.log command queue: 9x CAPTURE_NOW, 4x SET_RESOLUTION, rest GET_STATUS/IMAGE_WINDOW_REQUEST); clause stays open under this Test, which stays 'issue'. WR-01 regression watch: GREEN (beacons 'batt=valid' on all 484 accepted lines; no 'Critical battery' lines). Dashboard glance (UI-SPEC items): browser observations leave no console evidence and the operator flagged nothing — recorded unverified-by-log (operator-did-not-flag), NOT as pass. LOG-READING CAVEAT discovered this session: '[HTTP] GET /img/... -> 404' lines (48x, e.g. base5.log:123-124) are the handleNotFound DISPATCH banner printed BEFORE the image handler runs (main_basestation.cpp:3641) — they are not response codes; and healed-thumb finalize lines undercount 'B persisted' by design (sd_storage.cpp:234 'accounting restarts at each flip (conservative)' — image 14: 1231 B received / 800 B persisted, complete=true via read-back CRC, base5.log:108-109; image 15: 1340/740, :310-311). UPDATE (01-20 bench session #6, 2026-08-27 evening through 2026-08-28 ~00:20 local, base6.log + balloon11.log — LOG PROVENANCE: the balloon console cycled balloon6..balloon11 captures through the day's bootloop debug saga, so the session's balloon-side evidence lives in balloon11.log while balloon6-10 are bootloop/debug captures; base6.log spans the whole day including the bootloop period, lines 1-~1618, whose 170 GET_STATUS timeouts are pre-session noise — see .planning/debug/resolved/balloon-bootloops-at-bench.md: the bootloop root cause was GPS UART1 RX claiming reserved OPI-PSRAM pin 35, fixed by 3f2c2c6 GPS_TX_PIN 35->41; both boards otherwise ran round-#8 behavior the whole session, wire format unchanged): SERIES A ran and narrowed to its cleanest axis yet — 3 unspaced CAPTURE_NOW (base6.log:3909/:3922/:3933, seq=182/183/184) all reached the balloon and all ACKED (:3915/:3955/:3958) with ZERO series-A command timeouts; the 01-18 quiet gate held both retried transmits under image-24's chunk flow and every held command transmitted and reached ACKED (:3945 'command seq=184 transmit held - inbound chunk stream active', :3946 seq=183, then :3955/:3958 ACKED; five more held->ACKED lifecycles in the late session :17952->:17956, :17978->:18013, :18059->:18090, :18120->:18125, :18138->:18142) — COMMAND SURVIVABILITY UNDER UNSPACED LOAD IS SOLVED AT BENCH (the session-5 seq=6 4/4-transmission-loss class is dead). 3 captures executed (balloon11.log:345/:387/:399 'Captured image ID 24/25/26'). BUT the verdict acceptance failed 2/6: thumbs 25/26 COMPLETE (base6.log:3979/:4003), thumb 24 INCOMPLETE 2/6 (:4098 'thumbnail push stalled; passes exhausted'), and ALL THREE fulls were lost — every loss NAMED at both consoles, nothing silent: image 24's FULL manifest WAS received at base (:3947) and its full-pull held behind the thumb heal (:3948), but the balloon evicted the entry at depth-3 queue overflow when image 26 enqueued (balloon11.log:406 'ImageTx: queue overflow (depth 3); evicting class 3 (queued, no airtime invested) entry image 24 for image 26'), so all 4 passes of base window requests were rejected ('window request for unknown/evicted image 24 rejected' balloon11.log:495/:501/:530/:559/:585/:605/:636/:656; base INCOMPLETE 0/26 :4170); images 25/26: their FULL manifests were received at the base REPEATEDLY via the 01-17 re-announce (image 25 3x base6.log:4015/:4044/:4074, image 26 4x :4005/:4035/:4065/:4090) but no FULL window ever armed before the 3-re-announce bound expired ('image 25 full dropped - no FULL window armed after 3 re-announces' balloon11.log:553; image 26 :575) — the bound expires while the base is still serializing the burst's thumbnails — and every subsequent window request landed post-drop and was rejected ('unknown/evicted image 25' :680/:702/:729/:751, 'image 26' :771/:838/:845/:868; base INCOMPLETE 0/22 :4231, 0/14 :4306). G-01-7 stays OPEN, rescoped to exactly this named residual (see gap entry). SC-3 VISIBLE-EFFECT PAIRS NOT RUN — zero SET_BRIGHTNESS/contrast/saturation/quality commands all session (queue mix: 13 CAPTURE_NOW, 849 GET_STATUS, 103 IMAGE_WINDOW_REQUEST, 2 SET_RESOLUTION) — the pairs ride a FIFTH round. WR-03 CADENCE DISCRIMINATOR NOT RUN — zero AUTO_CAPTURE_ENABLE commands. SETTINGS REGRESSION PARTIAL: 'Set resolution to 10' (SVGA) ACKed (balloon11.log:3295; base6.log:5384 queued / :5391 ACKed) and larger-than-QVGA fulls completed cleanly in the late session — image 30 85/85 COMPLETE (base6.log:8602), image 31 82/82 COMPLETE (:13096; note its FULL manifest was re-announced once, balloon11.log:25435 'FULL manifest(image 31, 16362 B, 82 chunks) re-announced (1/3)', after which the FULL window armed (:25438) and the full still completed — the re-announce recovery path running live and benign), image 33 133/133 COMPLETE (:17096), image 34 133/133 COMPLETE (:18134) — but the CIF cycle and QVGA-restore clauses were NOT exercised (the second SET_RESOLUTION seq=755 ACKed base6.log:16551/:16564 with the balloon console already detached, so its value is unconfirmed from logs), and image 29's larger full (47479 B / 238 chunks) ended INCOMPLETE 222/238 'retransmit passes exhausted' (:6337) in the lossy early stretch (41 END MARKER MISS this session vs session 5's 0-6; zero CRC FAIL frames). Transfer-quality holds held: zero stored-bytes CRC mismatches all session, every logged thumb a genuine QQVGA payload (1073-1879 B / 6-10 chunks, drained-stale-frame discriminator at all 9 captures). WR-01 regression watch GREEN (0 'Critical battery' lines on either console; 4213 'batt=valid' beacon lines, 0 'batt=invalid'). Dashboard glance (UI-SPEC items): no console evidence exists for browser observations and the operator flagged nothing this session — recorded unverified-by-log (operator-did-not-flag), the session-5 convention. Test 3 stays 'issue' (series-A verdict residual + SC-3 + WR-03 + CIF/QVGA-restore clauses open). UPDATE (01-24 bench session #7, 2026-08-28, balloon.log + base.log — LOG NAMING DEVIATION: the session consoles were retained as balloon.log/base.log rather than the plan's balloon12.log/base7.log convention; provenance recorded here rather than renaming the operator's retained files): SESSION FAILED — the balloon HARD-CRASHED twice during series A and the session aborted; NOTHING FLIPS ON THIS EVIDENCE. Crash 1 (balloon.log:289-295 CAPTURE_NOW image 35 ACKed, full 8100 B; :298 enqueued thumb 1188 B / 6 chunks; :300 manifest sent): :301 'i2cWrite(): i2c_master_transmit failed: [259] ESP_ERR_INVALID_STATE' then :302 'Guru Meditation Error: Core 1 panic'ed (Interrupt wdt timeout on CPU1)', second panic :313 'Unhandled debug exception' with :314 'Stack canary watchpoint triggered (IDLE1)', EXCCAUSE 0x6 IntegerDivideByZero :311/:351 (likely residual), reboot rst:0xc (RTC_SW_CPU_RST) :377-380 — image 35 buffers lost. Crash 2 (post-reboot, balloon.log:506-517 CAPTURE_NOW image 36 ACKed, full 7810 B; :517 manifest sent 9 chunks; :518 benign AUX-missed line; :519-520 chunk 1/9 sent 217 B; :525 telemetry beacon stream started): :526-528 rst:0x7 (TG0WDT_SYS_RST) hard watchdog reset with NO panic output — image 36 buffers lost. Consequence: every window request for image 36 rejected ('ImageTx: window request for unknown/evicted image 36 rejected' balloon.log:670; 'CommandHandler: Command IMAGE_WINDOW_REQUEST - FAILED' :673/:728/:785/:845) with the base retrying futilely (NACK_INVALID seq 7/8/10/12 base.log:167/:199/:236/:273), and the two crash-window CAPTURE_NOWs hit the FORBIDDEN terminal 'CommandSender: Command seq=5 timeout after 3 retries' (base.log:141) and 'seq=6' (:143). SERIES-A VERDICT 0/6 COMPLETE (2 captures executed, 0 transferred — both images' buffers died in the resets); NONE of round #10's positive discriminators fired (zero 're-announce budget re-armed', zero receipt-evidenced class-5 evictions, zero base 'full-pull activation deadline reached' — the crashes precede any real burst). NEW DEFECT D1 (BLOCKER, routed G-01-10 + WINDOWS 15): crash regression at the start of post-capture radio push — both hard resets land within seconds of the thumbnail manifest send (crash 1) / first chunk + beacon start (crash 2); session #6 ran the identical pattern clean on pre-round-#10 firmware (3/3 ACKs, 25/26 thumbs COMPLETE, zero resets across a 12-image session), so the regression is from this round's changes (01-21/01-22/01-23); signature: I2C bus INVALID_STATE + interrupt-watchdog (INT_WDT CPU1 / TG0WDT) + Core-0 spinlock contention (xTaskIncrementTick -> spinlock_acquire); root cause NOT established — debug round required (addr2line on the crash-1 backtrace, ELF SHA256 3d2b351b4; suspects: 01-21 pushPending busy-hold/budget-re-arm paths, 01-23 framer resync or command_handler guard, or a latent instability the new TX cadence exercises). NEW DEFECT D2 (minor, routed G-01-11 + WINDOWS 16): the 01-21 discriminator 'ImageTx: re-announce held - inbound window traffic active' fires spuriously at EVERY boot with zero inbound traffic (balloon.log:135/:504/:652, each within seconds of System ready) — the zero-initialized global receipt stamp satisfies the wrap-safe (millis() - stamp) < IMG_FULL_REANNOUNCE_BUSY_MS check for the first 15 s of uptime, so 'never received' is indistinguishable from 'received at boot'; consumes nothing (latch-guarded, once per boot) but pollutes the discriminator evidence. SC-3 pairs / CIF cycle / QVGA restore NOT RUN — session aborted before any settings command (verifier-grepped: zero SET_* lines in either log). Dashboard glance: BASELINE ONLY observed (base web stack alive — /api/state serving, AP up, clients=1, beacons accepted seq 1-40); no LOOK verdicts recorded for the d8ba14e UI delta — the operator-LOOKED glance rides again. WR-01 battery watch: GREEN (55 'batt=valid' beacon lines in base.log, verifier-counted; zero 'Critical battery' lines either console). Test 3 stays 'issue' (crash-regression blocker D1 first in the queue; series-A verdict residual unjudgeable this session; SC-3 + CIF/QVGA clauses sixth round riding). UPDATE (01-27 bench session #8, 2026-08-28, balloon2.log + base2.log — LOG NAMING DEVIATION: consoles retained as balloon2.log/base2.log rather than the plan's balloon13.log/base8.log request; provenance recorded here, operator files never renamed, the 01-24 convention; firmware = round #11, build Aug 28 2026 09:48:55, balloon ELF SHA256 c53635e181 cross-checked against .pio/build before any decode): SESSION FAILED AT IMAGE 38's FULL TX — D1 RE-OPENED, D2 FLIPPED RESOLVED, NOTHING ELSE MOVES. D1 spaced smoke SURVIVED: the session-7 crash-1 pattern (first post-boot capture push) ran CLEAN — CAPTURE_NOW image 37 (balloon2.log:207-213) through enqueue/manifest/chunks/FULL manifest/window service with ZERO crash signatures, both kinds COMPLETE at base (thumb 8/8 base2.log:74-75, full 36/36 :238-239). Then SET_RESOLUTION wire-10 (SVGA) executed + ACKed (balloon2.log:589-592, base2.log:256/:260/:263 — the operator's step, not the plan's CIF wire-8; its capture's full 27377 B / 137 chunks is SVGA-class), second CAPTURE_NOW image 38 SUCCESS (balloon2.log:604-609), thumb COMPLETE 8/8 (base2.log:290-291), FULL manifest sent (:652) — and after six clean FULL windows (chunks 0..95, balloon2.log:660-:1216) the balloon reset at the FIRST CHUNK of re-armed window 96..109: :1220 'ImageTx: FULL window armed for image 38 (chunks 96..109)' → :1227 'window chunk(image 38 kind 1, 1/14, 200 B) sent' → :1228-1231 'rst:0x7 (TG0WDT_SYS_RST)' 'Saved PC:0x40376430' — decoded (ELF SHA c53635e181 verified first) as tick_hook at esp-idf int_wdt.c:111, ZERO project frames. Image 38 buffers lost; base INCOMPLETE 109/137 after 3 passes (base2.log:651-652) — crash-caused, NOT burst-delivery. Crash-signature grep counts, whole balloon2.log: i2cWrite ESP_ERR_INVALID_STATE = 2 (:885/:1577, both followed immediately by a successful BMP280 read, neither adjacent to the reset — recurring BMP280-path noise, distinct from the fatal TG0WDT); Guru Meditation = 0; stack canary/watchpoint = 0; rst:0x7 = 1 (the fatal); rst:0xc = 0; U+FFFD mojibake = 0 (session-7's corruption signature ABSENT; the ┬░ on BMP280 lines is the constant console degree-sign rendering, present on healthy lines at both boots). [MEM] evidence: enqueue line present and healthy (:611 heap=8354328 psram=8149436 stackHW=5744), last pre-reset :1175 heap=8332460 stackHW=5552 — no starvation — so the 01-25 PSRAM-warm-up hypothesis is DISCONFIRMED as sufficient (warm-up ran at both boots :102/:1321; crash recurred mid-service on healthy memory; session-8 evidence appended to .planning/debug/d1-crash-regression-push-start.md §6; new root-cause axis: task-watchdog starvation/loop-block during FULL window service). D2 BOOT WINDOWS CLEAN at BOTH boots — G-01-11 flipped resolved (zero 're-announce held' in either boot window before the first genuine inbound window request; the session's 2 fires are genuine: :335 during image 37's window service, :1389 five lines after a genuine (rejected) inbound request :1384-1388 — timestamp-correlated, the receipt-ever flag working as designed). SERIES A NEVER RAN (the two CAPTURE_NOWs are minutes apart — spaced; the session aborted before any unspaced burst; zero round-#10 discriminators: no 'budget re-armed', no 'evicting class 5' in balloon2.log, no base 'full-pull activation deadline reached') — G-01-7 stays open, seventh round riding. ZERO command timeouts in base2.log (no forbidden CAPTURE_NOW terminals, unlike session 7). SC-3 visible-effect pairs NOT RUN (zero SET_BRIGHTNESS/contrast/saturation/quality commands); WR-03 cadence discriminator NOT RUN (zero AUTO_CAPTURE commands, see Test 4); CIF-cycle-as-specified NOT RUN (the executed SET_RESOLUTION was wire-10/SVGA, not wire-8/CIF, and its full never completed); QVGA restore NOT RUN — seventh round riding. Dashboard LOOK glance NOT REACHED (session aborted at image 38; /api/state was serving — base2.log:527/:532 — but no LOOK verdicts recorded). WR-01 battery watch GREEN (zero 'Critical battery' lines; beacons batt=valid). WR-02 healthy-boot discriminator CLEAN at both boots (zero 'health check failed' lines in balloon2.log; both boots show only the honest skipped notes :109-110 and :1328-1329 — WINDOWS 18's stageable bench evidence). Test 3 stays 'issue' (D1 re-opened with new evidence; series-A unjudgeable; SC-3/CIF-as-specified/QVGA clauses seventh round riding). UPDATE (01-29 bench session #9, 2026-08-28, balloon3.log + base3.log — LOG NAMING DEVIATION: consoles retained as balloon3.log/base3.log rather than the plan's balloon14.log/base9.log request; provenance recorded here, operator files never renamed, the 01-24 convention; firmware = round #12, build banner Aug 28 2026 12:06:09 at :23 and :535, balloon ELF SHA256 e09dd034a5ab verified before decode): SESSION FAILED AT PROTOCOL STEP 3 — D1 RE-OPENED WITH ITS THIRD EXPRESSION, NOTHING FLIPS. The spaced smoke's FIRST WINDOW served CLEAN: CAPTURE_NOW image 39 (balloon3.log:366, ACKed base3.log:61) → enqueue :375 (full 7133 B / 36 chunks, thumb 1661 B / 9 chunks) → thumb COMPLETE 9/9 (base3.log:79) → FULL window 1 (chunks 0..15, seq 3, ACKed :89) armed balloon3.log:421 and served ALL 16/16 chunks on air (:427-:491). Then, in the INTER-WINDOW LULL (GET_STATUS :497-:501, [BCN] seq=23 :505, healthy [MEM] :511, loop Max 1277 ms :514, beacon TX :518-:519), the balloon reset at t=124026 ms: :520 'i2cWrite(): i2c_master_transmit failed: [259] ESP_ERR_INVALID_S∩┐╜ATE' (mojibake ON the line — the session-7 corruption-class byte sequence replacing the T of STATE, hex-verified; the UTF-8-replacement-char grep returns 0 — the twice-documented §7.4 trap) → :521-:522 ROM banner → :523 rst:0x7 TG0WDT_SYS_RST → :524 Saved PC:0x4037c7fa — DECODED (ELF SHA e09dd034 verified first, re-run by this executor) as esp_vApplicationTickHook at esp-idf freertos_hooks.c:34, the registered-tick-hook DISPATCH LOOP one frame above session-8's tick_hook sample in the SAME tick-ISR chain, ZERO project frames. Boot 2's own [BOOT] discriminator independently names the cause: :615 'ImageTx: [BOOT] reset-cause: TASK_WDT (G-01-10)' — the ROM reason register confirming the task-watchdog read. THE ROUND-#12 DISCRIMINATORS ANSWERED (debug doc §8.3): B2 [LOOP] slow-pass = ZERO fires whole-console with loopTask demonstrably healthy (Max 1277 ms vs the 2500 ms latch) — per §7.6's own read this IS the session-8 CPU0-side signature reproduced, and the Lever-A loop-hang hypothesis is disconfirmed (the loop cycled to the final second); B1 = worked at both boots; [MEM] healthy at enqueue :372 and last pre-reset :511 (heap=8534176, stackHW=5744) — exhaustion ruled out a third session. THE i2cWrite ADJACENCY BREAKS THE NOISE CLASSIFICATION (debug doc §8.4): sessions 7/8 called these errors non-adjacent BMP280 noise (successful read follows, far from reset) — session 9's single occurrence is DIRECTLY adjacent (:520 → :523), is NOT followed by any successful read (the reset intervened), AND carries the corruption bytes on its own line. Crash-signature grep counts, whole balloon3.log: rst:0x7 = 1 (:523, the fatal; the :12 rst:0x1 is the benign initial POWERON); Guru Meditation = 0; canary/watchpoint = 0; rst:0xc = 0; mojibake (corrected method) = 1 (ON :520 itself); i2cWrite ESP_ERR_INVALID_STATE = 1 (:520, adjacent — no longer classifiable as noise). Base-side consequence: window 1 lost chunk 7 on air (ordinary RF loss — 15/16 delivered), the base's retransmit windows (chunks 7..7, seq 5/6/7) hit the already-crashed balloon — NACK_INVALID ×3 (base3.log:162/:171/:181; honest post-reboot rejections balloon3.log:679/:692/:713 'window request for unknown/evicted image 39 rejected', the NVS-restored entry carrying no valid full buffer :647) → :194 'image 39 kind 1 finalized INCOMPLETE (retransmit passes exhausted): 15/36 chunks after 3 passes' — CRASH-CAUSED, not burst-delivery. Zero command timeouts in base3.log. D2 receipt-ever flag held again: exactly 2 're-announce held' fires, BOTH genuine (:492 immediately after chunk 16/16 — inbound window traffic active; :683 four lines after the rejected-but-received request :679-:682). WR-01 battery watch GREEN. WR-02 healthy-boot discriminator CLEAN again (zero 'health check failed' lines whole console). SC-3 pairs / CIF-cycle / QVGA restore / dashboard LOOK: NOT RUN — the session aborted at step 3, EIGHTH round riding. Test 3 stays 'issue' (D1 re-opened with the third expression — debug round #3 routed; series-A unjudgeable a third time; SC-3/CIF/QVGA clauses eighth round riding)

### 4. Auto-capture cadence + disable
expected: AUTO_CAPTURE_ENABLE captures at exactly the commanded interval (try a
value above 30 s to prove no legacy interleave); after an ACKed AUTO_CAPTURE_DISABLE
zero automatic captures occur; image IDs form one sequence across manual + interval
captures (GET_STATUS lastImageId truthful)
result: pass
note: G-01-4 resolved by 01-07 (754f834, delegated in-page AJAX submit for all section#capture forms); runtime truth confirmed at the 01-09 bench — the operator triggered captures from the dashboard across the session with no raw-JSON navigation or Back-button behavior reported. UPDATE (01-24 bench session #7, 2026-08-28): the WR-03 cadence discriminator did NOT run — zero AUTO_CAPTURE commands reached the balloon (verifier-grepped: zero AUTO_CAPTURE lines in balloon.log and base.log; the session aborted during series A on the D1 crash regression, G-01-10, before step 2 of the bench protocol). The SC-5 coincidental-reliance advisory (01-VERIFICATION.md — the 01-19 baseline change still lacks bench discrimination on current HEAD) is NOT discharged and keeps riding. Test 4's standing PASS is untouched — the auto-capture path was never exercised this session, so there is no regression evidence against it. UPDATE (01-27 bench session #8, 2026-08-28, balloon2.log/base2.log): the WR-03 cadence discriminator did NOT run — SEVENTH round riding (zero AUTO_CAPTURE commands in either console; the session aborted at image 38's FULL service on the D1 recurrence before the cadence step). The SC-5 coincidental-reliance advisory remains NOT discharged. Test 4's standing PASS again untouched — the auto-capture path was never exercised, no regression evidence against it. UPDATE (01-29 bench session #9, 2026-08-28, balloon3.log/base3.log): the WR-03 cadence discriminator did NOT run — EIGHTH round riding (zero AUTO_CAPTURE commands in either console; the session aborted at protocol step 3 on the D1 recurrence — the third crash expression — before the cadence step). The SC-5 coincidental-reliance advisory remains NOT discharged. Test 4's standing PASS again untouched — the auto-capture path was never exercised, no regression evidence against it

### 5. Prohibition review — no fabricated state on the wire or UI
expected: Judgment-tier prohibition from plan 01-06: the operator is never shown
protocol or camera state that does not reflect reality (no ACK for unexecuted
actions, no green link LED while failed, GET_STATUS fields match actual balloon
state)
result: pass

## Summary

total: 5
passed: 4
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-01-1
  truth: "ACK within 2 s; queue row updates to terminal state; counters advance"
  status: resolved
  reason: "User reported: There are currently problems with the balloon node not sending data nor using it's OLED screen"
  severity: blocker
  test: 1
  resolved_by: "1223f46 (debug session balloon-no-data-oled-blank — three stacked causes)"
  resolved_at: 2026-08-22
  verified_by: "UAT re-test pass on hardware"
  artifacts: []
  missing: []

- gap_id: G-01-3
  truth: "Adjusting camera settings from the web UI produces visible changes in captured images; CIF 400x296 option succeeds; base Storage section reflects the mounted 4GB FAT32 SD; picture transmissions complete without timing out"
  status: resolved
  reason: "User reported: There are other problems with the camera, the Storage section says 'Unavailable' but there is a 4Gb FAT32 formatted SD in the base station module. All the picture transmissions are failing with timeouts."
  severity: blocker
  test: 3
  root_cause: "Two INDEPENDENT causes. (A) Storage 'Unavailable' is a real SD.begin() mount failure, not a UI bug — wiring doesn't match the constants in base_station_config.h (SCK=12/MISO=13/MOSI=11/CS=10); 02-03-PLAN explicitly deferred pin confirmation to hardware UAT. (B) Image-transfer timeouts are an E32 transport failure specific to the system's only multi-RF-packet frames: 216-byte 0x13 chunk frames sub-pack into 4x58-byte RF packets (~1.0-1.4 s air time at factory-default 2.4 kbps) while every working frame class is a single RF packet. Ranked mechanisms: B1 air-rate config makes chunk TX exceed the 5-s AUX bound (e32_lora.cpp:187); B2 per-packet loss x4 defeats the 3-pass ARQ (base logs type=13 CRC FAIL); B3 30 dBm PA current sag corrupts ~1 s transmissions; B4 sustained push streams saturate the channel so window requests/GET_STATUS collide. One bench run of existing UART0 traces discriminates."
  artifacts:
    - path: "src/sd_storage.cpp"
      issue: "SD.begin() genuinely fails on hardware; begin()/pin code is correct — physical wiring/card issue"
    - path: "include/base_station_config.h"
      issue: "SD pin constants (SCK 12/MISO 13/MOSI 11/CS 10) to audit against actual wiring"
    - path: "src/e32_lora.cpp"
      issue: "transmit() AUX handshake bounds (1 s low / 5 s high) modeled without 58-byte sub-packing / 2.4 kbps default air-time reality"
    - path: "src/image_tx_manager.cpp"
      issue: "timing margins (5 s preemption / 8 s stall / 15 s window ACK / 3 passes) are the levers if mechanism is saturation/loss"
    - path: "src/image_rx_manager.cpp"
      issue: "RX logic exonerated; SD-independent by design (SD-down yields INCOMPLETE/notStored, never timeouts)"
    - path: "src/command_sender.cpp"
      issue: "where unACKed window requests become the 'Timeout' queue rows the user sees"
  missing:
    - "Audit SD breakout wiring (4 signal wires + 3V3/GND) against base_station_config.h constants; confirm via boot line 'SdStorage: card mounted'"
    - "Retry mount with a second known-good FAT32 card to rule out card-contact/module-power issues"
    - "Bench discriminator run: one capture attempt watching balloon serial (ImageTx chunk sent|FAILED, E32 Transmit timeout) and base UART0 (type=13 CRC FAIL, manifest, queue rows)"
    - "Raise E32 air data rate via module config (8-25x less time-on-air) and/or fix PA power per B1/B3"
    - "If saturation (B4) persists: pace captures or adjust chunk size/stall windows"
  debug_session: .planning/debug/storage-unavailable-image-tx-timeout.md
  resolved_by: "01-09 bench session: SD_MMC 1-bit transport switch (794df00, operator datasheet finding — built-in slot CLK=39/CMD=38/DATA=40, not the assumed SPI breakout) + both boards at 9.6 kbps (01-08 ce24cd8). The interim base-only flash severing the pair (balloon still at factory 2.4k — no telemetry, window requests TIMEOUT) confirmed the air-rate enforcement is real and load-bearing; balloon re-flash restored the link exactly as the 01-08 tripwire scenario predicted"
  resolved_at: 2026-08-23
  verified_by: "Operator bench observation: Storage tile OK (green); command round-trip ACK; one capture completed end-to-end — full image CRC-verified and rendered intact from SD. See 01-09-SUMMARY.md for the evidence table"
  artifacts_final:
    - "include/base_station_config.h + include/sd_storage.h + src/sd_storage.cpp (794df00): SD_MMC 1-bit on the built-in slot — the root-cause-A fix"
    - "src/e32_lora.cpp (01-08, e16845a/ce24cd8): real register protocol + boot-time 9.6k enforcement — the root-cause-B fix"

- gap_id: G-01-5
  truth: "A triggered capture's THUMBNAIL arrives intact and renders uncorrupted in the gallery (or its D-22 kind-addressed window heal recovers it to COMPLETE) while the full completes — no corrupt-preview, Incomplete-badged captures at bench range"
  status: resolved
  reason: "Operator bench report (01-09 iteration 5): 'the thumbnail image looks corrupted, and there is a 'Incomplete' message over it. The full image looks good.'"
  severity: major
  test: 3
  root_cause: "Thumbnail PUSH-burst residual loss (debug-matrix B2/B4 class) — NOT a transfer-logic fault. The full image (windowed ARQ pull: per-window verification, passCount reset on accepted progress) recovered everything at 9.6 kbps, eliminating B1 (chunks transmit and complete) and making B3 unlikely (the full's longer sustained multi-RF-packet stream verified end-to-end; PA sag would hit it hardest). The thumbnail streams as a blind push burst immediately after its manifest; holes are discovered only post-hoc and healed by the D-22 kind-addressable window path AFTER the full pull finishes (gate 1, image_rx_manager.cpp:190-196) — bounded to 3 passes (image_rx_manager.cpp:198-199, 'thumbnail push stalled; passes exhausted'). The partial IMG_{id}_T.JPG streams to SD during reception (acceptChunk writes every chunk), so the corrupt render + D-48 Incomplete badge is the designed honest degradation, working as built. Balloon-side servicing verified present by code read (kind-split window arming slices thumbBuffer; buffers retained post-SERVED for a 15-min TTL) — the heal was servable. Whether the heal windows were lost on air (B2/B4) or a timing/margin edge exists at the new air rate requires the serial discriminator below. UPDATE (01-10 bench discriminator round, mechanism NAMED): the dominant 'loss' is an E32 AUX handshake phantom transmit-failure — balloon-side TX accounting defect, NOT air loss. E32LoRa::transmit() (src/e32_lora.cpp:207-233) writes the bytes to UART FIRST (:223 serial->write + flush), THEN waits for AUX-low confirmation (:227, 1000 ms) and returns false on timeout — the data has already left the radio when the 'FAILED' verdict is booked. ~50% of chunk transmissions hit the timeout while the base received every chunk (v3: balloon tallied 37 sent / 41 FAILED, base finalized both kinds COMPLETE 36/36). The transfer/heal machinery is driven by this false loss signal: spurious window requests (4 [E32TX] cmd=30 rows in v2, 5 in v3) trigger re-transmission of data that already arrived. Real air loss is tiny and self-heals: END MARKER MISS totals 6 (v1 flood session) / 1 (v2) / 2 (v3), CRC FAIL 0 in every session, all recovered within one ARQ pass. B5 (close-range 30 dBm RX-overload/desense): NOT REPRODUCED — downgraded; every close-range capture on fresh-boot firmware completed with ~zero real loss, leaving B5 no unexplained loss to account for. The escalated 'fulls also failing at close range' symptom is attributed to the G-01-6 overwrite interaction (fresh-boot ID re-allocation overwriting card files) plus the v1-session phantom-failure flood, not to RF impairment."
  artifacts:
    - path: "src/image_rx_manager.cpp"
      issue: "thumbnail heal loop (gates 1/2 at :190-196, 3-pass bound + finalizeIncomplete at :198-199) — pass bound and stall margins are the levers"
    - path: "src/image_tx_manager.cpp"
      issue: "kind-split window arming (:634-732) + thumbBuffer retention — servicing verified present in code; bench-trace confirmation pending"
    - path: "include/image_protocol.h"
      issue: "IMG_WINDOW_STALL_MS 8000 / IMG_THUMB_HEAL_IDLE_MS 24000 / IMG_RETRANSMIT_MAX_PASSES 3 — pacing levers for the follow-up round (01-08's deferred follow-up condition)"
  missing:
    - "CLOSED by the 01-10 discriminator round: serial discriminator captured on both consoles (see discriminator_evidence below) — mechanism class named: E32 AUX handshake phantom transmit-failure (balloon-side TX accounting defect at src/e32_lora.cpp:223/:227 write-then-wait), not heal-lost-on-air"
    - "CLOSED by the 01-10 round: second-capture re-test — v2 AND v3 were both fresh captures on fresh-boot firmware; thumbnails arrived intact both times (8/8 and 36/36 COMPLETE first-try) — per-burst loss disproven as the standing mechanism"
    - "CLOSED by 01-11 (f265556): AUX-handshake accounting fixed — a missed AUX-low after a COMPLETE write no longer books FAILED ('E32: AUX-low missed after complete write - treated as sent (bytes left the radio)', 31x balloon.log / 0x-FAILED / 90x balloon3.log — all benign); the false loss signal and its spurious window requests are gone. Thumbnail sizing quirk closed by ride-along R2 (c67e1a5, QQVGA guard): correct sizes all sessions — 1362/1465/1468/1703 B / 7-9 chunks; the v3 7138-B quirk did not recur"
    - "CLOSED by the 01-11 post-fix series (2026-08-24, three sessions): before/after tally recorded — phantom FAILED 0/0/0 across sessions vs 41/capture baseline (v3); real air loss bounded and self-healing (END MARKER MISS 0-6/session, all recovered by pass budget); close-vs-separated quantification superseded — B5 stays downgraded (not reproduced) and the residual is mechanism-named as concurrent-transfer starvation (G-01-7), not air loss; with capture spacing 4/4 kinds COMPLETE (session 3)"
    - "EXECUTED at the 01-11 re-verification: the settings series ran and surfaced the SET_RESOLUTION VGA buffer-realloc defect (G-01-8, new); settings visible-effect + CIF 400x296 clauses stay open under Test 3 via G-01-8 (WR-01 regression-watch portion green since 01-10)"
  discriminator_evidence:
    - "BALLOON v3 (balloon3.log) per-chunk pattern, verbatim: 'E32: Transmit timeout - AUX didn't go low' followed by 'ImageTx: chunk(image 1, 1/36, 200 B) FAILED' / 'ImageTx: window chunk(image 1, 1/16, 200 B) FAILED' — while the base received everything (see finalize lines below)"
    - "BALLOON v3 tallies (balloon3.log): push burst 17 chunk sent / 19 FAILED (chunks 1-36); window service 18 sent / 22 FAILED (4 windows: 16+2+16+2 chunks); +2 manifests sent => 37 sent / 41 FAILED total; 43 AUX timeouts (the 2 extra attach to beacon/manifest sends — balloon.log v1 shows the same phantom on beacons: '[BCN] seq=40 sent=39 ok=0', '[BCN] seq=60 sent=58 ok=0' while the base accepted every beacon)"
    - "BALLOON v3 enqueue (balloon3.log:132, the thumbnail-sizing quirk): 'ImageTx: enqueued image 1 (full 7138 B, thumb 7138 B / 36 chunks, source 0)' — thumbnail size equals full size; v2's thumbnail was correctly 1465 B / 8 chunks"
    - "BASE v2 (base2.log:866-867): 'ImageRx: image 1 kind 0 finalized COMPLETE (8/8 chunks, 1465 B)' + 'SdStorage: finalized /images/IMG_00001_T.JPG (8/8 chunks, 1465 B persisted, complete=true)' — thumbnail COMPLETE first-try, no heal fired"
    - "BASE v2 (base2.log:2865-2866): 'ImageRx: image 1 kind 1 finalized COMPLETE (36/36 chunks, 7158 B)' + 'SdStorage: finalized /images/IMG_00001.JPG (36/36 chunks, 7158 B persisted, complete=true)' — one window re-request pass served it: base2.log:1857 'ImageRx: window request queued for image 1 kind 1 (chunks 14..15, seq 5, pass 1)'"
    - "BASE v3 (base3.log:3320-3321): 'ImageRx: image 1 kind 0 finalized COMPLETE (36/36 chunks, 7138 B)' + 'SdStorage: finalized /images/IMG_00001_T.JPG (36/36 chunks, 7138 B persisted, complete=true)'; (base3.log:5758-5759): kind 1 'COMPLETE (36/36 chunks, 7138 B)' + IMG_00001.JPG persisted complete=true — 4/4 kinds COMPLETE across v2/v3 despite 41 balloon-booked failures in v3"
    - "REAL AIR LOSS, all sessions (base logs): '[FRAME] type=13 len=216 END MARKER MISS' — 6x in base.log (v1), 1x in base2.log (v2), 2x in base3.log (v3); 'CRC FAIL' count 0 in every session; every miss recovered within one ARQ pass (approx 1-2 chunks per 16)"
    - "SPURIOUS WINDOW REQUESTS (the false-loss signal driving the heal machinery): '[E32TX] cmd=30' rows — 4 in base2.log (v2), 5 in base3.log (v3), per single capture each"
    - "CODE (src/e32_lora.cpp:207-233): :223 'size_t sent = serial->write(data, length); serial->flush();' executes BEFORE :227 'if (!waitForAuxLow(1000))' which prints :229 'E32: Transmit timeout - AUX didn't go low' and returns false — the bytes have already left the radio; the FAILED accounting is phantom"
    - "OPERATOR-REPORTED (live console, not retained in saved excerpts): 'CommandHandler: Response failed' ACK false-failures — same AUX mechanism hitting command-response writes; consistent with the 2 non-chunk AUX timeouts retained in balloon3.log"
    - "SECONDARY FINDING (base hot-loop error flood, NOT the loss mechanism — everything arrived despite it; cleanup routed to 01-11): '[3819832][E][esp32-hal-gpio.c:185] __digitalWrite(): IO 39 is not set as GPIO. Execute digitalMode(39, OUTPUT) first.' — 9,563 lines in base.log (v1), 3,513 in base2.log, 6,122 in base3.log, constant while idle and transferring (observed spacing ~22 ms). Cause: STATUS_LED_PIN 39 (src/main_basestation.cpp:40) is double-booked with SD_CLK_PIN 39 (include/base_station_config.h, SDMMC host owns the pin after SDStorage().begin()); updateLED()'s READY/NO_LINK branches call digitalWrite(STATUS_LED_PIN) EVERY loop pass (main_basestation.cpp:3672/3676 — only the 1 Hz blink branch is throttled), each triggering the HAL validation error + a synchronous UART print. The GPIO39 coexistence itself was documented at 01-09 ('cannot disturb the SD clock' — confirmed: all data arrived); the per-pass console flood and its ~7-10 ms blocking are the new finding. Fix shape: remap STATUS_LED_PIN off GPIO 39 + throttle the per-pass writes"
    - "WR-01 REGRESSION WATCH (Task 3 portion, GREEN): 0 'Critical battery' lines, no Emergency-mode entry, no camera auto-disable across all balloon logs; beacons carried a valid battery verdict throughout — base3.log verbatim: 'ImageRx: beacon seq=3 alt=0.0m temp=24.0C gps=no-fix batt=valid'. Operator-reported boot observations (live console; the retained balloon logs do not include the boot window — balloon2.log's capture ends at 'Sensor pins initialized'): boot banner 'Emergency Shutdown: Enabled' is power_manager.cpp:706 config echo, not a trigger; 'Battery monitoring active (raw reading: 4095)' (main_balloon.cpp:688) is ADC full-scale on the USB-powered bench unit's floating sense line — the [1.8,8.0]V validity gate cannot detect a full-scale float; expected at bench, flagged for flight config"
  debug_session: .planning/debug/storage-unavailable-image-tx-timeout.md
  resolved_by: "01-11 f265556 (branch c, operator-confirmed routing): E32LoRa::transmit() no longer books FAILED when a complete write is followed by a missed AUX-low — the benign case logs 'E32: AUX-low missed after complete write - treated as sent (bytes left the radio)'; short-write and pre-write readiness remain true failures. Ride-along R2 c67e1a5 (thumbnail QQVGA frame-dimension guard) closes the sizing quirk"
  resolved_at: 2026-08-24
  verified_by: "Operator bench re-verification on fixed firmware, three sessions (2026-08-24), all with consolidated UART0 logging. Session 1 (balloon.log/base.log): pre-resolution-incident capture PASSED the entire before/after table — 46 ImageTx chunk/window sends / 0 FAILED / 31 benign AUX-missed; base.log:68 'ImageRx: image 1 kind 0 finalized COMPLETE (7/7 chunks, 1362 B)' + :186 'ImageRx: image 1 kind 1 finalized COMPLETE (35/35 chunks, 6931 B)'; IO-39 flood ZERO lines (R1 verified). Session 3, the Task 3 discriminator (base3/balloon3, SPACED captures — operator waited for both finalize lines between triggers): base3.log:75 'ImageRx: image 4 kind 0 finalized COMPLETE (8/8 chunks, 1465 B)', :237 'kind 1 finalized COMPLETE (52/52 chunks, 10225 B)', :290 'image 5 kind 0 finalized COMPLETE (9/9 chunks, 1703 B)', :524 'image 5 kind 1 finalized COMPLETE (53/53 chunks, 10431 B)' — 4/4 kinds COMPLETE, tail 49..52 recovered on pass 2 (:491/:512); balloon3.log 157 chunk sends / 0 FAILED / 90 benign AUX-missed; base3.log 5 END MARKER MISS all recovered. Verdict: the named mechanism (phantom TX-failure accounting) is fixed and transfers complete reliably with capture spacing; the residual under unspaced captures is a DIFFERENT mechanism, routed as G-01-7"
  artifacts_final:
    - "src/e32_lora.cpp (f265556): complete-write-then-missed-AUX-low reclassified benign — the G-01-5 fix at the trace-named site"
    - "src/camera_manager.cpp (c67e1a5): thumbnail QQVGA guard — the sizing-quirk closure"

- gap_id: G-01-6
  truth: "The dashboard Image Gallery grows as new captures persist — grid, pager, and count reflect every finalized image (12 per page, newest first)"
  status: resolved
  reason: "Operator report (2026-08-23, continued bench after the 01-09 session): 'The gallery is not expanding to show new pictures, it's locked at 6 pictures.'"
  severity: major
  test: 3
  root_cause: "Undiagnosed — captured for the gap-planning round. Code-read triage: the server path looks sound (handleGalleryList main_basestation.cpp:3461-3523 pages 12/req via SD_GALLERY_PAGE_SIZE; index cap is 1000 with newest-window eviction sd_storage.cpp:532-545; lazy rebuild keyed on indexVersion sd_storage.cpp:569-573). The UI refresh contract is the leading suspect surface: '/api/state's galleryCount is the ONLY refresh signal' for the grid (main_basestation.cpp:1603-1615, D-36) — if the served galleryCount stops advancing (or advances on fewer paths than persistence does), the grid never re-fetches and stays frozen at whatever it last rendered. Competing mechanism: later captures finalize INCOMPLETE and never persist files to SD (index legitimately never grows) — plausible given G-01-5's escalated loss rate, in which case G-01-6 is a symptom of G-01-5, not a separate defect. UPDATE (01-10 bench discriminator round, branch CONFIRMED): persistence OVERWRITE, not refresh-stale — the balloon's image-ID counter resets on every reboot, so a fresh-boot balloon re-allocates image ID 1 ('CommandHandler: Captured image ID 1', balloon3.log:127) and the resulting IMG_00001_* filenames OVERWRITE the card's existing image-1 files instead of appending; the gallery index count cannot grow. Overwrite-not-append proven twice on hardware (v2 and v3 both persisted IMG_00001_T.JPG + IMG_00001.JPG onto a card whose index already held 6 images, and the index stayed at 6 after each persist). Compounding input: in the earlier failing session (v1) later captures never finalized at all (zero finalize lines in base.log), so historically both mechanisms starved the gallery — the failing-transfer half is explained by G-01-5's phantom-failure/overwrite interaction, and the durable half is the ID-reset overwrite, which persists even with perfect transfers (v2/v3 proved transfers can be perfect and the count still stays 6)."
  artifacts:
    - path: "src/main_basestation.cpp"
      issue: "gallery refresh signal — galleryCount served in /api/state (D-36, :3317) vs the footer script's galleryCountSeen comparison (:1603-1615); verify count source and every path that bumps it"
    - path: "src/sd_storage.cpp"
      issue: "indexVersion bump sites — every persist path (full, thumb, sidecars, finalize) must mark the index stale or ensureIndexCurrent() never rebuilds (:569-573)"
    - path: "src/image_rx_manager.cpp"
      issue: "finalize path — do INCOMPLETE captures persist indexed files (partials streamed during reception) or none? Determines whether G-01-6 can be a G-01-5 symptom"
  missing:
    - "CLOSED by the 01-10 discriminator round: zero-tooling discriminator run — operator fetched http://192.168.4.1/gallery?page=1 in the browser and the JSON total EQUALLED the dashboard grid count (both 6) => persistence branch (refresh signal is NOT stale; the served count faithfully reflects an index that never grows)"
    - "CLOSED by the 01-10 round: finalize verdicts named from the base console — the failing session (v1, base.log) has ZERO finalize lines for post-6th captures (transfer-slot stall, G-01-5 class), while v2/v3 fresh-boot sessions DID finalize and persist (quotes below) yet the index still read 6 — naming the overwrite, not a finalize defect, as the durable mechanism"
    - "CLOSED by 01-11 (678d4f1, branch e): image-ID counter persisted to NVS in AutoCapture (e-site deviation: trace named src/auto_capture.cpp, not the plan-anticipated src/sd_storage.cpp); re-verified on the bench — the gallery count GROWS (see verified_by)"
  discriminator_evidence:
    - "OPERATOR-OBSERVED (browser, Step 1): /gallery?page=1 JSON total == dashboard grid count == 6 => persistence branch named"
    - "BASE v2 (base2.log:49, :940, :2912): 'SdStorage: gallery index built — 6 image(s)' — printed at boot AND AGAIN after persisting IMG_00001_T.JPG (:867) and again after IMG_00001.JPG (:2866): the freshly persisted files land on names the card already holds, so the rebuilt index still counts 6"
    - "BASE v3 (base3.log:3492, :5893): 'SdStorage: gallery index built — 6 image(s)' — same pattern after the v3 thumb (:3321) and full (:5759) persists; overwrite-not-append proven twice"
    - "BALLOON v3 (balloon3.log:127): 'CommandHandler: Captured image ID 1' — fresh-boot balloon re-allocates ID 1 (the ID counter is RAM-only and resets on reboot), which is why every bench session's filenames are IMG_00001_*"
    - "BASE v1 (base.log): zero 'ImageRx: ... finalized' lines in the whole session — the earlier 'locked at 6' report's captures never finalized (failing-transfer half); v2/v3 prove the overwrite half persists even with 4/4 COMPLETE finalizes"
  opened_at: 2026-08-23
  resolved_by: "01-11 678d4f1 (branch e): AutoCapture persists the image-ID counter to NVS so a fresh-boot balloon continues the sequence instead of re-allocating ID 1 (fail-open — an NVS failure falls back to the RAM counter); e-site deviation: the fix landed in src/auto_capture.cpp per the trace, not the plan-anticipated src/sd_storage.cpp"
  resolved_at: 2026-08-24
  verified_by: "Operator bench re-verification on fixed firmware (2026-08-24): NVS persistence PROVEN across reboots — balloon2.log:106 'CommandHandler: Captured image ID 2' on the second boot (vs the pre-fix 'ID 1' every boot), :614 'CommandHandler: Captured image ID 3'; balloon3.log:104 'Captured image ID 4', :455 'Captured image ID 5' — IDs 1..5 sequential across three sessions/reboots. GALLERY GROWS for the first time: base2.log:243 'SdStorage: gallery index built — 2 image(s)' advancing to :370 '— 3 image(s)'; base3.log:84 '— 4 image(s)' advancing to :302/:528 '— 5 image(s)', with fresh-boot captures persisting IMG_00002_* .. IMG_00005_* (e.g. base2.log:232 'SdStorage: finalized /images/IMG_00002.JPG (51/51 chunks, 10172 B persisted, complete=true)') — no more overwrite of IMG_00001_*"
  artifacts_final:
    - "src/auto_capture.cpp (678d4f1): NVS-persisted image-ID counter — the G-01-6 fix"

- gap_id: G-01-4
  truth: "Pressing Auto Capture Enable or Trigger Camera Capture submits in-page (AJAX) and the operator stays on the admin page with the result reflected in the queue/status UI"
  status: resolved
  reason: "User reported: When I press the Auto Capture Enable, or the 'Trigger Camera Capture' button, I end up looking at the JSON result string and have to go back to see the admin page."
  severity: major
  test: 4
  root_cause: "The capture/auto-capture/settings forms are native HTML form posts with no client-side submit interception — in-page AJAX submission was never implemented for these buttons (the fetch+preventDefault pattern exists in the footer script only for the Phase 3 alerts-form and wifi-form). Handlers reply with bare application/json via sendResponse, so the browser does a default full-page POST to the raw JSON string. Latent since plan 01-01 (bd50a30), not a regression; not the 1223f46 script-truncation class (footer script demonstrably loads and executes)."
  artifacts:
    - path: "src/main_basestation.cpp"
      issue: "lines 2196-2360: plain form action + type=submit markup for capture, 7 settings, auto-capture enable/stop, event thresholds"
    - path: "src/main_basestation.cpp"
      issue: "lines 640-1839: footer script — submit listeners attach only to alerts-form (1412) and wifi-form (1477); zero fetch references to /capture, /auto-capture, or /set-* routes"
    - path: "src/main_basestation.cpp"
      issue: "lines 3582-3593: sendResponse returns bare JSON with no redirect/HTML — the page the browser lands on"
  missing:
    - "Mirror the in-repo alerts-form/wifi-form pattern (lines 1412-1441, 1477-1509): one delegated submit listener on section#capture covering all 10 control forms (no new ids needed)"
    - "preventDefault, serialize form elements to x-www-form-urlencoded, fetch(form.action, {method:'POST'})"
    - "Render returned {status,message} into a per-card message div"
    - "Call pollOnce() after submit so Command Queue panel, autocapture-chip, and link LED reflect the queued command"
    - "Additions ride the existing PROGMEM-streamed footer script (respect prior truncation-fix constraints)"
  debug_session: .planning/debug/capture-buttons-raw-json-navigation.md
  resolved_by: "01-07 (754f834): one delegated in-page AJAX submit listener covering all 11 section#capture control forms"
  resolved_at: 2026-08-23
  verified_by: "01-09 bench session runtime confirmation — operator triggered captures from the dashboard across the session, command ACKed, no raw-JSON navigation or Back-button behavior reported (WINDOWS ledger entry 1 closed)"

- gap_id: G-01-7
  truth: "A capture triggered while the previous image's transfer/heal is still pending does not prevent that pending transfer's completion — concurrent captures cannot starve or evict an in-flight thumbnail heal or full pull"
  status: open
  reason: "Operator bench finding (01-11 post-fix session 2, 2026-08-24): back-to-back captures left image 2's thumbnail INCOMPLETE 6/8 (heal starved behind image 2's own full pull, then evicted by image 3's window request) and image 3's full INCOMPLETE 15/52 after 3 passes — while session 3's SPACED captures completed 4/4 kinds COMPLETE, isolating concurrency as the discriminator"
  severity: major
  test: 3
  root_cause: "Concurrent-transfer starvation: a new capture's manifest/window traffic supersedes the previous image's pending work. Verbatim: balloon2.log:656 'ImageTx: window request for image 3 supersedes older entry image 2; evicted' — image 2's pending thumbnail heal was evicted when image 3 arrived, after base2.log:296 'ImageRx: image 2 kind 0 finalized INCOMPLETE (thumbnail push stalled; passes exhausted): 6/8 chunks after 3 passes' (the heal had been starved behind image 2's own full pull, base2.log:231 'kind 1 finalized COMPLETE (51/51 chunks, 10172 B)'). Image 3's full then hit bounded tail-chunk turnaround friction: base2.log:415/:431 'window request queued for image 3 kind 1 (chunks 13..14, seq 55/56, pass 1/2)' and :451/:462/:484 'chunks 14..14, seq 57/58/59, pass 1/2/3' — five re-requests for the 13..14/14..14 tail windows; the balloon armed and sent each time yet the chunks never arrived (6 END MARKER MISS in base2.log) — half-duplex immediate-retransmit turnaround-collision class; base2.log:498 'finalized INCOMPLETE (retransmit passes exhausted): 15/52 chunks after 3 passes'. Session 3 (spaced: operator waited for both finalize lines between triggers) completed everything — image 4 thumb 8/8 + full 52/52, image 5 thumb 9/9 + full 53/53 with the 49..52 tail recovered on pass 2 (base3.log:491/:512/:524) — proving the residual is concurrency, not air loss. Named lever (operator-proposed, routed to a future round): serialize transfers — a thumbnail completes fully (push + heal) before its full pull starts, and/or a pending heal entry is never evicted for a new capture; secondary: an inter-window RX-settle gap for the tail-chunk friction (bounded by the 3-pass budget today). NOT a regression from 01-11's commits — removing the phantom-failure noise exposed this pre-existing residual. Interim mitigation (documented operating discipline): space captures until serialization lands. UPDATE (01-12 levers implemented 1064480 + bench session #4, 2026-08-24 ~13:16): all three levers are IN and verifiably functioning, but the series-A acceptance ('3 unspaced captures finalize every kind COMPLETE — zero INCOMPLETE') FAILED, so the gap stays open with a RESCOPED residual mechanism. Lever evidence: the serialization hold fired 3x — base4.log:521 'ImageRx: full-pull activation held - thumbnail heal pending for image 7', :1127 (image 9), :2336 (image 11) — and in every case the thumb heal ran to its D-24 terminal verdict BEFORE the full pull activated (image 7: heal requests seq 20-24 then INCOMPLETE :634, only then full pull seq 25+ :638); ZERO mid-service evictions — all five supersede-evictions this session (balloon4.log:1235 image 6, :2323-2324 images 7+8, :2849 image 9, :4835 image 10) targeted non-mid-service entries and the evict-guard defer form ('supersede of image N deferred - window mid-service') never needed to fire. Residual failure modes, session 4: (a) image 7 thumb INCOMPLETE 35/36 (base4.log:634) — the heal was NOT starved and NOT evicted; its window-request commands hit the one-at-a-time BUSY deferral twice while image 8's thumb window was mid-service — balloon4.log:1154/:1161 'ImageTx: window request for image 7 deferred — image 8 window mid-service' returning FAILED (:1157/:1164, the session's only 2 failed commands) — and the D-24 pass budget exhausted on the deferred+retried requests under elevated air loss (61 END MARKER MISS this session vs 0-6 in every prior session); (b) image 7 full received all 36/36 chunks yet failed the stored-bytes CRC — base4.log:854 'ImageRx: image 7 stored-bytes CRC mismatch (got FCAFC250, manifest 9DE9EFA0)' (see G-01-9 defect B); (c) image 8's full silently never transferred — the balloon sent the FULL manifest (balloon4.log:722 'ImageTx: FULL manifest(image 8, 7160 B, 36 chunks) sent') but the base never received it (10 of 11 manifests arrived; no 'manifest image 8 kind 1' line) and no re-announce path exists (see G-01-9 defect C). Compounding factor: images 7/8 thumbs were FULL-SIZED (7157/7160 B, 36 chunks — G-01-9 defect A), multiplying the heal exposure that the deferral+pass interaction then exhausted. Baseline comparison: chunk-level outcomes improved (thumb 35/36 vs 6/8; full 36/36-received vs 15/52) but verdict-level the truth still fails. PRI-01/beacon side held: balloon transmitted all 253 [BCN] seqs consecutively (no TX gap), base accepted 238 (15 lost on air), beacons interleave with window service (balloon4.log:1145-1178). Named next levers: defer-aware pass accounting (a BUSY-deferred window request must not consume a D-24 pass), full-manifest re-announce (bounded), and the G-01-9 thumb-sizing fix (removes the 4-18x heal-exposure multiplier). UPDATE (01-13/01-14/01-15 remediation + bench session #5, 2026-08-25, balloon5.log/base5.log): ALL THREE prior failure modes are ELIMINATED at bench — (1) zero 'finalized INCOMPLETE' verdicts in either log (base5.log: 0 matches; session 4 had thumb 35/36 + two CRC-mismatch INCOMPLETEs); (2) zero 'stored-bytes CRC mismatch' lines (0 matches vs session-4 :854/:3267); (3) the BUSY-deferral/pass-burn interaction never arose — zero 'deferred — image N window mid-service' or 'deferred (balloon window BUSY)' lines (balloon5.log), the defer-aware D-24 accounting ran clean ('stall deferred' 16x + 'heal deferred' 1x, base5.log:132/:283 et al., every affected transfer still finalized COMPLETE). Serialization held 4x with heals completing first: base5.log:66 'ImageRx: full-pull activation held - thumbnail heal pending for image 14' (image 14 thumb COMPLETE :108 before full pull 21/21 COMPLETE :263), :377 (image 16), :2382 (image 19), :3356 (image 21 — thumb COMPLETE :3378 before full 21/21 :3518). Zero mid-service evictions (all 5 supersede-evictions target idle entries — balloon5.log:813-814 images 14+15, :1062 image 16, :1560 image 17, :6440 image 19 — plus one 15-min TTL eviction :4258). Thumbs correctly sized all session (the heal-exposure multiplier is gone: 6-7 chunks vs 36-145). Beacons held cadence: 484 accepted at the base, worst consecutive loss 2 seqs (base5.log:49-50, gap 5->8 during the series-A chunk storm; all other gaps 1 seq), interleave with window service throughout — vs session 4's 15 lost of 253. BUT the series-A acceptance FAILED on two NEW, narrower residual axes: (a) the THIRD unspaced CAPTURE_NOW (seq=6, queued base5.log:55) never reached the balloon — all 4 transmissions (original + retries 1/3 at :73, 2/3 at :81, 3/3 at :92) were lost under the concurrent image-14/15 chunk flow, and the base terminalized honestly: base5.log:126 'CommandSender: Command seq=6 timeout after 3 retries'. The first two triggers made it (seq=4 ACKED :35; seq=5 retry-2/3 ACKED :70 -> images 14/15), so 3 unspaced triggers produced 2 captures; the operator's later re-trigger (seq=14, :350) captured image 16 SPACED. Verdicts for the images that ran: 5/6 COMPLETE (img14 k0 :108, img14 k1 :263, img15 k0 :310, img16 k0 :401, img16 k1 :504) + 1 silently absent (below). (b) image 15's FULL was SILENTLY LOST: the balloon sent its manifest exactly once with TX-success accounting — balloon5.log:274 'ImageTx: FULL manifest(image 15, 4853 B, 25 chunks) sent' — the frame was air-lost (the base never logged 'manifest image 15 kind 1'; zero lines name image 15 kind 1 except the unrelated kind-0 slot recycle :2380), and because TX succeeded the 01-14 one-shot announce state was consumed with no re-announce and no drop log — the G-01-9 defect-C shape recurring through the AIR-LOSS path the TX-verdict-gated re-announce does not cover. RESCOPED residual: command-frame survivability under concurrent image flow (a 16-byte command frame lost 4/4 while 217-byte chunk frames flowed — half-duplex turnaround/priority class) + receipt-driven manifest recovery. Named next levers: command air-priority or command-burst pacing during window service; base-side manifest nudge (a thumb COMPLETE with no FULL manifest within a bound solicits re-announce) or balloon-side periodic bounded re-announce while an announced full is head-and-idle, independent of TX verdict. UPDATE (01-20 bench session #6, 2026-08-27/28, base6.log/balloon11.log, round-#8 firmware + GPS pin-41 fix 3f2c2c6 on the balloon — log provenance: the balloon console cycled balloon6..balloon11 through the day's bootloop debug saga, session evidence is balloon11.log, balloon6-10 are bootloop/debug captures, and base6.log lines 1-~1618 are the bootloop period whose 170 GET_STATUS timeouts are pre-session noise): BOTH session-5 residual axes are CLOSED at bench — (a) COMMAND SURVIVABILITY SOLVED: 3 unspaced CAPTURE_NOW (base6.log:3909/:3922/:3933, seq=182/183/184) all reached the balloon, all ACKED (:3915/:3955/:3958), zero series-A timeouts; the 01-18 quiet gate held retried transmits under active chunk flow and every held command transmitted and reached a terminal state (:3945 'command seq=184 transmit held - inbound chunk stream active', :3946 seq=183, then ACKED :3955/:3958; five more held->ACKED lifecycles late-session :17952->:17956, :17978->:18013, :18059->:18090, :18120->:18125, :18138->:18142) — the session-5 seq=6 4/4-transmission-loss class is dead. (b) RECEIPT-DRIVEN MANIFEST RECOVERY EXISTS AND DELIVERS: the 01-17 re-announce ran live all session — image 31 re-announced once (balloon11.log:25435 'FULL manifest(image 31, 16362 B, 82 chunks) re-announced (1/3)'), FULL window armed (:25438), full completed 82/82 COMPLETE (base6.log:13096); images 25/26 manifests received 3x/4x at the base (base6.log:4015/:4044/:4074 and :4005/:4035/:4065/:4090). The series-A truth now fails on ONE narrower mechanism — BURST FULL-DELIVERY: all three series-A fulls lost, every loss NAMED at both consoles (never silent — the G-01-9 defect-C class is dead): image 24's manifest was RECEIVED at base (base6.log:3947; full-pull held behind the thumb heal :3948) but the balloon's depth-3 TX queue evicted the entry at image-26 enqueue (balloon11.log:406 'ImageTx: queue overflow (depth 3); evicting class 3 (queued, no airtime invested) entry image 24 for image 26') — 'no airtime invested' ranks a full whose manifest the base demonstrably received and was requesting windows for — so all 4 passes of window requests were rejected ('window request for unknown/evicted image 24 rejected' balloon11.log:495/:501/:530/:559/:585/:605/:636/:656; base INCOMPLETE 0/26 base6.log:4170); images 25/26 dropped at the 3-re-announce bound (balloon11.log:553 'image 25 full dropped - no FULL window armed after 3 re-announces'; :575 image 26) WHILE the base was still serializing the burst's thumbnails — manifests in hand, every window request post-drop and rejected ('unknown/evicted image 25' :680/:702/:729/:751; 'image 26' :771/:838/:845/:868; base INCOMPLETE 0/22 :4231, 0/14 :4306). Session verdicts 19 COMPLETE / 5 INCOMPLETE overall; the late session (images 30-34) went 10/10 kinds COMPLETE including 133-chunk fulls (base6.log:8602/:13096/:17096/:18134), zero stored-bytes CRC mismatches, 4213 beacons accepted batt=valid. NAMED NEXT LEVERS (the discriminator lines point at SCHEDULING, not loss): (1) pause the re-announce drop-clock on RECEIPT EVIDENCE — an incoming window REQUEST for the entry proves the manifest arrived (images 25/26: the bound raced the base's serialized full-pull schedule and expired first); (2) rank the depth-3 eviction class by receipt evidence rather than 'no airtime invested' (image 24 was evicted while the base held its manifest and was actively requesting windows); (3) interim mitigation unchanged: space captures. Compounding context, honestly recorded: the early stretch was lossy (41 END MARKER MISS; image 29 full 222/238 INCOMPLETE base6.log:6337) while the late stretch was clean — the split is in the logs; its physical cause (antenna/bench) is NOT established by them. UPDATE (01-24 bench session #7, 2026-08-28, balloon.log/base.log): UNJUDGEABLE — the session aborted on the D1 crash regression (G-01-10) before any real burst could run. The balloon hard-crashed twice during series A (crash 1: balloon.log:301-302 I2C INVALID_STATE + INT_WDT CPU1 panic immediately after image 35's manifest :300; crash 2: :526-528 TG0WDT_SYS_RST after image 36's first chunk :519-520 and beacon start :525), series-A verdicts 0/6 (both images' buffers lost in the resets), the two crash-window CAPTURE_NOWs hit the forbidden 'timeout after 3 retries' terminal (base.log:141/:143), and NONE of round #10's positive discriminators fired (zero budget re-arms, zero receipt-evidenced class-5 evictions, zero base full-arm deadline releases — there was never a burst to discriminate). The 01-21/01-22 levers remain UNVERIFIED at bench, not disproven; the D2 boot-time spurious hold (G-01-11) additionally fires the 're-announce held' discriminator spuriously at every boot (balloon.log:135/:504/:652), polluting exactly the line this round was to watch. Next step renamed by evidence: the D1 debug round precedes any series-A re-run — root cause not established (suspects: 01-21 pushPending busy-hold/budget-re-arm, 01-23 framer resync/command-guard, or latent instability under the new TX cadence; addr2line + ELF SHA256 3d2b351b4); interim mitigation unchanged (space captures), now secondary to 'avoid unspaced capture bursts until D1 is fixed'. UPDATE (01-27 bench session #8, 2026-08-28, balloon2.log/base2.log): UNJUDGEABLE AGAIN — the series never ran (the session's two CAPTURE_NOWs were minutes apart, i.e. spaced; the session aborted at image 38's FULL window service on the D1 RECURRENCE — see G-01-10's session-8 extension — before any unspaced burst). Zero round-#10 discriminators engaged (no 're-announce budget re-armed', no receipt-evidenced class-5 eviction labels in balloon2.log; no base 'full-pull activation deadline reached'). For the record, NOT a burst-delivery failure: image 38's kind-1 INCOMPLETE 109/137 after 3 passes (base2.log:651) is CRASH-CAUSED — the balloon died at the first chunk of re-armed window 96..109 (balloon2.log:1227-1231) and every post-reboot window request was rejected (:1385/:1398); image 37 (spaced, pre-crash) went both-kinds COMPLETE (base2.log:74-75/:238-239), and image 38's thumb COMPLETE 8/8 (:290-291). Zero command timeouts in base2.log. The 01-21/01-22 levers remain UNVERIFIED at bench, not disproven; a D1 debug round #2 precedes any series-A re-run. UPDATE (01-29 bench session #9, 2026-08-28, balloon3.log/base3.log): UNJUDGEABLE A THIRD TIME — the series never ran (the session's single CAPTURE_NOW is the spaced smoke itself; the session aborted at protocol step 3 on the D1 RECURRENCE's third expression — the inter-window-lull TG0WDT, see G-01-10's session-9 extension — before any unspaced burst). Zero round-#10 discriminators engaged (no 're-announce budget re-armed', no receipt-evidenced class-5 eviction labels in balloon3.log; no base 'full-pull activation deadline reached' in base3.log). For the record, NOT a burst-delivery failure: image 39's kind-1 INCOMPLETE 15/36 after 3 passes (base3.log:194) is CRASH-CAUSED — window 1 served 16/16 chunks clean (balloon3.log:427-:491), chunk 7 lost on air (ordinary RF loss), and the retransmit requests (chunks 7..7, seq 5/6/7) hit the already-crashed balloon whose honest post-reboot rejections (balloon3.log:679/:692/:713) drew the NACK_INVALID ×3 (base3.log:162/:171/:181); image 39's thumb COMPLETE 9/9 (base3.log:79). Zero command timeouts in base3.log. The 01-21/01-22 levers remain UNVERIFIED at bench, not disproven; a D1 debug round #3 precedes any series-A re-run"
  artifacts:
    - path: "src/image_tx_manager.cpp"
      issue: "new-capture supersede/eviction rule (the 'window request for image N supersedes older entry' path) — the pending-heal eviction is the primary lever. 01-12: evict-guard landed (1064480); sessions 4+5 show zero mid-service evictions"
    - path: "src/image_rx_manager.cpp"
      issue: "heal gating behind the active full pull (gates 1/2, :190-216) + immediate window re-request cadence — the serialization / RX-settle levers. 01-12: hold + settle landed (1064480), hold verified 3x (session 4) + 4x (session 5) on the bench. RESIDUAL (session 5): command-frame survivability under concurrent chunk flow and receipt-driven manifest recovery — the next levers"
  missing:
    - "CLOSED by 01-15 (7dd5dab defer-aware D-24 pass accounting) — bench-verified session 5: zero INCOMPLETE verdicts, and the 'stall deferred'/'heal deferred' paths fired benignly (base5.log:132/:283) with every affected transfer still COMPLETE"
    - "CLOSED by 01-14 (bb2c517 success-gated bounded manifest re-announce) for the TX-FAILURE class — sessions 5 AND 6 exercised zero TX failures (that path remains unexercised-but-bounded); the AIR-LOSS class is closed by 01-17 below"
    - "CLOSED by 01-17 (f0ac5ac receipt-informed re-announce) — bench-verified session 6: the re-announce ran live (image 31 re-announced once, balloon11.log:25435, then FULL window armed :25438 and the full completed 82/82 COMPLETE, base6.log:13096) and air-lost manifests were re-delivered repeatedly (images 25/26 manifests received 3x/4x at base, base6.log:4015/:4044/:4074 and :4005/:4035/:4065/:4090); every un-recovered full is drop-logged BY NAME (balloon11.log:553/:575) — the silent-loss class is dead, and the defect-C truth is closed under G-01-9"
    - "CLOSED by 01-18 (channel-quiet transmit gate) — bench-verified session 6: command survivability under unspaced load SOLVED; 3/3 series-A CAPTURE_NOW ACKed with zero timeouts (base6.log:3915/:3955/:3958), held transmits released to ACKED 7x total (:3945/:3946 -> :3955/:3958; :17952->:17956, :17978->:18013, :18059->:18090, :18120->:18125, :18138->:18142)"
    - "BURST FULL-DELIVERY residual (session 6, the one remaining series-A failure axis, named by the new discriminators as a SCHEDULING race, not loss): (a) depth-3 queue overflow evicts an announced full whose manifest the base already received and is requesting windows for ('evicting class 3 (queued, no airtime invested) entry image 24' balloon11.log:406 vs manifest receipt base6.log:3947 and 4 passes of rejected window requests) — lever: rank the eviction class by receipt evidence (incoming window requests prove receipt); (b) the 3-re-announce drop bound expires while the base is still serializing the burst's thumbnails (images 25/26: manifests in hand, all window requests post-drop rejects, balloon11.log:553/:575 vs base6.log:4175/:4191/:4206/:4221) — lever: pause the drop-clock on receipt evidence (a window REQUEST for the entry proves the manifest arrived) or raise the bound under known-busy; interim mitigation unchanged: space captures"
    - "CLOSED by 01-13 (CR-03 drain + payload bound) for the thumb-sizing multiplier — bench-verified session 5: all thumbs genuine QQVGA (1176-1340 B / 6-7 chunks), drained-stale-frame discriminator at every logged capture"
    - "SC-3 visible-effect pairs STILL unjudged after session #6 (fifth round riding) — zero brightness/contrast/saturation/quality commands issued in sessions 5 AND 6 (session-6 queue mix: 13 CAPTURE_NOW, 849 GET_STATUS, 103 IMAGE_WINDOW_REQUEST, 2 SET_RESOLUTION); WR-03 cadence discriminator also still unrun (zero AUTO_CAPTURE_ENABLE both sessions); CIF-cycle and QVGA-restore clauses unexercised in session 6 — all ride the next bench moment"
    - "Series-A re-run RAN at session #6 on the remediation firmware: zero command timeouts and zero silent full losses ACHIEVED (3/3 ACKs, 3/3 captures, every loss named) but verdicts 2/6 COMPLETE — the acceptance now fails only on the burst full-delivery residual above"
    - "Series-A re-run ATTEMPTED at session #7 (2026-08-28, balloon.log/base.log) on round-#10 firmware and ABORTED — the D1 crash regression (G-01-10) hard-reset the balloon twice during the series (0/6 verdicts, forbidden command timeouts base.log:141/:143, zero round-#10 discriminators engaged); the burst full-delivery truth remains unjudged and the levers unverified — a D1 debug round must precede the re-run"
  opened_at: 2026-08-24

- gap_id: G-01-8
  truth: "SET_RESOLUTION to any advertised framesize either works or NACKs honestly — a size larger than the boot-allocated frame buffer must never leave the camera in a state that fails all subsequent captures"
  status: resolved
  reason: "Operator bench finding (01-11 post-fix session 1, 2026-08-24): after SET_RESOLUTION value 9 (VGA 640x480) returned SUCCESS (balloon.log:582 'CommandHandler: Command SET_RESOLUTION - SUCCESS'), the balloon console flooded 4,379 'cam_hal: FB-OVF' lines (from :588 onward) and all subsequent CAPTURE_NOW commands FAILED (4x 'CommandHandler: Command CAPTURE_NOW - FAILED') until reboot restored the boot resolution"
  severity: major
  test: 3
  root_cause: "Camera PSRAM frame buffers (fb_count=2, allocated at init for PSRAM) are sized at esp_camera_init for the boot framesize (QVGA); CameraManager::setFrameSize (src/camera_manager.cpp:398-412) changes only the sensor framesize (s->set_framesize) and never reallocates — a larger framesize overflows the fixed buffers (FB-OVF) and every fb capture fails until reboot. Latent since 01-04; NOT a regression from the 01-11 commits (which touched e32_lora.cpp, auto_capture.cpp, base config/UI, camera_manager's thumbnail guard only); exposed by this round's deferred settings visible-effect series. Fix class: NACK framesizes above the allocated buffer, or re-init the camera on a size change. Values 10-13 (SVGA..UXGA, include/command_protocol.h:72-76) are presumably worse; untested. RESOLVED by the 01-12 re-init-with-recovery bound (f51bad6): setFrameSize re-initializes the camera (end + delay(100) + begin, refreshing the PSRAM fb allocation) for any size above allocatedFrameSize, with mandatory recovery re-init to the previous size before an honest NACK — the verdict is now truthful because the buffers are truly resized"
  artifacts:
    - path: "src/camera_manager.cpp"
      issue: "setFrameSize (:398-412) — sensor-only framesize change without buffer reallocation; the fix site. FIXED by f51bad6: allocatedFrameSize tracking + two-path setFrameSize (sensor-only <= allocation; re-init-with-recovery above)"
    - path: "include/command_protocol.h"
      issue: "FrameSize wire codes (:65-77; FRAMESIZE_VGA = 9 at :72) — the NACK bound derives from the boot-allocated buffer size"
  missing:
    - "CLOSED by 01-12 (f51bad6): bound SET_RESOLUTION to the allocated buffer via camera re-init with mandatory recovery — bench-verified session 4 (see verified_by)"
    - "Settings visible-effect + CIF spot-checks: PARTIALLY exercised — CIF executed at 400x296-class size (image 9 full 11223 B / 57 chunks, 1.57x the QVGA 7157 B / 36 chunks baseline; full COMPLETE 57/57 CRC-verified) but the pairwise SC-3 visible-effect judgments (brightness -2 vs +2 etc.) were NOT run this session — clause stays open under Test 3"
    - "QVGA restore after a larger size + reboot boot-resolution check: NOT exercised this session (no 4th SET_RESOLUTION, single boot at balloon4.log:12) — clause stays open under Test 3 for the next bench round"
    - "Values 11-13 (XGA..UXGA) untested; the re-init mechanism class is proven for 8/9/10 (all three succeeded)"
  opened_at: 2026-08-24
  resolved_by: "01-12 f51bad6: allocatedFrameSize capacity bound written only inside initCamera + two-path setFrameSize — sensor-only for sizes within the allocation (thumbnail QQVGA pair always lands here), camera re-init via the existing reinitialize() pattern for growth, mandatory recovery re-init to the saved previous size before returning false (handleSetResolution then NACK_BUSYs honestly); initCamera re-applies cached saturation/WB/exposure so a re-init never silently resets operator settings"
  resolved_at: 2026-08-24
  verified_by: "Operator bench session #4 (2026-08-24 ~13:16, balloon4.log / base4.log), settings series CIF -> VGA -> SVGA. CIF (wire 8): balloon4.log:1791 'Camera: framesize growth requires re-init (6 -> 8)' + :1792 'CommandHandler: Set resolution to 8' + :1795 'Command SET_RESOLUTION - SUCCESS'; capture image 9 full 11223 B / 57 chunks vs the QVGA session baseline 7157 B / 36 chunks (1.57x — CIF 400x296 class); full COMPLETE 57/57 (base4.log:1464). VGA (wire 9 — the pre-fix brick value): balloon4.log:2784 're-init (8 -> 10)' + :2785 'Set resolution to 9' + :2788 SUCCESS; capture image 10 full COMPLETE 97/97 (base4.log:1938) with ZERO 'cam_hal: FB-OVF' lines at the step (baseline 4,379 at balloon.log:588 onward) and a correctly-sized QQVGA thumb 1343 B / 7 chunks COMPLETE (base4.log:1581) — operator: 'thumb was very small file size but came through completely. The full size VGA came through perfectly'. SVGA (wire 10, beyond-plan stretch): balloon4.log:3588 're-init (10 -> 11)' + :3592 SUCCESS; capture succeeded (full 28771 B / 144 chunks all received — its stored-bytes CRC mismatch is the G-01-9 transfer complex, not a camera fault). All six captures in the session returned 'CAPTURE_NOW - SUCCESS' (IDs 6-11); the camera never died and no NACK path was needed. NOTE: the growth-log numbers are the compiled framesize_t enum (framework-bundled sensor.h: QVGA=6, CIF=8, VGA=10, SVGA=11 — balloon4.log:73 boot dump 'Frame size: 6' for the QVGA config); the by-NAME wire translation (01-06) handled the enum difference correctly at every step, as designed. Unexercised clauses recorded in missing (QVGA restore, reboot boot-resolution, SC-3 pairs — they ride the next bench round)"

- gap_id: G-01-9
  truth: "Every capture's THUMBNAIL is a genuine QQVGA-sized image, every finalized-COMPLETE full's stored bytes match its manifest CRC by construction (no balloon-source-side corruption), and no full is silently dropped for a lost FULL manifest"
  status: resolved
  reason: "Operator bench report (01-12 session #4, 2026-08-24): 'The first 3 captures all look corrupted (the thumbs have visible errors and so does the detail view)', CIF 'thumb has corruption but the detail looks good', and the SVGA thumb anomaly 'the thumb size went from 7 chunks (VGA) to 145 chunks (SVGA), not sure why. The full SVGA pic is 144 chunks (less than the thumb).' Plus UI observation: 'the incomplete overlay is showing over pictures that are actually complete'"
  severity: major
  test: 3
  root_cause: "THREE log-proven defects, one complex. DEFECT A (full-sized thumbnails — the visible corruption): CameraManager::createThumbnail reports 'Camera: Thumbnail created, size: 7157 bytes' (balloon4.log:355) with 'enqueued image 7 (full 7157 B, thumb 7157 B / 36 chunks)' (:356) — the thumb payload is FULL-RESOLUTION while passing the 01-11 R2 QQVGA guard (camera_manager.cpp:361 checks ONLY fb->width/height == 160/120 — metadata, not payload; zero 'settings did not take' guard bails in the whole log). 4 of 6 captures affected (7: 7157==7157, 8: 7160==7160, 9: 11223==11223, 11: 28808 vs full 28771); correct thumbs only for images 6 (1411 B, first post-boot) and 10 (1343 B, VGA step). The two QVGA cases each coincide with 2x 'cam_hal: FB-OVF' DURING the thumbnail capture (balloon4.log:353-354, :396-397 — between 'Captured image ID' and 'Thumbnail created'); the CIF/SVGA cases show the payload swap with no OVF. Named candidate mechanism: esp32-camera (fb_count 2, CAMERA_GRAB_LATEST) hands back a stale/in-flight PRE-dowNSHIFT frame after the set_framesize(QQVGA) register write — content full-res, metadata stamped from the current config — a payload-vs-metadata mismatch the dimension guard cannot catch; race-shaped (intermittent, correlates with capture timing after sensor state changes). Consequences: 4-18x push/heal exposure (36-145 chunks vs 7-9) driving INCOMPLETE thumbs (7: 35/36; 9: 56/57) and corrupt-looking galleries. DEFECT B (all-chunks-received stored-bytes CRC mismatch): image 7 full and image 11 full received every chunk yet failed the read-back CRC — base4.log:854 'image 7 stored-bytes CRC mismatch (got FCAFC250, manifest 9DE9EFA0)' -> :855 INCOMPLETE (36/36, 7157 B); :3267 'image 11 stored-bytes CRC mismatch (got CC9DADC2, manifest D043406D)' -> :3268 INCOMPLETE (144/144, 28771 B). 0 'CRC FAIL' frames all session and all 61 END MARKER MISS caught by the framer -> NOT undetected air corruption (a CRC16 collision twice in one session is implausible); the divergence is balloon-SOURCE-side between the manifest-time CRC and the bytes served. Both affected fulls are images whose thumb size equalled the full (the defect-A common factor). Named candidates: (a) entry buffer content mutated after announce (heap/buffer-lifetime class), (b) wrong-buffer slicing under same-entry kind-0/kind-1 window re-arm races — the 16 post-terminal 'chunk for finalized image 11 kind 0 ignored' stragglers (base4.log:3286-3310) PROVE late kind-0 re-arms occurred during/after the kind-1 pull (driven by CommandSender retry overruns, see below). The D-48 Incomplete badge fired correctly in every case — the operator's 'incomplete overlay over complete pictures' observation is this honest flagging of byte-corrupt-but-renderable JPEGs (both render because the wrong/corrupt bytes are a small fraction). DEFECT C (lost FULL manifest = silent full drop): balloon4.log:722 'ImageTx: FULL manifest(image 8, 7160 B, 36 chunks) sent' — never received (10 of 11 manifests arrived); no re-announce path exists, so image 8's full never transferred and nothing surfaces the loss. RIDE-ALONG FINDING (contributor, separately routed as a minor defect): CommandSender retry overrun — 13x 'Retrying command seq=N (attempt 4/3)' (base4.log:115, :290, :372, :2330 et al.) — one retry beyond the documented 3-attempt bound (D-05/D-07); the late retries are what re-armed image 11's kind-0 windows post-finalize. UPDATE (01-13/01-14/01-15 remediation + bench session #5, 2026-08-25, balloon5.log/base5.log): DEFECT A RESOLVED at bench — every logged enqueue is a genuine QQVGA thumb, no byte-equality line all session: balloon5.log:198 'enqueued image 14 (full 4094 B, thumb 1231 B / 7 chunks, source 0)', :242 (4853/1340), :699 (4853/1329), :1029 (6112/1332 — the CIF capture), :1525 (15921/1283 — SVGA), :5014 (16354/1176 — SVGA), :6399 (4090/1304 — post-QVGA-restore); the CR-03 drained-stale-frame discriminator fired at EVERY logged capture — :196/:240/:697/:1027/:1523/:5012/:6397 'Camera: drained stale frame after QQVGA downshift (160x120, N B)' — and the post-reboot capture (image 21, balloon console detached) landed a correct 1319 B / 7 chunks at the base (base5.log:3338). Contrast with session 4's 4-of-6 impostors (7157-28808 B / 36-145 chunks): eliminated. DEFECT B RESOLVED at bench — zero 'stored-bytes CRC mismatch' lines across the whole session INCLUDING the larger sizes the truth demands: CIF-class image 17 full COMPLETE 31/31 (base5.log:776-777, complete=true), SVGA-class image 18 COMPLETE 80/80 (:1243-1244) and image 19 COMPLETE 82/82 (:2770-2771); the 01-13 kind-stamped routing is live on the wire — balloon5.log:289 'ImageTx: window chunk(image 14 kind 0, 1/5, 200 B) sent' — with zero kind misroutes (no misroute can hide: every finalize's read-back CRC passed). DEFECT C REMAINS OPEN, now precisely bounded: the 01-14 re-announce (bb2c517) covers only the TX-FAILURE class (a manifest whose transmission the balloon books as failed retries up to 3x then drops with a named log) — session 5 produced ZERO TX failures, so that path was never exercised, and the AIR-LOSS class silently dropped a full exactly as in session 4: image 15's FULL manifest left the balloon with TX-success accounting (balloon5.log:274 'ImageTx: FULL manifest(image 15, 4853 B, 25 chunks) sent'), never arrived (zero 'manifest image 15 kind 1' lines in base5.log), consumed the one-shot announce state, and nothing at either console names the loss — the operator's series-A report 'some of the pictures were missed' is this image. Next lever (named): receipt-driven manifest recovery — base-side nudge when a completed thumb has no FULL manifest within a bound (the D-24 24-s idle-fallback class), or balloon-side periodic bounded re-announce while an announced full is head-and-idle, independent of TX verdict. FINAL UPDATE (01-20 bench session #6, 2026-08-27/28, base6.log/balloon11.log): DEFECT C RESOLVED at bench — the 01-17 receipt-informed re-announce is live and the SILENT-loss class is dead across a 12-image session (images 23-34, 19 COMPLETE / 5 INCOMPLETE verdicts, every INCOMPLETE named at a console, zero silently-absent fulls): (1) the RECOVERY path ran live — image 31's FULL manifest re-announced once ('ImageTx: FULL manifest(image 31, 16362 B, 82 chunks) re-announced (1/3) - no window armed since announce', balloon11.log:25435), the FULL window armed (:25438), and the full completed 82/82 COMPLETE (base6.log:13096); (2) the AIR-LOSS re-delivery path ran live — images 25/26 manifests received at the base 3x/4x (base6.log:4015/:4044/:4074, :4005/:4035/:4065/:4090) after their original announces failed to arm; (3) the BOUND path names every unrecoverable loss by name — 'ImageTx: image 25 full dropped - no FULL window armed after 3 re-announces' (balloon11.log:553) and ':575 image 26', with the base-side honest verdicts INCOMPLETE 0/22 / 0/14 (base6.log:4231/:4306); (4) the fourth loss class this session (queue-overflow eviction, image 24) is equally named on both consoles ('queue overflow (depth 3); evicting class 3 (queued, no airtime invested) entry image 24 for image 26' balloon11.log:406 with the base holding its RECEIVED manifest base6.log:3947; 8x 'window request for unknown/evicted image 24 rejected' balloon11.log:495/:501/:530/:559/:585/:605/:636/:656; base INCOMPLETE 0/26 :4170). The image-15 class (TX-success consumed the one-shot announce, nothing at either console names the loss) cannot recur: the announce state is no longer one-shot-consumed on TX success and the drop is logged at the bound. Defects A and B stayed resolved all session (9/9 logged thumbs genuine QQVGA 1073-1879 B / 6-10 chunks with the drained-stale-frame discriminator at every capture; zero stored-bytes CRC mismatches and zero CRC FAIL frames). The remaining series-A verdict failure (fulls lost under unspaced bursts before the base can pull them) is G-01-7's rescoped residual — a delivery-scheduling race, not a silent-loss defect"
  artifacts:
    - path: "src/camera_manager.cpp"
      issue: "createThumbnail (:305-404) — the R2 guard (:361) checks fb dimensions only; a payload-plausibility check (fb->len vs a QQVGA bound, or JPEG SOF parse) or a drain-and-recapture after the downshift is the named lever"
    - path: "src/image_tx_manager.cpp"
      issue: "serviceWindowChunk kind-selected slicing + window re-arm (:714-731) — needs kind-tagged chunk logging (or per-window served-CRC) to discriminate defect-B candidates (a) vs (b)"
    - path: "src/image_tx_manager.cpp"
      issue: "FULL manifest announce path — no re-announce; a bounded re-announce while the entry is head-and-idle is the named lever for defect C"
    - path: "src/command_sender.cpp"
      issue: "retry bound overrun ('attempt 4/3') — the attempt counter exceeds MAX_RETRIES before the terminal guard stops it"
  missing:
    - "CLOSED by 01-13 (CR-03 drain-after-downshift + THUMB_MAX_BYTES payload bound) — bench-verified session 5: defect A eliminated (all thumbs genuine QQVGA 1176-1340 B / 6-7 chunks; drained-stale-frame discriminator at every logged capture, balloon5.log:196/:240/:697/:1027/:1523/:5012/:6397)"
    - "CLOSED by 01-13 (kind-tagged chunk wire + kind-exact routing) — bench-verified session 5: defect B eliminated (zero stored-bytes CRC mismatches including CIF 31-chunk base5.log:776 and SVGA 80/82-chunk :1243/:2770 fulls; kind-stamped 'window chunk(image N kind K' lines live, balloon5.log:289)"
    - "CLOSED by 01-17 (f0ac5ac) — bench-verified session 6: receipt-informed re-announce live (image 31 recovered to COMPLETE 82/82: balloon11.log:25435 re-announce -> :25438 window armed -> base6.log:13096; images 25/26 manifests re-delivered 3x/4x base6.log:4015/:4044/:4074 and :4005/:4035/:4065/:4090) and every unrecovered full named by the bound ('image 25 full dropped - no FULL window armed after 3 re-announces' balloon11.log:553, ':575 image 26') — nothing silently absent all session"
    - "CLOSED by 01-15 (retry-ordinal label) — bench-verified session 5: zero beyond-bound labels, all 52 retry lines read '(retry K/3)' with K<=3, the one exhausted command terminalized honestly at exactly 3 (base5.log:126); WINDOWS entry 6 flipped fixed"
    - "CLOSED at bench session 6 (2026-08-27/28): series-A re-run on the remediation firmware achieved ZERO command timeouts (3/3 ACKs, base6.log:3915/:3955/:3958) and ZERO silent full losses (every loss named on both consoles); verdicts 2/6 COMPLETE — the residual short-verdict mechanism is G-01-7's rescoped burst full-delivery race, not a defect-C silent loss"
  opened_at: 2026-08-24
  resolved_by: "01-13 (CR-03 drain-after-downshift + THUMB_MAX_BYTES payload bound + kind-tagged chunk wire with kind-exact routing) for defects A and B, bench-held sessions 5 and 6; 01-17 (f0ac5ac receipt-informed bounded FULL-manifest re-announce, keyed on fullWindowEverArmed with a named drop at the bound) for defect C, bench-proven session 6"
  resolved_at: 2026-08-28
  verified_by: "Operator bench session #6 (2026-08-27 evening -> 2026-08-28 ~00:20 local, base6.log + balloon11.log, 12 images 23-34, 19 COMPLETE / 5 INCOMPLETE verdicts, zero silently-absent fulls). Defects A/B held: all 9 logged thumbs genuine QQVGA (1073-1879 B / 6-10 chunks) with the drained-stale-frame discriminator at every capture; zero stored-bytes CRC mismatches and zero CRC FAIL frames all session. Defect C closed: re-announce recovery live ('ImageTx: FULL manifest(image 31, 16362 B, 82 chunks) re-announced (1/3) - no window armed since announce' balloon11.log:25435 -> FULL window armed :25438 -> full COMPLETE 82/82 base6.log:13096); air-loss re-delivery live (images 25/26 manifest receipts 3x/4x, base6.log:4015/:4044/:4074 and :4005/:4035/:4065/:4090); the bound names every unrecovered drop ('image 25 full dropped - no FULL window armed after 3 re-announces' balloon11.log:553, ':575 image 26'; base honest verdicts INCOMPLETE 0/22 / 0/14 base6.log:4231/:4306); the session's fourth loss class (queue-overflow eviction, image 24) equally named ('queue overflow (depth 3); evicting class 3 (queued, no airtime invested) entry image 24 for image 26' balloon11.log:406, base holding the RECEIVED manifest base6.log:3947, 8x reject lines balloon11.log:495/:501/:530/:559/:585/:605/:636/:656, base INCOMPLETE 0/26 :4170). Series-A command path: zero command timeouts across the session (4 GET_STATUS timeouts are link-idle, not command failures)"
  artifacts_final:
    - "src/camera_manager.cpp (01-13): drained-stale-frame after QQVGA downshift + THUMB_MAX_BYTES payload bound — defect A fix, bench-held sessions 5 and 6"
    - "src/command_protocol.cpp + src/image_tx_manager.cpp + src/image_rx_manager.cpp (01-13): kind-tagged chunk wire + kind-exact routing — defect B fix, bench-held sessions 5 and 6"
    - "src/image_tx_manager.cpp (01-17, f0ac5ac): receipt-informed bounded FULL-manifest re-announce + named drop at the bound — defect C fix, bench-proven session 6"

- gap_id: G-01-10
  truth: "The balloon survives the start of post-capture radio push — a capture's enqueue/manifest/chunk/beacon sequence never hard-resets the firmware (no panic, no watchdog reset), so image buffers and queue state survive to serve the image"
  status: open
  reason: "Operator bench report (01-24 session #7, 2026-08-28): the balloon hard-crashed twice during series A and the session aborted — 0/6 series-A verdicts, both captured images' buffers lost in the resets, the two crash-window CAPTURE_NOWs terminalized at the forbidden 'timeout after 3 retries'"
  severity: blocker
  test: 3
  root_cause: "NOT ESTABLISHED — routed for a debug round. Both hard resets land within seconds of the START of post-capture radio push. Crash 1: image 35 ACKed/enqueued/manifest-sent clean (balloon.log:289-300: 'Captured image ID 35', enqueued thumb 1188 B / 6 chunks, manifest sent), then ':301 [607000][E][esp32-hal-i2c-ng.c:275] i2cWrite(): i2c_master_transmit failed: [259] ESP_ERR_INVALID_STATE' and ':302 Guru Meditation Error: Core 1 panic'ed (Interrupt wdt timeout on CPU1)', second panic ':313 Unhandled debug exception' / ':314 Stack canary watchpoint triggered (IDLE1)', EXCCAUSE 0x6 IntegerDivideByZero :311/:351 (likely residual), reboot rst:0xc (RTC_SW_CPU_RST) :377-380. Crash 2 (after reboot): image 36 ACKed, manifest sent (:517), benign AUX-missed line (:518), chunk 1/9 sent (:519-520), telemetry beacon stream started (:525), then rst:0x7 (TG0WDT_SYS_RST) :526-528 with NO panic output. Signature: I2C bus INVALID_STATE + interrupt-watchdog (INT_WDT CPU1 / TG0WDT) + Core-0 spinlock contention in the dump (xTaskIncrementTick -> spinlock_acquire). REGRESSION SCOPE: session #6 ran the identical capture+push pattern clean on pre-round-#10 firmware (3/3 series-A ACKs, 25/26 thumbs COMPLETE, zero resets across a 12-image session), so the regression is from round #10's changes (01-21/01-22/01-23). Suspects (unranked — the debug round must rank them): 01-21 pushPending busy-hold gate / budget re-arm paths (fire exactly at post-capture push start), 01-23 framer inter-byte resync or command_handler hasCommand guard, or a latent instability the new TX cadence exercises. Consequences observed downstream: image 36's every window request rejected ('window request for unknown/evicted image 36 rejected' balloon.log:670; 'Command IMAGE_WINDOW_REQUEST - FAILED' :673/:728/:785/:845), base NACK_INVALID on seq 7/8/10/12 (base.log:167/:199/:236/:273) with futile pass retries, forbidden CAPTURE_NOW terminals base.log:141/:143. Debug artifacts available: retained consoles balloon.log (1,125 lines) / base.log (429 lines); ELF SHA256 3d2b351b4 for addr2line on the crash-1 backtrace. SESSION-8 EXTENSION (01-27 bench, 2026-08-28, balloon2.log/base2.log, round-#11 firmware, balloon ELF SHA256 c53635e181 verified before decode — full evidence in .planning/debug/d1-crash-regression-push-start.md §6): the crash RECURRED and the evidence re-scopes the root cause. What survived: the spaced smoke — the session-7 crash-1 pattern (first post-boot capture push) ran clean (image 37 both kinds COMPLETE); the 01-25 PSRAM warm-up ran at both boots (balloon2.log:102/:1321). What crashed: the SECOND image, ~3.5-4 min in, at the FIRST CHUNK of the SEVENTH re-armed FULL window (image 38 chunks 96..109 armed :1220, chunk 1/14 sent :1227, then rst:0x7 TG0WDT_SYS_RST :1230 with Saved PC:0x40376430 :1231 — decoded as esp-idf tick_hook at int_wdt.c:111, ZERO project frames). What the new evidence DISCRIMINATES: (1) [MEM] healthy at enqueue (:611) and at the last sample before reset (:1175 — heap 8332460, psram 8129116, stackHW 5552) → the 01-25 first-use-PSRAM hypothesis is DISCONFIRMED as sufficient; (2) zero U+FFFD mojibake, zero Guru Meditation, zero canary, and the 2 i2cWrite errors (:885/:1577) are non-adjacent BMP280-path noise each followed by a successful read → session-7's multi-subsystem corruption corroboration is ABSENT, weakening the internal-RAM-corruption-blast ranking; (3) the surviving constant across session-7 crash 2 and session 8 is TG0WDT_SYS_RST during SUSTAINED FULL-window chunk service with the PC in tick/WDT machinery → NEW NAMED AXIS: task-watchdog starvation / loopTask-or-kernel block during FULL window service (a lock-holder stall, an AUX/UART blocking wait, or a cache-suspended stretch starving TG0) — whether that is one mechanism with session-7's spinlock wedge or two faults is round #12's question. NOT established: any application frame, any memory-corruption mechanism visible this session. ROUND-#12 DETERMINATION (01-28 debug round #2, full record .planning/debug/d1-crash-regression-push-start.md §7): the WDT topology of the deployed build is pinned from its config artifacts and framework sources — rst:0x7 TG0WDT_SYS_RST = the TASK watchdog's stage-1 hardware backstop (MWDT0/TG0, 10 s unfed, task_wdt_impl_timergroup.c:124-126; stage-1 has NO print path — the silence is structural), and in this firmware ONLY IDLE0 feeds the TWDT (loopTask unsubscribed and pinned CPU1), so the reset PROVES IDLE0 starved ≥10 s on CPU0 while both cores' tick chains stayed alive to the end (the 300/600 ms IWDT never fired; it is fed from tick_hook with the CPU1 interlock) and the TWDT stage-0 interrupt (whose ISR prints unconditionally) was never serviced — a stall BELOW project-code visibility. All eight project-code candidates eliminated by duration/lock/interrupt arithmetic (§7.3; serial->flush() is the sole non-yielding stretch but holds no kernel lock and runs on CPU1). VERDICT on the one-or-two question: ONE mechanism family, two expressions — session-7's directly-observed kernel-portMUX CAS spin in the tick ISR (§1.4) and session-8's tick_hook Saved PC sample the SAME chain (increment phase vs hook phase of xPortSysTickHandler); session-7's corruption trio read as a co-effect of the same kernel-level wedge (inference, honestly labeled). RANKED ROOT CAUSE: CPU0 scheduler/interrupt-delivery stall (kernel level) during sustained FULL-window TX service — HONEST LIMIT (the 01-25 §3 convention): the stall's inner structure is NOT directly observable pre-bench (one PC sample, no dump, empty log to the banner); ranking is by elimination + blast radius only. FIX LANDED per the Task 2 selection (option (a)+(b), auto-advanced pending end-of-phase operator confirmation): yielding bounded TX-drain replacing the flush busy-spin in E32LoRa::transmit + discriminators 'ImageTx: [BOOT] reset-cause: <name> (G-01-10)' and 'ImageTx: [LOOP] slow pass gap %lu ms (G-01-10)' (latch-guarded, IMG_LOOP_SLOW_PASS_MS 2500); builds 2/2 SUCCESS, harness exit 0. Crash-truth closure stays with 01-29's hardware session — no bench claims made this round. SESSION-9 EXTENSION (01-29 bench, 2026-08-28, balloon3.log/base3.log, round-#12 firmware with Lever A + B1/B2 provably in-image, balloon ELF SHA256 e09dd034a5ab verified before decode — full evidence in .planning/debug/d1-crash-regression-push-start.md §8): CRASH RECURRED A THIRD TIME — the 01-28 fix is DISCONFIRMED as sufficient (second disconfirmation); the crash's THIRD expression: the INTER-WINDOW LULL after a cleanly completed FIRST window. What survived: the smoke's first post-boot capture (image 39) through thumb COMPLETE 9/9 (base3.log:79) and FULL window 1 armed (balloon3.log:421) and served 16/16 chunks on air (:427-:491). What crashed: at t=124026 ms, in the TX-LIGHT lull (GET_STATUS serviced, [BCN] seq=23, healthy [MEM], loop Max 1277 ms), a Wire-0 transaction failed ESP_ERR_INVALID_STATE :520 — DIRECTLY ADJACENT to the reset (:521-:523 rst:0x7 TG0WDT_SYS_RST, :524 Saved PC:0x4037c7fa = DECODED esp_vApplicationTickHook freertos_hooks.c:34, the registered-tick-hook DISPATCH LOOP one frame above session-8's tick_hook in the same tick-ISR chain, ZERO project frames) — and the i2cWrite line ITSELF carries the session-7 corruption-class mojibake bytes (replacing the T of STATE; hex-verified; the corrected §7.4 grep — the UTF-8-replacement-char grep returns 0, the twice-documented trap). The sessions-7/8 'non-adjacent BMP280 noise' classification of i2cWrite errors BREAKS on this session: adjacent, no successful read follows (the reset intervened), corruption on its own line. Round-#12 discriminators answered (§8.3): B2 [LOOP] = ZERO fires with loopTask healthy (Max 1277 ms < 2500 ms latch; the loop cycled to the final second :518-:519) — the session-8 CPU0-side signature reproduced AND the Lever-A loop-hang hypothesis disconfirmed as the mechanism; B1 = TASK_WDT named by the ROM reason register at boot 2 (:615); [MEM] healthy at :372/:511 — exhaustion ruled out a third session. NEW for debug round #3 (§8.6): the crash point MOVED from mid-service (session 8) to the inter-window lull (session 9) — 'sustained TX-heavy service' is no longer the constant context, and the lull-phase candidate set (Wire-0 transactions, GPS UART1 reads, NVS/flash ops) has NOT had the §7.3 arithmetic; the crash-adjacent I2C failure + own-line mojibake must be carried as discriminating evidence (driver-state corruption as wedge co-effect vs bus wedge with interrupts masked vs coincidence — the open question), not explained away. The [MEM]/B1/B2 instrumentation stays in until this gap closes"
  artifacts:
    - path: "src/image_tx_manager.cpp"
      issue: "leading suspect site — the 01-21 busy-hold gate (pushPending idle slot) + budget re-arm paths fire at the exact crash phase; NOT established"
    - path: "src/command_handler.cpp"
      issue: "01-23 hasCommand guard + framer inter-byte resync suspects (receive path active during push); NOT established"
    - path: "src/command_sender.cpp"
      issue: "01-23 framer resync twin suspect (base side); NOT established"
  missing:
    - "DONE (01-25 debug round): crash-1 backtrace decoded (addr2line, ELF SHA 3d2b351b4) — zero project frames, wedge signature in the kernel spinlock path; root cause ranked internal-RAM corruption at first-post-boot push; fix (PSRAM warm-up + [MEM] instrumentation) landed"
    - "ANSWERED at bench session #8 (partially): the crash is NOT deterministic at first-post-boot push — image 37's first-post-boot push ran CLEAN; the recurrence is mid-service of the second image's FULL window (~4 min in). Spaced captures did NOT avoid it this time (both session-8 captures were spaced; the second one still crashed mid-transfer). Interim discipline (avoid unspaced bursts) remains in force but is no longer sufficient — the crash class is not confined to bursts"
    - "DONE (01-28 debug round #2 / round #12): the new axis is decoded — WDT topology pinned (TG0WDT_SYS_RST = TWDT stage-1 10 s silent backstop; only IDLE0 feeds the TWDT in this build; Saved PC 0x40376430 = tick_hook int_wdt.c:111, the CPU0 tick-ISR hook phase), the crash window reconstructed (~1102 ms/pass sustained-service cadence, beacon seq 47 due in-window), all eight project-code candidates eliminated, ONE-mechanism-family verdict against session-7's spinlock wedge, root cause RANKED with the honest limit (CPU0 scheduler/interrupt-delivery stall below project-code visibility — elimination + blast radius only); fix landed green (yielding bounded TX-drain + [BOOT] reset-cause and [LOOP] slow-pass discriminators; builds 2/2, harness exit 0); the [MEM] instrumentation stays in until this gap closes"
    - "ANSWERED at bench session #9 (round-#12 discriminators, debug doc §8.3): B2 [LOOP] = ZERO fires with loopTask demonstrably healthy (Max 1277 ms vs the 2500 ms latch) — the CPU0-side signature reproduced and the Lever-A loop-hang class disconfirmed; B1 = worked at both boots (POWERON :103, TASK_WDT :615 — the ROM reason register independently confirming the task-watchdog read); [MEM] healthy a third session (:372/:511). BUT the crash itself RECURRED (third expression, inter-window lull) — the 01-28 fix is DISCONFIRMED as sufficient; debug round #3 is required before any bench re-run (§8.6 brief: lull-phase candidate audit, the crash-adjacent I2C+mojibake convergence, why the crash point moved)"
    - "Re-run series A only after the fix — G-01-7's burst levers remain unjudgeable on crashing firmware (sessions 8 AND 9 aborted before any burst: session 8 at image 38's window service, session 9 at protocol step 3 in the first image's inter-window lull)"
  opened_at: 2026-08-28

- gap_id: G-01-11
  truth: "The 're-announce held - inbound window traffic active' discriminator line means what it says — it never fires without actual inbound window traffic (a boot-time zero stamp is not receipt evidence)"
  status: resolved
  reason: "Operator bench observation (01-24 session #7, 2026-08-28): the line fires at EVERY boot with zero inbound window traffic (balloon.log:135, :504, :652 — each within seconds of System ready), polluting the G-01-7 discriminator evidence this round was watching"
  severity: minor
  test: 3
  root_cause: "Boot-epoch stamp semantics in the 01-21 global liveness clock: lastInboundWindowRequestMs is zero-initialized and the wrap-safe hold check (millis() - lastInboundWindowRequestMs) < IMG_FULL_REANNOUNCE_BUSY_MS is satisfied by stamp==0 for the first 15 s of uptime, so 'never received' is indistinguishable from 'received at boot'; the idle-slot hold checks the stamp before findReannounceCandidate and is reached every pass on an empty boot-time queue, so the latch-guarded episode line fires once per boot. Consumes nothing (no attempt, no idle-period advance, no drop) — the harm is evidence pollution: a bench grep for the discriminator cannot distinguish a boot artifact from a genuine inbound-traffic hold (exactly what session 7 observed: 3 fires at 3 boots, zero genuine holds). Fix class: gate the stamp on a has-ever-received flag (or stamp it at begin()) so the hold only engages after real receipt; rides the G-01-10 debug round. RESOLVED at bench session #8 — the 01-25 receipt-ever flag makes the boot-epoch fire impossible by construction and the session's two boots prove it live (see verified_by); the session's only 2 hold fires are BOTH genuine inbound-traffic holds, timestamp-correlated"
  artifacts:
    - path: "src/image_tx_manager.cpp"
      issue: "pushPending busy-hold gate + lastInboundWindowRequestMs stamp site — the boot-epoch false-fresh window. FIXED by 01-25: inboundWindowRequestSeen receipt-ever flag ANDed into the busy-hold gate"
  missing:
    - "CLOSED by 01-25 + bench session #8: the zero stamp is inert (receipt-ever flag), proven live at two boots — zero spurious boot-window fires"
  opened_at: 2026-08-28
  resolved_by: "01-25 (01-25-SUMMARY.md fix commit): inboundWindowRequestSeen receipt-ever flag declared beside the stamp, initialized false in ctor/begin(), set true at the stamp site before kind validation, ANDed into the busy-hold gate — the boot-epoch zero stamp becomes inert by construction while the hold line itself is NOT muted (the G-01-7 discriminator is preserved)"
  resolved_at: 2026-08-28
  verified_by: "Bench session #8 (01-27, 2026-08-28, balloon2.log + base2.log, firmware build Aug 28 2026 09:48:55 — the 01-25 fix provably in-image via the :102/:1321 warm-up lines): BOTH boot windows CLEAN of the hold line before the first genuine inbound window request — boot 1 (balloon2.log:85-135) has zero 're-announce held' lines and the first inbound IMAGE_WINDOW_REQUEST lands at :260; boot 2, the crash reboot (:1238-:1383), likewise zero hold lines with the first inbound (rejected) IMAGE_WINDOW_REQUEST at :1384. The session's exactly-2 fires are BOTH genuine by timestamp correlation: balloon2.log:335 fires mid-service of image 37's FULL window (window chunks :329/:332 immediately prior, base actively requesting windows — base2.log:238 area); balloon2.log:1389 fires 5 lines after the genuine inbound request at :1384-1388 ('Executing command 30' -> 'window request for unknown/evicted image 38 rejected' -> FAILED -> hold) — the receipt-ever flag stamped by that very request, the 15 s busy window then correctly holding the idle re-announce. That is exactly the legitimate traffic-correlated fire the truth permits. Contrast baseline: session 7 fired 3x at 3 boots with zero inbound traffic (balloon.log:135/:504/:652)"
