---
phase: 01-command-protocol-control
plan: 12
type: execute
wave: 9
depends_on: ["01-11"]
files_modified:
  - include/image_protocol.h
  - include/image_tx_manager.h
  - src/image_tx_manager.cpp
  - include/image_rx_manager.h
  - src/image_rx_manager.cpp
  - src/camera_manager.h
  - src/camera_manager.cpp
  - .planning/phases/01-command-protocol-control/01-UAT.md
  - .planning/WINDOWS.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [IMG-02, IMG-03, PRI-03, CTRL-02]

estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 3
  confidence: low   # calibration store has 0 samples (factor 1, not applied); two independent code mechanisms + one bench checkpoint

must_haves:
  truths:
    - "G-01-7 serialization: BOTH full-pull activation sites (activateNextPull and startTransfer's manifest-while-idle branch) hold activation while a non-terminal THUMBNAIL slot with holes and a same-id FULL slot exists — the thumbnail completes (push + heal, D-22 single path) before its full pull starts; D-19 FIFO order and the D-21 three base-speaks triggers are otherwise unchanged"
    - "G-01-7 eviction guard: evictEntriesOlderThan skips any entry with an armed, incomplete window (windowArmed && windowNextIndex < windowStart + windowCount) with a distinctive defer log — the supersede path never evicts an in-flight heal/pull; evictionClassOf's overflow ranking and sweepExpiredEntries' TTL path keep today's behavior exactly"
    - "G-01-7 RX-settle: IMG_WINDOW_RX_SETTLE_MS = 500 is defined in include/image_protocol.h with the invariant chain settle (500) < preempt (5000) < stall (8000), applied on the base window-complete cadence and on the balloon first-chunk-after-(re)arm path"
    - statement: "Bench series A: 3 back-to-back unspaced captures (the session-2 discriminator protocol, at the IMG_TX_QUEUE_DEPTH 3 boundary) finalize every kind COMPLETE — zero INCOMPLETE verdicts, no eviction of a mid-service entry, beacons holding the 5 s cadence throughout (G-01-7 truth)"
      verification: backstop
    - "G-01-8: CameraManager tracks allocatedFrameSize (the framesize the fb buffers were sized for, refreshed inside initCamera at every (re-)init); setFrameSize to a larger framesize re-inits the camera via the existing reinitialize() pattern and a failed re-init recovers to the previous size before returning false — the false-SUCCESS-then-all-captures-dead defect is closed because the verdict is truthful"
    - "G-01-8: initCamera applies the CACHED currentSaturation/currentWBMode/currentExposure (constructor defaults are 0, so boot behavior is bit-identical) — a re-init never silently resets operator settings; handleSetResolution and the resolution select need no change (every advertised value 5-13 now executes or NACKs honestly)"
    - statement: "Bench series B: SET_RESOLUTION CIF (8) executes and captures at 400x296; VGA (9) executes with zero cam_hal FB-OVF console lines or NACKs honestly; captures still succeed after the series and after reboot the boot resolution is QVGA; SC-3 settings visible-effect spot-checks operator-judged pairwise (G-01-8 truth + the report's behavior-unverified SC-3 clauses)"
      verification: backstop
    - "Both firmware targets build green and the wire harness passes; the PRI-01 beacon-priority order gates stay green — the hold and settle touch scheduling only, never the beacon early-return or command-response loop order"
    - "Pacing invariants hold at HEAD: IMG_RETRANSMIT_MAX_PASSES 3, IMG_WINDOW_STALL_MS 8000, IMG_WINDOW_SERVICE_PREEMPT_MS 5000, IMG_THUMB_HEAL_IDLE_MS 24000 (3x stall), IMG_ENTRY_TTL_MS 900000 all unchanged; the serialization hold is bounded by the D-24 3-pass finalization — a holed thumbnail can never block FIFO advance forever (its INCOMPLETE finalize makes it terminal and releases the hold)"
    - "UI-SPEC lift (explicit): before any command the Last Command panel renders the documented 'No commands yet' copy and every settings form renders a complete documented default value — no form is ever shown unfilled"
    - "UI-SPEC lift (explicit): an in-flight command shows the locked 'Sent' state string immediately after POST; the 1s /status poll advances the panel to a terminal state (status-line model, no spinners/skeletons)"
    - "UI-SPEC lift (explicit): Sent/Acked/Failed/Pending counters advance as commands process; 4-column grid on desktop, 2x2 below 480px"
    - "UI-SPEC lift (explicit): each setting is an independent single-field form — a multi-field partial state cannot occur; forms always display a complete value"
    - "UI-SPEC lift (explicit): the status bar collapses 4->2 columns under the 480px media query; counter cells never truncate; Last Command text wraps within card width — no clipping"
    - "UI-SPEC lift (explicit): numeric readouts only — no singular/plural copy; layout identical at 0, 1, and many"
    - "UI-SPEC lift (explicit): .message and label text wrap at 14px/1.5 inside the card; no ellipsis or truncation anywhere in the Phase 1 UI"
    - statement: "UI-SPEC lift (backstop): terminal 'Timeout' / 'Failed (retry N)' with documented error copy renders after retries are exhausted — hardware-proven at UAT Test 2 (degraded link incl. the duplicate-ACK edge); the CommandState-to-string mapping is code-verifiable"
      verification: backstop
  artifacts:
    - "src/image_rx_manager.cpp — serialization hold at both activation sites + pendingHealThumbnail helper + base-side settle gate on the window-complete cadence"
    - "include/image_rx_manager.h — pendingHealThumbnail declaration + healHoldLoggedId member"
    - "src/image_tx_manager.cpp — evictEntriesOlderThan mid-service guard + arming sets windowArmedAtMs + balloon-side settle gate in serviceWindowChunk"
    - "include/image_tx_manager.h — new ImageTxEntry field windowArmedAtMs"
    - "include/image_protocol.h — new constant IMG_WINDOW_RX_SETTLE_MS = 500 with invariant comment citing the G-01-7 discriminator evidence"
    - "src/camera_manager.h + src/camera_manager.cpp — allocatedFrameSize tracking, re-init-with-recovery setFrameSize, cached-settings initCamera"
    - ".planning/phases/01-command-protocol-control/01-UAT.md — G-01-7/G-01-8 status flips on bench evidence; Test 3 note update"
    - ".planning/WINDOWS.md — entries 3 (G-01-7) / 4 (G-01-8) updated with evidence"
    - ".planning/phases/01-command-protocol-control/01-12-SUMMARY.md"
  key_links:
    - "Hold -> heal loop composition: while the hold blocks activation, findActivePull() returns nullptr, so heal gate 1 passes and the existing stall-driven heal request fires on its 8 s clock — the hold reorders priority, it does not add a fourth base-speaks trigger (D-21)"
    - "Eviction-guard scope boundary: the guard applies ONLY inside evictEntriesOlderThan (the supersede path called from FULL window requests); the overflow-eviction class ranking and the TTL sweep keep evicting as today — otherwise 3 pinned slots would wedge the IMG_TX_QUEUE_DEPTH 3 TX queue"
    - "settle < preempt < stall chain: a settled window (500 ms) still preempts push work inside the 5000 ms bound and can never trip the base's 8000 ms stall clock — the settle gate delays only the FIRST chunk of a freshly (re-)armed window; mid-window continuation is unaffected"
    - "allocatedFrameSize vs currentFrameSize: currentFrameSize stays the single framesize identity (GET_STATUS reports it via frameSizeFromEsp); allocatedFrameSize is a derived capacity bound written only inside initCamera — never a second source of truth"
    - "Thumbnail-path safety: QQVGA (framesize_t 1) is always <= allocatedFrameSize, so createThumbnail's downshift/restore pair stays on the sensor-only path — no re-init storm per capture"
  prohibitions:
    - statement: "MUST NOT fabricate image-transfer or config success — verdicts come from bench finalize lines or operator observation, never from code-level reasoning"
      status: flagged
      verification: backstop
    - statement: "MUST NOT mark G-01-7 or G-01-8 resolved on code-level evidence alone — closure requires the operator bench series (A for G-01-7, B for G-01-8)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT add a parallel best-effort thumbnail mechanism — D-22's kind-addressed windowed heal stays the single reliability path; the serialization hold changes WHEN the heal runs, not HOW"
      status: flagged
      verification: backstop
    - statement: "MUST NOT apply the mid-service eviction guard to the overflow-eviction path or the TTL sweep — evictionClassOf's class ranking keeps its full toolset"
      status: flagged
      verification: backstop
    - statement: "MUST NOT touch the beacon early-return or command-response loop order — PRI-01 telemetry priority is proven by the harness order gates and must stay green"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT leave the camera dead after a failed framesize re-init — the recovery re-init to the previous size is mandatory before returning false; the G-01-8 failure mode is captures-dead-until-reboot and the fix must never reproduce it"
      status: flagged
      verification: backstop
    - statement: "MUST NOT change the /gallery refresh contract (D-36 count-driven refetch) — this round touches no UI refresh behavior"
      status: flagged
      verification: machine-checkable
---

<objective>
Remediation round for the two gaps the 01-11 bench re-verification left open: G-01-7 (concurrent-transfer starvation — a new capture's window traffic evicts a pending heal and fulls starve thumbnail heals behind them) and G-01-8 (SET_RESOLUTION above the boot-allocated frame buffer falsely SUCCEEDs, floods FB-OVF, and fails all captures until reboot).

Purpose: the 01-11 round closed G-01-5/G-01-6 and its discriminator sessions PROVED the mechanism for each new gap — spaced captures complete 4/4 kinds while unspaced captures INCOMPLETE (concurrency, not air loss: G-01-7), and the VGA false-SUCCESS with 4,379 FB-OVF lines traces to a sensor-only setFrameSize against QVGA-sized PSRAM buffers (G-01-8). The mechanisms are named; this round implements the operator-proposed levers at their trace-named sites and re-verifies both on the bench, carrying the SC-3 settings visible-effect + CIF spot-checks that G-01-8 has blocked since the 01-11 settings series.

Output: three levers for G-01-7 (serialize thumbnail completion ahead of full-pull activation; never evict a mid-service entry from the supersede path; a bounded inter-window RX-settle gap for the tail-chunk turnaround friction) plus the G-01-8 re-init-with-recovery bound; one operator bench session (series A unspaced captures / series B settings incl. CIF visible-effect); G-01-7/G-01-8 status flips on evidence; WINDOWS entries 3/4 closed or honestly re-scoped.

Spec-less probe fallback: recorded visible skip — this phase has no machine-mapped requirement IDs (phase_req_ids null), so no probe predicates were generated this run and no probe-derived must_haves exist; must_haves derive from the phase goal, the open gaps in 01-VERIFICATION.md / 01-UAT.md, and prior plan conventions.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-11-SUMMARY.md

Binding decisions and prior-round constraints:
- D-19 (FIFO pull order by manifest arrival), D-21 (base speaks only on window completion / stall / manifest-while-idle), D-22 (kind-addressed windowed heal is the SINGLE reliability mechanism), D-24 (bounded consecutive unhealed stalls, 3 passes), D-36 (galleryCount is the only /gallery refetch signal — untouched this round), D-48 (honest Incomplete badge)
- 01-08 single-lever discipline: pacing-constant changes require a named discriminator — the G-01-7 mechanism IS now named (spaced-vs-unspaced sessions discriminate concurrency), so the settle constant is the named secondary lever, not a blind change; the five standing constants stay untouched
- 01-09 lesson: both boards always reflashed together (divergent E32 config severs the pair)
- Baseline evidence to beat: session 2 unspaced — thumbnail INCOMPLETE 6/8 after 3 passes (base2.log:296), full INCOMPLETE 15/52 after five tail re-requests seq 53/55-59 (base2.log:415-498), eviction line 'window request for image 3 supersedes older entry image 2; evicted' (balloon2.log:656); session 3 spaced — 4/4 kinds COMPLETE. G-01-8: SET_RESOLUTION VGA SUCCESS (balloon.log:582) then 4,379 'cam_hal: FB-OVF' lines and 4x CAPTURE_NOW FAILED until reboot
- Camera facts: boot config FRAMESIZE_QVGA / quality 10 (include/balloon_config.h:40-41); fb_count 2 in PSRAM sized at esp_camera_init (camera_manager.cpp:159-161); reinitialize() exists (end + delay(100) + begin, :81-85); initCamera hardcodes saturation/WB/ae to 0 (:117-120) while caching currentSaturation/currentExposure/currentWBMode; createThumbnail downshifts QQVGA then restores (:388); framesize_t is monotonically ordered by pixel count in esp32-camera
- Transfer facts: IMG_TX_QUEUE_DEPTH 3; IMG_ENTRY_TTL_MS 900000 (a held-queued full is safe far beyond any heal duration); RX slots drive FIFO via activateNextPull (image_rx_manager.cpp:769) plus the manifest-while-idle inline activation (:534-549); evictEntriesOlderThan is called ONLY from FULL window requests (:687-694) BEFORE the BUSY deferral check (:699-708)
</context>

<tasks>

<task type="auto">
  <name>Task 1: G-01-7 — serialize thumbnail completion, guard mid-service eviction, add the inter-window RX-settle gap</name>
  <files>include/image_protocol.h, include/image_tx_manager.h, src/image_tx_manager.cpp, include/image_rx_manager.h, src/image_rx_manager.cpp</files>
  <read_first>
  - src/image_rx_manager.cpp — process() window driver and heal loop (:97-218), startTransfer manifest-while-idle activation (:504-549), finalize paths calling activateNextPull (:704/:728), activateNextPull (:769-802), issueWindowRequest (:804-845)
  - src/image_tx_manager.cpp — handleWindowRequest evict call + BUSY deferral + arming block (:687-733), serviceWindowChunk (:735-792), evictEntriesOlderThan (:798-806), pushPending preempt (:429-478)
  - include/image_tx_manager.h — ImageTxEntry window-context fields (:104-119)
  - include/image_protocol.h — transfer-constants block and invariant comments (:49-101)
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-7 root_cause (the discriminator evidence and named levers)
  </read_first>
  <action>
  Implement the three G-01-7 levers at their trace-named sites. Nothing else changes.

  LEVER 1 — base-side serialization (thumbnail completes before its full pull starts):
  - Add a private helper to ImageRxManager (declare in include/image_rx_manager.h beside findActivePull; implement near it): ImageRxTransfer* pendingHealThumbnail() — scans RX_TRANSFER_SLOTS and returns the earliest-by-arrivalSeq transfer that is used, non-terminal, imageKind THUMBNAIL, receivedCount < totalChunks, AND findTransfer(t.imageId, FULL_IMAGE) != nullptr. The same-id FULL slot's existence is the gate-2 proof the thumbnail push finished (manifests arrive post-push), so this condition can never hold while a push is still streaming — no separate push-state tracking is needed.
  - Gate BOTH activation sites with it: (a) at the top of activateNextPull() — if pendingHealThumbnail() returns a slot, log once per held image id (new member uint16_t healHoldLoggedId initialized 0; log 'ImageRx: full-pull activation held - thumbnail heal pending for image %u' only when the held id differs from healHoldLoggedId, then record it) and return without activating; (b) in startTransfer's manifest-while-idle branch (:534-549) extend the activation condition from findActivePull() == nullptr to ALSO require pendingHealThumbnail() == nullptr. Site (a) covers the FIFO branch (:161) and both post-finalize advances (:704/:728).
  - Do NOT touch the heal loop (:164-217): while the hold blocks activation there is no active pull, so gate 1 passes and the existing stall-driven heal fires on its own clock. D-21's three base-speaks triggers are unchanged — this is priority ordering between two existing triggers, not a new one.

  LEVER 2 — balloon-side mid-service eviction guard:
  - In evictEntriesOlderThan (src/image_tx_manager.cpp:798-806): skip (do not free) any entry where entries[i].windowArmed && entries[i].windowNextIndex < entries[i].windowStart + entries[i].windowCount, logging 'ImageTx: supersede of image %u deferred - window mid-service' for each skip. An in-flight heal/pull is never evicted by a newer capture's FULL window request.
  - Scope boundary (prohibition): the guard lives ONLY in this function. evictionClassOf's overflow ranking and sweepExpiredEntries' TTL path keep today's behavior exactly — three pinned slots must never wedge the queue.

  LEVER 3 — inter-window RX-settle gap (one constant, two insertion points):
  - include/image_protocol.h, after IMG_WINDOW_SERVICE_PREEMPT_MS (:84): add IMG_WINDOW_RX_SETTLE_MS = 500 with a comment citing the discriminator (five tail re-requests seq 53/55-59 for the 13..14 / 14..14 windows, 6 END MARKER MISS, base2.log:415-498; half-duplex immediate-retransmit turnaround-collision class) and the invariant: strictly below IMG_WINDOW_SERVICE_PREEMPT_MS 5000 which is strictly below IMG_WINDOW_STALL_MS 8000.
  - Base: in process()'s window-complete branch (:110-123), issue the next window request only when millis() minus pull->lastProgressMs is >= IMG_WINDOW_RX_SETTLE_MS; otherwise skip this pass entirely (no pass charged, no other branch fires — nest the check inside the branch so the stall branch cannot run during the settle window).
  - Balloon: add uint32_t windowArmedAtMs to ImageTxEntry (include/image_tx_manager.h, beside lastActivityMs); set it in the arming block (:719-725) alongside lastActivityMs. In serviceWindowChunk (:735-740), before the completion check's work: if entry.windowNextIndex == entry.windowStart and millis() minus entry.windowArmedAtMs is < IMG_WINDOW_RX_SETTLE_MS, return true without transmitting — only the FIRST chunk of a freshly (re-)armed window waits out the settle; a re-armed span re-settles (exactly the retransmit-collision case); mid-window continuation and the preempt clock are unaffected.

  COMMENT HONESTY: update the two comments the new ordering contradicts — the D-19 FIFO comment at the FIFO branch (:157-162) and the 'FULL requests ONLY' eviction comment (:687-694) must both state the hold/deferral exceptions so code and comments cannot disagree.
  </action>
  <verify>
  <automated>powershell -Command "if((Select-String -Path include/image_protocol.h -Pattern 'IMG_WINDOW_RX_SETTLE_MS\s*=\s*500').Count -ne 1){exit 1}" ; powershell -Command "if(((Select-String -Path include/image_protocol.h -Pattern 'IMG_WINDOW_SERVICE_PREEMPT_MS\s*=\s*5000').Count -ne 1) -or ((Select-String -Path include/image_protocol.h -Pattern 'IMG_WINDOW_STALL_MS\s*=\s*8000').Count -ne 1)){exit 1}" ; powershell -Command "if((Select-String -Path src/image_rx_manager.cpp -Pattern 'thumbnail heal pending').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path src/image_tx_manager.cpp -Pattern 'deferred - window mid-service').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path include/image_tx_manager.h -Pattern 'windowArmedAtMs').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path src/image_tx_manager.cpp -Pattern 'windowArmedAtMs').Count -lt 2){exit 1}" ; pio run -e esp32-s3-balloon -e esp32-s3-basestation ; node scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - include/image_protocol.h defines IMG_WINDOW_RX_SETTLE_MS = 500 exactly once, and IMG_RETRANSMIT_MAX_PASSES 3, IMG_WINDOW_STALL_MS 8000, IMG_WINDOW_SERVICE_PREEMPT_MS 5000, IMG_THUMB_HEAL_IDLE_MS 24000, IMG_ENTRY_TTL_MS 900000 all still hold their values (verify gate enforces the two invariant anchors)
  - src/image_rx_manager.cpp contains the hold log token 'thumbnail heal pending' and the manifest-while-idle branch condition includes the pendingHealThumbnail check (grep 'pendingHealThumbnail' count >= 3: declaration in include/image_rx_manager.h + two gated sites in src/image_rx_manager.cpp)
  - src/image_tx_manager.cpp: evictEntriesOlderThan contains the mid-service skip with the 'deferred - window mid-service' log; evictionClassOf and sweepExpiredEntries bodies are byte-identical to HEAD
  - The arming block sets windowArmedAtMs; serviceWindowChunk reads it (grep 'windowArmedAtMs' in src/image_tx_manager.cpp count >= 2)
  - `pio run -e esp32-s3-balloon -e esp32-s3-basestation` reports 2 succeeded; `node scripts/verify_protocol_roundtrip.mjs` exits 0 (wire format untouched — timing-only change)
  - git diff file set for this task is exactly the five listed files
  </acceptance_criteria>
  <done>All three levers implemented at their named sites, invariants documented, builds and harness green, committed atomically as fix(01-12): G-01-7 serialization + eviction guard + RX-settle per 01-11 bench discriminator.</done>
  <reversibility rating="reversible">Firmware code only; revert is a git revert of one commit.</reversibility>
</task>

<task type="auto">
  <name>Task 2: G-01-8 — framesize re-init with recovery, bounded to the allocated buffer</name>
  <files>src/camera_manager.h, src/camera_manager.cpp</files>
  <read_first>
  - src/camera_manager.cpp — constructor (:8-40), begin/end/reinitialize (:52-85), initCamera (:91-132), configureCameraForBalloon (:134-176), setFrameSize (:398-416), createThumbnail downshift/restore pair (:340-392)
  - src/camera_manager.h — private members (:40-86)
  - src/command_handler.cpp — handleSetResolution (:239-279): already NACK_BUSYs when setFrameSize returns false; NO handler change is needed
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-8 root_cause (fb_count 2 PSRAM buffers sized at init for boot QVGA; sensor-only change since 01-04)
  </read_first>
  <action>
  Make setFrameSize truthful for sizes above the allocated frame buffer by re-initializing the camera, with mandatory recovery on failure.

  1. src/camera_manager.h: add private member framesize_t allocatedFrameSize; beside currentFrameSize (:51).
  2. Constructor (:8-40): initialize allocatedFrameSize = currentFrameSize (which is BALLOON_CAMERA_FRAMESIZE = FRAMESIZE_QVGA from include/balloon_config.h:40).
  3. initCamera (:91-132): (a) after the successful esp_camera_init and sensor-handle fetch, set allocatedFrameSize = cameraConfig.frame_size — the fb buffers are sized for exactly this framesize at every (re-)init, so the bound refreshes automatically; (b) replace the three hardcoded zeros that mirror operator-settable settings — set_saturation(s, 0), set_wb_mode(s, 0), set_ae_level(s, 0) at :117-120 — with currentSaturation, currentWBMode, currentExposure. Boot is bit-identical (constructor defaults are 0); a re-init now restores operator settings instead of silently resetting them. Leave every static optimization (special_effect, aec2, agc_gain, gainceiling, bpc, wpc, raw_gma, lenc, dcw, colorbar) untouched.
  4. setFrameSize (:398-416) rework into two paths:
     - Sensor-only path (size <= allocatedFrameSize): today's body unchanged — s->set_framesize, then cache currentFrameSize and cameraConfig.frame_size. framesize_t is monotonically ordered by pixel count in esp32-camera (QQVGA < QVGA < CIF < VGA < SVGA < XGA < SXGA < UXGA); state this assumption in a comment. The thumbnail QQVGA downshift/restore pair always takes this path (no re-init per capture).
     - Re-init path (size > allocatedFrameSize): save prevFrameSize = currentFrameSize and prevConfigSize = cameraConfig.frame_size; set currentFrameSize = size and cameraConfig.frame_size = size; call reinitialize() (the existing end + delay(100) + begin pattern — the bounded block is a documented cost of an explicit operator command, same class as the AUX handshake waits). On success, log 'Camera: framesize growth requires re-init (%d -> %d)' and return true (allocatedFrameSize was refreshed inside initCamera). On failure, restore currentFrameSize and cameraConfig.frame_size to the saved values, call reinitialize() again (recovery — the camera must never be left deinitialized), log 'Camera: re-init to larger framesize FAILED - recovered at previous framesize', and return false; handleSetResolution then NACKs honestly.
  5. No handler, protocol, or UI change: handleSetResolution already maps false to NACK_BUSY, and the resolution select (main_basestation.cpp:2324-2335) keeps advertising values 5-13 — after this fix every advertised value either executes (re-init succeeds) or NACKs honestly (recovery path), which is exactly the G-01-8 truth.
  </action>
  <verify>
  <automated>powershell -Command "if((Select-String -Path src/camera_manager.h -Pattern 'allocatedFrameSize').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path src/camera_manager.cpp -Pattern 'allocatedFrameSize').Count -lt 3){exit 1}" ; powershell -Command "if((Select-String -Path src/camera_manager.cpp -Pattern 'recovered at previous framesize').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path src/camera_manager.cpp -Pattern 'set_saturation\(s, currentSaturation\)').Count -ne 1){exit 1}" ; powershell -Command "if((Select-String -Path src/camera_manager.cpp -Pattern 'framesize growth requires re-init').Count -lt 1){exit 1}" ; pio run -e esp32-s3-balloon -e esp32-s3-basestation ; node scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - src/camera_manager.h declares allocatedFrameSize; src/camera_manager.cpp references it at the three sites (constructor init, initCamera refresh, setFrameSize bound check)
  - The re-init branch calls reinitialize(), and its failure path restores the saved framesize values and calls reinitialize() again BEFORE returning false (grep 'recovered at previous framesize' >= 1)
  - initCamera applies cached currentSaturation/currentWBMode/currentExposure (grep 'set_saturation\(s, currentSaturation\)' count exactly 1; the hardcoded-zero form is gone)
  - The ONLY reinitialize() call sites remain reinitialize()'s own body callers as before plus the new setFrameSize branch — createThumbnail's QQVGA pair stays on the sensor-only path
  - `pio run -e esp32-s3-balloon -e esp32-s3-basestation` reports 2 succeeded; harness exits 0
  - git diff file set for this task is exactly src/camera_manager.h and src/camera_manager.cpp
  </acceptance_criteria>
  <done>setFrameSize is truthful above the allocated buffer with mandatory recovery; builds and harness green; committed as fix(01-12): G-01-8 framesize re-init with recovery, bounded to allocatedFrameSize.</done>
  <reversibility rating="reversible">Firmware code only; no persistent hardware state is written; revert is a git revert.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Bench re-verification — series A (unspaced captures, G-01-7) and series B (settings + CIF + visible-effect, G-01-8/SC-3), then flip statuses on evidence</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md, .planning/WINDOWS.md, .planning/STATE.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-7/G-01-8 gap blocks (the truths and baseline evidence), Test 3 note
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — Gaps Summary (the two truths' full statements) and the behavior_unverified SC-3 item
  </read_first>
  <what-built>
  Task 1 (G-01-7 levers) and Task 2 (G-01-8 re-init bound) on both firmware targets.
  </what-built>
  <how-to-verify>
  1. Flash BOTH boards with this round's firmware (01-09 severance lesson: always both together). Keep both serial consoles logging.
  2. SERIES A (G-01-7 truth): from the dashboard, trigger 3 CAPTURE_NOW commands back-to-back WITHOUT waiting for finalize lines between triggers (the session-2 unspaced protocol, now at the IMG_TX_QUEUE_DEPTH 3 boundary). Expected: all 3 images finalize BOTH kinds COMPLETE ('ImageRx: image N kind K finalized COMPLETE (x/x chunks ...)' for kinds 0 and 1, all ids); zero 'finalized INCOMPLETE' lines; the eviction line, if it appears at all, never names an entry whose window was mid-service; 'full-pull activation held - thumbnail heal pending' appears while thumbnails heal ahead of fulls; the deferred-supersede line may appear and is benign. PRI-01 watch: beacons keep the 5 s cadence throughout the series (record any stall verbatim). Baseline to beat: INCOMPLETE 6/8 (base2.log:296), INCOMPLETE 15/52 (:498), mid-heal eviction (balloon2.log:656).
  3. SERIES B (G-01-8 truth + SC-3 clauses): settings series from the UI forms, one command at a time:
     a. SET_RESOLUTION CIF (value 8): expect ACK; then trigger a capture — it must succeed and capture at 400x296 (manifest chunk count/size roughly doubles vs QVGA; gallery thumbnail renders).
     b. SET_RESOLUTION VGA (value 9): expect ACK; capture must succeed with ZERO 'cam_hal: FB-OVF' lines on the balloon console (baseline: 4,379 after the pre-fix false SUCCESS at balloon.log:582), and further captures keep succeeding.
     c. Optional stretch: one larger value (e.g. XGA 11) — either outcome is a pass (executes, or NACKs honestly with captures still working after). Note: a full above IMG_MAX_IMAGE_SIZE 50000 legitimately never announces (the balloon logs the oversize warning, the thumbnail still completes) — that is documented honest degradation, not a failure.
     d. SET_RESOLUTION QVGA (value 6): capture succeeds at boot-equivalent sizing.
     e. Reboot the balloon: the first capture after reboot is QVGA-sized — boot resolution unaffected by the earlier commands.
     f. SC-3 visible-effect spot-checks (closes the report's behavior-unverified truth): at a fixed resolution, adjust ONE setting between two captures and compare the pair — brightness -2 vs +2, plus at least one other setting class (e.g. saturation or quality). Record the pairwise visible-difference judgment per exercised setting.
  4. Flip statuses ONLY on evidence: G-01-7 and G-01-8 to resolved in 01-UAT.md with verbatim console quotes as verified_by (line numbers and counts, per house convention); Test 3's settings visible-effect + CIF clauses updated; WINDOWS entries 3 and 4 to fixed; STATE.md records the round outcome and names /gsd-secure-phase 1 as the remaining phase-close gate. If any series fails: record the residual honestly with the named next mechanism and keep the gap open — never force a close (carried prohibition).
  </how-to-verify>
  <acceptance_criteria>
  - Series A: 3 unspaced captures, every kind COMPLETE, zero INCOMPLETE verdicts, beacon cadence held (or any stall recorded verbatim); before/after comparison against the session-2 baseline recorded in 01-UAT.md
  - Series B: CIF executes at 400x296; VGA executes with 0 FB-OVF console lines or NACKs honestly; captures succeed after each step and after reboot the boot resolution is QVGA
  - SC-3: pairwise visible-effect judgments recorded for brightness plus at least one other setting class
  - 01-UAT.md flips cite operator-observed quotes (no code-level-only closure); WINDOWS entries 3/4 consistent with the flips; STATE.md updated
  - Any failed series leaves its gap open with the residual named — no fabricated success
  </acceptance_criteria>
  <done>Both gap truths operator-observed post-fix; statuses flipped on evidence or residuals honestly re-scoped; STATE.md names the next step (/gsd-secure-phase 1).</done>
  <resume-signal>Type "re-verified" with both series' tallies vs baseline, or describe the residual outcome</resume-signal>
</task>

</tasks>

<assumption_delta_decision>
Detector fired (pluralization term "second" — traced to 01-09 bench prose "second-card retry", not an architectural transition). Decision: no-change. The round's only new facet is allocatedFrameSize, a derived capacity bound written exclusively inside initCamera; currentFrameSize remains the single framesize identity (GET_STATUS reports it). No promote or add-alongside debt is accepted. Companion invariant encoding (accepted in lieu of a test): the settle < preempt < stall constant chain is enforced by Task 1's verify gate, so a future change that breaks the timing contract fails the gate rather than landing silently.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link (balloon <-> base) | Untrusted wireless; CRC16/framing/type-dispatch defenses unchanged — this round alters scheduling/recovery only, never the wire format |
| Camera re-init path (operator command -> PSRAM fb reallocation) | SET_RESOLUTION now triggers esp_camera_deinit/init — a failed re-init could deinitialize the sensor mid-flight (the exact G-01-8 failure mode) |
| TX queue bounds (balloon, IMG_TX_QUEUE_DEPTH 3) | The eviction guard pins entries; an over-broad guard could wedge all three slots |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12-01 | Tampering | setFrameSize re-init path (camera_manager.cpp) | high | mitigate | Recovery re-init to the saved previous size is mandatory before returning false (Task 2 action step 4); bench series B step b/c proves captures keep working after a large-value round-trip; ASVS L1 block-on-high satisfied by the recovery gate + bench evidence |
| T-01-12-02 | DoS | evictEntriesOlderThan guard pinning all 3 TX slots | high | mitigate | Guard scoped to the supersede path ONLY; overflow ranking (evictionClassOf) and TTL sweep keep full eviction rights (prohibition + acceptance criterion: both bodies unchanged); a held window also self-clears when its service completes |
| T-01-12-03 | DoS | serialization hold starving fulls behind a never-completing thumbnail | medium | mitigate | D-24 3-pass bound finalizes the thumbnail INCOMPLETE (terminal) and releases the hold — bounded by construction; bench series A watches full completion |
| T-01-12-04 | DoS | settle gap delaying telemetry | low | mitigate | Beacon early-return and command-response loop order untouched (prohibition, machine-checkable via harness order gates); settle delays only the first chunk of a freshly armed window |
| T-01-12-05 | Repudiation | gap closure claimed without bench proof | medium | mitigate | Task 3 is a blocking checkpoint; flips must quote operator-observed console evidence (carried 01-11 prohibition) |
| T-01-12-06 | Tampering | untrusted RF input reaching the new code paths | low | mitigate | No new wire surface; window-request validation (kind byte, range bounds, T-02-04/T-02-11) runs BEFORE the evict/guard logic; harness re-run confirms wire contract |
</threat_model>

<verification>
- Task 1: PowerShell pattern gates (settle constant + both invariant anchors, hold/defer log tokens, windowArmedAtMs wiring) + dual-target builds + wire harness exit 0
- Task 2: PowerShell pattern gates (allocatedFrameSize at 3 sites, recovery log, cached-saturation setter) + dual-target builds + harness exit 0
- Task 3: blocking bench checkpoint — series A/B with before/after tallies vs the session-1/2 baselines; status flips cite verbatim operator evidence
- Post-plan: git diff file sets match the per-task lists; 01-UAT.md / WINDOWS.md / STATE.md mutually consistent; no fabricated closures
</verification>

<success_criteria>
1. G-01-7: unspaced back-to-back captures complete every kind COMPLETE (the discriminator scenario that failed 2/3 kinds in session 2) — or the residual is honestly recorded with the next named mechanism
2. G-01-8: SET_RESOLUTION above the boot buffer either executes (CIF at 400x296, VGA with zero FB-OVF) or NACKs honestly with the camera still capturing; boot resolution survives reboot
3. The SC-3 settings visible-effect + CIF spot-checks are operator-judged (the report's one behavior-unverified truth)
4. All documented invariants hold (settle < preempt < stall; passes 3; heal idle 3x stall; TTL unchanged; D-19/D-21/D-22/D-24/D-36/D-48 preserved)
5. Both targets green, harness green, PRI-01 order gates green; statuses flipped only on operator-observed evidence; WINDOWS entries 3/4 consistent
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-12-SUMMARY.md` when done — levers implemented, bench series A/B tallies vs baseline, status flips, residuals (if any) with named next mechanisms
</output>

## Artifacts this phase produces

- `IMG_WINDOW_RX_SETTLE_MS` — new constexpr (500) in include/image_protocol.h
- `ImageRxManager::pendingHealThumbnail()` — new private helper (declared in include/image_rx_manager.h, implemented in src/image_rx_manager.cpp)
- `ImageRxManager::healHoldLoggedId` — new private member (uint16_t, hold log-once latch)
- `ImageTxEntry::windowArmedAtMs` — new field (uint32_t) in include/image_tx_manager.h
- `CameraManager::allocatedFrameSize` — new private member (framesize_t) in src/camera_manager.h
- New log lines (bench discriminators): 'ImageRx: full-pull activation held - thumbnail heal pending for image %u', 'ImageTx: supersede of image %u deferred - window mid-service', 'Camera: framesize growth requires re-init (%d -> %d)', 'Camera: re-init to larger framesize FAILED - recovered at previous framesize'
- Evidence artifacts: 01-UAT.md status flips, WINDOWS.md entries 3/4 updates, STATE.md round outcome, 01-12-SUMMARY.md
