---
phase: 01-command-protocol-control
plan: "07"
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_ids: [G-01-4]
files_modified:
  - src/main_basestation.cpp
autonomous: true
requirements: [CTRL-01, CTRL-02, CTRL-03, CTRL-04]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "UAT Test 4 truth (G-01-4): pressing Trigger Camera Capture, Enable/Disable Auto-Capture, any of the 7 camera-settings submits, or Save Event Thresholds submits in-page via fetch — the browser stays on the admin page (no full-page POST navigation to the raw JSON body), because ONE delegated submit listener on section#capture intercepts every control form in that section"
    - "The operator sees the server's verdict without leaving the page: each submitted form renders the returned {status,message} JSON (sendResponse shape, main_basestation.cpp:3582-3593) into a per-form message div styled with the existing .message success/error/info classes — only server-returned truth, never optimistic ACK wording (IN-03 discipline; terminal states still come from the queue poll)"
    - "The queue/status UI reflects the queued command immediately: pollOnce() runs after each submit settles, so the Command Queue panel, pinned last-command row, autocapture-chip, and event-chip pick up the new command on the next poll without a manual page refresh"
    - "The Phase 3 alerts-form and wifi-form handlers are untouched and never double-submit: the delegated listener returns early when ev.defaultPrevented is set (their direct handlers run first and prevent) and explicitly skips form ids alerts-form and wifi-form"
    - "The 1223f46 script-delivery fix is respected: the entire addition lives inside the PROGMEM HTML_FOOTER raw literal; the dynamic String-built section of handleRoot (lines ~2138-2515) grows by zero bytes of markup; the script stays ES5-style (function expressions, no arrow functions, no template literals) and writes DOM only via textContent/createElement per the D-36 header comment"
  artifacts:
    - path: "src/main_basestation.cpp"
      provides: "Delegated submit listener on the section#capture element in the HTML_FOOTER PROGMEM script: defaultPrevented + known-id guards, generic x-www-form-urlencoded serialization over form.elements, per-form lazily-created message div, in-flight element disabling, fetch(form action), pollOnce() after settle"
      contains: "defaultPrevented"
  key_links:
    - from: "section#capture submit events (11 native forms: capture 2202, 7 settings 2212-2300, auto-capture 2308, auto-capture-stop 2316, event-form 2334)"
      to: "fetch POST to each form's own action attribute route (/capture, /set-*, /auto-capture, /auto-capture-stop, /set-event-thresholds)"
      via: "one delegated listener attached to the section element; form.getAttribute('action') supplies the route so no route list is hardcoded"
      pattern: "getEventListener on 'capture'"
    - from: "fetch response JSON (message field)"
      to: "per-form message div + pollOnce()"
      via: "r.json() then setClass('message ' + ok?'success':'error') and setText(msg), then pollOnce() — mirrors the alerts-form handler at 1412-1441"
      pattern: "pollOnce()"
  prohibitions:
    - statement: "MUST NOT render fabricated command state: the in-page message shows only the server-returned message text; ACK/terminal wording continues to come exclusively from the polled queue truth — the new handler never writes a state word the server did not say"
      status: unverified
      flagged: true
      verification: judgment
---

<objective>
Close UAT gap G-01-4 (major, Test 4): the Phase 1 control forms on the base station admin page are native HTML form posts, so pressing Trigger Camera Capture or Auto Capture Enable navigates the browser to the raw JSON response and the operator must press Back to return to the page.

Root cause (debug session capture-buttons-raw-json-navigation.md, CONFIRMED): the capture, 7 settings, auto-capture enable/stop, and event-thresholds forms (src/main_basestation.cpp:2202-2355) have zero client-side submit interception — the footer script attaches fetch+preventDefault handlers only to the Phase 3 alerts-form (1412) and wifi-form (1477), and every command handler replies with a bare application/json body via sendResponse (3582-3593). Latent since plan 01-01 (bd50a30); not the 1223f46 script-truncation class.

Purpose: CTRL-01/CTRL-02/CTRL-03/CTRL-04 all funnel through these buttons — the UAT could not properly exercise camera settings, auto-capture cadence, or (visually) captures while every press threw the operator off the page.
Output: one delegated in-page submit handler covering all control forms in section#capture, riding the existing PROGMEM footer script; both firmware targets green; operator-verifiable on the bench (final confirmation folded into the 01-09 hardware session).
</objective>

<execution_context>
@C:/Users/Amp/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Amp/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/debug/capture-buttons-raw-json-navigation.md

The in-repo pattern to mirror is the alerts-form submit handler at src/main_basestation.cpp:1412-1441 (preventDefault, disable elements in flight, urlencoded body, fetch, r.json() to {ok, msg}, message class by ok, pollOnce, re-enable, catch path) — the new handler generalizes exactly this shape.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delegated in-page submit handler for every control form in section#capture (G-01-4)</name>
  <read_first>
  - src/main_basestation.cpp 640-714 (setText/setClass/setColor helpers with the D-36 textContent-only rule, stateClass, pollOnce and its backoff scheduling)
  - src/main_basestation.cpp 1410-1509 (alerts-form submit handler 1412-1441 — THE pattern to generalize; wifi-form submit handler 1477-1509 — note its two-step confirm, NOT needed here)
  - src/main_basestation.cpp 2196-2443 (the entire section#capture markup: Manual Capture card 2200-2205, Camera Settings card with the 7 forms 2208-2302, Auto-Capture card 2304-2325 with the display-only autocapture-chip at 2323, Event Capture card with event-form 2331-2359, Alert Thresholds card with alerts-form 2366-2403, WiFi card with wifi-form 2412-2441 — confirms BOTH already-handled forms live INSIDE this section, hence the guards below)
  - src/main_basestation.cpp 3578-3593 (sendResponse — the {status,message} JSON body every command handler returns)
  - src/main_basestation.cpp 1770-1796 (poll-driven autocapture-chip / event-chip / evBusy rendering the handler must compose with)
  - src/main_basestation.cpp 1837-1842 (script tail: updateNavCurrent + initial pollOnce + the closing rawliteral — insertion point context)
  - .planning/phases/01-command-protocol-control/01-UAT.md (G-01-4 missing list)
  </read_first>
  <files>src/main_basestation.cpp</files>
  <behavior>
  - Behavior 1 (host-level structure expectation, checked in Task 2): the HTML_FOOTER script contains a submit listener attached to the element with id capture using event delegation (one listener covering all 11 native control forms in the section, no new form ids added).
  - Behavior 2: a submit on alerts-form or wifi-form does NOT trigger a second fetch — the delegated handler returns before serializing when ev.defaultPrevented is truthy or the form id is alerts-form or wifi-form.
  - Behavior 3: any other form in the section serializes every named, non-button control to x-www-form-urlencoded (number inputs and selects alike — e.g. resolution=8, interval=10, altitude-m=150), POSTs via fetch to the form's action attribute, disables the form's elements while in flight, and re-enables them on settle or catch.
  - Behavior 4: the response message field renders into a message div adjacent to the submitted form (created lazily on first submit, reused after), class message success when the HTTP status is ok, message error otherwise — textContent only.
  - Behavior 5: pollOnce() runs after every settle (success and catch) so the queue panel, last-command row, autocapture-chip, and link state reflect the queued command without a page reload.
  </behavior>
  <action>
  Gap G-01-4 closure — generalize the existing alerts-form AJAX pattern to every control form, with one delegated listener instead of 11 per-form handlers:

  1. Insertion point: inside the PROGMEM HTML_FOOTER script, immediately AFTER the wifi-form submit handler block ends (line ~1509, before the renderWiFi function). Keep the insertion entirely inside the raw literal — the dynamic String-built handleRoot section must not grow by a single byte (this is the 1223f46 truncation-fix constraint; the 57 KB script streams from PROGMEM precisely so String heap churn cannot drop it).

  2. Attach ONE delegated listener: document.getElementById('capture').addEventListener('submit', function (ev) { ... }). The section element (markup line 2197) contains all 11 control forms, so no per-form wiring and no new ids are needed.

  3. Two guards at the top of the handler, in this order:
     a. If ev.defaultPrevented is truthy, return — the alerts-form and wifi-form direct listeners sit on the form elements themselves, so they run first during bubbling and call preventDefault; this guard prevents double submission of those two Phase 3 POSTs.
     b. Read the submitter form via ev.target; if its id is alerts-form or wifi-form, return — explicit belt-and-braces exclusion so the guard holds even if handler ordering ever changes.

  4. For every other form: call ev.preventDefault() (this IS the fix — the default full-page POST to the bare JSON body never happens), then serialize generically: iterate form.elements, include each control that has a non-empty name and a type other than submit/button, join name=encodeURIComponent(value) pairs with ampersands into a urlencoded body (mirrors the alerts-form body building at 1419-1421 but driven by the elements loop, not a hardcoded field list). The auto-capture-stop form serializes to an empty body — correct, its route takes no arguments.

  5. Per-form message element: lazily create a div on first submit of each form (document.createElement, setClass to message info initially) and insert it immediately after the form via insertBefore(msgDiv, form.nextSibling); store nothing global — recover it the same way on later submits (e.g. check whether form.nextSibling carries a marker class before creating). Zero markup added to the dynamic section; styling rides the existing .message classes.

  6. In-flight state: disable all the form's elements before the fetch (loop as at 1416), re-enable them in BOTH the settle and catch paths (as at 1430/1437) — this composes with the poll-driven evBusy disabling of event-form (1791-1796), which re-applies its own state on the next poll.

  7. Submit: fetch with method POST, Content-Type application/x-www-form-urlencoded, and the route taken from form.getAttribute('action') — the relative path straight from the markup, so all ten routes (/capture, the seven /set-*, /auto-capture, /auto-capture-stop, /set-event-thresholds) are covered with zero route list to maintain. Response handling mirrors the alerts-form then-chain: r.json() to extract the message field, resolve to an ok flag from r.ok plus that message; on settle set the message div class to message success when ok else message error, setText to the returned message (fallback strings Command sent / Failed to send command when the body carries none), re-enable elements, then call pollOnce() so the Command Queue panel, pinned last-command row, autocapture-chip, event-chip, and link state pick up the queued command. Catch path: console.error, re-enable, message error with a failed-to-send text, and still pollOnce().

  8. IN-03 prohibition discipline: the message div renders ONLY the server-returned message text — never add ACK/Sent wording of your own beyond the fallback strings; terminal-state wording continues to come exclusively from the polled queue truth.

  9. Style constraints: ES5 only (function expressions, no arrow functions, no template literals, no let-only block scoping assumptions beyond what the file already uses), DOM writes exclusively through the setText/setClass helpers or createElement/insertBefore — never innerHTML (D-36 header comment at 641-645 is the governing rule).
  </action>
  <verify>
    <automated>cd "C:/Work/Prog/Cosmic1" && pio run -e esp32-s3-basestation -e esp32-s3-balloon && test $(grep -c "defaultPrevented" src/main_basestation.cpp) -ge 1 && test $(grep -c "getElementById('capture')" src/main_basestation.cpp) -ge 1 && test $(grep -c "form.getAttribute('action')" src/main_basestation.cpp) -ge 1 && test $(grep -c 'R"rawliteral(' src/main_basestation.cpp) -eq 2</automated>
  </verify>
  <done>
  Both firmware targets build green; the footer script carries one delegated submit listener on the section with id capture guarded by defaultPrevented and the two known-form ids; serialization is generic over form elements with the route taken from the form's action attribute; a per-form message div renders the server-returned message; pollOnce() runs after every settle; the two raw literals remain intact (exactly 2 rawliteral openers); the dynamic handleRoot markup section is byte-identical to before this plan.
  </done>
</task>

<task type="auto">
  <name>Task 2: Structure gates — no navigation fallback, no dynamic-section growth, handlers intact</name>
  <read_first>
  - src/main_basestation.cpp 638-646 (HTML_FOOTER literal opener — confirm the new handler landed inside it)
  - src/main_basestation.cpp 1843-1850 (literal closer and what follows — the script must still close exactly once)
  - src/main_basestation.cpp 1412-1441 and 1477-1509 (alerts-form and wifi-form handlers — must be untouched apart from whitespace)
  - Output of: git diff --stat src/main_basestation.cpp (whole change should be one contiguous insertion inside HTML_FOOTER)
  </read_first>
  <files>src/main_basestation.cpp</files>
  <action>
  Verification-only task — prove the gap closure did not regress the three prior invariants:

  1. Script delivery integrity: confirm with git diff that every added line falls strictly between the HTML_FOOTER rawliteral opener (line 638) and its closer (line ~1842) — the dynamic String section (2138-2515) and HTML_HEADER show zero diff lines. If ANY diff line falls outside the footer literal, move the change; this is the 1223f46 class of bug (String heap churn dropping script content) the plan is forbidden to reintroduce.

  2. No navigation path remains for the control forms: confirm the delegated listener calls preventDefault on every non-excluded form BEFORE any serialization or fetch (guard order from Task 1 step 3-4). Confirm no new inline onsubmit attributes and no new form ids were added to the markup section.

  3. Existing handlers intact: the alerts-form listener still attaches at ~1412 and the wifi-form listener at ~1477 with their bodies unchanged; their POSTs remain single-shot (guard a of Task 1 makes the delegated handler skip default-prevented events).

  4. Commit with message fix(01-07): in-page AJAX submit for all section#capture control forms (G-01-4).
  </action>
  <verify>
    <automated>cd "C:/Work/Prog/Cosmic1" && test $(git diff src/main_basestation.cpp | grep -c '^-[^-]') -eq 0 && grep -n "addEventListener('submit'" src/main_basestation.cpp</automated>
  </verify>
  <done>
  git diff shows additions only inside the HTML_FOOTER literal (zero deletions, zero changes to the dynamic markup section); exactly three submit listeners exist in the file (alerts-form, wifi-form, the new delegated one); both targets still build green; the change is committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → WebServer POST routes | untrusted form values cross into /capture, /set-*, /auto-capture*, /set-event-thresholds (unchanged by this plan — the AJAX layer moves WHERE the POST originates from, not what the server accepts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07-01 | Tampering | client-side serialization replaces native form POST | low | accept | server-side WR-07 range validation in the POST routes is unchanged and remains the enforcement point; the JS layer is UX only — a crafted direct POST behaves identically to today |
| T-01-07-02 | Tampering (XSS) | per-form message div rendering server JSON | medium | mitigate | message text reaches the DOM exclusively via textContent (setText helper) — never innerHTML; server messages are plain strings from sendResponse, no HTML interpretation path |
| T-01-07-03 | Repudiation | operator submits a command twice rapidly | low | mitigate | in-flight element disabling (alerts-form pattern) blocks double-submits of the same form while a POST is pending |
</threat_model>

<verification>
- pio run -e esp32-s3-basestation -e esp32-s3-balloon green
- Structure gates from Task 1/2 automated blocks all pass
- Operator truth (confirmed during the 01-09 bench session, which flashes this build): pressing Trigger Camera Capture keeps the browser on the admin page, the card shows the server message, and the queue panel shows the new command row without pressing Back
</verification>

<success_criteria>
- UAT Test 4 truth achievable: pressing Auto Capture Enable or Trigger Camera Capture submits in-page and the operator stays on the admin page with the result reflected in the queue/status UI (G-01-4)
- alerts-form/wifi-form behavior unchanged (no double POST)
- No regression risk to the 1223f46 PROGMEM streaming fix (additions confined to the footer literal)
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-07-SUMMARY.md` when done
</output>
