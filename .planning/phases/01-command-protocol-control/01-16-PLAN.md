---
phase: 01-command-protocol-control
plan: 16
type: execute
wave: 12
depends_on: ["01-14", "01-15"]
files_modified:
  - .planning/phases/01-command-protocol-control/01-UAT.md
  - .planning/WINDOWS.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [CTRL-01, CTRL-02, IMG-02, IMG-03]

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low   # calibration store has 0 samples (factor 1, not applied); one operator bench checkpoint + evidence-ledger updates

must_haves:
  truths:
    - statement: "G-01-7 series-A acceptance (the truth that has failed two rounds): 3 back-to-back unspaced captures on remediated firmware finalize EVERY kind COMPLETE — zero INCOMPLETE verdicts across all 6 verdicts, thumbs correctly sized (bytes under 8 KB, QVGA-class chunk counts 7-10), beacons holding cadence; a BUSY deferral, if one occurs, is followed by heal completion rather than pass exhaustion"
      verification: backstop
    - statement: "G-01-9 defect A: every thumbnail in the session is a genuine QQVGA-sized payload — no 'enqueued image N (full X B, thumb X B ...)' byte-equality lines, no thumb chunk counts at or above the full's; the drained-stale-frame log appears after QQVGA downshifts (the discriminator record)"
      verification: backstop
    - statement: "G-01-9 defect B: no fully-received full fails its stored-bytes CRC — zero 'stored-bytes CRC mismatch' lines at 100 percent chunk reception across the whole session (including CIF/SVGA-class captures)"
      verification: backstop
    - statement: "G-01-9 defect C: no silently absent full — every enqueued armable full either transfers (both manifests observed arriving) or its loss is surfaced by a named drop log; if a manifest transmit fails at bench, the bounded re-announce recovers it or the drop log names it"
      verification: backstop
    - statement: "WINDOWS entry 6: zero retry-ordinal lines beyond the bound in the new bench logs (the mislabeled attempt-ordinal string cannot be produced by the new firmware), and zero post-terminal chunk re-arm floods (no 'chunk for finalized image N kind K ignored' clusters driven by late retries)"
      verification: backstop
    - statement: "SC-3 clauses (Test 3 ride-alongs, unjudged since round #4): QVGA restore after a larger size succeeds at boot-equivalent sizing; the first capture after reboot is QVGA-sized; pairwise visible-effect judgments recorded for brightness -2 vs +2 plus at least one other setting class"
      verification: backstop
    - "UI-SPEC lift (explicit): before any command the Last Command panel renders the documented 'No commands yet' copy and every settings form renders a complete documented default value — no form is ever shown unfilled"
    - "UI-SPEC lift (explicit): an in-flight command shows the locked 'Sent' state string immediately after POST; the 1s /status poll advances the panel to a terminal state (status-line model, no spinners/skeletons)"
    - "UI-SPEC lift (explicit): Sent/Acked/Failed/Pending counters advance as commands process; 4-column grid on desktop, 2x2 below 480px"
    - "UI-SPEC lift (explicit): each setting is an independent single-field form — a multi-field partial state cannot occur; forms always display a complete value"
    - "UI-SPEC lift (explicit): the status bar collapses 4->2 columns under the 480px media query; counter cells never truncate; Last Command text wraps within card width — no clipping"
    - "UI-SPEC lift (explicit): numeric readouts only — no singular/plural copy; layout identical at 0, 1, and many"
    - "UI-SPEC lift (explicit): .message and label text wrap at 14px/1.5 inside the card; no ellipsis or truncation anywhere in the Phase 1 UI"
    - statement: "UI-SPEC lift (backstop): terminal 'Timeout' / 'Failed (retry N)' with documented error copy renders after retries are exhausted — held-out verification remains the UAT Test 2 hardware proof (already PASS); during this session the link LED stays green/truthful while heals defer (the WR-05 effect) instead of showing No-link during chunk flow"
      verification: backstop
    - "Ledger honesty: G-01-7, G-01-9, and WINDOWS entries 3/5/6 flip ONLY on operator-observed console/dashboard evidence with verbatim quotes, line numbers, and counts (house convention); any failed series leaves its gap open with the residual mechanism named — never a forced close"
  artifacts:
    - "Consolidated bench session logs (balloon5.log / base5.log naming convention) covering series A, the settings series, and the series-B remainder — retained in the repo root as every prior session was"
    - ".planning/phases/01-command-protocol-control/01-UAT.md — G-01-7/G-01-9 status flips (or honest residuals) with verified_by quotes; Test 3 note updated with the SC-3/QVGA-restore/reboot outcomes"
    - ".planning/WINDOWS.md — entries 3, 5, 6 updated (fixed with evidence, or rescoped with the named residual); counts reconciled"
    - ".planning/STATE.md — round outcome recorded; /gsd-secure-phase 1 named as the remaining phase-close gate"
    - ".planning/phases/01-command-protocol-control/01-16-SUMMARY.md"
  key_links:
    - "Wire change <-> bench flash: 01-13 changed the chunk frame — BOTH boards must flash this round's firmware together BEFORE any radio traffic; running the pair mixed breaks chunk framing entirely (the 01-09 air-rate severance lesson, now applying to the body layout)"
    - "Series-A verdicts <-> the three fixed mechanisms: a residual INCOMPLETE must be attributed to its mechanism using the new discriminator logs (kind-stamped window chunks, drained-stale-frame lines, manifest drop logs, defer/stall-deferred lines) before any gap is left open — the round's diagnostics exist precisely to name the next lever if one is still needed"
    - "UI regression <-> untouched UI files: no UI file is modified this round; any dashboard deviation observed at bench is a regression to route, not a fix to make in this plan"
  prohibitions:
    - statement: "MUST NOT fabricate image-transfer or config success — closures cite operator-observed finalize lines, gallery renders, or console quotes, never code-level reasoning"
      status: flagged
      verification: backstop
    - statement: "MUST NOT mark G-01-7 or G-01-9 resolved on code-level evidence alone — closure requires this bench session's series evidence"
      status: flagged
      verification: backstop
    - statement: "MUST NOT flash only one board — the 01-13 wire change makes the pair protocol-incompatible when mixed"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT fix newly observed bench defects inside this plan — route them (WINDOWS ledger / todos) as every prior round did; this plan's code work ended at 01-15"
      status: flagged
      verification: backstop
---

<objective>
Operator bench re-verification of the G-01-9/G-01-7-residual remediation round (plans 01-13/01-14/01-15), carrying the series-B remainder (QVGA restore, reboot boot-resolution) and the SC-3 visible-effect spot-checks that have ridden unjudged since round #4 — then flipping the gap ledgers on evidence.

Purpose: the acceptance truth ("3 unspaced captures finalize every kind COMPLETE") has failed two consecutive rounds for three named mechanisms — the CR-01 kind-less chunk misroute, the CR-02/WR-01 one-shot manifest consumption, and the BUSY-deferral D-24 pass burn compounded by full-sized thumbnails. The code for all of them is now in; only hardware truth can close the gaps, and the fresh discriminator logging (kind-stamped chunks, drained-frame records, manifest drop lines, stall-deferred lines) either proves the closures or names the next residual precisely.

Output: one consolidated bench session (both consoles logged); G-01-7/G-01-9 flipped or honestly rescoped in 01-UAT.md; WINDOWS entries 3/5/6 updated; STATE.md records the outcome and the remaining phase-close gate (/gsd-secure-phase 1).

Spec-less probe fallback: recorded visible skip — this phase has no machine-mapped requirement IDs (phase_req_ids null), so no probe predicates were generated this run.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/phases/01-command-protocol-control/01-13-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-14-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-15-SUMMARY.md

Binding decisions and bench conventions:
- House evidence convention: verbatim console quotes with line numbers and counts, both consoles retained (balloon5.log/base5.log naming), before/after tallies against the session-4 baseline
- 01-09 lesson (now wire-critical): both boards flash together — 01-13 changed the chunk body layout; a mixed pair cannot frame chunks
- Baselines to beat (session 4, balloon4.log/base4.log): thumb 35/36 INCOMPLETE after BUSY deferrals (:634, balloon4.log:1154/:1157); image 7 full 36/36-received CRC mismatch (:854-855); image 8 full silently lost (balloon4.log:722, zero base receipts); image 11 full 144/144 CRC mismatch (:3267-3268); 4-of-6 full-sized thumbs (:356/:399/:1820/:3620); 13x retry-overrun labels; 14-16 post-terminal ignores (:3286-3310); correct thumbs only 1411/1343 B
- New discriminator lines to watch (this round's code): 'Camera: drained stale frame after QQVGA downshift (...)', 'ImageTx: window chunk(image N kind K, ...)', 'ImageTx: FULL manifest ... failed after N attempts; full dropped' (only if a manifest transmit genuinely fails), 'ImageRx: stall deferred - window request seq N still in flight', 'CommandSender: ... deferred (balloon window BUSY) - retry pending', 'Retrying command seq=N (retry K/3)'
- Series-B/G-01-8 unexercised clauses riding this session: QVGA restore after a larger size (d), reboot boot-resolution (e), SC-3 visible-effect pairs (f); the resolution select advertises values 5-13 with QVGA the boot default; an oversize full (>50000 B) legitimately never announces — documented honest degradation, not a failure
- UAT Test 3 is the 'issue' holder; SC-2/SC-4/SC-5 stand PASS and must not regress (beacon cadence, retry/TIMEOUT semantics, auto-capture untouched this round)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Bench session — flash both boards, series A (unspaced), settings series (CIF/SVGA + QVGA restore + reboot + SC-3 pairs), dashboard regression glance</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-7/G-01-9 gap blocks (truths, baselines, named next levers) and Test 3's note (the riding clauses)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — Gaps Summary + behavior_unverified_items (the exact truths this session must exercise)
  - .planning/phases/01-command-protocol-control/01-13-SUMMARY.md, 01-14-SUMMARY.md, 01-15-SUMMARY.md — what changed and the new log lines to expect
  </read_first>
  <what-built>
  Plans 01-13 (kind-tagged chunk wire + kind-exact routing + thumbnail payload guard), 01-14 (success-gated bounded manifest re-announce + overflow mid-service skip), 01-15 (defer-aware D-24 pass accounting + BUSY-retry for window requests + retry-ordinal label) — all built green on both targets.
  </what-built>
  <how-to-verify>
  1. PRE-FLIGHT: `pio run -e esp32-s3-balloon -e esp32-s3-basestation` (2 succeeded) and `node scripts/verify_protocol_roundtrip.mjs` (exit 0) on the round's final commit. Then flash BOTH boards with that exact firmware (never one alone — the chunk body layout changed). Start both serial consoles logging to balloon5.log / base5.log.
  2. SERIES A (G-01-7 truth — the acceptance that has failed twice): from the dashboard, trigger 3 CAPTURE_NOW commands back-to-back WITHOUT waiting between triggers. Expected on remediated firmware: 6 of 6 finalize lines read COMPLETE (both kinds, all 3 ids); zero INCOMPLETE; every thumb correctly sized (enqueue lines show thumb bytes well under the full's, chunk counts 7-10 at QVGA); 'drained stale frame after QQVGA downshift' lines appear; any 'deferred (balloon window BUSY)' line is followed by the window eventually arming and the kind completing (NOT by pass exhaustion); zero 'stored-bytes CRC mismatch'; retry lines read '(retry K/3)' and never an ordinal above 3; no 'chunk for finalized image N kind K ignored' clusters. Record any beacon cadence gap verbatim.
  3. SETTINGS SERIES (G-01-9 defect-B coverage at larger sizes + series-B remainder): one command at a time from the UI forms —
     a. SET_RESOLUTION CIF: ACK; capture — both kinds COMPLETE, thumb correctly sized (contrast with session 4's 11223 B impostor), zero CRC mismatch.
     b. SET_RESOLUTION SVGA (or VGA): ACK; capture — both kinds COMPLETE or the oversize case honestly surfaced (a full above 50000 B logs the cap warning and the thumb still completes); the thumb must NOT approach the full's chunk count (session 4's 145-vs-144 anomaly is the defect-A signature).
     c. SET_RESOLUTION QVGA (series-B step d): ACK; capture succeeds at boot-equivalent sizing.
     d. Reboot the balloon (series-B step e): first capture after reboot is QVGA-sized; image IDs continue the NVS sequence (no reset to 1).
     e. SC-3 pairs (step f): at a fixed resolution, adjust ONE setting between two captures and compare the pair — brightness -2 vs +2, plus at least one other class (contrast, saturation, or quality). Record the visible-difference judgment per pair.
  4. DASHBOARD REGRESSION GLANCE (UI-SPEC lift, all series long): Last Command panel shows 'No commands yet' before the first command and 'Sent' immediately after each POST, advancing via the poll; counters advance; link LED stays truthful (no red during heal deferrals while chunks flow); status bar collapses 2x2 if you narrow under 480px; no truncated/clipped text anywhere.
  5. Record everything: verbatim quotes with line numbers for every verdict, deferral, drain, drop, and retry line the session produces; before/after tallies vs the session-4 baseline. If ANY series fails: capture the discriminating lines (they exist to name the next mechanism), and leave that gap open — do not force a close.
  </how-to-verify>
  <acceptance_criteria>
  - Series A: 6/6 COMPLETE verdicts, zero INCOMPLETE, thumbs correctly sized, beacons held (or gaps quoted); BUSY deferrals (if any) resolved by arming, not pass exhaustion
  - Settings series: CIF and SVGA/VGA captures both-kinds COMPLETE with correctly-sized thumbs and zero stored-bytes CRC mismatches; QVGA restore and reboot-boot-resolution clauses exercised and recorded; SC-3 pairwise judgments recorded for brightness plus one other class
  - Zero retry-ordinal lines beyond the bound; zero post-terminal re-arm ignore clusters; zero silently absent fulls (every armable full transferred or its drop logged by name)
  - Dashboard regression glance: all eight UI-SPEC considerations hold (explicit seven + the backstop LED/truth behavior)
  - Both console logs retained as balloon5.log / base5.log with the evidence quotes locatable by line number
  </acceptance_criteria>
  <resume-signal>Type "approved" with the console excerpts, or describe the failing series with its discriminating log lines</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Flip the ledgers on evidence — 01-UAT.md, WINDOWS.md entries 3/5/6, STATE.md</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md, .planning/WINDOWS.md, .planning/STATE.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-UAT.md — the G-01-7/G-01-9 gap blocks and Test 3 note (flip targets; house quote convention from the G-01-5/G-01-6/G-01-8 resolutions)
  - .planning/WINDOWS.md — entries 3, 5, 6 (table + JSON copies must both update; counts in front matter reconcile)
  - .planning/STATE.md — Current Status / Next Steps (the round outcome and the /gsd-secure-phase 1 gate)
  - The operator's Task 1 evidence (approved resume message with console excerpts, plus balloon5.log/base5.log)
  </read_first>
  <action>
  Update the three ledgers strictly from the Task 1 evidence.

  1. 01-UAT.md: for each of G-01-7 and G-01-9 — if its acceptance passed, set status resolved with resolved_by naming the plans/commits (01-13/01-14/01-15 sites), resolved_at, and verified_by quoting the operator-observed lines with line numbers/counts (match the G-01-8 resolution's structure); if it failed, keep status open and extend root_cause with the residual mechanism the new discriminator lines named, updating the missing list to the next levers. Update Test 3's note: SC-3 pairwise judgments, QVGA restore, reboot boot-resolution outcomes; flip Test 3 to pass only if every clause it holds is now judged.
  2. WINDOWS.md: update entries 3 (G-01-7), 5 (G-01-9), 6 (retry overrun) — status fixed with the evidence reason, or the reason field carries the honest residual; update BOTH the markdown table and the JSON copy; reconcile open_count/fixed_count/total_count in the front matter.
  3. STATE.md: record the round outcome in Current Status (which gaps closed on what evidence, what remains open if anything), and set Next Steps to the security gate (/gsd-secure-phase 1) as the remaining phase-close step, then /gsd-progress.
  4. Commit the ledger updates separately from any log retention, message docs(01-16): bench re-verification evidence + ledger flips.
  </action>
  <verify>
  <automated>powershell -Command "if((Select-String -Path .planning/phases/01-command-protocol-control/01-UAT.md -Pattern 'G-01-7').Count -lt 2){exit 1}" ; powershell -Command "if((Select-String -Path .planning/WINDOWS.md -Pattern '\"id\": 3').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path .planning/WINDOWS.md -Pattern '\"id\": 6').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path .planning/STATE.md -Pattern 'secure-phase').Count -lt 1){exit 1}"</automated>
  </verify>
  <acceptance_criteria>
  - Every status flip cites operator-observed quotes with line numbers/counts (no code-level-only closure); failed series leave their gap open with the residual mechanism named in the new discriminator vocabulary
  - WINDOWS.md table and JSON copies agree; front-matter counts equal the actual row statuses
  - STATE.md names /gsd-secure-phase 1 as the remaining phase-close gate
  - Test 3 reflects the SC-3/QVGA/reboot outcomes; Tests 1/2/4/5 notes untouched (no regression evidence to record)
  </acceptance_criteria>
  <done>The gap ledgers reflect hardware truth: closures evidenced, residuals named, ship-blocking WINDOWS entries updated, and the phase's remaining gate identified.</done>
  <reversibility rating="reversible">Documentation-only ledger updates.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench operator -> firmware flash + consoles | Human performs the flash and observes; logs are retained as evidence (read-only artifacts) |
| RF pair -> bench evidence | The two-radio link under bench conditions produces the verdicts; no untrusted input enters the firmware beyond the (validated) RF frames already covered in 01-13/01-15 threat models |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-16-01 | Tampering | evidence integrity | medium | mitigate | House convention enforced: closures quote verbatim console lines with line numbers/counts from retained logs (balloon5.log/base5.log); no closure from paraphrase or code reasoning |
| T-01-16-02 | DoS | mixed-firmware flash | high | mitigate | Blocking checkpoint step 1 flashes BOTH boards before any traffic; the wire change makes a mixed pair self-evident (zero chunk framing) rather than silently corrupt |
| T-01-16-03 | Safety | bench hardware | low | accept | Standard bench rig (USB-powered base, battery balloon at desk); WR-01 battery watch green in all prior sessions; nothing in this round changes power paths |
</threat_model>

## Artifacts this phase produces

- balloon5.log / base5.log — retained bench console evidence (repo root, prior-session convention)
- Updated gap blocks G-01-7/G-01-9 + Test 3 note (01-UAT.md)
- Updated WINDOWS entries 3/5/6 with reconciled counts (.planning/WINDOWS.md)
- Round outcome + next-gate record (.planning/STATE.md)
- No source files, no new symbols, no env vars

<verification>
- Both firmware targets built green and harness exited 0 immediately before the flash (pre-flight step, on record in the session)
- Every ledger flip traces to a quotable line in the retained logs
- WINDOWS counts reconcile with row statuses; /gsd-ship remains blocked exactly as long as an entry is honestly open
</verification>

<success_criteria>
- Series A acceptance truth finally exercised on remediated firmware: 6/6 COMPLETE, or the residual named with its discriminating evidence
- G-01-9's three defects each evidenced fixed (or routed) at bench: sized thumbs, zero 100-percent CRC mismatches, no silent full loss
- Entry 6 confirmed at bench: truthful retry ordinals, no post-terminal re-arm traffic
- SC-3 riding clauses judged; Test 3 status reflects reality
- Ledgers consistent; phase's remaining gate (/gsd-secure-phase 1) named
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-16-SUMMARY.md` when done
</output>
