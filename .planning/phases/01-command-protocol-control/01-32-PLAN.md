---
phase: 01-command-protocol-control
plan: 32
type: execute
wave: 24
depends_on: []
files_modified:
  - src/main_balloon.cpp
  - src/image_tx_manager.cpp
  - .planning/debug/d1-crash-regression-push-start.md
  - .planning/WINDOWS.md
  - .planning/phases/01-command-protocol-control/01-UAT.md
autonomous: true
gap_closure: true
requirements: [CTRL-01, CTRL-06, PRI-02]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low   # calibration store has 0 samples (factor 1, not applied); mirrors the 01-30 implementation half — debug doc §10.5 fully names the package, so this round has no audit task

must_haves:
  truths:
    - statement: "The [STACK] instrument (debug doc §10.5 item 1, the round's designed discriminator) is wired: the existing idle0TickHook in src/main_balloon.cpp samples uxTaskGetStackHighWaterMark(NULL) — the hook runs in IDLE0's context, so NULL resolves to IDLE0 — throttled by its own tick count (every 1024 increments of s_idle0TickCount, ~1 s of IDLE0-run time at the 1000 Hz tick), maintaining a new-low latch; the existing 1 Hz sample block prints a '[IDLE0] stack watermark' line when the watermark improves (first print = the boot baseline); both new constructs carry G-01-10 + §10.5 citations and the [MEM]-convention REMOVAL CONDITION in-source"
      verification: explicit
    - statement: "The [TWDT] handle fix (§10.5 item 2) landed at src/image_tx_manager.cpp:172 — esp_task_wdt_status(xTaskGetIdleTaskHandleForCPU(0)) replaces the core-ambiguous xTaskGetIdleTaskHandle() — AND the deployed WDT config is re-derived from the pinned framework's sdkconfig artifacts with file paths recorded in debug doc §11 (CONFIG_ESP_TASK_WDT_CHECK_IDLE_TASK_CPU0, CONFIG_ESP_TASK_WDT_INIT, CONFIG_ESP_TASK_WDT_TIMEOUT_S), settling in writing what session 10 could not (the two-reading question: wrong-handle instrument bug vs genuinely-unsubscribed IDLE0)"
      verification: explicit
    - statement: "The [IDLE0] registered printf bug (§10.5 item 2) is fixed at src/main_balloon.cpp:236-237: the esp_err_t return of esp_register_freertos_idle_hook_for_cpu is captured in an esp_err_t and the line prints registered=1 if and only if the return equals ESP_OK — success and failure are now distinguishable on the console"
      verification: explicit
    - statement: "The [I2C] write-path coverage gap (§10.5 item 3) is dispositioned by the plan's decision tree executed in source: if the OLED/Wire-0 write path exposes an observable failure return in src/status_display.cpp's stack, a latch-guarded probe branch prints '[I2C] write-path failure ... (G-01-10)' with the same probe verdicts and episode/probe counters; if the deployed HAL/library provably swallows write errors end-to-end, that finding is recorded in §11 with source paths as the coverage answer — either way the session-10 gap ('the instrument wired only to the invalid-READING path') is closed in writing"
      verification: explicit
    - statement: "The optional hook-unregistered A/B question (§10.5 item 4) is made cheap to kill: the registration call is guarded by a G01_D1_IDLE_HOOK_DISABLED preprocessor macro (default build unchanged, hook body still compiled), and §11 documents producing the A/B image by adding -DG01_D1_IDLE_HOOK_DISABLED=1 to the esp32-s3-balloon env build_flags — no bench-time code edit required"
      verification: explicit
    - statement: "Both firmware targets build green and the wire-format harness exits 0 on the round's final commit; every retained surface stays byte-intact: Lever A (e32_lora.cpp), B1 [BOOT] + B2 [LOOP], the [MEM] instrumentation, the round-#10 discriminator lines (budget re-arm, class-5 eviction labels, base deadline release), the D2 receipt-ever flag, the round-#13 instruments ([IDLE0] counter + frozen latch, [I2C] reading-path probe, [TWDT] one-shot), CMD_TX_CHANNEL_QUIET_MS = 750, D-05/D-07 pacing, the wire format"
      verification: explicit
    - statement: "Ledger honesty: WINDOWS entry 15 and 01-UAT.md G-01-10 carry the round-#14 instrument-package determination (root_cause/missing extended, the §10.5 items marked shipped) but stay OPEN — the crash truth and the hypothesis verdict flip only on 01-34's hardware evidence; §11 names the exact log lines 01-34 greps; the SUMMARY makes no bench claims"
      verification: explicit
  artifacts:
    - "src/main_balloon.cpp — the [STACK] watermark sampling in idle0TickHook + the new-low print in the 1 Hz block; the fixed registered= line; the [I2C] write-path branch (or its recorded swallowing disposition); the G01_D1_IDLE_HOOK_DISABLED guard"
    - "src/image_tx_manager.cpp — the [TWDT] ForCPU(0) handle fix"
    - ".planning/debug/d1-crash-regression-push-start.md — §11 appended: the implementation record, the WDT-config re-derivation with paths, the 01-34 discriminator-line table, the A/B build instructions, the selection-provenance record (sixth pending operator confirmation), the no-D1-lever statement"
    - ".planning/WINDOWS.md — entry 15 reason extended with the round-#14 record; stays open; counts unchanged (2/17/19)"
    - ".planning/phases/01-command-protocol-control/01-UAT.md — G-01-10 root_cause + missing extended with the round-#14 package shipped; stays open"
    - ".planning/phases/01-command-protocol-control/01-32-SUMMARY.md"
  key_links:
    - "The [STACK] instrument <-> 01-34's D1 acceptance: the bench session's designed deliverable is the watermark trajectory (baseline -> new lows -> value at the crash instant or session minimum), which CONFIRMs/REFUTEs §10.5's labeled stack-capacity hypothesis in one session — §11's discriminator table is exactly what the bench greps"
    - "The [TWDT] fix <-> §7's WDT-chain reasoning: the ForCPU(0) handle + the re-derived config decide whether §7's 'only IDLE0 feeds the TWDT' premise stands (ESP_OK expected) or falls (ESP_ERR_NOT_FOUND real) — until 01-34 reads the line, no WDT-chain conclusion changes"
    - "New instruments <-> the retained round-#13 instruments: [STACK] EXTENDS the [IDLE0] hook (the counter and frozen latch stay verbatim); nothing is stripped — the removal condition (strips WITH the [MEM]/B1/B2 family after G-01-10 closes on bench evidence) is carried on every new construct"
    - "The no-D1-lever guard <-> 01-33: the G-01-7 admission/eviction lever (separate gap, named by the session-10 discriminator census) may land in this round's wake, but NO D1 lever ships here — the D1 fix decision waits for the watermark answer per §10.5 item 5"
  prohibitions:
    - statement: "MUST NOT ship any D1 fix lever in this plan — §10.5 item 5's standing guard: three elimination-only rounds plus two bench-disconfirmed levers make lever-speculation ahead of the watermark evidence dishonest; the ONLY code changes are the four instrument items + the A/B macro"
      status: flagged
      verification: backstop
    - statement: "MUST NOT flip WINDOWS 15 or G-01-10 to resolved — crash-truth closure requires 01-34's hardware session (survival ladder + the [STACK]/[TWDT]/[I2C] readings)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT delete, mute, or alter any retained discriminator surface: Lever A, B1 [BOOT], B2 [LOOP], [MEM], the round-#10 lines, the D2 receipt-ever flag, the round-#13 [IDLE0] counter/frozen latch + [I2C] reading-path probe, CMD_TX_CHANNEL_QUIET_MS = 750, D-05/D-07 pacing, the wire format — the harness must exit 0 unchanged"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT add unbounded or unlatched printing to the idle-hook or 1 Hz paths — the [STACK] sample is throttled inside the hook and prints only on a new low from the 1 Hz block (the [MEM] convention), so the instrument cannot perturb the timing it observes"
      status: flagged
      verification: backstop
    - statement: "MUST NOT apply the §7.6-rejected WDT-config lever (lengthen CONFIG_ESP_TASK_WDT_TIMEOUT_S / drop stage-1) — the config is RE-READ and recorded this round, never edited; the rejection stands"
      status: flagged
      verification: backstop
---

<objective>
Land the D1 debug round #4 instrument package (G-01-10 / WINDOWS 15, 01-VERIFICATION.md gap 1, the phase's top blocker) exactly as debug doc §10.5 names it: the [STACK] IDLE0-watermark instrument — the one-session discriminator for the labeled stack-capacity hypothesis — plus the [TWDT] handle fix, the [IDLE0] registered printf fix, the [I2C] write-path coverage, and the A/B-ready hook guard. Code + builds + harness only; hardware acceptance and the hypothesis verdict belong to 01-34's bench session. NO D1 lever ships (§10.5 item 5's guard holds until [STACK] answers).

Purpose: session 10's crash (the FOURTH expression — an IDLE0 stack-canary panic with a full dump at chunk 12/14 of image 43's re-armed window) answered the round-#13 instruments in a way that re-scoped the campaign: starvation is DISCONFIRMED (the [IDLE0] counter ran to the crash instant), the [TWDT] probe returned ESP_ERR_NOT_FOUND at both boots putting §7's WDT-topology premise under re-examination, and the [I2C] instrument missed its trigger (write-path coverage gap). Session 10 also exposed three of the round's own instrument defects (the printf bug, the ambiguous handle, the coverage gap) — routed here per the no-inline-fix prohibition. The [STACK] watermark is the designed discriminator between the labeled family's two visible signatures: a stack-capacity breach under IRQ pressure during heavy TX (canary sessions 7/10) vs the scheduler/tick wedge (watchdog sessions 8/9).

Output: the five §10.5 items wired green on both targets with the harness passing; debug doc §11 recording the implementation, the WDT-config re-derivation with paths, the 01-34 discriminator-line table, and the A/B instructions; WINDOWS 15 and G-01-10 carrying the round-#14 determination, honestly still open.

Multi-source coverage audit: GOAL/REQ covered — this plan addresses the D1 blocker gating CTRL-01/CTRL-06/PRI-02's hardware truths; RESEARCH/debug-doc item covered — §10.5 items 1-4 (+5 as the macro); CONTEXT — no new D-XX decisions this round; the probe-fallback surface (11 unresolved spec-less rows) is authored in full in 01-34's must_haves.flagged_assumptions (no silent drops). Prohibition recall: kept prohibitions are authored descriptor-less under must_haves.prohibitions below (canon: UAT Test 5, §7.6 rejection, the 01-30/01-31 prohibition sets, the §10.5 guard).

<assumption_delta_decision>
The deterministic assumption-delta scan fired on the Phase 1 ROADMAP section with the same two signals rounds #11, #12, and #13 dispositioned: "second" (round-prose "disconfirmed as sufficient a second time") and "fallback" (the executed 01-14 plan description "never-wedge fallback") — both trace to completed-plan prose in the roadmap listing, not to round #14's scope. Decision: no-change — this round wires diagnostic instruments onto the existing single-pipeline architecture, introducing no second platform, auth method, tenant, region, or source of truth. No identity-model promotion is warranted; no invariant test added (the [BOOT]/[LOOP]/[MEM]/[IDLE0]/[TWDT]/[I2C]/[STACK] instrument family already encodes the crash-regression tripwire).
</assumption_delta_decision>
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/debug/d1-crash-regression-push-start.md
@.planning/phases/01-command-protocol-control/01-30-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-31-SUMMARY.md
@.planning/WINDOWS.md

Binding evidence and constraints:
- The round-#4 brief is debug doc §10.5 (lines ~1452-1463): (1) [STACK] instrument — uxTaskGetStackHighWaterMark(NULL) from the existing [IDLE0] idle hook at 1 Hz with a new-low latch (the [MEM] pattern applied to IDLE0); (2) fix the [TWDT] handle (xTaskGetIdleTaskHandleForCPU(0)) and the [IDLE0] registered printf (print err == ESP_OK); (3) extend the [I2C] probe to the write-failure path; (4) optional A/B: idle hook unregistered; (5) NO lever until [STACK] answers.
- Instrument sites at HEAD (verifier-read at re-verification #12): src/main_balloon.cpp:137-141 (s_idle0TickCount + idle0TickHook), :236-237 (registration + the printf bug — `bool idle0HookOk = esp_register_freertos_idle_hook_for_cpu(idle0TickHook, 0); Serial0.printf("[IDLE0] hook cpu0 registered=%d (G-01-10)\n", idle0HookOk ? 1 : 0);` — esp_err_t (ESP_OK=0) assigned to bool prints 0 on SUCCESS), :340-365 (the 1 Hz sample block with the idle0FrozenLogged latch), :843-875 (the [I2C] probe on the BMP280-invalid READING path — i2c_master_probe on i2cBusHandle(0), latch-guarded, episode/probe counters); src/image_tx_manager.cpp:159-172 (the [TWDT] one-shot — esp_task_wdt_status(xTaskGetIdleTaskHandle())).
- Session-10 facts the package answers (debug doc §10.3, verifier-reproduced): [TWDT] = ESP_ERR_NOT_FOUND at BOTH boots (balloon4.log:105/:1577) — wrong-handle (xTaskGetIdleTaskHandle() from CPU1 setup context returns IDLE1) vs genuinely-unsubscribed, NOT discriminated; the config artifacts were not re-derivable from the retained build dir (no sdkconfig artifact in .pio/build/esp32-s3-balloon/) — the re-derivation must use the PINNED FRAMEWORK's sdkconfig under the PlatformIO packages dir (framework-arduinoespressif32/tools/sdk/esp32s3/), recording the exact path; [I2C] did not fire — the session's failure (:1093) was on the HAL WRITE path (esp32-hal-i2c-ng.c:275 i2cWrite) while the instrument watches the invalid-READING transition (main_balloon.cpp:836-866).
- The session-10 crash context for §11's discriminator table: the canary fired at the campaign's TX-heaviest stretch (AUX-low-missed ×81, 119-chunk CIF-class window mid-service); [IDLE0] had ZERO frozen lines whole-console (starvation disconfirmed); [MEM] healthy a fourth session (loopTask stackHW 5772 constant — the breached stack is IDLE0's, which [MEM] does not monitor).
- House conventions: evidence quotes carry file:line; every new instrument line carries the G-01-10 citation + a REMOVAL CONDITION (strips WITH the [MEM]/B1/B2 family after G-01-10 closes on bench evidence — currently 8 occurrences across the two files, 4 per file); the auto-advance convention (01-23/01-25/01-26/01-28/01-30): the fix-shape selection auto-advanced under workflow.auto_advance=true is recorded as pending end-of-phase operator confirmation (this round joins as the SIXTH — the selection was pre-named by §10.5 + STATE.md's round-#14 routing).
- Scope guards: the 01-18 quiet gate (CMD_TX_CHANNEL_QUIET_MS 750 in include/command_sender.h) and the 01-15/01-19 command-path semantics are protected surfaces; scripts/verify_protocol_roundtrip.mjs is the wire-format tripwire; the §7.6 WDT-config rejection stands (read the config, never edit it).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the §10.5 instrument package — [STACK] watermark, [TWDT] ForCPU(0) fix, [IDLE0] printf fix, [I2C] write-path coverage, hook-guard A/B macro</name>
  <files>src/main_balloon.cpp, src/image_tx_manager.cpp</files>
  <read_first>
  - .planning/debug/d1-crash-regression-push-start.md — §10 in full (the fourth expression, §10.3's instrument answers with the printf-bug diagnosis and the two-reading [TWDT] question, §10.5's itemized round-#14 routing this task implements)
  - src/main_balloon.cpp — :130-145 (the idle hook body + s_idle0TickCount), :230-245 (the registration site + printf bug), :330-370 (the 1 Hz block: lastOledMs gate, the [IDLE0] sample + frozen latch, where the new-low print goes), :830-880 (the [I2C] reading-path probe: the latch, counters, i2c_master_probe call, verdict switch, log line)
  - src/image_tx_manager.cpp — :155-175 (the [TWDT] one-shot + B1 beside it)
  - src/status_display.cpp + include/status_display.h — the OLED refresh path: which library/class sends the 1 Hz frame over Wire-0, and whether a write failure is observable from project code (a return value, TwoWire's error accessor, or nothing)
  - src/sensor_manager.cpp — the BMP280 read path (what makes isBMP280Ready() flip false — the existing instrument's trigger) for contrast with the write path
  - The pinned framework headers under the PlatformIO packages dir: FreeRTOS task.h (confirm xTaskGetIdleTaskHandleForCPU(CPU_NUM) exists in the pinned arduino-esp32 core; if the ForCPU variant is absent, name the pinned core's documented equivalent in §11 before substituting), esp_freertos_hooks.h:44 (the esp_err_t return — the printf-bug ground truth), esp32-hal-i2c-ng.c (the i2cWrite error path: what it logs, what it returns, whether any project-observable surface exists)
  - scripts/verify_protocol_roundtrip.mjs — the wire tripwire that must exit 0 unchanged
  </read_first>
  <action>
  Implement the five §10.5 items, each new construct commented with its G-01-10 citation, its §10.5 item number, and the REMOVAL CONDITION (strips WITH the [MEM]/B1/B2 family after G-01-10 closes on bench evidence):
  1. [STACK] (item 1) in src/main_balloon.cpp: inside idle0TickHook, after the existing s_idle0TickCount++ increment, add a throttle-gated sample — when (s_idle0TickCount & 0x3FF) == 0 (every 1024 idle ticks, ~1 s of IDLE0-run time), read uxTaskGetStackHighWaterMark(NULL) (the hook executes in IDLE0's context, so NULL is IDLE0 per §10.5) into a new static volatile UBaseType_t s_idle0StackMinWords that keeps the running minimum (initialize to 0 = unknown; the first sample sets it). In the existing 1 Hz block beside the idle0Delta logic, add the new-low print: when s_idle0StackMinWords is nonzero and smaller than the last printed value, Serial0.printf("[IDLE0] stack watermark %u words free (new low, t=%lu ms) (G-01-10)\n", ...) — the first print is the boot baseline; track the last-printed value in a static. Do NOT print per-sample (the print is new-low-latched from the 1 Hz loopTask block, never from the hook — the [MEM] convention that keeps the instrument from perturbing what it measures). Leave the existing counter + frozen-latch logic byte-identical.
  2. [TWDT] (item 2) in src/image_tx_manager.cpp:172: change esp_task_wdt_status(xTaskGetIdleTaskHandle()) to esp_task_wdt_status(xTaskGetIdleTaskHandleForCPU(0)) — the explicit CPU0 idle handle that removes the core ambiguity of calling from CPU1 setup() context. Keep the log line format, the esp_err_to_name rendering, the comment, and the REMOVAL CONDITION verbatim except for the handle call and a one-line comment naming the §10.5 fix. Confirm the pinned core's task.h declares xTaskGetIdleTaskHandleForCPU; if absent, stop and record the finding in §11 instead of substituting silently.
  3. [IDLE0] printf fix (item 2) in src/main_balloon.cpp:236-237: capture the registration result as esp_err_t idle0HookErr = esp_register_freertos_idle_hook_for_cpu(idle0TickHook, 0); and print Serial0.printf("[IDLE0] hook cpu0 registered=%d (G-01-10)\n", idle0HookErr == ESP_OK ? 1 : 0); — success now reads registered=1, and a genuine failure reads registered=0. Update the adjacent comment to cite §10.5's printf-bug diagnosis.
  4. [I2C] write-path coverage (item 3) in src/main_balloon.cpp, decided by what the status_display stack actually exposes: (a) if an observable failure return exists on the 1 Hz OLED transmit path (library return value or TwoWire error accessor), add a sibling latch + episode/probe counters that, on an observed write failure, run the same i2c_master_probe((i2c_master_bus_handle_t)i2cBusHandle(0), 0x76, 50) verdict and print Serial0.printf("[I2C] write-path failure t=%lu ms - probe 0x76 -> %s (episodes=%lu probes=%lu) (G-01-10)\n", ...) — latch-guarded one line per episode, re-armed on a healthy subsequent write, mirroring the reading-path instrument's structure; (b) if the write path provably swallows errors end-to-end (nothing surfaces to project code), do NOT add a speculative poller — record the swallowing finding with the HAL/library source paths in §11 as the coverage answer (§10.3's own alternative: "or verify the HAL swallows write errors"). Whichever branch executes, the reading-path instrument stays untouched.
  5. A/B guard (item 4) in src/main_balloon.cpp: wrap ONLY the registration call (item 3's line pair) in #ifndef G01_D1_IDLE_HOOK_DISABLED / #endif — the hook body stays compiled either way; the default build is byte-identical in behavior. A one-line comment explains the A/B purpose (§10.5 item 4: rule the round-#13 hook dispatch in or out of the fault family).
  6. Verify: pio run -e esp32-s3-balloon -e esp32-s3-basestation (2/2 SUCCESS) and node scripts/verify_protocol_roundtrip.mjs (exit 0). Then confirm the retained-surface greps still pass: CMD_TX_CHANNEL_QUIET_MS = 750 in include/command_sender.h; uart_ll_is_tx_idle ×3 in src/e32_lora.cpp; [MEM] mentions in src/image_tx_manager.cpp; the round-#10 discriminator lines present; the REMOVAL CONDITION count in the two touched files grows by exactly the new constructs (was 8).
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation 2>&1 | grep -c "SUCCESS" | grep -qv "^0$" && node scripts/verify_protocol_roundtrip.mjs && grep -q "uxTaskGetStackHighWaterMark(NULL)" src/main_balloon.cpp && grep -q "stack watermark" src/main_balloon.cpp && grep -q "xTaskGetIdleTaskHandleForCPU(0)" src/image_tx_manager.cpp && grep -q "idle0HookErr == ESP_OK" src/main_balloon.cpp && grep -q "G01_D1_IDLE_HOOK_DISABLED" src/main_balloon.cpp && grep -q "CMD_TX_CHANNEL_QUIET_MS = 750" include/command_sender.h && grep -c "uart_ll_is_tx_idle" src/e32_lora.cpp | grep -qv "^0$"</automated>
  </verify>
  <acceptance_criteria>
  - Both targets build SUCCESS and the harness exits 0 on this task's commit
  - idle0TickHook contains the throttled uxTaskGetStackHighWaterMark(NULL) sample with a running-minimum latch, and the 1 Hz block prints the '[IDLE0] stack watermark' new-low line — the existing counter/frozen-latch logic unchanged
  - image_tx_manager.cpp:172 calls esp_task_wdt_status on xTaskGetIdleTaskHandleForCPU(0); the log-line format and REMOVAL CONDITION survive
  - The registered= line derives from an esp_err_t compared against ESP_OK (registered=1 on success)
  - Either the '[I2C] write-path failure' branch exists latch-guarded at an observable write-failure site, or the swallowing finding is staged for §11 with the source paths — no unbounded poller added
  - G01_D1_IDLE_HOOK_DISABLED guards only the registration call; the default build behavior is unchanged
  - Protected surfaces intact: CMD_TX_CHANNEL_QUIET_MS = 750, Lever A, [MEM], the round-#10 lines, the D2 flag, the round-#13 instruments
  </acceptance_criteria>
  <done>The five §10.5 items are wired in source with the house instrument conventions, both targets and the harness are green, and every retained discriminator surface survives verbatim.</done>
  <reversibility rating="costly">The touched sites sit in the balloon's idle hook and 1 Hz loop — exactly the paths the crash family lives on; a perturbing instrument would surface only at the bench, hence the throttle/latch requirements and the no-D1-lever guard.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Debug doc §11 implementation record + the 01-34 discriminator table; ledgers extended honestly (no flips); commit</name>
  <files>.planning/debug/d1-crash-regression-push-start.md, .planning/WINDOWS.md, .planning/phases/01-command-protocol-control/01-UAT.md</files>
  <read_first>
  - .planning/debug/d1-crash-regression-push-start.md — §9.6 and §10.5 (the instrument-naming and routing conventions §11 follows), §10.3 (the defects this round fixes and the questions it settles in writing)
  - src/main_balloon.cpp + src/image_tx_manager.cpp — Task 1's landed constructs (quote the exact line numbers and log-line formats)
  - The pinned framework's sdkconfig under the PlatformIO packages dir (framework-arduinoespressif32/tools/sdk/esp32s3/) — the WDT-config re-derivation source
  - .planning/WINDOWS.md — entry 15 (extend the reason; stay open; counts stay 2/17/19)
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-10 (root_cause round-#14 extension + the §10.5 items marked shipped in missing)
  </read_first>
  <action>
  Write §11 (ELEVEN — ROUND-#14 IMPLEMENTATION RECORD) into the debug doc in the house convention:
  1. PROVENANCE: the round's commit SHA(s), the touched files with line ranges, and the statement that the package is implementation-only — NO D1 lever shipped (§10.5 item 5's guard), the D1 fix decision explicitly deferred until the watermark answers at 01-34.
  2. WDT-CONFIG RE-DERIVATION: the deployed-config facts relevant to the [TWDT] question — CONFIG_ESP_TASK_WDT_INIT / CONFIG_ESP_TASK_WDT_CHECK_IDLE_TASK_CPU0 / CONFIG_ESP_TASK_WDT_TIMEOUT_S — read from the pinned framework's sdkconfig with the exact file path quoted, reconciled against §7.1's topology reading; state which of §10.3's two readings the config predicts (wrong-handle bug if CPU0-subscription is configured, making ESP_OK the expected 01-34 reading) and that the live boot line remains the deciding evidence.
  3. THE 01-34 DISCRIMINATOR TABLE: every new/changed log line with its exact format string, its print site and cadence, its trigger, and its REMOVAL CONDITION — the [STACK] baseline + new-low lines (predicted signature under the stack-capacity hypothesis: new lows accelerating toward 0 words under heavy TX before a canary panic; under the wedge hypothesis: a healthy constant margin at the crash instant), the fixed registered= line (1 expected), the [TWDT] ForCPU(0) line (ESP_OK expected under the config reading; ESP_ERR_NOT_FOUND would falsify §7's premise and re-open the sessions-8/9 TG0WDT attribution), the [I2C] write-path line (or the recorded swallowing disposition).
  4. THE A/B INSTRUCTIONS: produce the A/B image by adding -DG01_D1_IDLE_HOOK_DISABLED=1 to the esp32-s3-balloon env build_flags, rebuild, flash, compare — when to reach for it (if 01-34's watermark shows healthy margin through a crash, the hook-dispatch question rises in rank).
  5. THE SELECTION-PROVENANCE RECORD: per the 01-23/01-25/01-26/01-28/01-30 convention, the fix-shape selection (the §10.5 package verbatim, option b-instrument+fix lineage) was pre-named by debug doc §10.5 and STATE.md's round-#14 routing and auto-advanced under workflow.auto_advance=true — recorded as pending end-of-phase operator confirmation, the SIXTH of the campaign.
  Then the ledgers: extend WINDOWS entry 15's reason with the round-#14 record (the five items shipped, the config re-derivation verdict, entry stays open, counts unchanged 2/17/19, BOTH the table and JSON copies); extend 01-UAT.md G-01-10's root_cause with the round-#14 determination and mark the §10.5 items shipped in its missing list (status stays open). Neither ledger claims the crash fixed or the hypothesis decided. Commit docs+code separately as the round requires: fix(01-32): D1 debug round #4 — the [STACK]/[TWDT]/[I2C] instrument package per debug doc §10.5, then docs(01-32): round-#14 record + ledgers.
  </action>
  <verify>
  <automated>grep -q "^## 11" .planning/debug/d1-crash-regression-push-start.md && grep -qi "CONFIG_ESP_TASK_WDT" .planning/debug/d1-crash-regression-push-start.md && grep -qi "G01_D1_IDLE_HOOK_DISABLED" .planning/debug/d1-crash-regression-push-start.md && grep -q "open_count: 2" .planning/WINDOWS.md && grep -qi "round-#14\|round #14" .planning/WINDOWS.md && grep -qi "round-#14\|round #14" .planning/phases/01-command-protocol-control/01-UAT.md</automated>
  </verify>
  <acceptance_criteria>
  - §11 exists with the five subsections; the WDT-config values carry the framework-sdkconfig file path; the discriminator table quotes the exact format strings from source
  - The no-D1-lever statement is explicit in §11 and neither ledger claims a crash fix or a hypothesis verdict
  - WINDOWS entry 15 extended in BOTH copies, still open, counts 2/17/19; 01-UAT.md G-01-10 still open with the round-#14 extension and the shipped-items markers
  - The selection-provenance record names the auto-advance convention and the sixth-pending-confirmation lineage
  - The SUMMARY makes no bench claims — the round's hardware acceptance belongs to 01-34
  </acceptance_criteria>
  <done>The round's record is durable: §11 carries the implementation, the config re-derivation, the bench discriminator table, the A/B recipe, and the provenance; the ledgers carry the round-#14 determination honestly open.</done>
  <reversibility rating="reversible">Documentation and ledger updates only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crash evidence -> instrument design | Retained consoles (balloon4.log) + the verifier-reproduced ELF SHA acce78241 are the only ground truth; framework sources and the pinned sdkconfig are read-only reference material — no untrusted input enters the firmware in this round |
| instrumentation -> the idle path it observes | The instruments execute inside the exact loop the fault family lives on; throttle/latch bounds + the harness + protected-surface greps are the regression bound |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-32-01 | Tampering | evidence integrity | medium | mitigate | §11 claims carry file:line quotes and the framework-sdkconfig path; the WDT-config reading is reconciled against §7.1 so the re-derivation is checkable, not asserted |
| T-01-32-02 | DoS | a D1 lever smuggled in ahead of the watermark evidence | high | mitigate | The §10.5 item-5 guard is a prohibition with a backstop verification; the task actions enumerate the ONLY permitted code changes (five items) |
| T-01-32-03 | DoS | instrumentation perturbing the idle-path timing it measures | high | mitigate | The [STACK] sample is tick-throttled inside the hook and new-low-latched at print; no per-sample prints; the harness + builds gate regressions |
| T-01-32-04 | DoS | the A/B macro accidentally flipping the default build | medium | mitigate | The guard wraps only the registration call; the default build behavior is unchanged and the builds gate proves it compiles clean both ways |
| T-01-32-05 | Safety | bench hardware | low | accept | No hardware work in this plan (code + docs only); the bench rig risk stays with 01-34's protocol |

## Artifacts this phase produces

- The [STACK] instrument: s_idle0StackMinWords latch + the throttled in-hook sample + the '[IDLE0] stack watermark %u words free (new low, t=%lu ms) (G-01-10)' line format in src/main_balloon.cpp
- The [TWDT] fixed call esp_task_wdt_status(xTaskGetIdleTaskHandleForCPU(0)) in src/image_tx_manager.cpp (log format unchanged)
- The fixed registration print: esp_err_t idle0HookErr + 'registered=%d' now 1 on ESP_OK, src/main_balloon.cpp
- The [I2C] write-path branch + '[I2C] write-path failure t=%lu ms - probe 0x76 -> %s (episodes=%lu probes=%lu) (G-01-10)' line format (or the recorded swallowing disposition in §11)
- The G01_D1_IDLE_HOOK_DISABLED preprocessor guard (new macro name)
- Debug doc §11; WINDOWS entry 15 round-#14 extension; 01-UAT.md G-01-10 round-#14 extension
- .planning/phases/01-command-protocol-control/01-32-SUMMARY.md
- No new modules, no env vars, no wire-format changes, no new source files

<verification>
- Both targets build 2/2 and the harness exits 0 on the final commit; protected-surface greps clean
- The five §10.5 items are each verifiable in source by the task's automated greps; §11 names every discriminator line 01-34 will grep
- WINDOWS 15 / G-01-10 open with the round-#14 determination recorded — zero code-level flips, no bench claims
</verification>

<success_criteria>
- The [STACK] watermark instrument — the round's designed one-session discriminator — is armed, bounded, and removal-conditioned, with its predicted signatures under both labeled hypotheses written down before the bench reads it
- The three session-10 instrument defects (printf bug, ambiguous handle, write-path coverage gap) are fixed or honestly dispositioned in writing, and the WDT-config question is settled on paper pending the live line
- Every riding truth's gate is now the single 01-34 bench session, which reads exactly the lines §11 tabulates
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-32-SUMMARY.md` when done
</output>
