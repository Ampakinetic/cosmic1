---
phase: 01-command-protocol-control
plan: 15
type: execute
wave: 11
depends_on: ["01-13"]
files_modified:
  - include/image_rx_manager.h
  - src/image_rx_manager.cpp
  - src/command_sender.cpp
autonomous: true
gap_closure: true
requirements: [CTRL-06, PRI-02, IMG-03]

estimate:
  tokens: 13000
  raw_tokens: 13000
  tasks: 2
  confidence: low   # calibration store has 0 samples (factor 1, not applied); one cross-module defer-aware guard + one sender state-machine change

must_haves:
  truths:
    - "G-01-7 rescoped lever (defer-aware D-24 pass accounting): a stall never charges a retransmit pass or queues a duplicate window request while the transfer's outstanding IMAGE_WINDOW_REQUEST is still non-terminal (PENDING or SENT) — the request had no transfer opportunity (NACK-BUSY-deferred by the balloon's one-at-a-time guard, ACK-lost, or still backoff-paced), so the stall clock extends instead (lastProgressMs refresh, log-once per sequence) and the D-24 passCount is charged only once the request reaches a terminal state (session 4's failure: image 7 thumb 35/36 exhausted 3 passes on deferred+retried requests, balloon4.log:1154/:1157)"
    - "WR-04: the base never queues an overlapping window request while the prior one is still tracked-and-non-terminal for the same transfer — the duplicate-request churn that consumed MAX_PENDING_COMMANDS slots, collided on the half-duplex link, and re-armed already-serviced windows is suppressed at both stall sites and by cancelling the serviced request at window-complete advance"
    - "WR-05 (window-request class): a NACK_BUSY response to an IMAGE_WINDOW_REQUEST no longer terminalizes the command — it defers (state PENDING, retryCount advanced, backoff-paced retransmit within the existing D-05/D-07 budget, response not stored, pendingCommandCount untouched); only budget exhaustion or a non-BUSY NACK terminalizes. All other command classes keep today's terminal FAILED semantics exactly (CAPTURE_NOW/SET_* NACK_BUSY behavior unchanged)"
    - "Entry 6 / IN-01: the retry log prints the retry ordinal against maxRetries — an attempt-ordinal label can no longer be produced by retryCommand; the retry bound itself (retryCount reaching maxRetries, check at the timeout branch) is unchanged and now label-consistent"
    - "Post-terminal re-arm churn: finalizeTransfer and finalizeIncomplete cancel the transfer's outstanding window request (cancelCommand on the tracked seq), and the window-complete advance cancels the prior request before issuing the next — chunks for a finalized image re-armed by a late retry cannot occur because the retry no longer exists (session 4: 14-16 post-terminal ignores on image 11, base4.log:3286-3310)"
    - "D-24's bound semantics are otherwise unchanged: passCount still counts only stall-triggered re-requests after a terminal request outcome, acceptChunk remains the sole passCount zero-writer, and the 3-pass bound still finalizes a genuinely unhealable transfer INCOMPLETE (PRI-03 honest degradation intact)"
    - "Both firmware targets build green and the wire harness stays green (no wire change in this plan; the PRI-01 order gates untouched)"
  artifacts:
    - "include/image_rx_manager.h — ImageRxTransfer field uint16_t windowRequestSeq (0 = none) + private helper windowRequestInFlight"
    - "src/image_rx_manager.cpp — issueWindowRequest records the tracked seq; both stall sites gain the defer-aware guard; window-complete advance and both finalize paths cancel the outstanding request"
    - "src/command_sender.cpp — NACK_BUSY deferral branch for IMAGE_WINDOW_REQUEST in handleResponse; retry-ordinal log label in retryCommand"
    - ".planning/phases/01-command-protocol-control/01-15-SUMMARY.md"
  key_links:
    - "windowRequestSeq <-> getCommandState: a terminal command whose slot was later recycled reads as IDLE (findFreeSlot memsets only terminal commands) — IDLE, ACKED, FAILED, and TIMEOUT all mean 'had its opportunity' and fall through to normal pass charging; only PENDING/SENT mean in-flight"
    - "BUSY deferral <-> defer-aware guard: the guard is only correct because a BUSY-deferred request STAYS non-terminal (PENDING) — if handleResponse terminalized NACK_BUSY the guard would charge a pass for a request that never had a transfer opportunity; the two tasks are one mechanism split across two files"
    - "cancelCommand <-> windowRequestSeq lifecycle: seq is set on queue success, replaced at the next issueWindowRequest, and zeroed never (slot re-init zeroes it via ImageRxTransfer{}); cancelCommand is safe on terminal slots (returns false) so unconditional cancel-at-finalize needs no pre-check"
    - "Backoff pacing <-> BUSY retry: the retransmit is paced by the existing D-07 backoff check (retryCount > 0 gates on lastRetryTime) — a BUSY-deferred retry can never fire back-to-back with the failed attempt"
  prohibitions:
    - statement: "MUST NOT change the D-24 pass bound (IMG_RETRANSMIT_MAX_PASSES 3), the stall window (8000 ms), or any other pacing constant — defer-awareness changes WHEN a pass is charged, never the bounds"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT extend the defer-aware skip to requests that reached a terminal state — a genuinely lost window (ACKED, chunks lost on air) must still charge its pass and re-request on the existing D-24 clock"
      status: flagged
      verification: backstop
    - statement: "MUST NOT apply the NACK_BUSY deferral to any command class other than IMAGE_WINDOW_REQUEST — CAPTURE_NOW/SET_*/GET_STATUS keep today's terminal FAILED semantics and their UAT-proven operator-visible behavior"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT fabricate transfer or config success — verdicts come from bench finalize lines; code-level changes close code-level truths only (bench is 01-16)"
      status: flagged
      verification: backstop
---

<objective>
Base-side remediation for the rescoped G-01-7 residual (defer-aware D-24 pass accounting) plus the CommandSender ride-alongs the verification routed to this round: the retry-bound label off-by-one (WINDOWS entry 6 / IN-01), the duplicate window-request churn (WR-04), and the NACK_BUSY terminalization that both burned heal passes and poisoned the link-truth LED (WR-05, window-request class).

Purpose: session 4 proved the 01-12 levers engage but the series-A truth still failed on the BUSY-deferral pass-burn mechanism — image 7's thumb heal had its window requests NACK-deferred twice while image 8's window was mid-service, and the D-24 budget consumed the deferred+retried requests (a deferred request had no transfer opportunity). The fix makes deferral cheap (retry the request inside its own command budget, never charge a heal pass while it is being worked) and makes the retry accounting truthful to its documented bound.

Output: windowRequestSeq-tracked transfers with defer-aware stall guards at both stall sites and request cancellation at finalize/advance; a NACK_BUSY deferral branch scoped to IMAGE_WINDOW_REQUEST; a retry-ordinal log label; builds and harness green. No gap status flips (bench proof is 01-16's job).

Spec-less probe fallback: recorded visible skip — this phase has no machine-mapped requirement IDs (phase_req_ids null), so no probe predicates were generated this run.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-REVIEW.md
@.planning/phases/01-command-protocol-control/01-13-SUMMARY.md

Binding decisions and prior-round constraints:
- D-05 (per-class ACK timeouts; WINDOW 15000 ms bounds only the arm handshake), D-07 (exponential backoff 2000/4000/8000 ms before retries 1/2/3 — the retryCount>0 backoff block is the single pacing authority), D-21 (base speaks only on window completion / stall / manifest-while-idle), D-24 (bounded CONSECUTIVE unhealed stalls; acceptChunk is the sole passCount zero-writer), D-22 (single reliability mechanism)
- The deferred-items.md route for WINDOWS entry 6: 'fold into the G-01-9 round's CommandSender-adjacent work'; the review's WR-04 route says the same; WR-05's retry-on-BUSY option is the implementation vehicle the defer-aware guard requires (a terminalized BUSY would defeat the in-flight check)
- Bench evidence: image 7 thumb heal window requests NACK-deferred twice (balloon4.log:1154/:1161, returning FAILED :1157/:1164 — the session's only 2 failed commands) while image 8's thumb window was mid-service, then 'finalized INCOMPLETE ... 35/36 chunks after 3 passes' (base4.log:634); 13x retry-overrun label lines in base4.log (:115/:290/:372/:2330 et al.); 14-16 post-terminal kind-0 ignores (base4.log:3286-3310) driven by those late retries; link-truth LED red while chunks flowed (WR-05)
- Code facts: the full-pull stall branch at src/image_rx_manager.cpp:137-169 (pass charge at :147-149); the heal loop's bound/charge at :216-222; issueWindowRequest at :870-911 (returns the tracked seq; seq==0 means queue-full and windowActive=false); finalizeTransfer :672-753 and finalizeIncomplete :755-777; the settle-nested window-complete advance at :111-136; handleResponse at src/command_sender.cpp:393-450 (terminal-state guard :410, response storage :414-416, NACK split :438-449); retryCommand at :524-538 (the label at :533-536 prints the post-increment retryCount plus one against maxRetries); the backoff pacing block at :207-220; getCommandState at :558-566; cancelCommand at :159-179; the base never sends IMAGE_WINDOW_REQUEST for any other purpose, and CommandSender is included by image_rx_manager.cpp already (:4)
- Termination truth for the label fix: retryCount increments once per retry and the timeout branch retries while retryCount < maxRetries, so exactly maxRetries retries fire (1 initial + 3 retries) — the CODE matches D-07's three-retry design; the printed ordinal did not. The fix makes the label print the retry ordinal; the bound semantics are not relaxed or tightened
</context>

<tasks>

<task type="auto">
  <name>Task 1: Defer-aware D-24 pass accounting — windowRequestSeq tracking, in-flight stall guards, request cancellation at advance and finalize</name>
  <files>include/image_rx_manager.h, src/image_rx_manager.cpp</files>
  <read_first>
  - src/image_rx_manager.cpp — process() full-pull stall branch (:106-169, pass charge :147-149) and heal loop (:185-235, bound/charge :216-222); the settle-nested window-complete advance (:111-136); issueWindowRequest (:870-911); finalizeTransfer (:672-753); finalizeIncomplete (:755-777); activateNextPull (:816-868)
  - include/image_rx_manager.h — ImageRxTransfer (:59-101), private helper block (:175-203)
  - src/command_sender.cpp — getCommandState (:558-566), cancelCommand (:159-179), CommandState enum usage (PENDING/SENT terminal vs non-terminal)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — G-01-7 gap entry (the defer-aware missing item and its session-4 evidence)
  </read_first>
  <action>
  Track each transfer's outstanding window request and make both stall sites defer-aware.

  1. include/image_rx_manager.h: add `uint16_t windowRequestSeq;` to ImageRxTransfer inside the D-21 window-context block (comment: the tracked-command sequence of the last successfully QUEUED IMAGE_WINDOW_REQUEST for this transfer; 0 means none; a recycled terminal command slot reads as IDLE which the in-flight check treats as terminal — correct, because only terminal commands are recycled). Declare a private const helper beside findActivePull: `bool windowRequestInFlight(const ImageRxTransfer& t) const;`
  2. src/image_rx_manager.cpp — implement the helper near findActivePull: returns true iff t.windowRequestSeq != 0 AND CmdSender().getCommandState(t.windowRequestSeq) is PENDING or SENT. All other results (ACKED, FAILED, TIMEOUT, IDLE) mean the request had its opportunity.
  3. issueWindowRequest: on queue success store t.windowRequestSeq = seq (beside the windowActive/windowBase/windowCount writes); on the seq==0 queue-full path leave it as-is (the existing no-window-in-flight semantics already handle retry-on-stall without charging a pass).
  4. Full-pull stall branch: as the FIRST check inside the stall else-if (before the D-24 bound check), guard: if windowRequestInFlight(*pull), refresh pull->lastProgressMs = millis() (extend the stall clock — the tracked command is still working the problem inside its own D-05/D-07 budget) and return from the branch without charging a pass or issuing anything. Log once per sequence number: add member uint16_t deferSkipLoggedSeq (init 0 beside healHoldLoggedId) and log 'ImageRx: stall deferred - window request seq %u still in flight for image %u kind %u' only when pull->windowRequestSeq differs from the latch, then record it.
  5. Heal loop: the same guard as the first check inside the per-transfer stall handling (after the used/terminal/kind and lastProgressMs preconditions, before the pass-exhausted check at :216): if windowRequestInFlight(t), refresh t.lastProgressMs and continue to the next slot — no pass, no request.
  6. Window-complete advance: in the branch that issues the next window request after windowSliceComplete (:127-136), cancel the serviced request first: if pull->windowRequestSeq != 0, CmdSender().cancelCommand(pull->windowRequestSeq) — the slice is complete, so the request's remaining retries (ACK lost after service) would only re-arm an already-satisfied span (the WR-04 churn).
  7. Both finalize paths: in finalizeTransfer and finalizeIncomplete, before the release/activate-next work, cancel the outstanding request the same way (a finalized transfer's request must stop retrying — the post-terminal re-arm traffic class).
  8. Update the D-24 comment at the pass-charge sites to state the defer-aware rule: a pass is charged only when the prior request reached a terminal state; while a request is PENDING/SENT (including BUSY-deferred — see Task 2) the stall clock extends instead.
  </action>
  <verify>
  <automated>powershell -Command "if((Select-String -Path include/image_rx_manager.h -Pattern 'windowRequestSeq').Count -lt 2){exit 1}" ; powershell -Command "if((Select-String -Path src/image_rx_manager.cpp -Pattern 'windowRequestInFlight').Count -lt 4){exit 1}" ; powershell -Command "if((Select-String -Path src/image_rx_manager.cpp -Pattern 'stall deferred - window request seq').Count -ne 1){exit 1}" ; powershell -Command "if((Select-String -Path src/image_rx_manager.cpp -Pattern 'cancelCommand').Count -lt 3){exit 1}" ; powershell -Command "if((Select-String -Path src/image_rx_manager.cpp -Pattern 'windowRequestSeq = seq').Count -ne 1){exit 1}" ; powershell -Command "if((Select-String -Path include/image_protocol.h -Pattern 'IMG_RETRANSMIT_MAX_PASSES\s*=\s*3').Count -ne 1){exit 1}" ; pio run -e esp32-s3-balloon -e esp32-s3-basestation ; node scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - include/image_rx_manager.h declares windowRequestSeq and windowRequestInFlight; src/image_rx_manager.cpp implements the helper and applies the guard at BOTH stall sites (grep windowRequestInFlight >= 4: declaration + helper + two call sites)
  - issueWindowRequest stores the tracked seq on success (grep 'windowRequestSeq = seq' == 1); both finalize paths and the window-complete advance cancel the outstanding request (grep cancelCommand >= 3 in image_rx_manager.cpp)
  - The defer-skip log fires once per sequence (log-once latch member present); the D-24 comments state the defer-aware charging rule
  - The pass bound and every pacing constant are unchanged (IMG_RETRANSMIT_MAX_PASSES 3 anchor still exactly once); acceptChunk remains the only passCount zero-writer
  - `pio run -e esp32-s3-balloon -e esp32-s3-basestation` reports 2 succeeded; harness exits 0 (PRI-01 order gates green)
  - git diff file set for this task is exactly include/image_rx_manager.h and src/image_rx_manager.cpp
  </acceptance_criteria>
  <done>A BUSY-deferred or ACK-lost window request no longer consumes D-24 passes or duplicates itself; finalized transfers cancel their outstanding requests — the pass budget now measures transfer opportunities actually had.</done>
  <reversibility rating="reversible">Two files, scheduling logic only, no wire change; git revert.</reversibility>
</task>

<task type="auto">
  <name>Task 2: CommandSender — NACK_BUSY deferral for IMAGE_WINDOW_REQUEST + the retry-ordinal label (WR-05 window class, WR-04 partner, WINDOWS entry 6)</name>
  <files>src/command_sender.cpp</files>
  <read_first>
  - src/command_sender.cpp — handleResponse (:393-450: terminal-state guard, response storage, the ACK/STATUS vs NACK split), the backoff pacing block (:207-220), the PENDING transmit branch (:222-250), retryCommand (:524-538)
  - include/command_protocol.h — ResponseType enum values (ACK/STATUS/NACK/NACK_BUSY/NACK_PARAM), CameraCommand::IMAGE_WINDOW_REQUEST
  - src/main_basestation.cpp — processLoRa lastOutcomeBad latch (:2183-2200): FAILED/TIMEOUT drive the red link state; PENDING/SENT do not — read to confirm no UI-side change is needed
  - .planning/phases/01-command-protocol-control/deferred-items.md — WINDOWS entry 6 route (fold into this round's CommandSender-adjacent work)
  </read_first>
  <action>
  Make BUSY a deferral for the one command class where it is by-construction transient, and make the retry log tell the truth about its bound.

  1. handleResponse: AFTER the terminal-state guard (:410-412) and BEFORE the response storage (:414-416), insert the deferral branch: if the response type is NACK_BUSY AND the tracked command's packet.cmd is IMAGE_WINDOW_REQUEST AND cmd->retryCount < maxRetries — advance cmd->retryCount, set cmd->lastRetryTime = millis() and cmd->sendTime = cmd->lastRetryTime, set cmd->state = CommandState::PENDING, log 'CommandSender: Command seq=%d deferred (balloon window BUSY) - retry pending', and return WITHOUT storing the response, decrementing pendingCommandCount, or counting a failure. The existing backoff block paces the retransmit (retryCount > 0 gates on lastRetryTime), and the PENDING branch performs it — this is retry-with-backoff through the existing D-07 machinery, not a new timer. When the budget is exhausted (retryCount == maxRetries) or the class is any other command, control falls through to today's storage + terminal NACK handling unchanged (prohibition).
  2. retryCommand label: print the retry ordinal, not the attempt ordinal — the log becomes 'Retrying command seq=%d (retry %d/%d)' with cmd->retryCount (the post-increment value, 1..maxRetries) against maxRetries. Do not alter any counter, bound check, or timing — the bound semantics stay exactly as they are (retryCount reaching maxRetries at the timeout branch is the D-05/D-07 three-retry design; only the label conflated ordinals).
  3. No main_basestation.cpp change: with BUSY no longer terminalizing window requests, the lastOutcomeBad latch never sees a FAILED for a mere deferral (PENDING clears it, per the latch's existing else-branch) — the link LED stops poisoning during serialized heal/pull sequences as a consequence, and budget-exhausted deferrals still count as real failures exactly as today.
  </action>
  <verify>
  <automated>powershell -Command "if((Select-String -Path src/command_sender.cpp -Pattern 'deferred \(balloon window BUSY\)').Count -ne 1){exit 1}" ; powershell -Command "if((Select-String -Path src/command_sender.cpp -Pattern 'IMAGE_WINDOW_REQUEST').Count -lt 3){exit 1}" ; powershell -Command "if((Select-String -Path src/command_sender.cpp -Pattern '\(retry %d/%d\)').Count -ne 1){exit 1}" ; powershell -Command "if((Select-String -Path src/command_sender.cpp -Pattern 'NACK_BUSY').Count -lt 2){exit 1}" ; pio run -e esp32-s3-balloon -e esp32-s3-basestation ; node scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - handleResponse contains exactly one deferral branch, gated on response type NACK_BUSY AND packet.cmd IMAGE_WINDOW_REQUEST AND retryCount < maxRetries, placed before response storage; it returns without decrementing pendingCommandCount or incrementing commandsFailed
  - The retry log prints the retry ordinal against maxRetries (grep '(retry %d/%d)' == 1); no attempt-ordinal retry label remains anywhere in the file (the old attempt-numbered format string is gone)
  - retryCommand's counters, bound checks, and sendTime/lastRetryTime writes are byte-identical to HEAD apart from the log format string
  - All other command classes' NACK handling is unchanged (the deferral branch's class gate is the only new condition in handleResponse)
  - `pio run -e esp32-s3-balloon -e esp32-s3-basestation` reports 2 succeeded; harness exits 0
  - git diff file set for this task is exactly src/command_sender.cpp
  </acceptance_criteria>
  <done>A BUSY-deferred window request retries inside its own D-05/D-07 budget and stays non-terminal (making Task 1's in-flight guard correct), the link LED no longer flags routine deferrals as link loss, and the retry log's ordinal matches the bound it enforces.</done>
  <reversibility rating="reversible">Single file, no wire change; git revert.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link -> base tracked-command table | NACK_BUSY responses from the balloon drive CommandSender state transitions (a spoofed BUSY can defer a request) |
| Tracked-command table -> transfer pass accounting | getCommandState results drive D-24 pass charging (a stale seq or wrong terminal mapping corrupts the bound) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-15-01 | Tampering | NACK_BUSY deferral branch | medium | mitigate | Deferral is bounded by the existing maxRetries budget and scoped to IMAGE_WINDOW_REQUEST (the class whose BUSY is by-construction transient); a spoofed BUSY stream delays one window request by at most the D-05/D-07 budget, then the command terminalizes FAILED and the D-24 path proceeds — no unbounded stall |
| T-01-15-02 | DoS | stall-clock extension while in-flight | medium | mitigate | The extension is bounded by the tracked command's own lifetime (15 s ACK class x attempts + D-07 backoffs), after which the request is terminal and passes charge on the unchanged D-24 clock — a slot can never stall forever (PRI-03) |
| T-01-15-03 | Tampering | windowRequestSeq staleness | low | accept | Sequence numbers are unique per queue slot generation; recycled slots hold only terminal commands whose seq differs, so a stale lookup reads IDLE (terminal) and charges passes — the conservative direction |
| T-01-15-04 | Repudiation | retry log ordinals | low | accept | The label now names the true ordinal against the bound, making bench logs trustworthy evidence for the entry-6 closure |
</threat_model>

## Artifacts this phase produces

- `ImageRxTransfer.windowRequestSeq` (include/image_rx_manager.h) — new field
- `ImageRxManager::windowRequestInFlight(const ImageRxTransfer&) const` (include/image_rx_manager.h + src/image_rx_manager.cpp) — new private helper
- `ImageRxManager` member `deferSkipLoggedSeq` (src/image_rx_manager.cpp / include/image_rx_manager.h) — log-once latch
- 'stall deferred - window request seq %u still in flight' + 'deferred (balloon window BUSY) - retry pending' log lines (two files)
- retryCommand's retry-ordinal label '(retry %d/%d)' (src/command_sender.cpp)
- No new files, env vars, or CLI flags

<verification>
- `pio run -e esp32-s3-balloon -e esp32-s3-basestation` — 2 succeeded
- `node scripts/verify_protocol_roundtrip.mjs` — exit 0 (PRI-01 order gates green)
- Pacing anchors unchanged: IMG_RETRANSMIT_MAX_PASSES 3, IMG_WINDOW_STALL_MS 8000, IMG_WINDOW_RX_SETTLE_MS 500
- Diff hygiene: two commits matching the tasks' file sets; main_basestation.cpp absent from both
</verification>

<success_criteria>
- D-24 passes are charged only for requests that reached a terminal state; BUSY deferrals retry within their own budget and never burn a heal pass
- No duplicate overlapping window request can be queued while the prior is non-terminal; finalized transfers cancel their outstanding requests
- The retry label cannot express an ordinal beyond its bound; entry 6's code half is closed (bench confirmation is 01-16's job)
- No gap status flips in this plan
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-15-SUMMARY.md` when done
</output>
