---
phase: 01-command-protocol-control
plan: 11
type: execute
wave: 8
depends_on: ["01-10"]
files_modified:
  - include/image_protocol.h
  - src/image_tx_manager.cpp
  - src/image_rx_manager.cpp
  - src/main_basestation.cpp
  - include/e32_lora.h
  - src/e32_lora.cpp
  - src/sd_storage.cpp
  - .planning/phases/01-command-protocol-control/01-UAT.md
  - .planning/WINDOWS.md
autonomous: false
gap_closure: true
requirements: [IMG-02, IMG-03, IMG-06, PRI-03]

estimate:
  tokens: 6000
  raw_tokens: 6000
  tasks: 3
  confidence: low   # calibration store has 0 samples (factor 1, not applied); branch-conditional — actual cost depends on the selected lever

must_haves:
  truths:
    - "Exactly one remediation path (a single branch, a branch combination, or CLOSE) is selected from the 01-10 evidence at the routing decision, and the selection cites the discriminating console line"
    - "If branch (a) pacing runs: the documented invariants hold in include/image_protocol.h — IMG_WINDOW_SERVICE_PREEMPT_MS remains strictly below IMG_WINDOW_STALL_MS, and IMG_THUMB_HEAL_IDLE_MS remains 3x the stall window"
    - "If branch (b) power runs: include/e32_lora.h defines E32_TARGET_TX_POWER (default POWER_30dBm — flight behavior unchanged) enforced at boot via masked OPTION [1:0] read-modify-write inside the existing fail-open ensureLinkConfig pattern, and BOTH boards are reflashed together (severance invariant)"
    - "If branch (d) refresh runs: the /gallery refetch stays count-driven per D-36 — the fix resets galleryCountSeen on fetch failure so the next poll retries; no per-poll refetch is introduced"
    - "If branch (c)/(e) defect runs: the fix lands at the exact file:line the trace named, with the discriminating line quoted in the commit message"
    - statement: "Bench re-verification of the affected truth is operator-observed and recorded (thumbnail intact rate for branches a/b/c; gallery growth for d/e) — remediation effectiveness is never claimed from code alone"
      verification: backstop
    - "Both firmware targets build green and the wire harness passes after the selected change; no regression in the beacon-priority order gates"
  artifacts:
    - "The selected branch's diff (branch-specific file list from files_modified — only the selected branch's files actually change)"
    - ".planning/phases/01-command-protocol-control/01-UAT.md — G-01-5/G-01-6 status flips (resolved/open-with-named-branch) backed by bench re-verification"
    - ".planning/WINDOWS.md — entry 2 status updated (fixed with evidence, or open with the follow-up named)"
    - ".planning/phases/01-command-protocol-control/01-11-SUMMARY.md"
  key_links:
    - "Pacing invariants: IMG_WINDOW_SERVICE_PREEMPT_MS < IMG_WINDOW_STALL_MS (balloon preempts push before the base's stall clock trips); heal idle = 3x stall"
    - "Power lever symmetry: OPTION [1:0] enforcement rides the same boot-time read-modify-write as the SPED air-rate bits (e32_lora.cpp:481-492 pattern), fail-open with the loud modules-may-mismatch ERROR"
    - "Gallery refresh contract (D-36): onGalleryCount remains the ONLY refetch trigger; a failed fetch must not strand the grid"
  prohibitions:
    - statement: "MUST NOT change image pacing constants before the 01-10 discriminator names the mechanism — single-lever discipline (01-08 decision: pacing/stall-window changes stay out unless the discriminator proves B4 saturation persists at 9.6 kbps)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT add a parallel best-effort thumbnail mechanism — D-22's kind-addressed windowed heal stays the single reliability path"
      status: flagged
      verification: backstop
    - statement: "MUST NOT refetch /gallery on every poll — D-36's count-driven refresh contract governs any refresh fix"
      status: flagged
      verification: backstop
    - statement: "MUST NOT fabricate image-transfer or config success — remediation verdicts come from bench finalize lines or operator observation"
      status: flagged
      verification: backstop
    - statement: "MUST NOT mark G-01-5 or G-01-6 resolved on code-level evidence alone — closure requires operator-observed hardware truth"
      status: flagged
      verification: backstop
---

<objective>
Branch-conditional remediation for G-01-5 and G-01-6, selected by the 01-10 discriminator evidence. The routing decision names the mechanism; this plan implements exactly the selected lever (or records the justified CLOSE), then re-verifies it on the bench.

Purpose: 01-10 produces the diagnosis; this plan converts it into the minimal, invariant-respecting fix. Single-lever discipline (01-08) forbids pre-landing pacing changes — every lever below is gated behind the decision, and each branch names its exact constants, files, and line anchors so the executor needs no interpretation whichever branch fires.

Output: One branch implemented (or CLOSE recorded); bench re-verification; G-01-5/G-01-6 status flips backed by evidence; WINDOWS entry 2 resolved or re-scoped.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-10-SUMMARY.md

Binding decisions:
- D-22 (heal is the single reliability mechanism), D-24 (bounded consecutive unhealed stalls), D-36 (galleryCount is the ONLY refresh signal), D-48 (honest Incomplete badge)
- 01-08: single-lever scope; 01-09: both-boards-same-config is a hard link invariant (the severance event)
- E32Power enum already exists (include/e32_lora.h:65-70): POWER_30dBm=0x00 (T30D default) .. POWER_21dBm=0x03, OPTION register bits [1:0]
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Routing decision — select the remediation branch from the 01-10 evidence</name>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-5/G-01-6 evidence blocks and tallies from 01-10
  - .planning/phases/01-command-protocol-control/01-10-SUMMARY.md — the evidence table
  </read_first>
  <decision>
  Which remediation branch (or combination, or CLOSE) does the 01-10 discriminator evidence select?
  </decision>
  <context>
  The decision is evidence-forced, not preference: each branch below lists its trigger signature from the 01-10 traces. Combinations are allowed when both gaps independently need work (e.g. branch b for G-01-5 AND branch d for G-01-6). CLOSE is valid only if the tallies show an acceptable intact rate at both separations AND the gallery grows — an episodic-loss finding.
  </context>
  <options>
    <option id="close">
      <name>CLOSE — no remediation</name>
      <pros>Zero code risk; gaps flip resolved on bench evidence alone</pros>
      <cons>Only honest if both separation series show healthy intact rates and G-01-6's discriminator showed the grid merely lagging a working signal</cons>
    </option>
    <option id="branch-a">
      <name>(a) B4 pacing round — image_protocol.h levers</name>
      <pros>01-08's armed follow-up, exactly as enumerated; no architectural change</pros>
      <cons>Only fires if heal windows were served (balloon armed + chunks sent) but lost amid push traffic at BOTH separations</cons>
    </option>
    <option id="branch-b">
      <name>(b) B5 power lever — E32_TARGET_TX_POWER enforcement</name>
      <pros>Directly targets close-range RX overload; flight default unchanged (30 dBm)</pros>
      <cons>Only fires if the close-range incomplete rate is much higher than the separated rate; requires both boards reflashed together</cons>
    </option>
    <option id="branch-c">
      <name>(c) Heal-servicing defect fix (G-01-5)</name>
      <pros>Fixes a real code defect if present</pros>
      <cons>Only fires if the base logged repeated cmd=30 while the balloon logged NO 'THUMBNAIL window armed'</cons>
    </option>
    <option id="branch-d">
      <name>(d) G-01-6 refresh-stale client fix</name>
      <pros>One-line robustness fix; D-36 preserved</pros>
      <cons>Only fires if /gallery total exceeded the grid count (the client stranded after a failed fetch)</cons>
    </option>
    <option id="branch-e">
      <name>(e) G-01-6 persistence/index defect fix</name>
      <pros>Restores gallery growth if captures finalize but never index</pros>
      <cons>Only fires if finalize lines appear but /gallery total stays frozen; fix is trace-named (buildIndex/mergeIntoIndex or the finalize persist path)</cons>
    </option>
  </options>
  <resume-signal>Select: close, branch-a, branch-b, branch-c, branch-d, branch-e, or a combination (e.g. "branch-b + branch-d") — and cite the console line that forced it</resume-signal>
  <acceptance_criteria>
    - The selection is recorded with the discriminating 01-10 console line quoted
    - If CLOSE: the justification names both separation tallies and the G-01-6 discriminator result
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement the selected branch</name>
  <files>branch-specific (see action)</files>
  <read_first>
  - The selected branch's anchor files (listed per branch below)
  - include/image_protocol.h — the transfer-constants block (:54-101) and each constant's invariant comment
  - src/e32_lora.cpp — ensureLinkConfig masked read-modify-write (:473-505, SPED handling :481-492) as the pattern for branch (b)
  </read_first>
  <action>
  Implement ONLY the branch(es) selected in Task 1. Branch anchors:

  BRANCH (a) — B4 pacing round (files: include/image_protocol.h only):
  - IMG_RETRANSMIT_MAX_PASSES: 3 -> 5 (heal pass bound; thumbnails are ~30 chunks, extra passes are cheap airtime)
  - IMG_WINDOW_SERVICE_PREEMPT_MS: 5000 -> 3000 (an armed heal window preempts push work sooner; the value MUST remain strictly below IMG_WINDOW_STALL_MS 8000 — update the invariant comment at :78-84 to cite the discriminator lines that forced the change)
  - IMG_THUMB_HEAL_IDLE_MS stays 24000 (3x stall invariant, documented at :86-95 — do not touch)
  - No other file changes; no manager logic changes — this is 01-08's deferred follow-up condition executing verbatim.

  BRANCH (b) — B5 power lever (files: include/e32_lora.h, src/e32_lora.cpp):
  - include/e32_lora.h: add `static constexpr uint8_t E32_TARGET_TX_POWER = static_cast<uint8_t>(E32Power::POWER_30dBm);` adjacent to E32_TARGET_AIR_DATA_RATE (:107-108) with a comment citing the 01-10 close-vs-separated tallies; default stays 30 dBm (flight range needs it) — the constant exists so a bench/short-range profile is a one-line edit
  - src/e32_lora.cpp: extend the ensureLinkConfig read-modify-write (:481-492 pattern) to OPTION bits [1:0] (mask 0x03): if the measured power bits differ from the target, patch them in the same echo-verified writeConfigRegisters call as the SPED correction (single config session, single EEPROM write); same fail-open discipline — failure logs the loud modules-may-mismatch ERROR, never aborts boot
  - Reflash BOTH boards (the severance invariant: divergent e32 config severs the pair — 01-09 lesson)

  BRANCH (c) — heal-servicing defect (files: src/image_tx_manager.cpp, possibly src/image_rx_manager.cpp — exactly where the trace points):
  - The 01-10 trace showed base cmd=30 retries with NO balloon 'THUMBNAIL window armed': the defect is in the window-request matcher or entry state (:634-732 kind-split arming; :691 THUMBNAIL-never-evicts rule) or in the base heal gating (image_rx_manager.cpp:190-216). Fix exactly what the trace names; quote the trace line in the commit message; do not refactor beyond the defect.

  BRANCH (d) — G-01-6 refresh-stale client fix (files: src/main_basestation.cpp only):
  - In galleryEnsureList's .catch handler (~:1630-1632, currently `console.error(err); // keep whatever grid is shown`): add `galleryCountSeen = null;` before/after the console.error — a failed fetch must not strand the grid; nulling the latch makes the next poll's onGalleryCount(count) see a change and retry. This is the ONLY change; the refetch stays count-driven (D-36) and the stale-response guard (galleryFetchSeq) is untouched.

  BRANCH (e) — G-01-6 persistence/index defect (files: src/sd_storage.cpp, possibly src/image_rx_manager.cpp — trace-named):
  - The 01-10 evidence showed finalize lines but a frozen /gallery total: inspect buildIndex (:474-520), mergeIntoIndex (:520-561), ensureIndexCurrent (:569-573) and the finalizeImage indexVersion++ path (:278-307) against the specific ids that failed to appear; fix the exact broken link the evidence names (e.g. a persist path that writes files without bumping indexVersion, or a buildIndex filter that drops the new files).

  CLOSE — no code: record the closure rationale (both separation tallies + the G-01-6 discriminator result) in 01-UAT.md, flip G-01-5/G-01-6 to resolved, mark WINDOWS entry 2 fixed, run the verification builds/harness to confirm the tree is green, and skip Task 3's re-verification (the 01-10 session already provided it).
  </action>
  <verify>
  <automated>Common chain (every path, including CLOSE): pio run -e esp32-s3-balloon -e esp32-s3-basestation (both SUCCESS) && node scripts/verify_protocol_roundtrip.mjs (exit 0). Then run ONLY the Task-1-selected branch's gate — never all of them; each gate is a PowerShell if-guard that exits 1 on mismatch (a wrong implementation fails the verify), and every pattern is spacing-tolerant: (a) powershell -Command "if(-not (((Select-String -Path include/image_protocol.h -Pattern 'IMG_WINDOW_SERVICE_PREEMPT_MS\s*=\s*3000').Count -eq 1) -and ((Select-String -Path include/image_protocol.h -Pattern 'IMG_WINDOW_STALL_MS\s*=\s*8000').Count -eq 1))){exit 1}"; (b) powershell -Command "if((Select-String -Path include/e32_lora.h -Pattern 'E32_TARGET_TX_POWER').Count -lt 1){exit 1}"; (d) powershell -Command "if((Select-String -Path src/main_basestation.cpp -Pattern 'galleryCountSeen\s*=\s*null').Count -lt 2){exit 1}" (declaration :1611 + the catch-site reset; the strict-equality check at :1637 does not match this pattern). Branches (c)/(e) are trace-named with no grep gate — the common chain plus the trace-quoted commit message govern them; CLOSE runs the common chain only.</automated>
  </verify>
  <acceptance_criteria>
  - Only the selected branch's files are modified (git diff file list matches the branch's file set)
  - Branch (a): the three constants hold their invariant relations (preempt 3000 < stall 8000; heal idle 24000 == 3x stall) and the invariant comments cite the discriminator
  - Branch (b): E32_TARGET_TX_POWER defined with default POWER_30dBm; OPTION patch rides the same echo-verified config write as SPED; fail-open ERROR retained
  - Branch (c)/(e): the commit message quotes the discriminating console line; the fix is at the trace-named site
  - Branch (d): exactly one statement added in the catch handler; the fetch sequence guard untouched; no per-poll refetch introduced
  - `pio run -e esp32-s3-balloon -e esp32-s3-basestation` reports 2 succeeded; harness exits 0
  </acceptance_criteria>
  <done>The selected lever is implemented exactly as named (or CLOSE recorded with rationale); builds and harness green; atomic commit (e.g. fix(01-11): <branch> per 01-10 discriminator evidence).</done>
  <reversibility rating="costly">Branch (b) writes OPTION bits to module EEPROM — persistent hardware state; revert requires re-flashing with the original constant and one boot (the enforcement is bidirectional), but a bench session in between would observe the changed power.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Bench re-verification — repeat the affected series and flip the gap statuses on evidence</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md, .planning/WINDOWS.md, .planning/STATE.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-5/G-01-6 blocks (the truths being re-verified)
  - .planning/phases/01-command-protocol-control/01-10-SUMMARY.md — the baseline tallies to compare against
  </read_first>
  <what-built>
  The selected remediation from Task 2 (skipped entirely if Task 1 selected CLOSE — the 01-10 session already provided this verification).
  </what-built>
  <how-to-verify>
  1. Flash the affected board(s) — BOTH boards for branch (b) (severance invariant).
  2. For branches (a)/(b)/(c): at the range placement the 01-10 close-range series used, repeat the 8-10 capture series with both consoles logging; tally finalize verdicts per kind and compare against the 01-10 baseline tallies. The affected truth: thumbnail intact rate materially improved (target: the corrupt-preview + Incomplete-badge outcome no longer the norm at bench range); full incomplete rate likewise for (b).
  3. For branches (d)/(e): trigger 3+ new captures; after each, confirm the dashboard grid grows without a manual refresh (count advances and the grid refetches on the next poll); confirm via /gallery?page=1 that total, grid, and pager agree.
  4. PRI-01 regression watch (branches a/c): telemetry must keep updating at the 5 s cadence during every transfer — the beacon's priority is untouched by design; record any observed stall.
  5. Flip statuses on evidence: G-01-5/G-01-6 to resolved in 01-UAT.md (with the new tally as verified_by), WINDOWS.md entry 2 to fixed — OR record the residual with the named next mechanism if the rate did not improve (never force a close: prohibition — no fabricated success, no code-level-only closure).
  </how-to-verify>
  <acceptance_criteria>
  - The post-fix tally (or gallery-growth observation) is recorded in 01-UAT.md alongside the 01-10 baseline for comparison
  - Gap status flips cite operator-observed evidence; WINDOWS entry 2 updated consistently
  - For branch (b): both boards were reflashed together and the E32 cfg boot line shows the enforced values on both
  - PRI-01 watch: telemetry cadence held during transfers (or any stall recorded verbatim)
  </acceptance_criteria>
  <done>The affected gap truth is operator-observed post-fix; statuses flipped on evidence or the residual honestly re-scoped; STATE.md records the round outcome.</done>
  <resume-signal>Type "re-verified" with the post-fix tally vs baseline, or describe the residual outcome</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link (balloon <-> base) | Untrusted wireless; existing CRC16/framing/type-dispatch mitigations unchanged by every branch |
| E32 module EEPROM (branch b) | Persistent hardware config written at boot — a divergence between boards severs the link |
| Web UI client state (branch d) | The gallery refresh latch — a client-side robustness fix only, no new input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11-01 | DoS | Pacing levers vs telemetry beacon priority (branch a) | high | mitigate | Beacon early-return and command-response loop order are not touched (02-05 design); harness PRI-01 order-gates must stay green (Task 2 verify); preempt value stays strictly under the base stall clock by invariant |
| T-01-11-02 | Tampering | OPTION EEPROM write desyncing the pair (branch b) | high | mitigate | Boot-time enforcement on BOTH boards with the same fail-open read-modify-write as the air rate; Task 3 requires both boards reflashed together and the cfg boot line confirmed on both (01-09 severance lesson) |
| T-01-11-03 | Repudiation | Remediation claimed effective without bench proof | medium | mitigate | Task 3 is a blocking checkpoint; status flips must cite operator-observed post-fix tallies (prohibition: no code-level-only closure) |
| T-01-11-04 | DoS | Branch (d) latch reset turning into a refetch loop | low | mitigate | The reset only arms the NEXT onGalleryCount(count) comparison — still one fetch per count change, never per poll; the stale-response guard (galleryFetchSeq) is untouched |
</threat_model>

<verification>
- Task 1: decision recorded with the forcing console line
- Task 2: branch-gated automated gates + dual-target builds + harness
- Task 3: blocking bench re-verification with before/after tallies
- Post-plan: git diff file set matches the selected branch; 01-UAT.md/WINDOWS.md/STATE.md consistent; no fabricated closures
</verification>

<success_criteria>
1. The selected branch (or CLOSE) is implemented/recorded exactly as named, single-lever discipline preserved
2. All documented invariants hold (preempt < stall; heal idle = 3x stall; D-36 count-driven refresh; D-22 single heal path)
3. Bench re-verification compares post-fix tallies against the 01-10 baseline
4. G-01-5/G-01-6 statuses flip only on operator-observed evidence; WINDOWS entry 2 consistent
5. Both targets green, harness green, PRI-01 order-gates green
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-11-SUMMARY.md` when done — branch taken, forcing evidence, before/after tallies, status flips
</output>

## Artifacts this phase produces

- `E32_TARGET_TX_POWER` — new constexpr in include/e32_lora.h (branch b only; default POWER_30dBm)
- Changed constants (branch a only): IMG_RETRANSMIT_MAX_PASSES (3 -> 5), IMG_WINDOW_SERVICE_PREEMPT_MS (5000 -> 3000) in include/image_protocol.h
- One statement in galleryEnsureList's catch (branch d only): galleryCountSeen = null — no new symbols
- Trace-named defect fixes (branches c/e): no new symbols anticipated; commit messages quote the forcing trace line
- Evidence artifacts: 01-UAT.md status flips, WINDOWS.md entry 2 update, STATE.md round outcome, 01-11-SUMMARY.md
