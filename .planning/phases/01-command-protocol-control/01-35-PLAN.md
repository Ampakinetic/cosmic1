---
phase: 01-command-protocol-control
plan: 35
type: execute
wave: 27
depends_on: []
files_modified:
  - src/image_tx_manager.cpp
  - .planning/WINDOWS.md
  - .planning/phases/01-command-protocol-control/01-UAT.md
  - .planning/phases/01-command-protocol-control/COVERAGE.md
autonomous: true
gap_closure: true
requirements: [CTRL-01, CTRL-03, CTRL-06, PRI-02]

estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low   # calibration store has 0 samples (factor 1, not applied); mirrors the 01-33 lever-round sizing (14 min, code-only, no bench claims)

must_haves:
  truths:
    - statement: "A FULL window request is ADMITTED whenever the queue holds a free slot: the supersede scan runs only when every IMG_TX_QUEUE_DEPTH slot is used, so the session-11 OVER-REJECTION mode (8 honest-rejects at 2/5 occupancy starving image 45's SVGA FULL 0/165, balloon5.log:883-:1056) is structurally dead — a request at partial occupancy can never be refused by the supersede path again"
      verification: explicit
    - statement: "When the queue IS full and the supersede scan must evict, an older candidate whose fullAcked bitmap covers every full chunk (all its WINDOW_COMPLETE receipts merged at image_tx_manager.cpp:1564-1573) is selected FIRST as the safe-reclaim class (class 0, below THUMB_PUSHED) ahead of any in-flight receipt-evidenced (class 5) entry, printing a named discriminator line naming the reclaimed image and the receipt evidence — the shared evictionClassOf is EXTENDED (overflow scan at :701 and supersede scan at :1997 rank identically; never forked, T-01-33-02 discipline)"
      verification: explicit
    - statement: "The round-#14 honest-reject line ('window request for image %u rejected - queue holds only receipt-evidenced entries (G-01-7)') SURVIVES with a narrowed domain: it fires only when the queue is full AND every older non-mid-service candidate is an in-flight receipt-evidenced class-5 entry — the designed protection against sacrificing an actively-pulled entry; the 01-12 mid-service deferral guard is verbatim"
      verification: explicit
    - statement: "No wire change, no new protocol surface, no operator-visible semantics change beyond eviction/rejection timing: PRI-01 orderings intact (beacon early-return precedes the pushPending call; the preemption check precedes findActiveEntry), and the protected surfaces are byte-intact (CMD_TX_CHANNEL_QUIET_MS 2000, the D1 instruments [STACK]/[STAMP]/[TICKSTAMP]/[SCHEDSNAP]/[IDLE1]/[CAPWIN], B1/B2, [MEM], D-24 bounds, IMG_ENTRY_TTL_MS 900000, KIND_COMPLETE-frees-entry at :1597-1604)"
      verification: explicit
    - statement: "Ledger honesty: WINDOWS entry 3 and 01-UAT G-01-7 carry the round-#15 lever-shipped note with ZERO bench claims — G-01-7 flips only on 01-36's operator-observed series-A verdicts, where engaged-and-passing / engaged-and-failing (residual re-named) / never-engaged are distinct verdicts the discriminator lines separate"
      verification: explicit
  artifacts:
    - "src/image_tx_manager.cpp — the capacity-need gate at the supersede call site (:1429-1452 region), the file-local fully-receipted helper, the extended evictionClassOf class-0 case, the named safe-reclaim Serial.printf line"
    - ".planning/WINDOWS.md — entry 3 round-#15 lever-shipped note (both table and JSON copies), counts reconciled"
    - ".planning/phases/01-command-protocol-control/01-UAT.md — G-01-7 block round-#15 lever note (open, no bench claims)"
    - ".planning/phases/01-command-protocol-control/COVERAGE.md — round-#15 re-affirmation line (still no external API surface)"
    - ".planning/phases/01-command-protocol-control/01-35-SUMMARY.md"
  key_links:
    - "The capacity gate <-> the session-11 evidence: the gate's condition (a free slot exists -> skip eviction, admit) directly negates the observed failure geometry (2/5 occupancy, sole older candidate fully SERVED)"
    - "The safe-reclaim class <-> the WINDOW_COMPLETE receipt merge: class 0 reads ONLY the fullAcked bits that handleImageAck already merges (:1564-1573) and KIND_FAILED_CRC already clears (:1623) — no new receipt surface, the evidence already exists on the entry"
    - "The lever <-> 01-36's series A: the named lines make engaged-and-passing / engaged-and-failing / never-engaged distinct bench verdicts (the 01-33 precedent)"
  prohibitions:
    - statement: "MUST NOT make any bench claim or flip G-01-7 — closure requires 01-36's operator-observed series-A verdicts; this plan ships code only (the 01-33 convention)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT fork evictionClassOf or add a supersede-private ranking — the extension lives in the ONE shared function both scans call (T-01-33-02)"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT touch camera_manager.cpp, image_rx_manager.cpp, main_balloon.cpp instruments, the wire format, or any timing constant — the lever is queue policy only (the round-#14 commit-scope discipline)"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT re-implement KIND_COMPLETE-frees-entry — it already exists (:1597-1604); the plan's re-verify grep confirms it, it does not recreate it"
      status: flagged
      verification: backstop
---

<objective>
Gap-closure round #15, plan 1 of 2 — the G-01-7 capacity-aware supersede admission lever (WINDOWS entry 3, verdict engaged-and-failing via the NEW over-rejection mode at bench session #11). Implement the routed fix class exactly: force-evict only when capacity is actually needed, and treat fully-receipted (all-full-chunks-confirmed) entries as the safe reclaim class ahead of in-flight receipt-evidenced entries. The 01-REVIEW WR-02 companion (receipt stamp before range validation) is ALREADY FIXED upstream — the stamp now sits after full validation (image_tx_manager.cpp:1417-1427, in-source comment citing WR-02) — so this plan verifies that ordering survives and does NOT re-fix it.

Purpose: bench session #11 proved the round-#14 lever's honest-rejection form fires REGARDLESS of capacity need — image 45's SVGA FULL starved 0/165 behind image 44's fully-delivered entry while the queue held 2 of 5 slots. Session #12-#35's image-transfer rework since then added the missing ingredient the original routing named: WINDOW_COMPLETE receipts (fullAcked bitmap) give the balloon transfer-complete feedback per chunk, so "fully delivered" is now OBSERVABLE at the eviction site, not assumed. This lever composes with that rework: the capacity gate kills the partial-occupancy rejection; the class-0 safe reclaim converts a full queue's oldest fully-receipted entry from a starvation cause into the safe victim.

Output: modified src/image_tx_manager.cpp (queue policy only), builds 2/2 + harness exit 0, PRI-01 orderings and every protected surface intact, ledger notes shipped with zero bench claims, round #15 ready for the 01-36 shared bench session.

Spec-less probe fallback (visible, recorded choice): init.phase_req_ids is null this run — no probe predicates are generated; the 11 unresolved spec-less probe rows authored in 01-34's must_haves.flagged_assumptions remain surfaced there, carried, never silently dropped.

<assumption_delta_decision>
Detector fired on the terms "second"/"fallback" — both trace to prior plan-list prose ("second-card retry", "never-wedge fallback" in ROADMAP wave entries), not this round's scope. Determination: no-change — a queue-policy lever introduces no architectural identity change (same as the 01-32/01-34 determinations).
</assumption_delta_decision>
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/WINDOWS.md
@.planning/debug/d1-crash-regression-push-start.md

Binding evidence and conventions:
- The over-rejection record: 01-UAT.md G-01-7 SESSION-11 UPDATE + WINDOWS entry 3 SESSION-11 READING — 8 honest-rejects at 2/5 occupancy, the sole older candidate fully SERVED base-side, two 0/165 D-24 lifecycles, thumbnails immune (kind-0 bypasses the scan), the routed fix class verbatim: "capacity-aware supersede admission — evict only when a slot is actually needed, with SERVED/complete entries as the safe reclaim class".
- The current code state (read at HEAD, post session-35): the supersede call site image_tx_manager.cpp:1429-1452 (range validation :1400-1415 BEFORE the receipt stamp :1417-1427 — WR-02 already fixed); evictEntriesOlderThan :1935-2041 (pass-1 mid-service deferral verbatim; pass-2 ranked scan; the class-5 refuse at :2007-2014; the avoid-form line :2028; the honest-reject line lives at the call site :1449); evictionClassOf :415-431 (SHARED: overflow scan :701, supersede scan :1997); the WINDOW_COMPLETE merge :1558-1578; KIND_FAILED_CRC clears fullAcked :1617-1626; KIND_COMPLETE_CRC_OK frees the full entry :1597-1604 (transfer-complete feedback EXISTS — do not re-add).
- The bench-session-35 state this must compose with: debug doc §38 — round-#25 build carries [SCHEDSNAP] (ELF b5809923... pre-01-35); 01-36 flashes HEAD (instruments + this lever) and records fresh SHAs.
- Protected surfaces (grep-pinned after the change): CMD_TX_CHANNEL_QUIET_MS 2000 (command_sender.h — the 9e5d5b3 value, NOT the old 750), IMG_WINDOW_STALL_MS 15000, IMG_TX_QUEUE_DEPTH 5 (include/image_protocol.h:95), IMG_ENTRY_TTL_MS 900000, the D1 instrument lines in src/main_balloon.cpp, PRI-01 anchors (sendTelemetryBeacon early-return :305 precedes the pushPending call :311; the preemption check precedes findActiveEntry :945).
- Estimate convention: the project calibration store has 0 samples — confidence low, factor not applied.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capacity-aware supersede admission — the free-slot gate, the class-0 safe reclaim, the named discriminator line</name>
  <files>src/image_tx_manager.cpp</files>
  <read_first>
  - src/image_tx_manager.cpp — the supersede call site (:1400-1466: range validation, receipt stamp, the evictEntriesOlderThan gate, the honest-reject line, the BUSY check), evictEntriesOlderThan (:1935-2041), evictionClassOf (:395-431 with its class contract comment), the WINDOW_COMPLETE merge (:1558-1578), KIND_FAILED_CRC clearing (:1608-1626), KIND_COMPLETE-frees-entry (:1582-1606)
  - include/image_tx_manager.h — the fullAcked[16] field and its contract comment (RAM-only, WINDOW_COMPLETE-merged, 1024 bits for the 919-chunk worst case)
  - include/image_protocol.h — IMG_TX_QUEUE_DEPTH 5 and the IMG_MAX_IMAGE_SIZE 204800 cap (the arithmetic the helper's bounds derive from)
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-7 block, the SESSION-11 UPDATE (the over-rejection evidence the fix answers)
  - .planning/WINDOWS.md — entry 3 (the routed fix class verbatim)
  </read_first>
  <action>
  Three changes, queue policy only, in src/image_tx_manager.cpp:

  1. CAPACITY-NEED GATE at the supersede call site (the :1429-1452 region): run evictEntriesOlderThan ONLY when the queue has no free slot — scan entries for any !used slot first; when a free slot exists, skip the supersede scan entirely and fall through to the BUSY check and arm (the request is admitted; no older entry is touched; no line printed). When the queue is full, the existing evictEntriesOlderThan call, its false-return honest-reject, and the named reject line all run exactly as today. Update the block comment at the call site to name the session-11 evidence (2/5-occupancy over-rejection, balloon5.log:883-:1056) and the G-01-7 round-#15 routing, keeping the existing WR-02/receipt-stamp comments above it verbatim.

  2. SAFE-RECLAIM CLASS in the SHARED evictionClassOf (:395-431): before the switch, a fully-receipted candidate ranks class 0 — a new file-local static helper (name it fullReceiptComplete) returns true when fullTotalChunks > 0 AND every chunk 0..fullTotalChunks-1 has its fullAcked bit set (loop chunk>>6/chunk&63 over the 16-word bitmap; the entry struct is already the function's parameter type, no signature change). Guard the helper on fullTotalChunks > 0 so a thumb-only entry (the 01-26 full-unavailable degradation path) never ranks class 0 vacuously. Document class 0 in the function's class-contract comment: "0 FULL fully receipt-confirmed (every WINDOW_COMPLETE in) — safe reclaim: the base provably holds every byte; buffers reclaimable". This is an EXTENSION of the one shared ranking — the overflow scan (:701) and the supersede scan (:1997) both inherit it; do NOT fork or add a supersede-private ranking.

  3. NAMED DISCRIMINATOR LINE in evictEntriesOlderThan's pass-2 eviction: when the selected victim's class is 0, print a distinct named line BEFORE the existing generic supersede-evicted line — one Serial.printf naming the reclaimed image id and the receipt evidence, tagged (G-01-7), in the house log style of the two existing round-#14 discriminator lines (:2028 and the call-site :1449). Keep the generic line for every eviction including class 0 (bench-log tooling greps both). The mid-service deferral guard (pass 1) and the class-5 refuse path (:2007-2014) stay verbatim.

  Composition rules (respect all of them): the pass-1 mid-service deferral precedes everything — a fully-receipted entry with an armed incomplete window is STILL never a victim this call; the helper reads only entry state that already exists (no new fields, no header change); the wire format, timing constants, queue depth, and TTL are untouched; KIND_COMPLETE-frees-entry (:1597-1604) already provides the terminal feedback — the class-0 path covers only the pre-verdict window (verdict lost on air, base still serializing), which is exactly the session-11 starvation geometry.
  </action>
  <behavior>
  - Session-11 geometry replay (the acceptance bar): queue at 2/5 occupancy, sole older candidate receipt-evidenced — a FULL window request for the newer image is ADMITTED (no reject, no eviction, no defer line).
  - Full-queue safe reclaim: queue 5/5, incoming FULL request, every older candidate class 5 except one fully-receipted entry — the fully-receipted entry is the victim (class 0 selected first), its named line prints, the request proceeds.
  - Designed protection preserved: queue 5/5, every older candidate class 5 and NOT fully receipted — honest-reject line fires, UNKNOWN_IMAGE returned, base D-24 retries within its bound.
  - Degradation guard: an entry with fullTotalChunks == 0 (thumb-only) never ranks class 0.
  - Mid-service: an armed incomplete window is deferred in pass 1 regardless of class.
  </behavior>
  <verify>
    <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation && node scripts/verify_protocol_roundtrip.mjs</automated>
    <fails_when>either command exits non-zero, the build reports fewer than "2 succeeded", or the harness prints any FAIL line or a non-zero-exit summary</fails_when>
  </verify>
  <done>Both targets build green and the wire-format harness exits 0 with the lever in the tree; the three behavior changes are present at source (free-slot gate, shared class-0 ranking, named line); no other file is modified.</done>
</task>

<task type="auto">
  <name>Task 2: Source gates (PRI-01, protected surfaces, discriminator lines) + ledger notes with zero bench claims + COVERAGE re-affirmation + commit</name>
  <files>.planning/WINDOWS.md, .planning/phases/01-command-protocol-control/01-UAT.md, .planning/phases/01-command-protocol-control/COVERAGE.md</files>
  <read_first>
  - src/image_tx_manager.cpp — the Task 1 result (grep anchors for the new lines)
  - .planning/WINDOWS.md — entry 3 in BOTH the markdown table and the JSON copy (the house dual-copy convention; counts reconcile from 2 open / 17 fixed / 19 total)
  - .planning/phases/01-command-protocol-control/01-UAT.md — the G-01-7 block (the ROUND-#14 UPDATE is the note pattern to extend with a ROUND-#15 note)
  - .planning/phases/01-command-protocol-control/COVERAGE.md — the round-re-affirmation pattern in its header
  </read_first>
  <action>
  1. Source gates, run and record in the SUMMARY: PRI-01 structural ordering (the sendTelemetryBeacon early-return line number precedes the pushPending call-site line number in src/image_tx_manager.cpp; the preemption check precedes findActiveEntry); protected-surface greps (CMD_TX_CHANNEL_QUIET_MS 2000 in the command sender header; IMG_WINDOW_STALL_MS 15000; IMG_TX_QUEUE_DEPTH 5 in include/image_protocol.h; IMG_ENTRY_TTL_MS 900000; the KIND_COMPLETE freeEntry call at the :1597-1604 region — confirm present, not recreated); the round-#14 discriminator lines still present (the avoid-form line and the honest-reject line); the new round-#15 lines present (the class-0 safe-reclaim line, the class-0 case in the class-contract comment); evictionClassOf still the SINGLE ranking (exactly one definition; both scan sites call it).
  2. WINDOWS.md entry 3: append a ROUND-#15 LEVER SHIPPED note to BOTH copies — the lever shape (free-slot gate + shared class-0 safe reclaim + named line), the session-11 geometry it answers, builds 2/2 + harness exit 0, ZERO bench claims, entry STAYS OPEN, counts unchanged (2/17/19). Reconcile the front-matter counts if the tooling expects it (they should not change).
  3. 01-UAT.md G-01-7 block: append a ROUND-#15 UPDATE note (same evidence class as the ROUND-#14 one): the routed fix class implemented, the WR-02 companion confirmed ALREADY FIXED upstream (stamp after validation, :1417-1427 — verified, not re-fixed), no bench claims, closure waits on 01-36's series-A verdicts.
  4. COVERAGE.md: extend the header's re-affirmation list with round #15 (still no external API surface — on-device queue scheduling).
  5. Commit: fix(01-35): G-01-7 capacity-aware supersede admission — free-slot gate + fully-receipted safe-reclaim class per the session-11 over-rejection routing.
  </action>
  <verify>
    <automated>grep -c "fullReceiptComplete" src/image_tx_manager.cpp | grep -qv "^0$" && grep -c "safe reclaim" src/image_tx_manager.cpp | grep -qv "^0$" && grep -c "queue holds only receipt-evidenced entries" src/image_tx_manager.cpp | grep -qv "^0$" && grep -c "CMD_TX_CHANNEL_QUIET_MS 2000" include/command_sender.h src/command_sender.cpp 2>/dev/null | grep -qv "^0$" && grep -c '"id": 3' .planning/WINDOWS.md | grep -qv "^0$" && grep -c "ROUND-#15" .planning/phases/01-command-protocol-control/01-UAT.md | grep -qv "^0$"</automated>
    <fails_when>any grep in the chain returns zero matches (chain exits non-zero) — a missing helper, missing safe-reclaim naming, a deleted round-#14 reject line, an altered quiet-gate constant, or an unwritten ledger note</fails_when>
  </verify>
  <acceptance_criteria>
  - PRI-01 ordering assertions hold at HEAD: the beacon early-return line number is less than the pushPending call line number; the preemption check precedes findActiveEntry — recorded in the SUMMARY with the actual line numbers
  - Exactly one evictionClassOf definition exists and both the overflow scan and the supersede scan call it (no fork) — recorded with line numbers
  - WINDOWS.md table and JSON copies both carry the round-#15 note; front-matter counts still read 2 open / 17 fixed / 19 total (no flips)
  - The 01-UAT G-01-7 round-#15 note names the WR-02-already-fixed verification explicitly (stamp position after range validation) and makes no bench claim
  - COVERAGE.md carries the round-#15 re-affirmation; the working tree commits with the stated message and the plan's four files
  </acceptance_criteria>
  <done>The lever is shipped behind green builds and a green harness, every protected surface grep-pinned intact, the ledgers carry the honest no-bench-claims note, and round #15's bench (01-36) can flash a HEAD that contains both the round-#25 instruments and this lever.</done>
  <reversibility rating="reversible">Queue-policy-only change behind named lines; a single-commit revert restores round-#14 behavior; no wire or persisted-format change exists to migrate.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link -> balloon window-request handler | Untrusted 0x11 window requests drive eviction decisions; CRC16 + kind/range validation gate them before any queue mutation |
| Base receipts -> balloon eviction evidence | WINDOW_COMPLETE/KIND verdict frames are the trust input for class 0; a forged receipt could mark chunks delivered that the base does not hold |
| Bench evidence -> ledger claims | The round ships code with zero bench claims; the ledger records the ship, never a verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-35-01 | Spoofing/Information | class-0 safe-reclaim selection | medium | mitigate | Class 0 requires fullAcked bits that only merge from CRC16-validated IMAGE_ACK frames after per-kind range validation (WR-02 ordering verified in Task 2); a forged receipt can only cause reclaiming a buffer the base claims to hold — the failure mode degrades to a heal re-pull, bounded by the D-24 pass budget |
| T-01-35-02 | DoS | eviction timing under request floods | medium | mitigate | Eviction requires a FULL queue; the mid-service deferral is verbatim; the strictly-older candidate bound and the ascending-class order are unchanged; KIND_FAILED_CRC clears receipt bits so a rejected image cannot stay pinned |
| T-01-35-03 | DoS | PSRAM retention at partial occupancy | low | accept | The gate lets moved-past entries linger while slots are free — bounded by IMG_TX_QUEUE_DEPTH x cap arithmetic (5 x ~212 KB worst case, the 01-33 comment's class), the enqueue-overflow scan (now class-0-first), and sweepExpiredEntries TTL 900000 |
| T-01-35-04 | Repudiation | lever engagement unprovable after the fact | medium | mitigate | The named class-0 line and the surviving round-#14 lines make engaged-and-passing / engaged-and-failing / never-engaged distinct greppable verdicts at 01-36 |
| T-01-35-05 | Tampering | evidence integrity (ledger over-claim) | high | mitigate | The prohibition set forbids any bench claim or G-01-7 flip in this plan; the Task 2 notes record ship-state only (the 01-33 convention, enforced by the Task 2 acceptance criteria) |

## Artifacts this phase produces

- src/image_tx_manager.cpp modified: file-local static helper fullReceiptComplete(const ImageTxEntry&); evictionClassOf gains the class-0 case (documented in its class-contract comment); evictEntriesOlderThan gains the class-0 named Serial.printf line; the supersede call site gains the free-slot capacity gate (no new public symbols, no header change, no wire change)
- .planning/WINDOWS.md entry 3 round-#15 note (table + JSON)
- .planning/phases/01-command-protocol-control/01-UAT.md G-01-7 round-#15 note
- .planning/phases/01-command-protocol-control/COVERAGE.md round-#15 re-affirmation
- .planning/phases/01-command-protocol-control/01-35-SUMMARY.md
- No env vars, no new files in src/ or include/, no protocol constants

<verification>
- pio run both environments 2/2 SUCCESS and verify_protocol_roundtrip.mjs exit 0 at the round's commit (recorded in the SUMMARY)
- PRI-01 orderings and every protected surface grep-pinned with actual line numbers in the SUMMARY
- WINDOWS counts reconcile (2/17/19); both copies carry the round-#15 note; zero bench claims anywhere in the round's records
</verification>

<success_criteria>
- The session-11 over-rejection geometry is structurally impossible: a FULL window request at partial queue occupancy admits without consulting the supersede scan
- At full occupancy, a fully-receipted older entry is the safe victim (class 0) with its named line, ahead of any in-flight receipt-evidenced entry; the honest-reject survives only for genuinely-in-flight-only queues
- The round-#14 discriminator lines, the mid-service deferral, PRI-01 orderings, and every protected constant are intact at HEAD
- The ledgers carry the ship-state honestly; G-01-7 remains open pending 01-36's operator-observed series-A verdicts
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-35-SUMMARY.md` when done
</output>
