---
phase: 01-command-protocol-control
plan: 24
type: execute
wave: 16
depends_on: ["01-21", "01-22", "01-23"]
files_modified:
  - .planning/phases/01-command-protocol-control/01-UAT.md
  - .planning/WINDOWS.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [CTRL-01, CTRL-02, CTRL-03, CTRL-04, CTRL-06, PRI-02, IMG-02, IMG-03]

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low   # calibration store has 0 samples (factor 1, not applied); one operator bench checkpoint + evidence-ledger updates (mirrors 01-20 sizing)

must_haves:
  truths:
    - statement: "G-01-7 series-A acceptance (the truth that has failed four rounds, now on its narrowest named axis): 3 back-to-back unspaced captures on this round's firmware all reach the balloon — 3 ACKs, 3 captures, 6/6 verdicts COMPLETE, zero command timeouts, zero silently absent fulls; the round's new discriminator lines either prove the levers engaged ('re-announce held - inbound window traffic active', 're-announce budget re-armed', 'full-pull activation deadline reached') or name the next mechanism precisely"
      verification: backstop
    - statement: "SC-3 visible-effect pairs (unjudged for FIVE rounds): at fixed QVGA, brightness -2 vs +2 judged as a visible pair, plus at least one other class (contrast -2/+2, saturation -2/+2, or quality 10 vs 30) judged as a pair — per-pair judgments recorded in Test 3; with this clause judged, every settings clause has an operator verdict"
      verification: backstop
    - statement: "WR-03 cadence discriminator: with auto-capture enabled at a known interval (20 s), a manual capture resets the schedule — the next interval capture lands one full interval after the MANUAL capture (both 'Captured image ID' timestamps quoted), not at the pre-manual schedule; this also discharges the SC-5 coincidental-reliance advisory (the 01-19 baseline change finally has bench discrimination on current HEAD)"
      verification: backstop
    - statement: "CIF-cycle and QVGA-restore clauses (Test 3's two unexercised session-6 clauses): one SET_RESOLUTION to CIF (wire 8) with both kinds COMPLETE at CIF-class sizing, and one SET_RESOLUTION back to QVGA (wire 6) with the next capture QVGA-class by wire evidence (chunk count/size), zero FB-OVF at every step"
      verification: backstop
    - statement: "Dashboard glance is operator-LOOKED this time (a UI change, d8ba14e gallery-detail resLabel/filesize, has landed since the last looked-at glance): all seven explicit UI-SPEC considerations hold plus the LED-truth backstop — and the new gallery detail fields render"
      verification: backstop
    - statement: "WINDOWS entries 10-14 (review round #9) end in their decided terminal states with their evidence classes honored: fixed entries carry code + builds + harness reasons with unexercised-at-bench recorded by name unless the session's logs exercised them (e.g. a 'second command frame dropped' line); waived entries carry their reason; none silently flips on nothing"
      verification: backstop
    - statement: "Ledger honesty: G-01-7/WINDOWS entry 3 and Test 3/Test 4 clauses flip ONLY on operator-observed console/dashboard evidence with verbatim quotes, line numbers, and counts; any failed series leaves its gap open with the residual mechanism named in the new discriminator vocabulary — never a forced close"
      verification: explicit
    - "UI-SPEC lift (explicit): before any command the Last Command panel renders the documented 'No commands yet' copy and every settings form renders a complete documented default value — no form is ever shown unfilled"
    - "UI-SPEC lift (explicit): an in-flight command shows the locked 'Sent' state string immediately after POST; the 1s /status poll advances the panel to a terminal state (status-line model, no spinners/skeletons)"
    - "UI-SPEC lift (explicit): Sent/Acked/Failed/Pending counters advance as commands process; 4-column grid on desktop, 2x2 below 480px"
    - "UI-SPEC lift (explicit): each setting is an independent single-field form — a multi-field partial state cannot occur; forms always display a complete value"
    - "UI-SPEC lift (explicit): the status bar collapses 4->2 columns under the 480px media query; counter cells never truncate; Last Command text wraps within card width — no clipping"
    - "UI-SPEC lift (explicit): numeric readouts only — no singular/plural copy; layout identical at 0, 1, and many"
    - "UI-SPEC lift (explicit): .message and label text wrap at 14px/1.5 inside the card; no ellipsis or truncation anywhere in the Phase 1 UI"
    - statement: "UI-SPEC lift (backstop): terminal 'Timeout' / 'Failed (retry N)' with documented error copy renders after retries are exhausted — held-out verification remains the UAT Test 2 hardware proof (already PASS); during this session the link LED stays truthful while commands hold for channel quiet (no red No-link driven by a hold that is working as designed)"
      verification: backstop
  artifacts:
    - "Consolidated bench session logs (balloon12.log / base7.log naming convention) covering series A, the WR-03 cadence discriminator, the SC-3 pairs, the CIF/QVGA-restore clauses, and the dashboard glance — retained in the repo root as every prior session was"
    - ".planning/phases/01-command-protocol-control/01-UAT.md — G-01-7 status flip (or honest residual) with verified_by quotes; Test 3 note updated with the SC-3-pair, CIF-cycle, QVGA-restore, and WR-03 outcomes; Test 4 note carries the cadence discriminator outcome"
    - ".planning/WINDOWS.md — entries 3 and 10-14 updated per evidence; counts reconciled"
    - ".planning/STATE.md — round outcome recorded; /gsd-secure-phase 1 named as the remaining phase-close gate"
    - ".planning/phases/01-command-protocol-control/01-24-SUMMARY.md"
  key_links:
    - "Behavior changes on BOTH boards this round (balloon re-announce/eviction receipt-evidence + review fixes; base full-arm deadline + framer resync) — flash both together BEFORE any radio traffic; mixed firmware yields misleading evidence even though the wire format is unchanged (the 01-09 severance lesson applied to behavior)"
    - "Series-A verdicts <-> this round's three levers: a residual failure must be attributed to its mechanism using the new discriminator lines ('re-announce held', 're-announce budget re-armed', 'full-pull activation deadline reached', receipt-evidenced eviction labels) before any gap is left open — the diagnostics exist precisely to name the next lever (burst-admission depth / IMG_TX_QUEUE_DEPTH is the named fallback if eviction pressure persists)"
    - "SC-3/WR-03 clauses <-> the 01-24 protocol steps: both have ridden five rounds because sessions issued zero settings/auto-capture commands — this session's protocol makes those commands explicit numbered steps, not ride-alongs"
  prohibitions:
    - statement: "MUST NOT fabricate image-transfer, command, or config success — closures cite operator-observed finalize/queue/console lines, never code-level reasoning, except where an entry's recorded evidence class (unexercised/unstageable, named BY NAME) explicitly permits the code+builds+harness flip"
      status: flagged
      verification: backstop
    - statement: "MUST NOT mark G-01-7 resolved on code-level evidence alone — closure requires this bench session's series evidence (6/6 verdicts)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT flash only one board — both boards' behavior changed this round"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT fix newly observed bench defects inside this plan — route them (WINDOWS ledger / todos) as every prior round did; this plan's code work ended at 01-21/01-22/01-23"
      status: flagged
      verification: backstop
    - statement: "MUST NOT show protocol or camera state that does not reflect reality (recalled project prohibition, UAT Test 5 tier): no ACK for unexecuted actions, no green link LED while failed, GET_STATUS fields match actual balloon state"
      status: flagged
      verification: backstop
---

<objective>
Operator bench re-verification session #7 of the burst full-delivery round (plans 01-21/01-22/01-23), finally carrying the two judgments that have ridden unjudged for five rounds (SC-3 visible-effect pairs, WR-03 cadence discriminator) plus Test 3's two unexercised clauses (CIF cycle, QVGA restore) and an operator-LOOKED dashboard glance — then flipping the gap ledgers on evidence.

Purpose: the series-A acceptance truth has narrowed to one named axis (burst full-delivery: the re-announce bound racing the base's burst thumb serialization, and depth-3 eviction ranking by airtime instead of receipt evidence). This round's three levers (balloon receipt-evidence re-announce/eviction, base full-arm deadline) are wired with named discriminator lines that either prove engagement or name the next mechanism. The SC-3 pairs and WR-03 discriminator have zero bench coverage across five sessions because no settings/auto-capture commands were issued — this session makes them explicit protocol steps. The dashboard glance must be LOOKED, not did-not-flag, because d8ba14e changed the UI since the last looked-at glance.

Output: one consolidated bench session (both consoles logged, balloon12.log/base7.log); G-01-7 flipped or honestly rescoped in 01-UAT.md; WINDOWS entries 3 and 10-14 updated per evidence; Test 3 judged on its final open clauses; STATE.md records the outcome and the remaining phase-close gate (/gsd-secure-phase 1).

Spec-less probe fallback: recorded visible skip — phase_req_ids null this run, no probe predicates generated (standard goal-backward verification, the established determination).
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/phases/01-command-protocol-control/01-21-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-22-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-23-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-20-SUMMARY.md

Binding decisions and bench conventions:
- House evidence convention: verbatim console quotes with line numbers and counts, both consoles retained (balloon12.log/base7.log naming), before/after tallies against the session-6 baseline.
- Session-6 baseline to beat (base6.log/balloon11.log): series A 3/3 ACKs and 3/3 captures with zero command timeouts ALREADY achieved; verdicts 2/6 (thumbs 25/26 COMPLETE, thumb 24 INCOMPLETE 2/6, all three fulls lost — 24 evicted at depth-3 balloon11.log:406, 25/26 dropped at the re-announce bound :553/:575); everything else green (zero stored-bytes CRC mismatches, zero CRC FAIL, retry labels in bound, genuine QQVGA thumbs at all 9 captures, 19 COMPLETE / 5 INCOMPLETE overall).
- New discriminator lines to watch (this round's code): balloon 'ImageTx: re-announce held - inbound window traffic active', 'ImageTx: re-announce budget re-armed - window request received for image N', receipt-evidenced class-5 eviction labels ('or receipt-evidenced'); base 'ImageRx: full-pull activation deadline reached - activating image N despite thumbnail heal pending for image M'; from 01-23 (if fixes approved): balloon 'CommandHandler: second command frame dropped - handler busy' (only if two commands land in one drain) and 'Camera disabled due to critical power' (only at critical battery — not stageable).
- SC-3 pairs protocol: brightness -2 vs +2 plus one other class (contrast -2/+2, saturation -2/+2, or quality 10 vs 30 — quality changes file size visibly); judge at fixed QVGA; the queue's SET commands ACK; the pair comparison is the operator judgment; thumbnails suffice unless the full is needed (note which).
- WR-03 discriminator protocol: AUTO_CAPTURE_ENABLE at 20 s, one interval capture, one manual CAPTURE_NOW, then the NEXT interval capture must land ~20 s after the manual (quote both 'Captured image ID' timestamps).
- CIF/QVGA-restore clauses: SET_RESOLUTION 8 (CIF) then 6 (QVGA) — session 5's wire-evidence pattern (CIF full 31 chunks vs QVGA 21; post-restore capture QVGA-class by chunk count) is the judging template; zero FB-OVF at every step is the G-01-8 regression watch.
- WR-01 battery watch: no 'Critical battery' lines; beacons batt=valid (all prior sessions green).
- UAT Test 3 is the 'issue' holder; Tests 1/2/4/5 stand PASS and must not regress (Test 4's 'exact cadence' expectation is unaffected by the baseline change: interval captures still fire at exactly the commanded interval from the last capture of ANY source).
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Bench session #7 — flash both boards, series A (unspaced), WR-03 cadence discriminator, SC-3 visible-effect pairs, CIF cycle + QVGA restore, dashboard LOOK glance</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-7 gap block (truth, session-6 baseline, named levers) and Test 3's note (the riding clauses this session must judge)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — Gaps Summary + behavior_unverified_items + coincidental_reliance_items (the exact truths this session must exercise)
  - .planning/phases/01-command-protocol-control/01-21-SUMMARY.md, 01-22-SUMMARY.md, 01-23-SUMMARY.md — what changed and the new log lines to expect
  </read_first>
  <what-built>
  Plans 01-21 (balloon receipt-evidence re-announce + eviction ranking), 01-22 (base full-arm deadline bounding the thumbnail-heal serialization hold), 01-23 (review round #9 routing + operator-dispositioned companion fixes) — all built green on both targets, harness green.
  </what-built>
  <how-to-verify>
  1. PRE-FLIGHT: pio run -e esp32-s3-balloon -e esp32-s3-basestation (2 succeeded) and node scripts/verify_protocol_roundtrip.mjs (exit 0) on the round's final commit. Flash BOTH boards with that exact firmware (behavior changed on both sides; a mixed pair yields misleading evidence). Start both serial consoles logging to balloon12.log / base7.log.
  2. SERIES A (G-01-7 acceptance, fourth attempt at this truth): from the dashboard, trigger 3 CAPTURE_NOW commands back-to-back WITHOUT waiting. Expected: 3 ACKs (quiet-gate holds may precede them — a held command must NEVER reach 'timeout after 3 retries'); 3 captures; 6/6 finalize lines COMPLETE; every full either transfers, re-announces-and-transfers, or (worst case) is drop-logged by name; watch for the new discriminator lines ('re-announce held', 're-announce budget re-armed', 'full-pull activation deadline reached') — their presence with a passing series proves the levers engaged; their ABSENCE with a passing series means the natural cadence sufficed (also fine — record which). If the series fails, capture the discriminating lines before leaving the gap open.
  3. WR-03 CADENCE DISCRIMINATOR: AUTO_CAPTURE_ENABLE at 20 s; let one interval capture fire; trigger a manual CAPTURE_NOW; verify the NEXT interval capture lands ~20 s AFTER the manual trigger (quote both 'Captured image ID' timestamps from the balloon console). Then AUTO_CAPTURE_DISABLE and confirm zero further automatic captures.
  4. SC-3 VISIBLE-EFFECT PAIRS (fifth round riding — this session MUST judge them): at fixed QVGA — (a) SET_BRIGHTNESS -2, capture; SET_BRIGHTNESS +2, capture; judge the pair visibly different and record the verdict; (b) one other class: contrast -2 vs +2, saturation -2 vs +2, or quality 10 vs 30; capture a pair and judge. Record per-pair verdicts (note whether thumbnails sufficed for judgment).
  5. CIF CYCLE + QVGA RESTORE (Test 3's unexercised clauses): SET_RESOLUTION 8 (CIF) — expect the re-init log, a capture with both kinds COMPLETE at CIF-class sizing, zero FB-OVF; then SET_RESOLUTION 6 (QVGA) — expect ACK plus a subsequent capture at QVGA-class sizing by wire evidence (chunk count ~21 vs CIF ~31 vs SVGA ~82), both kinds COMPLETE.
  6. DASHBOARD LOOK GLANCE (operator-LOOKED, not did-not-flag — d8ba14e changed the UI since the last looked-at glance): 'No commands yet' before the first command; 'Sent' immediately after each POST advancing via the poll; counters advance; link LED stays truthful (green while chunks/holds flow); status bar collapses 2x2 under 480px; no truncated/clipped text; gallery detail renders the resLabel/filesize fields.
  7. Record everything: verbatim quotes with line numbers for every verdict, hold, re-announce, drop, deadline-release, and retry line; before/after tallies vs the session-6 baseline. If ANY series fails: capture the discriminating lines, leave that gap open — do not force a close.
  </how-to-verify>
  <acceptance_criteria>
  - Series A: 3/3 ACKs, 3/3 captures, 6/6 COMPLETE verdicts, zero command timeouts; zero silently absent fulls (transferred, re-announced-and-transferred, or drop-logged by name); new discriminator lines present or their absence explained
  - WR-03: next interval capture lands one full interval after the manual capture — both timestamps quoted; disable stops automatic captures
  - SC-3: brightness pair judged plus one other class judged, per-pair verdicts recorded (Test 3's final open settings clauses)
  - CIF cycle and QVGA restore: both-kinds COMPLETE at the expected size classes, zero FB-OVF at every step
  - Dashboard glance: all seven explicit UI-SPEC considerations hold plus the LED-truth backstop, and the gallery detail resLabel/filesize fields render
  - Both console logs retained as balloon12.log / base7.log with every cited quote locatable by line number
  </acceptance_criteria>
  <resume-signal>Type "approved" with the console excerpts (series-A verdicts + discriminator lines, WR-03 timestamps, SC-3 pair judgments, CIF/QVGA-restore evidence), or describe the failing series with its discriminating log lines</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Flip the ledgers on evidence — 01-UAT.md, WINDOWS.md entries 3 + 10-14, STATE.md</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md, .planning/WINDOWS.md, .planning/STATE.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-UAT.md — the G-01-7 gap block, Test 3 note (SC-3/CIF/QVGA clauses), Test 4 note (WR-03 rides); house quote convention from the G-01-5/G-01-6/G-01-8/G-01-9 resolutions
  - .planning/WINDOWS.md — entries 3 and 10-14 (table + JSON copies must both update; counts in front matter reconcile)
  - .planning/STATE.md — Current Status / Next Steps (the round outcome and the /gsd-secure-phase 1 gate)
  - The operator's Task 1 evidence (approved resume message with console excerpts, plus balloon12.log/base7.log)
  </read_first>
  <action>
  Update the three ledgers strictly from the Task 1 evidence:
  1. 01-UAT.md G-01-7: if series A passed 6/6 per the acceptance criteria, set status resolved with resolved_by naming the lever plans/sites (01-21 receipt-evidence re-announce + eviction ranking; 01-22 full-arm deadline; 01-18 quiet gate carried from session 6), resolved_at, and verified_by quoting the operator-observed lines with line numbers/counts including the discriminator lines that prove lever engagement (match the G-01-8/G-01-9 resolution structure). If it failed, keep status open and extend root_cause with the mechanism the new discriminator lines named, updating missing to the next lever (burst-admission depth / IMG_TX_QUEUE_DEPTH is the named fallback for persistent eviction pressure).
  2. 01-UAT.md Test 3 note: record the SC-3 per-pair verdicts, the CIF-cycle and QVGA-restore outcomes; flip Test 3 to pass only if every clause it holds is now judged and none failed. Test 4 note: record the WR-03 cadence discriminator outcome (this discharges the SC-5 coincidental-reliance advisory from 01-VERIFICATION.md).
  3. WINDOWS.md: entry 3 (G-01-7) flips on series evidence or stays open rescoped with the named residual; entries 10-14 per their recorded dispositions — a fixed entry stays fixed with any bench-exercised evidence appended (e.g. a 'second command frame dropped' line) or its unexercised/unstageable status left named as recorded at 01-23; a waived entry is untouched unless the bench contradicts its waive reason (then flag, do not silently flip). Update BOTH the markdown table and the JSON copy; reconcile open_count/waived_count/fixed_count/total_count in the front matter.
  4. STATE.md: record the round outcome in Current Status (what closed on what evidence, what remains open if anything), and set Next Steps to the security gate (/gsd-secure-phase 1; Phase 2/3 gates also pending) as the remaining phase-close step, then /gsd-progress.
  5. Commit the ledger updates separately from any log retention, message docs(01-24): bench re-verification evidence + ledger flips.
  </action>
  <verify>
  <automated>grep -q "balloon12\|base7" .planning/phases/01-command-protocol-control/01-UAT.md && grep -q "balloon12\|base7" .planning/WINDOWS.md && grep -q "01-24" .planning/STATE.md && grep -q "secure-phase" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
  - Every bench-evidence flip cites operator-observed quotes with line numbers/counts from balloon12.log/base7.log; failed series leave their gap open with the residual mechanism named in the new discriminator vocabulary
  - WINDOWS.md table and JSON copies agree; front-matter counts equal the actual row statuses
  - Session-7 evidence lands in the ledgers themselves whichever way each flip resolved (resolved quotes or residual-mechanism quotes both satisfy the citation check)
  - Test 3 reflects the SC-3-pair, CIF-cycle, and QVGA-restore outcomes; Test 4's note carries the WR-03 discriminator outcome; Tests 1/2/5 notes untouched absent regression evidence
  - STATE.md names /gsd-secure-phase 1 as the remaining phase-close gate
  </acceptance_criteria>
  <done>The gap ledgers reflect hardware truth: G-01-7 closed on 6/6 series evidence or honestly rescoped with its next lever, the five review findings in their decided terminal states, Test 3 judged on its final clauses, and the phase's remaining gate identified.</done>
  <reversibility rating="reversible">Documentation-only ledger updates.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench operator -> firmware flash + consoles | Human performs the flash and observes; logs are retained as evidence (read-only artifacts) |
| RF pair -> bench evidence | The two-radio link under bench conditions produces the verdicts; no untrusted input enters the firmware beyond the CRC16-validated frames covered in 01-21/01-22/01-23 threat models |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-24-01 | Tampering | evidence integrity | medium | mitigate | House convention enforced: closures quote verbatim console lines with line numbers/counts from retained logs (balloon12.log/base7.log); entries 10-14 flip only per their recorded evidence classes, never on absence |
| T-01-24-02 | DoS | mixed-firmware flash invalidating the evidence | high | mitigate | Blocking checkpoint step 1 flashes BOTH boards before any traffic; behavior changes on both sides make a mixed pair's evidence misleading (recorded as a key_link prohibition) |
| T-01-24-03 | Safety | bench hardware | low | accept | Standard bench rig (USB-powered base, battery balloon at desk); WR-01 battery watch green in all prior sessions; nothing in this round changes power paths |
</threat_model>

## Artifacts this phase produces

- balloon12.log / base7.log — retained bench console evidence (repo root, prior-session convention)
- Updated gap block G-01-7 + Test 3/Test 4 notes (01-UAT.md)
- Updated WINDOWS entries 3 and 10-14 with reconciled counts (.planning/WINDOWS.md)
- Round outcome + next-gate record (.planning/STATE.md)
- .planning/phases/01-command-protocol-control/01-24-SUMMARY.md
- No source files, no new symbols, no env vars

<verification>
- Both firmware targets built green and the harness exited 0 immediately before the flash (pre-flight step, on record in the session)
- Every ledger flip traces to a quotable line in the retained logs or carries its explicit evidence-class reason
- WINDOWS counts reconcile with row statuses; /gsd-ship remains blocked exactly as long as an entry is honestly open
</verification>

<success_criteria>
- The round's acceptance is judged on hardware: series A 6/6 verdicts (or the honest residual named with its discriminating evidence), SC-3 pairs and WR-03 discriminator finally judged, CIF/QVGA-restore clauses exercised, dashboard LOOKED
- With G-01-7 closed, the WINDOWS ledger reaches zero open entries and the phase's remaining close gate is the security review (/gsd-secure-phase 1)
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-24-SUMMARY.md` when done
</output>
