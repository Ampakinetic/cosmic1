---
phase: 01-command-protocol-control
plan: 25
type: execute
wave: 17
depends_on: []
files_modified:
  - .planning/debug/d1-crash-regression-push-start.md
  - src/image_tx_manager.cpp
  - include/image_tx_manager.h
  - src/command_handler.cpp
  - platformio.ini
  - .planning/WINDOWS.md
autonomous: false
gap_closure: true
requirements: [CTRL-01, CTRL-06, PRI-02, IMG-01, IMG-03, IMG-04, PRI-03]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low   # calibration store has 0 samples (factor 1, not applied); debug round = decode + diff triage + root-cause-dependent fix (heavier than the 01-23 review round; no bench claims)

must_haves:
  truths:
    - statement: "Every backtrace address in the session-7 crash dumps resolves to a named symbol via addr2line against the retained deployed ELF — crash 1 Core 1 (PC 0x40379576, backtrace balloon.log:327 incl. the 0x00040022 |<-CORRUPTED terminal), crash 1 Core 0 (PC 0x4037948d, backtrace :355), crash 1 saved PC 0x420190e3, crash 2 saved PC 0x400559e3 — with the ELF provably the deployed build (the decode of 0x40379576 and 0x4037d7eb reproduces the inline symbolization already in balloon.log:306/:358)"
      verification: explicit
    - statement: "One root cause is RANKED with evidence and recorded, reconciling the full signature set (i2cWrite ESP_ERR_INVALID_STATE, INT_WDT CPU1 with CPU 0 stuck in spinlock_acquire inside xTaskIncrementTick, the IDLE1 stack-canary second panic, the corrupted stack marker, and crash 2's silent TG0WDT_SYS_RST) — and each named suspect is dispositioned confirmed-or-eliminated: 01-21 pushPending busy-hold (:533-539) + per-entry budget re-arm (:935-941), 01-23 framer inter-byte resync (command_handler.cpp:820-823) + hasCommand guard (:887-893), the base-side 01-22/01-23 cadence changes (indirect, via changed air timing), and the latent-instability-under-new-cadence class"
      verification: explicit
    - statement: "Crash determinism is analyzed at code level: whether the mechanism requires first-post-boot state (both session-7 crashes were the first post-boot capture push — cold queue, first beacon stream, first PSRAM image allocations) and whether spaced captures avoid it; the analysis names the discriminator 01-27's bench uses to confirm"
      verification: explicit
    - statement: "The D1 fix implements the ranked root cause's named lever (fix shape selected at the Task 2 checkpoint from the evidence); round-#10 lever semantics that are NOT the root cause survive — the 01-18 quiet gate, D-05/D-07 pacing, and the wire format are untouched unless the root cause IS a wire-adjacent site (then the change is named in the debug doc)"
      verification: explicit
    - statement: "D2 fix (G-01-11/WINDOWS 16): the busy-hold gate at image_tx_manager.cpp:533 engages ONLY after a genuine inbound window request — a receipt-ever flag (inboundWindowRequestSeen) set at the :869 stamp site (before kind validation, preserving the documented trust-boundary ordering) makes the zero-initialized stamp inert; boot windows no longer satisfy the hold"
      verification: explicit
    - statement: "Both firmware targets build green and the wire-format harness exits 0 on the fix commit"
      verification: explicit
    - statement: "Ledger honesty: WINDOWS entries 15/16 gain their established root cause in the reason field but stay OPEN — the D1/D2 truths flip only on the 01-27 bench session's hardware evidence, never on code-level reasoning (G-01-10's own acceptance shape)"
      verification: explicit
  artifacts:
    - ".planning/debug/d1-crash-regression-push-start.md — the debug-session doc (house convention: .planning/debug/): decoded symbol tables for every crash address, suspect-by-suspect disposition, ranked root cause with evidence, named fix lever, determinism analysis, bench discriminator for 01-27"
    - "src/image_tx_manager.cpp + include/image_tx_manager.h — the D2 receipt-ever flag fix (certain) and the D1 root-cause fix (evidence-selected; sites named in the debug doc)"
    - "src/command_handler.cpp — the D1 fix IF the root cause is the framer/guard suspect (conditional, dispositioned in the debug doc)"
    - "platformio.ini — the D1 fix IF the root cause is loopTask stack exhaustion (conditional: ARDUINO_LOOP_STACK_SIZE raise in the balloon env's build_flags; dispositioned in the debug doc)"
    - ".planning/WINDOWS.md — entries 15/16 reason fields updated with root_cause_established content; counts unchanged (both stay open)"
  key_links:
    - "Retained ELF .pio/build/esp32-s3-balloon/firmware.elf (Aug 28 04:02, the deploy that crashed; log-printed ELF SHA256 3d2b351b4) <-> the addr2line decode — the decode is only valid against the deployed build; the executor proves the match via the two inline-symbol cross-checks before trusting any decoded frame"
    - "Root cause <-> fix shape: the Task 2 selection is evidence-gated — an unevidenced fix is forbidden; if Task 1 cannot rank a root cause, the instrument-first option ships diagnostics and 01-27 discriminates (never a blind fix)"
    - "D1 fix <-> the 01-27 bench moment: series A (the exact crashing pattern) is the acceptance surface; this plan makes NO bench claims"
    - "D2 fix <-> the G-01-7 discriminator line: after the fix the 're-announce held' line means inbound traffic by construction — restoring the evidentiary value 01-27's series-A judgment depends on"
  prohibitions:
    - statement: "MUST NOT flip WINDOWS 15/16 or G-01-10/G-01-11 to resolved in this plan — crash-truth closure requires the 01-27 hardware session (zero resets under the crashing pattern); code + builds + harness only establishes the fix is wired"
      status: flagged
      verification: backstop
    - statement: "MUST NOT ship a fix whose lever is not named by Task 1's evidence — a speculative fix that masks the crash without removing the mechanism leaves the regression latent (the exact failure the debug round exists to prevent)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT alter the wire format, the 01-18 quiet gate constants, or D-05/D-07 pacing while fixing D1 — the harness must exit 0 unchanged and the round-#10 lever semantics survive unless the root cause itself is one of them (then named explicitly)"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT delete or weaken round-#10 diagnostic lines (busy-hold episode log, budget re-arm log, deadline-release log) to hide noise — D2 makes the stamp inert, it does not silence the discriminator"
      status: flagged
      verification: backstop
---

<objective>
Close the G-01-10 root-cause half (01-VERIFICATION.md gap 1 / WINDOWS 15, the phase's top blocker): decode the session-7 crash dumps against the retained deployed ELF, reconcile the I2C INVALID_STATE + INT_WDT CPU1 + IDLE1-canary + TG0WDT signatures into one evidence-ranked root cause, implement the selected fix, and land the D2 boot-epoch stamp fix (G-01-11 / WINDOWS 16) that rides this round — code + builds + harness only, with the hardware acceptance deferred to the 01-27 bench session.

Purpose: there is currently NO clean bench evidence on HEAD firmware — the balloon hard-crashed at both first-post-boot capture pushes (images 35/36, buffers lost, forbidden CAPTURE_NOW terminals), a pattern session 6 ran clean on pre-round-#10 firmware. Every riding truth (G-01-7 series A, SC-3 pairs, WR-03 discriminator, CIF/QVGA clauses) is gated behind this fix, and the D2 spurious boot fire pollutes the exact discriminator line the G-01-7 judgment must watch.

Output: a debug-session doc with the decode, dispositions, ranked root cause, and named lever; the D1 + D2 fixes landed green on both targets with the harness passing; WINDOWS 15/16 carrying their established root cause, honestly still open.

Spec-less probe fallback: recorded visible skip — phase_req_ids null this run, no probe predicates generated (standard goal-backward verification, the established determination).

<assumption_delta_decision>
The deterministic assumption-delta scan fired on the Phase 1 ROADMAP section (terms: "second" in the executed 01-09 bench step "second-card retry", "fallback" in the executed 01-14 plan description "never-wedge fallback"). Decision: no-change — both signals trace to completed-plan prose in the roadmap listing, not to round #11's scope; this round fixes a crash regression and a stamp-inertness defect on the existing single-pipeline architecture, introducing no second platform, auth method, tenant, or source of truth. No identity-model promotion is warranted; no invariant test added (the D1/D2 bench discriminators in 01-27 already encode the regression tripwire).
</assumption_delta_decision>
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-21-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-23-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-24-SUMMARY.md
@.planning/WINDOWS.md

Binding evidence and constraints:
- Crash evidence (retained, verifier-read, every citation locatable): balloon.log 1,125 lines / base.log 429 lines. Crash 1: image 35 ACKed/captured/enqueued (:292-298), manifest sent :300, then :301 i2cWrite ESP_ERR_INVALID_STATE, :302 INT_WDT CPU1 panic, :313-314 second panic (IDLE1 stack canary), EXCCAUSE 0x6, Core 0 backtrace :355 stuck in spinlock_acquire inside xTaskIncrementTick, Core 1 backtrace :327 ends 0x00040022 |<-CORRUPTED, rst:0xc :377-380. Crash 2: image 36 ACKed :509, manifest :517, chunk 1/9 :519-520, first-beacon line :525, then rst:0x7 TG0WDT_SYS_RST :526-528 with NO panic output. Both crashes were the FIRST post-boot capture push.
- Round-#10 delta (the regression scope — session 6 ran the identical pattern clean): commits 866087b (01-21, image_tx trio +106/-5), 84f8aea (01-22, image_rx pair), 3d0aaea (01-23, command/sd/power files + WINDOWS). Zero source drift HEAD..working-tree in src/ and include/.
- Deployed-build identity: .pio/build/esp32-s3-balloon/firmware.elf retained (Aug 28 04:02); the panic printed ELF file SHA256 3d2b351b4 (balloon.log:375). Cross-check the ELF is the deployed build by decoding 0x40379576 (must resolve to esp_cpu_wait_for_intr, matching :306) and 0x4037d7eb (must resolve to spinlock_acquire, matching :358) before trusting any decoded frame.
- addr2line toolchain: xtensa-esp32s3-elf-addr2line under the toolchain-xtensa-esp32s3 package in the PlatformIO packages directory; run with -pfiaC -e against the retained ELF. Note crash 2's saved PC 0x400559e3 sits in the ROM range — its decode may legitimately resolve to a ROM function or nothing; record what it gives.
- Structural context already read at source this planning round: pushPending's busy-hold gate (:533-539) is a pure check-and-return; the beacon early-return (:114-117) and one-transmit-per-pass discipline bound blocking; enqueueCapture's PSRAM copies (:276-300); the framer resync (:820-823) and hasCommand guard (:887-893); the Arduino loop order (main_balloon.cpp loop: processSensors -> processCommunications [CmdHandler then ImageTx] -> processPowerManagement -> processPacketHandling -> legacy sendTelemetryData/Heartbeat/Status -> 1 Hz OLED refresh) — the legacy packet path (sendTelemetryData:956+, review IN-08) still creates packets into the undrained 16-slot queue every 5 s and ran at :521 seconds before crash 2.
- House debug convention: debug-session docs live in .planning/debug/ (see the resolved balloon-bootloops-at-bench.md precedent); evidence quotes carry file:line.
- Scope guards: the 01-18 quiet gate (CMD_TX_CHANNEL_QUIET_MS/HOLD_MAX + channelQuietForTx/canTransmitNow in command_sender.cpp) and the 01-15/01-19 command-path semantics are protected surfaces; the wire harness (scripts/verify_protocol_roundtrip.mjs) is the wire-format tripwire.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decode the crash dumps, disposition every suspect, rank the root cause — .planning/debug/d1-crash-regression-push-start.md</name>
  <files>.planning/debug/d1-crash-regression-push-start.md</files>
  <read_first>
  - balloon.log — crash 1 (:289-384: the capture/manifest sequence, the i2cWrite error, both panic dumps with register/backtrace blocks, the ELF SHA line, the reboot banner) and crash 2 (:495-534: post-reboot boot window incl. the D2 spurious hold :504, the capture/chunk/beacon sequence, the silent rst:0x7); also the three boot windows' 'System ready' lines (:131/:500/:648 area) for the D2 boot-epoch pattern
  - base.log — the base-side counterpart of the crash windows: forbidden CAPTURE_NOW terminals :141/:143, NACK_INVALID retries :167/:199/:236/:273 (what the base experienced while the balloon reset)
  - src/image_tx_manager.cpp — process() (:86-122: sweep/enqueue/beacon-early-return/pushPending order), sendTelemetryBeacon (:128-199), pushPending (:472-554 incl. the busy-hold gate :533-539), enqueueCapture (:236-360), handleWindowRequest's stamp + re-arm sites (:857-945)
  - src/command_handler.cpp — the framer inter-byte resync (:820-823), the hasCommand second-frame guard (:887-893), resetReceiveState (:900+), and the drain/execute path that interleaves with ImageTx in the loop
  - src/main_balloon.cpp — loop() (:253-330: subsystem order incl. the legacy sendTelemetryData/Heartbeat/Status gates and the 1 Hz OLED block), processCommunications (:797-812 region), the health-check block (:557-577, the WR-02 inverted check — context for the boot window only, its fix is 01-26's)
  - platformio.ini — the esp32-s3-balloon env's build_flags (no ARDUINO_LOOP_STACK_SIZE today — the loopTask stack is the framework default) and psram/memory_type settings
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-10 and G-01-11 gap blocks (the exact missing items this task discharges)
  </read_first>
  <action>
  Create the debug-session doc and build it in four numbered sections (house convention: verbatim quotes with file:line for every load-bearing claim):
  1. DECODE. Run the toolchain addr2line with -pfiaC -e against .pio/build/esp32-s3-balloon/firmware.elf over every address in the crash dumps: crash 1 Core 1 (PC 0x40379576 and the :327 backtrace chain 0x42055fb0, 0x42056028, 0x403760a2, 0x4037613e, 0x4201952f, 0x420191ec, 0x42019209, 0x42056303, 0x420191a1, 0x40375eb1, 0x403762d8, 0x00040022), crash 1 Core 0 (PC 0x4037948d and the :355 chain 0x4037948a, 0x4037d7eb, 0x4037e922, 0x4037de8b, 0x4037df08, 0x40377e71, 0x40379573, 0x42055ea5, 0x4037e7ef, 0x4037d699), saved PCs 0x420190e3 (crash 1) and 0x400559e3 (crash 2, ROM range). FIRST prove the ELF is the deployed build: 0x40379576 resolves to esp_cpu_wait_for_intr and 0x4037d7eb to spinlock_acquire, matching the monitor's own inline symbolization (:306/:358); record the check. Interpret against the dump: the panic-handler frames vs the crashing context, whether ANY user (project) frames appear, what the 0x00040022 |<-CORRUPTED terminal and the IDLE1 canary imply about stack/memory integrity, and what crash 2's silent TGDT reset plus ROM saved-PC imply (interrupts-disabled or critical-section stall class).
  2. SUSPECT DISPOSITION. Triage the round-#10 delta (git show 866087b, 84f8aea, 3d0aaea) hunk-by-hunk against the crash phase, and disposition EACH named suspect confirmed-or-eliminated with a stated mechanism: (a) the 01-21 balloon hunks — busy-hold gate (:533-539), global liveness stamp before kind validation (:869), per-entry budget re-arm (:935-941), eviction-class receipt evidence (:227); (b) the 01-23 balloon receive-path hunks — framer inter-byte resync (:820-823) and hasCommand drop guard (:887-893); (c) the base-side 01-22/01-23 changes as an INDIRECT cause (changed TX cadence on the air changes the balloon's receive/transmit interleave timing at the crash phase — note the base held-command quiet-gate lines timing against the balloon's push); (d) latent instability newly exercised by the cadence (e.g., the i2cWrite ESP_ERR_INVALID_STATE from the BMP280 Wire transaction at :301 — determine which I2C master and which call site could emit it in that loop position, and whether the camera SCCB and the Wire bus share hardware). Include a stack/heap audit section: the call depth and largest stack frames live at the crash phase (loop -> processCommunications -> CmdHandler/ImageTx -> enqueueCapture's locals -> transmit), PSRAM/internal-heap state at first-post-boot allocations, and whether loopTask's default stack (no ARDUINO_LOOP_STACK_SIZE in platformio.ini) plausibly overflows into the IDLE1 region given the canary hit.
  3. ROOT CAUSE. Rank ONE primary mechanism that reconciles the full signature set, with the evidence for and against; state the fix lever it names (site + change shape). If the evidence ranks but cannot uniquely confirm (corrupted context is expected for this crash class), say so explicitly and name the discriminating instrumentation (e.g., periodic uxTaskGetStackHighWaterMark(NULL) + heap-free log line) that 01-27's bench session would read.
  4. DETERMINISM + DISCRIMINATOR. Analyze whether the mechanism requires first-post-boot state (cold queue, first beacon stream, first PSRAM image allocations — both session-7 crashes were the first post-boot push) and whether spaced captures avoid it; name the bench discriminator (log line or absence) 01-27 uses to confirm the fix, and the D2 discriminator (boot windows contain zero busy-hold episode lines before the first genuine inbound window request).
  Commit the doc docs(01-25): D1 debug round — crash decode + suspect dispositions.
  </action>
  <verify>
  <automated>grep -q "Root cause" .planning/debug/d1-crash-regression-push-start.md && grep -q "0x40379576" .planning/debug/d1-crash-regression-push-start.md && grep -q "0x400559e3" .planning/debug/d1-crash-regression-push-start.md && grep -qi "866087b" .planning/debug/d1-crash-regression-push-start.md && grep -qi "3d0aaea" .planning/debug/d1-crash-regression-push-start.md</automated>
  </verify>
  <acceptance_criteria>
  - The doc contains a decode table where every listed crash address resolves to a named symbol (or is explicitly recorded as unresolvable-with-reason, e.g., the ROM-range saved PC), and the deployed-ELF cross-check (esp_cpu_wait_for_intr / spinlock_acquire reproducing :306/:358) is recorded before any decoded frame is used
  - Each of the four suspect classes (a)-(d) carries a confirmed-or-eliminated disposition with a stated mechanism and file:line evidence — no suspect is left unexamined
  - Exactly one primary root cause is ranked, with evidence for and against, and it names its fix lever (site + change shape); if confirmation is impossible pre-bench, the doc says so and names the discriminating instrumentation instead of guessing
  - The determinism section answers the first-post-boot question mechanically and names the 01-27 bench discriminator for both D1 (crash-signature absence) and D2 (boot-window busy-hold absence)
  </acceptance_criteria>
  <done>The crash evidence is decoded, every round-#10 suspect dispositioned, one root cause ranked with its named lever, and the bench discriminators defined — the G-01-10 'root cause NOT established' state is replaced by an evidence-ranked mechanism in a retained debug doc.</done>
  <reversibility rating="reversible">Analysis and documentation only; no source changes in this task.</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Fix-shape selection — evidence-gated (operator decision point)</name>
  <files>.planning/debug/d1-crash-regression-push-start.md</files>
  <read_first>
  - .planning/debug/d1-crash-regression-push-start.md — Task 1's decode, dispositions, ranked root cause, and named lever (the input this decision consumes)
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-10/G-01-11 gap blocks (the acceptance shapes the fix must serve)
  </read_first>
  <decision>
  Select the D1 fix shape from Task 1's evidence: implement the ranked root cause's named lever (option a, recommended), ship the top fix plus discriminating instrumentation when confirmation is incomplete (option b), or revert the implicated round-#10 hunk set when no cleaner mechanism exists at the implicated site (option c). The D2 fix in Task 3 is unconditional and rides whichever option is chosen.
  </decision>
  <context>
  A crash fix is only as good as its evidence: a speculative fix can mask the symptom (raising a stack size, gating a log) while the mechanism stays latent — which is exactly the failure mode this debug round exists to close. Task 1's ranked root cause names a lever; this checkpoint makes the selection explicit and recorded (the 01-11 branch-selection precedent). Under YOLO auto-advance the recommended option auto-selects and is recorded for end-of-phase operator confirmation, per the 01-23 convention. Whatever is chosen must keep the protected surfaces intact: wire format, 01-18 quiet gate constants, D-05/D-07 pacing.
  </context>
  <options>
    <option id="option-a">
      <name>Implement the ranked root cause's named lever (recommended)</name>
      <pros>Smallest correct change at the mechanism's site; round-#10 lever semantics and discriminators survive untouched; harness stays green by construction</pros>
      <cons>Valid only if Task 1's evidence uniquely ranks the mechanism — a corrupted-context crash may leave residual uncertainty (mitigated by option b's instrumentation as a ride-along)</cons>
    </option>
    <option id="option-b">
      <name>Top fix plus discriminating instrumentation</name>
      <pros>Honest under incomplete evidence: ships the most-probable fix AND the measurement (stack high-water mark, heap-free counters, or the crash-phase trace line the doc names) that 01-27's bench reads to confirm; converts residual uncertainty into a discriminator</pros>
      <cons>Instrumentation lines must be bounded (not per-loop-pass floods) and later removable; adds a small surface to the bench firmware</cons>
    </option>
    <option id="option-c">
      <name>Revert the implicated round-#10 hunk set</name>
      <pros>Guaranteed regression removal when a specific hunk is implicated but has no clean local fix; bisects by behavior at the next bench</pros>
      <cons>Loses the implicated G-01-7 lever work (busy-hold/budget re-arm or framer guard) until re-implemented correctly; the G-01-7 series-A discriminators lose their lines — the re-implementation rides a later round</cons>
    </option>
  </options>
  <resume-signal>Type "approved" with the chosen option (e.g. "option-a" or "option-b with the stack high-water instrumentation"), or name a different evidence-named lever with its justification from the debug doc.</resume-signal>
  <acceptance_criteria>
  - The resume message records exactly one fix shape and, for option-b/c, the specific instrumentation or hunk set named
  - The selection traces to the debug doc's ranked root cause — a lever the doc does not name routes back to this checkpoint rather than defaulting silently
  </acceptance_criteria>
  <done>The D1 fix shape is explicitly selected and recorded against Task 1's evidence — no unevidenced fix proceeds.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the D1 fix + the unconditional D2 stamp fix, builds + harness, WINDOWS 15/16 reason updates</name>
  <files>src/image_tx_manager.cpp, include/image_tx_manager.h, src/command_handler.cpp, platformio.ini, .planning/WINDOWS.md</files>
  <read_first>
  - .planning/debug/d1-crash-regression-push-start.md — the ranked root cause, the selected lever (Task 2's recorded decision), and the protected-surfaces list
  - src/image_tx_manager.cpp — the busy-hold gate (:533-539), the stamp site (:869, with its trust-boundary ordering comment), and whichever sites the selected lever names
  - include/image_tx_manager.h — the member block around lastInboundWindowRequestMs (:223-232) where the receipt-ever flag lands
  - src/command_handler.cpp — the framer/guard sites (:820-823, :887-893) IF the selected lever touches them
  - platformio.ini — the esp32-s3-balloon env build_flags IF the selected lever is the loopTask stack raise (ARDUINO_LOOP_STACK_SIZE)
  - .planning/WINDOWS.md — entries 15/16 (reason updates only; status stays open; both copies + counts)
  </read_first>
  <action>
  1. Implement the selected D1 fix exactly as Task 2 recorded it, at the site(s) Task 1's doc names. Keep the protected surfaces byte-identical: the wire format (harness is the tripwire), the 01-18 quiet-gate constants and latch, D-05/D-07 pacing, the 01-15 BUSY-deferral scoping, and the 01-19 dispatch gate. If the lever is the loopTask stack raise, add the ARDUINO_LOOP_STACK_SIZE define to the balloon env's build_flags with a comment citing the debug doc's evidence, sized per the doc's audit (record the chosen value and rationale). If the lever ships instrumentation (option b), bound it (a once-per-second-or-slower diagnostic, not a per-pass print) and name its removal condition.
  2. D2 fix (unconditional, G-01-11/WINDOWS 16): add a bool member inboundWindowRequestSeen (include/image_tx_manager.h, initialized false in the constructor and in begin()) beside lastInboundWindowRequestMs; set it true at the :869 stamp site immediately before-or-after the millis() stamp (inside the same before-kind-validation position, preserving the documented trust-boundary ordering); extend the :533 gate so the hold engages only when the flag is true. Do NOT silence or weaken the busy-hold episode log or the budget re-arm log — the fix makes the zero stamp inert, it does not mute the discriminator.
  3. Build both targets (pio run -e esp32-s3-balloon -e esp32-s3-basestation) and run node scripts/verify_protocol_roundtrip.mjs — both green.
  4. Update .planning/WINDOWS.md entries 15 and 16 in BOTH copies (markdown table and JSON): append to the reason fields that the root cause is established (name it in one clause) with the fix landed and builds/harness green, status stays open pending the 01-27 bench hardware evidence, and name the bench discriminator. Reconcile the front-matter counts (unchanged: 3 open / 13 fixed / 16 total — 17-19 are 01-26's to add).
  5. Commit fix(01-25): D1 crash-regression fix per debug round evidence + D2 receipt-ever stamp fix.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation > /tmp/pio-0125.log 2>&1 && grep -qE 'SUCCESS|succeeded' /tmp/pio-0125.log && node scripts/verify_protocol_roundtrip.mjs && grep -q inboundWindowRequestSeen include/image_tx_manager.h && [ $(grep -c inboundWindowRequestSeen src/image_tx_manager.cpp) -ge 2 ] && grep -q "channelQuietForTx" src/command_sender.cpp && grep -q '"id": 15' .planning/WINDOWS.md</automated>
  </verify>
  <acceptance_criteria>
  - The selected D1 fix is source-visible at the site the debug doc names, with its rationale comment citing the debug doc; instrumentation (if any) is bounded and named for removal
  - The D2 fix is source-visible: inboundWindowRequestSeen declared in the header, initialized false at both construction and begin(), set at the :869 stamp site, and gating the :533 hold — the zero stamp can no longer satisfy the busy-hold check
  - The busy-hold episode log, budget re-arm log, and all round-#10 discriminator lines still exist verbatim (nothing silenced)
  - Both builds succeed; the harness exits 0; the 01-18 quiet gate is intact (channelQuietForTx present)
  - WINDOWS 15/16 reasons carry the established root cause and stay open in both copies; counts reconcile
  </acceptance_criteria>
  <done>The D1 mechanism is fixed at its evidence-named site, the D2 boot-epoch false window is inert by construction, everything builds green with the harness passing, and the ledger honestly holds both truths open for the 01-27 hardware session.</done>
  <reversibility rating="costly">The D1 fix lands in the crash path of flight firmware; reverting re-opens a hardware-confirmed crash regression — the 01-27 bench validates before anything builds on it.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link -> balloon receive path | Inbound frames (chunk storms, commands) are the untrusted input crossing into the framer/handler code that was changed in round #10 and is suspect in the crash |
| bench logs -> debug evidence | balloon.log/base.log are read-only retained evidence; the decode must not rewrite or re-interpret away citations |
| deployed ELF -> symbol decode | The retained build artifact is the only valid decode target; a mismatched ELF produces confident nonsense |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-25-01 | Tampering | evidence integrity (decode validity) | high | mitigate | Deployed-build cross-check is mandatory before trusting decoded frames (0x40379576/0x4037d7eb must reproduce the log's own inline symbols); every doc claim carries file:line |
| T-01-25-02 | DoS | the D1 crash regression itself (balloon hard-reset under capture push) | high | mitigate | This plan IS the mitigation: evidence-ranked root cause + fix + bench acceptance gated in 01-27; no flip without hardware evidence (prohibition 1) |
| T-01-25-03 | Tampering | a speculative fix masking the crash without removing the mechanism | medium | mitigate | Task 2 is evidence-gated (an unevidenced lever cannot proceed); option b converts residual uncertainty into bench discriminators instead of silent fixes |
| T-01-25-04 | Information Spoofing | D2 discriminator pollution (boot-epoch false 'receipt') | low | mitigate | The receipt-ever flag makes the hold engage only after genuine inbound traffic, restoring the line's evidentiary meaning for the G-01-7 judgment |
</threat_model>

## Artifacts this phase produces

- .planning/debug/d1-crash-regression-push-start.md — the retained debug-session doc (decode table, suspect dispositions, ranked root cause, determinism analysis, bench discriminators)
- ImageTxManager.inboundWindowRequestSeen (new bool member, include/image_tx_manager.h) — the D2 receipt-ever flag
- The D1 fix at its evidence-named site (file(s) per the Task 2 selection; possibly the ARDUINO_LOOP_STACK_SIZE define in platformio.ini's balloon env)
- WINDOWS entries 15/16 reason updates (root cause established, still open; both copies)
- .planning/phases/01-command-protocol-control/01-25-SUMMARY.md
- No wire-format changes; no new env vars beyond the optional build define

<verification>
- The debug doc's decode is reproducible (the addr2line invocation and address list are recorded in it)
- Both firmware targets build green and the harness exits 0 on the fix commit
- The protected surfaces are diff-verified untouched: wire format, quiet-gate constants, D-05/D-07 pacing
- WINDOWS 15/16 stay open with the root cause recorded — the honest pre-bench state
</verification>

<success_criteria>
- G-01-10's "root cause NOT established" is replaced by an evidence-ranked mechanism with a landed fix, and G-01-11's stamp fix makes the busy-hold discriminator truthful by construction
- The round leaves 01-27 ready: both boards carry the fix, the discriminators are named, and no bench claim was made prematurely
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-25-SUMMARY.md` when done
</output>
