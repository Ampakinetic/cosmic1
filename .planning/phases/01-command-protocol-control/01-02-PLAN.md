---
phase: 01-command-protocol-control
plan: "02"
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - include/command_protocol.h
  - src/command_protocol.cpp
  - include/command_sender.h
  - src/command_sender.cpp
  - include/command_handler.h
  - src/command_handler.cpp
  - scripts/verify_protocol_roundtrip.mjs
autonomous: true
requirements: [CTRL-01, CTRL-06, PRI-02]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "LoRa command packets transmitted from base station to balloon and acknowledged (SC-2): every sequence number 1-65535 survives the balloon's CRC validation (CR-01 closed)"
    - "serializeCommand and serializeResponse reject any packet exceeding the 240-byte LoRa limit via the shared CMD_MAX_PACKET_SIZE constant, and all sender send/receive buffers are sized to that constant (CR-02 closed)"
    - "A packet whose payload or response data contains the 0x0D 0x0A byte pair is received intact by BOTH receivers — no truncation at an embedded end-marker (CR-03 closed)"
    - "Failed commands are retried with timeout and reach a terminal state (SC-4 mechanics): a PENDING command whose transmit repeatedly fails reaches FAILED after maxRetries attempts (D-06: max 3 retry attempts, implemented in plan 01-01 and maintained here); the ACK-timeout window is command-specific (D-05: TRIGGER 2000ms, SETTINGS 5000ms, COMPLEX 10000ms — replacing the flat 2000ms constant) and retries are paced by exponential backoff (D-07: 2000/4000/8000ms between attempts — replacing fixed retryDelayMs pacing); cancelCommand never decrements pendingCommandCount for an already-terminal command (CR-04/WR-03/WR-04 closed)"
    - "GET_STATUS responses are serialized with the STATUS response type (not ACK) and the base-station sender treats both ACK and STATUS as success (WR-05 closed)"
  artifacts:
    - path: "include/command_protocol.h"
      provides: "Shared CMD_MAX_PACKET_SIZE=240 constant next to CMD_MAX_PAYLOAD_SIZE, plus D-05 command-specific ACK-timeout constants (2000/5000/10000ms) and the D-07 retry-backoff base constant (2000ms)"
      contains: "CMD_MAX_PACKET_SIZE"
    - path: "src/command_protocol.cpp"
      provides: "CRC computed over the real sequence byte; oversize rejection via shared constant"
      contains: "calculateCRC16"
    - path: "include/command_sender.h"
      provides: "CommandQueueEntry struct and getCommandQueue enumeration API feeding the D-16 base-station live queue view"
      contains: "getCommandQueue"
    - path: "src/command_sender.cpp"
      provides: "Length-driven response framing, retry terminal states, D-05 per-command ACK-timeout selection, D-07 exponential backoff pacing, cancel guard, STATUS-as-success, getCommandQueue implementation"
    - path: "src/command_handler.cpp"
      provides: "Length-driven command framing, response typed by CommandResult.responseType"
    - path: "scripts/verify_protocol_roundtrip.mjs"
      provides: "Host-side wire-format regression harness (CRC round-trip, oversize rejection, embedded-end-marker framing)"
  key_links:
    - from: "src/command_protocol.cpp serializeCommand"
      to: "src/command_protocol.cpp calculateCRC16"
      via: "real sequenceNumber low byte written at header offset 3 BEFORE the CRC loop; no post-serialization patch"
      pattern: "buffer\\[3\\] = static_cast<uint8_t>\\(cmd\\.sequenceNumber"
    - from: "src/command_sender.cpp processIncomingByte"
      to: "src/command_handler.cpp processIncomingByte"
      via: "identical length-driven framing rule (responses: header+4+dataLength+4; commands: header+5+payloadLength+4) — both sides must use header bytes 4-5 as the body length"
      pattern: "CMD_HEADER_SIZE \\+ 4 \\+ bodyLen \\+ 4|CMD_HEADER_SIZE \\+ 5 \\+ bodyLen \\+ 4"
    - from: "src/command_handler.cpp process"
      to: "src/command_sender.cpp handleResponse"
      via: "successful CommandResult serialized with result.responseType (ACK or STATUS); sender maps both to ACKED"
      pattern: "ResponseType::STATUS"
  prohibitions:
    - statement: "MUST NOT let camera command processing starve or block the balloon's telemetry/flight loop — retry attempts must be paced and reach terminal states instead of retrying forever"
      status: unverified
      flagged: true
      verification: judgment
---

<objective>
Close the four protocol-level defects (CR-01, CR-02, CR-03, CR-04 + WR-03/WR-04/WR-05) that the Phase 1 verifier proved deterministically break the command link. After this plan, a serialized command survives the balloon's CRC validation for every sequence number, embedded CR/LF byte pairs no longer truncate packets, transmit failures reach the FAILED terminal state with pacing, and GET_STATUS responses are typed correctly end to end. Task 3 also encodes two locked decisions the executed code omitted — D-05: command-specific ACK timeouts (TRIGGER 2000ms / SETTINGS 5000ms / COMPLEX 10000ms, replacing the flat 2000ms window) and D-07: exponential retry backoff (2000/4000/8000ms, replacing fixed retryDelayMs pacing) — and exposes the queue snapshot the UI needs for D-16. The plan preserves the packet structure delivered by executed plan 01-01 — D-01: command packet types extend the existing telemetry protocol, D-02: numeric action codes, D-03: camera settings as compact binary payloads — without redesigning it.

Purpose: SC-2 ("LoRa command packets transmitted from base station to balloon and acknowledged") is currently failed by arithmetic-level defects, not hardware. This plan makes the wire protocol correct so CTRL-01/CTRL-06/PRI-02 become hardware-verifiable instead of code-broken.
Output: Fixed command_protocol.h/.cpp (including the new D-05 timeout and D-07 backoff constants), command_sender.h/.cpp (terminal states, per-command timeouts, exponential backoff, queue snapshot API), command_handler framing/response-typing in command_handler.cpp, plus a host-side wire-format regression script.
</objective>

<execution_context>
@C:/Users/Amp/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Amp/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-PATTERNS.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md

Source files (current, defective state — line numbers in the verification report have shifted before and will shift again; anchor by content):
- src/command_protocol.cpp
- src/command_handler.cpp
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Fix CRC/sequence ordering (CR-01) and packet-size limits (CR-02) — make every sequence number validatable</name>
  <read_first>
  - src/command_protocol.cpp (whole file — serializeCommand lines ~44-92 and serializeResponse lines ~145-189 are the analog pair; the response serializer already does it right)
  - include/command_protocol.h (constants block near CMD_MAX_PAYLOAD_SIZE / CMD_HEADER_SIZE)
  - src/command_sender.cpp (transmitCommand buffer, ~line 337)
  - include/command_sender.h (receiveBuffer member, ~line 101)
  - .planning/phases/01-command-protocol-control/01-PATTERNS.md (sections "src/command_protocol.cpp" and "include/command_protocol.h")
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md (gap 1 and gap 6)
  </read_first>
  <files>include/command_protocol.h, src/command_protocol.cpp, include/command_sender.h, src/command_sender.cpp, scripts/verify_protocol_roundtrip.mjs</files>
  <action>
  CR-01 (per verification gap 1): In CommandProtocol::serializeCommand(src/command_protocol.cpp), write the real sequence byte — the low byte of cmd.sequenceNumber, same expression serializeResponse uses for resp.refSequence at its header write — into header offset 3 INSTEAD of the zero placeholder byte, so the CRC16 computed afterward covers the transmitted value. Delete the two post-serialization lines that patch buffer[3] after the end bytes are written (the block after the end-byte writes that assigns the sequence low byte to buffer[3]). Do not touch serializeResponse — it is the correct analog.

  CR-02 (per verification gap 6): In include/command_protocol.h, add a file-scope shared constant CMD_MAX_PACKET_SIZE = 240 (size_t, static constexpr, placed in the existing packet-limits block next to CMD_MAX_PAYLOAD_SIZE; do NOT make it a class member). In both serializeCommand and serializeResponse in src/command_protocol.cpp, replace the literal 240 in the packet-length rejection with CMD_MAX_PACKET_SIZE, and in serializeCommand also explicitly reject cmd.payloadLength greater than CMD_MAX_PACKET_SIZE minus 16 (header 7 + command block 5 + CRC 2 + end 2). In src/command_sender.cpp transmitCommand, change the stack buffer declaration from 128 bytes to CMD_MAX_PACKET_SIZE bytes. In include/command_sender.h, change the receiveBuffer member from 128 bytes to CMD_MAX_PACKET_SIZE bytes. Leave CommandHandler's 256-byte receiveBuffer as is (already above the protocol maximum).

  Create scripts/verify_protocol_roundtrip.mjs (Node, zero dependencies): transcribe the WIRE FORMAT contract from the fixed code — the Modbus-style CRC16 (initial 0xFFFF, reflected polynomial 0xA001, per-byte XOR then 8 shift steps), the fixed serializeCommand layout (start AA 55, type byte, sequence low byte at offset 3, big-endian payloadLength at offsets 4-5, CRC8 pad byte, cmd byte, big-endian sequence, big-endian payloadLength, payload, big-endian CRC16, end 0D 0A), the validateCRC rule (CRC over all bytes except the last 4), and the constants CMD_HEADER_SIZE=7, CMD_MAX_PACKET_SIZE=240. Assert with exit code 0 only if ALL hold: (a) for every sequence number 1..65535, serializing a command and running the balloon-side validation rules (start bytes, end bytes, validateCRC) accepts it; (b) serializeCommand rejects a payload of length CMD_MAX_PACKET_SIZE-15 (225 bytes, which would produce a 241-byte packet); (c) a command whose payload contains the consecutive bytes 0x0D 0x0A passes validation when framed by total length (this clause becomes the framing regression in Task 2 — for now assert the packet itself validates). Add a header comment in the script stating it mirrors src/command_protocol.cpp and must be updated when the wire format changes. Run it once against the OLD algorithm shape (zero placeholder at offset 3, patch after) by keeping a local copy of the defective layout in a clearly named function, and assert the defective variant FAILS the sweep — this proves the harness has teeth.
  </action>
  <verify>
  <automated>node scripts/verify_protocol_roundtrip.mjs && pio run -e esp32-s3-balloon -e esp32-s3-basestation && ! grep -q "will be overwritten" src/command_protocol.cpp && ! grep -q "Fix sequence number in header" src/command_protocol.cpp && grep -q "CMD_MAX_PACKET_SIZE" include/command_protocol.h && grep -q "buffer\[CMD_MAX_PACKET_SIZE\]" src/command_sender.cpp</automated>
  </verify>
  <acceptance_criteria>
  - src/command_protocol.cpp serializeCommand writes the real sequence low byte at header offset 3 before the calculateCRC16 call; grep for the old zero-placeholder header comment ("will be overwritten") returns nothing
  - The post-serialization patch block after the end-byte writes is gone (grep "Fix sequence number in header" finds nothing)
  - include/command_protocol.h contains CMD_MAX_PACKET_SIZE = 240 next to CMD_MAX_PAYLOAD_SIZE
  - serializeCommand and serializeResponse both use CMD_MAX_PACKET_SIZE (no bare literal 240 remains in their length checks)
  - src/command_sender.cpp transmitCommand uses uint8_t buffer[CMD_MAX_PACKET_SIZE]; include/command_sender.h receiveBuffer is uint8_t[CMD_MAX_PACKET_SIZE]
  - scripts/verify_protocol_roundtrip.mjs exits 0 (all sequences 1..65535 validate; oversize payload rejected; defective-variant sweep fails as expected)
  - Both PlatformIO environments build: pio run -e esp32-s3-balloon -e esp32-s3-basestation reports SUCCESS for both
  </acceptance_criteria>
  <done>The command round-trip survives balloon-side CRC validation for every sequence number, oversized payloads are rejected at serialization, and buffers cannot overflow the protocol maximum. CR-01 and CR-02 are closed at the code level.</done>
</task>

<task type="auto">
  <name>Task 2: Length-driven framing in both receivers (CR-03) and STATUS response typing (WR-05)</name>
  <read_first>
  - src/command_sender.cpp (processIncomingByte ~lines 208-241 and handleResponse ~lines 243-279)
  - src/command_handler.cpp (processIncomingByte ~lines 599-634, process ~lines 70-120)
  - src/command_protocol.cpp (serializeCommand/serializeResponse wire layout; createResponsePacket)
  - .planning/phases/01-command-protocol-control/01-PATTERNS.md (sections "src/command_sender.cpp" and "src/command_handler.cpp" — the framing twin rule)
  </read_first>
  <files>src/command_sender.cpp, src/command_handler.cpp, scripts/verify_protocol_roundtrip.mjs</files>
  <action>
  CR-03 (per verification gap 2): Replace the rolling two-byte end-marker scan in BOTH processIncomingByte implementations with length-driven framing, symmetrically. Keep the existing start-byte hunt (0xAA then 0x55 while not inPacket). Once inPacket and the accumulated byte count reaches CMD_HEADER_SIZE, read the big-endian body length from header offsets 4-5 (this field is payloadLength for commands, dataLength for responses — both serializers write it at the same offsets). Compute the expected total: CMD_HEADER_SIZE + 5 + bodyLen + 4 for commands in src/command_handler.cpp, CMD_HEADER_SIZE + 4 + bodyLen + 4 for responses in src/command_sender.cpp. If the expected total exceeds the receive buffer size, or bodyLen exceeds the corresponding protocol maximum (CMD_MAX_PAYLOAD_SIZE for commands, CMD_MAX_RESPONSE_DATA for responses), reset the receive state and return (bogus header). When the accumulated count reaches the expected total, verify the end marker at the last two bytes of that expected length, then run the existing validatePacket + deserialize path, then reset the receive state regardless of outcome. Do not check for the end-marker pair at any other position. The two implementations must follow this identical rule — a fix on only one side recreates the defect on the other.

  WR-05 (per verification warning): In src/command_handler.cpp process(), the success branch currently always builds an ACK via createACK. Change it to build the response with result.responseType as the response type (use CommandProtocol::createResponsePacket with result.responseData/result.responseLength, or createACK only when result.responseType is ACK) so handleGetStatus's STATUS-typed result is transmitted as STATUS. The failure branch (createNACK with result.responseType) is already correct — leave it. In src/command_sender.cpp handleResponse(), extend the success condition so both ResponseType::ACK and ResponseType::STATUS transition the tracked command to ACKED (decrement pendingCommandCount, increment commandsAcked); every other response type keeps the FAILED path.

  Extend scripts/verify_protocol_roundtrip.mjs with a framing section: transcribe the two length-driven accumulators (command flavor and response flavor) exactly as implemented, feed each a byte stream produced by the transcribed serializers with payloads/response-data containing the consecutive bytes 0x0D 0x0A (e.g. a 4-byte payload 0x00 0x0D 0x0A 0x00 and a response whose data starts 0x0D 0x0A), and assert both accumulate to the full packet and validate; also assert a stream with a truncated packet (half the expected bytes, then idle) does not validate. Keep clause (c) from Task 1 green.
  </action>
  <verify>
  <automated>node scripts/verify_protocol_roundtrip.mjs && pio run -e esp32-s3-balloon -e esp32-s3-basestation && ! grep -q "receiveIndex - 2\] == CMD_END_BYTE1" src/command_sender.cpp && ! grep -q "receiveIndex - 2\] == CMD_END_BYTE1" src/command_handler.cpp && grep -q "CMD_HEADER_SIZE + 4 + bodyLen + 4\|CMD_HEADER_SIZE + 5 + bodyLen + 4" src/command_sender.cpp</automated>
  </verify>
  <acceptance_criteria>
  - Neither processIncomingByte contains the old rolling end-marker comparison expression (grep "receiveIndex - 2\] == CMD_END_BYTE1" finds nothing in either file)
  - Both receivers compute an expected total from the header length field before testing the end marker; command side uses header+5+bodyLen+4, response side header+4+bodyLen+4
  - Both receivers reset on expected-total overflow or bodyLen above the protocol maximum
  - src/command_handler.cpp process() success branch honors result.responseType (STATUS results are sent as STATUS, not ACK)
  - src/command_sender.cpp handleResponse() maps ResponseType::ACK and ResponseType::STATUS to the ACKED state; NACK types still map to FAILED
  - scripts/verify_protocol_roundtrip.mjs framing section proves embedded 0x0D 0x0A payloads survive both receivers and truncated streams are rejected
  - Both PlatformIO environments build SUCCESS
  </acceptance_criteria>
  <done>Payloads and response data containing the CR LF byte pair are received intact on both sides of the link, and GET_STATUS round-trips as a typed STATUS response the sender counts as success. CR-03 and WR-05 are closed.</done>
</task>

<task type="auto">
  <name>Task 3: Retry terminal states, command-specific ACK timeouts and exponential backoff (D-05/D-07), cancel guard (CR-04, WR-03, WR-04) + queue getters</name>
  <read_first>
  - src/command_sender.cpp (process ~lines 140-202, cancelCommand ~lines 119-134, retryCommand ~lines 350-362, getCommandState ~lines 382-390)
  - include/command_sender.h (private members, public state-query block)
  - include/command_protocol.h (packet-limits constants block — Task 1 adds CMD_MAX_PACKET_SIZE there; the new D-05 timeout constants and D-07 backoff constant belong in the same block)
  - .planning/phases/01-command-protocol-control/01-PATTERNS.md (section "src/command_sender.cpp" retry state machine excerpt)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md (gap 3: CR-04, WR-03, WR-04 detail)
  </read_first>
  <files>include/command_protocol.h, include/command_sender.h, src/command_sender.cpp</files>
  <action>
  CR-04 (per verification gap 3): In CommandSender::process(), the PENDING branch currently only handles transmit success. Add the failure else-branch mirroring the SENT-timeout TIMEOUT branch: increment retryCount, set lastRetryTime to the current time, and if retryCount has reached maxRetries, set state to FAILED, decrement pendingCommandCount, and increment commandsFailed (terminal — the slot becomes reclaimable); otherwise leave the command PENDING so the pacing check re-attempts it later (D-06: max 3 retry attempts — implemented in plan 01-01, maintained by this terminal-state path).

  D-05 (command-specific ACK timeouts): In include/command_protocol.h, in the same packet-limits constants block as CMD_MAX_PACKET_SIZE, add static constexpr uint32_t constants CMD_ACK_TIMEOUT_TRIGGER_MS = 2000, CMD_ACK_TIMEOUT_SETTINGS_MS = 5000, CMD_ACK_TIMEOUT_COMPLEX_MS = 10000, and CMD_RETRY_BACKOFF_BASE_MS = 2000. In src/command_sender.cpp add a file-scope static helper ackTimeoutFor(uint8_t commandType) returning the timeout for a tracked command's stored CameraCommand value: CameraCommand::CAPTURE_NOW maps to CMD_ACK_TIMEOUT_TRIGGER_MS (fast capture); the seven SET_* commands (SET_QUALITY, SET_BRIGHTNESS, SET_CONTRAST, SET_RESOLUTION, SET_SATURATION, SET_EXPOSURE, SET_WB) plus AUTO_CAPTURE_ENABLE and AUTO_CAPTURE_DISABLE map to CMD_ACK_TIMEOUT_SETTINGS_MS (configuration); GET_STATUS maps to CMD_ACK_TIMEOUT_COMPLEX_MS (multi-setting query). Replace every use of the flat 2000ms ACK-timeout value in the SENT-state timeout comparison with ackTimeoutFor of that command's type, so the window before a TIMEOUT retry is selected per command. If a timeoutMs member or local carried the flat value, stop comparing against it there; remove the member and its initializer only when no other reference remains.

  WR-04 + D-07 (exponential backoff): The retry-delay check at the bottom of the per-command loop is dead code (its only action is the loop's last statement). Move the delay check ABOVE the PENDING and SENT state branches so it actually guards them: when the command is PENDING or SENT, retryCount is above zero, and currentTime minus lastRetryTime is below the backoff for the upcoming retry attempt, skip the rest of the loop body for that command this pass. The backoff is NOT a flat retryDelayMs value: compute backoffMs = CMD_RETRY_BACKOFF_BASE_MS shifted left by (attempt - 1), where attempt is the retry attempt about to run (1..maxRetries), clamping the shift at 2 — the waits are exactly 2000ms before retry 1, 4000ms before retry 2, and 8000ms before retry 3 (D-07: exponential backoff 2s/4s/8s between retries). Wire this to the existing retryCount/lastRetryTime bookkeeping; the flat retryDelayMs comparison disappears from the pacing check (delete the member and its initializer only if nothing else references it). In retryCommand(), on transmit failure also assign sendTime = lastRetryTime (currently only the success path sets sendTime), so the ACK-timeout window restarts from the failed attempt instead of expiring instantly.

  WR-03: In cancelCommand(), decrement pendingCommandCount only when the command is currently in a counted state (PENDING or SENT); an already-terminal command (ACKED/FAILED/TIMEOUT) must not decrement again. The state assignment to FAILED stays unconditional for the found command.

  New public API for plan 01-03 (per-command outcome surfacing and the D-16 live queue view): add to include/command_sender.h a const method getCommandRetryCount(uint16_t sequenceNumber) returning the retryCount of the tracked command with that sequence (0 when not found), implemented in src/command_sender.cpp next to getCommandState using the same lookup loop. Also add to include/command_sender.h a plain struct CommandQueueEntry { uint16_t sequenceNumber; uint8_t commandType; CommandState state; uint8_t retryCount; } and a const method getCommandQueue(CommandQueueEntry* out, uint8_t maxEntries) that fills out with one entry per non-IDLE slot of the command table in slot order (commandType is the slot's stored CameraCommand value as a byte) and returns the entry count — the base-station UI (plan 01-03 Task 3) renders every occupied slot as the live command queue (D-16: pending/in-progress states for all commands, not just the last one). Implement it in src/command_sender.cpp next to getCommandState using the same slot iteration.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation && grep -q "commandsFailed++" src/command_sender.cpp && test "$(grep -c "pendingCommandCount--" src/command_sender.cpp)" = "5" && grep -q "getCommandRetryCount" include/command_sender.h && grep -q "getCommandRetryCount" src/command_sender.cpp && grep -q "CMD_ACK_TIMEOUT_TRIGGER_MS" include/command_protocol.h && grep -q "CMD_ACK_TIMEOUT_SETTINGS_MS" include/command_protocol.h && grep -q "CMD_ACK_TIMEOUT_COMPLEX_MS" include/command_protocol.h && grep -q "CMD_RETRY_BACKOFF_BASE_MS" src/command_sender.cpp && grep -q "ackTimeoutFor" src/command_sender.cpp && grep -q "getCommandQueue" include/command_sender.h</automated>
  </verify>
  <acceptance_criteria>
  - src/command_sender.cpp PENDING branch has an else-path that increments retryCount, sets lastRetryTime, and transitions to FAILED with pendingCommandCount-- and commandsFailed++ when retryCount reaches maxRetries (D-06: max 3 retry attempts)
  - include/command_protocol.h defines CMD_ACK_TIMEOUT_TRIGGER_MS = 2000, CMD_ACK_TIMEOUT_SETTINGS_MS = 5000, CMD_ACK_TIMEOUT_COMPLEX_MS = 10000, and CMD_RETRY_BACKOFF_BASE_MS = 2000 next to CMD_MAX_PACKET_SIZE (D-05/D-07), and the SENT-state timeout comparison no longer uses a flat 2000ms value
  - The SENT-state ACK-timeout window comes from ackTimeoutFor per command type: CAPTURE_NOW 2000ms, the seven SET_* commands plus AUTO_CAPTURE_ENABLE/AUTO_CAPTURE_DISABLE 5000ms, GET_STATUS 10000ms (D-05)
  - include/command_sender.h declares struct CommandQueueEntry and getCommandQueue(CommandQueueEntry*, uint8_t) const, implemented in src/command_sender.cpp, returning one entry per non-IDLE slot with sequence, command type, state, and retry count (feeds D-16)
  - The retry-delay check executes BEFORE the PENDING/SENT branches and skips transmit/retry when currentTime - lastRetryTime is under the D-07 exponential backoff for the upcoming attempt — 2000ms before retry 1, 4000ms before retry 2, 8000ms before retry 3 — with the shift clamped so no larger value can occur (no longer the final statement of the loop body, and no longer a flat retryDelayMs comparison)
  - retryCommand() sets sendTime on both transmit success and transmit failure
  - cancelCommand() decrements pendingCommandCount only for commands in PENDING or SENT state
  - pendingCommandCount-- appears exactly at the five counted transitions: ACKED, NACK-FAILED, TIMEOUT, PENDING-transmit-FAILED, and the guarded cancel path
  - include/command_sender.h declares getCommandRetryCount(uint16_t) const and src/command_sender.cpp implements it
  - Both PlatformIO environments build SUCCESS
  </acceptance_criteria>
  <done>A command whose transmission repeatedly fails terminates in FAILED after maxRetries paced attempts (D-06: max 3) instead of retrying forever; ACK-timeout windows are command-specific (D-05: TRIGGER 2000ms / SETTINGS 5000ms / COMPLEX 10000ms) and retries back off exponentially (D-07: 2000/4000/8000ms); cancellation cannot underflow the pending count; the UI plan can query per-command retry counts and the full queue snapshot for its D-16 live queue view. CR-04, WR-03, and WR-04 are closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web client → base station HTTP | Unauthenticated requests on the AP-mode WebServer (all POST routes) |
| RF medium → both receivers | Any transmitter on the shared 915 MHz channel can inject bytes into the UART byte stream |
| Base station stack → E32 UART | transmitCommand serializes onto a fixed stack buffer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | processIncomingByte (both) | medium | accept | Injected/misaligned bytes now fail length+end-marker+CRC checks and reset the receiver; full command authentication is out of scope for v1 (single balloon, hobby RF) — revisit if deployed shared-spectrum |
| T-01-02 | Tampering | sendCommand payloadSize | high | mitigate | CR-02: serializeCommand rejects payloadLength above CMD_MAX_PACKET_SIZE-16 and buffers are sized to the constant — the stack-overflow path is closed |
| T-01-03 | DoS | CommandSender::process | medium | mitigate | CR-04/WR-04: transmit failures reach FAILED after maxRetries paced attempts, bounding the retry storm; the residual ~1s per-attempt AUX block (WR-01) is deferred to hardware bring-up — see Deferred Warnings |
| T-01-04 | Spoofing | LoRa command protocol | low | accept | No authentication in the Phase 1 protocol by design (CRC16 is integrity, not authenticity); accepted for v1 with rationale, tracked for a future shared-secret field |
</threat_model>

<deferred_warnings>
Explicitly deferred to hardware bring-up (per 01-VERIFICATION.md warnings, with rationale):

- WR-01 (E32LoRa::transmit blocks up to 7s in AUX polling): a real non-blocking rework needs measured AUX timing on physical modules; this plan bounds the retry-storm consequence at the state-machine level (terminal states + pacing) so the loop is no longer starved indefinitely. Hardware UAT item 2 exercises this.
- WR-02 (E32 config API contradicts datasheet — no 0xC0 prefix): the config path is uncalled in Phase 1; changing register-frame format without a module to validate risks writing garbage either way. Fix during radio bring-up when the config path is first exercised.
</deferred_warnings>

<flagged_assumptions>
Spec-less edge-probe items left unresolved at plan time (never auto-dismissed; surfaced for the next verification/UAT cycle):

- CTRL-01 (unclassified — review manually): assumed the manual-trigger path needs no behavior beyond CRC-correct delivery; acceptance is the hardware round-trip test.
- CTRL-06 (unclassified — review manually): the retry contract is the CONTEXT.md parameter set — D-05: per-command-type ACK timeouts (2000/5000/10000ms), D-06: max 3 retries, D-07: exponential backoff (2000/4000/8000ms); whether those durations suit real radio latency is the hardware UAT degraded-link test.
- PRI-02 (idempotency — what happens if this runs twice on the same input?): on ACK loss the balloon re-executes a retried command; setters are idempotent but CAPTURE_NOW may capture twice. Accepted for Phase 1 (image IDs are per-execution); deduplication belongs to Phase 2 image sequencing.
- PRI-02 (concurrency — if interrupted or run in parallel, what is guaranteed?): single-threaded loop serializes all processing; the pacing fix guarantees retries cannot stack within one command, but LoRa RX during a blocking transmit can still lose bytes (accepted, hardware bring-up will quantify).
</flagged_assumptions>

<verification>
- node scripts/verify_protocol_roundtrip.mjs — full sequence sweep validates, oversize rejected, embedded end-marker framing survives both receivers, defective-variant sweep fails
- pio run -e esp32-s3-balloon -e esp32-s3-basestation — both envs SUCCESS
- Structural gates per task acceptance_criteria (grep-level assertions on the removed defect patterns, plus presence of the D-05 timeout constants, D-07 backoff constant, ackTimeoutFor, and getCommandQueue)
- Hardware round-trip remains a UAT item (01-VERIFICATION.md "Human Verification Required" items 1-2) — this plan closes the code-level blockers only
</verification>

<success_criteria>
- All four critical review findings (CR-01..CR-04) and WR-03/WR-04/WR-05 are closed at the code level with build-green proof
- The wire-format regression script exists and passes, preventing silent reintroduction
- SC-2's blocking defect is removed; CTRL-01/CTRL-06/PRI-02 become verifiable on hardware
</success_criteria>

<artifacts_produced>
Symbols and files this plan creates or changes (for drift-verification exclusion):

- include/command_protocol.h: new constant CMD_MAX_PACKET_SIZE (size_t, 240); new D-05 timeout constants CMD_ACK_TIMEOUT_TRIGGER_MS/CMD_ACK_TIMEOUT_SETTINGS_MS/CMD_ACK_TIMEOUT_COMPLEX_MS (2000/5000/10000ms) and D-07 backoff constant CMD_RETRY_BACKOFF_BASE_MS (2000ms)
- include/command_sender.h: receiveBuffer resized to CMD_MAX_PACKET_SIZE; new method getCommandRetryCount(uint16_t) const; new struct CommandQueueEntry and method getCommandQueue(CommandQueueEntry*, uint8_t) const
- src/command_sender.cpp: length-driven response framing in processIncomingByte; PENDING transmit-failure terminal path; repositioned retry-delay pacing using D-07 exponential backoff; D-05 per-command ACK-timeout selection via ackTimeoutFor; cancelCommand state guard; STATUS-as-success in handleResponse; buffer resized in transmitCommand; retryCommand sets sendTime on failure
- src/command_handler.cpp: length-driven command framing in processIncomingByte; success responses typed by CommandResult.responseType
- src/command_protocol.cpp: real sequence byte before CRC; CMD_MAX_PACKET_SIZE-based rejection in both serializers; post-hoc patch removed
- scripts/verify_protocol_roundtrip.mjs: NEW host regression harness (CRC sweep, oversize rejection, framing accumulator)
</artifacts_produced>

<output>
Create `.planning/phases/01-command-protocol-control/01-02-SUMMARY.md` when done
</output>
