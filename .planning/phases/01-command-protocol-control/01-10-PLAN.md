---
phase: 01-command-protocol-control
plan: 10
type: execute
wave: 7
depends_on: ["01-09"]
files_modified:
  - src/main_balloon.cpp
  - .planning/phases/01-command-protocol-control/01-UAT.md
  - .planning/WINDOWS.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [IMG-02, IMG-03, IMG-06, PRI-03, CTRL-02]
user_setup:
  - service: bench-hardware
    why: "G-01-5/G-01-6 are hardware truths — gap closure requires an operator bench session"
    dashboard_config:
      - task: "Serial console logging on BOTH boards (USB UART0) for the discriminator capture"
        location: "PlatformIO serial monitor / terminal per board, timestamps on"
      - task: "Ability to relocate the balloon unit >= 10 m from the base (B5 separation discriminator)"
        location: "Physical bench arrangement"

estimate:
  tokens: 5200
  raw_tokens: 5200
  tasks: 3
  confidence: low   # calibration store has 0 samples (factor 1, not applied); analog: 01-09 estimated 8000, actual 2278 — bench-heavy plans over-estimate

must_haves:
  truths:
    - "G-01-6 discriminator evidence recorded in 01-UAT.md: the JSON total from GET /gallery?page=1 compared against the dashboard grid count, with the branch named (total > grid = refresh-stale; total == grid = persistence-stopped)"
    - "G-01-5 serial discriminator evidence captured on both consoles for at least one capture — base: [E32TX] cmd=30 heal-request rows, [FRAME] type=13 CRC FAIL counts, ImageRx finalize verdicts (kind 0 and kind 1); balloon: manifest/chunk push lines, THUMBNAIL window armed, window chunk sent|FAILED"
    - "Reliability metric recorded per the G-01-5 escalation: capture-count vs complete-count per kind (thumbnail, full) at close range AND at increased separation — the B5 close-range-overload discriminator"
    - "Second-capture re-test result recorded: whether a fresh capture's thumbnail arrives intact (per-burst vs systematic loss)"
    - "SC-3 ride-along clauses closed on hardware: at least brightness, saturation, and quality produce visible differences in captured images, and the CIF 400x296 resolution option captures successfully (closes 01-VERIFICATION.md's one behavior-unverified truth)"
    - "WR-01 closed at code level: zero dummy PowerData constructions remain in src/main_balloon.cpp (grep '0.1f, 85' == 0); battery-driven safety branches act only on validity-gated PowerMgr readings; both firmware targets build green"
    - "UI-SPEC loading truth holds through the ride-along series: an in-flight command shows the locked 'Sent' state immediately after POST and the 1s poll advances the panel to a terminal state"
    - "UI-SPEC populated truth holds through the ride-along series: Sent/Acked/Failed/Pending counters advance as commands process; every settings form displays a complete value"
    - statement: "Terminal 'Timeout' / 'Failed (retry N)' with documented error copy renders after retries are exhausted — held-out verification is the hardware UAT degraded-link test; the CommandState-to-string mapping itself is code-verifiable without hardware"
      verification: backstop
  artifacts:
    - "src/main_balloon.cpp — WR-01 fix: three dummy PowerData sites replaced with validity-gated PowerMgr reads + static helper batteryReadingValid()"
    - ".planning/phases/01-command-protocol-control/01-UAT.md — G-01-5/G-01-6 evidence blocks updated with discriminator results and the reliability tally"
    - ".planning/phases/01-command-protocol-control/01-10-SUMMARY.md — evidence table mapping every plan truth to operator observations"
  key_links:
    - "PowerMgr().update() 1s cadence (main_balloon.cpp:798-802) feeds validity-gated reads at all three former dummy sites (:727, :805, :916-918)"
    - "Battery validity gate ([1.8,8.0] V AND nonzero raw ADC) precedes every emergency/low-power safety action — floating sense line preserves availability"
    - "Both firmware targets build green and wire harness (scripts/verify_protocol_roundtrip.mjs) stays green after the WR-01 change"
  prohibitions:
    - statement: "MUST NOT fabricate image-transfer or config success — every reliability tally and gap-closure verdict comes from bench console finalize lines or operator observation, never assumption"
      status: flagged
      verification: backstop
    - statement: "MUST NOT mark G-01-5 or G-01-6 resolved on code-level evidence alone — closure requires operator-observed hardware truth (T-01-09-03 discipline)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT let an invalid battery reading trigger emergency or camera-disable — the [1.8,8.0] V + nonzero-raw-ADC gate precedes every safety action"
      status: flagged
      verification: backstop
---

<objective>
Gap-closure discriminator round for G-01-5 (thumbnail push-burst loss, escalated: fulls also failing at close range — new candidate B5, 30 dBm PA RX-overload/desense) and G-01-6 (gallery locked at 6), plus the two enumerated ride-alongs (SC-3 settings visible-effect + CIF 400x296 spot-checks; battery-telemetry honesty via WR-01).

Purpose: The phase's one open hardware failure (G-01-5, WINDOWS ledger entry 2 — blocks /gsd-ship) and the new G-01-6 are both UNDIAGNOSED at the mechanism level. Every discriminator log line already exists in the firmware and is enabled by default (DEBUG_IMAGE_TX/DEBUG_IMAGE_RX/DEBUG_COMMAND_SENDER all default true); the missing artifact is the evidence itself. This round captures it in one structured bench session and routes the outcome — remediation branch or closure — into plan 01-11. The only unconditional code is WR-01 (dummy PowerData), which touches the power path only and cannot confound the transfer-path evidence.

As a balloon operator, I want every captured photo — thumbnail and full — to arrive intact and appear in a growing gallery, so that I can trust the downlink during flight.

Output: WR-01 commit; discriminator evidence in 01-UAT.md (both gaps); reliability tally close-vs-separated; SC-3 spot-check results; routing decision inputs for 01-11.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/phases/01-command-protocol-control/01-09-SUMMARY.md

Prior-round decisions that bind this round:
- 01-08 single-lever discipline (STATE.md): pacing/stall-window changes stay OUT unless the discriminator run proves B4 saturation persists at 9.6 kbps — this round observes only; remediation levers land in 01-11 behind the routing decision.
- 01-09 severance lesson: both boards must run rate-matched firmware; any e32-config divergence severs the pair.
- D-22: the kind-addressed windowed heal is the SINGLE reliability mechanism; D-24 bounds consecutive unhealed stalls; D-48: the Incomplete badge is honest degradation, never fabricated success.
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-01 closure — replace the three dummy PowerData sites with validity-gated PowerMgr reads</name>
  <files>src/main_balloon.cpp</files>
  <read_first>
  - src/main_balloon.cpp — the three dummy sites: updateAppState (:726-728), processPowerManagement (:804-805 and its safety branches :808-824), sendTelemetryData (:915-918); the existing PowerMgr().update() 1s cadence (:798-802) and the analogRead(BATTERY_SENSE_PIN) use at :686 (proves sensor_pins.h is already included)
  - src/power_manager.h — accessors getBatteryVoltage() :143, getBatteryPercentage() :144, getTotalCurrent() :147, getBatteryStatus() :125
  - src/image_tx_manager.cpp — :150-160, the beacon's battery validity-gate idiom to replicate ([1.8,8.0] V AND analogRead(BATTERY_SENSE_PIN) != 0 — floating sense line never reports a fake pack)
  </read_first>
  <action>
  Fold WR-01 into this round (01-VERIFICATION.md recommendation — rationale: the dummy {3.7f, 0.1f, 85} keeps emergency/low-power branches permanently unreachable AND puts a fabricated 85% into telemetry, violating the phase's own no-fabricated-state prohibition; the fix is power-path-only so it cannot confound the G-01-5/G-01-6 discriminator evidence; PowerMgr is already wired at 1s cadence so no new timing work exists).

  1. Add a file-static helper in main_balloon.cpp: bool batteryReadingValid() — reads PowerMgr().getBatteryVoltage() and returns true only when the voltage is in [1.8f, 8.0f] AND analogRead(BATTERY_SENSE_PIN) != 0 (mirror image_tx_manager.cpp:154 exactly; a second read of the same gate idiom, not a new policy).
  2. Site 1 — updateAppState (:727): replace the literal PowerData construction with values from PowerMgr (voltage = getBatteryVoltage(), current = getTotalCurrent(), percentage = (int)getBatteryPercentage()). Set appState.lowPowerMode = (valid && percentage < BATTERY_LOW_THRESHOLD) — an invalid reading leaves lowPowerMode false, preserving the prior available behavior.
  3. Site 2 — processPowerManagement (:805): same replacement. The CRITICAL and LOW branches (:808-824) act ONLY when batteryReadingValid() is true — a floating or garbage sense line must NEVER triggerEmergency("Critical battery level") or disable the camera mid-bench/mid-flight. On the validity transition (invalid->valid or valid->invalid, tracked with a static bool), emit ONE SYS_WARNING naming the measured voltage and the gate verdict — never per-loop spam.
  4. Site 3 — sendTelemetryData (:916-918): telemetryData.batteryVoltage = PowerMgr().getBatteryVoltage(); batteryCurrent = PowerMgr().getTotalCurrent(); batteryPercentage = (int)PowerMgr().getBatteryPercentage(). Leave telemetryData.rssi (-85 default) untouched — E32 provides no RSSI (03-03 decision); that is a separate documented limitation, not WR-01.
  5. Do NOT touch any image_tx/image_rx/e32/sd file — the discriminator must observe the transfer path exactly as the 01-09 bench last ran it.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation (both SUCCESS) && node scripts/verify_protocol_roundtrip.mjs (exit 0) && powershell -Command "(Select-String -Path src/main_balloon.cpp -Pattern '0.1f, 85').Count -eq 0" && powershell -Command "(Select-String -Path src/main_balloon.cpp -Pattern 'batteryReadingValid').Count -ge 4"</automated>
  </verify>
  <acceptance_criteria>
  - src/main_balloon.cpp contains zero occurrences of the dummy literal pattern "0.1f, 85"
  - src/main_balloon.cpp contains a batteryReadingValid() definition plus at least 3 call sites (count >= 4 matches)
  - The critical/low battery branches in processPowerManagement are guarded by the validity check before triggerEmergency / Camera().enableCamera(false)
  - `pio run -e esp32-s3-balloon -e esp32-s3-basestation` reports 2 succeeded
  - `node scripts/verify_protocol_roundtrip.mjs` exits 0 (49/49 clauses)
  - No file under src/ other than main_balloon.cpp is modified (git status clean otherwise)
  </acceptance_criteria>
  <done>Dummy PowerData eliminated; safety branches validity-gated; telemetry battery fields read PowerMgr; both targets and the harness green; single-file diff committed atomically (e.g. fix(01-10): wire PowerMgr into power state/telemetry, validity-gated (WR-01)).</done>
  <reversibility rating="reversible">Pure wiring of existing accessors; revert restores the dummy in one commit.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bench discriminator round — G-01-6 zero-tooling check, G-01-5 serial discriminator, reliability series (B5), second-capture re-test</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md, .planning/WINDOWS.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-5 and G-01-6 missing lists (this task executes them verbatim)
  - .planning/phases/01-command-protocol-control/01-09-SUMMARY.md — the bench-session evidence-table format to reproduce
  - src/image_tx_manager.cpp — log lines :505, :537, :592, :728, :767 (balloon side)
  - src/command_sender.cpp — log lines :383, :519 (base side); src/image_rx_manager.cpp :678/:717 (finalize verdicts)
  </read_first>
  <what-built>
  Task 1's WR-01 build is the only code change; this task is the evidence-capture session the gaps enumerate. All discriminator lines already exist and are enabled by default (DEBUG_IMAGE_TX true, DEBUG_COMMAND_SENDER true — verify at flash time that no build flags disable them).
  </what-built>
  <how-to-verify>
  PREP: Flash the balloon with the Task-1 build (pio run -t upload -e esp32-s3-balloon); base keeps its current firmware (WR-01 is balloon-side only; no e32 config touched). Open a serial console on EACH board with timestamps.

  STEP 1 — G-01-6 discriminator (zero tooling, run FIRST, ~1 minute):
  a. Browser address bar: http://192.168.4.1/gallery?page=1 — note the JSON "total" and "pageCount".
  b. Compare against the dashboard gallery grid item count.
  c. Record the branch: total > grid count => refresh-signal stale (client never refetched); total == grid count (e.g. both 6) => persistence/indexing stopped — then check the base console for "ImageRx: image N kind K finalized" lines around later captures: finalizing-but-not-indexed names an index defect; NO finalize lines at all names transfer-slot stall (G-01-5 symptom) as the cause.
  d. Optional corroboration: one /gallery/{id} spot-check for an id > the 6th.

  STEP 2 — G-01-5 serial discriminator (the tracer capture):
  a. With BOTH consoles logging, trigger ONE capture from the dashboard.
  b. Base console — capture: every "[E32TX] cmd=30" row (heal/window requests; count them), every "[FRAME] type=13 CRC FAIL" line (count them during the thumbnail push phase), and the two finalize lines "ImageRx: image N kind 0 finalized ..." (thumbnail) and "kind 1 finalized ..." (full) with their COMPLETE/INCOMPLETE verdicts and X/Y chunk counts.
  c. Balloon console — capture: "ImageTx: manifest(image N, thumb ... B, ... chunks)" + the "ImageTx: chunk(image N, x/y, ... B) sent|FAILED" push-burst rows, then "ImageTx: FULL manifest(...)" and — if the base healed — "ImageTx: THUMBNAIL window armed for image N (chunks X..Y)" and "ImageTx: window chunk(image N, x/y, ... B) sent|FAILED" rows.
  d. Mechanism reading: base logged repeated cmd=30 AND balloon logged THUMBNAIL window armed + window chunks sent => heal served but lost on air (B2/B4/B5 class); balloon logged NO arming while base retried cmd=30 => servicing defect (branch c); base never issued cmd=30 within the observation window => heal never fired — timing/margin edge (gate analysis needed); CRC FAIL counts cluster during the push => air corruption, not loss.

  STEP 3 — Second-capture re-test: trigger a SECOND capture; record whether its thumbnail arrives intact (per-burst loss) or is again corrupt (systematic).

  STEP 4 — Reliability series (the escalation metric, B5 discriminator):
  a. CLOSE range (current bench placement): trigger a series of 8-10 captures. Tally per kind from the finalize lines: captures triggered vs finalized COMPLETE vs finalized INCOMPLETE (thumbnail and full separately). Also note any capture whose transfer never finalized within the observation window.
  b. SEPARATED: relocate the balloon unit >= 10 m from the base (another room is fine); repeat the 8-10 capture series and the same tally.
  c. B5 verdict: incomplete rate at close range MUCH higher than separated => close-range RX overload/desense (B5) confirmed; roughly equal rates => B2/B4 class (loss/saturation regardless of range); healthy at both => the escalated report was episodic.

  STEP 5 — Record everything: append the captured lines (or verbatim excerpts) + both tallies to 01-UAT.md under G-01-5, and the Step-1 branch call under G-01-6. Update WINDOWS.md entry 2's reason field with the discriminator outcome (leave status open until 01-11 closes or the CLOSE branch is taken).
  </how-to-verify>
  <acceptance_criteria>
  - 01-UAT.md G-01-5 block contains: at least one base-console excerpt with [E32TX] cmd=30 rows and the finalize verdicts, at least one balloon-console excerpt with the push/window lines, the second-capture re-test result, and the two reliability tallies (close vs separated) with the B5 verdict
  - 01-UAT.md G-01-6 block contains: the /gallery?page=1 JSON total vs grid count comparison and the named branch (refresh-stale vs persistence-stopped) with the console evidence that picked it
  - The mechanism class for G-01-5 is NAMED from the trace (heal-lost-on-air vs heal-not-serviced vs heal-not-fired), not guessed
  - WINDOWS.md entry 2 reason names the discriminator outcome
  </acceptance_criteria>
  <done>Both gaps carry captured bench evidence; G-01-6 has its branch call; G-01-5 has its mechanism class and the close-vs-separated reliability metric; evidence recorded in 01-UAT.md before resume.</done>
  <resume-signal>Type "bench-evidence-recorded" plus a one-line summary of the G-01-5 mechanism class and the G-01-6 branch, or describe what blocked the session</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SC-3 ride-along spot-checks — settings visible-effect series, CIF 400x296, WR-01 runtime regression watch</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — behavior_unverified_items (SC-3 clauses this task closes)
  - .planning/phases/01-command-protocol-control/01-UAT.md — Test 3 (the clauses ride this gap round)
  - src/main_basestation.cpp — section#capture forms (:2266-2512) and the delegated submit handler (:1511-1578); the settings route names for the exercise list
  </read_first>
  <what-built>
  The 7 camera-settings forms and the CIF resolution option are code-complete since 01-03/01-04/01-06 but their visible-effect clauses were never exercised — images only began flowing end-to-end at the 01-09 bench. This task closes UAT Test 3's residual clauses and 01-VERIFICATION.md's single behavior-unverified truth.
  </what-built>
  <how-to-verify>
  1. Settings visible-effect series — for each of brightness (lowest vs highest), saturation (minimum vs maximum), and quality (highest vs lowest): set the value in-page, trigger a capture, and compare the received images pairwise. Record per setting: visibly different / not visibly different.
  2. CIF spot-check — select the CIF 400x296 frame-size option, submit in-page, trigger a capture: the SET command must ACK (not NACK), the image must arrive, and the capture must be 400x296 (visual aspect vs the default QVGA 320x240, or the gallery detail view's dimensions if available). Record the verdict.
  3. WR-01 runtime regression watch — across the whole session (Tasks 2+3): the balloon must NEVER log "Critical battery level" / enter Emergency mode / auto-disable the camera at a healthy pack, despite the analog read now feeding the safety branches every second. Any false trigger is a Task-1 defect: capture the measured voltage and the gate verdict line.
  4. Record the three clause verdicts in 01-UAT.md Test 3 (settings-visibility, CIF, and the regression watch) — this closes the SC-3 behavior-unverified half and flips Test 3's residual routing note.
  </how-to-verify>
  <acceptance_criteria>
  - 01-UAT.md Test 3 records a per-setting visible-difference verdict for at least brightness, saturation, and quality
  - 01-UAT.md Test 3 records the CIF 400x296 verdict (ACKed + image arrived + 400x296 confirmed, or the failure named)
  - No false emergency/camera-disable occurred across the session (explicit "absent" note with the healthy-pack voltage reading)
  - The two UI-SPEC regression anchors held: in-flight commands show "Sent" then advance via the poll; Sent/Acked/Failed/Pending counters advance (record any deviation verbatim)
  </acceptance_criteria>
  <done>SC-3's behavior-unverified clauses are operator-judged and recorded; UAT Test 3's residual note is resolved; 01-VERIFICATION.md's behavior_unverified count can drop to 0 at the next re-verification.</done>
  <resume-signal>Type "ride-alongs-recorded" with the CIF verdict (succeeded/failed) and the settings-series outcome, or describe issues</resume-signal>
</task>

</tasks>

<assumption_delta_decision>
Detector fired on "second" — traced to the executed 01-09 plan's "second-card retry" bench step, not a singular-to-plural architectural transition. Decision: no-change. No new identity model this round: the gallery SD index remains the single persistence truth, D-22's heal remains the single reliability mechanism, and the added batteryReadingValid() gate mirrors an existing idiom rather than introducing a second source of power truth.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link (balloon <-> base) | Untrusted wireless medium — existing mitigations: CRC16, length-driven framing, WR-12 type dispatch (verifier-confirmed) |
| Analog battery sense line -> safety logic | NEW this round: raw ADC now feeds emergency/camera-disable branches |
| Bench operator -> serial consoles | Evidence source; local, non-authoritative alone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10-01 | Tampering | LoRa frames during bench | low | accept | Existing CRC16 + fixed-length 0x12/0x14 receivers + type dispatch already verifier-proven; no new surface this round |
| T-01-10-02 | DoS | processPowerManagement safety branches (main_balloon.cpp:808-824) | high | mitigate | Validity gate ([1.8,8.0] V AND nonzero raw ADC) precedes triggerEmergency/camera-disable; invalid reading preserves availability (previous no-action behavior); one-shot transition warning only — Task 1 acceptance criteria enforce it |
| T-01-10-03 | Repudiation | Bench discriminator evidence | medium | mitigate | Verdicts cite captured console lines recorded into 01-UAT.md; gap closure additionally requires operator confirmation (prohibition 2) — code-level evidence alone cannot close a gap |
</threat_model>

<verification>
- Task 1: dual-target build + harness + grep gates (all automated, above)
- Tasks 2-3: blocking human-verify checkpoints whose acceptance criteria are evidence recorded in 01-UAT.md
- Post-plan: both firmware targets green, harness 49/49, single-file code diff, evidence blocks present in 01-UAT.md for G-01-5 and G-01-6
</verification>

<success_criteria>
1. G-01-5 mechanism NAMED from captured traces (heal-lost-on-air / heal-not-serviced / heal-not-fired) with the close-vs-separated reliability tally recorded
2. G-01-6 branch NAMED (refresh-stale vs persistence-stopped) with the deciding evidence
3. SC-3 residual clauses operator-judged (settings visible-effect + CIF 400x296)
4. WR-01 closed: no dummy PowerData, validity-gated safety branches, builds green
5. Routing inputs for plan 01-11 complete (or the CLOSE branch is explicitly justified by the tallies)
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-10-SUMMARY.md` when done — evidence table mapping every truth above to operator observations, in the 01-09-SUMMARY format
</output>

## Artifacts this phase produces

- `batteryReadingValid()` — new file-static helper, src/main_balloon.cpp (voltage in [1.8,8.0] V AND nonzero raw ADC)
- Modified: src/main_balloon.cpp (three dummy PowerData sites replaced; validity-gated safety branches; telemetry battery fields from PowerMgr)
- No new files; no new routes, flags, config keys, or protocol constants
- Evidence artifacts: 01-UAT.md G-01-5/G-01-6 blocks, reliability tallies, WINDOWS.md entry 2 reason update
