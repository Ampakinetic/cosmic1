---
phase: 01-command-protocol-control
plan: 22
type: execute
wave: 15
depends_on: []
files_modified:
  - src/image_rx_manager.cpp
  - include/image_rx_manager.h
autonomous: true
gap_closure: true
requirements: [IMG-02, IMG-03, PRI-03]

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low   # calibration store has 0 samples (factor 1, not applied); single-function deadline release in one manager (mirrors 01-18 sizing)

must_haves:
  truths:
    - statement: "G-01-7 burst full-delivery, base lever (arm deadline): while a thumbnail heal holds full-pull activation, the FIFO-oldest queued FULL still activates — issues its first window request, arming a FULL window on the balloon — within IMG_FULL_ARM_DEADLINE_MS (20 s) of its manifest arrival, with a named deadline-release log. A FULL window therefore arms before the balloon's re-announce bound can expire (the images-25/26 race: manifests in hand, activation held behind burst thumb serialization, every request post-drop)"
      verification: backstop
    - statement: "Outside the deadline the 01-12 serialization hold is unchanged: heal-before-pull priority survives, the one-shot 'full-pull activation held' log survives, and when the deadline does preempt a held heal, the heal defers to the pull (gate 1) and finalizes honestly on its D-24 3-pass budget — never an infinite stall"
      verification: explicit
    - statement: "No new D-21 trigger path exists: activation still happens only at the existing FIFO-advance call sites; the deadline releases an existing hold, it does not add a parallel pull mechanism"
      verification: explicit
    - statement: "Both firmware targets build green and the wire-format harness passes unchanged — the base speaks only on existing triggers, zero wire-format bytes change"
      verification: explicit
  artifacts:
    - "src/image_rx_manager.cpp — deadline-bounded serialization hold in activateNextPull (FIFO scan before the hold branch), manifestArrivedMs stamp at the onManifestFrame slot fill, one new named log line"
    - "include/image_rx_manager.h — ImageRxTransfer.manifestArrivedMs field + IMG_FULL_ARM_DEADLINE_MS constant (base-side scheduling constant lives in its consumer's header, the CMD_TX_CHANNEL_QUIET_MS precedent)"
  key_links:
    - "onManifestFrame slot fill -> manifestArrivedMs -> activateNextPull hold branch -> first window request -> balloon fullWindowEverArmed: arming does double duty on the balloon (stops the re-announce clock AND promotes the entry to the protected eviction class), which is why the base-side deadline composes with 01-21's balloon-side levers rather than duplicating them"
    - "Deadline value vs the balloon's budget: IMG_FULL_ARM_DEADLINE_MS 20 s must fire before the balloon's worst case (10 s idle x 3 re-announce attempts, held further by 01-21's busy-gate) — 10 s of margin on the unheld clock, more once 01-21 holds it"
    - "Held heal <-> preempted heal: when the deadline fires, the held thumbnail loses link priority by design (gate 1 blocks further heal passes) and its stall clock charges D-24 passes to an honest terminal verdict"
  prohibitions:
    - statement: "MUST NOT remove the 01-12 serialization hold — the deadline bounds it, it does not delete it; outside the 20 s window heal-before-pull ordering is byte-identical"
      status: flagged
      verification: backstop
    - statement: "MUST NOT add a new pull/arming trigger outside the existing FIFO-advance sites (D-21 discipline — the base speaks only on the documented triggers)"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT touch src/image_tx_manager.cpp or include/image_protocol.h (01-21's files, same-wave file ownership) or any command-path file"
      status: flagged
      verification: machine-checkable
---

<objective>
G-01-7 burst full-delivery gap closure, base RX half: bound the thumbnail-heal serialization hold with a full-arm deadline, so a queued FULL manifest activates (arms a FULL window on the balloon) within 20 s even while the base is still serializing a burst's thumbnails.

Purpose: 01-VERIFICATION.md gap 1 names the base-side artifact exactly — "burst thumbnail serialization starves FULL-window arming — the base cannot arm a FULL window while its thumb-heal/full-pull serialization queue is saturated by the burst's own thumbnails" — and the ledger's named lever: "Full-window-priority interleave during thumb serialization (or arm FULL windows between thumb passes)". In session 6 the 01-12 hold (correct for the single-capture case) let image 24's failing thumb heal block ALL full-pull activation across the 3-capture burst; images 25/26's fulls then dropped at the balloon's re-announce bound with every base request post-drop. Arming a FULL window early is the single base-side action that both stops the balloon's re-announce clock and protects the entry from class-3 eviction (via fullWindowEverArmed), so the deadline composes with 01-21's balloon-side receipt-evidence levers into the 6/6 series-A target.

Output: a deadline-bounded hold in src/image_rx_manager.cpp with one new named discriminator log line for the 01-24 bench; both targets green; harness unchanged-green.

Spec-less probe fallback: recorded visible skip — phase_req_ids null this run, no probe predicates generated (standard goal-backward verification, the established determination).
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-12-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-PATTERNS.md

Binding evidence and constraints:
- Session-6 base-side failure chain (01-VERIFICATION.md gap 1): base6.log:3947 manifest image 24 received -> :3948 'full-pull activation held - thumbnail heal pending for image 24' -> thumbs 25/26 COMPLETE :3979/:4003 while their fulls waited -> image 24 thumb heal burned to INCOMPLETE 2/6 at :4098 -> by then the balloon had evicted 24 (balloon11.log:406) and dropped 25/26 at the re-announce bound (:553/:575) -> base INCOMPLETE 0/26 / 0/22 / 0/14 (:4170/:4231/:4306).
- The hold being bounded (01-12-SUMMARY.md): activateNextPull's pendingHealThumbnail hold reorders priority between two existing D-21 triggers, bounded by the D-24 3-pass finalization — session 6 proved that bound is looser than the balloon's re-announce clock under a 3-burst, which is precisely the race this deadline closes.
- Balloon-side budget this deadline must beat: IMG_FULL_REANNOUNCE_IDLE_MS 10000 x IMG_FULL_REANNOUNCE_MAX 3 attempts (include/image_protocol.h) — 20 s fires with 10 s margin on the unheld clock; 01-21's busy-hold extends the balloon's tolerance further.
- House millis idiom (D-26): wrap-safe subtraction millis() - start >= bound, never absolute comparisons.
- House convention: no VALIDATION.md exists for this phase (user-accepted at the Nyquist gate, same as all 20 prior plans).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-arm deadline — bound the serialization hold in activateNextPull</name>
  <files>src/image_rx_manager.cpp, include/image_rx_manager.h</files>
  <read_first>
  - src/image_rx_manager.cpp — activateNextPull (:887-939: the pendingHealThumbnail hold, the one-shot healHoldLoggedId log, the FIFO scan, the activation tail with openTransfer + first window request), onManifestFrame's slot fill where arrivalSeq is assigned (:568 area), the heal loop's gate 1 (:254-256)
  - include/image_rx_manager.h — ImageRxTransfer fields (arrivalSeq :61, lastProgressMs :86, pullActive :100) and the header's constant block for IMG_FULL_ARM_DEADLINE_MS
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — gap 1 missing item 3 ("Full-window-priority interleave during thumb serialization (or arm FULL windows between thumb passes)")
  </read_first>
  <action>
  Implement the deadline in three edits:
  1. include/image_rx_manager.h: add to ImageRxTransfer a uint32_t manifestArrivedMs field (comment: millis() at FULL-manifest acceptance — the arm-deadline clock, zero on slot reset) and a static constexpr uint32_t IMG_FULL_ARM_DEADLINE_MS = 20000 beside the manager's existing constants with a rationale comment (must fire before the balloon's re-announce bound: 10 s idle x 3 attempts, 10 s margin; composes with the balloon-side busy-hold from 01-21).
  2. src/image_rx_manager.cpp, onManifestFrame slot fill: stamp t->manifestArrivedMs = millis() at the same site arrivalSeq is assigned (:568 area) so every queued FULL carries its arrival clock. Verify the slot-pressure full reset (the ImageRxEntry{}-style zero fill from 02-05) zeroes the new field, or assign 0 explicitly wherever the struct is reset.
  3. src/image_rx_manager.cpp, activateNextPull: restructure so the FIFO scan for the earliest queued full (the existing best loop) runs BEFORE the hold branch. When held = pendingHealThumbnail() is non-null: if best == nullptr, keep the existing hold behavior (nothing queued to arm); else if (millis() - best->manifestArrivedMs) < IMG_FULL_ARM_DEADLINE_MS (wrap-safe subtraction), keep the existing one-shot 'full-pull activation held' log and return; else print the new named log 'ImageRx: full-pull activation deadline reached - activating image %u despite thumbnail heal pending for image %u' (best->imageId, held->imageId) and fall through to the existing activation tail (pullActive set, openTransfer, first window request) — the held heal then defers via the existing gate 1 and finalizes on its D-24 pass budget.
  Do not modify: the pendingHealThumbnail predicate, the heal loop's gates, issueWindowRequest, the FIFO-advance call sites of activateNextPull, or any file outside this plan's files_modified. Use the wrap-safe millis idiom for the deadline comparison.
  </action>
  <verify>
  <automated>grep -q "IMG_FULL_ARM_DEADLINE_MS" include/image_rx_manager.h src/image_rx_manager.cpp && grep -q "manifestArrivedMs" include/image_rx_manager.h src/image_rx_manager.cpp && grep -q "full-pull activation deadline reached" src/image_rx_manager.cpp && grep -q "full-pull activation held - thumbnail heal pending" src/image_rx_manager.cpp</automated>
  </verify>
  <acceptance_criteria>
  - IMG_FULL_ARM_DEADLINE_MS = 20000 exists in include/image_rx_manager.h with the balloon-budget rationale comment
  - manifestArrivedMs is stamped at the onManifestFrame slot fill and zeroed on slot reset
  - activateNextPull's FIFO scan precedes the hold branch; inside the deadline the existing hold log fires once (healHoldLoggedId behavior unchanged); past the deadline the new named log fires naming BOTH the activated image and the held-heal image, then the existing activation tail runs unchanged
  - The pre-existing 'full-pull activation held - thumbnail heal pending' log line still exists (hold survives outside the deadline)
  - The deadline comparison uses wrap-safe millis subtraction
  </acceptance_criteria>
  <done>A queued FULL manifest cannot wait longer than 20 s behind thumbnail serialization: the base arms a FULL window (stops the balloon's re-announce clock, protects the entry from class-3 eviction) while the held heal still finalizes honestly on its D-24 budget when preempted.</done>
  <reversibility rating="reversible">Deadline-bounded policy change in one function; constant-gated, no wire format, no persistent state.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Dual-target builds, harness, and round gates</name>
  <files>src/image_rx_manager.cpp, include/image_rx_manager.h</files>
  <read_first>
  - src/image_rx_manager.cpp and include/image_rx_manager.h — the Task 1 result (confirm the full diff before building)
  - scripts/verify_protocol_roundtrip.mjs — what the harness pins (this plan must not need a harness change)
  </read_first>
  <action>
  Run the round's verification battery and confirm diff discipline:
  1. Build both firmware targets: pio run -e esp32-s3-balloon -e esp32-s3-basestation — both must succeed.
  2. Run node scripts/verify_protocol_roundtrip.mjs — must exit 0 unchanged.
  3. Confirm via git diff --stat that ONLY src/image_rx_manager.cpp and include/image_rx_manager.h changed relative to this plan's start (image_tx_manager.*, image_protocol.h, and command-path files have zero hunks from this plan — they belong to 01-21/01-23 in the same wave).
  4. Commit with message feat(01-22): full-arm deadline bounds the thumbnail-heal serialization hold (G-01-7 burst full-delivery, base half).
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation 2>&1 | tail -5 && node scripts/verify_protocol_roundtrip.mjs && echo HARNESS-OK</automated>
  </verify>
  <acceptance_criteria>
  - pio run reports 2 succeeded environments
  - verify_protocol_roundtrip.mjs exits 0 with no harness edit in the diff
  - git diff --stat versus this plan's start lists exactly src/image_rx_manager.cpp, include/image_rx_manager.h
  - The commit exists with the feat(01-22) prefix
  </acceptance_criteria>
  <done>Both targets build green, the harness passes unchanged, the diff is confined to the base RX manager pair, and the work is committed for the 01-24 bench flash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link -> ImageRxManager | Inbound manifests/chunks remain validated by the existing T-02-04/T-02-11 paths; this plan adds no input parsing |
| scheduling policy -> transfer liveness | The deadline preempts a heal; a mis-set constant could starve heals under sustained bursts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-22-01 | DoS | deadline preempting heals under sustained bursts | medium | mitigate | The D-24 3-pass budget still terminalizes any preempted heal (honest INCOMPLETE, never an infinite stall); the deadline clock is per-manifest arrival, so each queued full gets exactly one deadline release, not a repeating preemption; bench series A judges the composition |
| T-01-22-02 | Tampering | manifestArrivedMs influencing pull order | low | mitigate | The field is write-once at manifest acceptance inside the existing validated frame path; it never feeds wire output |
| T-01-22-03 | Availability | same-id heal/pull inversion (activating full N while healing thumb N) | medium | mitigate | Gate 1 already blocks the heal while the pull owns the link and the D-24 budget finalizes it — the preemption trades a failing heal's remaining passes for the full's arm, which is the ledger-named priority under burst; recorded in the deadline log for bench attribution |
</threat_model>

## Artifacts this phase produces

- IMG_FULL_ARM_DEADLINE_MS (new constant, include/image_rx_manager.h)
- ImageRxTransfer.manifestArrivedMs (new field, include/image_rx_manager.h)
- New named log line in src/image_rx_manager.cpp: 'full-pull activation deadline reached - activating image N despite thumbnail heal pending for image M'
- .planning/phases/01-command-protocol-control/01-22-SUMMARY.md

<verification>
- Both targets build green; wire harness exits 0 unchanged
- The deadline-release discriminator line exists at source and will be watched at the 01-24 bench (series A)
- The 01-12 hold and the D-21 trigger discipline are provably intact (diff-scoped)
</verification>

<success_criteria>
- The base can no longer sit on queued FULL manifests behind burst thumbnail serialization longer than 20 s — the burst full-delivery race's base half is closed at source
- G-01-7 closure itself is NOT claimed here — it is judged at the 01-24 bench series-A re-run (6/6 verdicts COMPLETE) together with 01-21's balloon-side levers
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-22-SUMMARY.md` when done
</output>
