---
phase: 01-command-protocol-control
plan: 21
type: execute
wave: 15
depends_on: []
files_modified:
  - src/image_tx_manager.cpp
  - include/image_tx_manager.h
  - include/image_protocol.h
autonomous: true
gap_closure: true
requirements: [IMG-03, IMG-04, PRI-03]

estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 3
  confidence: low   # calibration store has 0 samples (factor 1, not applied); single-manager scheduling change with named levers (mirrors 01-17 sizing)

must_haves:
  truths:
    - statement: "G-01-7 burst full-delivery, balloon lever 1 (drop-clock): while an inbound IMAGE_WINDOW_REQUEST arrived within IMG_FULL_REANNOUNCE_BUSY_MS — any kind, any verdict including unknown/evicted rejects — no re-announce attempt is consumed and no drop fires (one named hold log per episode); when the inbound traffic stops, the 01-17 cadence resumes unchanged. The images-25/26 class (3-re-announce bound expiring while the base serializes the burst's thumbnails, balloon11.log:553/:575) cannot recur while base window traffic flows"
      verification: backstop
    - statement: "G-01-7 balloon lever 1 (receipt re-arm): an inbound window request that MATCHES an ANNOUNCED, never-FULL-armed entry resets that entry's reannounceAttempts to 0 with a named log — a manifest the base demonstrably holds never drops at the bound while the base keeps asking for the entry"
      verification: backstop
    - statement: "G-01-7 balloon lever 2 (eviction ranking): an ANNOUNCED entry that has ever received an inbound window request (lastWindowRequestMs != 0) is ranked in the protected last-resort eviction class alongside windowEverArmed — never as class 3 'queued, no airtime invested'. The image-24 class (evicted at depth-3 overflow while the base held its manifest and was requesting windows, balloon11.log:406 vs base6.log:3947) is dead"
      verification: backstop
    - statement: "The 01-17 bounds survive as the terminal path: IMG_FULL_REANNOUNCE_MAX 3 with the named 'full dropped' log still fires when no receipt evidence and no inbound window traffic exist; IMG_FULL_REANNOUNCE_IDLE_MS cadence is untouched"
      verification: explicit
    - statement: "The 01-18 quiet gate is untouched: CMD_TX_CHANNEL_QUIET_MS / CMD_TX_CHANNEL_HOLD_MAX_MS constants, the 0x13-only receive latch, and both gated transmit sites in src/command_sender.cpp are byte-identical (this plan does not modify command_sender.cpp or command_handler.cpp at all)"
      verification: explicit
    - statement: "Both firmware targets build green and the wire-format harness passes unchanged — this plan changes scheduling only, zero wire-format bytes"
      verification: explicit
  artifacts:
    - "src/image_tx_manager.cpp — receipt-informed re-announce gating (busy-hold + per-entry re-arm), receipt-evidence eviction ranking, three new named log lines"
    - "include/image_tx_manager.h — ImageTxEntry.lastWindowRequestMs field; ImageTxManager.lastInboundWindowRequestMs + reannounceHoldLogged latch fields"
    - "include/image_protocol.h — IMG_FULL_REANNOUNCE_BUSY_MS constant beside the existing IMG_FULL_REANNOUNCE_* block"
  key_links:
    - "handleWindowRequest (every inbound request) -> lastInboundWindowRequestMs stamp -> findReannounceCandidate busy-hold -> reannounceFullManifest attempt/drop: the session-6 loss chain (base requesting while the balloon's bound expired) is severed at the stamp site"
    - "handleWindowRequest (matched target) -> target->lastWindowRequestMs stamp + reannounceAttempts reset -> evictionClassOf protected ranking: the second loss chain (base holding the manifest while the balloon evicted the entry) is severed at the same site"
    - "IMG_ENTRY_TTL_MS 15-min sweep and the 01-14 two-pass victim scan keep their full rights — the receipt-evidence ranking changes WHICH class an evidenced entry belongs to, not the scan's mid-service skip or the TTL path"
  prohibitions:
    - statement: "MUST NOT remove or weaken the IMG_FULL_REANNOUNCE_MAX named-drop terminal path — receipt evidence holds the clock, it never deletes the bound; an unrecoverable full still drops with its named log"
      status: flagged
      verification: backstop
    - statement: "MUST NOT touch src/command_sender.cpp, include/command_sender.h, src/command_handler.cpp, or src/image_rx_manager.cpp — the quiet gate, BUSY-deferral scoping, and the base hold belong to plans 01-18/01-15/01-22 respectively (wrap-don't-rewrite, diff-hunk discipline)"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT fabricate gap-closure evidence — G-01-7 closure is judged at the 01-24 bench, never on this plan's code alone"
      status: flagged
      verification: backstop
---

<objective>
G-01-7 burst full-delivery gap closure, balloon TX half: make the re-announce drop-clock and the depth-3 overflow eviction receipt-aware, so a full whose manifest the base demonstrably holds cannot be dropped at the re-announce bound or evicted as "no airtime invested" while the base is still working the burst.

Purpose: session #6 proved command survivability and silent-loss are dead, but series-A verdicts failed 2/6 on exactly two balloon-side scheduling mechanisms (01-VERIFICATION.md gap 1, artifacts src/image_tx_manager.cpp): the 3-re-announce bound expired while the base was still serializing the burst's thumbnails (images 25/26 — the base held their manifests 3x/4x and every post-drop window request was rejected), and the depth-3 eviction ranked image 24's announced full as "no airtime invested" while the base held its manifest and was actively requesting windows. The ledger's named levers are implemented here verbatim: "pause/re-arm the re-announce drop-clock on receipt evidence (an incoming window request for the entry), or raise the bound under known-busy" and "rank the depth-3 eviction class by receipt evidence rather than 'no airtime invested'".

Output: receipt-informed scheduling in src/image_tx_manager.cpp with three new named discriminator log lines for the 01-24 bench; both targets green; harness unchanged-green.

Spec-less probe fallback: recorded visible skip — phase_req_ids null this run, no probe predicates generated (same determination as all prior rounds; ROADMAP Goal line is not in user-story format, standard goal-backward verification applies).

Assumption-delta determination (advisory checkpoint): no-change — the detector's pluralization signal traces to historical 01-09 bench prose ("second-card retry", a long-resolved SD diagnostic), not to any second platform/auth/tenant/source-of-truth introduced this round; the identity model (one balloon, one base, one uint16 image-ID sequence) is unchanged.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-17-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-PATTERNS.md

Binding evidence and constraints:
- Session-6 failure quotes this plan targets (01-VERIFICATION.md gap 1 / WINDOWS entry 3): balloon11.log:553 'image 25 full dropped - no FULL window armed after 3 re-announces' and :575 image 26, WHILE the base held their manifests (base6.log:4015/:4044/:4074 and :4005/:4035/:4065/:4090) and its window requests kept arriving only to be rejected post-drop (balloon11.log:680/:702/:729/:751, :771/:838/:845/:868); balloon11.log:406 'queue overflow (depth 3); evicting class 3 (queued, no airtime invested) entry image 24 for image 26' while the base held image 24's manifest (base6.log:3947) and was requesting windows (8 rejects balloon11.log:495-:656).
- The rejects themselves are channel-liveness evidence: the base WAS issuing window requests through the whole drop window — stamping every inbound request (including rejects) as known-busy is the mechanism that would have held 25/26's clock.
- 01-17 design being extended (01-17-SUMMARY.md): re-announce fires only in pushPending's idle slot (no push work, no armed window), keyed on fullWindowEverArmed, bounded at IMG_FULL_REANNOUNCE_MAX 3 with park-and-free and a named drop; each transmit consumes one attempt AND one idle period.
- Carried prohibitions: evictionClassOf's mid-service skip (01-14 two-pass scan) and sweepExpiredEntries' TTL path keep their full eviction rights; the ranking change is the ledger-named lever, not a general eviction weakening.
- House convention: no VALIDATION.md exists for this phase (user-accepted at the Nyquist gate, same as all 20 prior plans).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-announce drop-clock — known-busy hold on inbound window traffic + per-entry receipt re-arm</name>
  <files>src/image_tx_manager.cpp, include/image_tx_manager.h, include/image_protocol.h</files>
  <read_first>
  - src/image_tx_manager.cpp — handleWindowRequest (:815-920: the untrusted-input validation order, target matching loop, the one-entry-at-a-time BUSY check), findReannounceCandidate (:740-757), reannounceFullManifest (:769-809), pushPending's idle-slot call site (:506-518)
  - include/image_tx_manager.h — ImageTxEntry fields (lastActivityMs :143, fullWindowEverArmed :127, reannounceAttempts :159) and the ImageTxManager member block for the new manager fields
  - include/image_protocol.h — the IMG_FULL_REANNOUNCE_IDLE_MS (:98) / IMG_FULL_REANNOUNCE_MAX (:105) block where the new constant lands
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — gap 1 (the exact missing items this task implements)
  </read_first>
  <action>
  Implement the ledger's drop-clock levers in three coordinated edits:
  1. include/image_protocol.h: add static constexpr uint32_t IMG_FULL_REANNOUNCE_BUSY_MS = 15000 beside the existing IMG_FULL_REANNOUNCE_* constants, with a rationale comment in the house style (known-busy hold: covers the base's 8 s D-24 stall cadence plus window service with margin — while inbound window requests keep arriving at least this often, the re-announce drop-clock is held; it resumes 15 s after the last inbound request).
  2. include/image_tx_manager.h: add two fields. In ImageTxEntry: uint32_t lastWindowRequestMs initialized 0 (= never) with a comment naming it the receipt-evidence stamp (an inbound window request that MATCHED this entry — proof the base holds at least one of its manifests and is asking). In ImageTxManager's private members: uint32_t lastInboundWindowRequestMs (channel-liveness stamp, every inbound request) and bool reannounceHoldLogged (one-shot latch for the hold log, cleared whenever a re-announce actually transmits). Initialize/reset all three in begin() and in the entry-reset path (freeEntry / the ImageTxEntry{} zero-fill covers the entry field — verify freeEntry zero-fills or assign explicitly).
  3. src/image_tx_manager.cpp, three sites:
     a. handleWindowRequest: immediately after the length/null guard and BEFORE kind validation, stamp lastInboundWindowRequestMs = millis() — every inbound request counts, including ones later rejected as unknown/evicted (the session-6 rejects are exactly the liveness evidence). After the target-matching loop resolves a non-null target (before the one-entry-at-a-time BUSY check), stamp target->lastWindowRequestMs = millis(); and when target->state is ANNOUNCED with !fullWindowEverArmed and reannounceAttempts > 0, reset reannounceAttempts = 0 and print the named log 'ImageTx: re-announce budget re-armed - window request received for image %u' (receipt evidence: the base is asking for this entry).
     b. pushPending's idle slot (before calling findReannounceCandidate): if millis() - lastInboundWindowRequestMs < IMG_FULL_REANNOUNCE_BUSY_MS, hold — do not call findReannounceCandidate this pass; print 'ImageTx: re-announce held - inbound window traffic active' once per episode (guarded by reannounceHoldLogged; clear the latch whenever reannounceFullManifest transmits). The hold consumes nothing: no attempt, no idle-period advance, no drop.
     c. reannounceFullManifest: clear reannounceHoldLogged when it runs (the latch reset site).
  Do not modify: the IMG_FULL_REANNOUNCE_MAX bound or the park-and-free drop branch (the terminal path survives verbatim), findReannounceCandidate's own predicates, the 01-14 two-pass victim scan, evictEntriesOlderThan, or any file outside this plan's files_modified.
  </action>
  <verify>
  <automated>grep -q "IMG_FULL_REANNOUNCE_BUSY_MS" include/image_protocol.h && grep -q "IMG_FULL_REANNOUNCE_MAX    = 3" include/image_protocol.h && grep -q "lastWindowRequestMs" include/image_tx_manager.h && grep -q "lastInboundWindowRequestMs" include/image_tx_manager.h src/image_tx_manager.cpp && grep -q "re-announce budget re-armed" src/image_tx_manager.cpp && grep -q "re-announce held - inbound window traffic active" src/image_tx_manager.cpp && grep -q "full dropped - no FULL window armed after" src/image_tx_manager.cpp && ! grep -q "lastInboundWindowRequestMs" src/command_sender.cpp</automated>
  </verify>
  <acceptance_criteria>
  - include/image_protocol.h contains IMG_FULL_REANNOUNCE_BUSY_MS = 15000 with the existing IMG_FULL_REANNOUNCE_MAX = 3 bound still present
  - handleWindowRequest stamps lastInboundWindowRequestMs before kind validation (reject-path liveness) and stamps target->lastWindowRequestMs after target resolution; the per-entry re-arm log names the image id and only fires when reannounceAttempts was > 0
  - The busy-hold gate sits in pushPending's idle slot before findReannounceCandidate, consumes no attempt/idle period, and its log is latch-guarded (one line per hold episode, not per pass)
  - The 'full dropped - no FULL window armed after' terminal log and the IMG_FULL_REANNOUNCE_MAX park-and-free branch are present and unchanged in src/image_tx_manager.cpp
  - No reference to the new stamps exists in src/command_sender.cpp (file untouched — machine-checked)
  </acceptance_criteria>
  <done>The re-announce mechanism is receipt-aware: inbound window traffic holds the drop-clock, a matched request re-arms the budget with a named log, and the bounded named-drop terminal path survives untouched.</done>
  <reversibility rating="reversible">Scheduling-policy change in one manager; constant-gated, no wire format, no persistent state.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Overflow eviction ranks ANNOUNCED entries by receipt evidence, not airtime</name>
  <files>src/image_tx_manager.cpp</files>
  <read_first>
  - src/image_tx_manager.cpp — evictionClassOf (:196-217, the class table and switch), the overflow victim scan in enqueueCapture (:328-387, two-pass structure and the className table), and Task 1's lastWindowRequestMs stamp (the evidence source this ranking consumes)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — gap 1 missing item 2 ("Rank the depth-3 eviction class by receipt evidence rather than 'no airtime invested'")
  </read_first>
  <action>
  Change the ANNOUNCED eviction ranking so receipt evidence protects the entry exactly as windowEverArmed does:
  1. evictionClassOf, ANNOUNCED case: return the protected class 5 when entry.windowEverArmed OR entry.lastWindowRequestMs != 0; return 3 only for a never-requested, never-armed entry. Update the class-table comment block (:196-202) to document the new class-3 definition ('queued, no receipt evidence — never armed, never asked for') and that an inbound window request is receipt evidence (the base demonstrably holds a manifest and is asking, per session-6 image 24).
  2. Update the className string table in the overflow scan only if needed for honesty: the class-5 label already reads 'ACTIVE-PULL context (last resort)' — acceptable for receipt-evidenced entries; append 'or receipt-evidenced' to that label string so the eviction log names the protection reason.
  3. Do NOT touch: the two-pass scan structure, the pass-1 mid-service skip, the oldest-within-class enqueueSeq tiebreak, sweepExpiredEntries, or evictEntriesOlderThan (the 01-12/01-14 carried prohibitions — the ranking change is the only edit).
  </action>
  <verify>
  <automated>grep -q "lastWindowRequestMs != 0" src/image_tx_manager.cpp && grep -q "receipt-evidenced" src/image_tx_manager.cpp && grep -q "no airtime invested" src/image_tx_manager.cpp</automated>
  </verify>
  <acceptance_criteria>
  - evictionClassOf's ANNOUNCED case returns 5 for entries with windowEverArmed OR lastWindowRequestMs != 0, and 3 otherwise (source-visible in the switch)
  - The class-table comment and the class-5 eviction label name receipt evidence
  - The string 'no airtime invested' still exists ONLY as the class-3 label for never-requested entries (the session-6 discriminator vocabulary stays greppable for the bench)
  - The two-pass scan body, mid-service skip predicate, and TTL sweep are unmodified relative to pre-task HEAD
  </acceptance_criteria>
  <done>A depth-3 overflow can no longer evict an announced full the base is actively requesting — the victim scan prefers genuinely unevidenced entries first, and the eviction log names which protection class the victim lacked.</done>
  <reversibility rating="reversible">Ranking-policy change in one function pair; no wire format, no persistent state.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Dual-target builds, harness, and round gates</name>
  <files>src/image_tx_manager.cpp, include/image_tx_manager.h, include/image_protocol.h</files>
  <read_first>
  - src/image_tx_manager.cpp and include/image_tx_manager.h — the Task 1/2 results (confirm the full diff before building)
  - scripts/verify_protocol_roundtrip.mjs — what the harness pins (wire format; this plan must not need a harness change)
  </read_first>
  <action>
  Run the round's verification battery and confirm the diff discipline:
  1. Build both firmware targets: pio run -e esp32-s3-balloon -e esp32-s3-basestation — both must succeed.
  2. Run node scripts/verify_protocol_roundtrip.mjs — must exit 0 with all checks passing, unchanged (zero wire-format bytes moved in this plan).
  3. Confirm via git diff --stat that ONLY this plan's three files changed relative to the round's starting commit, and that src/command_sender.cpp / src/command_handler.cpp / src/image_rx_manager.cpp have zero hunks from this plan (the quiet gate, dispatch, and base hold belong to other plans).
  4. Commit with message feat(01-21): receipt-informed re-announce + eviction ranking (G-01-7 burst full-delivery, balloon half).
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation > /tmp/pio-0121.log 2>&1 && tail -5 /tmp/pio-0121.log && grep -qE 'SUCCESS|succeeded' /tmp/pio-0121.log && node scripts/verify_protocol_roundtrip.mjs && echo HARNESS-OK</automated>
  </verify>
  <acceptance_criteria>
  - pio run reports 2 succeeded environments (esp32-s3-balloon, esp32-s3-basestation)
  - verify_protocol_roundtrip.mjs exits 0 (all PASS, zero FAIL) with no harness edit in the diff
  - git diff --stat versus the round start lists exactly src/image_tx_manager.cpp, include/image_tx_manager.h, include/image_protocol.h
  - The commit exists with the feat(01-21) prefix
  </acceptance_criteria>
  <done>Both targets build green, the wire harness passes unchanged, the diff is confined to this plan's files, and the work is committed for the 01-24 bench flash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link -> ImageTxManager | Inbound IMAGE_WINDOW_REQUEST frames are untrusted RF input; the new stamps consume them |
| scheduling policy -> transfer liveness | The busy-hold delays drops; a pathological hold could pin a full in PSRAM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-21-01 | Tampering | lastWindowRequestMs / re-announce re-arm | medium | mitigate | Stamps are written only from handleWindowRequest AFTER the existing T-02-04/T-02-11 untrusted-input validation (kind enum check, entry match, per-kind range bounds) — a crafted frame cannot reach the stamp without passing full validation; the global liveness stamp is write-only timing data, never used for content decisions |
| T-01-21-02 | DoS | receipt-evidence pins an entry forever | medium | mitigate | The per-entry re-arm resets a bounded counter (max 3); the IMG_ENTRY_TTL_MS 15-min sweep and the IMG_MAX_IMAGE_SIZE/queue-depth bounds still evict; the busy-hold delays but never removes the IMG_FULL_REANNOUNCE_MAX terminal drop once traffic stops |
| T-01-21-03 | DoS | log flood from the hold latch | low | mitigate | reannounceHoldLogged one-shot latch prints one line per hold episode; the re-arm log fires only when reannounceAttempts was > 0 (at most once per consumed attempt) |
</threat_model>

## Artifacts this phase produces

- IMG_FULL_REANNOUNCE_BUSY_MS (new constant, include/image_protocol.h)
- ImageTxEntry.lastWindowRequestMs (new field, include/image_tx_manager.h)
- ImageTxManager.lastInboundWindowRequestMs and ImageTxManager.reannounceHoldLogged (new private members, include/image_tx_manager.h)
- New named log lines in src/image_tx_manager.cpp: 're-announce held - inbound window traffic active', 're-announce budget re-armed - window request received for image N', and the receipt-evidenced class-5 eviction label
- .planning/phases/01-command-protocol-control/01-21-SUMMARY.md

<verification>
- Both targets build green; wire harness exits 0 unchanged
- The three new discriminator lines exist at source and will be watched at the 01-24 bench (series A)
- The IMG_FULL_REANNOUNCE_MAX named-drop terminal path and the 01-18 quiet gate are provably untouched (diff-scoped)
</verification>

<success_criteria>
- The two session-6 balloon-side loss mechanisms are structurally closed at source: no drop at the re-announce bound while inbound window traffic flows or a matched request re-arms the budget; no class-3 eviction of a receipt-evidenced entry
- G-01-7 closure itself is NOT claimed here — it is judged at the 01-24 bench series-A re-run (6/6 verdicts COMPLETE)
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-21-SUMMARY.md` when done
</output>
