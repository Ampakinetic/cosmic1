---
phase: 01-command-protocol-control
plan: 33
type: execute
wave: 25
depends_on: ["01-32"]
files_modified:
  - include/image_protocol.h
  - src/image_tx_manager.cpp
  - .planning/phases/01-command-protocol-control/01-G01-7-LEVER.md
  - .planning/WINDOWS.md
  - .planning/phases/01-command-protocol-control/01-UAT.md
autonomous: false
gap_closure: true
requirements: [CTRL-01, CTRL-03, CTRL-06, PRI-02]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low   # calibration store has 0 samples (factor 1, not applied); mirrors 01-30's audit+checkpoint+implement shape at smaller scope — the residual is already NAMED, the audit confirms the mechanism site and sizes the lever

must_haves:
  truths:
    - statement: "The G-01-7 lever-selection record exists (.planning/phases/01-command-protocol-control/01-G01-7-LEVER.md) with the session-10 evidence base quoted (the discriminator census: base deadline activation balloon4.log:428 -> supersede eviction :817 -> 7 honest rejections :1003-:1162 -> full 0/31), the supersede-path source reading (which victim image_tx_manager.cpp's supersede branch actually selects, and whether evictionClassOf's class-5 receipt-evidenced protection is consulted there at all), the depth-raise arithmetic (PSRAM worst case: IMG_TX_QUEUE_DEPTH entries x (full buffer up to IMG_MAX_IMAGE_SIZE + thumbBuffer up to THUMB_MAX_BYTES) against the bench-observed ~8.1 MB free psram plus the internal-RAM cost of the ImageTxEntry structs), and the ranked menu with the selected option and its provenance"
      verification: explicit
    - statement: "The selected lever is implemented and greppable: the queue-depth constant carries its new value with the arithmetic comment beside it; the supersede path's victim selection consults the receipt-evidenced ranking so a base-activated full-pull (windowEverArmed or lastWindowRequestMs != 0) is never the supersede victim while any lower-investment entry exists, with a named discriminator line proving the protection engaged ('supersede of receipt-evidenced image %u avoided - evicting class %u entry image %u instead (G-01-7)'); when ONLY receipt-evidenced entries exist, the incoming request is honestly rejected with its own named line (the existing unknown/evicted rejection class, extended) and the base's D-24 pass machinery retries within its existing bound — no new protocol surface"
      verification: explicit
    - statement: "The round-#10/01-21/01-22 lever surfaces are preserved and composed with, not replaced: the receipt-informed re-announce ladder (budget re-arm, the 15 s busy hold, the 3-attempt named drop), the base full-arm deadline release, the 01-18 quiet gate, the class-ranked overflow scan and its two-pass structure, and the D2 receipt-ever flag all stay byte-intact — the new lever TIGHTENS receipt-evidenced protection and never demotes it"
      verification: explicit
    - statement: "Both firmware targets build green and the wire-format harness exits 0 on the round's final commit; the PRI-01 order-gates stay green (the beacon outranks image chunks in transmit arbitration, never the reverse — the lever touches queue admission/eviction policy only, not the transmit-arbitration order)"
      verification: explicit
    - statement: "Ledger honesty: 01-UAT.md G-01-7 and WINDOWS entry 3 carry the round-#14 lever record (the named residual, the lever class selected and shipped, the discriminator lines 01-34 watches) but stay OPEN — G-01-7 flips only on 01-34's series-A hardware evidence; the SUMMARY makes no bench claims"
      verification: explicit
  artifacts:
    - ".planning/phases/01-command-protocol-control/01-G01-7-LEVER.md — the lever-selection record (evidence, source reading, arithmetic, ranked menu, selection + provenance)"
    - "include/image_protocol.h — IMG_TX_QUEUE_DEPTH raised with the arithmetic comment beside it"
    - "src/image_tx_manager.cpp — the supersede-path receipt-evidenced protection + the two new discriminator lines"
    - ".planning/WINDOWS.md — entry 3 round-#14 extension; stays open; counts unchanged (2/17/19)"
    - ".planning/phases/01-command-protocol-control/01-UAT.md — G-01-7 root_cause + missing extended with the lever shipped; stays open"
    - ".planning/phases/01-command-protocol-control/01-33-SUMMARY.md"
  key_links:
    - "The lever <-> the NAMED residual: every design element traces to a quoted session-10 line (the depth-3 mismatch the ledgers name verbatim: 'the levers work as designed; the design's queue depth vs the burst pattern is the mismatch') — nothing here is speculative redesign"
    - "The lever <-> 01-34's series-A acceptance: the bench judges 6/6 COMPLETE with the lever's discriminator lines proving engagement (supersede-protection lines, depth-5 overflow labels, honest-reject lines) — a series that fails with the lines ABSENT means the lever never engaged, a distinct verdict from engaged-but-insufficient"
    - "This round <-> 01-32: lands AFTER the D1 instrument package (depends_on 01-32) so 01-34's single bench session reads the [STACK] answer AND judges series A on this lever — the ledger routing ('admission control or oldest-queued-full eviction, AFTER D1 round #4') honored literally"
    - "The no-D1-lever boundary: this lever targets the G-01-7 transfer-scheduling residual named by the discriminator census — it is NOT a D1 crash fix and makes no crash claim; if the balloon still crashes at 01-34, D1's evidence path ([STACK]/[TWDT]/[I2C]) is untouched by this lever's scheduling changes"
  prohibitions:
    - statement: "MUST NOT claim bench evidence in this plan — code + builds + harness only; G-01-7 flips on 01-34's operator-observed verdicts"
      status: flagged
      verification: backstop
    - statement: "MUST NOT change operator-visible command semantics (NACK_BUSY on CAPTURE_NOW / admission refusal of captures) unless the Task 2 checkpoint selection explicitly names that option — the evidence-ranked default acts on queue policy only, because a refused capture under a busy balloon risks reproducing the forbidden 'timeout after 3 retries' terminal (CAPTURE_NOW ack-timeout 2000 ms x 3 under active chunk service)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT weaken any receipt-evidenced protection or discriminator surface: the class-5 ranking, the re-announce ladder, the budget re-arm, the base deadline release, the quiet gate, the D2 flag, the 01-32 instruments — the lever composes with them"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT alter the wire format, D-05/D-07 pacing, or the PRI-01 arbitration order — the harness must exit 0 unchanged and the beacon-first order-gates stay green"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT raise IMG_TX_QUEUE_DEPTH past the arithmetic the selection record justifies (the record's PSRAM/internal-RAM bound is the ceiling), and MUST NOT change sweepExpiredEntries' 15-min TTL or the freeEntry buffer lifetime semantics to make room"
      status: flagged
      verification: backstop
---

<objective>
Close the code half of G-01-7's burst full-delivery residual (01-VERIFICATION.md gap 2, WINDOWS entry 3): land the evidence-named lever against the depth-3 queue-cycling mechanism the session-10 discriminator census finally captured — the base deadline-activated image 41's full-pull, then image 42's window request evicted the pending entry (a receipt-evidenced class-5 victim), then seven honest rejections burned the base's pass budget to 0/31 while the newest capture's full completed. The ledgers route this lever AFTER D1 round #4 ("admission control or oldest-queued-full eviction, AFTER D1 round #4"); 01-32 landed the D1 instrument package, so this round ships the G-01-7 lever and 01-34's single bench session judges both gaps at once.

Purpose: the round-#10 levers are now BENCH-VERIFIED ENGAGED (session 10: budget re-arm x2, re-announces x3, genuine holds x4, the base deadline release, class-5 eviction labels) — the defect is no longer lever absence but the design's queue policy under the burst: depth 3 cannot hold the base-activated prior full plus the newest capture's entries, and the supersede path sacrifices the receipt-evidenced entry the base is actively pulling. This is NOT D1 speculation: the residual is named by quoted lines, the strongest evidence class in the campaign — the §10.5 no-lever guard governs D1 crash levers, which stay fenced in 01-32/01-34's instrument evidence path.

Output: the lever-selection record; the queue-depth raise with its arithmetic; supersede-path receipt-evidenced protection with engagement discriminator lines; builds green + harness exit 0; WINDOWS 3 and G-01-7 carrying the round-#14 lever record, honestly open pending the bench.

Multi-source coverage audit: GOAL/REQ covered — this plan addresses the G-01-7 blocker on CTRL-01/CTRL-03/CTRL-06/PRI-02's burst-delivery truths; RESEARCH — the ledger-named lever class verbatim; CONTEXT — no new D-XX decisions; the probe-fallback surface lives in 01-34's must_haves.flagged_assumptions (11 rows, no silent drops). Prohibition recall: kept prohibitions descriptor-less under must_haves.prohibitions.

<assumption_delta_decision>
Same determination as 01-32 (the scan's two signals trace to completed-plan prose, not this round's scope): no-change — this round adjusts queue admission/eviction policy within the existing single-pipeline transfer architecture; no second source of truth is introduced.
</assumption_delta_decision>
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/debug/d1-crash-regression-push-start.md
@.planning/phases/01-command-protocol-control/01-32-SUMMARY.md
@.planning/WINDOWS.md

Binding evidence and constraints:
- The named residual (01-UAT.md G-01-7 SESSION-10 UPDATE + 01-VERIFICATION.md gap 2, all verifier-reproduced): seq=7/8/9 series ran; 6/8 verdicts COMPLETE (image 40 2/2, image 42 2/2); image 41 0/2 — the base's 01-22 deadline activated image 41's full-pull (base4.log:428 'full-pull activation deadline reached - activating image 41 despite thumbnail heal pending'), then image 42's window request EVICTED the still-pending image 41 entry (balloon4.log:816-:817 'ImageTx: window request for image 42 supersedes older entry image 41; evicted' — and :816 the same for image 40), then 'window request for unknown/evicted image 41 rejected' x7 (:1003-:1162) burned the base's retransmit passes to full 0/31 (:484-:485); the newest capture's full (42) completed 2/2. The levers work as designed; the design's queue depth vs the burst pattern is the mismatch; the newest capture always wins the slot.
- Source sites at HEAD: include/image_protocol.h:61 (static constexpr uint8_t IMG_TX_QUEUE_DEPTH = 3); src/image_tx_manager.cpp:372-379 (evictionClassOf — the ANNOUNCED case returns protected class 5 when windowEverArmed || lastWindowRequestMs != 0, else class 3), :518-580 (the enqueue-overflow two-pass scan: pass 1 skips armed+incomplete via the BUSY predicate, pass 2 all-mid-service drops the skip with its own label — the overflow label prints IMG_TX_QUEUE_DEPTH verbatim), :1332-1343 (the SUPERSEDE path — 'window request for image %u supersedes older entry image %u; evicted' — the path that actually killed image 41; confirm in Task 1 whether this path consults evictionClassOf at all), :1124 (the receipt stamp target->lastWindowRequestMs = millis()), :1432 (the recycled-slot receipt reset, the G-01-7 lever-1 comment), :705-718 (the 01-21 re-announce busy hold on lastInboundWindowRequestMs / IMG_FULL_REANNOUNCE_BUSY_MS 15000).
- The D-24 pass machinery (base side, src/image_rx_manager.cpp): acceptChunk is the sole passCount zero-writer; passes count transfer OPPORTUNITIES (01-15); the honest 'unknown/evicted rejected' rejections are what burned image 41's budget — a rejected request consumes a pass, so the lever's protection must PREVENT the eviction, not soften the rejection.
- Session-10 context bounding the lever: image 43 (CIF-class, 119 chunks) was mid-window at the D1 crash — the burst pattern at bench includes larger-than-QVGA captures; the lever must hold for QVGA bursts (the series-A protocol) without regressing single-spaced captures (sessions 5/8's 4/4-kinds pattern).
- Scope guards: the wire format (scripts/verify_protocol_roundtrip.mjs must exit 0 unchanged), the 01-18 quiet gate (CMD_TX_CHANNEL_QUIET_MS 750), D-05/D-07 pacing, the PRI-01 beacon-first arbitration (node order-gates), and every 01-32 instrument are protected surfaces.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The lever-selection audit — supersede-path reading, depth-raise arithmetic, ranked menu — into 01-G01-7-LEVER.md</name>
  <files>.planning/phases/01-command-protocol-control/01-G01-7-LEVER.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-UAT.md — the G-01-7 gap block IN FULL (the session-10 discriminator census with every quoted line; the routed lever class)
  - src/image_tx_manager.cpp — :360-400 (evictionClassOf + the class names), :500-600 (enqueue + the two-pass overflow scan + freeEntry), :1100-1140 (the receipt stamp site), :1320-1360 (the supersede path — the audit's central question), :700-730 (the re-announce hold), :1420-1440 (the slot-recycle receipt reset)
  - include/image_protocol.h — :55-70 (IMG_TX_QUEUE_DEPTH and its neighbors), the IMG_MAX_IMAGE_SIZE / THUMB_MAX_BYTES constants, the ImageTxEntry definition (struct size for the internal-RAM arithmetic)
  - src/image_rx_manager.cpp — :900-940 (the base full-arm deadline + the activation hold), the D-24 pass accounting sites
  - balloon4.log — :428, :816-:817, :1003-:1162, :484-:485 (the census lines quoted in the record)
  </read_first>
  <action>
  Write .planning/phases/01-command-protocol-control/01-G01-7-LEVER.md with four sections, every claim carrying file:line or log-line quotes:
  1. EVIDENCE BASE: the session-10 census quoted (the deadline activation, the two supersede evictions, the seven rejections, the 0/31 and 2/2 verdicts) and the ledger-named residual verbatim.
  2. SOURCE READING: what the supersede path (image_tx_manager.cpp:1332-1343) actually does — which entry it selects as the victim, under what condition, and whether evictionClassOf's class-5 receipt-evidenced protection is consulted there (the enqueue-overflow scan consults it at :558; the census proves the supersede path evicted receipt-evidenced entries, so the audit must establish whether the protection is absent or was legitimately exhausted). Also: how many queue slots the burst pattern consumes (3 captures x 2 kinds minus completed-and-freed thumbs) and when the newest capture's request finds no acceptable victim.
  3. ARITHMETIC: the depth raise 3 -> 5 — worst-case PSRAM residency (5 entries x (fullBuffer up to the capture's full size, IMG_MAX_IMAGE_SIZE-capped, plus thumbBuffer up to THUMB_MAX_BYTES)) against the bench-observed free psram (~8.1 MB class per [MEM]); the internal-RAM cost (sizeof(ImageTxEntry) x 2 additional slots); the buffer-lifetime effect on sweepExpiredEntries' 15-min TTL. State the ceiling the arithmetic justifies.
  4. RANKED MENU + SELECTION: (a) depth raise + supersede-path receipt-evidenced protection (the supersede victim selection ranked through evictionClassOf so a receipt-evidenced entry is never the victim while a lower-investment entry exists, with engagement discriminator lines; when only receipt-evidenced entries exist, the incoming request is honestly rejected with a named line and the base's existing D-24 pass machinery retries) — evidence-ranked DEFAULT: it acts directly on the ledger-named mismatch ('the design's queue depth vs the burst pattern'), changes no operator-visible semantics, and composes with every round-#10 lever; (b) admission control (NACK_BUSY a CAPTURE_NOW while a receipt-evidenced unserved full exists) — REJECTED as default: operator-visible, and a refused capture under active chunk service risks reproducing the forbidden 'timeout after 3 retries' terminal (CAPTURE_NOW 2000 ms ack budget x 3); (c) depth raise alone (no supersede protection) — insufficient: session 10's killing evictions rode the supersede path, which a depth raise only makes rarer. Record the selected option as (a) unless the source reading falsifies its premise, with the auto-advance provenance noted (workflow.auto_advance=true, pending end-of-phase operator confirmation — the sixth pending confirmation joins this round's Task 2 gate).
  </action>
  <verify>
  <automated>grep -q "G01-7" .planning/phases/01-command-protocol-control/01-G01-7-LEVER.md && grep -qi "supersede" .planning/phases/01-command-protocol-control/01-G01-7-LEVER.md && grep -qi "IMG_TX_QUEUE_DEPTH" .planning/phases/01-command-protocol-control/01-G01-7-LEVER.md && grep -qi "psram" .planning/phases/01-command-protocol-control/01-G01-7-LEVER.md && grep -qi "admission" .planning/phases/01-command-protocol-control/01-G01-7-LEVER.md</automated>
  </verify>
  <acceptance_criteria>
  - The record exists with the four sections; the census lines are quoted with balloon4.log line numbers; the supersede-path reading names the actual victim-selection code path with file:line
  - The depth arithmetic uses the real constants (IMG_MAX_IMAGE_SIZE / THUMB_MAX_BYTES / sizeof(ImageTxEntry)) and states the justified ceiling
  - The ranked menu carries the three options with the evidence-ranked default and the explicit reason the admission-control option is not the default (the forbidden-terminal interaction)
  - The selection provenance names the auto-advance convention
  </acceptance_criteria>
  <done>The lever's mechanism site is established from source, its cost is bounded by arithmetic, and the selection record is durable for the checkpoint and the checker.</done>
  <reversibility rating="reversible">Documentation-only audit record.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Lever-selection checkpoint — confirm the evidence-ranked option from 01-G01-7-LEVER.md</name>
  <files>.planning/phases/01-command-protocol-control/01-G01-7-LEVER.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-G01-7-LEVER.md — Task 1's record (the ranked menu, the source reading, the arithmetic)
  - .planning/phases/01-command-protocol-control/01-UAT.md — the G-01-7 session-10 update (the residual the lever must answer)
  </read_first>
  <what-built>
  The lever-selection audit: the supersede-path mechanism reading, the depth-raise arithmetic, and the three-option ranked menu with the evidence-ranked default (a: depth raise + supersede receipt-evidenced protection).
  </what-built>
  <how-to-verify>
  Confirm or override the selection, then append a "LEVER SELECTED (round #14 Task 2 checkpoint record)" section to 01-G01-7-LEVER.md naming the option, the evidence that selected it, and (if auto-advanced) the provenance per the house convention. Option (a) is the evidence-honest default: it maps one-to-one onto the ledger-named mismatch and the census lines. Override only with a reason the record's evidence fails to support. Under workflow.auto_advance=true the house convention auto-selects the evidence-ranked option and records it as pending end-of-phase operator confirmation (joining the campaign's existing pending confirmations).
  </how-to-verify>
  <acceptance_criteria>
  - The checkpoint record exists in 01-G01-7-LEVER.md naming the selected option, the evidence, and the provenance
  - A selection of option (b) (admission control / NACK_BUSY on captures) is accompanied by the operator-visible-semantics risk being explicitly accepted in the record
  </acceptance_criteria>
  <resume-signal>Type "approved" with the selected option (and any override reasoning), or route back with what the audit fails to establish.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Implement the selected lever + discriminator lines; builds + harness + PRI-01 order-gates; ledgers extended (no flips)</name>
  <files>include/image_protocol.h, src/image_tx_manager.cpp, .planning/WINDOWS.md, .planning/phases/01-command-protocol-control/01-UAT.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-G01-7-LEVER.md — the Task 2 selection record this task implements verbatim
  - src/image_tx_manager.cpp — :360-400 (evictionClassOf — reuse, never fork it), :500-600 (enqueue + overflow scan), :1100-1140 (receipt stamp), :1320-1360 (the supersede path — the modification site), :1420-1440 (slot-recycle reset)
  - include/image_protocol.h — :55-70 (the depth constant + neighbors), the ImageTxEntry definition
  - scripts/verify_protocol_roundtrip.mjs + the PRI-01 node order-gates — the regression tripwires that must stay green
  - .planning/WINDOWS.md — entry 3; .planning/phases/01-command-protocol-control/01-UAT.md — G-01-7
  </read_first>
  <action>
  Implement option (a) exactly as the selection record specifies:
  1. include/image_protocol.h:61: IMG_TX_QUEUE_DEPTH 3 -> 5 (or the record's justified ceiling), with the arithmetic comment beside it citing G-01-7 and the record's section 3 — the constant is the ledger-named mismatch ('queue overflow (depth 3)') and the existing overflow label prints it verbatim, so the depth is self-evidencing in future logs.
  2. src/image_tx_manager.cpp supersede path (:1332-1343): the victim selection goes through evictionClassOf's ranking — a supersede must never evict a receipt-evidenced entry (class 5: windowEverArmed || lastWindowRequestMs != 0) while any lower-class candidate exists; when it avoids one, print Serial.printf("ImageTx: supersede of receipt-evidenced image %u avoided - evicting class %u entry image %u instead (G-01-7)\n", ...) (latch-free by design: it is an event line, bounded by queue events); when ONLY receipt-evidenced entries exist, do NOT evict — reject the incoming request honestly through the existing unknown/evicted rejection path extended with its own named line Serial.printf("ImageTx: window request for image %u rejected - queue holds only receipt-evidenced entries (G-01-7)\n", ...) so the base's D-24 pass machinery retries within its existing bound. Reuse evictionClassOf verbatim — no forked ranking.
  3. Composition guards: the re-announce ladder, the budget re-arm, the base deadline release, the two-pass overflow scan (including its all-mid-service pass-2 label), the 01-18 quiet gate, the D2 flag, and every 01-32 instrument stay byte-intact; the comment at each touched site cites G-01-7 + the record section.
  4. Verify: pio run -e esp32-s3-balloon -e esp32-s3-basestation (2/2 SUCCESS); node scripts/verify_protocol_roundtrip.mjs (exit 0); the PRI-01 order-gates green; the protected-surface greps clean (CMD_TX_CHANNEL_QUIET_MS = 750, the 01-32 instrument greps from 01-32's Task 1 verify, uart_ll_is_tx_idle x3).
  5. Ledgers: extend WINDOWS entry 3's reason with the round-#14 lever record (option shipped, the two new discriminator lines, entry stays open, counts 2/17/19, BOTH copies) and 01-UAT.md G-01-7's root_cause/missing (the lever-shipped marker; status stays open). Neither claims burst-delivery fixed. Commit fix(01-33): G-01-7 burst full-delivery lever — queue depth + supersede receipt-evidenced protection per 01-G01-7-LEVER.md.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation 2>&1 | grep -c "SUCCESS" | grep -qv "^0$" && node scripts/verify_protocol_roundtrip.mjs && grep -q "IMG_TX_QUEUE_DEPTH         = 5" include/image_protocol.h && grep -q "supersede of receipt-evidenced" src/image_tx_manager.cpp && grep -q "queue holds only receipt-evidenced entries" src/image_tx_manager.cpp && grep -q "CMD_TX_CHANNEL_QUIET_MS = 750" include/command_sender.h && grep -q "open_count: 2" .planning/WINDOWS.md</automated>
  </verify>
  <acceptance_criteria>
  - Both targets build SUCCESS, the harness exits 0, and the PRI-01 order-gates stay green on this task's commit
  - IMG_TX_QUEUE_DEPTH carries the record-justified value with the arithmetic comment; the supersede path routes through evictionClassOf with the two named discriminator lines present verbatim in source
  - The receipt-evidenced class-5 semantics are strengthened, not forked: evictionClassOf itself is unchanged
  - WINDOWS entry 3 extended in both copies, still open, counts 2/17/19; G-01-7 still open with the lever-shipped marker
  - The SUMMARY makes no bench claims — series-A judgment belongs to 01-34
  </acceptance_criteria>
  <done>The named residual has its lever in the build: the depth-3 mismatch is answered, the supersede path protects the entry the base is actively pulling, and the bench discriminator lines are named for 01-34 to grep.</done>
  <reversibility rating="costly">The supersede path arbitrates every window request under load; a wrong victim rule would strand live transfers — visible only at the bench, hence the checkpoint gate before this task and the reuse-don't-fork constraint on the ranking.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session-10 evidence -> lever design | The retained consoles + the verifier-reproduced census are the only ground truth; the design maps onto quoted lines, not code-level speculation |
| lever -> live transfer arbitration | The supersede path and queue depth govern every window request under load; the harness, PRI-01 order-gates, and protected-surface greps are the regression bound |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-33-01 | Tampering | evidence integrity | medium | mitigate | The selection record quotes the census lines with balloon4.log line numbers; the source reading carries file:line; the checker can re-derive both |
| T-01-33-02 | DoS | a forked eviction ranking diverging from evictionClassOf | high | mitigate | Task 3 reuses evictionClassOf verbatim; the class-5 semantics can only be strengthened; the composition guards are a machine-checkable prohibition |
| T-01-33-03 | DoS | admission control reproducing the forbidden command terminal | high | mitigate | Option (b) is rejected as the default with the explicit 2000 ms x 3 arithmetic; landing it requires the Task 2 checkpoint to accept the risk by name |
| T-01-33-04 | DoS | depth raise exhausting balloon memory under burst | medium | mitigate | The selection record's PSRAM/internal-RAM arithmetic bounds the ceiling before the constant changes; sweepExpiredEntries TTL and buffer lifetimes untouched |
| T-01-33-05 | Repudiation | lever engagement invisible at the bench | medium | mitigate | The two named discriminator lines make engagement (or honest rejection) greppable in 01-34 — an unengaged lever is a distinct verdict from an insufficient one |
| T-01-33-06 | Safety | bench hardware | low | accept | No hardware work in this plan; the rig risk stays with 01-34 |

## Artifacts this phase produces

- .planning/phases/01-command-protocol-control/01-G01-7-LEVER.md (new): the evidence base, the supersede-path source reading, the depth arithmetic, the ranked menu, the LEVER SELECTED checkpoint record
- include/image_protocol.h: IMG_TX_QUEUE_DEPTH raised (new value + arithmetic comment)
- src/image_tx_manager.cpp: the supersede-path receipt-evidenced protection + two new log lines — 'ImageTx: supersede of receipt-evidenced image %u avoided - evicting class %u entry image %u instead (G-01-7)' and 'ImageTx: window request for image %u rejected - queue holds only receipt-evidenced entries (G-01-7)'
- WINDOWS entry 3 round-#14 extension; 01-UAT.md G-01-7 lever-shipped marker
- .planning/phases/01-command-protocol-control/01-33-SUMMARY.md
- No new modules, no env vars, no wire-format changes, no new source files

<verification>
- Both targets build 2/2, harness exit 0, PRI-01 order-gates green on the final commit
- The selection record's option is what shipped (grep-verifiable constants + line formats); evictionClassOf unforked
- WINDOWS 3 / G-01-7 open with the lever record — zero bench claims
</verification>

<success_criteria>
- The depth-3 queue-cycling residual has its evidence-named lever landed with engagement discriminators, composing with every round-#10/01-21/01-22 surface
- The 01-34 bench can judge series A against a complete lever story: engaged-and-passing, engaged-and-failing (residual re-named), or never-engaged (distinct verdict)
- The G-01-7 evidence chain is unchanged for the checker: every design element traces to a quoted session-10 line
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-33-SUMMARY.md` when done
</output>
