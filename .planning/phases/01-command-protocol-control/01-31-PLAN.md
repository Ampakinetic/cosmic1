---
phase: 01-command-protocol-control
plan: 31
type: execute
wave: 23
depends_on: ["01-30"]
files_modified:
  - .planning/phases/01-command-protocol-control/01-UAT.md
  - .planning/WINDOWS.md
  - .planning/STATE.md
  - .planning/debug/d1-crash-regression-push-start.md
autonomous: false
gap_closure: true
requirements: [CTRL-01, CTRL-02, CTRL-03, CTRL-04, CTRL-06, PRI-02, IMG-02, IMG-03]

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low   # calibration store has 0 samples (factor 1, not applied); one operator bench checkpoint + evidence-ledger updates (mirrors 01-27/01-29 sizing; the new lull-dwell step adds minutes, not context)

must_haves:
  truths:
    - statement: "D1 crash-regression acceptance on the round-#13 firmware (G-01-10 / WINDOWS 15, the phase's top blocker) — ALL THREE crash expressions survived in ONE session with ZERO crash signatures anywhere in the balloon console: (1) the first-post-boot push (session 7's killer — the spaced smoke's capture/manifest/chunk sequence from a fresh boot); (2) SUSTAINED multi-window FULL service (session 8's killer — a full spanning at least seven windows, SVGA-class ~137 chunks reproducing image 38's pattern, serving EVERY window to COMPLETE); (3) the INTER-WINDOW LULL (session 9's killer — after a cleanly completed window, a TX-light dwell of at least 3 minutes with only beacons/sensors/idle traffic flowing, zero crash signatures through and beyond the dwell; session 9 died ~2 minutes into its first lull at t=124026 ms). Crash-signature greps each count zero across the WHOLE console: rst:0x7, rst:0xc, Guru Meditation, stack canary, i2cWrite-adjacent-to-failure, and the mojibake raw-byte grep (the corrected method — the UTF-8-replacement-char grep returns 0, the twice-documented trap). The 01-30 §9-named discriminator/instrument lines are quoted proving engagement (or their absence explained — an instrument that never fired while no crash occurred is a NEGATIVE answer, which is evidence)"
      verification: backstop
    - statement: "G-01-7 series-A acceptance (NINTH attempt at this truth): 3 back-to-back unspaced captures — 3 ACKs (held transmits must never reach 'timeout after 3 retries'), 3 captures, 6/6 verdicts COMPLETE, zero command timeouts, zero silently absent fulls, with the round-#10 discriminator lines ('re-announce budget re-armed', receipt-evidenced class-5 eviction labels, base 'full-pull activation deadline reached') proving lever engagement or the residual mechanism named precisely"
      verification: backstop
    - statement: "SC-3 visible-effect pairs (EIGHTH round riding — this session MUST judge them): at fixed QVGA, brightness -2 vs +2 judged as a visible pair, plus at least one other class (contrast -2/+2, saturation -2/+2, or quality 10 vs 30) judged as a pair — per-pair verdicts recorded in Test 3"
      verification: backstop
    - statement: "WR-03 cadence discriminator (EIGHTH round riding): AUTO_CAPTURE_ENABLE at 20 s, one interval capture, one manual CAPTURE_NOW, the next interval capture lands one full interval (~20 s) after the MANUAL capture — both 'Captured image ID' timestamps quoted; disable confirmed with zero further automatic captures; this discharges the SC-5 coincidental-reliance advisory (the 01-19 baseline change finally bench-discriminated on current HEAD)"
      verification: backstop
    - statement: "CIF-cycle (SET_RESOLUTION wire 8, as specified) with both kinds COMPLETE at CIF-class sizing (~31-chunk fulls vs QVGA ~21), and QVGA restore (wire 6) with the next capture QVGA-class by wire evidence, zero FB-OVF at every step (the G-01-8 regression watch; session 8's wire-10 deviation not repeated)"
      verification: backstop
    - statement: "Dashboard glance is operator-LOOKED (d8ba14e gallery-detail resLabel/filesize still unlooked-at after five sessions): all seven explicit UI-SPEC considerations hold plus the LED-truth backstop — and the gallery detail fields render"
      verification: backstop
    - statement: "Ledger honesty: G-01-10/G-01-7 and WINDOWS entries 15/3 flip ONLY on operator-observed console/dashboard evidence with verbatim quotes, line numbers, and counts; a crashed or failed session leaves its gaps open with the residual named, records requirements-completed: [] (the 01-24/01-27/01-29 failed-session convention), and any crash appends its dump to the debug doc (§10) and re-opens D1 — never a forced close; STATE.md names /gsd-secure-phase 1 as the remaining phase-close gate if everything closes"
      verification: explicit
    - "UI-SPEC lift (explicit): before any command the Last Command panel renders the documented 'No commands yet' copy and every settings form renders a complete documented default value — no form is ever shown unfilled"
    - "UI-SPEC lift (explicit): an in-flight command shows the locked 'Sent' state string immediately after POST; the 1s /status poll advances the panel to a terminal state (status-line model, no spinners/skeletons)"
    - "UI-SPEC lift (explicit): Sent/Acked/Failed/Pending counters advance as commands process; 4-column grid on desktop, 2x2 below 480px"
    - "UI-SPEC lift (explicit): each setting is an independent single-field form — a multi-field partial state cannot occur; forms always display a complete value"
    - "UI-SPEC lift (explicit): the status bar collapses 4->2 columns under the 480px media query; counter cells never truncate; Last Command text wraps within card width — no clipping"
    - "UI-SPEC lift (explicit): numeric readouts only — no singular/plural copy; layout identical at 0, 1, and many"
    - "UI-SPEC lift (explicit): .message and label text wrap at 14px/1.5 inside the card; no ellipsis or truncation anywhere in the Phase 1 UI"
    - statement: "UI-SPEC lift (backstop): terminal 'Timeout' / 'Failed (retry N)' with documented error copy renders after retries are exhausted — held-out verification remains the UAT Test 2 hardware proof (already PASS); during this session the link LED stays truthful while commands hold for channel quiet (no red No-link driven by a hold that is working as designed)"
      verification: backstop
  artifacts:
    - "Consolidated bench session logs (balloon15.log / base10.log naming request, deviation-tolerant and recorded per the 01-24 convention) covering all three D1 crash expressions, series A, the WR-03 cadence discriminator, the SC-3 pairs, the CIF/QVGA-restore clauses, and the dashboard glance — retained in the repo root as every prior session was"
    - ".planning/phases/01-command-protocol-control/01-UAT.md — G-01-10 flip (zero-crash evidence across all three expressions) or honest re-open with the new dump appended to the debug doc §10; G-01-7 flip (6/6 + discriminators) or named residual; Test 3 note updated with the SC-3-pair, CIF-cycle, QVGA-restore, and D1-survival outcomes; Test 4 note carries the cadence discriminator outcome"
    - ".planning/WINDOWS.md — entries 15 (D1) and 3 (G-01-7) updated per evidence, both copies (table + JSON), counts reconciled"
    - ".planning/STATE.md — round outcome recorded; /gsd-secure-phase 1 named as the remaining phase-close gate if everything closes"
    - ".planning/phases/01-command-protocol-control/01-31-SUMMARY.md"
  key_links:
    - "Behavior changed on the balloon this round (the 01-30 fix/instrumentation; base-side only if §9 named a base site — flash BOTH boards regardless, the standing both-boards rule; mixed firmware yields misleading evidence even with the wire format unchanged)"
    - "D1 acceptance <-> §9's instruments: the session's pass/fail for G-01-10 reads ALL THREE crash-expression survivals AND the 01-30 §9-named discriminator lines — absence of resets with the instruments never engaging is the instruments' designed negative answer, recorded as evidence either way"
    - "Series-A verdicts <-> the round-#10 levers + the round-#13 work: a crash under series A re-opens D1 with the evidence appended to the debug doc (§10) rather than being read as a transfer defect; a non-crash failure is attributed via the discriminator lines BEFORE any gap is left open"
    - "SC-3/WR-03/CIF clauses <-> the numbered protocol steps: all have ridden eight rounds because sessions issued zero settings/auto-capture commands — this session makes them explicit steps 6-8, not ride-alongs"
  prohibitions:
    - statement: "MUST NOT fabricate image-transfer, command, crash-survival, or settings-verdict success — closures cite operator-observed finalize/queue/console/dashboard lines with line numbers and counts, never code-level reasoning"
      status: flagged
      verification: backstop
    - statement: "MUST NOT mark G-01-10 or G-01-7 resolved on code-level evidence alone — closure requires this session's hardware evidence (all three crash expressions survived / 6/6 verdicts)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT flash only one board — both boards reflash before any radio traffic"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT fix newly observed bench defects inside this plan — route them (WINDOWS ledger / todos / debug docs) as every prior round did; this round's code work ended at 01-30"
      status: flagged
      verification: backstop
    - statement: "MUST NOT show protocol or camera state that does not reflect reality (recalled project prohibition, UAT Test 5 tier): no ACK for unexecuted actions, no green link LED while failed, GET_STATUS fields match actual balloon state"
      status: flagged
      verification: backstop
---

<objective>
Operator bench re-verification session #10 of gap-closure round #13: prove the D1 round-#13 work on hardware against ALL THREE crash expressions — the first-post-boot push (session 7's killer), the SUSTAINED multi-window FULL service (session 8's killer), and the NEW inter-window-lull dwell (session 9's killer: the TX-light lull after a cleanly completed window) — then re-run the G-01-7 series-A acceptance (ninth attempt), finally judge the truths that have ridden eight rounds (SC-3 visible-effect pairs, WR-03 cadence discriminator, CIF-cycle-as-specified + QVGA restore), take the operator-LOOKED dashboard glance, and flip the gap ledgers on evidence.

Purpose: session 9 proved even SPACED single captures are unsafe (the balloon died ~2 minutes into the FIRST image's inter-window lull), so no bench moment exists until D1 closes — and every riding clause (eighth round for the settings/cadence family) accumulates behind this one session. With D1 closed, the phase's remaining gate is the security review.

Output: one consolidated bench session (both consoles logged); G-01-10/G-01-7 flipped or honestly re-opened in 01-UAT.md; WINDOWS entries 15/3 updated per evidence with counts reconciled; Test 3 judged on its final open clauses; Test 4's note carries the WR-03 outcome (discharging the SC-5 advisory); STATE.md records the outcome and the /gsd-secure-phase 1 gate.

Spec-less probe fallback: recorded visible skip — phase_req_ids null this run, no probe predicates generated (standard goal-backward verification, the established determination).
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/debug/d1-crash-regression-push-start.md
@.planning/phases/01-command-protocol-control/01-30-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-29-SUMMARY.md
@.planning/WINDOWS.md

Binding decisions and bench conventions:
- House evidence convention: verbatim console quotes with line numbers and counts, both consoles retained (balloon15.log / base10.log naming request; the 01-24 deviation lesson — operator-retained names are recorded in the ledgers, never renamed).
- Session-9 crash baseline to beat (balloon3.log/base3.log): the spaced smoke's first window served CLEAN (thumb 9/9, FULL window 1 16/16 on air) but the balloon reset in the TX-light INTER-WINDOW LULL at t=124026 ms — :520 i2cWrite ESP_ERR_INVALID_STATE with session-7-class mojibake ON the line -> :523 rst:0x7 TG0WDT_SYS_RST, Saved PC 0x4037c7fa = esp_vApplicationTickHook. The D1 pass bar this session: ALL THREE expressions dead — first-post-boot push clean AND a multi-window sustained full serving EVERY window to COMPLETE AND a >=3-minute TX-light lull dwell survived — with zero crash signatures across the whole console AND the 01-30 §9-named discriminator/instrument lines engaged (or their absence explained as a negative answer).
- Discriminator lines to watch: the 01-30 §9-named lines (read §9 + 01-30-SUMMARY.md before the session), the round-#12 lines ([BOOT] reset-cause at each boot, [LOOP] slow-pass, [MEM] samples), the round-#10 levers (balloon 're-announce budget re-armed', receipt-evidenced class-5 eviction labels; base 'full-pull activation deadline reached'), and the D2-fixed 're-announce held' line (truthful by construction — fires only on genuine inbound traffic).
- Corrected mojibake grep (the twice-documented trap — the UTF-8-replacement-char grep returns 0): count the raw byte sequence with grep -c $'\xe2\x88\xa9\xe2\x94\x90\xe2\x95\x9c' <balloon-console-log> (bytes e2 88 a9 e2 94 90 e2 95 9c — the corruption renders as three CJK-box characters mid-word). For every i2cWrite occurrence also capture context: grep -n -B2 -A3 i2cWrite <balloon-console-log> — an occurrence followed by a successful BMP280 read and far from any reset is the known non-fatal class; one ADJACENT to a reset (or followed by no read) is the session-9 fatal pattern.
- SC-3 pairs protocol: brightness -2 vs +2 plus one other class (contrast -2/+2, saturation -2/+2, or quality 10 vs 30 — quality changes file size visibly); judge at fixed QVGA; the queue's SET commands ACK; the pair comparison is the operator judgment; thumbnails suffice unless the full is needed (note which).
- WR-03 discriminator protocol: AUTO_CAPTURE_ENABLE at 20 s, one interval capture, one manual CAPTURE_NOW, the NEXT interval capture must land ~20 s after the manual (quote both 'Captured image ID' timestamps); disable and confirm zero further automatic captures.
- CIF/QVGA-restore clauses: SET_RESOLUTION 8 (CIF — wire 8, as specified) then 6 (QVGA) — session 5's wire-evidence pattern (CIF full ~31 chunks vs QVGA ~21) is the judging template; zero FB-OVF at every step is the G-01-8 regression watch. The sustained-service step (step 3) may use SVGA (wire 10) — restore QVGA (wire 6) afterward and confirm by wire evidence.
- WR-01 battery watch: no 'Critical battery' lines; beacons batt=valid (all prior sessions green). WR-02 watch: zero 'health check failed' lines at healthy boots (bench-verified at sessions 8/9; regression watch only).
- UAT Test 3 is the 'issue' holder; Tests 1/2/4/5 stand PASS and must not regress. The interim spacing discipline (avoid unspaced bursts) applies UNTIL the D1 steps (2-4) pass — only then is a burst legitimate.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Bench session #10 — flash both boards, D1 all-three-expressions acceptance (spaced smoke + sustained multi-window service + lull dwell), series A, WR-03 cadence discriminator, SC-3 visible-effect pairs, CIF cycle (wire 8) + QVGA restore, dashboard LOOK glance</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md</files>
  <read_first>
  - .planning/debug/d1-crash-regression-push-start.md — §8 (the session-9 crash pattern this session must beat: the lull death at t=124026 ms) and §9 (the round-#13 root cause/audit, the fix/instrumentation, and THIS session's discriminator lines)
  - .planning/phases/01-command-protocol-control/01-30-SUMMARY.md — what changed, the touched sites, and the new log lines to expect
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-10 gap block (all three crash signatures + the lull extension), G-01-7 gap block (series-A acceptance + round-#10 discriminators), Test 3/Test 4 notes (the riding clauses)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — Gaps Summary + behavior_unverified_items + coincidental_reliance_items (the exact truths this session exercises)
  </read_first>
  <what-built>
  Plan 01-30: the D1 round-#13 fix and/or instrumentation at the §9-named site(s) with its discriminator lines, plus the audit record — built green on both targets, harness exit 0, no bench claims made.
  </what-built>
  <how-to-verify>
  1. PRE-FLIGHT: pio run -e esp32-s3-balloon -e esp32-s3-basestation (2 succeeded) and node scripts/verify_protocol_roundtrip.mjs (exit 0) on the round's final commit. Record sha256sum .pio/build/esp32-s3-balloon/firmware.elf (the round-#13 build's SHA — needed for any addr2line decode of a new crash; compare to the 01-30 record). Flash BOTH boards with that exact firmware (balloon behavior changed; base reflash is the standing both-boards rule). Start both serial consoles logging to balloon15.log / base10.log.
  2. D1 CLASS 1 — FIRST-POST-BOOT PUSH (session 7's killer): ONE CAPTURE_NOW as the very first post-boot capture; wait for both finalize lines (thumb + full COMPLETE); zero crash signatures.
  3. D1 CLASS 2 — SUSTAINED MULTI-WINDOW FULL SERVICE (session 8's killer): let a full serve to completion across MULTIPLE windows — ideally SVGA-class (~137 chunks, >=7 windows, the exact image-38 pattern; SET_RESOLUTION wire 10 first if needed, restore per step 8). Watch every window arm -> chunks -> completion with zero crash signatures; quote how many windows served and the finalize line.
  4. D1 CLASS 3 — INTER-WINDOW LULL DWELL (session 9's killer, NEW explicit step): at any point after a cleanly completed window (the smoke's window 1 qualifies), idle the pair TX-light for AT LEAST 3 MINUTES — no new captures or commands; ordinary beacons, sensor reads, and idle GET_STATUSs may flow. The balloon console must show zero crash signatures through the dwell and beyond (session 9 died ~2 minutes into its first lull). Record the balloon-side lines that prove the lull pattern ([BCN] beacons + sensor activity only, no window service) and the wall-clock span of the dwell.
  5. SERIES A (G-01-7 acceptance, ninth attempt — legitimate only after steps 2-4 pass): 3 CAPTURE_NOW back-to-back WITHOUT waiting. Expected: 3 ACKs (quiet-gate holds may precede; a held command must NEVER reach 'timeout after 3 retries'), 3 captures, 6/6 finalize lines COMPLETE, every full transferred / re-announced-and-transferred / drop-logged by name; the round-#10 discriminator lines present with a passing series proves the levers engaged; ABSENCE with a passing series means the natural cadence sufficed (also fine — record which). Any crash appends the dump to the debug doc (§10) and re-opens D1 — never read as a transfer defect.
  6. WR-03 CADENCE DISCRIMINATOR (eighth round riding — this session MUST judge it): AUTO_CAPTURE_ENABLE at 20 s; one interval capture; one manual CAPTURE_NOW; verify the NEXT interval capture lands ~20 s AFTER the manual trigger (quote both 'Captured image ID' timestamps); AUTO_CAPTURE_DISABLE; zero further automatic captures.
  7. SC-3 VISIBLE-EFFECT PAIRS (eighth round riding — MUST judge): at fixed QVGA — (a) SET_BRIGHTNESS -2, capture; SET_BRIGHTNESS +2, capture; judge the pair visibly different and record the verdict; (b) one other class: contrast -2/+2, saturation -2/+2, or quality 10 vs 30; capture and judge. Note per pair whether thumbnails sufficed.
  8. CIF CYCLE + QVGA RESTORE (as specified): SET_RESOLUTION 8 (CIF) — expect the re-init log, both kinds COMPLETE at CIF-class sizing (~31-chunk fulls), zero FB-OVF; then SET_RESOLUTION 6 (QVGA) — ACK plus a subsequent capture at QVGA-class sizing (~21 chunks), both kinds COMPLETE. (If step 3 used SVGA, this step doubles as the restore confirmation.)
  9. DASHBOARD LOOK GLANCE (operator-LOOKED, not did-not-flag): 'No commands yet' before the first command; 'Sent' immediately after each POST advancing via the poll; counters advance; link LED stays truthful (green while chunks/holds flow); status bar collapses 2x2 under 480px; no truncated/clipped text; gallery detail renders the resLabel/filesize fields.
  10. RECORD everything: verbatim quotes with line numbers for every verdict, hold, re-announce, drop, deadline-release, retry, discriminator, and the crash-signature grep counts across the WHOLE balloon console — rst:0x7, rst:0xc, Guru Meditation, stack canary, i2cWrite (with -B2 -A3 context per occurrence), and the corrected raw-byte mojibake grep — zero is a result worth quoting; the 01-30 §9-named lines and their contexts; the round-#12 lines ([BOOT] per boot, [LOOP] count, [MEM] samples). If ANY step fails: capture the discriminating lines, leave that gap open — do not force a close.
  </how-to-verify>
  <acceptance_criteria>
  - D1: the first-post-boot push, the sustained multi-window full service, AND the >=3-minute lull dwell all complete with zero crash signatures anywhere in the balloon console (grep counts quoted for each signature, mojibake via the corrected raw-byte method); the 01-30 §9-named discriminator/instrument lines engaged and quoted (or their absence explained as the instruments' negative answer)
  - Series A: 3/3 ACKs, 3/3 captures, 6/6 COMPLETE verdicts, zero command timeouts, zero silently absent fulls; round-#10 discriminators present or their absence explained
  - WR-03: next interval capture lands one full interval after the manual capture — both timestamps quoted; disable stops automatic captures
  - SC-3: brightness pair judged plus one other class judged, per-pair verdicts recorded
  - CIF cycle (wire 8) and QVGA restore (wire 6): both-kinds COMPLETE at the expected size classes, zero FB-OVF at every step
  - Dashboard glance: all seven explicit UI-SPEC considerations hold plus the LED-truth backstop, and the gallery detail resLabel/filesize fields render
  - Both console logs retained (balloon15.log / base10.log or the operator's chosen names, recorded) with every cited quote locatable by line number; the round-#13 ELF SHA recorded for any decode
  </acceptance_criteria>
  <resume-signal>Type "approved" with the console excerpts (crash-signature grep counts incl. the mojibake raw-byte count, lull-dwell span + lines, sustained-service window count + finalize, series-A verdicts + discriminator lines, WR-03 timestamps, SC-3 pair judgments, CIF/QVGA-restore evidence, dashboard glance verdicts), or describe the failing step with its discriminating log lines (a crash appends to the debug doc §10 and re-opens D1).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Flip the ledgers on evidence — 01-UAT.md (G-01-10/G-01-7, Test 3/Test 4), WINDOWS entries 15/3, STATE.md, debug doc §10 on any new crash</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md, .planning/WINDOWS.md, .planning/STATE.md, .planning/debug/d1-crash-regression-push-start.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-UAT.md — the G-01-10/G-01-7 gap blocks, Test 3 note (SC-3/CIF/QVGA clauses), Test 4 note (WR-03 rides); house quote convention from the G-01-8/G-01-9/G-01-11 resolutions and the 01-24/01-27/01-29 session extensions
  - .planning/WINDOWS.md — entries 15 and 3 (table + JSON copies must both update; counts in front matter reconcile from 2/17/19)
  - .planning/STATE.md — Current Status / Next Steps (the round outcome and the /gsd-secure-phase 1 gate)
  - .planning/debug/d1-crash-regression-push-start.md — §8/§9 structure (a new crash appends §10 in the same evidence convention)
  - The operator's Task 1 evidence (approved resume message with console excerpts, plus the retained session logs)
  </read_first>
  <action>
  Update the ledgers strictly from the Task 1 evidence:
  1. 01-UAT.md G-01-10: if the D1 pass bar held (zero crash signatures across the first-post-boot push AND the sustained multi-window service AND the >=3-minute lull dwell AND series A, with the 01-30 §9-named lines engaged or their negative answer recorded), set status resolved with resolved_by naming the round-#13 work (root cause + lever/instruments from debug doc §9), resolved_at, and verified_by quoting the operator-observed crash-signature grep counts (zero each, mojibake via the corrected raw-byte method), the lull-dwell span, the sustained-service window count + finalize line, and the discriminator quotes (match the G-01-8/G-01-9/G-01-11 resolution structure). If anything crashed, keep status open, append the new dump evidence to the debug doc (§10) in the house convention (provenance/SHA first, event map, decoded addresses vs the recorded round-#13 ELF SHA, discriminator readings), and extend root_cause with what the new evidence discriminates (a FOURTH expression). If the §9 instruments never engaged despite no crashes, record that honestly — the negative answer is evidence, and its meaning comes from §9's hypothesis table.
  2. 01-UAT.md G-01-7: if series A passed 6/6 per the acceptance criteria, set status resolved with resolved_by naming the lever chain (the round-#13 D1 closure restoring crash-free transfer + 01-21 receipt-evidence re-announce/eviction ranking + 01-22 full-arm deadline + 01-18 quiet gate) and verified_by quoting the verdicts, the zero-timeout evidence, and the discriminator lines. If it failed, keep status open and extend root_cause with the mechanism the discriminators named.
  3. 01-UAT.md Test 3 note: record the D1 survival outcome (all three expressions), the SC-3 per-pair verdicts, the CIF-cycle and QVGA-restore outcomes; flip Test 3 to pass only if every clause it holds is now judged and none failed. Test 4 note: record the WR-03 cadence discriminator outcome (this discharges the SC-5 coincidental-reliance advisory from 01-VERIFICATION.md).
  4. WINDOWS.md: entry 15 (D1) and entry 3 (G-01-7) flip on the session evidence or stay open with the residual; update BOTH the markdown table and the JSON copy; reconcile the counts in the front matter.
  5. STATE.md: record the round outcome in Current Status (what closed on what evidence, what remains open if anything), and set Next Steps to the security gate (/gsd-secure-phase 1; Phase 2/3 gates also pending) as the remaining phase-close step if everything closed, then /gsd-progress. Note: the five pending auto-advanced disposition confirmations (01-23, 01-25, 01-26, 01-28, 01-30) remain end-of-phase operator decisions unless the operator addresses them at this checkpoint — record any that are confirmed here.
  6. On a failed session: record requirements-completed: [] (the 01-24/01-27/01-29 convention) — no requirement claims ride a failed bench session; the SUMMARY names what rode instead.
  7. Commit the ledger updates separately from any log retention, message docs(01-31): bench session #10 evidence + ledger flips.
  </action>
  <verify>
  <automated>grep -q "01-31" .planning/STATE.md && grep -q "secure-phase" .planning/STATE.md && grep -c "G-01-10" .planning/phases/01-command-protocol-control/01-UAT.md | grep -qv "^0$" && grep -q '"id": 15' .planning/WINDOWS.md && grep -q '"id": 3' .planning/WINDOWS.md</automated>
  </verify>
  <acceptance_criteria>
  - Every bench-evidence flip cites operator-observed quotes with line numbers/counts from the retained session logs; failed steps leave their gap open with the residual mechanism named in the discriminator vocabulary
  - WINDOWS.md table and JSON copies agree; front-matter counts equal the actual row statuses
  - The D1 closure (if it happened) quotes the lull-dwell span AND the sustained-service evidence AND the 01-30 discriminator engagement (or the recorded negative answer) — not merely an absence of resets
  - Test 3 reflects the D1-survival, SC-3-pair, CIF-cycle, and QVGA-restore outcomes; Test 4's note carries the WR-03 discriminator outcome; Tests 1/2/5 notes untouched absent regression evidence
  - STATE.md names /gsd-secure-phase 1 as the remaining phase-close gate; on a failed session requirements-completed: [] is recorded
  </acceptance_criteria>
  <done>The gap ledgers reflect hardware truth: D1 closed on zero-crash evidence across all three expressions + series, or honestly re-opened with new dump evidence in §10; G-01-7 closed on 6/6 series evidence or honestly rescoped with its next lever; Test 3 judged on its final clauses; the SC-5 advisory discharged; and the phase's remaining gate identified.</done>
  <reversibility rating="reversible">Documentation-only ledger updates.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench operator -> firmware flash + consoles | Human performs the flash and observes; logs are retained as evidence (read-only artifacts) |
| RF pair -> bench evidence | The two-radio link under bench conditions produces the verdicts; no untrusted input enters the firmware beyond the CRC16-validated frames covered in prior rounds' threat models |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-31-01 | Tampering | evidence integrity | medium | mitigate | House convention enforced: closures quote verbatim console lines with line numbers/counts from retained logs; crash-signature grep counts (incl. the corrected raw-byte mojibake method) are quoted as zero-results, not assumed; the round-#13 ELF SHA is recorded before any decode |
| T-01-31-02 | DoS | mixed-firmware flash invalidating the evidence | high | mitigate | Blocking checkpoint step 1 flashes BOTH boards before any traffic; the standing both-boards rule with the 01-09 severance lesson recorded |
| T-01-31-03 | DoS | a crash mid-session misread as a transfer defect | medium | mitigate | Protocol step 5 names the re-open path: any crash appends its dump to the debug doc (§10) and re-opens D1 — never silently attributed to G-01-7 |
| T-01-31-04 | DoS | a shallow lull dwell masking the session-9 class | medium | mitigate | Step 4 sets a hard bar: >=3 minutes TX-light, spanning past the session-9 death latency (~2 min into the first lull), with the balloon-side lull-pattern lines recorded — a shorter or busy dwell is recorded as such and the clause stays open |
| T-01-31-05 | Safety | bench hardware | low | accept | Standard bench rig (USB-powered base, battery balloon at desk); WR-01 battery watch green in all prior sessions; the round changed no power paths |

## Artifacts this phase produces

- balloon15.log / base10.log (or operator-chosen names, recorded) — retained bench console evidence (repo root, prior-session convention)
- Updated gap blocks G-01-10/G-01-7 + Test 3/Test 4 notes (01-UAT.md)
- Updated WINDOWS entries 15 and 3 with reconciled counts (.planning/WINDOWS.md)
- Round outcome + next-gate record (.planning/STATE.md)
- Debug doc §10 (only on a new crash — the re-open evidence append)
- .planning/phases/01-command-protocol-control/01-31-SUMMARY.md
- No source files, no new symbols, no env vars

<verification>
- Both firmware targets built green and the harness exited 0 immediately before the flash (pre-flight step, on record in the session); the round-#13 ELF SHA recorded
- Every ledger flip traces to a quotable line in the retained logs or carries its explicit evidence-class reason
- WINDOWS counts reconcile with row statuses; /gsd-ship remains blocked exactly as long as an entry is honestly open
</verification>

<success_criteria>
- The round's acceptance is judged on hardware: all three D1 crash expressions dead (first-post-boot push + sustained multi-window service + lull dwell) plus series A, with the 01-30 instruments' answer recorded (or D1 re-opened with new dump evidence in §10), series A reaches 6/6 verdicts (or the honest residual named), SC-3 pairs and the WR-03 discriminator finally judged, CIF/QVGA-restore clauses exercised as specified, dashboard LOOKED
- With G-01-10/G-01-7 closed, the WINDOWS ledger reaches zero open entries and the phase's remaining close gate is the security review (/gsd-secure-phase 1)
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-31-SUMMARY.md` when done
</output>
