---
phase: 01-command-protocol-control
plan: "09"
type: execute
wave: 2
depends_on: ["01-07", "01-08"]
gap_closure: true
gap_ids: [G-01-3]
files_modified:
  - include/base_station_config.h
autonomous: false
requirements: [IMG-01, IMG-02, IMG-04, IMG-05, PRI-03]
user_setup:
  - service: physical-hardware
    why: "G-01-3 root cause (A) is a physical SD wiring/card fault and root cause (B) verification needs both radios on the bench — no CLI can touch these"
    steps:
      - "Physical access to the base station board and its SD breakout (4 signal wires + 3V3/GND)"
      - "A second known-good FAT32-formatted microSD card"
      - "Serial monitor access to BOTH boards (balloon UART0 + base USB at 115200) for the discriminator traces"
      - "Ability to flash both boards (pio run -e <env> -t upload or your usual method)"

estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "UAT Test 3 storage truth (G-01-3 root cause A): with the wiring audited against the constants in include/base_station_config.h (SCK=12/MISO=13/MOSI=11/CS=10) and a known-good FAT32 card seated, the base boots to the line SdStorage: card mounted, /images ready and the dashboard Storage section shows OK (green) instead of Unavailable"
    - "UAT Test 3 transfer truth (G-01-3 root cause B): after both boards run the 01-08 firmware (9.6 kbps air rate enforced at boot), a triggered capture completes end-to-end on the bench — queue rows reach terminal states without TIMEOUT, transfer progress reaches COMPLETE, the thumbnail renders in the UI, and the full image plus its sidecars land on the SD card (IMG_XXXXX.JPG, IMG_XXXXX_T.JPG, IMG_XXXXX.JSON, IMG_XXXXX_T.JSON in /images)"
    - "The rate change did not sever the pair: immediately after flashing both boards, a command round-trip still ACKs within 2 s (UAT Test 1 re-run) — the first thing the bench session proves, before any transfer test"
    - "The discriminator evidence is captured: one capture attempt with both serial consoles logging, matched against the debug session's B1/B2/B3/B4 matrix (balloon ImageTx chunk sent/FAILED and E32 Transmit timeout lines; base type=13 CRC FAIL, manifest, and Image Window Timeout queue rows) — so that IF failures persist, the residual mechanism is NAMED, not guessed"
    - "If the SD wiring differs from the constants, include/base_station_config.h SD_SCK_PIN/SD_MISO_PIN/SD_MOSI_PIN/SD_CS_PIN are edited to match the audited wiring and the base is rebuilt/reflashed by the executor before Task 2 — the editable-constants contract from 02-03"
  artifacts:
    - path: ".planning/phases/01-command-protocol-control/01-09-SUMMARY.md"
      provides: "Bench session record: wiring audit outcome, boot lines observed, E32 decoded-config line, discriminator trace lines captured, transfer outcomes, and either closure confirmation or the named residual mechanism for a follow-up round"
      contains: "G-01-3"
  key_links:
    - from: "operator bench steps (wiring audit, card seating, flash, serial capture)"
      to: "gap G-01-3 resolution evidence consumed by /gsd-verify-work UAT re-test"
      via: "this plan's checkpoints produce the recorded observations; the matching 01-09-SUMMARY.md marks the gap's plan set complete for resume reconciliation"
      pattern: "G-01-3"
  prohibitions:
    - statement: "MUST NOT mark G-01-3 resolved on code-level evidence alone: closure requires the operator-observed boot line (card mounted) AND an observed completed transfer on hardware — the two hardware truths the UAT gap names"
      status: unverified
      flagged: true
      verification: judgment
---

<objective>
Close UAT gap G-01-3 (blocker, Test 3) on hardware. The debug session (storage-unavailable-image-tx-timeout.md, FINAL) proved the two symptoms have two INDEPENDENT physical causes and that all code logic is correct: (A) Storage Unavailable is a real SD.begin() mount failure — wiring vs the pin constants in include/base_station_config.h, or card contact/power; (B) image-transfer timeouts isolate to E32 transport of the system's only multi-RF-packet frames. Plan 01-08 delivered the code-side rate remediation; this plan is the operator-side closure: audit the wiring, retry with a known-good card, flash both boards with the 01-07+01-08 firmware, run the one-shot bench discriminator the debug session defined, and confirm a capture completes end-to-end.

Purpose: G-01-3 is the blocker standing between Phase 01 and completion — its two sub-symptoms also gate the never-run Phase 02 hardware UAT truths (SD persistence, transfer completion) that every later phase assumes.
Output: operator-verified storage mount + completed transfer + captured discriminator evidence, recorded in 01-09-SUMMARY.md; a named residual mechanism (with trace lines) instead of a guess, if anything still fails.
</objective>

<execution_context>
@C:/Users/Amp/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Amp/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/debug/storage-unavailable-image-tx-timeout.md
@.planning/phases/01-command-protocol-control/01-07-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-08-SUMMARY.md

Hardware ground truth for the checkpoints: the SD pin constants live at include/base_station_config.h:34-37 (SD_SCK_PIN 12, SD_MISO_PIN 13, SD_MOSI_PIN 11, SD_CS_PIN 10 — explicitly editable constants, 02-03 user_setup note); the boot verdict lines are printed by src/sd_storage.cpp:55-57 (failure, naming the pins) and :81 (SdStorage: card mounted, /images ready); the E32 decoded-config and air-rate lines come from 01-08's ensureLinkConfig. Do NOT re-plan G-01-1 (balloon link fault) — resolved by 1223f46 and UAT-verified.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: SD wiring audit + known-good card retry (G-01-3 root cause A)</name>
  <action>
  Physical remediation only — the debug session proved the UI truthfully reports a REAL SD.begin() mount failure and that the begin()/pin code is correct (dedicated SPIClass(HSPI) + instance-overload SD.begin). The fault is physical: the wiring was an explicitly unverified assumption (02-03 deferred pin confirmation to exactly this hardware session), with card contact/module power as the secondary candidate.
  </action>
  <instructions>
  1. Power the base station OFF. Audit the SD breakout wiring against the constants in include/base_station_config.h:34-37 — SCK to GPIO 12, MISO to GPIO 13, MOSI to GPIO 11, CS to GPIO 10, plus 3V3 and GND. Fix any mismatched wire, OR note the actual pins if the wiring is deliberate and different (the constants are editable precisely for this — step 4 below handles it).
  2. Reseat the card. Power on with the USB serial monitor open at 115200 and read the boot verdict: the failure line names the pins (SdStorage: SD.begin failed (pins SCK=12 MISO=13 MOSI=11 CS=10); running WITHOUT storage...) vs the success line (SdStorage: card mounted, /images ready).
  3. If the failure line persists with wiring that matches the constants: power off, swap in the SECOND known-good FAT32 card, power on, read the verdict again. Success with card 2 = card 1 contact/fault issue (root cause A-secondary confirmed).
  4. If the wiring is deliberately different from the constants: report the four actual pins — the executor will update SD_SCK_PIN/SD_MISO_PIN/SD_MOSI_PIN/SD_CS_PIN in include/base_station_config.h, rebuild the base target, and ask you to re-flash and repeat step 2.
  </instructions>
  <verification>
  Expected end state: boot line SdStorage: card mounted, /images ready AND the dashboard Storage tile showing OK (green), not Unavailable. If neither card mounts with wiring that matches the constants, report that — it escalates to a board/module-level fault (new debug round), not a constants edit.
  </verification>
  <resume-signal>
  Report: (a) what the wiring audit found — "matched 12/13/11/10", or the four actual pins, or "rewired to match"; (b) which boot verdict line appeared; (c) which card succeeded (original / second / neither).
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Flash both boards, run the bench discriminator, verify end-to-end transfer + in-page submit</name>
  <what-built>
  Plans 01-07 and 01-08 built: (1) in-page AJAX submission for every control form in section#capture — buttons no longer navigate to raw JSON; (2) real E32 register read/write with a boot-time 9.6 kbps air-rate enforcement on BOTH boards (fail-open, loud ERROR tripwire on write failure). This checkpoint proves them on hardware and closes G-01-3's root cause B.
  </what-built>
  <how-to-verify>
  1. Flash BOTH boards with the current build: pio run -e esp32-s3-basestation -t upload and pio run -e esp32-s3-balloon -t upload (or your usual flashing method).
  2. Boot logs, both boards: confirm the E32 decoded-config line and the air-rate line — either E32: air rate already 9.6kbps or E32: air data rate 2.4kbps -> 9.6kbps (config persisted) on each board. A line reading E32: air-rate set FAILED - modules may mismatch is a STOP condition — report it and do not continue.
  3. Link sanity first (the rate change must not sever the pair): open the dashboard, press Trigger Camera Capture, confirm the queue row reaches ACK Received within 2 s. While pressing, also confirm the G-01-4 behavior: the browser STAYS on the admin page, the card shows the server's message, and the queue panel shows the new row with no Back-button navigation.
  4. Bench discriminator run: start logging BOTH serial consoles (balloon UART0 + base USB). Trigger ONE capture. Capture these trace lines: balloon ImageTx chunk sent/FAILED and E32 Transmit timeout lines; base type=13 CRC FAIL / manifest / Image Window Timeout queue rows. Match them against the debug session's matrix — B1 balloon TX timeouts (should be GONE at 9.6 kbps), B2 base CRC FAILs, B3 works/failed pattern on long frames, B4 window-request Timeout rows with partial chunks.
  5. Expected success at bench range: the transfer progress row advances to COMPLETE, the thumbnail renders in the UI, the queue shows terminal states with ZERO Timeout rows, and (with Task 1's card mounted) the /images directory on the SD card contains IMG_XXXXX.JPG, IMG_XXXXX_T.JPG and both .JSON sidecars for the new id.
  6. If anything still fails: report WHICH trace lines appeared — that names the residual mechanism (B3 = PA power feed hardware fix; B4 = a follow-up pacing plan on image_tx_manager margins). Do not guess; the matrix decides.
  </how-to-verify>
  <resume-signal>
  Type "approved" with the observed boot/trace lines (E32 config line, storage line, transfer outcome), or paste the failing trace lines and which matrix row (B1-B4) they match. Either way the executor records the evidence in 01-09-SUMMARY.md — closure or the named residual mechanism for a follow-up round.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → physical hardware | wiring changes and module EEPROM state (via 01-08's persisted rate) are operator-adjacent local actions; no network surface is added by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09-01 | Tampering | SD pin constants edited to match audited wiring | low | mitigate | constants change only shifts which GPIOs the existing, code-verified begin() uses; the 02-03 editable-constants contract; executor rebuilds and the boot verdict line re-confirms on the next power cycle |
| T-01-09-02 | Denial of Service | both modules now carry a persisted non-factory air rate | medium | accept | deliberate (G-01-3 remediation); recoverable by a single register write via the same 01-08 code path; both boards enforce the same target from begin() every boot, and step 2's STOP condition plus step 3's round-trip re-test catch any divergence |
| T-01-09-03 | Repudiation | gap marked resolved without hardware evidence | medium | mitigate | prohibition above: 01-09-SUMMARY.md must record the operator-observed boot line and transfer outcome; code-level evidence alone does not close this gap |
</threat_model>

<verification>
- Task 1 checkpoint: boot line SdStorage: card mounted, /images ready observed; Storage tile OK
- Task 2 checkpoint: E32 air-rate line on both boards, command round-trip still ACKs < 2 s, one capture completes end-to-end (progress COMPLETE, thumbnail renders, files on SD, zero Timeout queue rows), G-01-4 in-page submit behavior confirmed
- 01-09-SUMMARY.md records the evidence (or the named residual mechanism + trace lines)
</verification>

<success_criteria>
- G-01-3 hardware truths closed: Storage section reflects the mounted card; picture transmissions complete without timing out
- Discriminator evidence captured once, so any residual failure names its mechanism instead of restarting diagnosis
- G-01-4 operator truth spot-confirmed on the same flashed build
- If a residual mechanism is named (B3/B4), the SUMMARY records it as the input to the next gap round — not silently dropped
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-09-SUMMARY.md` when done
</output>
