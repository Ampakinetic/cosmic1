---
phase: 01-command-protocol-control
plan: 14
type: execute
wave: 11
depends_on: ["01-13"]
files_modified:
  - include/image_protocol.h
  - include/image_tx_manager.h
  - src/image_tx_manager.cpp
autonomous: true
gap_closure: true
requirements: [IMG-02, IMG-03, PRI-03]

estimate:
  tokens: 11000
  raw_tokens: 11000
  tasks: 2
  confidence: low   # calibration store has 0 samples (factor 1, not applied); two same-file state-machine guards

must_haves:
  truths:
    - "CR-02 (G-01-9 defect C): announceFullManifest consumes the ANNOUNCED state ONLY on transmit success — a failed FULL-manifest transmit leaves the entry in ANNOUNCE_FULL so the next process() pass retries, bounded by IMG_MANIFEST_MAX_ATTEMPTS (3); at the bound the entry logs the drop loudly, frees fullBuffer, and parks at THUMB_PUSHED — a single lost FULL manifest can never silently strand a full again (session 4: image 8's full, balloon4.log:722 sent / never received)"
    - "WR-01 (same family, push path): pushThumbManifest advances to PUSH_THUMB_CHUNKS only on transmit success — a failed thumbnail-manifest transmit retries on the next pass under the same shared attempt bound; at the bound the thumb is dropped honestly (buffer freed, log) and the entry proceeds via completedThumbState instead of pushing chunks no slot exists for"
    - "WR-02: the queue-overflow victim scan in enqueueCapture skips any entry with an armed, incomplete window (the same predicate as the one-at-a-time BUSY check) — an in-flight thumbnail heal is never the preferred overflow victim; a fallback pass without the skip keeps the depth-3 queue from ever wedging when every entry is mid-service"
    - "The one-transmit-per-pass discipline, the beacon early-return, the TX arbitration order, evictionClassOf's class ranking, and sweepExpiredEntries' TTL path are all unchanged (prohibitions); manifestAttempts resets on every manifest success and in freeEntry so a retried manifest never inherits a stale count"
    - "Both firmware targets build green and the wire harness stays green (no wire change in this plan)"
  artifacts:
    - "include/image_protocol.h — new constant IMG_MANIFEST_MAX_ATTEMPTS = 3 with the CR-02/WR-01 rationale comment"
    - "include/image_tx_manager.h — new ImageTxEntry field uint8_t manifestAttempts (shared by the two mutually exclusive manifest phases; reset on success and in freeEntry)"
    - "src/image_tx_manager.cpp — success-gated state transitions in pushThumbManifest and announceFullManifest with bounded retry + park-and-free at the bound; two-pass mid-service-aware overflow victim scan in enqueueCapture"
    - ".planning/phases/01-command-protocol-control/01-14-SUMMARY.md"
  key_links:
    - "manifestAttempts <-> the two manifest phases: PUSH_THUMB_MANIFEST and ANNOUNCE_FULL are sequential and mutually exclusive per entry; the counter is shared but reset to 0 on each success, so a thumb manifest that succeeded on attempt 2 never pre-charges the full announce"
    - "ANNOUNCED <-> window servicing: chunks still flow only through windows armed by handleWindowRequest (Pitfall 5 unchanged); the fix changes WHEN the announcement is consumed, never the serving model"
    - "Overflow skip predicate <-> BUSY predicate: identical expression (windowArmed && windowNextIndex < windowStart + windowCount) — copy it, do not paraphrase; a divergent predicate recreates the WR-02 hole or over-blocks"
    - "Bound <-> PRI-03 honest degradation: at the announce bound the full is dropped with a named log (never silent), the thumbnail has already pushed (push precedes announce by construction), so the base degrades to thumbnail-only for that capture — bounded, logged, honest"
  prohibitions:
    - statement: "MUST NOT change the one-transmit-per-pass discipline, the beacon early-return, or the TX arbitration order — PRI-01 stays harness-proven and untouched"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT alter evictionClassOf's class ranking or sweepExpiredEntries' TTL path — the WR-02 skip lives ONLY in the enqueueCapture overflow victim scan"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT retry manifests unboundedly — IMG_MANIFEST_MAX_ATTEMPTS is a hard bound with buffer-free at exhaustion; a dead link can never spin the announce or wedge a queue slot"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT close G-01-9 defect C on code-level evidence alone — closure requires the 01-16 bench (or at minimum an observed re-announce recovery at bench); code-level only closes the code half"
      status: flagged
      verification: backstop
---

<objective>
Balloon TX state-machine hardening for the lost-manifest defect family and the overflow-eviction hole: CR-02 — announceFullManifest sets ANNOUNCED unconditionally, so one failed FULL-manifest transmit strands the full forever (G-01-9 defect C, the G-01-7 image-8 mechanism); WR-01 — pushThumbManifest has the same defect on the thumbnail push path; WR-02 — the overflow victim scan ranks an in-flight thumbnail heal as the FIRST eviction victim (the supersede path got its guard in 01-12; the overflow path did not).

Purpose: of the three distinct series-A failure mechanisms, this plan removes the "silently lost FULL manifest" leg and hardens the two adjacent state-machine paths the fresh review named as the same family; combined with 01-13's thumb-sizing and misroute fixes and 01-15's defer-aware pass accounting, the series-A acceptance truth becomes achievable at the 01-16 bench.

Output: success-gated, bounded-retry manifest transitions with honest park-and-free at the bound; a mid-service-aware overflow victim scan with a never-wedge fallback; builds and harness green. No gap status flips (bench proof is 01-16's job).

Spec-less probe fallback: recorded visible skip — this phase has no machine-mapped requirement IDs (phase_req_ids null), so no probe predicates were generated this run.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-REVIEW.md
@.planning/phases/01-command-protocol-control/01-13-SUMMARY.md

Binding decisions and prior-round constraints:
- D-17 (thumbnail pushes first, then the one-time FULL announcement), D-19 (FIFO pull order), Pitfall 5 (balloon never volunteers chunks outside a requested window), PRI-03 (graceful degradation under bandwidth limits — bounded, logged, never silent)
- 01-12 scope boundary carried: the mid-service guard landed ONLY in evictEntriesOlderThan; evictionClassOf and sweepExpiredEntries kept full eviction rights — this plan extends the guard family to the overflow scan WITHOUT touching those two functions (prohibition)
- Bench evidence: image 8's FULL manifest sent once and never received (balloon4.log:722; 10 of 11 manifests arrived; no re-announce path exists — the base never saw the full and nothing surfaced the loss); the AUX phantom-failure class is already fixed (f265556), so remaining transmit failures are real (short-write / readiness) — rare but now recoverable instead of strand-ing
- Code facts: pushThumbManifest at src/image_tx_manager.cpp:480-516 (state advance at :512-513 unconditional); announceFullManifest at :567-604 (state advance at :599-602 unconditional — the review's CR-02 quote block); the overflow victim scan at :327-361 with evictionClassOf at :203-217; freeEntry at :904-929; the BUSY predicate at :703-712; ImageTxEntry fields at include/image_tx_manager.h:83-126
- The manifest frame is 34 B on the wire — a retry costs one process() pass of the cheapest frame class; the bound is about never spinning, not airtime conservation
</context>

<tasks>

<task type="auto">
  <name>Task 1: CR-02 + WR-01 — success-gated manifest transitions with a shared bounded attempt counter</name>
  <files>include/image_protocol.h, include/image_tx_manager.h, src/image_tx_manager.cpp</files>
  <read_first>
  - src/image_tx_manager.cpp — pushThumbManifest (:480-516), announceFullManifest (:567-604), completedThumbState (:556-561), freeEntry (:904-929), fullTransferArmable (:895-898)
  - include/image_tx_manager.h — ImageTxEntry (:83-126), the state enum and its ANNOUNCED/THUMB_PUSHED comments (:41-54)
  - include/image_protocol.h — constants block placement conventions (:59-110)
  - .planning/phases/01-command-protocol-control/01-REVIEW.md — CR-02 and WR-01 findings (the exact fix shapes this task implements)
  </read_first>
  <action>
  Gate both manifest transitions on transmit success; bound the retries; degrade honestly at the bound.

  1. include/image_protocol.h: add `static constexpr uint8_t IMG_MANIFEST_MAX_ATTEMPTS = 3;` in the constants block (near IMG_RETRANSMIT_MAX_PASSES) with a comment naming CR-02/WR-01: a manifest transmit failure must not consume its one-shot state; retries are bounded so a dead link cannot spin the announce; at the bound the payload buffer is freed and the kind degrades honestly (full dropped to thumbnail-only / thumbnail dropped to full-only).
  2. include/image_tx_manager.h: add `uint8_t manifestAttempts;` to ImageTxEntry beside lastActivityMs, commented as shared by the two mutually exclusive manifest phases (PUSH_THUMB_MANIFEST and ANNOUNCE_FULL are sequential; the counter resets on each success so phases never inherit counts).
  3. src/image_tx_manager.cpp pushThumbManifest: keep building and transmitting exactly as today. On transmit success: manifestAttempts = 0; advance to PUSH_THUMB_CHUNKS (today's behavior). On failure: manifestAttempts++; if manifestAttempts < IMG_MANIFEST_MAX_ATTEMPTS, stay in PUSH_THUMB_MANIFEST (the next process() pass retries — one transmit per pass is the existing pacing) and return false; at the bound log 'ImageTx: thumbnail manifest for image %u failed after %u attempts; thumbnail dropped' (with the attempt constant), free thumbBuffer (free + null, zero thumbLength/thumbCrc32/thumbTotalChunks), and set state = completedThumbState(entry) — the entry proceeds to ANNOUNCE_FULL or parks at THUMB_PUSHED, exactly as a completed push would, minus thumbnail bytes. lastActivityMs advances on every attempt (today's line stays last).
  4. src/image_tx_manager.cpp announceFullManifest: same shape. On success: manifestAttempts = 0; state = ANNOUNCED (today's behavior, comment updated from "emitted exactly ONCE" to "consumed only on a successful transmit; failures retry bounded by IMG_MANIFEST_MAX_ATTEMPTS"). On failure: manifestAttempts++; below the bound, stay in ANNOUNCE_FULL and return false; at the bound log 'ImageTx: FULL manifest for image %u failed after %u attempts; full dropped', free fullBuffer (free + null, zero fullLength/fullCrc32/fullTotalChunks), and set state = THUMB_PUSHED (parked, heal-only on the already-pushed thumbnail; the thumbBuffer stays owned so THUMBNAIL window heals keep working).
  5. src/image_tx_manager.cpp freeEntry: add manifestAttempts = 0 beside the other resets so a recycled slot starts clean.
  6. Update the ImageTxEntryState enum comment for ANNOUNCE_FULL/ANNOUNCED (include/image_tx_manager.h) so code and comments agree on the retry semantics — comment honesty per house rule.
  </action>
  <verify>
  <automated>powershell -Command "if((Select-String -Path include/image_protocol.h -Pattern 'IMG_MANIFEST_MAX_ATTEMPTS\s*=\s*3').Count -ne 1){exit 1}" ; powershell -Command "if((Select-String -Path include/image_tx_manager.h -Pattern 'manifestAttempts').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path src/image_tx_manager.cpp -Pattern 'manifestAttempts').Count -lt 7){exit 1}" ; powershell -Command "if((Select-String -Path src/image_tx_manager.cpp -Pattern 'FULL manifest for image %u failed after').Count -ne 1){exit 1}" ; powershell -Command "if((Select-String -Path src/image_tx_manager.cpp -Pattern 'thumbnail manifest for image %u failed after').Count -ne 1){exit 1}" ; pio run -e esp32-s3-balloon -e esp32-s3-basestation ; node scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - include/image_protocol.h defines IMG_MANIFEST_MAX_ATTEMPTS = 3 exactly once; all six standing pacing constants still hold their values
  - src/image_tx_manager.cpp: the ANNOUNCED assignment executes only inside the transmit-success branch of announceFullManifest; the PUSH_THUMB_CHUNKS assignment only inside pushThumbManifest's success branch (structure verified by the manifestAttempts reference count >= 7: two increments, two bound checks, two resets-on-success, one freeEntry reset)
  - Both bound paths free their payload buffer and land in a defined state (ANNOUNCE_FULL-park -> THUMB_PUSHED; thumb-drop -> completedThumbState) with the two named drop logs present exactly once each
  - freeEntry resets manifestAttempts
  - `pio run -e esp32-s3-balloon -e esp32-s3-basestation` reports 2 succeeded; harness exits 0
  - git diff file set for this task is exactly the three listed files
  </acceptance_criteria>
  <done>A failed manifest transmit retries bounded and consumes its one-shot state only on success; at the bound the kind degrades honestly with a named log and freed memory — the silently-stranded-full defect class is closed on the TX side.</done>
  <reversibility rating="reversible">State-machine logic in three files, no wire change; git revert.</reversibility>
</task>

<task type="auto">
  <name>Task 2: WR-02 — mid-service-aware overflow victim scan (never wedge the depth-3 queue)</name>
  <files>src/image_tx_manager.cpp</files>
  <read_first>
  - src/image_tx_manager.cpp — enqueueCapture overflow branch (:327-361), evictionClassOf (:203-217, DO NOT MODIFY), the BUSY check whose predicate must be copied verbatim (:703-712), freeEntry (:904-929)
  - include/image_tx_manager.h — ImageTxEntry window-context fields (:104-123)
  - .planning/phases/01-command-protocol-control/01-REVIEW.md — WR-02 finding (THUMB_PUSHED class-1 victim; the fix shape with the unless-every-entry caveat)
  </read_first>
  <action>
  Teach the overflow victim scan the same mid-service awareness the supersede path got in 01-12 — without taking any eviction right away from the queue's wedge safety.

  1. In enqueueCapture's overflow branch, make victim selection two-pass:
     - Pass 1: today's class-then-age scan with ONE added skip — an entry whose windowArmed is true AND whose windowNextIndex < windowStart + windowCount (copy the BUSY predicate verbatim; do not paraphrase) is not eligible. When a pass-1 victim is found, evict it with today's log line.
     - Pass 2 (only when pass 1 found nothing — every entry is mid-service): rerun today's scan with no skip and evict the class-ranked victim, logging 'ImageTx: queue overflow - all entries mid-service; evicting class %u (%s) entry image %u for image %u' so the operator sees the queue chose liveness over the in-flight window. The queue must never reject a capture because three windows happened to be armed.
  2. Update the overflow branch's scope comment to state the WR-02 boundary: the skip lives ONLY in this scan; evictionClassOf's ranking and sweepExpiredEntries' TTL path keep their full eviction rights (carried 01-12 prohibition — do not touch either function).
  3. Nothing else changes: the supersede-path guard (evictEntriesOlderThan), the BUSY deferral, and the settle gate are untouched.
  </action>
  <verify>
  <automated>powershell -Command "if((Select-String -Path src/image_tx_manager.cpp -Pattern 'all entries mid-service').Count -ne 1){exit 1}" ; powershell -Command "if((Select-String -Path src/image_tx_manager.cpp -Pattern 'evictionClassOf').Count -lt 3){exit 1}" ; powershell -Command "if((Select-String -Path src/image_tx_manager.cpp -Pattern 'windowNextIndex < entries\[i\].windowStart \+ entries\[i\].windowCount').Count -lt 3){exit 1}" ; pio run -e esp32-s3-balloon -e esp32-s3-basestation ; node scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - The overflow scan contains a mid-service skip pass and a distinct fallback pass whose log names the all-mid-service condition (grep 'all entries mid-service' == 1)
  - The skip predicate appears at three sites in the file: the BUSY check, the supersede guard, and the new overflow pass-1 (grep on the shared windowNextIndex/windowStart expression >= 3)
  - evictionClassOf and sweepExpiredEntries bodies are byte-identical to HEAD (diff-hunk check; the evictionClassOf references remain the declaration + overflow call only)
  - `pio run -e esp32-s3-balloon -e esp32-s3-basestation` reports 2 succeeded; harness exits 0
  - git diff file set for this task is exactly src/image_tx_manager.cpp
  </acceptance_criteria>
  <done>An in-flight thumbnail heal is never the preferred overflow eviction victim, and the depth-3 queue can still always admit a new capture — the WR-02 hole is closed with the 01-12 scope boundary intact.</done>
  <reversibility rating="reversible">Single-file scan change; git revert.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link -> balloon TX state machine | Transmit success/failure verdicts from E32LoRa drive manifest state transitions (a false verdict strands or spins transfers) |
| PSRAM budget -> TX queue | Queue depth 3 with PSRAM-owned copies; overflow eviction is the memory-pressure release valve |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14-01 | DoS | announceFullManifest retry loop | high | mitigate | Hard bound IMG_MANIFEST_MAX_ATTEMPTS 3 with buffer-free at exhaustion — a persistently dead link drops the kind honestly instead of spinning transmits or pinning PSRAM |
| T-01-14-02 | DoS | pushThumbManifest retry loop | medium | mitigate | Same shared bound; thumbnail drop frees thumbBuffer and proceeds to the full announce |
| T-01-14-03 | DoS | overflow eviction vs pinned slots | medium | mitigate | Two-pass scan: mid-service entries are skipped unless ALL are mid-service — the queue can never wedge on three armed windows |
| T-01-14-04 | Tampering | E32 AUX verdict feeding state transitions | low | accept | The AUX phantom-failure accounting defect was fixed in f265556 (complete-write-then-missed-AUX is benign); remaining failures are real short-write/readiness signals — trusting them is correct |
</threat_model>

## Artifacts this phase produces

- `IMG_MANIFEST_MAX_ATTEMPTS` (include/image_protocol.h) — static constexpr uint8_t, 3
- `ImageTxEntry.manifestAttempts` (include/image_tx_manager.h) — new field
- Two drop logs: 'FULL manifest for image %u failed after %u attempts; full dropped' and the thumbnail twin (src/image_tx_manager.cpp)
- 'all entries mid-service' fallback log + the two-pass overflow scan (src/image_tx_manager.cpp)
- No new files, tasks, env vars, or CLI flags

<verification>
- `pio run -e esp32-s3-balloon -e esp32-s3-basestation` — 2 succeeded
- `node scripts/verify_protocol_roundtrip.mjs` — exit 0
- Pacing-anchor greps: the six standing constants plus the new bound, each exactly once
- Diff hygiene: two commits matching the tasks' file sets; evictionClassOf/sweepExpiredEntries hunks absent
</verification>

<success_criteria>
- ANNOUNCED and PUSH_THUMB_CHUNKS are consumed only on successful transmits, bounded by a 3-attempt shared counter with honest park-and-free at the bound
- The overflow victim scan never prefers a mid-service window and never wedges
- No gap status flips in this plan (bench proof is 01-16's job)
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-14-SUMMARY.md` when done
</output>
