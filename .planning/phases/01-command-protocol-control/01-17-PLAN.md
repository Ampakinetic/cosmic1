---
phase: 01-command-protocol-control
plan: 17
type: execute
wave: 13
depends_on: ["01-16"]
files_modified:
  - include/image_protocol.h
  - include/image_tx_manager.h
  - src/image_tx_manager.cpp
  - .planning/WINDOWS.md
autonomous: true
gap_closure: true
requirements: [IMG-02, IMG-03, IMG-04, PRI-03]

estimate:
  tokens: 7000
  raw_tokens: 7000
  tasks: 3
  confidence: low   # calibration store has 0 samples (factor 1, not applied); two in-place mechanism additions + ledger routing

must_haves:
  truths:
    - statement: "WR-08 truth: a failed chunk transmit retries the SAME index on the next process() pass for up to IMG_CHUNK_TX_RETRY_MAX attempts before skipping — a transmit failure never permanently skips a chunk for its pass, and a window whose final chunk transmit failed is never marked SERVED (bytes never offered are never declared served)"
      verification: backstop
    - statement: "G-01-9 defect C truth: no full is silently dropped for a lost FULL manifest — an announced full that no FULL window ever arms is re-announced up to IMG_FULL_REANNOUNCE_MAX times while idle, then dropped with a named log; an air-lost manifest whose TX succeeded (the one-shot announce consumed, image-15 class, balloon5.log:274) recovers via re-announce and the full transfers"
      verification: backstop
    - "Idle-bound safety: IMG_FULL_REANNOUNCE_IDLE_MS (10000) sits above every legitimate pre-pull quiet period (base inter-window gap ~1-2 s, RX settle 500 ms, thumb-heal-to-full-pull handoff ~1-2 s), and a re-announce never fires once a FULL window has armed on the entry (fullWindowEverArmed gate — window-arm IS the receipt signal, making the re-announce receipt-informed)"
    - "Duplicate-manifest containment: re-announces stop at the first FULL window arm, so a duplicate manifest can only arrive pre-first-arm where the base-side restart is benign (no window context, no outstanding request — the IN-08 hazard window is never entered)"
  artifacts:
    - "include/image_protocol.h — IMG_CHUNK_TX_RETRY_MAX, IMG_FULL_REANNOUNCE_IDLE_MS, IMG_FULL_REANNOUNCE_MAX (new constants; existing pacing constants and the settle<preempt<stall invariant chain untouched)"
    - "include/image_tx_manager.h — ImageTxEntry fields fullWindowEverArmed, reannounceAttempts, thumbChunkFailStreak, windowChunkFailStreak"
    - "src/image_tx_manager.cpp — success-gated cursor advance in pushThumbChunk/serviceWindowChunk; findReannounceCandidate/reannounceFullManifest wired into pushPending's idle slot"
    - ".planning/WINDOWS.md — entries 8 (CR-04) and 9 (WR-08) recorded open, counts reconciled"
  key_links:
    - "pushPending ordering: beacon early-return (process()) > preempted window > push work > window service > re-announce — the re-announce occupies the pass's single transmit ONLY when the channel is otherwise idle; PRI-01 untouched"
    - "handleWindowRequest FULL arm <-> fullWindowEverArmed: set at the windowEverArmed assignment for non-thumb windows; reset only in freeEntry"
    - "freeEntry <-> every new field: a recycled slot starts with clean streaks, counters, and the re-announce gate open"
  prohibitions:
    - statement: "MUST NOT change any existing pacing constant or wire body — IMG_WINDOW_STALL_MS/PREEMPT_MS/RX_SETTLE_MS, IMG_MANIFEST_MAX_ATTEMPTS, manifest body 27, beacon body 19 stay byte-identical (new constants are additions)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT mark a window SERVED whose final chunk transmit failed, and MUST NOT advance a chunk cursor on a failed transmit except after the same-index bound"
      status: flagged
      verification: backstop
    - statement: "MUST NOT re-announce an entry whose FULL window has armed (receipt proven) — the fullWindowEverArmed gate is the containment for duplicate-manifest churn"
      status: flagged
      verification: backstop
---

<objective>
Close the balloon-side halves of the two open image-reliability residuals: WR-08 (TX push paths advance cursors on transmit failure) and G-01-9 defect C (a TX-success/air-lost FULL manifest silently consumes the one-shot announce with no recovery). Also routes the two unrated review findings (CR-04, WR-08) into the WINDOWS ledger.

Purpose: session 5 (balloon5.log/base5.log) eliminated every session-4 failure mode but proved two structural holes. Image 15's FULL manifest left the balloon with TX-success accounting (balloon5.log:274), never arrived (zero 'manifest image 15 kind 1' lines in base5.log), consumed the ANNOUNCED state, and nothing named the loss — the 01-14 re-announce is TX-verdict-gated and structurally cannot see air loss. And both TX push paths (pushThumbChunk src/image_tx_manager.cpp:601, serviceWindowChunk :881-895) advance cursors unconditionally after serialize+transmit, so a failed transmit permanently skips that chunk for its pass and a failed tail chunk can mark an entry SERVED whose final bytes never left the balloon (verifier-confirmed at HEAD, unexercised at session 5 only because zero TX failures occurred).

Output: WR-08 cursor/SERVED gating with a bounded same-index retry; a receipt-informed periodic FULL-manifest re-announce while an announced full is idle with no FULL window ever armed, ending in a named drop log at the bound; WINDOWS entries 8/9 recorded open. Both firmware targets green; wire harness green. No gap-status flips this plan (bench closure is 01-20).

Spec-less probe fallback: recorded visible skip — this phase has no machine-mapped requirement IDs (phase_req_ids null), so no probe predicates were generated this run.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/phases/01-command-protocol-control/01-14-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-16-SUMMARY.md

Binding evidence and invariants:
- Defect-C proof class (session 5): balloon5.log:274 'ImageTx: FULL manifest(image 15, 4853 B, 25 chunks) sent' with TX success, zero base receipts, silent full loss — the named lever (01-UAT.md G-01-9): "balloon-side periodic bounded re-announce while head-and-idle, independent of TX verdict". This plan implements that lever; the base-side-nudge alternative is NOT taken (no wire change, no new command type — and window-arm already is a receipt signal the balloon can observe).
- WR-08 sites (verifier-read at HEAD): pushThumbChunk advances entry.nextThumbChunk at src/image_tx_manager.cpp:601 regardless of ok; serviceWindowChunk advances entry.windowNextIndex at :881 and the completion block (:883-895) can clear windowArmed and mark SERVED on a failed tail transmit.
- The 01-14 manifest-attempt machinery (manifestAttempts, IMG_MANIFEST_MAX_ATTEMPTS 3, park-and-free at the bound, freeEntry reset) is the structural template for the re-announce bound — mirror its shape, do not touch it.
- PRI-01 invariant: beacon early-return in process() and command-response loop order are load-bearing (harness PRI order-gates) — the re-announce must live at the END of pushPending's priority chain, never before window service.
- IN-08 (carried Info): a duplicate manifest at the base restarts the transfer slot zeroing window context. Harmless pre-first-arm (no context exists); the fullWindowEverArmed gate keeps re-announce duplicates entirely inside that benign window. IN-08 itself stays carried — out of this round's named scope.
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-08 — gate chunk-cursor advance and the SERVED transition on transmit success, with a bounded same-index retry</name>
  <files>include/image_protocol.h, include/image_tx_manager.h, src/image_tx_manager.cpp</files>
  <read_first>
  - src/image_tx_manager.cpp — pushThumbChunk (:569-609) and serviceWindowChunk (:822-898), the two cursor-advance sites; freeEntry (:990-1016) for the field-reset pattern; the 01-14 success-gating comments in pushThumbManifest/announceFullManifest as the structural template
  - include/image_tx_manager.h — ImageTxEntry field block (where nextThumbChunk/manifestAttempts live, with their comment conventions)
  - include/image_protocol.h — Transfer Constants block (IMG_RETRANSMIT_MAX_PASSES / IMG_MANIFEST_MAX_ATTEMPTS neighborhood; the invariant-chain comments)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — gap 4 (WR-08 truth, reason, missing items)
  </read_first>
  <action>
  1. include/image_protocol.h, Transfer Constants block, next to IMG_RETRANSMIT_MAX_PASSES: add IMG_CHUNK_TX_RETRY_MAX = 3 (uint8_t) with a comment naming WR-08 — on a FAILED chunk transmit the same index retries on subsequent process() passes up to this bound before the chunk is skipped for its pass; the D-22 kind-addressable window re-request remains the recovery path for skipped chunks, so the bound trades 3 cheap same-index retries for a multi-second stall-timeout round trip.
  2. include/image_tx_manager.h, ImageTxEntry: add uint8_t thumbChunkFailStreak and uint8_t windowChunkFailStreak fields with WR-08 comments (consecutive same-index transmit failures in the push path and the window path respectively; reset on any successful transmit and in freeEntry).
  3. src/image_tx_manager.cpp pushThumbChunk: move entry.nextThumbChunk++ off the unconditional path — on ok, reset thumbChunkFailStreak to 0 and advance; on !ok, increment thumbChunkFailStreak and advance ONLY when the streak has reached IMG_CHUNK_TX_RETRY_MAX, logging the skip by name (for example: 'ImageTx: chunk(image %u kind %u, %u/%u) skipped after %u failed transmit attempts') and resetting the streak; the completion transition to completedThumbState stays evaluated only when nextThumbChunk actually reached thumbTotalChunks. Keep entry.lastActivityMs advancing on every attempt (a transmit attempt is activity — prevents preempt-clock thrash against pushPending).
  4. src/image_tx_manager.cpp serviceWindowChunk: the same pattern for entry.windowNextIndex / windowChunkFailStreak, with the skip log naming the window kind. Critically, the window-completion block (windowArmed = false, and the SERVED marking when the clamped span reached fullTotalChunks) evaluates ONLY on a transmit-successful final chunk: a final chunk skipped after the bound completes the window (cursor advanced, windowArmed cleared, the base's tail re-request re-opens service) but leaves the entry at ANNOUNCED — never SERVED for bytes that never left the balloon.
  5. src/image_tx_manager.cpp freeEntry: reset both streak fields alongside manifestAttempts.
  No changes to evictionClassOf, sweepExpiredEntries, the beacon path, or any existing constant.
  </action>
  <verify>
  <automated>powershell -Command "if((Select-String -Path include/image_protocol.h -Pattern 'IMG_CHUNK_TX_RETRY_MAX').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path include/image_tx_manager.h -Pattern 'thumbChunkFailStreak').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path include/image_tx_manager.h -Pattern 'windowChunkFailStreak').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path src/image_tx_manager.cpp -Pattern 'skipped after').Count -lt 2){exit 1}" ; pio run -e esp32-s3-balloon -e esp32-s3-basestation ; node scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - include/image_protocol.h defines IMG_CHUNK_TX_RETRY_MAX exactly once, value 3, in the Transfer Constants block
  - include/image_tx_manager.h declares thumbChunkFailStreak and windowChunkFailStreak on ImageTxEntry; src/image_tx_manager.cpp freeEntry resets both
  - src/image_tx_manager.cpp contains the skip log in BOTH the push path and the window path (grep 'skipped after' count >= 2)
  - In serviceWindowChunk, the SERVED assignment is reachable only from the transmit-success path of the final chunk (the skip-after-bound branch leaves the entry ANNOUNCED)
  - Both firmware targets build (2 succeeded); node scripts/verify_protocol_roundtrip.mjs exits 0 (52 checks; PRI order-gates green)
  </acceptance_criteria>
  <done>A transmit failure no longer permanently skips a chunk for its pass and can no longer mark a never-offered window SERVED; builds and harness green.</done>
  <reversibility rating="reversible">In-place logic gating plus additive constants/fields — single-commit revertable.</reversibility>
</task>

<task type="auto">
  <name>Task 2: G-01-9 defect C — receipt-informed FULL-manifest re-announce while ANNOUNCED-and-idle, with a named drop at the bound</name>
  <files>include/image_protocol.h, include/image_tx_manager.h, src/image_tx_manager.cpp</files>
  <read_first>
  - src/image_tx_manager.cpp — announceFullManifest (:623-686, the manifest body construction and the park-and-free bound this task mirrors), handleWindowRequest (:692-820, the arming block where windowEverArmed is set), pushPending (:455-504, the priority chain whose idle tail gets the re-announce), freeEntry
  - include/image_tx_manager.h — ImageTxEntry and the ImageTxManager private-method block
  - include/image_protocol.h — Transfer Constants (IMG_MANIFEST_MAX_ATTEMPTS comment as the bound-rationale template; IMG_WINDOW_RX_SETTLE_MS / IMG_THUMB_HEAL_IDLE_MS for the idle-bound rationale)
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-9 gap block, defect C residual paragraph and the named lever
  </read_first>
  <action>
  1. include/image_protocol.h, Transfer Constants: add IMG_FULL_REANNOUNCE_IDLE_MS = 10000 (uint32_t) and IMG_FULL_REANNOUNCE_MAX = 3 (uint8_t) with rationale comments: the idle bound sits above every legitimate pre-pull quiet period (base inter-window gap ~1-2 s, RX settle 500 ms, thumb-heal-to-full-pull handoff ~1-2 s) and below nothing it must not delay (a base that received the manifest arms its first FULL window within seconds of the hold releasing); the attempt bound mirrors IMG_MANIFEST_MAX_ATTEMPTS so a dead-link full can never spin re-announces — at the bound the full is dropped with a named log, never silently (PRI-03).
  2. include/image_tx_manager.h, ImageTxEntry: add bool fullWindowEverArmed (comment: a FULL window has armed on this entry — the receipt signal that permanently stops re-announces; distinct from windowEverArmed which THUMBNAIL heals also set) and uint8_t reannounceAttempts; both reset in freeEntry.
  3. src/image_tx_manager.cpp handleWindowRequest: at the arming block where windowEverArmed is set, also set fullWindowEverArmed = true for non-thumb windows only (a THUMBNAIL heal arm must NOT stop the full re-announce — image 15's exact shape is thumb-healed-COMPLETE with the FULL manifest air-lost).
  4. Extract the FULL manifest body construction currently inline in announceFullManifest into a private helper (for example fillFullManifestBody(const ImageTxEntry&) returning ImageManifestBody) and call it from both announceFullManifest and the new re-announce path — one body construction, no duplication, no wire change.
  5. Add private methods findReannounceCandidate() and reannounceFullManifest(ImageTxEntry&). findReannounceCandidate returns the earliest (lowest enqueueSeq) used entry with state == ANNOUNCED, fullBuffer != nullptr, fullWindowEverArmed == false, and (millis() - lastActivityMs) >= IMG_FULL_REANNOUNCE_IDLE_MS. reannounceFullManifest builds (via the shared helper), serializes, and transmits the manifest; logs by name on every attempt (for example 'ImageTx: FULL manifest(image %u, %u B, %u chunks) re-announced (%u/%u) - no window armed since announce', FAILED variant when the transmit fails); increments reannounceAttempts and advances lastActivityMs regardless of verdict (each transmit attempt is one bounded attempt and one idle period); at reannounceAttempts >= IMG_FULL_REANNOUNCE_MAX parks-and-frees exactly like the announce-bound branch in announceFullManifest — named drop log (for example 'ImageTx: image %u full dropped - no FULL window armed after %u re-announces'), free fullBuffer/fullLength/fullCrc32/fullTotalChunks, state = THUMB_PUSHED keeping thumbBuffer so thumbnail heals keep working.
  6. src/image_tx_manager.cpp pushPending: after the final findWindowServiceEntry() service block, when serving == nullptr, call findReannounceCandidate() and, if found, reannounceFullManifest(*lost) as that pass's single transmit. The re-announce thus fires only when no push work and no armed window exist — the channel's natural idle slot; the beacon early-return in process() still outranks it and PRI-01 is untouched.
  </action>
  <verify>
  <automated>powershell -Command "if((Select-String -Path include/image_protocol.h -Pattern 'IMG_FULL_REANNOUNCE_IDLE_MS').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path include/image_protocol.h -Pattern 'IMG_FULL_REANNOUNCE_MAX').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path include/image_tx_manager.h -Pattern 'fullWindowEverArmed').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path src/image_tx_manager.cpp -Pattern 'fullWindowEverArmed = true').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path src/image_tx_manager.cpp -Pattern 're-announced').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path src/image_tx_manager.cpp -Pattern 'no FULL window armed').Count -lt 1){exit 1}" ; pio run -e esp32-s3-balloon -e esp32-s3-basestation ; node scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - IMG_FULL_REANNOUNCE_IDLE_MS (10000) and IMG_FULL_REANNOUNCE_MAX (3) defined exactly once in include/image_protocol.h; no existing constant modified (grep each pre-existing pacing constant still exactly once)
  - ImageTxEntry carries fullWindowEverArmed and reannounceAttempts; freeEntry resets both; handleWindowRequest sets fullWindowEverArmed only for non-thumb windows
  - The re-announce log literal ('re-announced') and the drop log literal ('no FULL window armed') both exist in src/image_tx_manager.cpp
  - pushPending reaches findReannounceCandidate only after both findActiveEntry-driven push work and findWindowServiceEntry window service found nothing
  - The manifest body construction is shared (announceFullManifest and the re-announce path call the same helper) — one construction site
  - Both firmware targets build; harness exits 0 (manifest body still 27, beacon body still 19)
  </acceptance_criteria>
  <done>An air-lost FULL manifest is recovered by a bounded idle re-announce or surfaced by a named drop log — the silent-loss class is structurally gone; receipt (first FULL window arm) permanently stops the mechanism.</done>
  <reversibility rating="reversible">Additive state machine branch + fields + constants; reverts with the commit.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Route CR-04 and WR-08 in the WINDOWS ledger (entries 8 and 9, open)</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
  - .planning/WINDOWS.md — full ledger (table + JSON copy + front-matter counts; the entry-6/7 fixed rows as the description/reason style reference)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — gaps 3 and 4 (the CR-04/WR-08 findings with verifier-confirmed sites)
  </read_first>
  <action>
  Record the two unrouted findings as open entries — routing only, no status flips (flips happen at the 01-20 bench on evidence):
  1. Entry 8: kind unmet-truth, phase 01, file src/command_handler.cpp, line 139 — description: CR-04 blanket camera-ready gate in CommandHandler::executeCommand refuses EVERY command when camera->isReady() is false (isReady is just 'initialized'), so a low-battery camera-down window (main_balloon.cpp:864 enableCamera(false)) blocks IMAGE_WINDOW_REQUEST and GET_STATUS while ImageTx PSRAM buffers and the E32 radio stay operational — announced fulls become unretrievable exactly when an operator must recover them before power loss, and the base status poll fails while the balloon still beacons; found by review round #8 (01-REVIEW.md d546ace), confirmed at source by re-verification #7. Reason field: fix scoped to the 8 camera-touching handlers, landing in plan 01-19 this round; flip at the 01-20 bench.
  2. Entry 9: kind unmet-truth, phase 01, file src/image_tx_manager.cpp, line 601 — description: WR-08 push paths advance chunk cursors on failed transmit (pushThumbChunk nextThumbChunk++ and serviceWindowChunk windowNextIndex++ unconditional), and a failed tail chunk can mark an entry SERVED whose final bytes never left the balloon; unexercised at session 5 (zero TX failures); found by review round #8, confirmed at source by re-verification #7. Reason field: success-gated advance + SERVED with a same-index bound (IMG_CHUNK_TX_RETRY_MAX), landing in plan 01-17 Task 1 this round; flip at the 01-20 bench.
  3. Update BOTH the markdown table and the JSON copy; reconcile the front matter to open_count 4, fixed_count 5, total_count 9; update last_updated. No other entry changes.
  </action>
  <verify>
  <automated>powershell -Command "if((Select-String -Path .planning/WINDOWS.md -Pattern 'CR-04 blanket camera-ready gate').Count -lt 2){exit 1}" ; powershell -Command "if((Select-String -Path .planning/WINDOWS.md -Pattern 'WR-08 push paths advance chunk cursors').Count -lt 2){exit 1}" ; powershell -Command "if((Select-String -Path .planning/WINDOWS.md -Pattern 'open_count: 4').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path .planning/WINDOWS.md -Pattern 'total_count: 9').Count -lt 1){exit 1}"</automated>
  </verify>
  <acceptance_criteria>
  - WINDOWS.md carries entries 8 and 9 in the table AND the JSON copy, both status open, recorded_at set, descriptions quoting the verifier-confirmed sites and the fix plans
  - Front matter reads open_count 4 / fixed_count 5 / total_count 9 and equals the actual row statuses
  - Entries 1-7 are untouched
  </acceptance_criteria>
  <done>The two unrated review findings are ledgered open with named fixes and flip conditions — /gsd-ship blocking stays truthful.</done>
  <reversibility rating="reversible">Ledger bookkeeping only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link -> balloon ImageTx state machine | Chunk-frame and window-arm flows driven by CRC16-validated frames; the re-announce and cursor-gating changes alter transmit behavior only, not frame parsing |
| balloon TX budget -> shared half-duplex channel | Re-announces and same-index retries consume airtime that beacons (PRI-01) and window service share |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-17-01 | DoS | same-index retry loop (pushThumbChunk/serviceWindowChunk) | medium | mitigate | IMG_CHUNK_TX_RETRY_MAX 3 bounds consecutive same-index attempts; the skip-after-bound path falls back to the existing D-22 windowed re-request recovery |
| T-01-17-02 | DoS | reannounceFullManifest airtime exhaustion | medium | mitigate | IMG_FULL_REANNOUNCE_MAX 3 + 10 s idle spacing + park-and-free at the bound (mirrors the 01-14 manifest bound); a dead link can never spin re-announces |
| T-01-17-03 | Tampering | duplicate manifests at the base (IN-08 restart hazard) | low | mitigate | fullWindowEverArmed gate: duplicates stop at the first FULL window arm, confining them to the pre-first-arm phase where the base-side restart is benign (no window context, no outstanding request) |
</threat_model>

## Artifacts this phase produces

- include/image_protocol.h — constants IMG_CHUNK_TX_RETRY_MAX, IMG_FULL_REANNOUNCE_IDLE_MS, IMG_FULL_REANNOUNCE_MAX
- include/image_tx_manager.h — ImageTxEntry fields fullWindowEverArmed, reannounceAttempts, thumbChunkFailStreak, windowChunkFailStreak
- src/image_tx_manager.cpp — ImageTxManager private methods findReannounceCandidate, reannounceFullManifest, and the shared fillFullManifestBody helper; new log literals 'skipped after', 're-announced', 'no FULL window armed'
- .planning/WINDOWS.md — entries 8 (CR-04) and 9 (WR-08), open
- No wire-format changes, no new packet types, no UI changes, no env vars

<verification>
- Both firmware targets build green (pio run -e esp32-s3-balloon -e esp32-s3-basestation)
- node scripts/verify_protocol_roundtrip.mjs exits 0 — pacing-constant pins and PRI-01 order-gates prove the arbitration chain untouched
- Every pre-existing pacing constant still greps exactly once (additions only)
- WINDOWS.md counts reconcile with row statuses after routing
</verification>

<success_criteria>
- WR-08: cursor advance and the SERVED transition are transmit-success-gated with a bounded same-index retry — verifiable at source and at the 01-20 bench (same-index retry lines if any transmit failure occurs)
- G-01-9 defect C: an announced full with no FULL window ever armed is re-announced bounded while idle and dropped with a named log at the bound — the silent-loss class is structurally eliminated
- CR-04/WR-08 are routed in the WINDOWS ledger with named fixes and flip conditions
- No regression surface: beacon path, eviction ranking, TTL sweep, manifest attempt machinery, and every existing constant byte-identical
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-17-SUMMARY.md` when done
</output>
