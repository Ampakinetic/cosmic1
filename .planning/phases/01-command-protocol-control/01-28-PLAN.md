---
phase: 01-command-protocol-control
plan: 28
type: execute
wave: 20
depends_on: []
files_modified:
  - .planning/debug/d1-crash-regression-push-start.md
  - src/image_tx_manager.cpp
  - src/e32_lora.cpp
  - include/e32_lora.h
  - platformio.ini
  - .planning/WINDOWS.md
  - .planning/phases/01-command-protocol-control/01-UAT.md
autonomous: false
gap_closure: true
requirements: [CTRL-01, CTRL-06, PRI-02, IMG-03]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low   # calibration store has 0 samples (factor 1, not applied); debug round #2 = WDT-topology determination + framework-source audit + root-cause-dependent fix (01-25 sized 22000; this round adds the sdkconfig/framework-source pull and the one-or-two-mechanisms reconciliation)

must_haves:
  truths:
    - statement: "WDT topology is determined from the deployed build and recorded in the debug doc §7: which hardware watchdog a silent rst:0x7 TG0WDT_SYS_RST maps to in this arduino-esp32/ESP-IDF build (interrupt watchdog vs task watchdog), its configured period, its panic-vs-silent reset behavior — and therefore why session 8 reset SILENTLY where session 7 printed an 'Interrupt wdt timeout on CPU1' panic; the tick_hook decode (Saved PC 0x40376430 = int_wdt.c:111, ELF SHA c53635e181) is reconciled against what that function actually does in the deployed IDF version's source"
      verification: explicit
    - statement: "The post-chunk execution window is reconstructed: the fatal 'window chunk(image 38 kind 1, 1/14, 200 B) sent' line (balloon2.log:1227) prints AFTER E32LoRa::transmit returns, so the crash window is the REMAINDER of that loop pass plus the next pass — every candidate phase that plausibly executed between :1227 and the reset banner (:1228-1231) is named with its millis-timestamp bracket (last beacon [BCN] seq=46 at :1213 → next beacon due ~+5 s; window arm :1220-1224; last [MEM] sample :1175; the non-adjacent i2cWrite noise :885)"
      verification: explicit
    - statement: "Candidate-block arithmetic is complete: every site in the reconstructed window is dispositioned with (i) worst-case duration vs the determined WDT period, (ii) whether it masks interrupts, (iii) whether it holds a kernel lock (FreeRTOS SMP portMUX/spinlock) across a blocking wait, (iv) whether it suspends the flash cache — the named candidate set at minimum: E32LoRa::transmit's serial->flush() (217 B at 9600 baud ≈ 226 ms — does HardwareSerial flush yield or spin), the delay(10)-polled AUX waits (waitForAuxHigh/waitForAuxLow, e32_lora.cpp:534-558 — confirm delay() yields), the 1 Hz OLED full-frame I2C update on the BMP280-shared Wire bus (main_balloon.cpp loop block at the ~:301 region), the BMP280 and GPS sensor reads, the beacon transmit path (sendTelemetryBeacon), the PSRAM slicing memcpy in serviceWindowChunk's createChunkPacket, and Serial.printf console output"
      verification: explicit
    - statement: "The one-mechanism-or-two question raised by §6.5 is ANSWERED with evidence: session-7's Core-0 tick-ISR spinlock wedge (§1.4: SysTickIsrHandler → xTaskIncrementTick → spinlock_acquire compare-and-set spin) vs session-8's tick_hook Saved-PC stall — reconciled as ONE mechanism (a kernel-lock holder stalled past the WDT period, session-7's three-subsystem corruption read as a co-effect) or TWO distinct faults, with the discriminating evidence named either way"
      verification: explicit
    - statement: "One root cause is RANKED with evidence for/against and the honest limit recorded (the 01-25 §3 convention: if the mechanism cannot be uniquely confirmed pre-bench, the ranking says so by elimination and blast-radius reasoning, never by assumption)"
      verification: explicit
    - statement: "The D1 round-#12 fix implements the Task 2 checkpoint-selected lever at the site Task 1's evidence names, and ships the discriminator log line(s) the 01-29 bench reads to prove the lever engaged; protected surfaces survive byte-identical — the wire format, the 01-18 quiet gate constants (CMD_TX_CHANNEL_QUIET_MS 750 in include/command_sender.h), D-05/D-07 pacing, the round-#10 discriminator lines, and the [MEM] instrumentation (removal condition unchanged: strips only after G-01-10 closes on bench evidence)"
      verification: explicit
    - statement: "Both firmware targets build green and the wire-format harness exits 0 on the fix commit"
      verification: explicit
    - statement: "Ledger honesty: WINDOWS 15 and 01-UAT.md G-01-10 carry round #12's audit (root_cause extended with the WDT-starvation determination, missing list updated with the debug-round-#2 DONE item) but stay OPEN — the crash truth flips only on 01-29 hardware evidence (zero resets under the crashing pattern), never on code-level reasoning"
      verification: explicit
  artifacts:
    - ".planning/debug/d1-crash-regression-push-start.md — §7 appended (round #12): WDT topology, reconstructed crash window, candidate-by-candidate block arithmetic with the four properties, one-mechanism-or-two verdict, ranked root cause, named fix lever + bench discriminators, and the Task 2 fix-shape checkpoint record (the §5 convention)"
    - "The fix at the evidence-named site — candidates carried in files_modified (src/e32_lora.cpp transmit/flush/wait structure, src/image_tx_manager.cpp window-service pacing, platformio.ini balloon-env build_flags for any WDT-config lever); the actually-touched subset is named in §7 and the SUMMARY; untouched candidates are explicitly dispositioned in §7"
    - ".planning/WINDOWS.md — entry 15 reason field extended with the round-#12 root-cause determination; entry stays open; counts unchanged (2/17/19)"
    - ".planning/phases/01-command-protocol-control/01-UAT.md — G-01-10 root_cause extended + missing list updated; status stays open"
    - ".planning/phases/01-command-protocol-control/01-28-SUMMARY.md"
  key_links:
    - "Retained deployed ELF .pio/build/esp32-s3-balloon/firmware.elf (SHA256 c53635e181…, already verifier-reproduced by re-verification #10) <-> the audit: the audit runs BEFORE any code change, against the build that crashed — a rebuild mid-audit invalidates the decode's object of study"
    - "WDT topology <-> fix shape: the lever must target the ranked mechanism's own class (a kernel-lock-scoping change cannot fix an interrupt-masked stretch, and vice versa) — an unevidenced fix is forbidden (the 01-25 prohibition, carried)"
    - "Fix discriminators <-> 01-29's D1 acceptance: the bench session's pass/fail reads the discriminator lines this round names, not just absence of resets"
    - "[MEM] instrumentation <-> G-01-10's open state: the removal condition (named in-source at image_tx_manager.cpp:135-136 region) is NOT met — the instrumentation stays until the bench closes the gap"
  prohibitions:
    - statement: "MUST NOT flip WINDOWS 15 or G-01-10 to resolved in this plan — crash-truth closure requires 01-29's hardware session (zero resets under the crashing pattern incl. sustained FULL-window service); code + builds + harness only establishes the fix is wired"
      status: flagged
      verification: backstop
    - statement: "MUST NOT ship a fix whose lever is not named by Task 1's evidence — a speculative fix that masks the crash without removing the mechanism leaves the regression latent"
      status: flagged
      verification: backstop
    - statement: "MUST NOT alter the wire format, the 01-18 quiet gate constants, or D-05/D-07 pacing while fixing D1 — the harness must exit 0 unchanged and the round-#10 lever semantics survive unless Task 1's evidence names one of them as the root cause (then named explicitly in §7)"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT delete, mute, or gate the [MEM] instrumentation or the round-#10 discriminator lines (busy-hold episode, budget re-arm, deadline-release logs) — their removal conditions are unchanged and 01-29's judgments depend on them"
      status: flagged
      verification: backstop
---

<objective>
Close the root-cause half of the re-opened D1 crash regression (G-01-10 / WINDOWS 15, 01-VERIFICATION.md gap 1, the phase's top blocker) on the NEW evidence axis named by the debug doc §6.5: task-watchdog starvation / loopTask-or-kernel block during SUSTAINED FULL-window service. Audit the serviceWindowChunk → E32 transmit path (and the whole reconstructed crash window) for WDT-period blocks, determine the watchdog topology of the deployed build, answer the one-mechanism-or-two question against session-7's spinlock wedge, rank one root cause, and implement the checkpoint-selected fix with named bench discriminators — code + builds + harness only; hardware acceptance deferred to 01-29.

Purpose: session 8 disconfirmed the 01-25 PSRAM-warm-up hypothesis as sufficient and removed session-7's corruption corroboration — the crash moved to a deeper, cleaner signature (silent TG0WDT_SYS_RST at the first chunk of the seventh re-armed FULL window, Saved PC in the tick-ISR WDT machinery, zero project frames, healthy [MEM]). Every riding truth (G-01-7 series A, SC-3 pairs seventh round, WR-03 discriminator, CIF/QVGA clauses, dashboard LOOK) is gated behind this fix, and the interim spacing discipline is proven insufficient (session-8 captures were spaced).

Output: debug doc §7 with the WDT topology, the reconstructed crash window, dispositioned candidates, the one-or-two verdict, and the ranked root cause; the fix landed green on both targets with the harness passing and its discriminator lines shipped; WINDOWS 15 and G-01-10 carrying the round-#12 determination, honestly still open.

Spec-less probe fallback: recorded visible skip — phase_req_ids null this run, no probe predicates generated (standard goal-backward verification, the established determination).

<assumption_delta_decision>
The deterministic assumption-delta scan fired on the Phase 1 ROADMAP section (terms: "second" in the executed 01-09 bench step "second-card retry", "fallback" in the executed 01-14 plan description "never-wedge fallback" — the same two signals round #11 dispositioned). Decision: no-change — both signals trace to completed-plan prose in the roadmap listing, not to round #12's scope; this round audits and fixes a watchdog-starvation defect on the existing single-pipeline architecture, introducing no second platform, auth method, tenant, or source of truth. No identity-model promotion is warranted; no invariant test added (the 01-29 bench discriminators already encode the crash-regression tripwire this round's fix must survive).
</assumption_delta_decision>
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/debug/d1-crash-regression-push-start.md
@.planning/phases/01-command-protocol-control/01-25-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-27-SUMMARY.md
@.planning/WINDOWS.md

Binding evidence and constraints:
- Session-8 evidence (verifier-reproduced at re-verification #10; every citation locatable in balloon2.log 1,645 lines / base2.log 739 lines): the fatal event is balloon2.log:1220 window arm (image 38, chunks 96..109) → :1224 arm SUCCESS → :1227 chunk 1/14 sent (the line prints AFTER transmit returns) → :1228-1231 rst:0x7 TG0WDT_SYS_RST, Saved PC:0x40376430. Six prior FULL windows (0..95) served clean :660-:1216. [MEM] healthy at enqueue (:611) and last pre-reset sample (:1175: heap 8332460, psram 8129116, stackHW 5552 — stack never below 5552 of 8192). [BCN] seq=46 at :1213 (5 s cadence → next beacon due near the crash). Crash-signature counts, whole log: i2cWrite=2 (:885/:1577, non-adjacent BMP280 noise, each followed by a successful read), Guru=0, canary=0, rst:0x7=1, rst:0xc=0, U+FFFD=0.
- Deployed-build identity: .pio/build/esp32-s3-balloon/firmware.elf, SHA256 c53635e1819a6ca5107b1437450dbc324e5848e685b50de73ea6a4387c8e0553 — verifier-reproduced; addr2line toolchain: xtensa-esp32s3-elf-addr2line under the toolchain-xtensa-esp32s3 package in the PlatformIO packages directory, flags -pfiaC -e. The 0x40376430 → tick_hook (int_wdt.c:111) decode reproduces. AUDIT AGAINST THIS BUILD BEFORE TOUCHING CODE.
- Structural facts already read at source this planning round (line numbers shift as code changes — anchor by content): E32LoRa::transmit (e32_lora.cpp:207-270) = waitForAuxHigh(1000) → serial->write → serial->flush() → waitForAuxLow(1000, benign-miss) → waitForAuxHigh(5000); both AUX waits are delay(10)-polled loops (e32_lora.cpp:534-558) — delay() yields the CPU. serviceWindowChunk (image_tx_manager.cpp:1116-1225) = settle gate (500 ms on a re-armed window's first chunk) → PSRAM slice via createChunkPacket → serialize + lora->transmit → the kind-stamped log line. pushPending (image_tx_manager.cpp:558-645) enforces one transmit per pass. The balloon loop (main_balloon.cpp:253-330) runs updateSystemState → processSensors → processCommunications → processPowerManagement → processPacketHandling (CmdHandler().process() :904, AutoCap().process() :907, ImageTx().process() :914) → legacy telemetry/heartbeat/status gates → 1 Hz OLED I2C refresh. The project's Debug watchdog (debug_utils.cpp:213-235, triggerWatchdog :351 empty) is a disabled-by-default software latch — project code never configures or feeds any hardware WDT; whatever fired is framework-configured (arduino-esp32/ESP-IDF defaults), which is exactly what Task 1 must pin down from the build's config artifacts and the deployed framework sources.
- House debug convention: debug-session docs live in .planning/debug/; evidence quotes carry file:line; the checkpoint auto-advance convention (01-23/01-25/01-26): under workflow.auto_advance=true the Task 2 disposition auto-selects the evidence-named option and records the selection as pending end-of-phase operator confirmation.
- Scope guards: the 01-18 quiet gate (CMD_TX_CHANNEL_QUIET_MS 750 / channelQuietForTx, command_sender.cpp:575-579 region) and the 01-15/01-19 command-path semantics are protected surfaces; scripts/verify_protocol_roundtrip.mjs is the wire-format tripwire.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit the fatal window — WDT topology, post-chunk execution window, candidate-block arithmetic, one-mechanism-or-two verdict — debug doc §7</name>
  <files>.planning/debug/d1-crash-regression-push-start.md</files>
  <read_first>
  - balloon2.log — the fatal window (:1213 [BCN] seq=46, :1220-1224 window arm, :1226-1227 chunk send, :1228-1231 reset banner); the six clean windows' span (:660-:1216); [MEM] lines (:611, :1175, :1354); boot banners (:120, :1242); the non-adjacent i2cWrite noise with its successful follow-up reads (:885-886, :1577-1578); the spaced smoke (:207-253); image-38 capture/enqueue (:604-:614)
  - base2.log — the base-side counterpart: window request queued/sent/ACKED (:524-:531), the aftermath (image 38 kind 1 INCOMPLETE 109/137 after 3 passes :651-652), post-reboot rejects
  - .pio/build/esp32-s3-balloon/ — the deployed build artifacts: firmware.elf (SHA c53635e181 — confirm before use), plus the build's config artifacts (sdkconfig / config/sdkconfig.h or equivalent under the build dir) carrying CONFIG_ESP_INT_WDT / CONFIG_ESP_TASK_WDT / panic settings
  - The deployed framework sources under the PlatformIO packages dir — esp-idf int_wdt.c (what tick_hook at :111 actually does in THIS version), the uart/HardwareSerial flush implementation (does uart_wait_tx_done block on a FreeRTOS event or spin), the Wire implementation's locking (does an I2C transaction hold a kernel lock across a blocking wait)
  - src/image_tx_manager.cpp — process() order (beacon early-return, sweep, pushPending), pushPending (:558-645 incl. the preempt + one-transmit-per-pass discipline), serviceWindowChunk (:1116-1225), sendTelemetryBeacon, enqueueCapture's [MEM] watch
  - src/e32_lora.cpp — transmit (:207-270), waitForAuxHigh/waitForAuxLow (:534-558), isAuxHigh
  - src/main_balloon.cpp — loop (:253-330), the processing sites (:904/:907/:914), the 1 Hz OLED block, processSensors' BMP280/GPS/OLED paths
  - src/debug_utils.cpp — the software-latch watchdog (:213-235, :351) — the documented fact that no project code configures a hardware WDT
  - platformio.ini — the esp32-s3-balloon env (build_flags, framework/platform versions that pin the IDF behavior)
  - .planning/debug/d1-crash-regression-push-start.md — §1-§6 (the session-7 decode + §6.5's brief this task executes; §1.4's spinlock wedge table is the one-mechanism counterpart)
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-10 gap block (the missing list this task discharges)
  </read_first>
  <action>
  Execute the §6.5 brief and write §7 (SEVEN — SESSION-8 DEBUG ROUND #2) into the debug doc, following the house evidence convention (file:line quotes, honest limits). Six subsections:
  1. WDT TOPOLOGY OF THE DEPLOYED BUILD. Read the build's config artifacts and the framework sources for the exact versions platformio.ini pins. Answer: TG0WDT_SYS_RST maps to which watchdog in this build (interrupt watchdog vs task watchdog); its configured period (ms); its panic-vs-silent reset behavior — and with that, why session 8 reset with NO panic output while session 7 printed an INT_WDT CPU1 panic (the §1.5 silent-reset class vs the panic class — name the config knob that distinguishes them). Reconcile the tick_hook (int_wdt.c:111) decode against the deployed source: what the function does (feed/check path), and what its appearance as Saved PC implies about where the CPU was when the reset stage fired.
  2. POST-CHUNK EXECUTION WINDOW. The :1227 send log prints after transmit returns — so the fatal stretch is the REMAINDER of that loop pass plus the next pass. From ImageTx().process()'s early-return-after-one-transmit structure, the loop order (main_balloon.cpp:253-330), and the millis brackets ([BCN] seq=46 :1213 → next beacon due ~+5 s; arm :1220-1224; send :1227), enumerate every phase that plausibly executed between the send and the reset: the loop tail (legacy gates, 1 Hz OLED I2C refresh), the next pass's updateSystemState/processSensors (GPS UART1, BMP280 Wire, OLED), processPowerManagement, and the beacon transmit if its 5 s cadence landed in the window. State which candidates the timestamps favor.
  3. CANDIDATE-BLOCK ARITHMETIC. For each candidate in the window: worst-case duration vs the WDT period from subsection 1, and the three decisive properties — masks interrupts? holds a kernel lock across a blocking wait? suspends the flash cache? The minimum candidate set: serial->flush() inside E32LoRa::transmit (217 B at 9600 baud ≈ 226 ms — read the deployed HardwareSerial/uart flush implementation and say whether it blocks on a FreeRTOS event (yields) or spins); the delay(10)-polled AUX waits (confirm delay() yields — a single wait up to 5000 ms is then a yielding wait, not a starver — unless the WDT in question is fed from the tick ISR and something else blocks the tick); the 1 Hz OLED full-frame I2C update (frame size over the Wire clock — compute the ms; check the deployed Wire implementation's lock behavior across the transaction, and note it shares the bus with BMP280); BMP280/GPS reads; sendTelemetryBeacon's own E32 transmit; the PSRAM slice memcpy; Serial.printf. Disposition each: ELIMINATED (with the property that eliminates it) or SUSPECT (with the property that keeps it alive).
  4. ONE MECHANISM OR TWO. Reconcile with §1.4: session-7 crash 1's Core-0 dump (SysTickIsrHandler → xTaskIncrementTick → spinlock_acquire compare-and-set spin) and session-8's tick_hook Saved PC are the same tick-ISR chain. Test the single-mechanism hypothesis — a kernel portMUX holder stalled past the WDT period wedges the tick ISR → WDT reset; session-7's three-subsystem corruption (§1.6) a co-effect of the same stall or a genuinely separate fault. State the verdict and the discriminating evidence. Also reconcile why session 6 ran the same sustained-service pattern clean (what its logs show about window-service cadence vs session 8's — e.g. the 01-18 quiet gate's effect on base request cadence, the SVGA-class 137-chunk full's longer service tail).
  5. RANKED ROOT CAUSE. One ranked cause with evidence for/against and the honest limit (the 01-25 §3 convention — elimination + blast-radius reasoning is acceptable ONLY when the corrupting/blocking site itself cannot be directly observed; say so explicitly if that is the case).
  6. NAMED FIX LEVER + BENCH DISCRIMINATORS. The lever Task 2 dispositions (one or more, each tied to the ranked cause — e.g. a blocking-call restructure at the named site, a lock-scoping fix, a WDT-config change in the balloon env's build_flags, a service-pacing bound — whatever the evidence names) and the discriminator log line(s) the 01-29 bench greps to prove the lever engaged (one named, latch-guarded line in the fixed path, the [MEM]/busy-hold naming convention).
  Record every framework-source and config claim with its file path so the checker can re-derive it.
  </action>
  <verify>
  <automated>grep -q "^## 7" .planning/debug/d1-crash-regression-push-start.md && grep -qi "one mechanism\|two faults\|two mechanisms" .planning/debug/d1-crash-regression-push-start.md && grep -qi "tick_hook" .planning/debug/d1-crash-regression-push-start.md && grep -c "flush()" .planning/debug/d1-crash-regression-push-start.md | grep -qv "^0$"</automated>
  </verify>
  <acceptance_criteria>
  - Debug doc §7 exists with all six subsections; every session-8 citation in it is locatable in balloon2.log/base2.log by line number
  - The WDT determination cites the build's config artifacts and the deployed framework source paths; the silent-vs-panic question is answered by a named config behavior, not hand-waved
  - Every candidate in the minimum set carries duration arithmetic and the three properties, with an ELIMINATED-or-SUSPECT disposition
  - The one-mechanism-or-two verdict is stated with the discriminating evidence; the session-6-clean reconciliation is present
  - One root cause is ranked with evidence for/against and the honest limit; the fix lever and at least one named discriminator line are specified for Task 2
  </acceptance_criteria>
  <done>§7 lands in the debug doc: the deployed build's watchdog is identified with its period and reset behavior, the crash window is reconstructed with every phase dispositioned by duration and lock/interrupt/cache properties, session-7 and session-8 are reconciled into one mechanism or two with evidence, and one root cause is ranked with a named lever and bench discriminator.</done>
  <reversibility rating="reversible">Documentation-only audit record; no code or config changes in this task.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fix-shape disposition checkpoint — select the round-#12 lever from Task 1's ranked evidence</name>
  <files>.planning/debug/d1-crash-regression-push-start.md</files>
  <read_first>
  - .planning/debug/d1-crash-regression-push-start.md — §7 (Task 1's ranked root cause, the named lever candidates, and the bench discriminators); §3 and §5 (the 01-25 fix-shape precedent: what a checkpoint record looks like)
  - .planning/phases/01-command-protocol-control/01-25-SUMMARY.md — the auto-advance convention provenance (the THIRD pending operator confirmation joins this round's as the fourth)
  </read_first>
  <what-built>
  Task 1's §7 audit: the WDT topology of the deployed build, the reconstructed post-chunk crash window, dispositioned candidates, the one-mechanism-or-two verdict, and the ranked root cause with its named lever options.
  </what-built>
  <how-to-verify>
  Decide the fix shape from the evidence, then record the selection in a "FIX SHAPE SELECTED (round #12 Task 2 checkpoint record)" subsection closing §7 (the §5 convention):
  - Option (a): implement the evidence-named lever — appropriate when §7 ranks a cause whose blocking/locking site is named and the lever provably removes the mechanism (e.g. breaking a blocking stretch past the WDT period, fixing a lock scope, or the config-level bound the evidence points at).
  - Option (b): instrument-first — appropriate when §7 cannot rank a unique site: ship only the discriminating diagnostics (bounded, [MEM]-style, with a named removal condition) and let 01-29's bench discriminate. Never a blind fix.
  - Option (a)+(b) combined is legitimate when the lever is named AND the mechanism confirmation needs a discriminator (the 01-25 shape).
  Under workflow.auto_advance=true the house convention (01-23/01-25/01-26) auto-selects the evidence-supported option and records the selection + provenance as pending end-of-phase operator confirmation, joining the three existing pending confirmations.
  </how-to-verify>
  <acceptance_criteria>
  - The checkpoint record exists in the debug doc, naming the selected option, the evidence that selected it, and (if auto-advanced) the provenance line per the 01-23/01-25/01-26 convention
  - The selected lever maps to §7's ranked cause — a selection §7's evidence does not support is a failure of this task, not a preference
  </acceptance_criteria>
  <resume-signal>Type "approved" with the selected option (and the operator's own reasoning if overriding the evidence-ranked one), or route back with what the evidence fails to support.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Implement the selected fix + bench discriminators; builds + harness; ledgers honestly extended (no flips)</name>
  <files>src/e32_lora.cpp, include/e32_lora.h, src/image_tx_manager.cpp, platformio.ini, .planning/WINDOWS.md, .planning/phases/01-command-protocol-control/01-UAT.md</files>
  <read_first>
  - .planning/debug/d1-crash-regression-push-start.md — §7 (the ranked cause + the Task 2 selection + the named discriminator lines this task ships)
  - The fix-site files §7 names (of the candidates in files_modified — read the exact functions §7 fingers, plus their headers)
  - src/command_sender.cpp — the protected quiet-gate surfaces (channelQuietForTx :575-579 region, hold max) this task must not touch
  - scripts/verify_protocol_roundtrip.mjs — the wire tripwire that must exit 0 unchanged
  - .planning/WINDOWS.md — entry 15 (extend the reason; stay open; counts stay 2/17/19)
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-10 (root_cause extension + the missing-list item this round completes)
  </read_first>
  <action>
  Implement the Task 2 selection exactly as §7 specifies it:
  1. The fix at the §7-named site(s), with a code comment citing G-01-10 and the §7 subsection that ranked the mechanism (the house convention — every prior fix cites its gap and its evidence).
  2. The §7-named discriminator log line(s) in the fixed path — latch-guarded (once per episode, the reannounceHoldLogged convention), named so 01-29 can grep it, and honest (it reports what happened, never asserts health).
  3. The [MEM] instrumentation and the round-#10 discriminator lines untouched — their removal conditions are unchanged.
  4. Protected surfaces byte-identical: no wire-format change (the harness is the proof), no quiet-gate constant change (CMD_TX_CHANNEL_QUIET_MS stays 750 in include/command_sender.h), no D-05/D-07 pacing change, no command-path semantic change — unless §7's evidence explicitly named one of them as the root cause (then the change is exactly what §7 specifies and the SUMMARY says so).
  5. Verify: pio run -e esp32-s3-balloon -e esp32-s3-basestation (2/2 SUCCESS) and node scripts/verify_protocol_roundtrip.mjs (exit 0) on the fix commit.
  6. Ledgers: extend WINDOWS entry 15's reason with the round-#12 root-cause determination (entry stays open; counts unchanged) and 01-UAT.md G-01-10's root_cause + missing list (status stays open). Both ledger updates quote §7's verdict; neither claims the crash fixed at code level.
  7. Commit fix(01-28): D1 debug round #2 — <the §7-named lever>, citing the debug doc.
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation 2>&1 | grep -c "SUCCESS" | grep -qv "^0$" && node scripts/verify_protocol_roundtrip.mjs && grep -q "CMD_TX_CHANNEL_QUIET_MS = 750" include/command_sender.h && grep -q "open_count: 2" .planning/WINDOWS.md && grep -qi "round-#12\|round #12" .planning/WINDOWS.md</automated>
  </verify>
  <acceptance_criteria>
  - Both firmware targets build SUCCESS and the harness exits 0 on the fix commit
  - The fix landed at the §7-named site with a G-01-10-citing comment; the §7-named discriminator line exists in source (grep finds it verbatim)
  - CMD_TX_CHANNEL_QUIET_MS is still 750; the harness (wire format) unchanged; the [MEM] lines and round-#10 discriminator lines still present in src/image_tx_manager.cpp
  - WINDOWS entry 15's reason carries the round-#12 determination and the entry is still open (front-matter counts still 2/17/19); 01-UAT.md G-01-10 still open with the extended root_cause
  - The SUMMARY makes NO bench claims — the round's hardware acceptance belongs to 01-29
  </acceptance_criteria>
  <done>The round-#12 fix is wired at the evidence-named site with its discriminator shipped, both targets and the harness are green, and the ledgers carry the audit honestly open pending the 01-29 hardware session.</done>
  <reversibility rating="costly">The fix touches the radio-transmit/window-service path at the heart of every transfer; a wrong lever here would be discovered only at the bench — hence the Task 2 disposition gate before this task.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crash evidence -> root cause | Retained consoles + the deployed ELF are the only ground truth; framework sources/config are read-only reference material — no untrusted input enters the firmware in this round |
| fix -> radio path | The fix modifies the transmit/window-service path every future transfer depends on; the wire harness + protected-surface greps are the regression bound |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-28-01 | Tampering | evidence integrity | medium | mitigate | §7 claims carry file:line quotes and framework-source paths the checker can re-derive; the ELF SHA c53635e181 is re-confirmed before any decode is trusted (the §6.1 convention) |
| T-01-28-02 | DoS | a speculative fix masking the crash | high | mitigate | Task 2's evidence-gated disposition + the prohibition on unevidenced levers; the discriminator line makes masking visible at 01-29 |
| T-01-28-03 | DoS | fix regresses the wire/quiet-gate surfaces | high | mitigate | Protected-surface negative greps + harness exit 0 as the task's automated gate; the 01-25 prohibition carried verbatim |
| T-01-28-04 | Safety | bench hardware | low | accept | No hardware work in this plan (code + docs only); the bench rig risk stays with 01-29's protocol |

## Artifacts this phase produces

- Debug doc §7 (audit) + the Task 2 checkpoint record — .planning/debug/d1-crash-regression-push-start.md
- The fix: modified functions at the §7-named site(s) within src/e32_lora.cpp / include/e32_lora.h / src/image_tx_manager.cpp / platformio.ini (the touched subset named in the SUMMARY) — new named discriminator log line(s) in the fixed path
- WINDOWS entry 15 reason extension; 01-UAT.md G-01-10 root_cause extension + missing-list update
- 01-28-SUMMARY.md
- No new modules, no env vars, no wire-format changes

<verification>
- §7 complete with the six subsections and every claim re-derivable (config path or file:line quoted)
- Fix commit builds 2/2 and the harness exits 0; protected-surface greps clean
- WINDOWS 15 / G-01-10 open with the round-#12 determination recorded — zero code-level flips
</verification>

<success_criteria>
- The WDT-starvation axis is converted from a named hypothesis into a determined topology + reconstructed window + dispositioned candidates + a ranked root cause (or an honest instrument-first record when the mechanism cannot be uniquely confirmed pre-bench)
- The fix (or diagnostics, per the Task 2 selection) is wired green with its 01-29 discriminator named
- Every riding truth's gate is now the single 01-29 bench session
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-28-SUMMARY.md` when done
</output>
