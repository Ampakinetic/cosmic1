---
phase: 01-command-protocol-control
plan: 23
type: execute
wave: 15
depends_on: []
files_modified:
  - .planning/WINDOWS.md
  - src/sd_storage.cpp
  - src/command_handler.cpp
  - include/command_handler.h
  - src/command_sender.cpp
  - include/command_sender.h
  - include/command_protocol.h
  - src/main_basestation.cpp
  - src/main_balloon.cpp
autonomous: false
gap_closure: true
requirements: [CTRL-06, PRI-02, IMG-05, IMG-06, WEB-05]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low   # calibration store has 0 samples (factor 1, not applied); ledger routing + one blocking decision + five small review-shaped fixes (mirrors 01-02 sizing)

must_haves:
  truths:
    - statement: "Every confirmed round-#9 finding is routed: WINDOWS.md carries an entry for each of WR-01..WR-05 (table AND JSON copy, front-matter counts reconciled) — no confirmed review finding is unrouted at phase completion (the round-#7 house convention, 01-VERIFICATION.md gap 4)"
      verification: explicit
    - statement: "WR-05's entry stands SEPARATE from entry 8 (CR-04 fixed): the dead emergency camera-disable finding is its own ledger row with its own disposition — the camera-down story does not silently absorb it"
      verification: explicit
    - statement: "Each finding's disposition is DECIDED, not defaulted: fixed this round (code + builds 2/2 + harness, with unexercised-at-bench recorded by name where the rig cannot stage it — the established WIRED-unexercised convention) or waived with a recorded reason; the decision is the operator's at the Task 2 checkpoint"
      verification: explicit
    - statement: "WR-01 fix (if approved): finalizeImage computes storedToSd only from the transfer's OWN kind handle — the ownership guard (*handleId == meta.imageId) ANDs the persistedBytes check — so a slot-pressure-evicted transfer's sidecar can never claim storedToSd from a newer image's counter"
      verification: explicit
    - statement: "WR-02 fix (if approved): a second complete command frame arriving in the same drain before the first executes is refused with a named drop log (never a silent overwrite); D-05 ACK timeouts, D-07 backoff, the 01-15 BUSY-deferral scoping, and the 01-19 dispatch gate are untouched"
      verification: explicit
    - statement: "review-WR-03 fix (if approved): both length-driven framers (CommandSender and CommandHandler processIncomingByte) reset receive state when an in-packet inter-byte gap exceeds CMD_FRAME_INTERBYTE_MS 200 — a truncated frame can no longer wedge the parser and destroy subsequent frames; the 01-18 quiet-gate latch in command_sender.cpp is untouched (the latch keys on completed frame types, not framer buffer state)"
      verification: explicit
    - statement: "WR-04 fix (if approved): jsonEscape emits strictly RFC-8259-valid JSON for any 1-32 byte SSID — quote and backslash escaped, control bytes below 0x20 emitted as \\u00XX, bytes 0x80 and above dropped — so /api/state can never freeze the dashboard poll"
      verification: explicit
    - statement: "WR-05 fix (if approved): the CRITICAL battery branch in processPowerManagement disables the camera exactly as the LOW branch does (same enableCamera(false) + appState.cameraActive + named SYS_INFO shape), inside the existing !isEmergencyActive() guard — critical battery no longer keeps the biggest non-radio draw running"
      verification: explicit
    - statement: "Both firmware targets build green and the wire-format harness passes (any approved-fix subset)"
      verification: explicit
  artifacts:
    - ".planning/WINDOWS.md — entries 10-14 (WR-01..WR-05) with confirmed-finding descriptions, dispositions, and reconciled counts"
    - "src/sd_storage.cpp — the storedToSd ownership guard (one line)"
    - "src/command_handler.cpp — hasCommand-guarded frame acceptance + drop log; inter-byte resync in the command framer"
    - "src/command_sender.cpp + include/command_sender.h — inter-byte resync in the response framer (lastFrameByteMs member)"
    - "include/command_handler.h — CommandHandler lastFrameByteMs member"
    - "include/command_protocol.h — CMD_FRAME_INTERBYTE_MS constant"
    - "src/main_basestation.cpp — control-byte-safe jsonEscape"
    - "src/main_balloon.cpp — CRITICAL-branch camera disable with its named log"
  key_links:
    - "review findings <-> ledger: each of the five confirmed defects maps to exactly one new WINDOWS entry; none rides an existing fixed entry (the WR-05/entry-8 separation is the named trap)"
    - "WR-02/review-WR-03 <-> this round's bench: both are command-path robustness classes whose fixes ride the 01-24 reflash — their discriminator lines ('second command frame dropped', parser recovery) are watched at bench but NOT required for their flip (code + builds + harness evidence class, unexercised recorded by name)"
    - "WR-04 <-> /api/state <-> dashboard poll: the fix is the only change to jsonEscape's contract; the wifi-block ssid field (D-26/T-03-12) keeps escaping but never leaks the password"
  prohibitions:
    - statement: "MUST NOT waive a finding without a recorded reason in its ledger entry — an unreasoned waiver is an undecided hole (the exact failure mode the round-#7 convention exists to close)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT expand the fixes beyond the review's named shapes — no NACK_BUSY protocol addition for WR-02 (the 01-15 deferral stays scoped to IMAGE_WINDOW_REQUEST), no framer redesign for review-WR-03, no SSID policy change for WR-04"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT fabricate state anywhere (recalled project prohibition, UAT Test 5 tier): no sidecar field, JSON field, or console verdict may claim what did not happen — WR-01 and WR-04 are precisely fabrication-class defects"
      status: flagged
      verification: backstop
---

<objective>
Close 01-VERIFICATION.md gap 4: route review round #9's five confirmed warnings (WR-01..WR-05, 7342b46) into the WINDOWS ledger, obtain the operator's per-finding disposition (fix-now vs waive-with-reason), and implement the approved fixes — the two command-path findings (WR-02 drop, review-WR-03 framer resync) riding this round's bench as natural companions to the G-01-7 residual round.

Purpose: the verifier confirmed all five warnings at HEAD source and none is routed anywhere — per the project's own convention (round #7 precedent) unrouted confirmed findings are a verification gap that blocks phase completion. The five fixes are small, review-shaped, and three of them (WR-02, review-WR-03, WR-05) are named by the verification as companions to this round; WR-05 must additionally not silently ride entry 8's fixed status. The disposition is explicitly a human decision point (01-VERIFICATION.md Human Verification #5): acceptance of robustness-edge-case risk is a maintainer judgment.

Output: WINDOWS entries 10-14 with decided dispositions; the approved fixes landed green on both targets with the harness passing; ready for the 01-24 bench reflash.

Spec-less probe fallback: recorded visible skip — phase_req_ids null this run, no probe predicates generated (standard goal-backward verification, the established determination).
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-REVIEW.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/WINDOWS.md

Binding evidence and constraints:
- The five confirmed findings with fix shapes are in 01-REVIEW.md (Warnings section, WR-01 :75, WR-02 :87, review-WR-03 :105, WR-04 :119, WR-05 :144) — the review supplies concrete fix code for each; the verifier re-derived all five at HEAD (01-VERIFICATION.md gap 4 reason block carries the exact source lines).
- Disambiguation (the verification's own note): review-WR-03 (framer inter-byte timeout) is a DIFFERENT finding from 01-19's WR-03 (auto-capture baseline) — ledger entry text must name it as the framer finding to avoid collision with the WR-03 vocabulary used by WINDOWS entry history.
- Evidence-class convention (entries 8/9 precedent): a fix the rig cannot stage flips on code + builds + harness with the unstageable/unexercised scenario recorded BY NAME — none of the five requires a fabricated bench moment.
- Scope guards: the 01-15 BUSY-deferral is scoped to IMAGE_WINDOW_REQUEST only (every other command keeps FAILED-on-BUSY); the 01-18 quiet gate keys its 0x13 latch on completed frame types at frame-completion time (command_sender.cpp :396-403) — the framer inter-byte reset must not touch it; the 01-19 dispatch gate (commandRequiresCamera) is out of scope.
- House convention: no VALIDATION.md exists for this phase (user-accepted at the Nyquist gate, same as all 20 prior plans).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route WR-01..WR-05 as WINDOWS entries 10-14</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
  - .planning/WINDOWS.md — the table + JSON copy structure, front-matter count fields, and entries 8/9 (the round-#8 review-routing precedent whose format this round mirrors)
  - .planning/phases/01-command-protocol-control/01-REVIEW.md — Warnings section (each finding's file/line/issue/fix)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — gap 4 (the routing requirement and the WR-05/entry-8 separation)
  </read_first>
  <action>
  Add five entries to .planning/WINDOWS.md — ids 10-14, phase 01, kind unmet-truth, status open, recorded_at now:
  - id 10 / WR-01: file src/sd_storage.cpp line 313 — finalizeImage reads *persistedBytes unconditionally while the kind handle resets only when *handleId == meta.imageId (:296), so a slot-pressure-evicted transfer's sidecar (and /gallery detail) can claim storedToSd from the newer image's counter — fabricated stored-to-SD state; found by review round #9 (7342b46), confirmed at source by re-verification #8.
  - id 11 / WR-02: file src/command_handler.cpp line 872 — a second complete command frame in the same 100 ms drain overwrites pendingCommand before execution with no NACK; the base recovers via timeout+retry at 2-15 s cost and re-executes on retry (double-triggered captures); found by review round #9, confirmed at source.
  - id 12 / review-WR-03 (name it 'framer inter-byte timeout' in the description to avoid collision with the 01-19 WR-03 baseline vocabulary): files src/command_sender.cpp + src/command_handler.cpp — zero inter-byte resync timeout in either length-driven framer; a truncated frame (or corrupted bodyLen passing the bound check) wedges the parser mid-frame and destroys subsequent frames until accumulated bytes force a CRC failure; found by review round #9, confirmed at source.
  - id 13 / WR-04: file src/main_basestation.cpp line 3134 — jsonEscape escapes only quote and backslash; a station SSID containing a control byte makes /api/state invalid JSON (RFC 8259), JSON.parse throws, and the dashboard poll freezes for as long as the base is joined to that network; found by review round #9, confirmed at source.
  - id 14 / WR-05: file src/main_balloon.cpp line 1104 — the emergency camera-disable is unreachable (onSystemEvent dispatch body commented out, never registered) and the CRITICAL battery branch never disables the camera (only LOW does), so at critical battery the balloon enters emergency mode while the camera keeps running — STANDS SEPARATE from entry 8 (CR-04 scoped gate, fixed): entry 8 made camera-down safe to serve through; this finding is about reaching camera-down at all; found by review round #9, confirmed at source.
  Update BOTH the markdown table and the JSON copy; bump open_count and total_count in the front matter so counts equal the actual row statuses. Commit docs(01): route review round #9 findings WR-01..WR-05 (WINDOWS 10-14).
  </action>
  <verify>
  <automated>grep -q '"id": 10' .planning/WINDOWS.md && grep -q '"id": 11' .planning/WINDOWS.md && grep -q '"id": 12' .planning/WINDOWS.md && grep -q '"id": 13' .planning/WINDOWS.md && grep -q '"id": 14' .planning/WINDOWS.md && grep -c "review round #9" .planning/WINDOWS.md | grep -qv "^0$"</automated>
  </verify>
  <acceptance_criteria>
  - Entries 10-14 exist in BOTH the markdown table and the JSON copy with matching ids, files, lines, descriptions, and status open
  - Entry 12's description names the framer inter-byte timeout distinctly from the 01-19 WR-03 baseline vocabulary
  - Entry 14's description records the separation from entry 8 explicitly
  - Front matter open_count and total_count equal the actual row statuses (5 more open than before the task)
  - All five descriptions cite review round #9 (7342b46) and re-verification #8 confirmation
  </acceptance_criteria>
  <done>Every confirmed round-#9 finding lives in the ledger — the unrouted-findings gap's routing half is closed unconditionally, before any disposition decision.</done>
  <reversibility rating="reversible">Ledger documentation only.</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Disposition decision — fix-now vs waive-with-reason per finding (operator decision point)</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
  - .planning/WINDOWS.md — entries 10-14 as routed by Task 1
  - .planning/phases/01-command-protocol-control/01-REVIEW.md — the five Warnings with their fix shapes and risk notes
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — Human Verification #5 (why this is a maintainer judgment)
  </read_first>
  <decision>
  Per finding (WR-01, WR-02, review-WR-03, WR-04, WR-05) choose a disposition: fix this round, or waive-with-reason (or defer, leaving the entry open). Mixed per-finding dispositions are fine. The plan's recommendation is option-a (fix all five).
  </decision>
  <context>
  01-VERIFICATION.md's Human Verification #5 makes this explicitly a maintainer judgment: the verifier confirmed all five defects are real and unrouted, not whether they must be fixed in this milestone. None blocks the phase goal (all five are edge-case/robustness/dead-code classes outside the 5 SCs' exercised paths). Whatever is not fixed must still carry a decided disposition — the round-#7 house convention treats an unrouted-or-undecided confirmed finding as a verification gap, and Task 1's routing alone already meets the gap's minimum bar.
  </context>
  <options>
    <option id="option-a">
      <name>Fix all five this round (recommended)</name>
      <pros>Each fix is small, review-shaped, and low-risk (the review supplies concrete fix shapes); WR-02/review-WR-03/WR-05 are the verification's named companions to this round; WR-01/WR-04 close fabrication-class truths before the security gate; all five ride the 01-24 reflash and flip on code + builds + harness with unexercised-at-bench recorded by name</pros>
      <cons>Adds five-file firmware-diff surface to a round whose primary target is the G-01-7 bench verdict — any regression risk lands on the same bench session</cons>
    </option>
    <option id="option-b">
      <name>Fix the command-path companions only (WR-02, review-WR-03, WR-05)</name>
      <pros>Minimal scope exactly matching the verification's companion naming; the camera-down and command-robustness stories complete</pros>
      <cons>Leaves two state-truth defects open into the security gate (WR-01 sidecar storedToSd, WR-04 control-byte SSID freezing the dashboard poll) — they stay as open WINDOWS entries blocking /gsd-ship until later disposition</cons>
    </option>
    <option id="option-c">
      <name>Route only — waive or defer all five with reasons</name>
      <pros>Zero code risk this round; the routing gap is already closed by Task 1; waives with reasons are a legal terminal ledger state</pros>
      <cons>Five entries ride into phase close as open/waived; the two fabrication-class truths (WR-01/WR-04) remain live defects in the shipped behavior</cons>
    </option>
  </options>
  <resume-signal>Type "approved" with the chosen option and any per-finding overrides (e.g. "option-a" or "option-b, plus fix 04"), or a per-finding list (e.g. "fix 01/02/03/05, waive 04: reason"). Each waived finding MUST carry its reason in the same message.</resume-signal>
  <acceptance_criteria>
  - The resume message records a disposition for each of the five findings (fix, waive+reason, or defer-all)
  - Any waive carries a reason string; any absence of reason routes that finding back to this checkpoint rather than defaulting silently
  </acceptance_criteria>
  <done>Each of the five findings has an explicit, recorded disposition decision made by the operator — no undecided hole remains.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the approved fixes + builds + harness + entry updates</name>
  <files>src/sd_storage.cpp, src/command_handler.cpp, include/command_handler.h, src/command_sender.cpp, include/command_sender.h, include/command_protocol.h, src/main_basestation.cpp, src/main_balloon.cpp, .planning/WINDOWS.md</files>
  <read_first>
  - src/sd_storage.cpp — finalizeImage (:290-315: the fileFor handle/handleId/persistedBytes setup, the handle-close guard at :296, the storedToSd computation)
  - src/command_handler.cpp — processIncomingByte's complete-frame branch (:866-880: the unconditional pendingCommand assignment) and the framer body (the inPacket accumulation path)
  - src/command_sender.cpp + include/command_sender.h — processIncomingByte (the response framer twin) and the member block for lastFrameByteMs; the 01-18 quiet-gate latch at :396-403 (must remain untouched)
  - include/command_protocol.h — the CMD_MAX_PACKET_SIZE limits block where CMD_FRAME_INTERBYTE_MS lands
  - src/main_basestation.cpp — jsonEscape (:3130-3146) and its two /api/state ssid call sites
  - src/main_balloon.cpp — processPowerManagement (:849-868: the CRITICAL and LOW branches) and the dead dispatcher block (:1085-1118)
  - The operator's Task 2 disposition list
  </read_first>
  <action>
  Implement exactly the approved subset (skip any waived finding entirely):
  1. WR-01 (sd_storage.cpp): replace the storedToSd computation line in finalizeImage with the ownership-guarded form — storedToSd is true only when the kind handle's *handleId equals meta.imageId AND *persistedBytes is greater than zero. Keep the handle-close block at :296-299 unchanged.
  2. WR-02 (command_handler.cpp): in processIncomingByte's validated-frame branch, assign pendingCommand only when hasCommand is false; when hasCommand is already true, print the named drop log 'CommandHandler: second command frame dropped - handler busy (sender will retry)' and do NOT overwrite. No NACK, no counter change, no timing change (the base's D-05 timeout + D-07 retry re-deliver the dropped frame as a fresh command).
  3. review-WR-03 (both framers): add a uint32_t lastFrameByteMs member to CommandHandler (include/command_handler.h) and CommandSender (include/command_sender.h); add static constexpr uint32_t CMD_FRAME_INTERBYTE_MS = 200 to include/command_protocol.h beside CMD_MAX_PACKET_SIZE with a rationale comment (at 9600 baud a frame's bytes arrive near-continuously — a 200 ms in-packet gap means the frame is truncated; recovery is reset, the bytes were noise). At the top of BOTH processIncomingByte implementations: when inPacket is latched and millis() - lastFrameByteMs exceeds CMD_FRAME_INTERBYTE_MS (wrap-safe), call resetReceiveState() before processing the new byte; then stamp lastFrameByteMs = millis() on every byte. Do NOT touch the 0x13 quiet-gate latch — it keys on completed frame types at frame-completion time, not on framer buffer state.
  4. WR-04 (main_basestation.cpp): extend jsonEscape to emit strictly valid JSON — escape quote and backslash as today; for bytes below 0x20 emit the six-character \u00XX form via snprintf; pass through bytes below 0x80; DROP bytes 0x80 and above (comment: non-ASCII SSID bytes are dropped rather than emitted as invalid UTF-8). Update the function's header comment to say control bytes are escaped and non-ASCII dropped.
  5. WR-05 (main_balloon.cpp): in processPowerManagement's CRITICAL branch, inside the existing !isEmergencyActive() guard, disable the camera with the LOW branch's exact shape — if appState.cameraActive then Camera().enableCamera(false), appState.cameraActive = false, and a SYS_INFO line 'Camera disabled due to critical power' (distinct from the LOW branch's existing message so the bench can discriminate). Leave the commented-out onSystemEvent dispatcher block untouched (inert; its dead status is recorded in entry 14).
  6. Build both targets (pio run -e esp32-s3-balloon -e esp32-s3-basestation) and run node scripts/verify_protocol_roundtrip.mjs — both green.
  7. Update .planning/WINDOWS.md entries 10-14 per disposition in BOTH copies: fixed entries get status fixed with resolved_by naming this plan's commit and a reason citing code fix + builds 2/2 + harness exit 0 with 'unexercised at bench — the rig cannot stage <the finding's trigger>' recorded by name (two-commands-in-one-drain / truncated RF frame / control-byte SSID / critical battery as each applies); waived entries get status waived with the operator's reason verbatim; any deferred entry stays open. Reconcile the front-matter counts.
  8. Commit fix(01-23): review round #9 companion fixes per operator disposition (WINDOWS 10-14).
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation 2>&1 | tail -5 && node scripts/verify_protocol_roundtrip.mjs && grep -q "storedToSd = (\*handleId == meta.imageId)" src/sd_storage.cpp && grep -q "second command frame dropped" src/command_handler.cpp && grep -q "CMD_FRAME_INTERBYTE_MS" include/command_protocol.h src/command_sender.cpp src/command_handler.cpp && grep -q "u%04X" src/main_basestation.cpp && grep -q "Camera disabled due to critical power" src/main_balloon.cpp && grep -q "channelQuietForTx" src/command_sender.cpp</automated>
  </verify>
  <acceptance_criteria>
  - Every approved fix is source-visible: the storedToSd ownership guard on its own computation line (storedToSd = (*handleId == meta.imageId) && ... — distinct from the pre-existing :296 handle-close guard), the hasCommand-guarded assignment with its drop log, CMD_FRAME_INTERBYTE_MS = 200 consumed by BOTH framers with the lastFrameByteMs members, the \u00XX jsonEscape form, and the CRITICAL-branch camera disable with its distinct message
  - The 01-18 quiet gate is intact (channelQuietForTx present; the latch block unchanged relative to pre-task HEAD)
  - Both builds succeed; the harness exits 0
  - Waived findings have zero source changes (their files show no hunks for their fix sites) and their entries carry the operator's reason verbatim
  - Entries 10-14 statuses in table and JSON agree with the dispositions; front-matter counts reconcile
  </acceptance_criteria>
  <done>The five confirmed findings are decided and their dispositions landed: approved fixes green on both targets with the harness passing, ledger entries carrying their evidence-class reasons, and nothing waived without a reason.</done>
  <reversibility rating="reversible">Small guarded fixes at five named sites; the framer change is constant-gated and the WR-05 change mirrors an existing branch.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link -> command framers | Truncated/corrupted frames are the untrusted input the review-WR-03 fix recovers from |
| RF link -> balloon command drain | Two frames in one drain is the WR-02 untrusted-timing edge |
| LAN client / WiFi STA SSID -> /api/state JSON | Arbitrary 1-32 byte SSIDs cross into operator browsers (WR-04 — the fix IS the mitigation) |
| SD slot-pressure eviction -> sidecar truth | The WR-01 wrong-counter read is an internal-truthfulness boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-23-01 | Tampering | WR-04 JSON injection via control-byte SSID | high | mitigate | The jsonEscape fix itself: control bytes escaped as \\u00XX and non-ASCII dropped — /api/state stays RFC-8259-valid for any SSID, so the dashboard poll cannot be frozen by a crafted network name |
| T-01-23-02 | DoS | review-WR-03 parser wedge after a truncated frame | high | mitigate | Inter-byte resync (CMD_FRAME_INTERBYTE_MS 200) in both framers — a wedged parser self-clears in 200 ms instead of destroying subsequent frames until a forced CRC failure |
| T-01-23-03 | Tampering | WR-02 silent command overwrite | medium | mitigate | hasCommand guard + named drop log; the base's existing D-05/D-07 retry re-delivers the dropped frame — no new protocol surface |
| T-01-23-04 | Information Spoofing | WR-01 fabricated storedToSd | medium | mitigate | Ownership guard ties storedToSd to the transfer's own kind handle — gallery detail can no longer claim SD storage that belongs to another image's counter |
| T-01-23-05 | Safety | WR-05 camera kept on at critical battery | medium | mitigate | CRITICAL-branch disable mirrors the LOW branch; power-path behavior at bench is watched via the distinct SYS_INFO line (unstageable without power emulation — recorded by name in the entry) |
</threat_model>

## Artifacts this phase produces

- WINDOWS entries 10-14 (.planning/WINDOWS.md, both copies)
- CMD_FRAME_INTERBYTE_MS (new constant, include/command_protocol.h)
- CommandHandler.lastFrameByteMs and CommandSender.lastFrameByteMs (new members)
- New named log lines: 'CommandHandler: second command frame dropped - handler busy (sender will retry)' (src/command_handler.cpp) and 'Camera disabled due to critical power' (src/main_balloon.cpp)
- Behavioral changes: storedToSd ownership guard (src/sd_storage.cpp), control-byte-safe jsonEscape (src/main_basestation.cpp)
- .planning/phases/01-command-protocol-control/01-23-SUMMARY.md

<verification>
- Entries 10-14 exist with decided dispositions; counts reconcile; no waive without a reason
- Every approved fix is source-visible and both targets build green with the harness passing
- The 01-18 quiet gate, 01-15 BUSY-deferral scoping, and 01-19 dispatch gate are untouched (diff-scoped)
</verification>

<success_criteria>
- 01-VERIFICATION.md gap 4 is closed: every confirmed round-#9 finding lives in the WINDOWS ledger or a todo with its evidence class recorded, and the human decision the gap requires is captured at the Task 2 checkpoint
- The command-path companions (WR-02, review-WR-03) are on the 01-24 reflash with discriminator lines the bench can watch
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-23-SUMMARY.md` when done
</output>
