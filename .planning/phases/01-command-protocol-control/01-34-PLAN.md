---
phase: 01-command-protocol-control
plan: 34
type: execute
wave: 26
depends_on: ["01-32", "01-33"]
files_modified:
  - .planning/phases/01-command-protocol-control/01-UAT.md
  - .planning/WINDOWS.md
  - .planning/STATE.md
  - .planning/debug/d1-crash-regression-push-start.md
autonomous: false
gap_closure: true
requirements: [CTRL-01, CTRL-02, CTRL-03, CTRL-04, CTRL-06, PRI-02]

estimate:
  tokens: 11000
  raw_tokens: 11000
  tasks: 2
  confidence: low   # calibration store has 0 samples (factor 1, not applied); mirrors 01-27/01-29/01-31 bench-session sizing plus the instrument-reading collection step

must_haves:
  truths:
    - statement: "The [STACK] instrument ANSWERS — the round's designed deliverable (debug doc §10.5 item 1): the session quotes the watermark trajectory (boot baseline, every '[IDLE0] stack watermark ... new low' line, the session minimum, and the value at any crash instant) and the ledgers/§12 disposition it against §11's predicted signatures — a margin collapsing toward 0 words under heavy TX before a canary panic CONFIRMS the labeled stack-capacity hypothesis (the D1 fix becomes raise/relocate the IDLE0 stack, routed next round); a healthy constant margin through the TX-heaviest stretch REFUTES it (the wedge family re-ranks, the A/B hook question rises). The answer is recorded either way — a clean session reads the minimum reached, a crash reads the latch through the crash instant"
      verification: explicit
    - statement: "The [TWDT] two-reading question is RESOLVED by the live handle-fixed line against §11's config prediction: ESP_OK = IDLE0 genuinely TWDT-subscribed (the §7 premise stands, the sessions-8/9 TG0WDT attribution survives, the session-10 NOT_FOUND was the wrong-handle bug) vs ESP_ERR_NOT_FOUND = IDLE0 genuinely unsubscribed (§7's premise falsified, the attribution re-opened); the [IDLE0] registered= line reads 1 at both boots proving the printf fix; whichever reading lands is quoted and recorded in §12 + the ledgers"
      verification: explicit
    - statement: "The [I2C] write-path coverage answer lands: a write-failure episode fires the '[I2C] write-path failure ... probe 0x76 -> ...' line with its verdict (ACK/NACK/TIMEOUT discriminating §9.4's readings), or the recorded swallowing finding stands as the coverage answer with no line possible — either way §10.3's coverage gap is closed on evidence, not coverage-silence"
      verification: explicit
    - statement: "D1 crash-regression acceptance on the round-#14 firmware (G-01-10 / WINDOWS 15): the THREE crash-expression survivals judged in one session — (1) the first-post-boot push; (2) SUSTAINED multi-window FULL service (SVGA-class ~137 chunks, >=7 windows, every window served to COMPLETE); (3) the >=3-minute TX-light inter-window lull dwell — with zero crash signatures across the WHOLE console (rst:0x7, rst:0xc, Guru Meditation, stack canary each grep-counted zero; i2cWrite occurrences carried with -B2 -A3 context; mojibake via the corrected raw-byte method — the UTF-8-replacement-char grep returns 0, the twice-documented trap). G-01-10 flips ONLY on the full ladder + a passing series A + instrument readings consistent with §7's topology, all quoted — a single clean stretch is never a flip; any crash appends its dump to the debug doc §12 as the next expression with the [STACK]/[TWDT] readings at the crash instant, and D1 stays open"
      verification: backstop
    - statement: "G-01-7 series-A acceptance on the admission lever (WINDOWS entry 3): 3 back-to-back unspaced captures — 3/3 ACKs (held transmits never reach 'timeout after 3 retries'), 3/3 captures, 6/6 verdicts COMPLETE, zero command timeouts, zero silently absent fulls — with the lever's discriminator lines proving engagement (the 'supersede of receipt-evidenced image %u avoided' lines, the depth-5 overflow labels, the 'queue holds only receipt-evidenced entries' honest-reject lines) or their absence explained (a passing series without the lines means the natural cadence sufficed — recorded as such). A failed series with the lines engaged re-names the residual with every step a quoted line; a failed series with the lines ABSENT is the lever-never-engaged verdict, a distinct finding"
      verification: backstop
    - statement: "WR-03 cadence discriminator (TENTH round riding — judged if the session reaches it): AUTO_CAPTURE_ENABLE at 20 s, one interval capture, one manual CAPTURE_NOW, the next interval capture lands ~20 s AFTER the manual (both 'Captured image ID' timestamps quoted); AUTO_CAPTURE_DISABLE stops all automatic captures; this discharges the SC-5 coincidental-reliance advisory"
      verification: backstop
    - statement: "SC-3 visible-effect pairs (TENTH round riding — judged if the session reaches it): at fixed QVGA, brightness -2 vs +2 judged as a visible pair plus one other class (contrast -2/+2, saturation -2/+2, or quality 10 vs 30), per-pair verdicts recorded in Test 3"
      verification: backstop
    - statement: "QVGA restore clause (SET_RESOLUTION wire 6): the next capture is QVGA-class by wire evidence (~21-chunk class fulls vs CIF ~31+), both kinds COMPLETE, zero FB-OVF at every resolution step (the G-01-8 regression watch)"
      verification: backstop
    - statement: "Ledger honesty: G-01-10/G-01-7 and WINDOWS entries 15/3 flip ONLY on operator-observed console/dashboard evidence with verbatim quotes, line numbers, and counts; a crashed or failed session leaves its gaps open with the residual named, records requirements-completed: [] (the 01-24/01-27/01-29/01-31 failed-session convention), and any crash appends §12; STATE.md names the next routing — the D1 fix informed by the watermark answer if D1 stays open, else /gsd-secure-phase 1 as the remaining phase-close gate"
      verification: explicit
    - "UI-SPEC lift (explicit): before any command the Last Command panel renders the documented 'No commands yet' copy and every settings form renders a complete documented default value — no form is ever shown unfilled"
    - "UI-SPEC lift (explicit): an in-flight command shows the locked 'Sent' state string immediately after POST; the 1s /status poll advances the panel to a terminal state (status-line model, no spinners/skeletons)"
    - "UI-SPEC lift (explicit): Sent/Acked/Failed/Pending counters advance as commands process; 4-column grid on desktop, 2x2 below 480px"
    - "UI-SPEC lift (explicit): each setting is an independent single-field form — a multi-field partial state cannot occur; forms always display a complete value"
    - "UI-SPEC lift (explicit): the status bar collapses 4->2 columns under the 480px media query; counter cells never truncate; Last Command text wraps within card width — no clipping"
    - "UI-SPEC lift (explicit): numeric readouts only — no singular/plural copy; layout identical at 0, 1, and many"
    - "UI-SPEC lift (explicit): .message and label text wrap at 14px/1.5 inside the card; no ellipsis or truncation anywhere in the Phase 1 UI"
    - statement: "UI-SPEC lift (backstop): terminal 'Timeout' / 'Failed (retry N)' with documented error copy renders after retries are exhausted — held-out verification remains the UAT Test 2 hardware proof (already PASS); during this session the link LED stays truthful while commands hold for channel quiet (no red No-link driven by a hold that is working as designed)"
      verification: backstop
  flagged_assumptions:
    - "CTRL-01 / unclassified (probe unresolved — surfaced, never auto-resolved): no SPEC edge predicate exists for CTRL-01; no probe was derivable from the requirement text. Acceptance continues to ride the bench capture-trigger evidence (the D1 ladder + series A above) and the standing requirement-level verification; this round resolves nothing about it by fiat."
    - "CTRL-02 / adjacency (probe unresolved — surfaced, never auto-resolved): 'When two things are exactly equal or just touch, do they merge, collide, or separate?' — no SPEC answer exists; unresolved for the phase's settings-acceptance surface."
    - "CTRL-02 / empty (probe unresolved — surfaced, never auto-resolved): 'What is the result for empty, single-element, or null input?' — no SPEC answer exists; unresolved for the settings surface."
    - "CTRL-02 / ordering (probe unresolved — surfaced, never auto-resolved): 'When elements compare equal, is output order specified and stable?' — no SPEC answer exists; unresolved."
    - "CTRL-03 / unclassified (probe unresolved — surfaced, never auto-resolved): no SPEC edge predicate exists for CTRL-03; acceptance rides the WR-03 cadence discriminator and the standing capture-mode evidence."
    - "CTRL-04 / adjacency (probe unresolved — surfaced, never auto-resolved): boundary-equality semantics of interval timing have no SPEC answer; unresolved (the 20 s cadence discriminator exercises one point on that surface, it does not resolve the probe)."
    - "CTRL-04 / empty (probe unresolved — surfaced, never auto-resolved): zero/single-element interval semantics have no SPEC answer; unresolved."
    - "CTRL-04 / ordering (probe unresolved — surfaced, never auto-resolved): equal-timestamp capture ordering has no SPEC answer; unresolved."
    - "CTRL-06 / unclassified (probe unresolved — surfaced, never auto-resolved): no SPEC edge predicate exists for CTRL-06; acceptance rides the retry-machinery bench evidence (forbidden-terminal census, honest terminalization at exactly 3 retries)."
    - "PRI-02 / idempotency (probe unresolved — surfaced, never auto-resolved): 'What happens if this runs twice on the same input?' has no SPEC answer at the requirement tier; the duplicate-ACK terminal-state guard (01-06) covers the observed instance class, which is evidence, not a resolution of the probe."
    - "PRI-02 / concurrency (probe unresolved — surfaced, never auto-resolved): 'If interrupted or run in parallel, what is guaranteed?' has no SPEC answer at the requirement tier; the single-threaded event loop plus the queue/eviction levers bound the real concurrency surface — recorded as flagged, not resolved."
  artifacts:
    - "Consolidated bench session logs (balloon5.log / base5.log naming request; NOTE the collision hazard — session #5 of 2026-08-25 used those exact names; per the 01-24 convention the operator's retained names are recorded in the ledgers whatever they are, and pre-existing files are never overwritten silently) covering the D1 ladder, the instrument readings, series A on the lever, the WR-03 cadence discriminator, the SC-3 pairs, the QVGA-restore clause, and the dashboard glance"
    - ".planning/phases/01-command-protocol-control/01-UAT.md — G-01-10 flip (the full-ladder + instrument-consistent evidence) or honest extension with §12; G-01-7 flip (6/6 + lever discriminators) or named residual; Test 3/Test 4 notes updated with the riding-clause outcomes"
    - ".planning/WINDOWS.md — entries 15 (D1) and 3 (G-01-7) updated per evidence, both copies (table + JSON), counts reconciled"
    - ".planning/debug/d1-crash-regression-push-start.md — §12 on any new crash (provenance/SHA first, event map, the [STACK]/[TWDT]/[I2C] readings at the crash instant, the hypothesis disposition)"
    - ".planning/STATE.md — round outcome + next routing (the D1 fix round informed by the watermark, or the security gate)"
    - ".planning/phases/01-command-protocol-control/01-34-SUMMARY.md"
  key_links:
    - "The session <-> 01-32's §11 discriminator table: the D1 acceptance reads EXACTLY the lines §11 tabulates ([STACK] baseline/new-lows, registered=1, [TWDT] ForCPU(0), [I2C] write-path) — an instrument that never fired while its trigger ran is explained from §11, never passed off as health"
    - "Series-A verdicts <-> 01-33's lever: the 01-G01-7-LEVER.md discriminator lines distinguish engaged-and-passing from engaged-and-failing from never-engaged; a crash under series A appends §12 and re-opens/extends D1 — never read as a transfer defect (the sessions-8/9/10 lesson)"
    - "The watermark answer <-> the next routing: a CONFIRMED stack-capacity hypothesis routes round #15 to the IDLE0-stack fix; a REFUTED one routes to the wedge-family evidence (the A/B hook build rises in rank); STATE.md carries whichever routing results"
    - "Riding clauses <-> the numbered protocol steps: SC-3/WR-03/QVGA/LOOK are explicit steps 7-10, not ride-alongs — they run if the D1 ladder and series A leave the session alive, and their non-reach is recorded honestly"
  prohibitions:
    - statement: "MUST NOT fabricate image-transfer, command, crash-survival, instrument-reading, or settings-verdict success — closures cite operator-observed finalize/queue/console/dashboard lines with line numbers and counts; zero-count greps are quoted as zero, not assumed"
      status: flagged
      verification: backstop
    - statement: "MUST NOT mark G-01-10 or G-01-7 resolved on code-level evidence — closure requires this session's hardware evidence (the full ladder + series A + instrument readings / 6/6 verdicts + lever discriminators)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT flash only one board — both boards reflash with the same round-#14 image before any radio traffic (the standing both-boards rule; the 01-09 severance lesson)"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT fix newly observed bench defects inside this plan — route them (WINDOWS ledger / todos / debug doc §12) as every prior round did; this round's code work ended at 01-33"
      status: flagged
      verification: backstop
    - statement: "MUST NOT show protocol or camera state that does not reflect reality (recalled project prohibition, UAT Test 5 tier): no ACK for unexecuted actions, no green link LED while failed, GET_STATUS fields match actual balloon state"
      status: flagged
      verification: backstop
    - statement: "MUST NOT read a crash mid-series as a G-01-7 transfer defect — the re-open path appends the dump to debug doc §12 with the [STACK]/[TWDT] readings at the crash instant and extends D1 (the sessions-8/9/10 convention)"
      status: flagged
      verification: backstop
---

<objective>
Operator bench re-verification session #11 of gap-closure round #14: collect the [STACK]/[TWDT]/[I2C] instrument answers on hardware (the round's designed deliverable — one session CONFIRMs or REFUTEs the labeled IDLE0-stack-capacity hypothesis and resolves the [TWDT] two-reading question), judge the D1 three-expression survival ladder, re-run the G-01-7 series-A acceptance on the landed admission lever (3/3 ACKs, 6/6 COMPLETE, lever discriminators proving engagement), and finally judge the truths that have ridden nine rounds (SC-3 visible-effect pairs, WR-03 cadence discriminator, QVGA restore, the operator-LOOKED dashboard glance) — then flip the gap ledgers on evidence.

Purpose: rounds #11-#13 each ended with the bench answering the round's question and naming the next one; round #14's bench carries the campaign's first forward-predictive instrument ([STACK], whose predicted signatures §11 wrote down BEFORE the session) plus the first lever landed against a bench-NAMED residual (the depth-3 queue-cycling census). A clean session can close both blockers at once; a crash still delivers the watermark answer that routes the D1 fix. Nothing rides on speculation: every acceptance bar quotes §10.5/§11/01-G01-7-LEVER.md.

Output: one consolidated bench session (both consoles logged); G-01-10/G-01-7 flipped or honestly extended in 01-UAT.md; WINDOWS entries 15/3 updated with counts reconciled; §12 on any new crash; Test 3 judged on its final open clauses; Test 4's note carries the WR-03 outcome (discharging the SC-5 advisory); STATE.md records the outcome and the next routing.

Spec-less probe fallback: the round's $COVERAGE report surfaced 11 unresolved spec-less probe rows (CTRL-01/02/03/04/06, PRI-02 across the unclassified/adjacency/empty/ordering/idempotency/concurrency categories) — ALL ELEVEN are authored in must_haves.flagged_assumptions above, unresolved and never auto-resolved (the no-silent-drop equality: 11 surfaced = 11 authored; none dropped). The UI-SPEC lift is fully resolved (7 explicit + 1 backstop, 0 unresolved) and is lifted above. Prohibition recall: kept prohibitions descriptor-less under must_haves.prohibitions (canon: UAT Test 5, the 01-30/01-31/01-32/01-33 prohibition sets, the sessions-8/9/10 crash-attribution lesson).

<assumption_delta_decision>
Same determination as 01-32 (the scan's two signals trace to completed-plan prose, not this round's scope): no-change — a bench evidence session introduces no architectural identity change.
</assumption_delta_decision>
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/debug/d1-crash-regression-push-start.md
@.planning/phases/01-command-protocol-control/01-G01-7-LEVER.md
@.planning/phases/01-command-protocol-control/01-32-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-33-SUMMARY.md
@.planning/WINDOWS.md

Binding decisions and bench conventions:
- House evidence convention: verbatim console quotes with line numbers and counts, both consoles retained (balloon5.log / base5.log naming request, collision-hazard note in artifacts; the 01-24 deviation lesson — operator-retained names are recorded in the ledgers, never renamed).
- The predicted readings table is debug doc §11 (read it before the session): [STACK] baseline + new-low lines and their meaning under each labeled hypothesis; [TWDT] ForCPU(0) expected ESP_OK under §11's config prediction, ESP_ERR_NOT_FOUND would falsify §7's premise; [IDLE0] registered=1; the [I2C] write-path line or the swallowing disposition; B1 [BOOT] per boot; B2 [LOOP]; [MEM].
- Crash-signature greps for the record (whole balloon console): rst:0x7, rst:0xc, Guru Meditation, stack canary — each counted; every i2cWrite occurrence with grep -n -B2 -A3 context; the corrected raw-byte mojibake grep (grep -c $'\xe2\x88\xa9\xe2\x94\x90\xe2\x95\x9c' — the UTF-8-replacement-char grep returns 0, the twice-documented trap).
- Series-A protocol: 3 CAPTURE_NOW back-to-back WITHOUT waiting, legitimate only after the D1 ladder steps pass; quiet-gate holds may precede ACKs; a held command must NEVER reach 'timeout after 3 retries'; every full transferred, re-announced-and-transferred, or drop-logged by name; the 01-G01-7-LEVER.md discriminator lines prove lever engagement.
- SC-3 pairs protocol: brightness -2 vs +2 plus one other class at fixed QVGA; the queue's SET commands ACK; the pair comparison is the operator judgment; thumbnails suffice unless the full is needed (note which).
- WR-03 discriminator protocol: AUTO_CAPTURE_ENABLE at 20 s; one interval capture; one manual CAPTURE_NOW; the NEXT interval capture lands ~20 s after the manual (quote both timestamps); disable and confirm zero further automatic captures.
- Resolution clauses: the sustained-service step may use SVGA (wire 10); CIF (wire 8) as specified already has its clean session-10 run; QVGA restore (wire 6) with wire-evidence sizing + zero FB-OVF is the open clause.
- WR-01 battery watch: no 'Critical battery' lines; beacons batt=valid. WR-02 watch: zero 'health check failed' lines at healthy boots. UAT Test 3 is the 'issue' holder; Tests 1/2/4/5 stand PASS and must not regress.
- The interim spacing discipline (avoid unspaced bursts) applies UNTIL the D1 ladder steps pass — only then is a burst legitimate (unchanged from 01-31).
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Bench session #11 — flash both boards, the instrument readings ([STACK]/[TWDT]/[I2C]), the D1 three-expression ladder, series A on the admission lever, WR-03 cadence discriminator, SC-3 visible-effect pairs, QVGA restore, dashboard LOOK glance</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md</files>
  <read_first>
  - .planning/debug/d1-crash-regression-push-start.md — §10 (the fourth expression this firmware must beat), §11 (the round-#14 discriminator table + predicted readings this session reads)
  - .planning/phases/01-command-protocol-control/01-G01-7-LEVER.md — the lever's discriminator lines and the engagement criteria
  - .planning/phases/01-command-protocol-control/01-32-SUMMARY.md + 01-33-SUMMARY.md — what changed on the balloon, the touched sites, and the new log lines to expect
  - .planning/phases/01-command-protocol-control/01-UAT.md — the G-01-10 and G-01-7 gap blocks, Test 3/Test 4 notes (the riding clauses)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — Gaps Summary + behavior_unverified_items + coincidental_reliance_items
  </read_first>
  <what-built>
  Plans 01-32 + 01-33: the D1 round-#4 instrument package ([STACK] watermark with its §11-predicted signatures, the fixed [TWDT]/[IDLE0]/[I2C] instruments, the A/B guard) and the G-01-7 admission lever (queue depth + supersede receipt-evidenced protection with its discriminator lines) — built green on both targets, harness exit 0, no bench claims made.
  </what-built>
  <how-to-verify>
  1. PRE-FLIGHT: pio run -e esp32-s3-balloon -e esp32-s3-basestation (2 succeeded) and node scripts/verify_protocol_roundtrip.mjs (exit 0) on the round's final commit. Record sha256sum .pio/build/esp32-s3-balloon/firmware.elf (the round-#14 build SHA — needed for any addr2line decode; compare to the 01-32 record). Flash BOTH boards with that exact firmware (balloon behavior changed twice over; base reflash is the standing both-boards rule). Start both serial consoles logging (balloon5.log / base5.log requested; record the operator's actual names per the 01-24 convention — never overwrite session #5's 2026-08-25 files silently).
  2. INSTRUMENT BASELINE (boot readings, quoted): [IDLE0] hook cpu0 registered=1 at both boots; [TWDT] idle0 wdt status= (the ForCPU(0) reading — ESP_OK or ESP_ERR_NOT_FOUND, the two-reading question's answer); [BOOT] reset-cause per boot; the first '[IDLE0] stack watermark ... (new low)' line (the boot baseline).
  3. D1 CLASS 1 — FIRST-POST-BOOT PUSH: ONE CAPTURE_NOW as the very first post-boot capture; wait for both finalize lines (thumb + full COMPLETE); zero crash signatures.
  4. D1 CLASS 2 — SUSTAINED MULTI-WINDOW FULL SERVICE: a full serving to completion across MULTIPLE windows — SVGA-class (~137 chunks, >=7 windows, the image-38 pattern; SET_RESOLUTION wire 10 first if needed, restore per step 9). Quote how many windows served and the finalize line.
  5. D1 CLASS 3 — INTER-WINDOW LULL DWELL: after a cleanly completed window, idle the pair TX-light for AT LEAST 3 MINUTES (beacons/sensors/idle GET_STATUSs only); quote the balloon-side lull-pattern lines and the wall-clock span.
  6. SERIES A ON THE LEVER (legitimate only after steps 3-5 pass): 3 CAPTURE_NOW back-to-back WITHOUT waiting. Expected: 3/3 ACKs (holds may precede; never 'timeout after 3 retries'), 3/3 captures, 6/6 finalize lines COMPLETE, every full transferred / re-announced-and-transferred / drop-logged by name; quote the lever discriminator lines ('supersede of receipt-evidenced image %u avoided', any depth-5 overflow label, any 'queue holds only receipt-evidenced entries' reject) — their ABSENCE with a passing series means the natural cadence sufficed (record which).
  7. WR-03 CADENCE DISCRIMINATOR (tenth round riding — judge it): AUTO_CAPTURE_ENABLE at 20 s; one interval capture; one manual CAPTURE_NOW; verify the NEXT interval capture lands ~20 s AFTER the manual (quote both 'Captured image ID' timestamps); AUTO_CAPTURE_DISABLE; zero further automatic captures.
  8. SC-3 VISIBLE-EFFECT PAIRS (tenth round riding — judge it): at fixed QVGA — brightness -2 capture vs +2 capture judged as a pair; one other class (contrast -2/+2, saturation -2/+2, or quality 10 vs 30); record per-pair verdicts and whether thumbnails sufficed.
  9. QVGA RESTORE (wire 6): SET_RESOLUTION 6 — ACK plus a subsequent capture at QVGA-class sizing (~21-chunk class), both kinds COMPLETE, zero FB-OVF. (If step 4 used SVGA, this doubles as the restore confirmation.)
  10. DASHBOARD LOOK GLANCE (operator-LOOKED, not did-not-flag): 'No commands yet' before the first command; 'Sent' immediately after each POST advancing via the poll; counters advance; link LED stays truthful (green while chunks/holds flow); status bar collapses 2x2 under 480px; no truncated/clipped text; gallery detail renders the resLabel/filesize fields.
  11. RECORD everything: the [STACK] trajectory (every new-low line + the session minimum + the value at any crash instant); the [TWDT]/[IDLE0]/[I2C] readings; the crash-signature grep counts across the WHOLE console (rst:0x7, rst:0xc, Guru Meditation, stack canary, i2cWrite with -B2 -A3 context, the corrected raw-byte mojibake count); the round-#10 lines (budget re-arms, class-5 eviction labels, base deadline releases); the round-#12/13 lines ([BOOT]/[LOOP]/[MEM]); the lever lines. If ANY step fails: capture the discriminating lines, leave that gap open — do not force a close; a CRASH appends its dump to the debug doc §12 with the [STACK]/[TWDT] readings at the crash instant.
  </how-to-verify>
  <acceptance_criteria>
  - The instrument readings are quoted: the [STACK] baseline + trajectory + session minimum (and crash-instant value on any crash); [TWDT] ESP_OK or ESP_ERR_NOT_FOUND at both boots; registered=1 at both boots; the [I2C] write-path line or the standing swallowing disposition
  - D1: the first-post-boot push, the sustained multi-window full service, AND the >=3-minute lull dwell all complete with zero crash signatures (grep counts quoted per signature, mojibake via the corrected raw-byte method) — or the crash is recorded per step 11 with §12 appended and D1 extended
  - Series A: 3/3 ACKs, 3/3 captures, 6/6 COMPLETE, zero command timeouts, zero silently absent fulls, lever discriminator lines present (or their absence explained as natural-cadence success)
  - WR-03: both timestamps quoted, disable stops automatic captures; SC-3: per-pair verdicts recorded; QVGA restore: wire-evidence sizing + zero FB-OVF; dashboard: the seven UI-SPEC considerations + LED-truth backstop + gallery detail fields
  - Both console logs retained with every cited quote locatable by line number; the round-#14 ELF SHA recorded
  </acceptance_criteria>
  <resume-signal>Type "approved" with the console excerpts (instrument readings incl. the [STACK] trajectory, crash-signature grep counts incl. the corrected mojibake method, lull-dwell span + lines, sustained-service window count + finalize, series-A verdicts + lever lines, WR-03 timestamps, SC-3 pair judgments, QVGA-restore evidence, dashboard glance verdicts), or describe the failing step with its discriminating log lines (a crash appends §12 and extends D1).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Flip the ledgers on evidence — 01-UAT.md (G-01-10/G-01-7, Test 3/Test 4), WINDOWS entries 15/3, STATE.md routing, debug doc §12 on any new crash</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md, .planning/WINDOWS.md, .planning/STATE.md, .planning/debug/d1-crash-regression-push-start.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-UAT.md — the G-01-10/G-01-7 gap blocks, Test 3/Test 4 notes; the house quote convention from the G-01-8/G-01-9/G-01-11 resolutions and the 01-24/01-27/01-29/01-31 session extensions
  - .planning/WINDOWS.md — entries 15 and 3 (table + JSON copies both update; counts reconcile from 2/17/19)
  - .planning/debug/d1-crash-regression-push-start.md — §10/§11 structure (a new crash appends §12 in the same evidence convention; §11's predicted-signatures table is the disposition reference)
  - .planning/phases/01-command-protocol-control/01-G01-7-LEVER.md — the lever's engagement criteria
  - The operator's Task 1 evidence (the approved resume message + the retained session logs)
  </read_first>
  <action>
  Update the ledgers strictly from the Task 1 evidence:
  1. 01-UAT.md G-01-10: if the FULL ladder passed (first-post-boot push + sustained multi-window service + lull dwell + a passing series A) with zero crash signatures AND the instrument readings are quoted AND consistent with §7's topology ([TWDT] ESP_OK; [STACK] margin intact through the TX-heaviest stretch — or its collapse honestly dispositioned per §11's predicted signatures with the fix routed), set status resolved with resolved_by naming the round-#14 evidence chain, resolved_at, and verified_by quoting the grep counts (zero each, corrected mojibake method), the lull-dwell span, the sustained-service evidence, and the instrument readings. If anything crashed or any reading falsified a §7 premise, keep status open, append §12 (provenance/SHA first, event map, the readings at the crash instant, the hypothesis disposition per §11's table), and extend root_cause with the next expression's discriminating facts.
  2. 01-UAT.md G-01-7: if series A passed 6/6 with the lever discriminator lines present (or their absence explained as natural-cadence success), set status resolved with resolved_by naming the lever chain (01-33's depth + supersede protection on top of 01-21/01-22/01-17/01-18) and verified_by quoting the verdicts, the zero-timeout evidence, and the discriminator lines. If it failed, keep status open and extend root_cause with which verdict the engagement criteria yield (engaged-and-failing: the residual re-named with quoted lines; never-engaged: the lever-composition question routed).
  3. 01-UAT.md Test 3 note: record the D1-survival outcome, the SC-3 per-pair verdicts, the QVGA-restore outcome; flip Test 3 to pass only if every clause it holds is now judged and none failed. Test 4 note: record the WR-03 cadence discriminator outcome (this discharges the SC-5 coincidental-reliance advisory from 01-VERIFICATION.md).
  4. WINDOWS.md: entries 15 and 3 flip on the session evidence or stay open with the residual; update BOTH the markdown table and the JSON copy; reconcile the front-matter counts.
  5. STATE.md: record the round outcome in Current Status; set Next Steps to the routed next action — if D1 stayed open, the D1 fix round informed by the watermark disposition (a confirmed stack-capacity hypothesis routes the IDLE0-stack fix; a refuted one routes the wedge-family evidence and the §11 A/B build); if everything closed, /gsd-secure-phase 1 (Phase 2/3 gates also pending) as the remaining phase-close gate, then /gsd-progress. Note the six pending auto-advanced disposition confirmations (01-23, 01-25, 01-26, 01-28, 01-30, 01-32) remain end-of-phase operator decisions unless addressed here — record any that are confirmed.
  6. On a failed session: record requirements-completed: [] (the 01-24/01-27/01-29/01-31 convention) — no requirement claims ride a failed bench session; the SUMMARY names what rode instead.
  7. Commit the ledger updates: docs(01-34): bench session #11 evidence + ledger flips.
  </action>
  <verify>
  <automated>grep -q "01-34" .planning/STATE.md && grep -qi "secure-phase\|watermark" .planning/STATE.md && grep -c "G-01-10" .planning/phases/01-command-protocol-control/01-UAT.md | grep -qv "^0$" && grep -q '"id": 15' .planning/WINDOWS.md && grep -q '"id": 3' .planning/WINDOWS.md</automated>
  </verify>
  <acceptance_criteria>
  - Every flip cites operator-observed quotes with line numbers/counts from the retained logs; failed steps leave their gap open with the residual named in the discriminator vocabulary
  - WINDOWS.md table and JSON copies agree; front-matter counts equal the actual row statuses
  - The G-01-10 disposition (either way) quotes the instrument readings — the [STACK] trajectory and the [TWDT] line — not merely an absence of resets; the G-01-7 disposition names which engagement verdict the criteria yield
  - Test 3 reflects the D1-survival, SC-3-pair, and QVGA-restore outcomes; Test 4's note carries the WR-03 outcome; Tests 1/2/5 notes untouched absent regression evidence
  - STATE.md names the routed next action (the D1 fix round or the security gate); on a failed session requirements-completed: [] is recorded
  </acceptance_criteria>
  <done>The gap ledgers reflect hardware truth: D1 closed on zero-crash + instrument-consistent evidence or extended with §12's watermark answer routing the fix; G-01-7 closed on series evidence or honestly re-named via the engagement criteria; Test 3 judged on its final clauses; the SC-5 advisory discharged if WR-03 ran; and the next routing recorded.</done>
  <reversibility rating="reversible">Documentation-only ledger updates.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench operator -> firmware flash + consoles | The human performs the flash and observes; logs are retained as evidence (read-only artifacts) |
| RF pair -> bench evidence | The two-radio link under bench conditions produces the verdicts; no untrusted input enters the firmware beyond the CRC16-validated frames covered in prior rounds' threat models |
| instrument readings -> hypothesis disposition | The [STACK]/[TWDT]/[I2C] readings decide a labeled root-cause question; §11's pre-registered predicted signatures keep the disposition honest (the prediction was written before the data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-34-01 | Tampering | evidence integrity | medium | mitigate | House convention enforced: verbatim quotes with line numbers/counts; zero-count greps quoted; the round-#14 ELF SHA recorded before any decode; §11's pre-registered signatures govern the disposition |
| T-01-34-02 | DoS | mixed-firmware flash invalidating the evidence | high | mitigate | Blocking checkpoint step 1 flashes BOTH boards before any traffic; the standing both-boards rule with the 01-09 severance lesson recorded |
| T-01-34-03 | DoS | a crash mid-series misread as a transfer defect | high | mitigate | The re-open prohibition names the §12 path with the [STACK]/[TWDT] readings at the crash instant — the sessions-8/9/10 convention carried verbatim |
| T-01-34-04 | DoS | a shallow ladder masking the crash classes | medium | mitigate | Steps 3-5 set hard bars (multi-window count, the >=3-minute dwell spanning past the session-9 death latency); shorter or busy attempts are recorded as such and the clause stays open |
| T-01-34-05 | Repudiation | lever engagement unprovable after the fact | medium | mitigate | The 01-G01-7-LEVER.md discriminator lines are greppable in the retained console; never-engaged vs engaged-and-failing are distinct recorded verdicts |
| T-01-34-06 | Safety | bench hardware | low | accept | Standard bench rig (USB-powered base, battery balloon at desk); WR-01 battery watch green in all prior sessions; the round changed no power paths |

## Artifacts this phase produces

- balloon5.log / base5.log (or operator-chosen names, recorded) — retained bench console evidence (repo root, prior-session convention; session-#5 name-collision hazard noted)
- Updated gap blocks G-01-10/G-01-7 + Test 3/Test 4 notes (01-UAT.md)
- Updated WINDOWS entries 15 and 3 with reconciled counts (.planning/WINDOWS.md)
- Debug doc §12 (only on a new crash — the re-open evidence append with the instrument readings at the crash instant)
- Round outcome + next-routing record (.planning/STATE.md)
- .planning/phases/01-command-protocol-control/01-34-SUMMARY.md
- No source files, no new symbols, no env vars

<verification>
- Both firmware targets built green and the harness exited 0 immediately before the flash (pre-flight step, on record in the session); the round-#14 ELF SHA recorded
- Every ledger flip traces to a quotable line in the retained logs or carries its explicit evidence-class reason; the instrument readings are quoted either way
- WINDOWS counts reconcile with row statuses; the STATE routing names exactly one next action
</verification>

<success_criteria>
- The round's questions are answered on hardware: the [STACK] watermark confirms or refutes the stack-capacity hypothesis with its trajectory quoted, the [TWDT] two-reading question is resolved, the [I2C] coverage answer lands, the D1 ladder and series A are judged against their bars (or the crash honestly extends D1 with §12), the riding clauses are judged or their non-reach recorded
- With both blockers closed, the WINDOWS ledger reaches zero open entries and the phase's remaining close gate is the security review (/gsd-secure-phase 1); with D1 open, the watermark disposition routes the fix round with no speculation required
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-34-SUMMARY.md` when done
</output>
