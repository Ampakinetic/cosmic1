---
phase: 01-command-protocol-control
plan: 20
type: execute
wave: 14
depends_on: ["01-17", "01-18", "01-19"]
files_modified:
  - .planning/phases/01-command-protocol-control/01-UAT.md
  - .planning/WINDOWS.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [CTRL-01, CTRL-02, CTRL-03, CTRL-04, CTRL-06, PRI-02, IMG-02, IMG-03]

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low   # calibration store has 0 samples (factor 1, not applied); one operator bench checkpoint + evidence-ledger updates (mirrors 01-16)

must_haves:
  truths:
    - statement: "G-01-7 series-A acceptance (the truth that has failed three rounds, now on its narrowest axis): 3 back-to-back unspaced captures on this round's firmware all reach the balloon — 3 ACKs, 3 captures, 6/6 verdicts COMPLETE, zero command timeouts; 'transmit held' lines may appear under the chunk storm and every held command eventually transmits and ACKs (the quiet gate working), never terminalizing while held"
      verification: backstop
    - statement: "G-01-9 defect C acceptance: zero silently absent fulls across the whole session — every enqueued armable full either transfers, or a 're-announced' line names its recovery and the full still completes, or the 'no FULL window armed' drop log names its loss; nothing is silently missing (the image-15 class, balloon5.log:274 sent / never received / nothing named)"
      verification: backstop
    - statement: "SC-3 visible-effect pairs (unjudged for four rounds): at a fixed resolution, brightness -2 vs +2 judged as a visible pair, plus at least one other class (contrast, saturation, or quality) judged as a pair — per-pair judgments recorded; with this clause judged, Test 3's every clause has an operator verdict"
      verification: backstop
    - statement: "WR-03 cadence discriminator: with auto-capture enabled at a known interval, a manual capture resets the schedule — the next interval capture lands one full interval after the MANUAL capture (timestamps quoted), not at the pre-manual schedule"
      verification: backstop
    - statement: "WINDOWS entries 8 (CR-04) and 9 (WR-08) flip per their evidence classes: CR-04 on the code-structure fix + builds + harness with the unstageable camera-down scenario honestly recorded (the defect itself was source-confirmed — the inverse confirmation is equally source-level; do not fabricate a bench scenario the rig cannot stage); WR-08 flipped on same-index retry evidence if any chunk transmit fails at bench, else flipped on the code fix with the unexercised path recorded exactly as prior rounds recorded 'WIRED (unexercised)'"
      verification: backstop
    - "UI-SPEC lift (explicit): before any command the Last Command panel renders the documented 'No commands yet' copy and every settings form renders a complete documented default value — no form is ever shown unfilled"
    - "UI-SPEC lift (explicit): an in-flight command shows the locked 'Sent' state string immediately after POST; the 1s /status poll advances the panel to a terminal state (status-line model, no spinners/skeletons)"
    - "UI-SPEC lift (explicit): Sent/Acked/Failed/Pending counters advance as commands process; 4-column grid on desktop, 2x2 below 480px"
    - "UI-SPEC lift (explicit): each setting is an independent single-field form — a multi-field partial state cannot occur; forms always display a complete value"
    - "UI-SPEC lift (explicit): the status bar collapses 4->2 columns under the 480px media query; counter cells never truncate; Last Command text wraps within card width — no clipping"
    - "UI-SPEC lift (explicit): numeric readouts only — no singular/plural copy; layout identical at 0, 1, and many"
    - "UI-SPEC lift (explicit): .message and label text wrap at 14px/1.5 inside the card; no ellipsis or truncation anywhere in the Phase 1 UI"
    - statement: "UI-SPEC lift (backstop): terminal 'Timeout' / 'Failed (retry N)' with documented error copy renders after retries are exhausted — held-out verification remains the UAT Test 2 hardware proof (already PASS); during this session the link LED stays truthful while commands hold for channel quiet (no red No-link driven by a hold that is working as designed)"
      verification: backstop
    - "Ledger honesty: G-01-7, G-01-9, and WINDOWS entries 3/5/8/9 flip ONLY on operator-observed console/dashboard evidence with verbatim quotes, line numbers, and counts (house convention, with the evidence-class rules above for the unstageable/unexercised paths); any failed series leaves its gap open with the residual mechanism named — never a forced close"
  artifacts:
    - "Consolidated bench session logs (balloon6.log / base6.log naming convention) covering series A, the WR-03 cadence discriminator, the SC-3 pairs, and the settings regression — retained in the repo root as every prior session was"
    - ".planning/phases/01-command-protocol-control/01-UAT.md — G-01-7/G-01-9 status flips (or honest residuals) with verified_by quotes; Test 3 note updated with the SC-3-pair and WR-03 outcomes"
    - ".planning/WINDOWS.md — entries 3, 5, 8, 9 updated (fixed with evidence, or rescoped with the named residual); counts reconciled"
    - ".planning/STATE.md — round outcome recorded; /gsd-secure-phase 1 named as the remaining phase-close gate"
    - ".planning/phases/01-command-protocol-control/01-20-SUMMARY.md"
  key_links:
    - "Behavior changes on BOTH boards this round (balloon re-announce/cursor gating/dispatch scoping; base quiet gate) — flash both together BEFORE any radio traffic; mixed firmware yields misleading evidence even though the wire format is unchanged (the 01-09 severance lesson applied to behavior, not framing)"
    - "Series-A verdicts <-> this round's four mechanisms: a residual failure must be attributed to its mechanism using the new discriminator lines ('transmit held', 're-announced', 'no FULL window armed', 'skipped after', same-index window chunk repeats) before any gap is left open — the diagnostics exist precisely to name the next lever (the balloon-side periodic yield is the named fallback if command survivability still fails)"
    - "UI regression <-> untouched UI files: no UI file is modified this round; any dashboard deviation observed at bench is a regression to route, not a fix to make in this plan"
  prohibitions:
    - statement: "MUST NOT fabricate image-transfer, command, or config success — closures cite operator-observed finalize/queue/console lines, never code-level reasoning (except the two evidence-class rules encoded in must_haves for CR-04/WR-08, which record their unstageable/unexercised status explicitly)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT mark G-01-7 or G-01-9 resolved on code-level evidence alone — closure requires this bench session's series evidence"
      status: flagged
      verification: backstop
    - statement: "MUST NOT flash only one board — both boards' behavior changed this round"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT fix newly observed bench defects inside this plan — route them (WINDOWS ledger / todos) as every prior round did; this plan's code work ended at 01-19"
      status: flagged
      verification: backstop
---

<objective>
Operator bench re-verification of the narrow residual round (plans 01-17/01-18/01-19), carrying the SC-3 visible-effect pairs that have ridden unjudged for four rounds and the WR-03 cadence discriminator — then flipping the gap ledgers on evidence.

Purpose: the series-A acceptance truth ("3 unspaced captures, every kind COMPLETE, zero command timeouts, zero silent full losses") has narrowed each round and now rests on exactly two structural holes this round closed in code: command frames transmitted into an occupied half-duplex channel (the quiet gate now holds them for the stream's natural pause) and the TX-success/air-lost FULL manifest that consumed its one-shot announce (the receipt-informed re-announce now recovers it or names the drop). CR-04 and WR-08 ride the same bench for their ledger flips. The new discriminator lines either prove the closures or name the next lever precisely.

Output: one consolidated bench session (both consoles logged, balloon6.log/base6.log); G-01-7/G-01-9 flipped or honestly rescoped in 01-UAT.md; WINDOWS entries 3/5/8/9 updated; STATE.md records the outcome and the remaining phase-close gate (/gsd-secure-phase 1).

Spec-less probe fallback: recorded visible skip — this phase has no machine-mapped requirement IDs (phase_req_ids null), so no probe predicates were generated this run. (Roadmap Phase 1 is marked Mode: mvp but its Goal line is not in user-story format — standard goal-backward verification applies, the same determination as all prior rounds.)
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/phases/01-command-protocol-control/01-17-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-18-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-19-SUMMARY.md

Binding decisions and bench conventions:
- Known gap, user-accepted at the Nyquist gate this round: no VALIDATION.md exists for this phase (Dimension 8) — accepted by the user exactly as for all 16 prior plans; nyquist_validation remains enabled in .planning/config.json and no config change is part of this round.
- House evidence convention: verbatim console quotes with line numbers and counts, both consoles retained (balloon6.log/base6.log naming), before/after tallies against the session-5 baseline.
- Baselines to beat (session 5, balloon5.log/base5.log): seq=6 lost 4/4 under the image-14/15 chunk storm (queued :55, retries :73/:81/:92, honest timeout :126) — 3 unspaced triggers produced 2 captures; image 15's full silently lost to manifest air loss (balloon5.log:274 sent, zero base receipts, no drop log). Everything else was already green: zero INCOMPLETE verdicts, zero stored-bytes CRC mismatches, genuine QQVGA thumbs at every capture, serialization hold 4x, beacons 484 accepted worst-gap 2.
- New discriminator lines to watch (this round's code): base 'CommandSender: command seq=N transmit held - inbound chunk stream active' and 'held >= 30000 ms; transmitting best-effort'; balloon 'ImageTx: FULL manifest(image N, ...) re-announced (K/3) - no window armed since announce' and 'image N full dropped - no FULL window armed after 3 re-announces'; balloon 'skipped after N failed transmit attempts' (only if a transmit genuinely fails); window chunk lines repeating the same index (the same-index retry working).
- SC-3 pairs: brightness -2 vs +2 plus at least one other class (contrast, saturation, or quality — quality 10 vs 30 changes file size visibly); judge at fixed QVGA; the queue's SET commands ACK; the pair comparison is the operator judgment.
- WR-03 discriminator: AUTO_CAPTURE_ENABLE at 20 s, observe one interval capture, trigger a manual CAPTURE_NOW, then verify the NEXT interval capture lands ~20 s after the manual one (quote both 'Captured image ID' console timestamps) — pre-fix, the interval capture fires at the old schedule, potentially seconds after the manual.
- CR-04/WR-08 flip rules are encoded in must_haves (evidence-class matched: unstageable camera-down scenario recorded honestly for CR-04; unexercised-path recording for WR-08 if zero transmit failures occur — the established 'WIRED (unexercised)' convention).
- UAT Test 3 is the 'issue' holder; Tests 1/2/4/5 stand PASS and must not regress (beacon cadence, retry/TIMEOUT semantics, auto-capture cadence semantics now include the WR-03 baseline change — Test 4's 'exact cadence' expectation is unaffected: interval captures still fire at exactly the commanded interval from the last capture of ANY source).
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Bench session #6 — flash both boards, series A (unspaced), WR-03 cadence discriminator, SC-3 visible-effect pairs, settings regression, dashboard glance</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-7/G-01-9 gap blocks (truths, session-5 baselines, named levers) and Test 3's note (the riding clauses)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — Gaps Summary + behavior_unverified_items (the exact truths this session must exercise)
  - .planning/phases/01-command-protocol-control/01-17-SUMMARY.md, 01-18-SUMMARY.md, 01-19-SUMMARY.md — what changed and the new log lines to expect
  </read_first>
  <what-built>
  Plans 01-17 (receipt-informed FULL-manifest re-announce + WR-08 success-gated cursors/SERVED + WINDOWS routing), 01-18 (base command-survivability quiet gate — held transmits consume nothing, 30 s best-effort bound), 01-19 (CR-04 camera-gate scoping + WR-03 manual-capture baseline) — all built green on both targets, harness green.
  </what-built>
  <how-to-verify>
  1. PRE-FLIGHT: `pio run -e esp32-s3-balloon -e esp32-s3-basestation` (2 succeeded) and `node scripts/verify_protocol_roundtrip.mjs` (exit 0) on the round's final commit. Flash BOTH boards with that exact firmware (behavior changed on both sides; a mixed pair yields misleading evidence). Start both serial consoles logging to balloon6.log / base6.log.
  2. SERIES A (G-01-7 acceptance, third attempt at this truth): from the dashboard, trigger 3 CAPTURE_NOW commands back-to-back WITHOUT waiting. Expected: 3 ACKs (some may show 'transmit held' first — the quiet gate — then transmit and ACK; a held command must NEVER reach 'timeout after 3 retries'); 3 captures; 6/6 finalize lines COMPLETE; every thumb genuine QQVGA (drained-stale-frame discriminator at captures); zero stored-bytes CRC mismatches; retry labels stay '(retry K/3)'; beacons hold cadence (quote any gap wider than session 5's worst of 2 consecutive). If a FULL manifest air-loses, the 're-announced' line must appear and the full still completes (or the drop log names it — never silent).
  3. WR-03 CADENCE DISCRIMINATOR: AUTO_CAPTURE_ENABLE at 20 s; let one interval capture fire; trigger a manual CAPTURE_NOW; then watch the next interval capture land ~20 s AFTER the manual trigger (quote both capture timestamps from the balloon console). Pre-fix, the interval capture would fire at the pre-manual schedule, potentially seconds after the manual capture.
  4. SC-3 VISIBLE-EFFECT PAIRS (fourth round riding — this session must judge them): at fixed QVGA — (a) SET_BRIGHTNESS -2, capture; SET_BRIGHTNESS +2, capture; judge the pair visibly different and record the judgment; (b) one other class: contrast -2 vs +2, saturation -2 vs +2, or quality 10 vs 30; capture a pair and judge. Record per-pair verdicts (thumbnails suffice for judgment; note if the full was needed).
  5. SETTINGS REGRESSION (session-5 closures must hold): one CIF cycle and one SVGA cycle — both kinds COMPLETE, thumbs correctly sized, zero stored-bytes CRC mismatches; QVGA restore ACKed with the next capture QVGA-class. WR-01 battery watch: no 'Critical battery' lines; beacons batt=valid.
  6. DASHBOARD REGRESSION GLANCE (UI-SPEC lifts, all series long): 'No commands yet' before the first command; 'Sent' immediately after each POST advancing via the poll; counters advance; link LED stays truthful (green while chunks/holds flow — a hold is not a link failure); status bar collapses 2x2 under 480px; no truncated/clipped text.
  7. Record everything: verbatim quotes with line numbers for every verdict, hold, release, re-announce, drop, and retry line; before/after tallies vs the session-5 baseline. If ANY series fails: capture the discriminating lines (they exist to name the next mechanism — the balloon-side periodic yield is the named fallback for command survivability), and leave that gap open — do not force a close.
  </how-to-verify>
  <acceptance_criteria>
  - Series A: 3/3 ACKs, 3/3 captures, 6/6 COMPLETE verdicts, zero command timeouts; any 'transmit held' line is followed by that command transmitting and reaching a terminal state; zero silently absent fulls (transferred, re-announced-and-transferred, or drop-logged by name)
  - WR-03: next interval capture lands one full interval after the manual capture — both timestamps quoted
  - SC-3: brightness pair judged plus one other class judged, per-pair verdicts recorded (Test 3's final open clause)
  - Settings regression: CIF and SVGA both-kinds COMPLETE with correctly-sized thumbs, zero CRC mismatches; QVGA restore verified; WR-01 watch green
  - Dashboard glance: all eight UI-SPEC considerations hold (explicit seven + the backstop LED-truth behavior)
  - Both console logs retained as balloon6.log / base6.log with every cited quote locatable by line number
  </acceptance_criteria>
  <resume-signal>Type "approved" with the console excerpts (series-A verdicts, hold/re-announce lines, WR-03 timestamps, SC-3 pair judgments), or describe the failing series with its discriminating log lines</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Flip the ledgers on evidence — 01-UAT.md, WINDOWS.md entries 3/5/8/9, STATE.md</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md, .planning/WINDOWS.md, .planning/STATE.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-UAT.md — the G-01-7/G-01-9 gap blocks and Test 3 note (flip targets; house quote convention from the G-01-5/G-01-6/G-01-8 resolutions)
  - .planning/WINDOWS.md — entries 3, 5, 8, 9 (table + JSON copies must both update; counts in front matter reconcile)
  - .planning/STATE.md — Current Status / Next Steps (the round outcome and the /gsd-secure-phase 1 gate)
  - The operator's Task 1 evidence (approved resume message with console excerpts, plus balloon6.log/base6.log)
  </read_first>
  <action>
  Update the three ledgers strictly from the Task 1 evidence, applying the evidence-class rules encoded in this plan's must_haves:
  1. 01-UAT.md: for each of G-01-7 and G-01-9 — if its acceptance passed (series A per the criteria above), set status resolved with resolved_by naming the plans/sites (01-18 quiet gate for command survivability; 01-17 re-announce for defect C), resolved_at, and verified_by quoting the operator-observed lines with line numbers/counts (match the G-01-8 resolution's structure); if it failed, keep status open and extend root_cause with the residual mechanism the new discriminator lines named, updating missing to the next levers (the balloon-side periodic yield is the named fallback for command survivability). Update Test 3's note with the SC-3 pair judgments and the WR-03 cadence outcome; flip Test 3 to pass only if every clause it holds is now judged (with this round's pairs judged, that condition is met unless a clause failed).
  2. WINDOWS.md: entries 3 (G-01-7) and 5 (G-01-9 defect C) flip on bench series evidence per the house convention; entry 8 (CR-04) flips on the code-structure fix + builds + harness with the unstageable camera-down scenario honestly recorded in the reason (the defect was source-confirmed; the fix is equally source-confirmable — name the unstageability, never fabricate a scenario); entry 9 (WR-08) flips on same-index retry evidence if any chunk transmit failed at bench, else on the code fix with the unexercised path recorded (the established 'unexercised' convention). Update BOTH the markdown table and the JSON copy; reconcile open_count/fixed_count/total_count in the front matter.
  3. STATE.md: record the round outcome in Current Status (which gaps/entries closed on what evidence, what remains open if anything), and set Next Steps to the security gate (/gsd-secure-phase 1; Phase 2/3 gates also pending) as the remaining phase-close step, then /gsd-progress.
  4. Commit the ledger updates separately from any log retention, message docs(01-20): bench re-verification evidence + ledger flips.
  </action>
  <verify>
  <automated>powershell -Command "if((Select-String -Path .planning/phases/01-command-protocol-control/01-UAT.md -Pattern 'balloon6|base6').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path .planning/WINDOWS.md -Pattern 'balloon6|base6').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path .planning/STATE.md -Pattern '01-20').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path .planning/WINDOWS.md -Pattern '\"id\": 8').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path .planning/WINDOWS.md -Pattern '\"id\": 9').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path .planning/WINDOWS.md -Pattern '\"status\": \"open\"').Count -ne [int]((Get-Content .planning/WINDOWS.md | Select-String -Pattern '^open_count: (\d+)').Matches[0].Groups[1].Value)){exit 1}"</automated>
  </verify>
  <acceptance_criteria>
  - Every bench-evidence flip cites operator-observed quotes with line numbers/counts; the CR-04/WR-08 flips carry their evidence-class reason (unstageable / unexercised recorded by name); failed series leave their gap open with the residual mechanism named in the new discriminator vocabulary
  - WINDOWS.md table and JSON copies agree; front-matter counts equal the actual row statuses
  - Session-6 evidence lands in the ledgers themselves — balloon6/base6 citations present in 01-UAT.md and WINDOWS.md whichever way each flip resolved (resolved quotes or residual-mechanism quotes both satisfy it), and STATE.md records the 01-20 round outcome; the automated verify pins exactly these post-update invariants plus the count reconciliation, so a skipped Task 2 fails its own gate
  - STATE.md names /gsd-secure-phase 1 as the remaining phase-close gate
  - Test 3 reflects the SC-3-pair and WR-03 outcomes; Tests 1/2/4/5 notes untouched absent regression evidence
  </acceptance_criteria>
  <done>The gap ledgers reflect hardware truth: closures evidenced (or residuals named with their discriminating evidence), ship-blocking WINDOWS entries resolved or honestly open, and the phase's remaining gate identified.</done>
  <reversibility rating="reversible">Documentation-only ledger updates.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench operator -> firmware flash + consoles | Human performs the flash and observes; logs are retained as evidence (read-only artifacts) |
| RF pair -> bench evidence | The two-radio link under bench conditions produces the verdicts; no untrusted input enters the firmware beyond the CRC16-validated frames covered in 01-17/01-18/01-19 threat models |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-20-01 | Tampering | evidence integrity | medium | mitigate | House convention enforced: closures quote verbatim console lines with line numbers/counts from retained logs (balloon6.log/base6.log); the CR-04/WR-08 evidence-class rules are explicit in must_haves so neither flip can ride a fabricated or absent scenario |
| T-01-20-02 | DoS | mixed-firmware flash invalidating the evidence | high | mitigate | Blocking checkpoint step 1 flashes BOTH boards before any traffic; behavior changes on both sides make a mixed pair's evidence misleading (recorded as a key_link prohibition) |
| T-01-20-03 | Safety | bench hardware | low | accept | Standard bench rig (USB-powered base, battery balloon at desk); WR-01 battery watch green in all prior sessions; nothing in this round changes power paths |
</threat_model>

## Artifacts this phase produces

- balloon6.log / base6.log — retained bench console evidence (repo root, prior-session convention)
- Updated gap blocks G-01-7/G-01-9 + Test 3 note (01-UAT.md)
- Updated WINDOWS entries 3/5/8/9 with reconciled counts (.planning/WINDOWS.md)
- Round outcome + next-gate record (.planning/STATE.md)
- .planning/phases/01-command-protocol-control/01-20-SUMMARY.md
- No source files, no new symbols, no env vars

<verification>
- Both firmware targets built green and harness exited 0 immediately before the flash (pre-flight step, on record in the session)
- Every ledger flip traces to a quotable line in the retained logs or carries its explicit evidence-class reason
- WINDOWS counts reconcile with row statuses; /gsd-ship remains blocked exactly as long as an entry is honestly open
</verification>

<success_criteria>
- Series-A acceptance truth exercised on this round's firmware: 3/3 ACKs, 6/6 COMPLETE, zero command timeouts, zero silent full losses — or the residual named with its discriminating evidence
- SC-3 visible-effect pairs finally judged (fourth round riding ends); WR-03 cadence discriminator quoted
- WINDOWS entries 3/5/8/9 resolved or honestly open with named residuals; CR-04/WR-08 flips carry their evidence-class reasons
- Ledgers consistent; the phase's remaining gate (/gsd-secure-phase 1) named in STATE.md
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-20-SUMMARY.md` when done
</output>
