---
phase: 01-command-protocol-control
plan: 13
type: execute
wave: 10
depends_on: ["01-12"]
files_modified:
  - include/image_protocol.h
  - include/command_protocol.h
  - src/command_protocol.cpp
  - src/command_sender.cpp
  - src/image_tx_manager.cpp
  - src/image_rx_manager.cpp
  - src/camera_manager.cpp
  - scripts/verify_protocol_roundtrip.mjs
autonomous: true
gap_closure: true
requirements: [IMG-01, IMG-02, IMG-03, IMG-04]

estimate:
  tokens: 17000
  raw_tokens: 17000
  tasks: 3
  confidence: low   # calibration store has 0 samples (factor 1, not applied); one atomic wire change + one camera-payload guard

must_haves:
  truths:
    - "CR-01 (G-01-9 defect B root cause): every 0x13 chunk frame carries an imageKind byte — ImageChunkBody.imageKind serialized immediately after imageId (6-byte body overhead; framed total 7 + 6 + dataLen + 4 <= 217), written by serializeChunk, read and bounds-validated (reject kind values outside THUMBNAIL/FULL_IMAGE) by deserializeChunk, stamped by createChunkPacket at BOTH balloon call sites, framed by the base receiver's chunk dispatch, and mirrored by the host harness — all in one commit so the wire contract never disagrees with itself"
    - "CR-01 base routing: onChunkFrame routes every chunk by exact (imageId, imageKind) slot lookup — the heal-precedence branch and the wire-order fallback heuristic are gone; a chunk whose kind does not match a live non-terminal slot of that id is dropped with a log naming the frame's id and kind, so late thumbnail-heal stragglers can never land in the active FULL slot and corrupt a fully-received full (the base4.log:854/:3267 36/36-received CRC-mismatch signature)"
    - "CR-01 discriminator logging: the balloon's window-chunk log line names the kind the window serves, and the push chunk log names THUMBNAIL — bench logs can discriminate which kind's bytes left the balloon at every index"
    - "CR-03 (G-01-9 defect A): createThumbnail, after the QQVGA/quality-20 downshift succeeds, drains and discards ONE stale frame (logged with its width/height/byte length — the payload-vs-metadata discriminator), and the frame guard rejects a captured frame whose fb->len exceeds THUMB_MAX_BYTES (8192) through the existing honest bail path (no thumbnail captured, settings restored) — a stale full-size frame with QQVGA metadata can no longer be pushed as a thumbnail (4-of-6-captures defect, 4-18x heal-exposure multiplier)"
    - "Standing invariants hold: all pacing constants (IMG_RETRANSMIT_MAX_PASSES 3, IMG_WINDOW_STALL_MS 8000, IMG_WINDOW_SERVICE_PREEMPT_MS 5000, IMG_WINDOW_RX_SETTLE_MS 500, IMG_THUMB_HEAL_IDLE_MS 24000, IMG_ENTRY_TTL_MS 900000) unchanged; manifest (0x12) and beacon (0x14) body layouts unchanged; D-23 kind-split CRC verification unchanged; the beacon early-return and command-response loop order untouched (PRI-01)"
    - statement: "Bench truth (rides plan 01-16, not this plan): series-A captures with correctly-sized thumbs (bytes < THUMB_MAX_BYTES, QVGA-class chunk counts 7-10) finalize both kinds COMPLETE with zero stored-bytes CRC mismatches at 100 percent chunk reception"
      verification: backstop
  artifacts:
    - "include/image_protocol.h — ImageChunkBody.imageKind field + updated 0x13 section comments"
    - "include/command_protocol.h — createChunkPacket declaration gains the imageKind parameter"
    - "src/command_protocol.cpp — serializeChunk/deserializeChunk kind byte (write, read, validate) + createChunkPacket stamps it"
    - "src/command_sender.cpp — base chunk dispatch bodyOverhead 5 -> 6"
    - "src/image_tx_manager.cpp — both createChunkPacket call sites stamp kind; kind in chunk/window-chunk log lines"
    - "src/image_rx_manager.cpp — onChunkFrame kind-exact routing replacing the heuristic"
    - "src/camera_manager.cpp — THUMB_MAX_BYTES constant, stale-frame drain, fb->len guard bound"
    - "scripts/verify_protocol_roundtrip.mjs — chunk mirrors carry kind; roundtrip + kind-rejection checks"
  key_links:
    - "Serializer <-> deserializer <-> base frame dispatch: the 6-byte chunk overhead must agree in all three (serializeChunk packet arithmetic, deserializeChunk frame arithmetic, command_sender.cpp chunk-case bodyOverhead) — a one-sided edit makes every chunk frame unframeable at the base"
    - "Harness <-> C++ factories: the Node mirrors transcribe the real factories (house rule since 01-06); the mirror update lands in the SAME commit as the C++ serializer change or the harness stops being the executable spec"
    - "createChunkPacket callers <-> windowKind: serviceWindowChunk must stamp entry.windowKind (the armed window's kind), pushThumbChunk must stamp THUMBNAIL — stamping anything else recreates the misroute from the TX side"
    - "THUMB_MAX_BYTES <-> manifest validation: the bound (8192) is a camera-side payload plausibility check, independent of the base's MAX_IMAGE_SIZE manifest bound — the base still validates everything at manifest time (Pitfall 11 unchanged)"
    - "Both boards flash together at the bench (01-09 severance lesson): this is a wire change; mixed old/new firmware on the pair breaks chunk framing entirely — never flash one board alone after this commit"
  prohibitions:
    - statement: "MUST NOT change any pacing constant, the 0x12 manifest body layout, or the 0x14 beacon body layout — the wire change is the chunk body only"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT touch the beacon early-return in ImageTxManager::process or the command-response loop order — PRI-01 telemetry priority stays harness-proven"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT add a dual-format or compatibility mode for kind-less chunks — both boards are reflashed together; no transitional shim"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT close G-01-9 on code-level evidence alone — closure requires the 01-16 bench series (correctly-sized thumbs, both kinds COMPLETE, zero 100-percent-received CRC mismatches)"
      status: flagged
      verification: backstop
---

<objective>
Wire-honesty remediation for the two root-cause defects the fresh code review (01-REVIEW.md, 23726b1) confirmed in source and the re-verification independently re-confirmed: CR-01 — chunk frames carry no imageKind byte, so the base's routing heuristic misroutes late thumbnail-heal stragglers into the active FULL slot (the 36/36-received stored-bytes CRC mismatch, G-01-9 defect B, also the G-01-7 image-7-full failure); CR-03 — the thumbnail guard checks fb->width/height only, so stale full-size frames with QQVGA metadata pass as thumbnails (G-01-9 defect A, the 4-of-6 full-sized-thumbs defect and the 4-18x push/heal exposure multiplier named in both gaps' next levers).

Purpose: the series-A acceptance truth ("3 unspaced captures finalize every kind COMPLETE") failed on three distinct mechanisms; this plan removes the two that live in what bytes mean on the wire and what bytes the camera produces, at root cause — a kind byte in the chunk frame (the review's named fix, not the interim hardening) and a payload bound plus stale-frame drain in createThumbnail.

Output: kind-tagged chunk wire format (protocol + serializer + deserializer + factory + base dispatch + harness mirrors, one atomic commit), kind-exact base routing with discriminator logging, the CR-03 thumbnail payload guard; builds 2/2 and harness green. Bench re-verification is plan 01-16 — this plan does not flip any gap status.

Spec-less probe fallback: recorded visible skip — this phase has no machine-mapped requirement IDs (phase_req_ids null), so no probe predicates were generated this run; must_haves derive from the open gaps in 01-VERIFICATION.md / 01-UAT.md and prior plan conventions.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-REVIEW.md
@.planning/phases/01-command-protocol-control/01-12-SUMMARY.md

Binding decisions and prior-round constraints:
- D-22 (kind-addressed windowed heal is the SINGLE reliability mechanism), D-23 (end-to-end CRC32 per kind), Pitfall 9 (big-endian helpers, never struct memcpy onto the wire), Pitfall 11 (validate before allocating)
- House rule (01-06): the host harness transcribes the REAL factories — mirrors change in the same commit as the C++ wire change
- 01-09 lesson: both boards always reflashed together — this round IS a wire change, so the 01-16 bench MUST flash both; between this commit and the bench the pair must not be run mixed
- Bench evidence to beat: image 7 full 36/36 received then stored-bytes CRC mismatch (base4.log:854-855); image 11 full 144/144 then CRC mismatch (:3267-3268); duplicate drops of chunks 2/3/21/22/25/26/27 of image 7 while the 19..29 heal window was outstanding (base4.log:821-847); full-sized thumbs 7157/7160/11223/28808 B passing the dimension-only guard (balloon4.log:356/:399/:1820/:3620); correct thumbs only 1411/1343 B
- Camera facts: createThumbnail at src/camera_manager.cpp:305-404; the R2 dimension guard at :361-373; fb_count 2 with CAMERA_GRAB_LATEST (configureCameraForBalloon ~:168-173); QQVGA = 160x120, thumbnail quality 20; correct QQVGA thumbs at bench were 1341-1703 B
- Wire facts: ImageChunkBody at include/image_protocol.h:154-159 ({imageId, chunkIndex, dataLen, data} — no kind); chunk framing is 7 + 5 + dataLen + 4 with the header bodyLen carrying dataLen; the balloon's command_handler receiver dispatches ONLY 0x10 commands (no chunk arithmetic on the balloon side); the base's chunk case is src/command_sender.cpp:322-326 (bodyOverhead = 5)
</context>

<assumption_delta_decision>
 noun: none promoted — ImageKind (THUMBNAIL/FULL_IMAGE) remains the identity; the chunk body gains a TAG for an existing discriminator, not a new entity kind
 decision: no-change
 rationale: the detector fired on the historical phrase "second-card retry" in the ROADMAP's 01-09 plan entry (SD-card swap during the G-01-3 diagnosis), not on any singular-to-plural transition in this round's scope. No second source of truth, platform, tenant, or identity class is introduced; the kind byte makes an existing two-valued identity explicit on the wire where it was previously inferred.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: CR-01 wire change — imageKind byte in the 0x13 chunk frame (protocol + serializers + factory + base dispatch + harness, one atomic commit)</name>
  <files>include/image_protocol.h, include/command_protocol.h, src/command_protocol.cpp, src/command_sender.cpp, scripts/verify_protocol_roundtrip.mjs</files>
  <read_first>
  - include/image_protocol.h — ImageChunkBody (:154-159), the 0x13 section comment (:151-153), the constants block (:49-120), ImageKind enum (:33-36)
  - src/command_protocol.cpp — serializeChunk (:388-433), deserializeChunk (:435-473), createChunkPacket (:660-672)
  - include/command_protocol.h — createChunkPacket declaration (:305)
  - src/command_sender.cpp — processIncomingByte type-dispatch chunk case with its body-overhead constant (:322-326)
  - scripts/verify_protocol_roundtrip.mjs — createChunkPacketMirror (:521-537), serializeChunk mirror (:539-573), deserializeChunk mirror (:575-600), the receiver-mirror chunk framing (:750-770), chunk roundtrip/oversize tests (:875-915)
  - .planning/phases/01-command-protocol-control/01-REVIEW.md — CR-01 finding (root cause + the exact fix shape this task implements)
  </read_first>
  <action>
  Add the kind byte to the chunk frame across the whole stack in ONE commit. The body becomes: imageId BE16, imageKind u8, chunkIndex BE16, dataLen u8, data — 6 bytes of overhead before the payload.

  1. include/image_protocol.h: add `uint8_t imageKind;` to ImageChunkBody immediately after imageId with a comment naming CR-01 (late thumbnail-heal stragglers misrouted into the active FULL slot; kind-exact routing at the base; stamped from windowKind/push kind at both TX call sites). Update the 0x13 section comment from a 5-byte to a 6-byte overhead (framed total 7 + 6 + dataLen + 4, max 217 <= CMD_MAX_PACKET_SIZE 240). Touch NOTHING else in this header — all pacing constants, the 0x12 manifest body, and the 0x14 beacon body stay byte-identical (prohibition).
  2. src/command_protocol.cpp serializeChunk: packetLength arithmetic becomes CMD_HEADER_SIZE + 6 + dataLen + 4; write the kind byte right after the imageId write (writeUint16 then a single byte store).
  3. src/command_protocol.cpp deserializeChunk: minimum-length check becomes CMD_HEADER_SIZE + 6 + 4; read the kind byte after imageId; REJECT the frame when the kind is neither THUMBNAIL (0) nor FULL_IMAGE (1) — untrusted-RF defense in depth mirroring the T-02-11 kind validation on the request path; the frame-arithmetic agreement check becomes 7 + 6 + dataLen + 4 == length.
  4. src/command_protocol.cpp createChunkPacket: signature gains `uint8_t imageKind` as the second parameter (after imageId); assign packet.body.imageKind from it. Update the declaration in include/command_protocol.h (:305) to match.
  5. src/command_sender.cpp: in processIncomingByte's type dispatch, the chunk case's body-overhead constant goes from 5 to 6 (the framed arithmetic changes with the body; nothing else in the dispatch switch is touched — the manifest/beacon fixed-body cases stay as they are).
  6. scripts/verify_protocol_roundtrip.mjs, same commit: createChunkPacketMirror accepts and stamps the kind; the serializeChunk mirror emits the kind byte and uses the 6-overhead arithmetic; the deserializeChunk mirror reads the kind and returns null for kind values outside 0/1; the receiver-mirror chunk framing arithmetic is updated to match; the existing chunk roundtrip tests pass a kind and assert it survives the roundtrip next to imageId/chunkIndex/dataLen; add one negative check that a serialized chunk body carrying kind 2 fails deserialization (name it '(img-e) chunk kind validation' or the next free label in the file's numbering convention).
  </action>
  <verify>
  <automated>powershell -Command "if((Select-String -Path include/image_protocol.h -Pattern 'uint8_t\s+imageKind;').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path src/command_protocol.cpp -Pattern 'imageKind').Count -lt 6){exit 1}" ; powershell -Command "if((Select-String -Path include/command_protocol.h -Pattern 'createChunkPacket\(uint16_t imageId, uint8_t imageKind').Count -ne 1){exit 1}" ; powershell -Command "if((Select-String -Path src/command_sender.cpp -Pattern 'bodyOverhead = 6; // imageId/chunkIndex/dataLen block').Count -ne 1){exit 1}" ; powershell -Command "if((Select-String -Path include/image_protocol.h -Pattern 'IMG_WINDOW_STALL_MS\s*=\s*8000').Count -ne 1){exit 1}" ; pio run -e esp32-s3-balloon -e esp32-s3-basestation ; node scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - include/image_protocol.h: ImageChunkBody declares imageKind after imageId; a grep for the pacing anchors IMG_WINDOW_STALL_MS = 8000 / IMG_WINDOW_SERVICE_PREEMPT_MS = 5000 / IMG_RETRANSMIT_MAX_PASSES 3 still finds each exactly once (nothing else in the header changed)
  - src/command_protocol.cpp: serializeChunk writes the kind (6-overhead arithmetic), deserializeChunk reads it, rejects kind 2+, and enforces the 6-overhead frame arithmetic; createChunkPacket takes the kind parameter (grep imageKind count >= 6 across write/read/validate/factory sites)
  - include/command_protocol.h declares the new createChunkPacket signature exactly once
  - src/command_sender.cpp chunk case bodyOverhead comment/token updated to 6 (manifest and beacon cases untouched)
  - scripts/verify_protocol_roundtrip.mjs mirrors carry the kind end-to-end with a kind-rejection negative check
  - `pio run -e esp32-s3-balloon -e esp32-s3-basestation` reports 2 succeeded; `node scripts/verify_protocol_roundtrip.mjs` exits 0 with every prior check still passing
  - git diff file set for this task is exactly the five listed files
  </acceptance_criteria>
  <done>Chunk frames are kind-tagged on the wire, validated at deserialize, framed at 6-byte overhead on the base, and harness-mirrored — one atomic commit; builds and harness green.</done>
  <reversibility rating="costly">Wire-format change: reverting after the pair is flashed requires re-flashing both boards; before the bench it is a clean git revert of one commit.</reversibility>
</task>

<task type="auto">
  <name>Task 2: CR-01 consumers — TX kind stamping with discriminator logs, RX kind-exact routing replacing the heuristic</name>
  <files>src/image_tx_manager.cpp, src/image_rx_manager.cpp</files>
  <read_first>
  - src/image_tx_manager.cpp — pushThumbChunk (:518-553), serviceWindowChunk (:740-811, the kind-selected source block and the window-chunk log at :785-792)
  - src/image_rx_manager.cpp — onChunkFrame (:253-301: the heal-precedence branch, the wire-order fallback, the terminal/no-match drops)
  - Task 1's committed state of include/image_protocol.h + src/command_protocol.cpp (the kind-carrying factory this task calls)
  - .planning/phases/01-command-protocol-control/01-REVIEW.md — CR-01 fix paragraph (routing validation requirement)
  </read_first>
  <action>
  Wire the two managers to the kind-carrying factory and delete the routing heuristic.

  1. src/image_tx_manager.cpp pushThumbChunk: pass THUMBNAIL as the kind argument at the createChunkPacket call; extend the push-chunk log line to name the kind (the push is always kind 0 — printing it keeps push and window lines symmetric for log grepping).
  2. src/image_tx_manager.cpp serviceWindowChunk: pass entry.windowKind at the createChunkPacket call — the armed window names which buffer it serves, and now the frame says so on the wire; extend the window-chunk log line to name the kind (the defect-B discriminator named in G-01-9's missing list: bench must be able to tell which kind's bytes left the balloon at every index).
  3. src/image_rx_manager.cpp onChunkFrame: replace the ENTIRE routing heuristic — both the heal-window-precedence branch and the non-terminal-FULL-then-thumbnail fallback, with their comments — with a single kind-exact lookup: findTransfer(c.imageId, c.imageKind). The two existing drop paths remain, both kind-aware: no matching live slot -> log 'chunk for image %u kind %u with no matching manifest ignored' (add the kind to this line); terminal slot -> the existing finalized-ignore line already prints the slot's kind. Update the function's header comment to state kind-exact routing (CR-01): the frame's kind byte is authoritative, a mismatched-kind frame is dropped and counted, never written to a bitmap or SD. acceptChunk is untouched — the per-chunk length/bitmap/SD logic is unchanged.
  </action>
  <verify>
  <automated>powershell -Command "if((Select-String -Path src/image_tx_manager.cpp -Pattern 'createChunkPacket\(entry.imageId').Count -ne 2){exit 1}" ; powershell -Command "if((Select-String -Path src/image_tx_manager.cpp -Pattern 'window chunk\(image %u kind %u').Count -ne 1){exit 1}" ; powershell -Command "if((Select-String -Path src/image_rx_manager.cpp -Pattern 'kind-exact').Count -lt 1){exit 1}" ; powershell -Command "if((Select-String -Path src/image_rx_manager.cpp -Pattern 'Heal-window precedence').Count -ne 0){exit 1}" ; powershell -Command "if((Select-String -Path src/image_rx_manager.cpp -Pattern 'no matching manifest ignored').Count -ne 1){exit 1}" ; pio run -e esp32-s3-balloon -e esp32-s3-basestation ; node scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - src/image_tx_manager.cpp: exactly two createChunkPacket call sites remain (push + window service), each passing a kind argument (THUMBNAIL / entry.windowKind); the window-chunk log names the kind
  - src/image_rx_manager.cpp onChunkFrame: the routing is a single findTransfer on (imageId, imageKind); the old precedence comment token and the heuristic block are gone (count 0); the no-match drop names the kind
  - No behavior change to acceptChunk, startTransfer, the window driver, or the heal loop in this task — routing only
  - `pio run -e esp32-s3-balloon -e esp32-s3-basestation` reports 2 succeeded; harness exits 0
  - git diff file set for this task is exactly src/image_tx_manager.cpp and src/image_rx_manager.cpp
  </acceptance_criteria>
  <done>Every chunk frame is stamped with the kind its TX context serves, and the base routes chunks by exact (id, kind) — a late thumbnail-heal straggler can no longer occupy full-image indices (the 36/36 CRC-mismatch mechanism is removed at root cause).</done>
  <reversibility rating="reversible">Two source files; reverts cleanly with the Task 1 commit or standalone.</reversibility>
</task>

<task type="auto">
  <name>Task 3: CR-03 thumbnail payload guard — stale-frame drain + THUMB_MAX_BYTES bound in createThumbnail</name>
  <files>src/camera_manager.cpp</files>
  <read_first>
  - src/camera_manager.cpp — createThumbnail (:305-404): the QQVGA/quality-20 downshift (:327-337), the frame capture (:339-350), the R2 dimension guard (:352-373), the allocate-copy-restore tail (:375-404); the debug-config block at the top of the file for the constant's placement style
  - include/balloon_config.h — camera boot config (QVGA, quality 10)
  - .planning/phases/01-command-protocol-control/01-REVIEW.md — CR-03 finding (fb_count 2 / GRAB_LATEST stale-frame mechanism; the drain-and-bound fix shape)
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-9 defect A evidence (thumb bytes == full bytes for 4 of 6 captures; the two QVGA cases coincide with 2x FB-OVF during thumbnail capture)
  </read_first>
  <action>
  Make the thumbnail payload honest, not just its metadata.

  1. File-scope constant near the debug-config block: `static constexpr size_t THUMB_MAX_BYTES = 8192;` with a comment citing the bench distribution (correct QQVGA q20 thumbs 1341-1703 B; full-sized impostors 7157-28808 B; 8192 is ~5x the observed correct ceiling and far below every impostor).
  2. Stale-frame drain: after the setFrameSize(FRAMESIZE_QQVGA) + setQuality(20) downshift succeeds and BEFORE the real capture, fetch and discard ONE frame (esp_camera_fb_get; if non-null, log 'Camera: drained stale frame after QQVGA downshift (%ux%u, %u B)' with its width/height/len — the payload-vs-metadata discriminator the UAT missing list asks to capture at bench — then esp_camera_fb_return it; if null, proceed without draining). With fb_count 2 / CAMERA_GRAB_LATEST the first post-config frame can be the stale pre-downshift capture whose dimension metadata was already restamped — this drain removes it from the queue so the real capture waits for a fresh QQVGA frame.
  3. Guard bound: extend the existing dimension guard's bail condition with fb->len > THUMB_MAX_BYTES (same honest bail path: return the frame buffer, null the thumbnail member, clear valid, restore original framesize and quality, return false). The bail log gains the byte length and the bound so bench lines are self-diagnosing.
  4. Nothing else in createThumbnail changes: the CR-04 null-member discipline on every failure path, the exact-size malloc, and the restore pair stay exactly as they are. setFrameSize/setQuality are untouched (G-01-8's two-path logic is prior-round closed and must not drift).
  </action>
  <verify>
  <automated>powershell -Command "if((Select-String -Path src/camera_manager.cpp -Pattern 'THUMB_MAX_BYTES').Count -lt 3){exit 1}" ; powershell -Command "if((Select-String -Path src/camera_manager.cpp -Pattern 'drained stale frame after QQVGA downshift').Count -ne 1){exit 1}" ; powershell -Command "if((Select-String -Path src/camera_manager.cpp -Pattern 'fb->len > THUMB_MAX_BYTES').Count -ne 1){exit 1}" ; powershell -Command "if((Select-String -Path src/camera_manager.cpp -Pattern 'allocatedFrameSize').Count -lt 3){exit 1}" ; pio run -e esp32-s3-balloon -e esp32-s3-basestation ; node scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - src/camera_manager.cpp defines THUMB_MAX_BYTES = 8192 exactly once and references it in the guard condition (grep count >= 3: definition + bail log + condition)
  - The drain call sits between the successful downshift and the real capture, logs the drained frame's width/height/len, and returns the frame buffer
  - The bail path for an oversize frame reuses the existing honest-failure shape (settings restored, no dangling member) — grep allocatedFrameSize still >= 3 confirms the G-01-8 fix is untouched
  - `pio run -e esp32-s3-balloon -e esp32-s3-basestation` reports 2 succeeded; harness exits 0
  - git diff file set for this task is exactly src/camera_manager.cpp
  </acceptance_criteria>
  <done>A stale full-size frame can no longer be enqueued as a thumbnail: the drain removes the pre-downshift frame from the fb queue and the byte bound rejects any survivor through the honest bail path — the 4-18x push/heal exposure multiplier is gone.</done>
  <reversibility rating="reversible">Single file, no wire or state-machine impact; git revert.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link -> base receiver | Untrusted 0x12/0x13/0x14 frames cross the air into CommandSender::processIncomingByte -> ImageRxManager |
| esp32-camera driver -> CameraManager | Driver-owned fb queue hands frames to createThumbnail (stale-frame / oversize-payload class) |
| bench operator -> serial consoles | Read-only observation; no input crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-13-01 | Tampering | deserializeChunk / onChunkFrame | high | mitigate | Kind byte validated at deserialize (reject outside 0/1) AND routing is exact-(id,kind): a crafted frame can only address a slot whose same-kind manifest already passed Pitfall-11 validation; corrupted payload bytes still fail the D-23 end-to-end CRC32 at finalize — never fabricated COMPLETE |
| T-01-13-02 | Tampering | createThumbnail payload guard | medium | mitigate | fb->len bound (THUMB_MAX_BYTES 8192) + stale-frame drain: a driver-level payload/metadata mismatch bails honestly instead of pushing an impostor kind as another |
| T-01-13-03 | DoS | chunk frame length | low | accept | Existing bounds unchanged: header bodyLen <= IMG_CHUNK_PAYLOAD_SIZE and expected-total <= receive-buffer checks already reject oversize framing at dispatch |
| T-01-13-04 | Repudiation | bench logs | low | accept | Serial-console evidence is the established bench record; kind-stamped chunk logs (this plan) make the record more attributable, not less |
</threat_model>

## Artifacts this phase produces

- `ImageChunkBody.imageKind` (include/image_protocol.h) — new wire field, u8 after imageId
- `createChunkPacket(uint16_t imageId, uint8_t imageKind, uint16_t chunkIndex, const uint8_t* data, uint8_t dataLen)` — new signature (include/command_protocol.h, src/command_protocol.cpp)
- `THUMB_MAX_BYTES` (src/camera_manager.cpp) — file-scope static constexpr, 8192
- `Camera: drained stale frame after QQVGA downshift (...)` — new discriminator log line (src/camera_manager.cpp)
- Harness: kind-carrying chunk mirrors + the kind-rejection negative check (scripts/verify_protocol_roundtrip.mjs)
- No new files, classes, tasks, env vars, or CLI flags

<verification>
- `pio run -e esp32-s3-balloon -e esp32-s3-basestation` — 2 succeeded
- `node scripts/verify_protocol_roundtrip.mjs` — exit 0, all prior checks green plus the new kind checks
- Pacing-anchor greps: the six standing constants unchanged in include/image_protocol.h
- Diff hygiene: three commits, each matching its task's declared file set exactly
</verification>

<success_criteria>
- Chunk frames carry a validated kind byte end-to-end (wire, factory, dispatch, harness)
- onChunkFrame routing is kind-exact — the CR-01 misroute mechanism no longer exists in code
- createThumbnail drains the stale frame and bounds the payload — the CR-03 impostor mechanism no longer exists in code
- No gap status flips in this plan (bench proof is 01-16's job)
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-13-SUMMARY.md` when done
</output>
