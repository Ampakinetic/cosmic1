---
phase: 01-command-protocol-control
plan: 26
type: execute
wave: 18
depends_on: ["01-25"]
files_modified:
  - .planning/WINDOWS.md
  - src/image_tx_manager.cpp
  - src/main_balloon.cpp
  - src/main_basestation.cpp
  - .planning/phases/01-command-protocol-control/COVERAGE.md
autonomous: false
gap_closure: true
requirements: [IMG-02, IMG-03, CTRL-06]

estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 3
  confidence: low   # calibration store has 0 samples (factor 1, not applied); ledger routing + one blocking disposition + three small review-shaped fixes (mirrors 01-23 sizing)

must_haves:
  truths:
    - statement: "Every confirmed fresh-review finding is routed: WINDOWS.md carries an entry for each of review 7d96a98's WR-01/WR-02/WR-03 (table AND JSON copy, front-matter counts reconciled at 17-19) — no confirmed review finding is unrouted at phase completion (the round-#7 house convention, 01-VERIFICATION.md gap 4, the one gap the ledgers did NOT carry)"
      verification: explicit
    - statement: "Each finding's disposition is DECIDED, not defaulted: fixed this round (code + builds 2/2 + harness, with bench-exercisable discriminators named — WR-02's healthy-boot line IS stageable at the 01-27 bench, unlike the prior round's unstageable classes) or waived with a recorded reason; the decision is the operator's at the Task 2 checkpoint"
      verification: explicit
    - statement: "WR-01 fix (if approved): enqueueCapture's full-buffer ps_malloc failure no longer early-returns — it marks the full unavailable (nullptr buffer, zero length/chunks) with the renamed log naming 'full dropped, thumbnail still pushes', falls through to the thumbnail branch, and the existing both-empty guard (:331-334) keeps a nothing-transferable capture from occupying a queue slot — the degradation ladder keeps its cheapest payload under PSRAM exhaustion, consistent with the oversize path (:266) and the thumb-alloc-failure path (:306)"
      verification: explicit
    - statement: "WR-02 fix (if approved): a healthy active camera at boot no longer logs 'Camera system health check failed' — the inverted branch at main_balloon.cpp:573-577 is replaced with the honest shape (active camera: warn only when the sensor handle is absent; inactive camera: a skipped note) so the warning channel stops masking the real failure case"
      verification: explicit
    - statement: "WR-03 fix (if approved): the link-truth poll path is uint32 end-to-end — appState.ackedAtLastPoll widens to uint32 and the comparison at main_basestation.cpp:2206-2210 drops both truncating casts — each 65536-ACK wrap can no longer skip a lastAckTime refresh"
      verification: explicit
    - statement: "Both firmware targets build green and the wire-format harness passes (any approved-fix subset); waived findings have zero source changes at their fix sites"
      verification: explicit
  artifacts:
    - ".planning/WINDOWS.md — entries 17-19 (review 7d96a98 WR-01..WR-03) with confirmed-finding descriptions, dispositions, and reconciled counts (3 open added if all routed open-first, then per-disposition)"
    - "src/image_tx_manager.cpp — the WR-01 ps_malloc fall-through restructure (enqueueCapture :272-292)"
    - "src/main_balloon.cpp — the WR-02 honest health-check branch (:573-577)"
    - "src/main_basestation.cpp — the WR-03 uint32 widening (member :81, poll site :2206-2210)"
    - ".planning/phases/01-command-protocol-control/COVERAGE.md — the round-#11 re-check line appended to the standing no-external-API declaration (house convention, one line)"
  key_links:
    - "review findings <-> ledger: each of the three confirmed defects maps to exactly one new WINDOWS entry (17/18/19); none rides an existing fixed entry and none is left in the review file only (the 11 Info items stay in the review file per prior-cycle convention)"
    - "WR-01 <-> the 01-25 D1 fix: both touch enqueueCapture's neighborhood in src/image_tx_manager.cpp — THIS is why 01-26 runs in the wave after the debug round; the WR-01 restructure must not disturb whatever the D1 fix landed (read the file at its current post-01-25 state, not this plan's line numbers alone)"
    - "WR-02 <-> the 01-27 bench: the fix is the rare stageable review class — every healthy boot either shows the spurious warning (pre-fix behavior) or does not (post-fix); the bench session's boot windows are the discriminator"
    - "WR-03 <-> IN-03 LED truth: the widened comparison is the only change to the ackedAtLastPoll contract; the /status poll cadence and the link-staleness thresholds are untouched"
  prohibitions:
    - statement: "MUST NOT waive a finding without a recorded reason in its ledger entry — an unreasoned waiver is an undecided hole (the exact failure mode the round-#7 convention exists to close)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT expand the fixes beyond the review's named shapes — no capture-path redesign for WR-01 (the fall-through uses the existing oversize/thumb-failure branch shapes), no new health-check subsystem for WR-02, no counter-semantics change for WR-03 (the counter itself stays uint32 untouched)"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT fabricate state anywhere (recalled project prohibition, UAT Test 5 tier): the WR-01 renamed log must fire only on the actual allocation failure, and WR-02's warning only on the actual absent-handle condition"
      status: flagged
      verification: backstop
---

<objective>
Close 01-VERIFICATION.md gap 4: route the fresh adversarial review's three confirmed warnings (7d96a98, 0 Critical / 3 Warnings / 11 Info — verifier re-derived all three at HEAD source) into the WINDOWS ledger as entries 17-19, obtain the operator's per-finding disposition (fix-now vs waive-with-reason), and implement the approved fixes — WR-02 carrying a bench-stageable discriminator into the 01-27 session.

Purpose: per the project's own convention (round #7 precedent, encoded in 01-23's must-have), an unrouted confirmed finding is a verification gap — this is currently the one gap the ledgers do NOT carry, reopened by a new review after round #9's routing closed the same class. All three defects are real: PSRAM exhaustion drops the degradation ladder's cheapest payload; the inverted health check warns on every healthy boot (carried across two review cycles); the uint16 truncation mis-orders the LED-truth refresh at each 65536-ACK wrap. None blocks the phase goal; all three are small, review-shaped fixes in files the debug round just stabilized.

Output: WINDOWS entries 17-19 with decided dispositions; approved fixes green on both targets with the harness passing; ready for the 01-27 bench reflash.

Spec-less probe fallback: recorded visible skip — phase_req_ids null this run, no probe predicates generated (standard goal-backward verification, the established determination).
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-REVIEW.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/phases/01-command-protocol-control/01-25-SUMMARY.md
@.planning/WINDOWS.md

Binding evidence and constraints:
- The three confirmed findings with fix shapes are in 01-REVIEW.md's Warnings section: WR-01 (image_tx_manager.cpp:276-283 — ps_malloc early-return before the thumbnail branch, inconsistent with :264-270 and :302-313), WR-02 (main_balloon.cpp:573-577 — the negation lost when the health check was stubbed; carried across two review cycles), WR-03 (main_balasestation ackedAtLastPoll uint16 at :81 truncating the uint32 commandsAcked at :2206-2210). The verifier's re-derivation with exact HEAD lines is in 01-VERIFICATION.md gap 4 and the Anti-Patterns table (:196-198).
- Disambiguation: this round's WR-01/WR-02/WR-03 are the 7d96a98 review's numbering — distinct from round #9's WR-01..WR-05 (WINDOWS 10-14) and from 01-19's WR-03 baseline vocabulary; each new entry's description names review 7d96a98 to avoid collision.
- Evidence-class convention: entries 8/9/10-14 precedent — a fix the rig cannot stage flips on code + builds + harness with the unstageable scenario recorded BY NAME. WR-02 is the exception that CAN be staged (every healthy boot discriminates); WR-01 needs PSRAM exhaustion and WR-03 needs a 65536-ACK wrap — both unstageable at a bench session, both closed on code evidence with that recorded by name if approved.
- Sequencing: 01-25 (the D1 debug round) touched src/image_tx_manager.cpp — this plan's WR-01 fix lands on the post-01-25 state of enqueueCapture. Read the file fresh; the :276-283 citations are pre-01-25 line numbers.
- House convention: no VALIDATION.md exists for this phase (user-accepted at the Nyquist gate, same as all prior plans).
- Adjacent human item (recorded, not this plan's scope): 01-23's auto-advanced fix-all disposition (WINDOWS 10-14) still awaits end-of-phase operator confirmation per 01-23-SUMMARY — the Task 2 checkpoint resume message is a natural place for the operator to confirm it for the record alongside this round's dispositions.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route WR-01..WR-03 as WINDOWS entries 17-19 + COVERAGE re-check line</name>
  <files>.planning/WINDOWS.md, .planning/phases/01-command-protocol-control/COVERAGE.md</files>
  <read_first>
  - .planning/WINDOWS.md — the table + JSON copy structure, front-matter count fields, and entries 10-14 (the round-#9 review-routing precedent whose format this round mirrors)
  - .planning/phases/01-command-protocol-control/01-REVIEW.md — the three Warnings (each finding's file/line/issue/fix, reviewed 2026-08-27T20:39:30Z)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — gap 4 (the routing requirement) and the Anti-Patterns rows (:196-198, the verifier's HEAD re-derivations)
  - .planning/phases/01-command-protocol-control/COVERAGE.md — the standing no-external-API declaration and its per-round re-check convention
  </read_first>
  <action>
  Add three entries to .planning/WINDOWS.md — ids 17/18/19, phase 01, kind unmet-truth, status open, recorded_at now:
  - id 17 / WR-01: file src/image_tx_manager.cpp line 276 — enqueueCapture's full-buffer ps_malloc failure logs and returns BEFORE the thumbnail branch, so PSRAM exhaustion drops the cheapest highest-value payload the degradation ladder was designed to keep (inconsistent with the oversize path :266 and the thumb-alloc-failure path :306); the base sees no manifest at all; found by review 7d96a98, confirmed at HEAD by re-verification #9.
  - id 18 / WR-02: file src/main_balloon.cpp line 573 — the camera health check's negation was lost when the call was stubbed, so every healthy active camera logs a failed health check at boot (and an absent camera produces no warning at all) — warning-channel noise that masks the real failure case; carried across two review cycles; found by review 7d96a98, confirmed at HEAD by re-verification #9.
  - id 19 / WR-03: file src/main_basestation.cpp line 2207 — ackedAtLastPoll is uint16 while commandsAcked is uint32, so each 65536-ACK wrap between polls makes the truncated comparison false and skips that poll's lastAckTime refresh — the IN-03 LED-truth staleness signal runs stale until the next post-boundary ACK; found by review 7d96a98, confirmed at HEAD by re-verification #9.
  Update BOTH the markdown table and the JSON copy; bump open_count and total_count in the front matter so counts equal the actual row statuses. Append one line to COVERAGE.md recording the round-#11 re-check: the round's scope (crash-regression debug fix, review routing + robustness fixes, bench session) adds no external API surface — the declaration stands. Commit docs(01-26): route review 7d96a98 findings WR-01..WR-03 (WINDOWS 17-19).
  </action>
  <verify>
  <automated>grep -q '"id": 17' .planning/WINDOWS.md && grep -q '"id": 18' .planning/WINDOWS.md && grep -q '"id": 19' .planning/WINDOWS.md && grep -c "7d96a98" .planning/WINDOWS.md | grep -qv "^0$" && grep -q "round-#11" .planning/phases/01-command-protocol-control/COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
  - Entries 17-19 exist in BOTH the markdown table and the JSON copy with matching ids, files, lines, descriptions citing review 7d96a98 + re-verification #9 confirmation, and status open
  - Each description is distinct from the round-#9 WR vocabulary (names the 7d96a98 review; id 19 does not collide with the 01-19 WR-03 baseline finding)
  - Front matter open_count and total_count equal the actual row statuses (3 more open than before the task)
  - COVERAGE.md carries the round-#11 re-check line
  </acceptance_criteria>
  <done>Every confirmed 7d96a98 finding lives in the ledger — the unrouted-findings gap's routing half is closed unconditionally, before any disposition decision.</done>
  <reversibility rating="reversible">Ledger documentation only.</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Disposition decision — fix-now vs waive-with-reason per finding (operator decision point)</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
  - .planning/WINDOWS.md — entries 17-19 as routed by Task 1
  - .planning/phases/01-command-protocol-control/01-REVIEW.md — the three Warnings with their fix shapes and risk notes
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — Human Verification #5 (why this is a maintainer judgment)
  </read_first>
  <decision>
  Per finding (WR-01, WR-02, WR-03) choose a disposition: fix this round, or waive-with-reason (or defer, leaving the entry open). Mixed per-finding dispositions are fine. The plan's recommendation is option-a (fix all three).
  </decision>
  <context>
  01-VERIFICATION.md's Human Verification #5 makes this explicitly a maintainer judgment: the verifier confirmed all three defects are real and unrouted, not whether they must be fixed in this milestone. None blocks the phase goal (a degradation-ladder edge under PSRAM exhaustion, a noisy warning channel, a one-missed-refresh-per-65536-ACKs LED staleness edge). Whatever is not fixed must still carry a decided disposition — the round-#7 house convention treats an unrouted-or-undecided confirmed finding as a verification gap, and Task 1's routing alone already meets the gap's minimum bar. Adjacent record-keeping: the 01-23 auto-advanced fix-all disposition (entries 10-14) still awaits end-of-phase confirmation — the operator may confirm it in the same resume message.
  </context>
  <options>
    <option id="option-a">
      <name>Fix all three this round (recommended)</name>
      <pros>Each fix is small, review-shaped, and low-risk (the review supplies concrete fix shapes); WR-02 is bench-stageable at 01-27 (every healthy boot discriminates) and WR-01/WR-03 close on code + builds + harness with their unstageable triggers recorded by name; all three ride the 01-27 reflash</pros>
      <cons>Adds a three-file firmware-diff surface onto the same bench session that must validate the D1 fix — any regression risk lands together (mitigated: none of the three touches the crash path's scheduling semantics)</cons>
    </option>
    <option id="option-b">
      <name>Fix WR-02 only (the stageable one)</name>
      <pros>Minimal surface; the warning channel becomes truthful at the 01-27 bench with a live discriminator; WR-01/WR-03 stay routed-open for a later disposition</pros>
      <cons>Two confirmed defects ride into phase close as open entries blocking /gsd-ship until later disposition; the PSRAM-exhaustion payload loss stays live</cons>
    </option>
    <option id="option-c">
      <name>Route only — waive or defer all three with reasons</name>
      <pros>Zero code risk this round; the routing gap is already closed by Task 1; reasoned waives are a legal terminal ledger state</pros>
      <cons>Three entries ride into phase close as open/waived; the degradation-ladder and warning-channel truths stay live defects in shipped behavior; WR-02's free fix is skipped despite being bench-stageable this round</cons>
    </option>
  </options>
  <resume-signal>Type "approved" with the chosen option and any per-finding overrides (e.g. "option-a" or "option-b, plus fix 03"), or a per-finding list (e.g. "fix 01/02, waive 03: reason"). Each waived finding MUST carry its reason in the same message. Optionally confirm the 01-23 fix-all disposition (entries 10-14) in the same message for the end-of-phase record.</resume-signal>
  <acceptance_criteria>
  - The resume message records a disposition for each of the three findings (fix, waive+reason, or defer)
  - Any waive carries a reason string; any absence of reason routes that finding back to this checkpoint rather than defaulting silently
  </acceptance_criteria>
  <done>Each of the three findings has an explicit, recorded disposition decision made by the operator — no undecided hole remains.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the approved fixes + builds + harness + entry updates</name>
  <files>src/image_tx_manager.cpp, src/main_balloon.cpp, src/main_basestation.cpp, .planning/WINDOWS.md</files>
  <read_first>
  - src/image_tx_manager.cpp — enqueueCapture at its CURRENT (post-01-25) state: the fullArmable gate, the full-buffer ps_malloc block, the thumbnail branch, the both-empty guard, and whatever the 01-25 D1 fix landed nearby
  - src/main_balloon.cpp — the health-check block (:557-577 region: the pattern of honest skipped-check warnings around it — power/sensors/communication — whose SYS_WARNING shapes the WR-02 fix mirrors)
  - src/main_basestation.cpp — the appState member block (:81) and the poll site (:2206-2210) where ackedAtLastPoll is compared and stored
  - .planning/WINDOWS.md — entries 17-19 (to update per disposition) and the count fields
  - The operator's Task 2 disposition list
  </read_first>
  <action>
  Implement exactly the approved subset (skip any waived finding entirely):
  1. WR-01 (src/image_tx_manager.cpp): restructure the full-buffer allocation block so a failed ps_malloc does NOT return — set entry.fullBuffer to nullptr, fullLength to 0, fullTotalChunks to 0, change the log message to name the degradation explicitly (full dropped, thumbnail still pushes, with the image id and the failed size), and fall through to the thumbnail branch unchanged; the existing both-empty guard after the thumbnail branch already prevents occupying a slot when nothing is transferable, and the thumb branch's existing state assignment handles full-unavailable via the fullTransferArmable path it already uses. Mirror the review's fix shape; keep the oversize-skip and thumb-alloc-failure branches byte-identical.
  2. WR-02 (src/main_balloon.cpp): replace the inverted block — when appState.cameraActive is true, warn (and clear allPassed) only when the sensor handle is absent (esp_camera_sensor_get returning nullptr, with a message naming the absent handle); when the camera is inactive, emit the honest skipped-note warning in the same SYS_WARNING shape the neighboring power/communication checks use.
  3. WR-03 (src/main_basestation.cpp): widen appState.ackedAtLastPoll to uint32; at the poll site compare the uint32 counter directly and store it without the truncating casts; change nothing else in the poll block (cadence, lastAckTime assignment, staleness thresholds untouched).
  4. Build both targets (pio run -e esp32-s3-balloon -e esp32-s3-basestation) and run node scripts/verify_protocol_roundtrip.mjs — both green.
  5. Update .planning/WINDOWS.md entries 17-19 per disposition in BOTH copies: fixed entries get status fixed with resolved_by naming this plan's commit and a reason citing code fix + builds 2/2 + harness exit 0, with the bench-exercise status recorded — entry 18 (WR-02) names the 01-27 healthy-boot discriminator as its stageable check; entries 17/19 record their unstageable triggers by name (on-demand PSRAM exhaustion / a 65536-ACK wrap between polls) per the entries 8-14 convention. Waived entries get status waived with the operator's reason verbatim; any deferred entry stays open. Reconcile the front-matter counts.
  6. Commit fix(01-26): review 7d96a98 companion fixes per operator disposition (WINDOWS 17-19).
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation > /tmp/pio-0126.log 2>&1 && grep -qE 'SUCCESS|succeeded' /tmp/pio-0126.log && node scripts/verify_protocol_roundtrip.mjs && for spec in '17|thumbnail still pushes|src/image_tx_manager.cpp' '18|health check skipped|src/main_balloon.cpp' '19|ackedAtLastPoll;|src/main_basestation.cpp'; do id=${spec%%|*}; rest=${spec#*|}; pat=${rest%%|*}; file=${rest#*|}; st=$(awk -v i="\"id\": $id," 'index($0,i){f=1} f&&/"status":/{sub(/^.*"status": *"/,"");sub(/".*$/,"");print;exit}' .planning/WINDOWS.md); if [ "$st" = fixed ]; then grep -qF -- "$pat" "$file" || exit 1; fi; done && ! grep -q "static_cast<uint16_t>(acked)" src/main_basestation.cpp</automated>
  </verify>
  <acceptance_criteria>
  - Every approved fix is source-visible: the WR-01 fall-through with its renamed degradation log (and no early return on the full-buffer allocation failure), the WR-02 honest branch (healthy active camera warns nothing; absent handle warns naming it; inactive camera gets the skipped-note), and the WR-03 uint32 member with both truncating casts gone
  - The oversize-skip branch, thumb-alloc-failure branch, both-empty guard, poll cadence, and staleness thresholds are unchanged; the 01-25 D1 fix survives intact (diff shows no overlap with its sites)
  - Both builds succeed; the harness exits 0
  - Waived findings have zero source changes at their fix sites and their entries carry the operator's reason verbatim
  - Entries 17-19 statuses in table and JSON agree with the dispositions; front-matter counts reconcile
  </acceptance_criteria>
  <done>The three confirmed findings are decided and their dispositions landed: approved fixes green on both targets with the harness passing, ledger entries carrying their evidence-class reasons (entry 18 pointing at its 01-27 bench discriminator), and nothing waived without a reason.</done>
  <reversibility rating="reversible">Three small guarded fixes at named sites; each mirrors an existing branch shape in the same file.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PSRAM exhaustion -> capture degradation ladder | The WR-01 condition: internal memory pressure crossing into the enqueue path's payload-selection decision |
| sensor handle absence -> boot warning channel | The WR-02 trust question: the operator's warning channel must reflect real hardware state, not fabricated failure |
| ACK counter wrap -> LED-truth refresh | The WR-03 edge: a type-narrowing boundary in the operator-facing link-truth signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-26-01 | DoS | WR-01 payload loss under PSRAM exhaustion | medium | mitigate | The fall-through fix itself: memory pressure degrades to thumbnail-only instead of dropping the capture entirely from the airlink |
| T-01-26-02 | Information Spoofing | WR-02 fabricated health-check failure on every healthy boot | medium | mitigate | The honest branch: the warning fires only on the real absent-handle condition; the 01-27 healthy boots are the live discriminator |
| T-01-26-03 | Tampering | WR-03 missed LED-truth refresh at counter wrap | low | mitigate | uint32 end-to-end comparison removes the truncation boundary — the staleness signal refreshes on every ACK arrival |
| T-01-26-04 | Tampering | evidence integrity (dispositions) | medium | mitigate | No waive without a recorded reason (checkpoint gate); entries flip only per their recorded evidence classes, never on absence |
</threat_model>

## Artifacts this phase produces

- WINDOWS entries 17-19 (.planning/WINDOWS.md, both copies) — the 7d96a98 review routing
- Behavioral changes: enqueueCapture full-buffer-failure fall-through (src/image_tx_manager.cpp), honest camera health-check branch (src/main_balloon.cpp), uint32 ackedAtLastPoll (src/main_basestation.cpp appState member + poll site)
- New named log literals: the WR-01 degradation message (full dropped, thumbnail still pushes) and the WR-02 absent-handle / skipped-note warnings
- COVERAGE.md round-#11 re-check line
- .planning/phases/01-command-protocol-control/01-26-SUMMARY.md
- No wire-format changes, no new members besides the widened type, no env vars

<verification>
- Entries 17-19 exist with decided dispositions; counts reconcile; no waive without a reason
- Every approved fix is source-visible and both targets build green with the harness passing
- The 01-25 D1 fix and the protected surfaces (quiet gate, D-05/D-07, wire format) are untouched (diff-scoped)
</verification>

<success_criteria>
- 01-VERIFICATION.md gap 4 is closed: every confirmed 7d96a98 finding lives in the WINDOWS ledger with its evidence class recorded, and the human decision the gap requires is captured at the Task 2 checkpoint
- WR-02's fix carries a stageable discriminator into the 01-27 bench; WR-01/WR-03 ride the same reflash closed on code evidence with their unstageable triggers named
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-26-SUMMARY.md` when done
</output>
