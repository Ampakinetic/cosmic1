---
phase: 01-command-protocol-control
plan: 27
type: execute
wave: 19
depends_on: ["01-25", "01-26"]
files_modified:
  - .planning/phases/01-command-protocol-control/01-UAT.md
  - .planning/WINDOWS.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [CTRL-01, CTRL-02, CTRL-03, CTRL-04, CTRL-06, PRI-02, IMG-02, IMG-03]

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low   # calibration store has 0 samples (factor 1, not applied); one operator bench checkpoint + evidence-ledger updates (mirrors 01-24 sizing)

must_haves:
  truths:
    - statement: "D1 crash-regression acceptance (G-01-10 / WINDOWS 15, the round's blocker): the exact pattern that crashed twice at session 7 — a first-post-boot capture running through enqueue/manifest/chunk/beacon push, then the series-A unspaced burst — completes with ZERO resets across the whole session: no i2cWrite ESP_ERR_INVALID_STATE, no Guru Meditation, no stack-canary panic, no rst:0x7 / rst:0xc anywhere in the balloon console; images' buffers survive to serve their transfers"
      verification: backstop
    - statement: "D2 discriminator (G-01-11 / WINDOWS 16): every boot window in the session console is clean of the busy-hold episode line before the first genuine inbound window request — the line may still fire legitimately during a burst, and those fires must coincide with inbound window traffic by timestamp"
      verification: backstop
    - statement: "G-01-7 series-A acceptance (the truth that has failed four rounds, now on its narrowest named axis): 3 back-to-back unspaced captures on post-fix firmware — 3 ACKs, 3 captures, 6/6 verdicts COMPLETE, zero command timeouts, zero silently absent fulls; the round-#10 discriminator lines ('re-announce budget re-armed', receipt-evidenced class-5 eviction labels, base 'full-pull activation deadline reached') prove the levers engaged or the residual mechanism is named precisely (burst-admission depth is the named fallback)"
      verification: backstop
    - statement: "SC-3 visible-effect pairs (unjudged for SIX rounds): at fixed QVGA, brightness -2 vs +2 judged as a visible pair, plus at least one other class (contrast -2/+2, saturation -2/+2, or quality 10 vs 30) judged as a pair — per-pair judgments recorded in Test 3; with this clause judged, every settings clause has an operator verdict"
      verification: backstop
    - statement: "WR-03 cadence discriminator: with auto-capture enabled at a known interval (20 s), a manual capture resets the schedule — the next interval capture lands one full interval after the MANUAL capture (both 'Captured image ID' timestamps quoted); this also discharges the SC-5 coincidental-reliance advisory (the 01-19 baseline change finally has bench discrimination on current HEAD)"
      verification: backstop
    - statement: "CIF-cycle and QVGA-restore clauses (Test 3's two unexercised session-6 clauses): one SET_RESOLUTION to CIF (wire 8) with both kinds COMPLETE at CIF-class sizing, and one SET_RESOLUTION back to QVGA (wire 6) with the next capture QVGA-class by wire evidence (chunk count/size), zero FB-OVF at every step"
      verification: backstop
    - statement: "Dashboard glance is operator-LOOKED this time (a UI change, d8ba14e gallery-detail resLabel/filesize, has landed since the last looked-at glance): all seven explicit UI-SPEC considerations hold plus the LED-truth backstop — and the new gallery detail fields render"
      verification: backstop
    - statement: "WR-02 bench discriminator (if fixed per 01-26 disposition): every healthy boot in the session logs NO camera health-check failure (the pre-fix behavior warned on every healthy boot); the honest skipped-note appears only when expected"
      verification: backstop
    - statement: "Ledger honesty: G-01-10/G-01-11/G-01-7 and WINDOWS entries 3/15/16/17-19 flip ONLY on operator-observed console/dashboard evidence with verbatim quotes, line numbers, and counts; a failed or crashed series leaves its gap open with the residual mechanism named in the discriminator vocabulary — never a forced close"
      verification: explicit
    - "UI-SPEC lift (explicit): before any command the Last Command panel renders the documented 'No commands yet' copy and every settings form renders a complete documented default value — no form is ever shown unfilled"
    - "UI-SPEC lift (explicit): an in-flight command shows the locked 'Sent' state string immediately after POST; the 1s /status poll advances the panel to a terminal state (status-line model, no spinners/skeletons)"
    - "UI-SPEC lift (explicit): Sent/Acked/Failed/Pending counters advance as commands process; 4-column grid on desktop, 2x2 below 480px"
    - "UI-SPEC lift (explicit): each setting is an independent single-field form — a multi-field partial state cannot occur; forms always display a complete value"
    - "UI-SPEC lift (explicit): the status bar collapses 4->2 columns under the 480px media query; counter cells never truncate; Last Command text wraps within card width — no clipping"
    - "UI-SPEC lift (explicit): numeric readouts only — no singular/plural copy; layout identical at 0, 1, and many"
    - "UI-SPEC lift (explicit): .message and label text wrap at 14px/1.5 inside the card; no ellipsis or truncation anywhere in the Phase 1 UI"
    - statement: "UI-SPEC lift (backstop): terminal 'Timeout' / 'Failed (retry N)' with documented error copy renders after retries are exhausted — held-out verification remains the UAT Test 2 hardware proof (already PASS); during this session the link LED stays truthful while commands hold for channel quiet (no red No-link driven by a hold that is working as designed)"
      verification: backstop
  artifacts:
    - "Consolidated bench session logs (balloon13.log / base8.log naming convention, deviation-tolerant and recorded per the 01-24 lesson) covering the D1 spaced smoke, series A, the D2 boot windows, the WR-03 cadence discriminator, the SC-3 pairs, the CIF/QVGA-restore clauses, and the dashboard glance — retained in the repo root as every prior session was"
    - ".planning/phases/01-command-protocol-control/01-UAT.md — G-01-10/G-01-11 status flips (or honest residuals) with verified_by quotes; G-01-7 flip or rescoped residual; Test 3 note updated with the SC-3-pair, CIF-cycle, QVGA-restore, WR-03, and D1-survival outcomes; Test 4 note carries the cadence discriminator outcome"
    - ".planning/WINDOWS.md — entries 3, 15, 16, and 17-19 (as their dispositions require) updated per evidence; counts reconciled"
    - ".planning/STATE.md — round outcome recorded; /gsd-secure-phase 1 named as the remaining phase-close gate if everything closes"
    - ".planning/phases/01-command-protocol-control/01-27-SUMMARY.md"
  key_links:
    - "Behavior changed on BOTH boards this round (balloon D1/D2 fixes + WR-01/WR-02; base WR-03) — flash both together BEFORE any radio traffic; mixed firmware yields misleading evidence even with the wire format unchanged (the 01-09 severance lesson applied to behavior)"
    - "D1 acceptance <-> the 01-25 debug doc's discriminator section: the session's pass/fail for G-01-10 is read against the discriminator the debug round named (crash-signature absence under the crashing pattern), not just absence of obvious resets — grep the whole balloon console for the crash signatures"
    - "Series-A verdicts <-> the round-#10 levers + the D1 fix: a residual failure must be attributed to its mechanism using the discriminator lines BEFORE any gap is left open — and a crash under series A re-opens D1 with the new evidence appended to the debug doc rather than being read as a transfer defect"
    - "SC-3/WR-03 clauses <-> the protocol steps: both have ridden six rounds because sessions issued zero settings/auto-capture commands — this session's protocol makes those commands explicit numbered steps, not ride-alongs"
  prohibitions:
    - statement: "MUST NOT fabricate image-transfer, command, crash-survival, or config success — closures cite operator-observed finalize/queue/console lines, never code-level reasoning, except where an entry's recorded evidence class (unexercised/unstageable, named BY NAME) explicitly permits the code+builds+harness flip"
      status: flagged
      verification: backstop
    - statement: "MUST NOT mark G-01-10 or G-01-7 resolved on code-level evidence alone — closure requires this bench session's hardware evidence (zero resets under the crashing pattern / 6/6 verdicts)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT flash only one board — both boards' behavior changed this round"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT fix newly observed bench defects inside this plan — route them (WINDOWS ledger / todos / debug docs) as every prior round did; this plan's code work ended at 01-25/01-26"
      status: flagged
      verification: backstop
    - statement: "MUST NOT show protocol or camera state that does not reflect reality (recalled project prohibition, UAT Test 5 tier): no ACK for unexecuted actions, no green link LED while failed, GET_STATUS fields match actual balloon state"
      status: flagged
      verification: backstop
---

<objective>
Operator bench re-verification session #8 of gap-closure round #11: prove the D1 crash-regression fix on hardware (the exact pattern that hard-crashed twice at session 7 must now survive — spaced smoke first, then the series-A unspaced burst), verify the D2 boot-window discriminator is clean, re-run the G-01-7 series-A acceptance the crash blocked, finally judge the two truths that have ridden six rounds (SC-3 visible-effect pairs, WR-03 cadence discriminator) plus Test 3's CIF-cycle/QVGA-restore clauses and an operator-LOOKED dashboard glance — then flip the gap ledgers on evidence.

Purpose: there is currently NO clean bench evidence on HEAD firmware — session 7 died at both first-post-boot capture pushes. The 01-25 debug round's fix and the 01-26 review fixes ride this reflash; the D2 fix restores the evidentiary value of the busy-hold discriminator this session's G-01-7 judgment must watch. The SC-3 pairs and WR-03 discriminator have zero bench coverage across six sessions because no settings/auto-capture commands were issued — this session makes them explicit protocol steps.

Output: one consolidated bench session (both consoles logged); G-01-10/G-01-11/G-01-7 flipped or honestly rescoped in 01-UAT.md; WINDOWS entries 3/15/16 and any bench-exercisable 17-19 updated per evidence; Test 3 judged on its final open clauses; STATE.md records the outcome and the remaining phase-close gate (/gsd-secure-phase 1).

Spec-less probe fallback: recorded visible skip — phase_req_ids null this run, no probe predicates generated (standard goal-backward verification, the established determination).
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/phases/01-command-protocol-control/01-25-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-26-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-24-SUMMARY.md

Binding decisions and bench conventions:
- House evidence convention: verbatim console quotes with line numbers and counts, both consoles retained (balloon13.log / base8.log naming request; the 01-24 deviation lesson — operator-retained names are recorded in the ledgers, never renamed).
- Session-7 crash baseline to beat (balloon.log/base.log): both first-post-boot capture pushes hard-crashed (crash 1 :301-302 + :313-314, rst:0xc :377-380; crash 2 :526-528 rst:0x7), 0/6 series-A verdicts, forbidden CAPTURE_NOW terminals base.log:141/:143, every image-36 window request rejected. The D1 pass bar is the ABSENCE of every crash signature under the same pattern: i2cWrite ESP_ERR_INVALID_STATE, Guru Meditation, stack-canary watchpoint, rst:0x7, rst:0xc.
- D2 baseline to beat: three boots, three spurious busy-hold episode fires (:135/:504/:652, each seconds after System ready). Post-fix pass bar: every boot window clean before the first genuine inbound window request.
- Discriminator lines to watch: round-#10 levers (balloon 're-announce budget re-armed', receipt-evidenced class-5 eviction labels; base 'full-pull activation deadline reached') plus whatever discriminator the 01-25 debug doc named for its fix (read 01-25-SUMMARY.md before the session) and the WR-02 healthy-boot line (01-26 disposition permitting).
- SC-3 pairs protocol: brightness -2 vs +2 plus one other class (contrast -2/+2, saturation -2/+2, or quality 10 vs 30 — quality changes file size visibly); judge at fixed QVGA; the queue's SET commands ACK; the pair comparison is the operator judgment; thumbnails suffice unless the full is needed (note which).
- WR-03 discriminator protocol: AUTO_CAPTURE_ENABLE at 20 s, one interval capture, one manual CAPTURE_NOW, then the NEXT interval capture must land ~20 s after the manual (quote both 'Captured image ID' timestamps); disable and confirm zero further automatic captures.
- CIF/QVGA-restore clauses: SET_RESOLUTION 8 (CIF) then 6 (QVGA) — session 5's wire-evidence pattern (CIF full ~31 chunks vs QVGA ~21; post-restore capture QVGA-class by chunk count) is the judging template; zero FB-OVF at every step is the G-01-8 regression watch.
- WR-01 battery watch: no 'Critical battery' lines; beacons batt=valid (all prior sessions green).
- UAT Test 3 is the 'issue' holder; Tests 1/2/4/5 stand PASS and must not regress (Test 4's 'exact cadence' expectation is unaffected by the baseline change: interval captures still fire at exactly the commanded interval from the last capture of ANY source).
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Bench session #8 — flash both boards, D1 spaced smoke then series A, D2 boot windows, WR-03 cadence discriminator, SC-3 visible-effect pairs, CIF cycle + QVGA restore, dashboard LOOK glance</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-10/G-01-11 gap blocks (the crash signatures and D2 boot-epoch evidence), G-01-7 gap block (series-A acceptance + round-#10 discriminators), Test 3/Test 4 notes (the riding clauses)
  - .planning/debug/d1-crash-regression-push-start.md — the root cause, the fix, and the discriminator section (what this session reads to confirm the D1 fix)
  - .planning/phases/01-command-protocol-control/01-25-SUMMARY.md + 01-26-SUMMARY.md — what changed and the new log lines to expect (including any instrumentation 01-25 shipped)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — Gaps Summary + behavior_unverified_items + coincidental_reliance_items (the exact truths this session must exercise)
  </read_first>
  <what-built>
  Plans 01-25 (D1 crash-regression fix per the debug round's ranked root cause + D2 receipt-ever stamp fix) and 01-26 (review 7d96a98 routing + operator-dispositioned companion fixes WR-01/WR-02/WR-03) — all built green on both targets, harness green.
  </what-built>
  <how-to-verify>
  1. PRE-FLIGHT: pio run -e esp32-s3-balloon -e esp32-s3-basestation (2 succeeded) and node scripts/verify_protocol_roundtrip.mjs (exit 0) on the round's final commit. Flash BOTH boards with that exact firmware (behavior changed on both sides). Start both serial consoles logging to balloon13.log / base8.log.
  2. D2 BOOT CHECK (before any command): watch the balloon's boot window — record whether the busy-hold episode line appears before any inbound window traffic (post-fix expectation: it does not). Repeat the check at every reboot in the session.
  3. D1 SPACED SMOKE (the crashing pattern, gently): trigger ONE CAPTURE_NOW as the first post-boot capture — the exact sequence that crashed twice at session 7 (capture, enqueue, thumbnail manifest, chunks, beacon start). Wait for both finalize lines (thumb + full COMPLETE). Watch the balloon console for every crash signature (i2cWrite ESP_ERR_INVALID_STATE, Guru Meditation, stack canary, rst:0x7/0xc) — ZERO occurrences is the pass bar. If it crashes: capture the dump verbatim (it is new D1 evidence — the debug doc gets it appended, do not force anything), stop the session, and report.
  4. SERIES A (G-01-7 acceptance, fifth attempt at this truth): from the dashboard, trigger 3 CAPTURE_NOW commands back-to-back WITHOUT waiting. Expected: 3 ACKs (quiet-gate holds may precede them — a held command must NEVER reach 'timeout after 3 retries'); 3 captures; 6/6 finalize lines COMPLETE; every full either transfers, re-announces-and-transfers, or (worst case) is drop-logged by name; watch the round-#10 discriminator lines plus the 01-25 fix's own discriminator — their presence with a passing series proves the levers engaged; their ABSENCE with a passing series means the natural cadence sufficed (also fine — record which). Any crash under series A re-opens D1 with the new evidence (append to the debug doc) rather than reading as a transfer defect.
  5. WR-03 CADENCE DISCRIMINATOR: AUTO_CAPTURE_ENABLE at 20 s; let one interval capture fire; trigger a manual CAPTURE_NOW; verify the NEXT interval capture lands ~20 s AFTER the manual trigger (quote both 'Captured image ID' timestamps from the balloon console). Then AUTO_CAPTURE_DISABLE and confirm zero further automatic captures.
  6. SC-3 VISIBLE-EFFECT PAIRS (sixth round riding — this session MUST judge them): at fixed QVGA — (a) SET_BRIGHTNESS -2, capture; SET_BRIGHTNESS +2, capture; judge the pair visibly different and record the verdict; (b) one other class: contrast -2 vs +2, saturation -2 vs +2, or quality 10 vs 30; capture a pair and judge. Record per-pair verdicts (note whether thumbnails sufficed for judgment).
  7. CIF CYCLE + QVGA RESTORE (Test 3's unexercised clauses): SET_RESOLUTION 8 (CIF) — expect the re-init log, a capture with both kinds COMPLETE at CIF-class sizing, zero FB-OVF; then SET_RESOLUTION 6 (QVGA) — expect ACK plus a subsequent capture at QVGA-class sizing by wire evidence (chunk count ~21 vs CIF ~31), both kinds COMPLETE.
  8. DASHBOARD LOOK GLANCE (operator-LOOKED, not did-not-flag — d8ba14e changed the UI since the last looked-at glance): 'No commands yet' before the first command; 'Sent' immediately after each POST advancing via the poll; counters advance; link LED stays truthful (green while chunks/holds flow); status bar collapses 2x2 under 480px; no truncated/clipped text; gallery detail renders the resLabel/filesize fields.
  9. Record everything: verbatim quotes with line numbers for every verdict, hold, re-announce, drop, deadline-release, retry, and crash-signature grep (count them — zero is a result worth quoting); before/after tallies vs the session-7 baseline. If ANY series fails: capture the discriminating lines, leave that gap open — do not force a close.
  </how-to-verify>
  <acceptance_criteria>
  - D1: the spaced smoke AND series A complete with zero crash signatures anywhere in the balloon console (grep counts quoted: i2cWrite errors, Guru Meditation, stack canary, rst:0x7, rst:0xc each zero); both smoke and series-A images' transfers serve from surviving buffers
  - D2: every boot window clean of the busy-hold episode line before the first genuine inbound window request; any legitimate burst-time fires coincide with inbound window traffic by timestamp
  - Series A: 3/3 ACKs, 3/3 captures, 6/6 COMPLETE verdicts, zero command timeouts, zero silently absent fulls; round-#10 and 01-25 discriminators present or their absence explained
  - WR-03: next interval capture lands one full interval after the manual capture — both timestamps quoted; disable stops automatic captures
  - SC-3: brightness pair judged plus one other class judged, per-pair verdicts recorded (Test 3's final open settings clauses)
  - CIF cycle and QVGA restore: both-kinds COMPLETE at the expected size classes, zero FB-OVF at every step
  - Dashboard glance: all seven explicit UI-SPEC considerations hold plus the LED-truth backstop, and the gallery detail resLabel/filesize fields render
  - Both console logs retained (balloon13.log / base8.log or the operator's chosen names, recorded) with every cited quote locatable by line number
  </acceptance_criteria>
  <resume-signal>Type "approved" with the console excerpts (crash-signature grep counts, series-A verdicts + discriminator lines, D2 boot-window observations, WR-03 timestamps, SC-3 pair judgments, CIF/QVGA-restore evidence, dashboard glance verdicts), or describe the failing series with its discriminating log lines (a crash appends to the debug doc and re-opens D1).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Flip the ledgers on evidence — 01-UAT.md (G-01-10/G-01-11/G-01-7, Test 3/Test 4), WINDOWS entries 3/15/16/17-19, STATE.md</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md, .planning/WINDOWS.md, .planning/STATE.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-UAT.md — the G-01-10/G-01-11/G-01-7 gap blocks, Test 3 note (SC-3/CIF/QVGA clauses), Test 4 note (WR-03 rides); house quote convention from the G-01-8/G-01-9 resolutions
  - .planning/WINDOWS.md — entries 3, 15, 16, and 17-19 as dispositioned by 01-26 (table + JSON copies must both update; counts in front matter reconcile)
  - .planning/STATE.md — Current Status / Next Steps (the round outcome and the /gsd-secure-phase 1 gate)
  - The operator's Task 1 evidence (approved resume message with console excerpts, plus the retained session logs)
  </read_first>
  <action>
  Update the three ledgers strictly from the Task 1 evidence:
  1. 01-UAT.md G-01-10: if the D1 pass bar held (zero crash signatures across the spaced smoke AND series A), set status resolved with resolved_by naming the 01-25 fix (root cause + lever from the debug doc), resolved_at, and verified_by quoting the operator-observed crash-signature grep counts (zero each) plus the surviving-transfer evidence (match the G-01-8/G-01-9 resolution structure). If anything crashed, keep status open, append the new dump evidence to .planning/debug/d1-crash-regression-push-start.md, and extend root_cause with what the new evidence discriminates.
  2. 01-UAT.md G-01-11: flip resolved on the clean boot windows (quote each boot window's lines); otherwise keep open with the residual named.
  3. 01-UAT.md G-01-7: if series A passed 6/6 per the acceptance criteria, set status resolved with resolved_by naming the lever chain (01-21 receipt-evidence re-announce + eviction ranking; 01-22 full-arm deadline; 01-18 quiet gate; the 01-25 fix restoring the crashing path) and verified_by quoting the verdicts, the zero-timeout evidence, and the discriminator lines that prove engagement. If it failed, keep status open and extend root_cause with the mechanism the discriminators named (burst-admission depth / IMG_TX_QUEUE_DEPTH is the named fallback for persistent eviction pressure).
  4. 01-UAT.md Test 3 note: record the D1 survival outcome, the SC-3 per-pair verdicts, the CIF-cycle and QVGA-restore outcomes; flip Test 3 to pass only if every clause it holds is now judged and none failed. Test 4 note: record the WR-03 cadence discriminator outcome (this discharges the SC-5 coincidental-reliance advisory from 01-VERIFICATION.md).
  5. WINDOWS.md: entry 15 (D1) and entry 16 (D2) flip on the session evidence or stay open with the residual; entry 3 (G-01-7) per its outcome; entries 17-19 — entry 18 (WR-02) appends its healthy-boot bench evidence if fixed, and any other bench-exercised entry appends its line; waived/unexercised entries are untouched unless the bench contradicts their recorded reason (then flag, do not silently flip). Update BOTH the markdown table and the JSON copy; reconcile the counts in the front matter.
  6. STATE.md: record the round outcome in Current Status (what closed on what evidence, what remains open if anything), and set Next Steps to the security gate (/gsd-secure-phase 1; Phase 2/3 gates also pending) as the remaining phase-close step if everything closed, then /gsd-progress.
  7. Commit the ledger updates separately from any log retention, message docs(01-27): bench re-verification evidence + ledger flips.
  </action>
  <verify>
  <automated>grep -q "01-27" .planning/STATE.md && grep -q "secure-phase" .planning/STATE.md && grep -c "G-01-10" .planning/phases/01-command-protocol-control/01-UAT.md | grep -qv "^0$" && grep -q '"id": 15' .planning/WINDOWS.md && grep -q '"id": 16' .planning/WINDOWS.md</automated>
  </verify>
  <acceptance_criteria>
  - Every bench-evidence flip cites operator-observed quotes with line numbers/counts from the retained session logs; failed series leave their gap open with the residual mechanism named in the discriminator vocabulary
  - WINDOWS.md table and JSON copies agree; front-matter counts equal the actual row statuses
  - Session-8 evidence lands in the ledgers themselves whichever way each flip resolved (resolved quotes or residual-mechanism quotes both satisfy the citation check)
  - Test 3 reflects the D1-survival, SC-3-pair, CIF-cycle, and QVGA-restore outcomes; Test 4's note carries the WR-03 discriminator outcome; Tests 1/2/5 notes untouched absent regression evidence
  - STATE.md names /gsd-secure-phase 1 as the remaining phase-close gate
  </acceptance_criteria>
  <done>The gap ledgers reflect hardware truth: the crash regression closed on zero-reset evidence or honestly re-opened with new dump evidence, D2's discriminator truthful at boot, G-01-7 closed on 6/6 series evidence or honestly rescoped with its next lever, Test 3 judged on its final clauses, and the phase's remaining gate identified.</done>
  <reversibility rating="reversible">Documentation-only ledger updates.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench operator -> firmware flash + consoles | Human performs the flash and observes; logs are retained as evidence (read-only artifacts) |
| RF pair -> bench evidence | The two-radio link under bench conditions produces the verdicts; no untrusted input enters the firmware beyond the CRC16-validated frames covered in 01-25/01-26 threat models |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-27-01 | Tampering | evidence integrity | medium | mitigate | House convention enforced: closures quote verbatim console lines with line numbers/counts from retained logs; crash-signature grep counts are quoted as zero-results, not assumed |
| T-01-27-02 | DoS | mixed-firmware flash invalidating the evidence | high | mitigate | Blocking checkpoint step 1 flashes BOTH boards before any traffic; behavior changes on both sides make a mixed pair's evidence misleading (recorded as a key_link prohibition) |
| T-01-27-03 | DoS | a crash mid-session misread as a transfer defect | medium | mitigate | Protocol step 4 names the re-open path: any crash under series A appends its dump to the debug doc and re-opens D1 — never silently attributed to G-01-7 |
| T-01-27-04 | Safety | bench hardware | low | accept | Standard bench rig (USB-powered base, battery balloon at desk); WR-01 battery watch green in all prior sessions; nothing in this round changes power paths |
</threat_model>

## Artifacts this phase produces

- balloon13.log / base8.log (or operator-chosen names, recorded) — retained bench console evidence (repo root, prior-session convention)
- Updated gap blocks G-01-10/G-01-11/G-01-7 + Test 3/Test 4 notes (01-UAT.md)
- Updated WINDOWS entries 3, 15, 16, and bench-exercisable 17-19 with reconciled counts (.planning/WINDOWS.md)
- Round outcome + next-gate record (.planning/STATE.md)
- .planning/phases/01-command-protocol-control/01-27-SUMMARY.md
- No source files, no new symbols, no env vars

<verification>
- Both firmware targets built green and the harness exited 0 immediately before the flash (pre-flight step, on record in the session)
- Every ledger flip traces to a quotable line in the retained logs or carries its explicit evidence-class reason
- WINDOWS counts reconcile with row statuses; /gsd-ship remains blocked exactly as long as an entry is honestly open
</verification>

<success_criteria>
- The round's acceptance is judged on hardware: the crashing pattern survives with zero resets (or D1 re-opens with new dump evidence), the D2 boot windows are clean, series A reaches 6/6 verdicts (or the honest residual is named with its discriminating evidence), SC-3 pairs and the WR-03 discriminator finally judged, CIF/QVGA-restore clauses exercised, dashboard LOOKED
- With G-01-10/G-01-11/G-01-7 closed, the WINDOWS ledger reaches zero open entries and the phase's remaining close gate is the security review (/gsd-secure-phase 1)
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-27-SUMMARY.md` when done
</output>
