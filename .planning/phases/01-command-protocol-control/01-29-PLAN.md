---
phase: 01-command-protocol-control
plan: 29
type: execute
wave: 21
depends_on: ["01-28"]
files_modified:
  - .planning/phases/01-command-protocol-control/01-UAT.md
  - .planning/WINDOWS.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [CTRL-01, CTRL-02, CTRL-03, CTRL-04, CTRL-06, PRI-02, IMG-02, IMG-03]

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low   # calibration store has 0 samples (factor 1, not applied); one operator bench checkpoint + evidence-ledger updates (mirrors 01-27 sizing)

must_haves:
  truths:
    - statement: "D1 crash-regression acceptance on the round-#12 firmware (G-01-10 / WINDOWS 15, the phase's top blocker): the spaced smoke AND the SUSTAINED FULL-SERVICE phase — one capture whose full spans at least seven windows (SVGA-class ~137 chunks reproduces session 8's image-38 pattern exactly; any multi-window full qualifies if SVGA is unavailable) serving EVERY window to COMPLETE — AND series A all complete with ZERO crash signatures anywhere in the balloon console (i2cWrite ESP_ERR_INVALID_STATE adjacent to a failure, Guru Meditation, stack canary, rst:0x7, rst:0xc each grep-count zero); the 01-28 fix's §7-named discriminator lines are quoted proving the lever engaged (or their absence explained)"
      verification: backstop
    - statement: "G-01-7 series-A acceptance (eighth attempt at this truth): 3 back-to-back unspaced captures — 3 ACKs (held transmits must never reach 'timeout after 3 retries'), 3 captures, 6/6 verdicts COMPLETE, zero command timeouts, zero silently absent fulls, with the round-#10 discriminator lines ('re-announce budget re-armed', receipt-evidenced class-5 eviction labels, base 'full-pull activation deadline reached') proving lever engagement or the residual mechanism named precisely"
      verification: backstop
    - statement: "SC-3 visible-effect pairs (SEVENTH round riding — this session MUST judge them): at fixed QVGA, brightness -2 vs +2 judged as a visible pair, plus at least one other class (contrast -2/+2, saturation -2/+2, or quality 10 vs 30) judged as a pair — per-pair verdicts recorded in Test 3"
      verification: backstop
    - statement: "WR-03 cadence discriminator (seventh round riding): AUTO_CAPTURE_ENABLE at 20 s, one interval capture, one manual CAPTURE_NOW, the next interval capture lands one full interval (~20 s) after the MANUAL capture — both 'Captured image ID' timestamps quoted; disable confirmed; this discharges the SC-5 coincidental-reliance advisory (the 01-19 baseline change finally bench-discriminated on current HEAD)"
      verification: backstop
    - statement: "CIF-cycle (SET_RESOLUTION wire 8, as specified — session 8's wire-10 deviation finally corrected) with both kinds COMPLETE at CIF-class sizing, and QVGA restore (wire 6) with the next capture QVGA-class by wire evidence (chunk count ~21 vs CIF ~31), zero FB-OVF at every step (the G-01-8 regression watch)"
      verification: backstop
    - statement: "Dashboard glance is operator-LOOKED (d8ba14e gallery-detail resLabel/filesize still unlooked-at after four sessions): all seven explicit UI-SPEC considerations hold plus the LED-truth backstop — and the gallery detail fields render"
      verification: backstop
    - statement: "Ledger honesty: G-01-10/G-01-7 and WINDOWS entries 15/3 flip ONLY on operator-observed console/dashboard evidence with verbatim quotes, line numbers, and counts; a crashed or failed session leaves its gaps open with the residual named, records requirements-completed: [] (the 01-24/01-27 failed-session convention), and any crash appends its dump to the debug doc and re-opens D1 — never a forced close; STATE.md names /gsd-secure-phase 1 as the remaining phase-close gate"
      verification: explicit
    - "UI-SPEC lift (explicit): before any command the Last Command panel renders the documented 'No commands yet' copy and every settings form renders a complete documented default value — no form is ever shown unfilled"
    - "UI-SPEC lift (explicit): an in-flight command shows the locked 'Sent' state string immediately after POST; the 1s /status poll advances the panel to a terminal state (status-line model, no spinners/skeletons)"
    - "UI-SPEC lift (explicit): Sent/Acked/Failed/Pending counters advance as commands process; 4-column grid on desktop, 2x2 below 480px"
    - "UI-SPEC lift (explicit): each setting is an independent single-field form — a multi-field partial state cannot occur; forms always display a complete value"
    - "UI-SPEC lift (explicit): the status bar collapses 4->2 columns under the 480px media query; counter cells never truncate; Last Command text wraps within card width — no clipping"
    - "UI-SPEC lift (explicit): numeric readouts only — no singular/plural copy; layout identical at 0, 1, and many"
    - "UI-SPEC lift (explicit): .message and label text wrap at 14px/1.5 inside the card; no ellipsis or truncation anywhere in the Phase 1 UI"
    - statement: "UI-SPEC lift (backstop): terminal 'Timeout' / 'Failed (retry N)' with documented error copy renders after retries are exhausted — held-out verification remains the UAT Test 2 hardware proof (already PASS); during this session the link LED stays truthful while commands hold for channel quiet (no red No-link driven by a hold that is working as designed)"
      verification: backstop
  artifacts:
    - "Consolidated bench session logs (balloon14.log / base9.log naming request, deviation-tolerant and recorded per the 01-24 convention) covering the D1 sustained-service phase, series A, the WR-03 cadence discriminator, the SC-3 pairs, the CIF/QVGA-restore clauses, and the dashboard glance — retained in the repo root as every prior session was"
    - ".planning/phases/01-command-protocol-control/01-UAT.md — G-01-10 flip (zero-crash evidence incl. sustained service) or honest re-open with the new dump appended to the debug doc; G-01-7 flip (6/6 + discriminators) or named residual; Test 3 note updated with the SC-3-pair, CIF-cycle, QVGA-restore, and D1-survival outcomes; Test 4 note carries the cadence discriminator outcome"
    - ".planning/WINDOWS.md — entries 15 (D1) and 3 (G-01-7) updated per evidence, both copies (table + JSON), counts reconciled"
    - ".planning/STATE.md — round outcome recorded; /gsd-secure-phase 1 named as the remaining phase-close gate if everything closes"
    - ".planning/phases/01-command-protocol-control/01-29-SUMMARY.md"
  key_links:
    - "Behavior changed on the balloon this round (the 01-28 fix; base-side only if §7 named a base site — flash BOTH boards regardless, the standing both-boards rule; mixed firmware yields misleading evidence even with the wire format unchanged)"
    - "D1 acceptance <-> §7's discriminator: the session's pass/fail for G-01-10 reads BOTH the crash-signature greps AND the 01-28 fix's own discriminator lines — absence of resets with the discriminator never engaging means the lever was never exercised, not that it works"
    - "Series-A verdicts <-> the round-#10 levers + the D1 fix: a crash under series A re-opens D1 with the evidence appended to the debug doc (§8) rather than being read as a transfer defect; a non-crash failure is attributed via the discriminator lines BEFORE any gap is left open"
    - "SC-3/WR-03/CIF clauses <-> the numbered protocol steps: all have ridden seven rounds because sessions issued zero settings/auto-capture commands — this session makes them explicit steps 5-7, not ride-alongs"
  prohibitions:
    - statement: "MUST NOT fabricate image-transfer, command, crash-survival, or settings-verdict success — closures cite operator-observed finalize/queue/console/dashboard lines with line numbers and counts, never code-level reasoning"
      status: flagged
      verification: backstop
    - statement: "MUST NOT mark G-01-10 or G-01-7 resolved on code-level evidence alone — closure requires this session's hardware evidence (zero resets under the sustained-service pattern / 6/6 verdicts)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT flash only one board — both boards reflash before any radio traffic"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT fix newly observed bench defects inside this plan — route them (WINDOWS ledger / todos / debug docs) as every prior round did; this plan's code work ended at 01-28"
      status: flagged
      verification: backstop
    - statement: "MUST NOT show protocol or camera state that does not reflect reality (recalled project prohibition, UAT Test 5 tier): no ACK for unexecuted actions, no green link LED while failed, GET_STATUS fields match actual balloon state"
      status: flagged
      verification: backstop
---

<objective>
Operator bench re-verification session #9 of gap-closure round #12: prove the D1 round-#12 fix on hardware against BOTH crash classes — the spaced smoke (session-7's killer), the SUSTAINED FULL-WINDOW SERVICE pattern (session-8's killer: a multi-window full serving to completion, ideally SVGA-class ~137 chunks), and the series-A unspaced burst — re-run the G-01-7 series-A acceptance, finally judge the truths that have ridden seven rounds (SC-3 visible-effect pairs, WR-03 cadence discriminator, CIF-cycle-as-specified + QVGA restore), take the operator-LOOKED dashboard glance, then flip the gap ledgers on evidence.

Purpose: there is still NO clean bench session on HEAD — session 8 proved the first-post-boot class dead but died at the seventh window of a sustained FULL service, showing the crash class is not confined to bursts (session-8 captures were spaced). Every riding clause accumulates behind this one session; with D1 closed, the phase's remaining gate is the security review.

Output: one consolidated bench session (both consoles logged); G-01-10/G-01-7 flipped or honestly re-opened in 01-UAT.md; WINDOWS entries 15/3 updated per evidence with counts reconciled; Test 3 judged on its final open clauses; Test 4's note carries the WR-03 outcome (discharging the SC-5 advisory); STATE.md records the outcome and the /gsd-secure-phase 1 gate.

Spec-less probe fallback: recorded visible skip — phase_req_ids null this run, no probe predicates generated (standard goal-backward verification, the established determination).
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/debug/d1-crash-regression-push-start.md
@.planning/phases/01-command-protocol-control/01-28-SUMMARY.md
@.planning/phases/01-command-protocol-control/01-27-SUMMARY.md
@.planning/WINDOWS.md

Binding decisions and bench conventions:
- House evidence convention: verbatim console quotes with line numbers and counts, both consoles retained (balloon14.log / base9.log naming request; the 01-24 deviation lesson — operator-retained names are recorded in the ledgers, never renamed).
- Session-8 crash baseline to beat (balloon2.log/base2.log): the spaced smoke SURVIVED (image 37 both kinds COMPLETE) but the balloon reset at the FIRST CHUNK of the SEVENTH re-armed FULL window of the SECOND image (~4 min in — :1220 armed 96..109 → :1227 chunk 1/14 sent → :1228-1231 rst:0x7, Saved PC 0x40376430 = tick_hook). The D1 pass bar this session: BOTH crash classes dead — first-post-boot push clean AND a multi-window sustained full serving EVERY window to COMPLETE — with zero crash signatures (i2cWrite ESP_ERR_INVALID_STATE adjacent to a failure, Guru Meditation, stack-canary watchpoint, rst:0x7, rst:0xc) across the whole console, AND the 01-28 fix's §7-named discriminator lines engaged.
- Discriminator lines to watch: the 01-28 fix's own §7-named lines (read §7 + 01-28-SUMMARY.md before the session) plus the round-#10 levers (balloon 're-announce budget re-armed', receipt-evidenced class-5 eviction labels; base 'full-pull activation deadline reached') and the D2-fixed 're-announce held' line (now truthful by construction — fires only on genuine inbound traffic).
- SC-3 pairs protocol: brightness -2 vs +2 plus one other class (contrast -2/+2, saturation -2/+2, or quality 10 vs 30 — quality changes file size visibly); judge at fixed QVGA; the queue's SET commands ACK; the pair comparison is the operator judgment; thumbnails suffice unless the full is needed (note which).
- WR-03 discriminator protocol: AUTO_CAPTURE_ENABLE at 20 s, one interval capture, one manual CAPTURE_NOW, the NEXT interval capture must land ~20 s after the manual (quote both 'Captured image ID' timestamps); disable and confirm zero further automatic captures.
- CIF/QVGA-restore clauses: SET_RESOLUTION 8 (CIF — wire 8, as specified, correcting session 8's wire-10 deviation) then 6 (QVGA) — session 5's wire-evidence pattern (CIF full ~31 chunks vs QVGA ~21) is the judging template; zero FB-OVF at every step is the G-01-8 regression watch.
- WR-01 battery watch: no 'Critical battery' lines; beacons batt=valid (all prior sessions green). WR-02 watch: zero 'health check failed' lines at healthy boots (bench-verified at session 8; regression watch only).
- UAT Test 3 is the 'issue' holder; Tests 1/2/4/5 stand PASS and must not regress. The interim spacing discipline (avoid unspaced bursts) applies UNTIL the D1 sustained-service + series-A steps pass — only then is a burst legitimate.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Bench session #9 — flash both boards, D1 spaced smoke + sustained FULL-window service + series A, WR-03 cadence discriminator, SC-3 visible-effect pairs, CIF cycle (wire 8) + QVGA restore, dashboard LOOK glance</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md</files>
  <read_first>
  - .planning/debug/d1-crash-regression-push-start.md — §6 (the session-8 crash pattern this session must beat) and §7 (the round-#12 root cause, the fix, and THIS session's discriminator lines)
  - .planning/phases/01-command-protocol-control/01-28-SUMMARY.md — what changed, the touched sites, and the new log lines to expect
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-10 gap block (the crash signatures + the sustained-service missing clause), G-01-7 gap block (series-A acceptance + round-#10 discriminators), Test 3/Test 4 notes (the riding clauses)
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — Gaps Summary + behavior_unverified_items + coincidental_reliance_items (the exact truths this session exercises)
  </read_first>
  <what-built>
  Plan 01-28: the D1 round-#12 fix at the §7-named site with its discriminator lines, plus the audit record — built green on both targets, harness exit 0, no bench claims made.
  </what-built>
  <how-to-verify>
  1. PRE-FLIGHT: pio run -e esp32-s3-balloon -e esp32-s3-basestation (2 succeeded) and node scripts/verify_protocol_roundtrip.mjs (exit 0) on the round's final commit. Flash BOTH boards with that exact firmware (balloon behavior changed; base reflash is the standing both-boards rule). Start both serial consoles logging to balloon14.log / base9.log.
  2. D1 SPACED SMOKE (session-7's killer, proven dead at session 8 — quick regression check): ONE CAPTURE_NOW as the first post-boot capture; wait for both finalize lines (thumb + full COMPLETE); zero crash signatures.
  3. D1 SUSTAINED FULL SERVICE (session-8's killer — THE new step): let the smoke image's full (or a second spaced capture's full) serve to completion across MULTIPLE windows — ideally SVGA-class (~137 chunks, ≥7 windows, the exact image-38 pattern; SET_RESOLUTION wire 10 first if needed, then revert per step 7). Watch every window arm → chunks → completion with zero crash signatures; quote how many windows served and the finalize line. This step alone does not pass D1 — it must survive steps 4-8 too.
  4. SERIES A (G-01-7 acceptance, eighth attempt): 3 CAPTURE_NOW back-to-back WITHOUT waiting. Expected: 3 ACKs (quiet-gate holds may precede; a held command must NEVER reach 'timeout after 3 retries'), 3 captures, 6/6 finalize lines COMPLETE, every full transferred / re-announced-and-transferred / drop-logged by name; the round-#10 discriminator lines present with a passing series proves the levers engaged; ABSENCE with a passing series means the natural cadence sufficed (also fine — record which). Any crash appends the dump to the debug doc (§8) and re-opens D1 — never read as a transfer defect.
  5. WR-03 CADENCE DISCRIMINATOR: AUTO_CAPTURE_ENABLE at 20 s; one interval capture; one manual CAPTURE_NOW; verify the NEXT interval capture lands ~20 s AFTER the manual trigger (quote both 'Captured image ID' timestamps); AUTO_CAPTURE_DISABLE; zero further automatic captures.
  6. SC-3 VISIBLE-EFFECT PAIRS (seventh round riding — this session MUST judge them): at fixed QVGA — (a) SET_BRIGHTNESS -2, capture; SET_BRIGHTNESS +2, capture; judge the pair visibly different and record the verdict; (b) one other class: contrast -2/+2, saturation -2/+2, or quality 10 vs 30; capture and judge. Note per pair whether thumbnails sufficed.
  7. CIF CYCLE + QVGA RESTORE (as specified this time): SET_RESOLUTION 8 (CIF) — expect the re-init log, both kinds COMPLETE at CIF-class sizing (~31 chunks), zero FB-OVF; then SET_RESOLUTION 6 (QVGA) — ACK plus a subsequent capture at QVGA-class sizing (~21 chunks), both kinds COMPLETE.
  8. DASHBOARD LOOK GLANCE (operator-LOOKED, not did-not-flag): 'No commands yet' before the first command; 'Sent' immediately after each POST advancing via the poll; counters advance; link LED stays truthful (green while chunks/holds flow); status bar collapses 2x2 under 480px; no truncated/clipped text; gallery detail renders the resLabel/filesize fields.
  9. Record everything: verbatim quotes with line numbers for every verdict, hold, re-announce, drop, deadline-release, retry, discriminator, and the crash-signature grep counts across the WHOLE balloon console (i2cWrite adjacent-to-failure, Guru Meditation, stack canary, rst:0x7, rst:0xc — count each; zero is a result worth quoting); the 01-28 discriminator lines and their contexts. If ANY step fails: capture the discriminating lines, leave that gap open — do not force a close.
  </how-to-verify>
  <acceptance_criteria>
  - D1: the spaced smoke, the sustained multi-window full service, AND series A complete with zero crash signatures anywhere in the balloon console (grep counts quoted for each signature); the 01-28 fix's §7-named discriminator lines engaged and quoted (or their absence explained)
  - Series A: 3/3 ACKs, 3/3 captures, 6/6 COMPLETE verdicts, zero command timeouts, zero silently absent fulls; round-#10 discriminators present or their absence explained
  - WR-03: next interval capture lands one full interval after the manual capture — both timestamps quoted; disable stops automatic captures
  - SC-3: brightness pair judged plus one other class judged, per-pair verdicts recorded
  - CIF cycle (wire 8) and QVGA restore: both-kinds COMPLETE at the expected size classes, zero FB-OVF at every step
  - Dashboard glance: all seven explicit UI-SPEC considerations hold plus the LED-truth backstop, and the gallery detail resLabel/filesize fields render
  - Both console logs retained (balloon14.log / base9.log or the operator's chosen names, recorded) with every cited quote locatable by line number
  </acceptance_criteria>
  <resume-signal>Type "approved" with the console excerpts (crash-signature grep counts, sustained-service window count + finalize, series-A verdicts + discriminator lines, WR-03 timestamps, SC-3 pair judgments, CIF/QVGA-restore evidence, dashboard glance verdicts), or describe the failing step with its discriminating log lines (a crash appends to the debug doc and re-opens D1).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Flip the ledgers on evidence — 01-UAT.md (G-01-10/G-01-7, Test 3/Test 4), WINDOWS entries 15/3, STATE.md</name>
  <files>.planning/phases/01-command-protocol-control/01-UAT.md, .planning/WINDOWS.md, .planning/STATE.md</files>
  <read_first>
  - .planning/phases/01-command-protocol-control/01-UAT.md — the G-01-10/G-01-7 gap blocks, Test 3 note (SC-3/CIF/QVGA clauses), Test 4 note (WR-03 rides); house quote convention from the G-01-8/G-01-9/G-01-11 resolutions
  - .planning/WINDOWS.md — entries 15 and 3 (table + JSON copies must both update; counts in front matter reconcile)
  - .planning/STATE.md — Current Status / Next Steps (the round outcome and the /gsd-secure-phase 1 gate)
  - The operator's Task 1 evidence (approved resume message with console excerpts, plus the retained session logs)
  </read_first>
  <action>
  Update the three ledgers strictly from the Task 1 evidence:
  1. 01-UAT.md G-01-10: if the D1 pass bar held (zero crash signatures across the spaced smoke AND sustained multi-window service AND series A, with the 01-28 fix's discriminator engaged), set status resolved with resolved_by naming the round-#12 fix (root cause + lever from debug doc §7), resolved_at, and verified_by quoting the operator-observed crash-signature grep counts (zero each), the sustained-service window count + finalize line, and the discriminator quotes (match the G-01-8/G-01-9/G-01-11 resolution structure). If anything crashed, keep status open, append the new dump evidence to the debug doc (§8), and extend root_cause with what the new evidence discriminates. If the fix's discriminator never engaged despite no crashes, record that honestly — the lever is unexercised, not proven.
  2. 01-UAT.md G-01-7: if series A passed 6/6 per the acceptance criteria, set status resolved with resolved_by naming the lever chain (the round-#12 D1 fix restoring crash-free transfer + 01-21 receipt-evidence re-announce/eviction ranking + 01-22 full-arm deadline + 01-18 quiet gate) and verified_by quoting the verdicts, the zero-timeout evidence, and the discriminator lines. If it failed, keep status open and extend root_cause with the mechanism the discriminators named.
  3. 01-UAT.md Test 3 note: record the D1 survival outcome (incl. sustained service), the SC-3 per-pair verdicts, the CIF-cycle and QVGA-restore outcomes; flip Test 3 to pass only if every clause it holds is now judged and none failed. Test 4 note: record the WR-03 cadence discriminator outcome (this discharges the SC-5 coincidental-reliance advisory from 01-VERIFICATION.md).
  4. WINDOWS.md: entry 15 (D1) and entry 3 (G-01-7) flip on the session evidence or stay open with the residual; update BOTH the markdown table and the JSON copy; reconcile the counts in the front matter.
  5. STATE.md: record the round outcome in Current Status (what closed on what evidence, what remains open if anything), and set Next Steps to the security gate (/gsd-secure-phase 1; Phase 2/3 gates also pending) as the remaining phase-close step if everything closed, then /gsd-progress.
  6. On a failed session: record requirements-completed: [] (the 01-24/01-27 convention) — no requirement claims ride a failed bench session; the SUMMARY names what rode instead.
  7. Commit the ledger updates separately from any log retention, message docs(01-29): bench session #9 evidence + ledger flips.
  </action>
  <verify>
  <automated>grep -q "01-29" .planning/STATE.md && grep -q "secure-phase" .planning/STATE.md && grep -c "G-01-10" .planning/phases/01-command-protocol-control/01-UAT.md | grep -qv "^0$" && grep -q '"id": 15' .planning/WINDOWS.md && grep -q '"id": 3' .planning/WINDOWS.md</automated>
  </verify>
  <acceptance_criteria>
  - Every bench-evidence flip cites operator-observed quotes with line numbers/counts from the retained session logs; failed steps leave their gap open with the residual mechanism named in the discriminator vocabulary
  - WINDOWS.md table and JSON copies agree; front-matter counts equal the actual row statuses
  - The D1 closure (if it happened) quotes the sustained-service evidence AND the 01-28 discriminator engagement — not merely an absence of resets
  - Test 3 reflects the D1-survival, SC-3-pair, CIF-cycle, and QVGA-restore outcomes; Test 4's note carries the WR-03 discriminator outcome; Tests 1/2/5 notes untouched absent regression evidence
  - STATE.md names /gsd-secure-phase 1 as the remaining phase-close gate
  </acceptance_criteria>
  <done>The gap ledgers reflect hardware truth: D1 closed on zero-crash sustained-service + series evidence or honestly re-opened with new dump evidence, G-01-7 closed on 6/6 series evidence or honestly rescoped with its next lever, Test 3 judged on its final clauses, the SC-5 advisory discharged, and the phase's remaining gate identified.</done>
  <reversibility rating="reversible">Documentation-only ledger updates.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench operator -> firmware flash + consoles | Human performs the flash and observes; logs are retained as evidence (read-only artifacts) |
| RF pair -> bench evidence | The two-radio link under bench conditions produces the verdicts; no untrusted input enters the firmware beyond the CRC16-validated frames covered in prior rounds' threat models |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-29-01 | Tampering | evidence integrity | medium | mitigate | House convention enforced: closures quote verbatim console lines with line numbers/counts from retained logs; crash-signature grep counts are quoted as zero-results, not assumed |
| T-01-29-02 | DoS | mixed-firmware flash invalidating the evidence | high | mitigate | Blocking checkpoint step 1 flashes BOTH boards before any traffic; the standing both-boards rule with the 01-09 severance lesson recorded |
| T-01-29-03 | DoS | a crash mid-session misread as a transfer defect | medium | mitigate | Protocol step 4 names the re-open path: any crash appends its dump to the debug doc and re-opens D1 — never silently attributed to G-01-7 |
| T-01-29-04 | Safety | bench hardware | low | accept | Standard bench rig (USB-powered base, battery balloon at desk); WR-01 battery watch green in all prior sessions; the round changed no power paths |

## Artifacts this phase produces

- balloon14.log / base9.log (or operator-chosen names, recorded) — retained bench console evidence (repo root, prior-session convention)
- Updated gap blocks G-01-10/G-01-7 + Test 3/Test 4 notes (01-UAT.md)
- Updated WINDOWS entries 15 and 3 with reconciled counts (.planning/WINDOWS.md)
- Round outcome + next-gate record (.planning/STATE.md)
- .planning/phases/01-command-protocol-control/01-29-SUMMARY.md
- No source files, no new symbols, no env vars

<verification>
- Both firmware targets built green and the harness exited 0 immediately before the flash (pre-flight step, on record in the session)
- Every ledger flip traces to a quotable line in the retained logs or carries its explicit evidence-class reason
- WINDOWS counts reconcile with row statuses; /gsd-ship remains blocked exactly as long as an entry is honestly open
</verification>

<success_criteria>
- The round's acceptance is judged on hardware: both D1 crash classes dead (spaced smoke + sustained multi-window service + series A) with the 01-28 discriminator engaged (or D1 re-opened with new dump evidence), series A reaches 6/6 verdicts (or the honest residual named), SC-3 pairs and the WR-03 discriminator finally judged, CIF/QVGA-restore clauses exercised as specified, dashboard LOOKED
- With G-01-10/G-01-7 closed, the WINDOWS ledger reaches zero open entries and the phase's remaining close gate is the security review (/gsd-secure-phase 1)
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-29-SUMMARY.md` when done
</output>
