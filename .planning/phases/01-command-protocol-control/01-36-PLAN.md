---
phase: 01-command-protocol-control
plan: 36
type: execute
wave: 28
depends_on: ["01-35"]
files_modified:
  - .planning/debug/d1-crash-regression-push-start.md
  - .planning/phases/01-command-protocol-control/01-UAT.md
  - .planning/WINDOWS.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [CTRL-01, CTRL-02, CTRL-03, CTRL-04, CTRL-06, PRI-02]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low   # calibration store has 0 samples (factor 1, not applied); mirrors 01-27/01-29/01-31/01-34 bench-session sizing plus the [SCHEDSNAP] interrogation step

must_haves:
  truths:
    - statement: "The [SCHEDSNAP] scheduler-state probe ANSWERS at its first death (debug doc §38.10's pre-registered route): every death's readout is quoted and classified — state=2 (SUSPENDED) names the vTaskSuspendAll-without-resume wedge (§21.3 member 1) and routes the suspend/resume-pairing code audit as round #26's NAMED lever; state=1 (RUNNING) names the switch/yield layer (§21.3 member 2, the belief-not-proof limit pre-written); state=0/tasks=0/tick=0 with valid magic is a NEW early-freeze shape (decode before routing) or an instrument failure; a missing [SCHEDSNAP] boot line at ANY boot is an instrument failure — STOP and record before trusting any readout"
      verification: explicit
    - statement: "Every death is classified by signature FIRST (the §38.10 Q3 rule stands): +15000±1 silent = family S; +2..+37-with-dump = the probe-photograph class; anything else = a NEW shape decoded before routing; stage-2 latency (T_f−T_i) recorded per death; every addr2line decode runs only against the recorded on-disk ELF SHA (addresses never transfer across ELFs, only classes do)"
      verification: explicit
    - statement: "The interrogation contract is met per §38.10: ONE session, card REMOVED (card-out regime correct at every boot), the round-#25 instruments in-image and verified at every boot ([IDLE1] hook cpu1 registered=1; a [SCHEDSNAP] boot line), >=6 captures with resolution pinned, the corrected raw-byte mojibake census (the UTF-8-replacement-char grep returns 0 — the twice-documented trap)"
      verification: explicit
    - statement: "G-01-7 series-A acceptance on the 01-35 capacity-aware lever (WINDOWS entry 3): 3 back-to-back unspaced captures — 3/3 ACKs (never 'timeout after 3 retries'), 3/3 captures, 6/6 verdicts COMPLETE, zero command timeouts, zero silently absent fulls — with the lever's lines proving engagement: NO honest-reject while any slot is free (the session-11 8-reject shape cannot recur), the class-0 safe-reclaim line if a full queue forced eviction, the round-#14 honest-reject line ONLY at genuine full occupancy; a passing series without the lines means the natural cadence sufficed (recorded as such); a failed series re-names the residual with every step a quoted line"
      verification: backstop
    - statement: "SC-3 visible-effect pairs (ELEVENTH round riding — judged if the session reaches it): at fixed QVGA, brightness -2 vs +2 judged as a visible pair plus one other class (contrast -2/+2, saturation -2/+2, or quality 10 vs 30), per-pair verdicts recorded in Test 3 — on the current natural-auto-defaults camera firmware (2b99f51), noting any auto-exposure interaction with the judged pairs"
      verification: backstop
    - statement: "WR-03 cadence discriminator (ELEVENTH round riding — judged if the session reaches it): AUTO_CAPTURE_ENABLE at 20 s, one interval capture, one manual CAPTURE_NOW, the next interval capture lands ~20 s AFTER the manual (both 'Captured image ID' timestamps quoted); AUTO_CAPTURE_DISABLE stops all automatic captures; this discharges the SC-5 coincidental-reliance advisory"
      verification: backstop
    - statement: "Dashboard operator-LOOKED glance on the CURRENT tabbed dashboard (ac5325d era): the Phase-1 control surface judged operator-LOOKED, not did-not-flag — capture trigger round-trip, settings forms render complete values, Command Queue panel advances Sent->terminal, link LED stays truthful during holds/chunks, gallery detail fields render; the tab layout itself is Phase-3 scope and is not judged here"
      verification: backstop
    - statement: "The QVGA-restore clause (wire 6 after a larger size) and the reboot boot-resolution clause are DEFERRED, recorded as deferred — any SET_RESOLUTION would break §38.10's pinned-resolution controlled variable during the card-out interrogation; they route to the post-D1 card-in confirmation run (the clause's third disposition: previously not-run ten rounds, now explicitly deferred with the reason)"
      verification: explicit
    - statement: "Ledger honesty: G-01-10/G-01-7 and WINDOWS entries 15/3 flip ONLY on operator-observed console/dashboard evidence with verbatim quotes, line numbers, and counts; a crashed or failed session leaves its gaps open with the residual named and records requirements-completed: [] (the 01-24/01-27/01-29/01-31/01-34 failed-session convention); the session record appends to the debug doc as §39 in the §36-§38 house convention (provenance/ELF SHAs first); STATE.md names the next routing"
      verification: explicit
  artifacts:
    - "Consolidated bench session logs at repo root (operator's retained names recorded whatever they are, per the 01-24 convention; the session-35 name was balloon32.log with NO base capture — this round REQUIRES both consoles logged, the §38.1 delivery-chain-unverified debt is closed only if the base side is captured)"
    - ".planning/debug/d1-crash-regression-push-start.md — §39: the session-36 record (provenance/ELF SHAs, pre-verify, census, every death's [SCHEDSNAP] readout + P-branch disposition, signature-first classification, stage-2 latencies, the second-half evidence)"
    - ".planning/phases/01-command-protocol-control/01-UAT.md — G-01-10 session-36 extension (the [SCHEDSNAP] answer + routing), G-01-7 verdict (6/6 flip or re-named residual), Test 3/Test 4 notes with the riding-clause outcomes and the QVGA deferral"
    - ".planning/WINDOWS.md — entries 15 and 3 updated per evidence, BOTH copies, counts reconciled"
    - ".planning/STATE.md — round outcome + next routing (the P-branch-named round #26, or /gsd-secure-phase 1)"
    - ".planning/phases/01-command-protocol-control/01-36-SUMMARY.md"
  key_links:
    - "The session <-> §38.10's pre-registered branches: the [SCHEDSNAP] readout is judged ONLY against the P1-P5 dispositions written BEFORE the session (the forward-predictive discipline; §11's table precedent)"
    - "Series-A verdicts <-> 01-35's lever: the capacity gate + class-0 line separate engaged-and-passing from engaged-and-failing from never-engaged; a crash under series A appends to §39 and extends D1 — never read as a transfer defect (the sessions-8/9/10 lesson)"
    - "The ELF SHAs <-> every decode: the flashed HEAD build (round-#25 instruments + the 01-35 lever) records fresh SHAs at pre-flight; §38.9's b5809923 is the PRIOR image — the banner/SHA gate compares against THIS round's recorded values, not §38.9's"
    - "Riding clauses <-> the numbered protocol steps: SC-3/WR-03/dashboard are explicit second-half steps AFTER the D1 contract, each individually skippable with an honest NOT-RUN record"
  prohibitions:
    - statement: "MUST NOT fabricate transfer, command, crash-survival, instrument, or settings verdicts — closures cite operator-observed lines with line numbers and counts; zero-count greps quoted as zero"
      status: flagged
      verification: backstop
    - statement: "MUST NOT mark G-01-10 resolved on this session unless zero crash signatures span the WHOLE console AND the interrogation contract is met AND the [SCHEDSNAP] reading is consistent with a fixed mechanism — an instrument answer alone is evidence, not a fix verdict (the session-11 zero-crash lesson)"
      status: flagged
      verification: backstop
    - statement: "MUST NOT flash only one board and MUST NOT erase flash (the esptool-v5 §36 lesson — an erase took the app with it): write both boards with the same HEAD build, verify [IDLE1]+[SCHEDSNAP] boot lines before trusting any reading"
      status: flagged
      verification: machine-checkable
    - statement: "MUST NOT change resolution during the interrogation (§38.10 pins it); the QVGA-restore clause is deferred, not run and not fabricated"
      status: flagged
      verification: backstop
    - statement: "MUST NOT fix newly observed bench defects inside this plan — route them (debug doc §39 routing, WINDOWS, todos) as every prior round did"
      status: flagged
      verification: backstop
    - statement: "MUST NOT show protocol or camera state that does not reflect reality (UAT Test 5 tier): no ACK for unexecuted actions, no green link LED while failed, GET_STATUS fields match actual balloon state"
      status: flagged
      verification: backstop
---

<objective>
Gap-closure round #15, plan 2 of 2 — the shared bench session #36: the D1 round-#25 scheduler-state interrogation (debug doc §38.10's pre-registered route, card-out) as the primary contract, with the G-01-7 series-A acceptance on the fresh 01-35 capacity-aware lever and the long-riding command-path clauses as the explicitly-ordered second half. Then flip the ledgers on evidence.

Purpose: the D1 campaign has photographed its wedge nine times (the both-cores systimer spin) and §38.10 pre-wrote exactly what its next instrument answers — [SCHEDSNAP] splits the scheduler-global wedge into SUSPENDED (a vTaskSuspendAll pairing bug, a named lever) vs RUNNING (the switch/yield layer, the next discriminator). The G-01-7 lever round finally has both ingredients the original routing asked for: the capacity gate (the session-11 2/5-occupancy rejection becomes impossible) and the safe-reclaim evidence (WINDOW_COMPLETE receipts now exist per-chunk). One session answers both, at the session-10-class capture load the D1 bars demand — with every second-half clause individually skippable and honestly recorded rather than fabricated.

Output: both consoles logged and retained; debug doc §39 (the session record in the house convention); G-01-10/G-01-7 answered or honestly extended in 01-UAT.md; WINDOWS entries 15/3 updated both copies; Test 3/Test 4 notes carry the riding-clause outcomes and the QVGA deferral; STATE.md names the next routing.

Spec-less probe fallback (visible, recorded choice): init.phase_req_ids is null this run — no probe predicates are generated; the 11 unresolved spec-less probe rows authored in 01-34's must_haves.flagged_assumptions remain carried there, never silently dropped.

<assumption_delta_decision>
Same determination as 01-35/01-32/01-34: the detector's signals trace to prior plan-list prose, not this round's scope — no-change; a bench evidence session introduces no architectural identity change.
</assumption_delta_decision>
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-command-protocol-control/01-UAT.md
@.planning/phases/01-command-protocol-control/01-VERIFICATION.md
@.planning/WINDOWS.md
@.planning/debug/d1-crash-regression-push-start.md
@.planning/phases/01-command-protocol-control/01-35-SUMMARY.md

Binding decisions and bench conventions:
- §38.10 IS the interrogation protocol (read it before the session): one session, card REMOVED, >=6 captures, resolution pinned, pre-verify ([IDLE1] registered=1 at every boot; a [SCHEDSNAP] boot line at every boot — missing = instrument failure, STOP), signature-first classification (+15000±1 silent = family S; +2..+37-with-dump = probe-photograph; else NEW — decode before routing), stage-2 latency per death, the P1-P5 branch dispositions pre-written.
- The debug-doc house convention (§36-§38): provenance/ELF SHAs FIRST and verified before any decode; census via the corrected raw-byte mojibake grep (raw EF BF BD count quoted; the UTF-8-replacement-char grep returns 0 — the twice-documented trap); [STAMP]/[TICKSTAMP]/[SCHEDSNAP] readout at EVERY death; addresses never transfer across ELFs, only classes do.
- Series-A protocol: 3 CAPTURE_NOW back-to-back WITHOUT waiting, legitimate only after the D1 contract is satisfied; quiet-gate holds may precede ACKs (the gate is now CMD_TX_CHANNEL_QUIET_MS 2000, hold bound 120 s per 9e5d5b3); a held command must NEVER reach 'timeout after 3 retries'; every full transferred, re-announced-and-transferred, or drop-logged by name.
- SC-3 pairs protocol: brightness -2 vs +2 plus one other class at fixed QVGA; thumbnails suffice unless the full is needed (note which); the camera now runs natural-auto defaults (2b99f51, auto exposure ON) — record any auto-exposure interaction with the pair judgment.
- WR-03 discriminator protocol: AUTO_CAPTURE_ENABLE at 20 s; one interval capture; one manual CAPTURE_NOW; the NEXT interval capture lands ~20 s after the manual (quote both timestamps); disable and confirm zero further automatic captures.
- WR-01 battery watch: no 'Critical battery' lines; beacons batt=valid. WR-02 watch: zero 'health check failed' lines at healthy boots. UAT Test 3 is the 'issue' holder; Tests 1/2/4/5 stand PASS and must not regress.
- The failed-session convention: any crash/abort leaves the gaps open, records requirements-completed: [], appends the session record to the debug doc, and names the residual in the discriminator vocabulary.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Bench session #36 — flash both boards, the §38.10 [SCHEDSNAP] interrogation (card-out, >=6 captures), then series A on the capacity-aware lever, SC-3 pairs, WR-03 cadence, dashboard LOOK</name>
  <files>.planning/debug/d1-crash-regression-push-start.md</files>
  <read_first>
  - .planning/debug/d1-crash-regression-push-start.md — §38 (session 35: the five deaths, the splitter verdict, §38.9 the [SCHEDSNAP] instrument, §38.10 the pre-registered route this session executes), §36 (the esptool-v5 lesson), §21-§22 (the §21.3 wedge members P1/P2 name)
  - .planning/phases/01-command-protocol-control/01-35-SUMMARY.md — the lever's named lines and the session-11 geometry they answer
  - .planning/phases/01-command-protocol-control/01-UAT.md — G-01-10 and G-01-7 blocks, Test 3/Test 4 notes
  - .planning/phases/01-command-protocol-control/01-VERIFICATION.md — behavior_unverified_items (SC-3) + coincidental_reliance_items (SC-5/WR-03)
  </read_first>
  <what-built>
  Round #15: the 01-35 capacity-aware supersede admission lever (free-slot gate + class-0 fully-receipted safe reclaim + named line) on top of the round-#25 [SCHEDSNAP] scheduler-state probe (debug doc §38.9, ELF b5809923... as the PRE-01-35 image) and the sessions-26-#35 fix chain (QQVGA downswitch retired, NVS id-commit deferred, IRAM-safe hooks, collision-proofed link, 200 KB cap). Built green, harness exit 0, no bench claims made.
  </what-built>
  <how-to-verify>
  1. PRE-FLIGHT: pio run -e esp32-s3-balloon -e esp32-s3-basestation (2 succeeded) and node scripts/verify_protocol_roundtrip.mjs (exit 0) at HEAD (includes 01-35). Record sha256sum of BOTH .pio/build/*/firmware.elf fresh (these are THIS round's decode identities — §38.9's b5809923 is the prior image). Flash BOTH boards, write only — NO erase (the §36 esptool-v5 lesson). Start BOTH serial consoles logging (both REQUIRED this round — the §38.1 debt: session 35 skipped the base side and the delivery chain went unverified; record the operator's retained names, never overwrite older session files silently).
  2. PHASE-0 PRE-VERIFY at EVERY boot (§38.10): banner census; [IDLE1] hook cpu1 registered=1; a [SCHEDSNAP] boot line (a missing line is an instrument failure — STOP and record before trusting any readout); card-OUT regime correct at every boot (SD_MMC.begin failed + volatile-fallback + no-rescan lines); [CAPWIN]/[STAMP]/[TICKSTAMP]/[MEM]/[STACK] boot lines as designed.
  3. D1 INTERROGATION (PRIMARY CONTRACT): operator-triggered captures, resolution PINNED, at least 6 captures or until the operator judges the interrogation complete. EVERY death: classify by signature FIRST (+15000±1 silent = family S; +2..+37-with-dump = probe-photograph class; anything else = NEW shape — decode before routing); quote its [SCHEDSNAP] readout and disposition it against §38.10's P1-P5 branches; record stage-2 latency (T_f−T_i); every addr2line decode ONLY against the recorded on-disk ELF.
  4. SERIES A ON THE LEVER (after step 3's contract): 3 CAPTURE_NOW back-to-back WITHOUT waiting. Expected: 3/3 ACKs (holds may precede; never 'timeout after 3 retries'), 3/3 captures, 6/6 finalize lines COMPLETE, every full transferred / re-announced-and-transferred / drop-logged by name. Quote the lever lines: the ABSENCE of honest-rejects while slots are free, any class-0 safe-reclaim line, any round-#14 honest-reject line (legitimate ONLY at full occupancy).
  5. SC-3 VISIBLE-EFFECT PAIRS (eleventh round riding): at fixed QVGA — brightness -2 capture vs +2 capture judged as a pair; one other class (contrast -2/+2, saturation -2/+2, or quality 10 vs 30); per-pair verdicts; note the natural-auto/auto-exposure interaction.
  6. WR-03 CADENCE DISCRIMINATOR: AUTO_CAPTURE_ENABLE at 20 s; one interval capture; one manual CAPTURE_NOW; the NEXT interval capture ~20 s AFTER the manual (quote both timestamps); AUTO_CAPTURE_DISABLE; zero further automatic captures.
  7. DASHBOARD LOOK GLANCE (operator-LOOKED on the CURRENT tabbed dashboard): capture trigger round-trip visible; settings forms render complete values; Command Queue panel advances Sent->terminal; link LED truthful during holds/chunks; gallery detail fields render. (Tab layout beyond the Phase-1 surface is not judged here.)
  8. DEFERRED CLAUSES (record as deferred, do NOT run): QVGA-restore (wire 6 after a larger size) and reboot boot-resolution — any SET_RESOLUTION breaks the pinned-resolution controlled variable; routed to the post-D1 card-in confirmation run.
  9. RECORD EVERYTHING: the full crash census (rst:0x1/0x7/0x8/0xc/0x3, Guru panic, stack canary, Cache error, assert/abort — corrected raw-byte mojibake method), the [SCHEDSNAP]/[STAMP]/[TICKSTAMP] readouts at every death, boot counts, capture IDs, the lever lines, the SC-3/WR-03/dashboard verdicts. A crash during the second half appends to §39 and extends D1 — never read as a transfer defect.
  </how-to-verify>
  <acceptance_criteria>
  - Pre-verify is quoted per boot: [IDLE1] registered=1, the [SCHEDSNAP] boot line, the card-out regime lines; both ELF SHAs recorded at pre-flight
  - Every death carries: signature-first classification, its [SCHEDSNAP] readout, the P1-P5 disposition, stage-2 latency — or the session is honestly recorded as death-free with the census zeros quoted
  - Series A: 3/3 ACKs, 3/3 captures, 6/6 COMPLETE, zero command timeouts, zero silently absent fulls, the lever lines quoted (or their absence explained as natural-cadence success)
  - SC-3 per-pair verdicts recorded; WR-03 both timestamps quoted + disable verified; dashboard verdicts recorded per item; the QVGA-restore + reboot-boot-resolution deferral recorded with its reason
  - Both console logs retained with every cited quote locatable by line number
  </acceptance_criteria>
  <resume-signal>Type "approved" with the console excerpts (pre-verify lines per boot, every death's signature + [SCHEDSNAP] readout + P-branch disposition + stage-2 latency, census counts, series-A verdicts + lever lines, SC-3 pair judgments, WR-03 timestamps, dashboard verdicts, the deferral note), or describe the failing step with its discriminating log lines (a crash appends §39 and extends D1).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Flip the ledgers on evidence — debug doc §39, 01-UAT.md (G-01-10/G-01-7, Test 3/Test 4), WINDOWS entries 15/3, STATE.md routing</name>
  <files>.planning/debug/d1-crash-regression-push-start.md, .planning/phases/01-command-protocol-control/01-UAT.md, .planning/WINDOWS.md, .planning/STATE.md</files>
  <read_first>
  - The operator's Task 1 evidence (the approved resume message + the retained session logs)
  - .planning/debug/d1-crash-regression-push-start.md — §36-§38 (the house record structure §39 extends; §38.10's P-branch routes)
  - .planning/phases/01-command-protocol-control/01-UAT.md — the G-01-10/G-01-7 blocks and Test 3/Test 4 notes (the house quote convention)
  - .planning/WINDOWS.md — entries 15 and 3, BOTH copies, counts from 2 open / 17 fixed / 19 total
  </read_first>
  <action>
  Update the ledgers strictly from the Task 1 evidence:
  1. Debug doc §39 (the session-36 record, house convention): provenance first (retained log names, both ELF SHAs, build banner census), pre-verify per boot, the corrected census table, every death's signature classification + [SCHEDSNAP] readout + P-branch disposition + stage-2 latency, the second-half evidence (series A + lever lines, SC-3, WR-03, dashboard), the deferral record, and the routing decision the P-branch mandates.
  2. 01-UAT.md G-01-10: append the session-36 extension — the [SCHEDSNAP] ANSWER dispositioned against §38.10. P1 (SUSPENDED) routes round #26 as the suspend/resume-pairing code audit with the mechanism-class fix as a NAMED lever (stage-2 latency stratification cross-tabbed if the classes split); P2 (RUNNING) routes the within-P2 discriminator decision (no speculative lever); P3/P4/P5 per their pre-written routes. G-01-10 flips resolved ONLY on: zero crash signatures whole-console AND the interrogation contract met AND the survival bars passed on a mechanism-named-and-fixed build — an instrument answer alone never flips it.
  3. 01-UAT.md G-01-7: on 6/6 series A + zero forbidden terminals + the lever lines (or their natural-cadence absence explained), set resolved with resolved_by naming the lever chain (01-35's capacity gate + class-0 safe reclaim on the 01-21/01-22/01-33 levers) and verified_by quoting the verdicts and lines. Otherwise keep open and extend with which engagement verdict the criteria yield (engaged-and-failing: the residual re-named with quoted lines; never-engaged: the lever-composition question routed).
  4. 01-UAT.md Test 3 note: record the SC-3 pair verdicts and the QVGA-restore DEFERRAL (with the pinned-resolution reason); Test 3 flips to pass only if every clause it holds is judged and none failed — the deferral keeps it 'issue' unless the operator explicitly accepts the clause as satisfied by the boot-resolution evidence already on file. Test 4 note: the WR-03 outcome (this discharges the SC-5 coincidental-reliance advisory when it ran).
  5. WINDOWS.md: entries 15 and 3 updated per the evidence, BOTH the markdown table and the JSON copy, front-matter counts reconciled.
  6. STATE.md: the round outcome in Current Status; Next Steps carries exactly one routed next action — the P-branch-mandated round #26 (P1: the pairing audit + named lever; P2: the within-P2 discriminator), or /gsd-secure-phase 1 (Phase 2/3 gates also pending) if both blockers closed. Record that the auto-advanced disposition confirmations pending end-of-phase operator review now span 01-23/01-25/01-26/01-28/01-30/01-32 PLUS the debug-workflow rounds' selections (§20-§38 commits) — surface the full list at the phase-close review.
  7. On a failed session: requirements-completed: [] (the failed-session convention); the SUMMARY names what rode instead.
  8. Commit: docs(01-36): bench session #36 evidence + ledger updates.
  </action>
  <verify>
    <automated>grep -c "## 39" .planning/debug/d1-crash-regression-push-start.md | grep -qv "^0$" && grep -c "SCHEDSNAP" .planning/phases/01-command-protocol-control/01-UAT.md | grep -qv "^0$" && grep -c "G-01-10" .planning/phases/01-command-protocol-control/01-UAT.md | grep -qv "^0$" && grep -q '"id": 15' .planning/WINDOWS.md && grep -q '"id": 3' .planning/WINDOWS.md && grep -c "01-36" .planning/STATE.md | grep -qv "^0$"</automated>
    <fails_when>any grep in the chain returns zero matches (chain exits non-zero) — a missing §39 record, a UAT update that never names the probe or the gap, a WINDOWS JSON copy missing an entry, or a STATE.md that was never touched</fails_when>
  </verify>
  <acceptance_criteria>
  - §39 exists in the house convention: provenance/SHAs first, census, per-death [SCHEDSNAP] + P-branch dispositions, second-half evidence, the deferral record, the routing
  - Every ledger flip cites operator-observed quotes with line numbers/counts; failed steps leave their gap open with the residual named in the discriminator vocabulary
  - WINDOWS.md table and JSON copies agree; front-matter counts equal the actual row statuses
  - Test 3 reflects the SC-3 outcomes and the explicit QVGA deferral; Test 4 carries the WR-03 outcome; Tests 1/2/5 untouched absent regression evidence
  - STATE.md names exactly one routed next action; on a failed session requirements-completed: [] is recorded
  </acceptance_criteria>
  <done>The ledgers reflect hardware truth: D1 extended with the [SCHEDSNAP] answer and its pre-registered routing (or closed on the full fix verdict), G-01-7 closed on series evidence or honestly re-named via the engagement criteria, the riding clauses judged or their non-reach/deferral recorded, and the next routing on record.</done>
  <reversibility rating="reversible">Documentation-only ledger updates.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench operator -> firmware flash + consoles | The human performs the flash (write-only, no erase) and observes; logs are retained read-only evidence |
| RF pair -> bench evidence | The two-radio link under bench (card-out) conditions produces the verdicts; no untrusted input enters beyond the CRC16-validated frames covered in prior rounds |
| instrument readings -> hypothesis disposition | The [SCHEDSNAP] readout decides a pre-named wedge question; §38.10's P-branches were written BEFORE the session and keep the disposition honest |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-36-01 | Tampering | evidence integrity | medium | mitigate | House convention: verbatim quotes with line numbers/counts, zero-count greps quoted, BOTH ELF SHAs recorded at pre-flight before any decode, §38.10's pre-registered branches govern |
| T-01-36-02 | DoS | mixed-firmware flash invalidating the evidence | high | mitigate | Blocking checkpoint step 1 flashes BOTH boards before any traffic (the 01-09 severance lesson); pre-verify lines confirm the build in-image |
| T-01-36-03 | DoS | flash erase destroying the deployed app | high | mitigate | Write-only flashing; the §36 esptool-v5 lesson recorded in the step; no erase command in the protocol |
| T-01-36-04 | DoS | a crash mid-series misread as a transfer defect | high | mitigate | The re-open prohibition names the §39 path — the sessions-8/9/10 convention carried verbatim |
| T-01-36-05 | DoS | the second half confounding the interrogation's controlled variables | medium | mitigate | The D1 contract is ordered FIRST; resolution stays pinned (the QVGA clause explicitly DEFERRED); second-half clauses are individually skippable with honest NOT-RUN records |
| T-01-36-06 | Repudiation | lever engagement unprovable after the fact | medium | mitigate | The 01-35 named lines + the surviving round-#14 lines are greppable in the retained console; three-way engagement separation preserved |
| T-01-36-07 | Safety | bench hardware | low | accept | Standard bench rig; WR-01 battery watch green in every prior session; the round changes no power paths |

## Artifacts this phase produces

- Both retained bench console logs at repo root (operator's names recorded; both consoles REQUIRED — the §38.1 base-side debt closes only with the base log captured)
- Debug doc §39 (the session-36 record with the P-branch dispositions)
- Updated gap blocks G-01-10/G-01-7 + Test 3/Test 4 notes (01-UAT.md)
- Updated WINDOWS entries 15 and 3 with reconciled counts (both copies)
- Round outcome + next-routing record (.planning/STATE.md)
- .planning/phases/01-command-protocol-control/01-36-SUMMARY.md
- No source files, no new symbols, no env vars

<verification>
- Both firmware targets built green and the harness exited 0 immediately before the flash; both ELF SHAs recorded at pre-flight
- Every ledger update traces to a quotable line in the retained logs or carries its explicit evidence-class reason; the [SCHEDSNAP] readings are quoted either way
- WINDOWS counts reconcile with row statuses; the STATE routing names exactly one next action
</verification>

<success_criteria>
- The §38.10 interrogation contract is met: card-out, >=6 captures, pre-verify clean, every death signature-classified with its [SCHEDSNAP] readout dispositioned against the pre-written P-branches (or a death-free session with the census zeros quoted)
- Series A judged on the capacity-aware lever with the three-way engagement separation (or its clauses honestly not reached)
- The riding clauses judged or their non-reach/deferral recorded; the SC-5 advisory discharged if WR-03 ran
- With both blockers closed, the WINDOWS ledger reaches zero open entries and the remaining phase-close gate is /gsd-secure-phase 1; with D1 open, the P-branch mandates round #26 with no speculation required
</success_criteria>

<output>
Create `.planning/phases/01-command-protocol-control/01-36-SUMMARY.md` when done
</output>
