---
phase: 03-enhanced-web-interface
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main_basestation.cpp
  - scripts/verify_protocol_roundtrip.mjs
  - include/image_protocol.h
  - src/command_protocol.cpp
  - src/image_tx_manager.cpp
  - include/image_rx_manager.h
  - src/image_rx_manager.cpp
  - src/main_balloon.cpp
autonomous: true
requirements: [WEB-01, WEB-03, WEB-04, ALRT-02]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The dashboard is one scrolling single-page document in the D-45 order — sticky section nav, header, telemetry panel + map frame, capture/settings cards, command queue + transfers, then the gallery slot — superseding Phase 1's D-13 separate-page structure (WEB-04, D-45)"
    - "GET /api/state is the single live-data endpoint: one hand-built String serializer aggregating telemetry, link truth, command queue, transfer progress, and storage — one serializer, no fabricated state (D-34, WEB-01)"
    - "The browser polls /api/state every 5 s (the 1 s setInterval retired); on consecutive failures it backs off 5s->15s->30s showing the amber stale badge with live data age, and snaps back to 5 s on recovery without a page reload (D-33, D-35, WEB-03)"
    - "The telemetry panel shows six tiles — Link (LED + Ready/No link/Unknown), Altitude, Temperature, GPS, Battery, Data age — 18px/700 values over 14px labels, fed only by the latest 0x14 beacon snapshot; absent data renders honest-null, never zero-filled (WEB-01)"
    - "Battery displays a voltage only while batteryValid is true; while invalid or absent it renders the em-dash placeholder — never a fabricated voltage (ALRT-02 display half)"
    - "The 0x14 telemetry beacon carries battery: body 17->19 bytes with u16 batteryMilliV (BE16) and flags bit1 batteryValid, populated on the balloon from the GPIO4 ADC read behind a 1.8-8 V plausibility gate; the change-set atomically covers the struct, serializer, deserializer strict-length check, snapshot, /api/state JSON, and the wire harness (D-41, ALRT-02)"
    - "DOM re-renders only on value change (diff-checked dataset gates) and every server-derived string renders via textContent (D-36)"
    - "WEB-03 adjacency resolved: polls never overlap — the next /api/state fetch is scheduled only after the previous one settles, so a slow response delays the cycle instead of stacking concurrent requests"
    - "WEB-03 empty resolved: before the first successful poll the dashboard renders honest-null telemetry ('No telemetry received yet' / placeholder values), never zero-valued fields"
    - "WEB-03 ordering resolved: when successive polls carry equal values the diff-checked render leaves the DOM untouched — render order is stable and value-identity-driven"
    - "WEB-03 idempotency resolved: re-polling with unchanged server state produces zero DOM mutations — the render is idempotent per poll"
    - "WEB-03 concurrency resolved: a failed or aborted poll increments the backoff ladder and never corrupts panel state — the last good render stays until recovery"
    - "Telemetry panel (E2) loading: status-line model, no spinners — values fill from the first successful poll; populated: six live tiles with counting data age; partial: per-field honest-null, never a partial or zero-filled row"
    - "Telemetry panel (E2) error: poll failure surfaces ONLY as the stale badge with 5->15->30s backoff, clearing silently on recovery — never a fabricated banner"
    - "Telemetry panel (E2) overflow: the auto-fit grid (minmax(110px,1fr)) collapses to 2 columns at <=480px; zero-one-many: fixed six-tile grid, layout constant, only values change; long-text: coordinate strings wrap in grid cells at 14px/1.5, no truncation"
    - statement: "On forced poll failure the stale badge appears with a live age count and the poll backs off 5->15->30s; on recovery the badge clears and the cadence snaps back to 5s without a page reload"
      verification: backstop
  artifacts:
    - path: "src/main_basestation.cpp"
      provides: "D-45 single-page shell (section nav, section order, harmonized tokens), GET /api/state handler, 5s poll scheduler with backoff + stale badge, diff-checked telemetry panel"
      contains: "/api/state"
    - path: "include/image_protocol.h"
      provides: "TelemetryBeaconBody.batteryMilliV + IMG_TELEMETRY_BEACON_BODY_SIZE 19"
      contains: "batteryMilliV"
    - path: "src/command_protocol.cpp"
      provides: "beacon serializer/deserializer battery field with the strict 19-byte length check kept"
      contains: "batteryMilliV"
    - path: "src/image_tx_manager.cpp"
      provides: "sendTelemetryBeacon battery populate behind the plausibility gate"
      contains: "battery"
    - path: "include/image_rx_manager.h"
      provides: "TelemetrySnapshot.batteryMv + batteryValid"
      contains: "batteryMv"
    - path: "scripts/verify_protocol_roundtrip.mjs"
      provides: "19-byte beacon assertions + battery round-trip + 17-byte rejection clauses"
      contains: "19"
  key_links:
    - from: "src/main_basestation.cpp handleApiState"
      to: "include/image_rx_manager.h getTelemetrySnapshot"
      via: "the single serializer reads the beacon snapshot (now including batteryMv/batteryValid) — the only live-data path the browser polls"
      pattern: "getTelemetrySnapshot"
    - from: "scripts/verify_protocol_roundtrip.mjs"
      to: "include/image_protocol.h"
      via: "harness pins IMG_TELEMETRY_BEACON_BODY_SIZE = 19 and the 7+19+4=30 total — wire changes land as harness clauses first (Phase 2 discipline)"
      pattern: "IMG_TELEMETRY_BEACON_BODY_SIZE"
    - from: "browser poll scheduler (main_basestation.cpp PROGMEM JS)"
      to: "GET /api/state"
      via: "fetch('/api/state') on the 5s ladder with named constants; stale badge derives from client-side poll timing only"
      pattern: "api/state"
  prohibitions:
    - statement: "MUST NOT display fabricated telemetry freshness — a value whose beacon age is stale renders with its true age (data-age tile / stale badge), never presented as live"
      status: unverified
      flagged: true
      verification: judgment
    - statement: "MUST NOT display a battery voltage when the beacon reports none or an implausible reading — the tile shows the placeholder while batteryValid is false, never a fabricated number"
      status: unverified
      flagged: true
      verification: judgment
  assumptions:
    - "Probe edge WEB-01 unclassified/unresolved — flagged assumption: telemetry panel values derive solely from the 0x14 beacon snapshot; sustained beacon ages up to ~10 s during heavy transfers are acceptable (02-UAT acceptance shape) and surface via the data-age tile"
    - "Probe edge WEB-04 unclassified/unresolved — flagged assumption: the single scrolling D-45 page with sticky nav is usable on the target devices (laptop + phone over the AP LAN); visual verification is a UAT item"
    - "Probe edge ALRT-02 unclassified/unresolved — flagged assumption: a battery divider is physically wired to GPIO4 with ~2:1 ratio (research A2); the plausibility gate plus a UAT calibration item covers the unwired case"
---

<objective>
Tracer slice for Phase 3: restructure the base web UI into the D-45 single-page dashboard, create the /api/state single polling endpoint with the locked 5s cadence + backoff + stale badge + diff-checked rendering, ship the six-tile telemetry panel, and extend the 0x14 telemetry beacon with battery voltage (the one pre-authorized balloon-side wire change) so live telemetry — including battery — displays end-to-end.

Purpose: This plan proves the phase's architecture (single page, single endpoint, single serializer, 5s poll ladder) on the agent's best early context, and lands the only new *truth* the phase adds (battery) through every layer: balloon ADC -> beacon wire -> base snapshot -> /api/state -> browser tile.
Output: Restructured main_basestation.cpp (WEB-01/WEB-03/WEB-04), battery-extended beacon contract with green harness and both firmware targets.
</objective>

<execution_context>
@C:/Users/Amp/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Amp/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-enhanced-web-interface/03-CONTEXT.md
@.planning/phases/03-enhanced-web-interface/03-RESEARCH.md
@.planning/phases/03-enhanced-web-interface/03-PATTERNS.md
@.planning/phases/03-enhanced-web-interface/03-UI-SPEC.md
</context>

<assumption_delta_decision>
The assumption-delta detector fired on the ROADMAP phrase "5-second WebSocket/polling update cycle" (pluralization term "second"). This is a time-duration phrase, not a second entity of any noun — the phase introduces no singular-to-plural transition (still exactly one balloon, one base, one telemetry source, one live-data endpoint). Decision: **no-change** — the existing identity model already names the right nouns. No invariant test required.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: D-45 single-page dashboard shell + /api/state + 5s poll ladder + telemetry panel</name>
  <reversibility rating="costly">D-45 is rated costly in CONTEXT: after the single-page structure ships, undoing it means re-splitting the served page, nav, and poll wiring across multiple documents.</reversibility>
  <read_first>
  - src/main_basestation.cpp (full current page: HTML template with card order, the poll script at ~495-568 with setInterval(updateStatus, 1000) at 567, handleStatus ~1370-1499, computeLinkTruth ~88-113, route registrations 718-730, loop ~606-638)
  - .planning/phases/03-enhanced-web-interface/03-UI-SPEC.md (Design System: section order, new components 1-4 and 8; Spacing: harmonize-on-touch 20->24 / 15->16 / 13-12-11->14; Typography; Color; Copywriting: stale badge)
  - .planning/phases/03-enhanced-web-interface/03-PATTERNS.md (main_basestation section: hand-built String JSON, dataset-gate diff idiom, textContent rule, non-blocking loop)
  </read_first>
  <files>src/main_basestation.cpp</files>
  <action>
  Restructure the PROGMEM HTML into the D-45 single-page dashboard and rewire the poll pipeline (per D-33/D-34/D-35/D-36/D-45):

  1. Page structure, top to bottom: sticky `.section-nav` (full-width, #1e293b bg, 1px #475569 bottom border, links Body 14/700 in #94a3b8, hover + current-section #60a5fa, 8px vertical padding) -> `.header` -> telemetry panel + `.map-frame` placeholder (container-width, 360px tall, radius 8px, 1px #475569 border, #0f172a bg, containing the UI-SPEC waiting message "Waiting for GPS fix — the track appears once the balloon reports a valid position." as a .message.info) -> capture/settings cards in the locked order (📸 Manual Capture, ⚙️ Camera Settings, ⏱ Auto-Capture, 🛰 Event Capture) -> 📡 Command Queue + 📦 Image Transfers -> the existing 🖼 Latest Capture card LAST as the interim thumbnail surface (the gallery plan absorbs it; do not delete the thumbnail display in this plan). Nav links only for sections that exist after this task: Map, Capture, Queue. Single-column card stack; no multi-column layout.

  2. Command Queue card: the Sent/Acked/Failed/Pending counters move in as an inline chip row (18px/700 numerals, 14px muted labels) beside the existing queue rows; the link LED keeps its IN-03 semantics.

  3. Token harmonization is FILE-WIDE over the served page (UI-SPEC Spacing): this task restructures the entire PROGMEM page top-to-bottom, so harmonize-on-touch here reaches every served CSS rule — card padding 20px -> 24px, message padding 15px -> 16px, h2 margins 15px -> 16px, transfer-row text 13/12/11px -> 14px, and any other served declaration still off the standard spacing set {4,8,16,24,32,48,64} or the four font sizes {14,16,18,24} migrates to the nearest standard value during the rebuild. The negative grep in <verify> scans the whole file, so a single untouched legacy rule left off the standard set fails the gate — complete the migration file-wide in this task, not per-section.

  4. New route: register GET /api/state handled by handleApiState — rename/extend the existing handleStatus aggregation (telemetry block, queue, link truth, event thresholds, transfers, storage) and keep GET /status registered to the SAME handler so existing hardware-UAT scripts keep working (one serializer, two paths, per D-34). reserve() the JSON String for the larger payload.

  5. Telemetry panel: six tiles on the status-bar auto-fit grid pattern (minmax(110px,1fr), 2-column collapse <=480px) — Link (existing computeLinkTruth LED + Ready/No link/Unknown), Altitude (m, 1 decimal), Temperature (°C, 1 decimal), GPS (lat/lon at 6 decimals, or "No fix" while gpsValid false), Battery (renders volts at 1 decimal when the JSON carries batteryValid true, otherwise the em-dash placeholder — in this task the JSON has no battery key yet, which must render as the placeholder), Data age (seconds since last beacon, counting). Values 18px/700 #60a5fa except the LED-colored Link tile; labels 14px #94a3b8. Absent telemetry renders the honest-null copy "No telemetry received yet".

  6. Poll rewrite: replace the 1s interval with a scheduler built on named constants POLL_INTERVAL_MS 5000 and a consecutive-failure backoff ladder 5000/15000/30000 (e.g. POLL_BACKOFF_STEPS). The next fetch is scheduled only after the previous settles (no overlapping fetches). On failure: show the amber stale badge "Stale — data {N} s old · retrying every {15|30} s" (amber chip pairing #eab308/#713f12/#fbbf24, pinned in the telemetry panel's Link item position) with a live age count. On recovery: clear the badge silently and snap back to 5s — no "recovered" banner.

  7. Diff-checked rendering (D-36): extend the existing dataset-gate idiom (thumbImg.dataset.id !== String(thumbId), ~532-539) to every tile, counter, queue row, and transfer row — DOM writes happen only when the value string actually changes. All server-derived strings render via textContent; the innerHTML property is never used with server data (XSS boundary).
  </action>
  <verify>
  <automated>pio run -e esp32-s3-basestation && node scripts/verify_protocol_roundtrip.mjs && grep -q '"/api/state"' src/main_basestation.cpp && grep -q "POLL_INTERVAL_MS" src/main_basestation.cpp && grep -q "section-nav" src/main_basestation.cpp && grep -q "map-frame" src/main_basestation.cpp && grep -q "Stale" src/main_basestation.cpp && ! grep -q "updateStatus, 1000" src/main_basestation.cpp && ! grep -qE 'font-size:\s*(11|12|13|15|20)px' src/main_basestation.cpp</automated>
  </verify>
  <acceptance_criteria>
  - src/main_basestation.cpp registers GET /api/state and GET /status to one shared handler; the browser poll fetches /api/state only
  - The served HTML contains .section-nav, .map-frame with the waiting message, the six telemetry tiles, and the D-45 section order with the Latest Capture card retained below transfers
  - The poll scheduler uses named constants POLL_INTERVAL_MS 5000 plus a 5->15->30s backoff ladder; the 1s interval call is gone from the source
  - The stale badge copy renders on consecutive failures with a live age and clears silently on recovery; the badge derives from client-side poll timing only
  - The Battery tile renders the em-dash placeholder while the JSON carries no battery data (this task) — the placeholder logic keys on absence/invalid, not on a default number
  - No font-size outside {14,16,18,24}px appears anywhere in the served CSS (gate: the file-wide negative grep over src/main_basestation.cpp); the rebuilt page uses 24px card padding
  - Dynamic rendering goes through textContent with dataset diff-gates; no server string is injected as HTML
  - Base target builds SUCCESS; the wire harness still exits 0
  </acceptance_criteria>
  <done>The operator opens the base station page and sees the restructured single-page dashboard with live telemetry tiles updating every 5 seconds, an honest stale badge under link failure, and no visual-language drift — WEB-01, WEB-03, WEB-04 delivered; the shell every later Phase 3 plan plugs into is proven.</done>
</task>

<task type="auto">
  <name>Task 2: 0x14 beacon battery extension — 17->19 bytes, harness-first atomic change-set (D-41, ALRT-02 enabler)</name>
  <reversibility rating="costly">Wire-level contract shared by both firmwares: the beacon body size is pinned in the harness and length-checked by the base deserializer — an atomic 19-byte migration across serializer, deserializer, snapshot, populate, and harness; mismatched halves reject every beacon.</reversibility>
  <read_first>
  - scripts/verify_protocol_roundtrip.mjs (IMG_TELEMETRY_BEACON_BODY_SIZE = 17 constant ~line 73; the 7+17+4=28 assertions ~919-920; beacon round-trip clauses)
  - include/image_protocol.h (TelemetryBeaconBody ~148-158; IMG_TELEMETRY_BEACON_BODY_SIZE ~107)
  - src/command_protocol.cpp (serializeTelemetryBeacon/deserializeTelemetryBeacon ~475-545, including the strict length check ~516)
  - src/image_tx_manager.cpp (sendTelemetryBeacon ~115-155; gpsValid flag population pattern)
  - src/power_manager.h + src/power_manager.cpp (update() at ~115; readBatteryVoltage() at ~315-324; BATTERY_SENSE_PIN GPIO4 via include/sensor_pins.h:46)
  - include/image_rx_manager.h (TelemetrySnapshot ~105-114) + src/image_rx_manager.cpp (onTelemetryBeaconFrame)
  - src/main_balloon.cpp (loop ~606-638 area for the millis-timer slot; note the stale "Method doesn't exist" comment at ~697)
  - .planning/phases/03-enhanced-web-interface/03-RESEARCH.md (Pitfall 1 blast-radius list, Pitfall 2 plausibility gate, Code Examples verbatim excerpts)
  </read_first>
  <files>scripts/verify_protocol_roundtrip.mjs, include/image_protocol.h, src/command_protocol.cpp, src/image_tx_manager.cpp, include/image_rx_manager.h, src/image_rx_manager.cpp, src/main_balloon.cpp, src/main_basestation.cpp</files>
  <action>
  Land the pre-authorized balloon-side wire extension atomically (D-41 escape hatch; battery does NOT ride the beacon today — research Finding 1):

  1. HARNESS FIRST (RED before firmware): in scripts/verify_protocol_roundtrip.mjs update the transcribed IMG_TELEMETRY_BEACON_BODY_SIZE constant from 17 to 19, update the total-length assertions from 7+17+4=28 to 7+19+4=30, add a beacon round-trip clause asserting batteryMilliV (e.g. 4200) and flags bit1 survive serialize->deserialize, and add a rejection clause proving a 17-byte body fails the deserializer's length check. Run the harness — it must fail against the current transcription before firmware changes.

  2. include/image_protocol.h: add `uint16_t batteryMilliV;` to TelemetryBeaconBody (BE16, appended AFTER flags so flags stays at byte offset 16 — bytes 17-18 are the new field; total body 19), bump IMG_TELEMETRY_BEACON_BODY_SIZE from 17 to 19, and document flags bit1 = batteryValid in the flags field comment (bit0 gpsValid unchanged).

  3. src/command_protocol.cpp: serializer appends writeUint16(buffer + offset, pkt.body.batteryMilliV) with offset += 2 after the flags byte; deserializer mirrors with readUint16 into the body; the strict `length != CMD_HEADER_SIZE + IMG_TELEMETRY_BEACON_BODY_SIZE + 4` check is updated via the constant and kept strict — no weakened validation.

  4. Balloon populate, src/image_tx_manager.cpp sendTelemetryBeacon(): read the battery voltage via the PowerManager getter (readBatteryVoltage per power_manager.cpp:315 — confirm the public name in power_manager.h). Plausibility gate per Pitfall 2: when the computed voltage is within 1.8-8.0 V AND the raw analog read is nonzero, set batteryMilliV = (uint16_t)lroundf(v * 1000.0f) and set flags bit1 (0x02); otherwise batteryMilliV = 0 and flags bit1 clear. Wire PowerMgr().update() into the balloon main loop (src/main_balloon.cpp) on a millis timer (~1000 ms slot, the D-26 timer idiom) so the ADC actually refreshes — update() exists (power_manager.cpp:115); the "Method doesn't exist" comment at main_balloon.cpp ~697 is stale, fix the comment while touching the area.

  5. Base snapshot: include/image_rx_manager.h TelemetrySnapshot gains `uint16_t batteryMv;` and `bool batteryValid;` (documented: 0/false until a valid beacon carries it); src/image_rx_manager.cpp onTelemetryBeaconFrame populates both from the deserialized body.

  6. src/main_basestation.cpp handleApiState telemetry block: emit "batteryMv":<n> and "batteryValid":<true|false>. The Task 1 Battery tile now renders volts (batteryMv/1000 at 1 decimal) when batteryValid is true and keeps the em-dash placeholder otherwise. No other balloon behavior changes: the balloon diff for the whole phase stays limited to main_balloon.cpp (PowerMgr wiring + comment), image_tx_manager.cpp (battery populate), and the shared protocol files.
  </action>
  <verify>
  <automated>node scripts/verify_protocol_roundtrip.mjs && pio run -e esp32-s3-balloon -e esp32-s3-basestation && grep -q "batteryMilliV" include/image_protocol.h && grep -q "= 19" include/image_protocol.h && grep -q "batteryValid" include/image_rx_manager.h && grep -q "batteryMv" src/main_basestation.cpp && grep -q "PowerMgr().update()" src/main_balloon.cpp</automated>
  </verify>
  <acceptance_criteria>
  - The harness pins IMG_TELEMETRY_BEACON_BODY_SIZE 19 with 7+19+4=30 assertions, a battery round-trip clause, and a 17-byte-body rejection clause; node scripts/verify_protocol_roundtrip.mjs exits 0
  - TelemetryBeaconBody carries batteryMilliV at bytes 17-18 with flags unchanged at offset 16; IMG_TELEMETRY_BEACON_BODY_SIZE is 19 in include/image_protocol.h
  - The base deserializer's strict length check uses the 19-byte constant — a 17-byte beacon body is rejected, CRC16 validation untouched
  - sendTelemetryBeacon sets batteryMilliV + flags bit1 only when the 1.8-8 V plausibility gate passes; PowerMgr().update() runs from a loop timer in main_balloon.cpp
  - TelemetrySnapshot exposes batteryMv + batteryValid; /api/state telemetry includes both; the Battery tile shows volts only while batteryValid is true
  - git diff over src/ + include/ against the pinned phase-start ref (see <verification>: the last commit touching the phase PLAN files) shows balloon-side changes limited to main_balloon.cpp, image_tx_manager.cpp, and the shared protocol files (no app_httpd.cpp / camera_index.h / wifi_config.h changes)
  - Both firmware targets build SUCCESS
  </acceptance_criteria>
  <done>Battery voltage flows end-to-end — balloon ADC, plausibility-gated, on the wire in a 19-byte beacon the harness pins, into the base snapshot and the /api/state telemetry block, onto the dashboard tile with honest absence — the data foundation for ALRT-02 (D-41's one permitted balloon-side change, landed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RF link -> base firmware | 0x14 beacons arrive over the shared LoRa channel — extended body still untrusted input |
| Browser -> base HTTP | /api/state poll and the page come from the AP/STA LAN (pre-existing unauthenticated posture) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | 0x14 beacon fixed-length + CRC validation on the 19-byte body | high | mitigate | The strict `length != CMD_HEADER_SIZE + 19 + 4` check and CRC16 stay — the extension moves the constant, never weakens the gate; the harness rejection clause proves a 17-byte body is refused |
| T-03-02 | Tampering / XSS | telemetry + battery strings into dashboard DOM | high | mitigate | textContent-only rendering for every server-derived string (UI-SPEC interaction model); no innerHTML with server data anywhere in the new render code |
| T-03-03 | Tampering | implausible battery readings fabricated into alerts (unwired divider floats) | medium | mitigate | 1.8-8 V plausibility gate + nonzero-raw check + batteryValid flag; invalid renders the em-dash placeholder, never a number (Pitfall 2); divider calibration is a UAT item |
</threat_model>

<verification>
- pio run -e esp32-s3-balloon -e esp32-s3-basestation reports SUCCESS for both targets
- node scripts/verify_protocol_roundtrip.mjs exits 0 (now pinning the 19-byte beacon + battery clauses)
- All task acceptance-criteria greps pass (route registration, poll constants, section shell, token gates, battery fields, PowerMgr wiring)
- git diff --name-only PHASE_BASE -- src/ include/ — with PHASE_BASE the pinned phase-start ref resolved as the last commit touching the phase PLAN files (git log -1 --format=%H -- .planning/phases/03-enhanced-web-interface/03-01-PLAN.md), because wave commits land on main during execution and a diff against the main tip would be vacuous — shows balloon-side files limited to main_balloon.cpp, image_tx_manager.cpp, command_protocol.cpp, image_protocol.h (the shared wire set); WEB-06 untouched
- Hardware UAT (deferred): dashboard renders over AP, stale badge appears when the station LAN hiccups, battery tile shows a real voltage only when the divider is wired (calibration item 03-UAT)
</verification>

<success_criteria>
- The D-45 single-page dashboard shell exists with the locked tokens and section order (WEB-04)
- One /api/state endpoint serves the whole 5s poll cycle with backoff + stale badge + diff-checked rendering (WEB-01, WEB-03, D-33..D-36)
- Battery rides the 0x14 beacon (19-byte body) with green harness and both targets building (D-41, ALRT-02 data path)
</success_criteria>

## Artifacts this phase produces (plan 03-01)

- HTTP: GET /api/state (single live-data serializer; GET /status aliased to the same handler)
- Browser: POLL_INTERVAL_MS/POLL_BACKOFF poll scheduler with stale badge, dataset-gate diff-render core, six-tile telemetry panel, .section-nav sticky nav, .map-frame placeholder
- Wire: TelemetryBeaconBody.batteryMilliV (BE16, bytes 17-18) + flags bit1 batteryValid; IMG_TELEMETRY_BEACON_BODY_SIZE 19
- TelemetrySnapshot fields batteryMv/batteryValid (include/image_rx_manager.h)
- Balloon: PowerMgr().update() loop-timer wiring in src/main_balloon.cpp feeding sendTelemetryBeacon
- Harness: 19-byte beacon assertions + battery round-trip + 17-byte rejection clauses in scripts/verify_protocol_roundtrip.mjs

<output>
Create `.planning/phases/03-enhanced-web-interface/03-01-SUMMARY.md` when done
</output>
