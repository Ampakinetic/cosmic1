---
phase: 03-enhanced-web-interface
plan: "04"
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - include/sd_storage.h
  - src/sd_storage.cpp
  - src/main_basestation.cpp
autonomous: true
requirements: [IMG-06]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SdStorage enumerates /images at boot (openNextFile) into a RAM index {id, hasThumb, hasFull, sidecar flags, size} sorted newest-first, rebuilt lazily when the finalized-image version changes — pagination slices the index, never the directory (IMG-06, D-36)"
    - "GET /gallery?page=N returns 12 entries newest-first with page count and total; GET /gallery/{id} (handleNotFound dispatch, strictly-numeric id) returns the sidecar-parsed detail — only sidecar-verified fields, absent fields omitted, never zero-filled (D-47)"
    - "Images that finalized incomplete appear in the gallery normally with the amber Incomplete badge in grid and detail; the detail view shows the thumbnail and recorded chunk accounting when the full did not verify (D-48)"
    - "The gallery grid is repeat(auto-fill, minmax(200px,1fr)) with a 16px gap (3 columns at the 752px content width); the pager (Previous/Next + 'Page {n} of {m}' + numbered buttons) hides entirely on a single page (D-46)"
    - "Clicking a gallery image opens the inline detail card (no modal): the full-size /img/{id} when complete (the thumbnail otherwise) plus the sidecar definition list (Captured, Trigger, Altitude, Position, Camera, Chunks n/m, percent, Status) and a Back to Gallery button (D-47)"
    - "The gallery list refreshes only when the finalized-image count changes (D-36); thumbnails stream via the existing /img/{id}_t.jpg route and fulls via the existing /img/{id} route — no duplicate serving paths"
    - "IMG-06 adjacency resolved: pagination boundaries are exact — page N lists exactly entries [(N-1)*12 .. N*12-1] of the newest-first index, no id duplicated or dropped at a page seam"
    - "IMG-06 empty resolved: with no finalized images the gallery shows 'No images received yet — trigger a capture to start.' and hides the pager"
    - "IMG-06 ordering resolved: gallery order is strictly descending image id — ids are unique so the order is total and stable across refreshes and pages"
    - "Gallery grid (E4) loading: the previous grid stays until the new page arrives and the list refreshes only when the finalized-image count changes; populated: 12 items newest-first per page, 3 columns at the 752px content width"
    - "Gallery grid (E4) error: missing image routes keep the existing 404 copy; incomplete images keep the amber badge forever — never an error state; partial: incomplete images list normally with the badge, never rounded up"
    - "Gallery grid (E4) overflow: the grid auto-fills down to 1 column and the pager centers; zero-one-many: numeric pager only, one page hides the pager, filenames never show on grid items; long-text: pager text is fixed-length, nothing truncates"
    - "Detail view (E5) empty: reachable only from a listed image — no empty-detail state exists; loading: the definition list renders immediately from the sidecar while the image loads progressively"
    - "Detail view (E5) error/partial: only sidecar-verified fields render — absent fields omitted, never zero-filled; 'Position: GPS no fix' when the fix was invalid at capture; populated/zero-one-many: exactly one image at max-width 100%; overflow/long-text: the card grows vertically, sidecar values wrap"
  artifacts:
    - path: "include/sd_storage.h"
      provides: "enumeration API: SdGalleryEntry, SD_GALLERY_MAX_ENTRIES, buildIndex/getIndexPage/getPageCount/getTotalCount, readSidecarMeta, finalize-version counter"
      contains: "getIndexPage"
    - path: "src/sd_storage.cpp"
      provides: "boot enumeration via openNextFile, IMG_{id} name parsing, newest-first RAM index, lazy rebuild on version change, untrusted sidecar parsing (bounded + clamped)"
      contains: "openNextFile"
    - path: "src/main_basestation.cpp"
      provides: "GET /gallery?page=N route, GET /gallery/{id} detail via handleNotFound, /api/state galleryCount, gallery grid + pager + inline detail card UI, retirement of the interim Latest Capture card"
      contains: "gallery"
  key_links:
    - from: "src/sd_storage.cpp finalizeImage"
      to: "src/sd_storage.cpp buildIndex"
      via: "finalization bumps a version counter; the /gallery route compares it against the indexed version and rebuilds lazily — never a per-request directory rescan"
      pattern: "indexVersion"
    - from: "GET /gallery/{id}"
      to: "src/sd_storage.cpp readSidecarMeta"
      via: "detail JSON is parsed from IMG_{id}.JSON / IMG_{id}_T.JSON with every value treated as untrusted (bounded copies, numeric clamps, absent fields omitted)"
      pattern: "readSidecarMeta"
    - from: "browser gallery grid"
      to: "/img/{id}_t.jpg (existing route)"
      via: "grid thumbnails stream from the existing route; the poll's galleryCount drives when the list refetches (D-36) — no parallel serving path is created"
      pattern: "_t.jpg"
  prohibitions:
    - statement: "MUST NOT hide, filter out, or recycle incomplete images from the gallery — the flight's full received history stays listed with its incomplete badge; nothing is deleted or truncated to keep the grid fast"
      status: unverified
      flagged: true
      verification: judgment
  assumptions: []
---

<objective>
The paginated image gallery over the Phase 2 SD-card contract: SdStorage gains enumeration (a boot-built RAM index with lazy refresh), the /gallery list and /gallery/{id} detail routes serve it, and the gallery UI renders the thumbnail grid with pager, incomplete badges, and the sidecar-driven detail view — absorbing the interim Latest Capture card.

Purpose: IMG-06 completes the D-29..D-32 contract's purpose — everything Phase 2 stored becomes browsable flight history, honestly badged when degraded.
Output: enumeration API in SdStorage, gallery routes, gallery grid + pager + detail card.
</objective>

<execution_context>
@C:/Users/Amp/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Amp/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-enhanced-web-interface/03-CONTEXT.md
@.planning/phases/03-enhanced-web-interface/03-RESEARCH.md
@.planning/phases/03-enhanced-web-interface/03-PATTERNS.md
@.planning/phases/03-enhanced-web-interface/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SdStorage enumeration + RAM index + /gallery list and /gallery/{id} detail routes (D-46/D-47 data half)</name>
  <read_first>
  - include/sd_storage.h (current API: serveFile, imagePath/sidecarPath builders, SdImageMetadata ~30-61, writeSidecar contract ~144)
  - src/sd_storage.cpp (begin(), finalizeImage both kinds, the hand-built sidecar JSON writer — the parser's input format)
  - src/main_basestation.cpp (handleImage ~1508-1537 strictly-numeric path parsing to reuse verbatim; handleNotFound dispatch ~1577-1587; handleApiState; route registrations ~718-730)
  - .planning/phases/03-enhanced-web-interface/03-RESEARCH.md (Pattern 6 gallery over the sidecar contract; Pitfall 5 enumeration performance)
  - .planning/phases/03-enhanced-web-interface/03-PATTERNS.md (sd_storage section: honest-absence rule, untrusted-sidecar discipline)
  </read_first>
  <files>include/sd_storage.h, src/sd_storage.cpp, src/main_basestation.cpp</files>
  <action>
  Extend SdStorage with enumeration and the routes that serve it:

  1. Index model (include/sd_storage.h): struct SdGalleryEntry { uint16_t id; bool hasThumb; bool hasFull; bool hasThumbSidecar; bool hasFullSidecar; uint32_t fullSize; }; SD_GALLERY_MAX_ENTRIES = 1000 (editable constant; ~5.5 h at the 20 s cadence — beyond it the newest 1000 show, documented in the header comment as honest degradation). Static array + count, kept sorted descending by id.

  2. buildIndex() (src/sd_storage.cpp): SD.open("/images") + openNextFile() walk at boot (after begin succeeds); for each name matching IMG_%05u with the .JPG / _T.JPG / .JSON / _T.JSON suffixes (5-digit zero-padded ids per D-32), set the matching flags on the entry for that id (strtol on the digit slice, id range 1..0xFFFF); sort descending by id once after the walk. finalizeImage() (both kinds) bumps a module-level indexVersion counter.

  3. Lazy refresh + accessors: getIndexVersion()/getIndexedVersion(); ensureIndexCurrent() rebuilds when versions differ (called at the top of the gallery handlers — bounded work, boot-warm). getIndexPage(uint16_t pageIdx, SdGalleryEntry* dst, uint8_t max) returns entries [(pageIdx-1)*12 .. pageIdx*12-1] of the descending index (12 per page, D-46); getPageCount() = ceil(total/12); getTotalCount().

  4. readSidecarMeta(uint16_t id, bool thumb, SdImageMetadata* out): read IMG_{id}.JSON or IMG_{id}_T.JSON and parse the hand-built JSON with bounded scanning (indexOf + substring into fixed buffers with clamped lengths, numeric clamps) — every value untrusted per the security domain (the SD is removable). Expose the D-30 fields: captureTimeMs, triggerSource, altitudeM, lat, lon, gpsValid, camera settings, chunksReceived/chunksTotal, complete, storedToSd.

  5. Routes (src/main_basestation.cpp): server.on("/gallery", HTTP_GET, handleGalleryList) — arg "page" parsed numeric with WR-07 discipline (default 1; values < 1 or > pageCount rejected 400); responds hand-built String JSON {page, pageCount, total, entries:[{id, hasThumb, hasFull, complete}]} where complete derives from the full sidecar's complete flag (an entry with only thumbnail data is not complete). handleNotFound gains the /gallery/{id} prefix dispatch (strictly-numeric id parsing copied from handleImage, range 1..0xFFFF): reads the full sidecar when present (falling back to the thumbnail sidecar), responds {id, complete, capturedMs, trigger, altitudeM, lat, lon, gpsValid, camera:{...}, chunksReceived, chunksTotal, percent, hasFull} — ONLY fields present in the sidecar; absent fields omitted, never zero-filled; 404 when neither sidecar nor any file exists for the id. handleApiState gains "galleryCount":total so the client knows when to refresh the list (D-36).

  6. SD-unavailable degradation: when SdStorage reports unavailable, /gallery returns the empty listing {page:1, pageCount:1, total:0, entries:[]} — honest empty, never an error page (the storage chip already surfaces UNAVAILABLE in the panel).
  </action>
  <verify>
  <automated>pio run -e esp32-s3-basestation && pio run -e esp32-s3-balloon && grep -q "getIndexPage" include/sd_storage.h && grep -q "openNextFile" src/sd_storage.cpp && grep -q '"/gallery"' src/main_basestation.cpp && grep -q "galleryCount" src/main_basestation.cpp && grep -q "readSidecarMeta" src/sd_storage.h && node scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - include/sd_storage.h defines SdGalleryEntry, SD_GALLERY_MAX_ENTRIES, getIndexPage/getPageCount/getTotalCount, readSidecarMeta, and the version-counter API; the index is a fixed static array sorted descending by id
  - Enumeration runs once at boot and rebuilds only when finalizeImage's version counter advances — no per-request directory rescan
  - GET /gallery validates the page arg (numeric, 1..pageCount) and slices exactly entries [(N-1)*12 .. N*12-1]; a seam id never duplicates or drops
  - GET /gallery/{id} parses the id strictly-numerically (handleImage discipline) and returns only sidecar-present fields; unknown ids 404
  - Sidecar parsing treats every value as untrusted — bounded copies, numeric clamps, no unbounded string handling
  - /api/state carries galleryCount; SD-unavailable serves the honest empty listing
  - Both firmware targets build SUCCESS; the wire harness still exits 0
  </acceptance_criteria>
  <done>The SD card's flight history is enumerable, paginated, and detail-queryable through two honest routes — the data half of IMG-06 over the D-29..D-32 contract.</done>
</task>

<task type="auto">
  <name>Task 2: Gallery UI — thumbnail grid, pager, incomplete badge, inline detail view; retire the Latest Capture card (D-46/D-47/D-48)</name>
  <read_first>
  - src/main_basestation.cpp (after Task 1: /gallery + /gallery/{id} JSON shapes, the D-45 section order with the interim Latest Capture card at the bottom, the poll diff-render idiom and galleryCount from /api/state, existing /img/{id} and /img/{id}_t.jpg routes)
  - .planning/phases/03-enhanced-web-interface/03-UI-SPEC.md (component 5 gallery spec: grid/pager/detail copy + structure; Color: incomplete badge amber pairing; Copywriting: gallery empty, badge, pager, detail metadata labels)
  </read_first>
  <files>src/main_basestation.cpp</files>
  <action>
  Build the gallery section at the bottom of the D-45 order and absorb the interim thumbnail surface:

  1. 🖼 Image Gallery section (last section; "Gallery" nav link added to .section-nav): a .gallery-grid of CSS grid repeat(auto-fill, minmax(200px, 1fr)) with a 16px gap (3 columns at the 752px content width). Each .gallery-item is a thumbnail img loading /img/{id}_t.jpg (width 100%, radius 8px) with the amber Incomplete chip overlaid top-right when the entry's complete flag is false (chip pairing #eab308/#713f12/#fbbf24, 2px vertical padding). Filenames never render on grid items. Empty state: the .message.info copy "No images received yet — trigger a capture to start." with the pager hidden.

  2. .pager below the grid: Previous / Next standard buttons with "Page {n} of {m}" Body text between them plus numbered page buttons styled as standard buttons, centered. A single page hides the pager entirely. Clicking a page or Previous/Next fetches /gallery?page=N and re-renders the grid; the previous grid stays visible until the new page arrives (E4 loading).

  3. Detail view (D-47): clicking a grid item replaces the grid (within the gallery section) with an inline .card — no modal. Content: the full-size image at max-width 100% radius 8px from /img/{id} when the sidecar says complete, otherwise the thumbnail /img/{id}_t.jpg with the Incomplete badge still visible; a definition list of the sidecar fields with labels "Captured · Trigger · Altitude · Position · Camera · Chunks {n}/{m} · {percent}% · Status: Complete/Incomplete" — rendered ONLY for fields present in the detail JSON, values wrapping (14px values #e2e8f0, 14px muted labels), with "Position: GPS no fix" when the capture-time fix was invalid; and the Back to Gallery standard button restoring the grid without a refetch unless galleryCount changed.

  4. List refresh discipline (D-36): the poll watches /api/state galleryCount; only when it changes does the client refetch the current /gallery page (and page count). Individual tile images keep browser caching; nothing refetches per poll.

  5. Retirement: remove the interim thumbnail card that 03-01 kept as the transitional latest-image surface (its markup, its styles, and the poll script's latest-thumb diff-gate code) — the gallery's newest grid item (first item of page 1) is now the latest-capture surface. Remove the card heading text along with the markup so no orphaned label survives in the served page; the /img routes remain (the gallery uses them).
  </action>
  <verify>
  <automated>pio run -e esp32-s3-basestation && grep -q "gallery-grid" src/main_basestation.cpp && grep -q "minmax(200px" src/main_basestation.cpp && grep -q "Back to Gallery" src/main_basestation.cpp && grep -q "No images received yet" src/main_basestation.cpp && grep -q "Incomplete" src/main_basestation.cpp && grep -q "galleryCount" src/main_basestation.cpp && ! grep -q "Latest Capture" src/main_basestation.cpp</automated>
  </verify>
  <acceptance_criteria>
  - The gallery section renders under Queue/Transfers with the Gallery nav link; the grid is auto-fill minmax(200px,1fr) with a 16px gap
  - Entries render newest-first, 12 per page, thumbnails via /img/{id}_t.jpg; incomplete entries carry the amber badge in grid AND detail
  - The pager shows Previous/Next + "Page {n} of {m}" + numbered buttons, hides on a single page, and the empty state shows the locked copy with no pager
  - The detail card renders inline (no modal) with full image when complete (thumbnail otherwise), only sidecar-present fields, GPS-no-fix position copy, and Back to Gallery
  - The client refetches the gallery list only when /api/state galleryCount changes — never per poll
  - The interim Latest Capture card is removed from the served page (gate: the literal is gone) while the /img routes remain in use
  - Base target builds SUCCESS
  </acceptance_criteria>
  <done>The operator browses every received image of the flight, pages through history, opens sidecar-rich detail views, and sees honestly badged incomplete images — IMG-06 delivered (D-46/D-47/D-48).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SD medium -> base firmware | Sidecar JSON was written by this firmware but the card is removable — every parsed value is untrusted input |
| Browser -> base HTTP | /gallery?page and /gallery/{id} accept LAN client input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Tampering | sidecar parsing (removable SD) | high | mitigate | Bounded substring copies into fixed buffers, numeric clamps on every parsed number, absent-field omission — a hostile sidecar cannot overflow or inject through the parser (research Security Domain) |
| T-03-10 | Tampering | path construction from gallery ids | high | mitigate | Ids parsed strictly-numerically (strtol + per-char isDigit + 1..0xFFFF range, the handleImage pattern); file names built only from %05u-formatted ids — no network string reaches a path |
| T-03-11 | DoS | SD enumeration latency on the 5s cycle | medium | mitigate | Boot-built RAM index + lazy rebuild only on finalize-version change; pagination slices the index, never the directory (Pitfall 5) |
| T-03-11b | Tampering | detail strings into the DOM | high | mitigate | textContent-only rendering of every sidecar-derived value (standing UI rule) |
</threat_model>

<verification>
- pio run -e esp32-s3-balloon -e esp32-s3-basestation SUCCESS; node scripts/verify_protocol_roundtrip.mjs exits 0
- All task acceptance-criteria greps pass (enumeration API, routes, grid/pager/detail tokens, galleryCount, Latest Capture retirement)
- Hardware UAT (deferred): after a flight with completes AND an induced incomplete, the SD card's full listing appears paginated with badges; detail fields match the sidecar contents exactly; page seams show no gaps or duplicates
</verification>

<success_criteria>
- All received images display with pagination (IMG-06, D-46)
- Detail views expose the D-30 sidecar metadata honestly, incompletes badged (D-47/D-48)
- Gallery refresh is count-driven (D-36) and the interim card is absorbed
</success_criteria>

## Artifacts this phase produces (plan 03-04)

- SdStorage additions: SdGalleryEntry, SD_GALLERY_MAX_ENTRIES, buildIndex/ensureIndexCurrent/getIndexPage/getPageCount/getTotalCount/getIndexVersion, readSidecarMeta, finalize-time indexVersion bump
- HTTP: GET /gallery?page=N (list JSON), GET /gallery/{id} (detail JSON via handleNotFound dispatch)
- /api/state addition: galleryCount
- Browser: 🖼 gallery grid + .pager + inline detail card + Incomplete badge + Gallery nav link; retirement of the interim Latest Capture card

<output>
Create `.planning/phases/03-enhanced-web-interface/03-04-SUMMARY.md` when done
</output>
