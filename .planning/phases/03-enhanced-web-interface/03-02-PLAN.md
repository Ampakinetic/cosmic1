---
phase: 03-enhanced-web-interface
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/trajectory_buffer.h
  - src/trajectory_buffer.cpp
  - scripts/embed_web_assets.mjs
  - src/web_assets.h
  - src/main_basestation.cpp
  - platformio.ini
autonomous: true
requirements: [WEB-02]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A capped trajectory ring buffer on the base records every valid GPS fix from the received 0x14 beacons (one point per beacon seq, wraparound-safe, internal RAM, default cap 500 points as TRAJ_MAX_POINTS) and /api/state carries it as a compact array-of-arrays [[lat,lon,altM],...] in chronological order (D-38)"
    - "The map renders the balloon position as an L.circleMarker plus the flight track as altitude-banded L.polyline segments — green #22c55e below 1000 m, amber #eab308 1000-3000 m, red #ef4444 above 3000 m — with the required OpenStreetMap contributors attribution (WEB-02, D-37)"
    - "When OSM tiles fail (AP mode / no uplink) the same .map-frame switches to the auto-scaled offline canvas plot rendered from the identical trajectory data — a render-path swap, never a data change — with the offline chip visible while active (D-37)"
    - "The map auto-follows the balloon each update; user drag/zoom cancels following and shows the Recenter overlay chip, which resumes following on click (D-39)"
    - "Leaflet 1.9.4 JS/CSS load from firmware-served gzipped PROGMEM assets (GET /leaflet.js and /leaflet.css with Content-Encoding: gzip + Cache-Control), generated by scripts/embed_web_assets.mjs pinned to leaflet@1.9.4 from leafletjs.com or unpkg with a committed provenance comment — no CDN reference at runtime (CONTEXT discretion: the page must work in AP mode)"
    - "Map (E3) empty: before the first valid GPS fix the frame shows the waiting message and renders no position; long-text: attribution and chip text wrap within the frame at 14px/1.5"
    - "Map (E3) loading: the frame shows the waiting message until data exists; tiles fill per Leaflet default after the first successful poll; no spinners"
    - "Map (E3) error: tile failure switches to the canvas fallback of the identical track plus the offline chip — a degraded mode, not an error state"
    - "Map (E3) populated: full trajectory with banded track and current-position circleMarker (#22c55e fill, #0f172a stroke); partial: 1 point renders a marker only, no polyline — whatever points exist render"
    - "Map (E3) overflow: map bounds contain the track and Recenter restores auto-follow after drag; zero-one-many: identical render path at 0/1/many points (0 = waiting message, 1 = marker only, many = banded polyline)"
    - statement: "The canvas fallback and the Leaflet view render the identical trajectory data with the identical altitude banding — verified visually by forcing tile failure (AP mode) with a populated track"
      verification: backstop
  artifacts:
    - path: "src/trajectory_buffer.h"
      provides: "TrajectoryBuffer manager + Trajectory() singleton: TRAJ_MAX_POINTS ring, TrajectoryPoint {latE6, lonE6, altM}, begin/process/getSnapshot"
      contains: "TRAJ_MAX_POINTS"
    - path: "src/web_assets.h"
      provides: "generated gzipped Leaflet JS/CSS PROGMEM arrays (LEAFLET_JS_GZ/LEAFLET_CSS_GZ + _LEN) with provenance comment"
      contains: "LEAFLET_JS_GZ"
    - path: "scripts/embed_web_assets.mjs"
      provides: "build-time dist -> gzip -> C-header generator (committed, rerunnable)"
      contains: "gzip"
    - path: "src/main_basestation.cpp"
      provides: "/api/state traj[] + trajCount, /leaflet.js + /leaflet.css routes, map section with Leaflet render + canvas fallback + follow/recenter"
      contains: "circleMarker"
  key_links:
    - from: "src/trajectory_buffer.cpp process()"
      to: "include/image_rx_manager.h getTelemetrySnapshot"
      via: "polls the beacon snapshot and appends one point when seq advances with gpsValid — pull-based, no image_rx_manager changes"
      pattern: "getTelemetrySnapshot"
    - from: "src/main_basestation.cpp map renderers (Leaflet + canvas)"
      to: "/api/state traj[]"
      via: "both render paths consume the identical traj array from the single poll payload — the fallback is a render path, not a data path"
      pattern: "traj"
    - from: "scripts/embed_web_assets.mjs"
      to: "src/web_assets.h"
      via: "one-way generation: vendored dist -> gzip -> PROGMEM header, committed with provenance; firmware never fetches the library"
      pattern: "LEAFLET"
  prohibitions:
    - statement: "MUST NOT render a current balloon position before the first valid GPS fix or while gpsValid is false — the map shows the waiting message, never a guessed or stale position presented as current (the historical track may legitimately show past fixes)"
      status: unverified
      flagged: true
      verification: judgment
  assumptions:
    - "Probe edge WEB-02 unclassified/unresolved — flagged assumption: OSM standard tiles tolerate the light single-operator volume in STA mode within published policy (research A3); if tiles are blocked, the D-37 canvas fallback is the designed-in answer — verify in STA-mode UAT"
---

<objective>
Map and trajectory: a capped base-side ring buffer records the flight's GPS fixes into /api/state, and the map section renders them with embedded Leaflet over OSM tiles, altitude-banded track, auto-follow/recenter, and the D-37 offline canvas fallback.

Purpose: WEB-02 is the dashboard's informational anchor — the whole flight always visible, spatially usable in the field (AP mode, no internet) because both the library and the fallback render offline.
Output: trajectory_buffer module, embedded Leaflet assets + generator script, the map section with two render paths from one data path.
</objective>

<execution_context>
@C:/Users/Amp/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Amp/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-enhanced-web-interface/03-CONTEXT.md
@.planning/phases/03-enhanced-web-interface/03-RESEARCH.md
@.planning/phases/03-enhanced-web-interface/03-PATTERNS.md
@.planning/phases/03-enhanced-web-interface/03-UI-SPEC.md
@.planning/phases/03-enhanced-web-interface/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trajectory ring buffer module + compact /api/state trajectory array (D-38)</name>
  <read_first>
  - src/sd_storage.h (manager/singleton skeleton to copy: class shape ~73-84, extern accessor ~147-152)
  - include/image_rx_manager.h (TelemetrySnapshot ~105-114 — seq, lat, lon, altitudeM, gpsValid, receivedMs; getTelemetrySnapshot)
  - src/main_basestation.cpp (after 03-01: handleApiState, setup() init sequence ~590-594, loop ~606-638 millis-timer slots, the existing telemetry JSON block)
  - .planning/phases/03-enhanced-web-interface/03-RESEARCH.md (Pattern 1 payload-budget discipline; Pitfall 4 bloat math)
  - .planning/phases/03-enhanced-web-interface/03-PATTERNS.md (trajectory_buffer section; No Analog Found ring-buffer note)
  </read_first>
  <files>src/trajectory_buffer.h, src/trajectory_buffer.cpp, src/main_basestation.cpp, platformio.ini</files>
  <action>
  Create the base-side trajectory module following the manager/singleton conventions (header beside implementation in src/, no heap per point — the base env has no PSRAM, budget internal RAM):

  1. src/trajectory_buffer.h/.cpp: class TrajectoryBuffer with begin() (no-op init, log banner like other modules) and process(); singleton accessor `extern TrajectoryBuffer& Trajectory();` following the SDStorage() pattern. TRAJ_MAX_POINTS = 500 (constant in the header, editable; within the CONTEXT discretion range 500-1000 and the measured payload budget below). Fixed static array of TrajectoryPoint { int32_t latE6; int32_t lonE6; int32_t altM; } (~12 B/pt = ~6 KB) with head/count indices — overwrite-oldest on wrap, wraparound-safe arithmetic. process(): poll ImageRx().getTelemetrySnapshot(); when snapshot.valid && snapshot.gpsValid && snapshot.seq != lastRecordedSeq, append (lroundf(lat*1e6), lroundf(lon*1e6), lroundf(altitudeM)) and set lastRecordedSeq. Exactly one point per beacon seq — a duplicate snapshot never double-appends.

  2. src/main_basestation.cpp: Trajectory().begin() in setup() beside the other managers; Trajectory().process() in loop(). handleApiState gains the compact trajectory encoding after the telemetry block: "trajCount":N plus "traj":[[lat,lon,alt],...] in chronological order (oldest first) using String(value, 6) fixed precision for lat/lon and integer meters for altitude — array-of-arrays, not verbose objects (Pattern 1 budget: ~22-28 B/pt keeps 500 pts near 14 KB). reserve() growth already handled by the Task-1-from-03-01 reserve. While measuring, log (Serial, gated debug) the built JSON length once at the first full 500-point payload; if the measurement exceeds ~16 KB, keep the 500 cap (do not grow to 1000) and note the measurement in the SUMMARY.

  3. platformio.ini: add -<trajectory_buffer.cpp> to the balloon env's build_src_filter (base-only module, the sd_storage exclusion discipline).
  </action>
  <verify>
  <automated>pio run -e esp32-s3-balloon -e esp32-s3-basestation && grep -q "TRAJ_MAX_POINTS" src/trajectory_buffer.h && grep -q "Trajectory()" src/trajectory_buffer.h && grep -q "getTelemetrySnapshot" src/trajectory_buffer.cpp && grep -q "traj" src/main_basestation.cpp && grep -q "trajectory_buffer.cpp" platformio.ini && node scripts/verify_protocol_roundtrip.mjs</automated>
  </verify>
  <acceptance_criteria>
  - src/trajectory_buffer.h defines TRAJ_MAX_POINTS 500, TrajectoryPoint, and the Trajectory() singleton accessor; the ring is a fixed static array with head/count indices (no per-point allocation)
  - process() appends only when the snapshot's seq advances AND gpsValid is true — one point per beacon; invalid fixes never append
  - handleApiState emits "traj" as array-of-arrays [[lat,lon,alt],...] oldest-first plus "trajCount", built into the reserved String
  - The balloon env excludes trajectory_buffer.cpp; both firmware targets build SUCCESS; the wire harness still exits 0
  - The SUMMARY records the measured full-track /api/state payload length (the D-38 discretion measurement)
  </acceptance_criteria>
  <done>The whole flight's GPS track accumulates on the base in bounded memory and ships in every poll as a compact array — the single data path both map render paths consume (D-38).</done>
</task>

<task type="auto">
  <name>Task 2: Embedded Leaflet assets + map section — banded track, follow/recenter, offline canvas fallback (D-37, D-39)</name>
  <read_first>
  - .planning/phases/03-enhanced-web-interface/03-RESEARCH.md (Pattern 2 gzip asset serving with the existing binary-serve shape at main_basestation.cpp:1544-1548; Pattern 3 degradation ladder; Pitfall 8 marker assets; Registry Safety provenance lock)
  - .planning/phases/03-enhanced-web-interface/03-UI-SPEC.md (component 4 map-frame spec, Color trajectory banding, Copywriting map empty/offline/attribution)
  - src/main_basestation.cpp (after Task 1: .map-frame placeholder + waiting message, the binary-serve shape ~1544-1548, the poll script's dataset-gate idiom, <head> of the served page)
  - .planning/codebase/STACK.md (node v24 available for the generator script)
  </read_first>
  <files>scripts/embed_web_assets.mjs, src/web_assets.h, src/main_basestation.cpp</files>
  <action>
  Vendor Leaflet and build the map section:

  1. scripts/embed_web_assets.mjs (new, committed): a node script that (a) reads leaflet.js and leaflet.css from a local vendor/ directory — the executor downloads them ONCE from https://unpkg.com/leaflet@1.9.4/dist/leaflet.js and leaflet.css (or leafletjs.com; never a mirror), recording the sha256 of each download; (b) gzips both at max level via node:zlib; (c) writes src/web_assets.h containing PROGMEM byte arrays LEAFLET_JS_GZ / LEAFLET_CSS_GZ with LEAFLET_JS_GZ_LEN / LEAFLET_CSS_GZ_LEN constants and a provenance comment block: pinned source URL, sha256, retrieval date, and the BSD 2-Clause license notice (confirm the license text on the download page while vendoring). Idempotent: rerunning regenerates the identical header.

  2. src/main_basestation.cpp: include web_assets.h; register GET /leaflet.js and GET /leaflet.css serving the gzipped arrays with the binary-serve shape plus server.sendHeader("Content-Encoding", "gzip") and Cache-Control public, max-age=86400. The served page <head> links /leaflet.css and loads /leaflet.js before the app script — no external URL appears anywhere in the served HTML.

  3. Map section (inside the existing .map-frame): initialize Leaflet on load into the frame div with the standard OSM tile layer and the default attribution control (must retain "© OpenStreetMap contributors"; attribution + zoom-control text restyled to Body 14px on #1e293b surfaces — the explicit harmonization obligation). Render from the poll payload: current position as L.circleMarker (#22c55e fill, #0f172a stroke) and the track as L.polyline segments split by altitude band — #22c55e below 1000 m, #eab308 1000-3000 m, #ef4444 above 3000 m. Use circleMarker + polyline ONLY (no L.marker image assets — Pitfall 8). Track re-renders only when trajCount changes (D-36 diff discipline); the position marker updates every poll. 1 point renders a marker only, no polyline.

  4. Auto-follow (D-39): each update calls setView on the newest position while follow == true. Leaflet user-interaction events (dragstart, zoomstart from user input) set follow = false and reveal the Recenter overlay chip (accent-gradient button, 8px inset inside the frame); clicking it sets follow = true and recenters. 

  5. Offline fallback (D-37): detect tile failure via Leaflet 'tileerror' events plus a first-load timeout (~8 s with zero successful tiles). On failure: hide the Leaflet container and render the identical traj array onto a <canvas> inside the same frame — auto-scaled to fit the track, altitude-banded polyline in the same three colors, current-position dot, grid lines #475569, coordinate labels #94a3b8 at 14px — and show the offline chip "Offline — tiles unavailable, showing plotted track." while the fallback is active. A later successful tile load restores the Leaflet path and hides the chip. Before the first valid GPS fix, the frame shows the 03-01 waiting message and renders nothing.
  </action>
  <verify>
  <automated>node scripts/embed_web_assets.mjs && grep -q "LEAFLET_JS_GZ" src/web_assets.h && grep -q "leaflet@1.9.4" src/web_assets.h && pio run -e esp32-s3-basestation && grep -q "circleMarker" src/main_basestation.cpp && grep -q "polyline" src/main_basestation.cpp && grep -q "tileerror" src/main_basestation.cpp && grep -q "Recenter" src/main_basestation.cpp && grep -q "OpenStreetMap contributors" src/main_basestation.cpp && ! grep -qi 'src="http' src/main_basestation.cpp</automated>
  </verify>
  <acceptance_criteria>
  - scripts/embed_web_assets.mjs regenerates src/web_assets.h deterministically; the header carries LEAFLET_JS_GZ/LEAFLET_CSS_GZ arrays, length constants, and a provenance comment naming the pinned leaflet@1.9.4 URL, sha256, date, and BSD 2-Clause notice
  - /leaflet.js and /leaflet.css serve the gzipped PROGMEM arrays with Content-Encoding: gzip and Cache-Control headers; the page loads the library from these routes only (gate: no script/link src pointing at an external http URL in the served HTML)
  - The track renders as banded polylelines with the current-position circleMarker; the attribution retains "© OpenStreetMap contributors"; no L.marker/image-icon usage exists
  - Drag/zoom cancels auto-follow and shows the Recenter chip; clicking it resumes following and recenters
  - Tile failure (tileerror events or the first-load timeout) swaps to the canvas fallback inside the same frame from the identical traj data, shows the offline chip, and restores Leaflet on later tile success
  - No valid GPS fix renders nothing but the waiting message
  - Base target builds SUCCESS
  </acceptance_criteria>
  <done>The operator sees the whole flight track on a real map in STA mode and an equivalent plotted track offline in AP mode, with follow/recenter behavior — WEB-02 delivered end-to-end on one data path (D-37/D-38/D-39).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> OSM tile servers | Tiles are fetched by the browser (not the firmware) with normal Referer/User-Agent headers |
| Browser -> base HTTP | New /leaflet.js + /leaflet.css routes serve firmware-embedded assets to the LAN |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | vendored Leaflet 1.9.4 dist (web-asset supply chain) | high | mitigate | Provenance lock: download ONLY from leafletjs.com or unpkg pinned to leaflet@1.9.4 (never a mirror); sha256 recorded in the generated header's provenance comment; committed artifact is reviewable; no runtime CDN reference (Registry Safety) |
| T-03-04 | DoS | /api/state trajectory payload stalls the single-threaded server | medium | mitigate | Compact array-of-arrays encoding, 500-point cap, reserved String, measured payload logged at first full track (Pitfall 4) |
| T-03-05 | Information disclosure | firmware never proxies external requests | low | accept | The ESP32 makes zero outbound requests for the map; only the operator's browser fetches tiles — nothing to exfiltrate through |
</threat_model>

<verification>
- node scripts/embed_web_assets.mjs runs clean and src/web_assets.h regenerates identically (committed)
- pio run -e esp32-s3-balloon -e esp32-s3-basestation SUCCESS; node scripts/verify_protocol_roundtrip.mjs exits 0
- All task acceptance-criteria greps pass (module constants, singleton, traj serialization, Leaflet routes, renderer markers, tileerror, Recenter, attribution, no external script src)
- Hardware UAT (deferred): STA mode shows tiled OSM map with a populated track; AP mode (or blocked tiles) shows the canvas fallback with the same track and banding; drag/zoom + Recenter behave (see the cross-render parity backstop truth)
</verification>

<success_criteria>
- Full-flight trajectory recorded on the base in bounded memory and served in the 5s poll (D-38)
- Map renders online (Leaflet/OSM) and offline (canvas) from one data path with identical banding (WEB-02, D-37)
- Auto-follow with user-override and recenter (D-39)
- Leaflet embedded — the page fully loads with no internet (discretion item honored)
</success_criteria>

## Artifacts this phase produces (plan 03-02)

- Module Trajectory() (src/trajectory_buffer.h/.cpp): TRAJ_MAX_POINTS 500 ring, TrajectoryPoint {latE6, lonE6, altM}, begin/process/getSnapshot
- scripts/embed_web_assets.mjs — vendor dist -> gzip -> PROGMEM header generator (committed tool)
- src/web_assets.h — LEAFLET_JS_GZ / LEAFLET_CSS_GZ arrays + _LEN constants + provenance block (generated, committed)
- HTTP: GET /leaflet.js, GET /leaflet.css (gzip + cache headers)
- /api/state additions: traj[] (array-of-arrays, oldest first), trajCount
- Browser: map section with Leaflet render path, canvas fallback renderer, follow/recenter controller, offline chip

<output>
Create `.planning/phases/03-enhanced-web-interface/03-02-SUMMARY.md` when done
</output>
