# Name,   Type,  SubType, Offset,   Size,    Flags
# G-01-10 (session-30, debug doc section 33): nvs REVERTED to 0x9000 - test E.
# The session-28 relocation to 0xF00000 did not stop the write deaths
# (balloon25/26 card-out, balloon27 card-in+claimed), and the verified
# survivor matrix now reads: (old, card-in)=survived the whole SD era;
# (old, card-out), (new, card-out), (new, card-in) all die. Test E
# (old + card-in + current code) isolates the LOCATION factor: survive ->
# the high-flash region is the trigger (permanent config = this table);
# die -> location exonerated, the 08-30+ code deltas are bisected next.
# Balloon25-27's ID restarts (1..8) are already on the base's card - a
# reissue collision with those bench rows is accepted for the bench.
nvs,      data,  nvs,     0x9000,   0x5000,
otadata,  data,  ota,     0xe000,   0x2000,
app0,     app,   ota_0,   0x10000,  0x3c0000,
fr,       data,        ,  0x3d0000, 0x20000,
coredump, data,  coredump,0x3f0000, 0x10000,
