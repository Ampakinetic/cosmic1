# Name,   Type,  SubType, Offset,   Size,    Flags
# G-01-10 (session-28, balloon25 verdict): nvs RELOCATED out of the 0x9000
# region (24 bench sessions of per-capture churn put the 20 KB partition into
# per-write garbage-collection, whose 4 KB sector erase loses the 300 ms
# INT-WDT race - see debug doc section 31) and GROWN 4x into fresh flash at
# 0xF00000. Consequence, accepted and recorded: reflashing with this table
# wipes NVS content - the image-ID sequence restarts at 1 (the base re-issues
# filenames for re-captured IDs; bench-only exposure, G-01-6 re-read) and the
# base's stored WiFi credentials reset (re-enter after flashing).
otadata,  data,  ota,     0xe000,   0x2000,
app0,     app,   ota_0,   0x10000,  0x3c0000,
fr,       data,        ,  0x3d0000, 0x20000,
coredump, data,  coredump,0x3f0000, 0x10000,
nvs,      data,  nvs,     0xf00000, 0x10000,
